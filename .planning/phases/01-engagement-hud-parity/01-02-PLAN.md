---
phase: 01-engagement-hud-parity
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx
  - src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx
autonomous: true
requirements: [HUD-01, HUD-03, I18N-01]

must_haves:
  truths:
    - "Player sees a live ComboPill in the game HUD that increments on each correct note, live and note-by-note (HUD-01, D-04)"
    - "Combo resets the instant a note's timing window closes on a miss, via the existing RAF tick() miss branch (HUD-01, D-04)"
    - "OnFireBadge + OnFireSplash appear when combo crosses 5; reduced-motion is honored by the self-contained shared components, no reduced-motion prop passed (HUD-03, D-08)"
    - "OnFireSplash renders at the game-tree root (fixed inset-0), not inside SightReadingLayout (D-08)"
    - "Combo/on-fire accessible labels reuse existing games.engagement.combo / games.engagement.onFire keys — EN+HE parity with no new keys (I18N-01, D-07)"
    - "The existing gentle encouragement / loss path is untouched — no GameOverScreen, no LivesDisplay added (D-01, D-03)"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "combo/on-fire HUD wiring + render"
      contains: "OnFireSplash"
    - path: "src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx"
      provides: "integration coverage for live increment/reset + on-fire render"
  key_links:
    - from: "SightReadingGame.jsx handleNoteDetected correct branch (~line 1858)"
      to: "incrementCombo()"
      via: "call after recordPerformanceResult(result)"
      pattern: "incrementCombo\\(\\)"
    - from: "SightReadingGame.jsx schedulePerformanceTimeline tick() miss branch (~line 2135)"
      to: "resetCombo()"
      via: "call after recordPerformanceResult(missed)"
      pattern: "resetCombo\\(\\)"
    - from: "context isOnFire false->true"
      to: "OnFireSplash show"
      via: "local showFireSplash useEffect"
      pattern: "setShowFireSplash"
---

<objective>
Wire the session-owned combo/on-fire state (from Plan 01) into `SightReadingGame.jsx`: call
`incrementCombo()` at the single correct-note record site and `resetCombo()` at the single miss-record
site inside the existing RAF timeline; render the shared `ComboPill`/`OnFireBadge` in the HUD header and
`OnFireSplash` at the game-tree root; trigger a one-shot splash on the `false -> true` on-fire
transition. All strings reuse the existing `games.engagement.*` keys (D-07). No new mechanics, no lives,
no game-over — the encouragement screen stays (D-01/D-03).

Purpose: Delivers the visible, live combo + on-fire feedback loop that is the whole point of the phase
(HUD-01, HUD-03), reusing shared v3.6 HUD components verbatim.
Output: Sight-reading players get combo/on-fire parity with sibling games, tested by a new integration
suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engagement-hud-parity/01-CONTEXT.md
@.planning/phases/01-engagement-hud-parity/01-RESEARCH.md
@.planning/phases/01-engagement-hud-parity/01-PATTERNS.md
@.planning/phases/01-engagement-hud-parity/01-VALIDATION.md
@.planning/phases/01-engagement-hud-parity/01-01-SUMMARY.md

<interfaces>
<!-- Shared HUD component contracts (VERIFIED). Import, do not fork. Do NOT pass a reduced-motion prop. -->
ComboPill:    ComboPill({ combo, isOnFire = false })   // src/components/games/shared/hud/ComboPill.jsx — self-animates, self-tints tiers at 3/8
OnFireBadge:  OnFireBadge({ active })                  // src/components/games/shared/hud/OnFireBadge.jsx — dual-source reduced-motion internal
OnFireSplash: OnFireSplash({ show })                   // src/components/games/shared/hud/OnFireSplash.jsx — renders fixed inset-0 z-[70]; MUST be a root-level sibling

<!-- Context fields added by Plan 01 (now available on useSightReadingSession()): -->

combo: number, isOnFire: boolean, incrementCombo: () => void, resetCombo: () => void

<!-- VERIFIED exact integration line numbers in SightReadingGame.jsx (current): -->

- imports of sibling HUD components: lines 56-57 (`../shared/hud/ProgressBar`, `../shared/hud/ScorePill`)
- useSightReadingSession() destructure block: lines 222-235 (closes at `} = useSightReadingSession();`)
- correct-note record site: line 1858 `recordPerformanceResult(result);` (inside handleNoteDetected useCallback)
- handleNoteDetected deps array: lines 1941-1949 (contains recordPerformanceResult already)
- miss record site: line 2135 `recordPerformanceResult(missed);` (inside schedulePerformanceTimeline's tick())
- schedulePerformanceTimeline is a useCallback; its deps array is lines 2181-2186: [completePerformance, getElapsedMsFromPerformanceStart, inputMode, recordPerformanceResult]
- HUD header "Right Controls" flex container: line 3610; ScorePill rendered at 3612-3615
- top-level return fragment: line 3811 (`return (<>`); SightReadingLayout wrapper `</div>` closes at line 3839

<!-- i18n keys (VERIFIED present in both locales at lines 685-693): -->

games.engagement.combo -> EN "Combo" / HE "קומבו"
games.engagement.onFire -> EN "ON FIRE!" / HE "מדהים!"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write combo integration test + extend the shared session mock (RED)</name>
  <files>src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx, src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx</files>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx (the analog — exact mocking conventions: mocked useSightReadingSession object at lines 61-78, mocked KlavierKeyboard, vi.useFakeTimers()/vi.advanceTimersByTimeAsync() to drive schedulePerformanceTimeline)
    - src/components/games/sight-reading-game/SightReadingGame.jsx (destructure block 222-235, header render, root fragment — to know what the test can assert on)
    - .planning/phases/01-engagement-hud-parity/01-RESEARCH.md ("Validation Architecture") and 01-PATTERNS.md (the SightReadingGame.combo.test.jsx section)
  </read_first>
  <action>
    A. Extend the existing `useSightReadingSession` mock in
       `src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx` (the object at lines
       61-78) by adding these four fields so the component destructures real values, not `undefined`:
    ```javascript
    combo: 0,
    isOnFire: false,
    incrementCombo: vi.fn(),
    resetCombo: vi.fn(),
    ```
       Change nothing else in that file — its mic-restart assertions must stay green.

    B. Create `src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx`, modeled on the
       micRestart test's mocking setup (reuse the same module mocks for audio/pattern/keyboard). Make the
       `useSightReadingSession` mock STATEFUL so the render reflects live combo, using one of the two
       proven approaches documented in RESEARCH/PATTERNS:
       - back the mock with a real `useState`-driven combo inside a wrapper, whose `incrementCombo`/
         `resetCombo` mutate that state; OR
       - assert on the captured `incrementCombo`/`resetCombo` `vi.fn()` call counts/timing.

       Write these cases (RED now — the game does not yet call the hooks or render the pills):
       1. `"increments combo on a correct note"` — simulate a correct note (drive the mocked
          KlavierKeyboard `onNotePlayed`, or capture useMicNoteInput's `onNoteEvent`) → `incrementCombo`
          is called (or rendered combo increases).
       2. `"resets on miss when the timing window closes"` — start performance, advance fake timers past
          the note window (`vi.advanceTimersByTimeAsync(...)`) so `schedulePerformanceTimeline`'s tick()
          records `missed` → `resetCombo` is called.
       3. `"persists across exercises"` — combo value is not zeroed by `goToNextExercise` (mock
          goToNextExercise does not reset the mock combo) — assert combo survives an exercise advance.
       4. `"on-fire renders ComboPill + OnFireBadge at combo >= 5"` — with the mock combo at 5 and
          isOnFire true, `ComboPill` and `OnFireBadge` are in the DOM and `OnFireSplash` shows.

       Follow the micRestart file's `vi.useFakeTimers()` lifecycle (setup/teardown) exactly.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "incrementCombo: vi.fn()" src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx` and `grep -q "resetCombo: vi.fn()"` both succeed; `grep -q "combo: 0" ...micRestart.test.jsx` succeeds.
    - `SightReadingGame.micRestart.test.jsx` still PASSES (the mock extension is additive).
    - File `SightReadingGame.combo.test.jsx` exists; `grep -c "it(\|test(" ...combo.test.jsx` >= 4.
    - Running the command executes both suites without import/parse errors; the new combo suite is RED
      (its assertions fail because the game isn't wired yet).
  </acceptance_criteria>
  <done>micRestart mock extended (still green); new combo integration suite exists with 4 RED cases targeting the unwired game.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire and render combo/on-fire HUD in SightReadingGame (GREEN)</name>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (the file being modified — imports 56-57, destructure 222-235, sites 1858 + 1941-1949, 2135 + 2181-2186, header 3610, root 3811-3839)
    - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx (the RED spec from Task 1)
    - .planning/phases/01-engagement-hud-parity/01-PATTERNS.md (SightReadingGame.jsx section — verbatim insertion code and placement)
    - src/components/games/shared/hud/ComboPill.jsx / OnFireBadge.jsx / OnFireSplash.jsx (prop contracts — confirm no reduced-motion prop)
  </read_first>
  <behavior>
    - A correct note calls incrementCombo() (single shared hook — both mic + keyboard route through handleNoteDetected).
    - A note whose window closes with no correct hit calls resetCombo() (single shared hook in tick()).
    - When context isOnFire transitions false->true, a one-shot splash shows for 1500ms.
    - ComboPill + OnFireBadge render in the HUD header; OnFireSplash renders at the fragment root.
    - No reduced-motion prop is passed (shared components self-handle). No lives/GameOverScreen added.
  </behavior>
  <action>
    Edit `src/components/games/sight-reading-game/SightReadingGame.jsx`:

    1. Add three imports immediately after line 57 (`import { ScorePill } from "../shared/hud/ScorePill";`):
    ```javascript
    import { ComboPill } from "../shared/hud/ComboPill";
    import { OnFireBadge } from "../shared/hud/OnFireBadge";
    import { OnFireSplash } from "../shared/hud/OnFireSplash";
    ```

    2. Extend the `useSightReadingSession()` destructure (add before the closing `} =` at line 235):
    ```javascript
      combo,
      isOnFire,
      incrementCombo,
      resetCombo,
    ```

    3. Add a one-shot splash side-effect (place with the other hooks in the component body, after the
       destructure). Do NOT play a fire sound: sight-reading runs continuous mic pitch-detection during
       PERFORMANCE, and an 880->1760 Hz oscillator blip (~A5-A6, inside the A2-C6 detection range) risks a
       phantom mic detection / false note. The splash + badge deliver the celebration safely.
    ```javascript
    const [showFireSplash, setShowFireSplash] = useState(false);
    const prevIsOnFireRef = useRef(isOnFire);
    useEffect(() => {
      if (isOnFire && !prevIsOnFireRef.current) {
        setShowFireSplash(true);
        const t = setTimeout(() => setShowFireSplash(false), 1500);
        prevIsOnFireRef.current = isOnFire;
        return () => clearTimeout(t);
      }
      prevIsOnFireRef.current = isOnFire;
    }, [isOnFire]);
    ```
       (`useState`, `useRef`, `useEffect` are already imported in this file.)

    4. At the correct-note record site, line 1858, immediately after `recordPerformanceResult(result);`, add:
    ```javascript
        incrementCombo();
    ```
       Then add `incrementCombo` to the `handleNoteDetected` dependency array (lines 1941-1949), alongside
       the existing `recordPerformanceResult` entry (it is a stable empty-deps callback — no churn).

    5. At the miss record site, line 2135, immediately after `recordPerformanceResult(missed);`, add:
    ```javascript
        resetCombo();
    ```
       Then add `resetCombo` to the `schedulePerformanceTimeline` dependency array (lines 2181-2186),
       alongside the existing `recordPerformanceResult` entry (stable callback — no churn). This single
       site covers both silent misses (`timingStatus: "missed"`) and uncorrected wrong-pitch notes, per
       01-PATTERNS.md.

    6. Render the pills in the HUD header "Right Controls" flex container (line 3610), immediately BEFORE
       the `<ScorePill .../>` at line 3612. Wrap each in an accessible container that consumes the existing
       i18n keys (D-07 / I18N-01) — the shared components accept no label prop, so the aria-label lives on
       the wrapper:
    ```jsx
        {isOnFire && (
          <div aria-label={t("games.engagement.onFire")}>
            <OnFireBadge active={isOnFire} />
          </div>
        )}
        <div role="status" aria-label={t("games.engagement.combo")}>
          <ComboPill combo={combo} isOnFire={isOnFire} />
        </div>
    ```

    7. Render `OnFireSplash` at the top-level return fragment root — as a sibling AFTER the
       `<div className="relative">...</div>` block closes (line 3839), NOT inside `SightReadingLayout`
       (D-08, RESEARCH Pitfall 3 — placing a `fixed inset-0` node inside the scroll-contained layout clips it):
    ```jsx
        <OnFireSplash show={showFireSplash} />
    ```

    Do NOT add any reduced-motion prop to any of the three components (D-08). Do NOT import or render
    `GameOverScreen`/`LivesDisplay`; do NOT change the encouragement/loss path (D-01/D-03).

  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "shared/hud/ComboPill" src/components/games/sight-reading-game/SightReadingGame.jsx`, `grep -q "shared/hud/OnFireBadge"`, and `grep -q "shared/hud/OnFireSplash"` all succeed.
    - `grep -q "incrementCombo();" ...SightReadingGame.jsx` and `grep -q "resetCombo();"` both succeed.
    - `grep -q "<OnFireSplash show={showFireSplash} />" ...SightReadingGame.jsx` succeeds AND it is placed after the `relative` wrapper div closes (outside SightReadingLayout).
    - `grep -q "games.engagement.combo" ...SightReadingGame.jsx` and `grep -q "games.engagement.onFire"` both succeed (I18N-01, reused keys — no new locale keys added).
    - `grep -c "GameOverScreen\|LivesDisplay" ...SightReadingGame.jsx` returns 0 (D-01/D-03 — no lives/game-over introduced).
    - No reduced-motion prop passed: `grep -c "reduced" ...` near the three HUD renders returns 0.
    - `SightReadingGame.combo.test.jsx` PASSES (GREEN) and `npm run test:run` is fully green.
  </acceptance_criteria>
  <done>Game increments/resets combo at the two existing record sites, renders ComboPill/OnFireBadge in the header and OnFireSplash at root, reuses games.engagement.* keys; loss path untouched; combo integration suite + full suite GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Purely client-side presentational wiring: reads already-validated `isCorrect`/`timingStatus` from the existing scoring pipeline and renders shared HUD components. No new network call, no persistence, no new user-input parsing, no auth/session code. The mic/keyboard input validation (timing windows, anti-cheat, debounce) is untouched. |

## STRIDE Threat Register

| Threat ID | Category          | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                   |
| --------- | ----------------- | ------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-03   | Tampering         | Client combo display + on-fire splash | accept      | Display-only, non-authoritative; derived from validated `performanceResults`. Tampering affects only the user's own celebration, no score-of-record or content gate. No PII.                                                                                                      |
| T-01-04   | Denial of Service | RAF `tick()` + on-fire `setTimeout`   | accept      | `resetCombo()` fires only inside the existing `already`-guarded miss block (at most once per note), not per frame — preserves the Phase B event-driven discipline (no reintroduced 60Hz setState). Splash timeout is a single cleared 1500ms timer. No unbounded work introduced. |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx` — both GREEN.
- `npm run test:run` — full suite green (per-wave merge gate).
- All grep acceptance checks in Task 2 pass (imports, hook calls, root splash placement, reused i18n keys,
  zero GameOverScreen/LivesDisplay, zero reduced-motion props).
</verification>

<success_criteria>

- Live ComboPill increments on correct notes and resets on window-close misses (HUD-01, D-04).
- OnFireBadge + OnFireSplash celebrate at combo >= 5, reduced-motion honored by the components (HUD-03, D-08).
- OnFireSplash is a root-level `fixed inset-0` sibling, not inside SightReadingLayout (D-08).
- Combo/on-fire accessible labels reuse `games.engagement.combo`/`onFire` in EN+HE (I18N-01, D-07).
- No lives, no GameOverScreen; encouragement screen unchanged (D-01, D-03).
  </success_criteria>

<output>
After completion, create `.planning/phases/01-engagement-hud-parity/01-02-SUMMARY.md`.
</output>
