---
phase: 01-engagement-hud-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/SightReadingSessionContext.jsx
  - src/contexts/SightReadingSessionContext.test.jsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HUD-01]

must_haves:
  truths:
    - "combo starts at 0 and isOnFire at false; incrementCombo() raises combo by 1, resetCombo() returns it to 0 (HUD-01, D-04)"
    - "isOnFire flips to true exactly when combo reaches ON_FIRE_THRESHOLD = 5 (HUD-03 foundation, D-06)"
    - "Combo is session-wide: goToNextExercise() does NOT reset combo; only startSession()/resetSession() reset it to 0 (D-05)"
    - "REQUIREMENTS.md marks HUD-02 as DEFERRED with the D-01 rationale (D-02)"
  artifacts:
    - path: "src/contexts/SightReadingSessionContext.jsx"
      provides: "session-wide combo/isOnFire state + incrementCombo/resetCombo callbacks"
      contains: "incrementCombo"
    - path: "src/contexts/SightReadingSessionContext.test.jsx"
      provides: "unit coverage of combo lifecycle + session-boundary reset semantics"
  key_links:
    - from: "src/contexts/SightReadingSessionContext.jsx startSession/resetSession"
      to: "combo reset"
      via: "comboRef.current = 0 + setCombo(0)"
      pattern: "comboRef\\.current = 0"
    - from: "src/contexts/SightReadingSessionContext.jsx goToNextExercise"
      to: "NO combo reset (session-wide)"
      via: "absence of combo reset call"
      pattern: "goToNextExercise"
---

<objective>
Add session-wide combo / on-fire state to `SightReadingSessionContext` so the sight-reading game can
drive a live combo counter and on-fire celebration on par with the sibling games — reusing the exact
`comboRef`/`setCombo` ref+state double-write pattern from `NotesRecognitionGame` and the constant
`ON_FIRE_THRESHOLD = 5` (D-06). Combo must survive exercise transitions (D-05) and reset only on
session boundaries. Also formalize the HUD-02 deferral (D-01/D-02) in REQUIREMENTS.md.

Purpose: This is the state foundation Plan 02 wires into the game component. Owning combo in the
session context (not per-exercise local state) is what makes the on-fire threshold reachable across a
multi-exercise session.
Output: Combo-capable `SightReadingSessionContext.jsx`, its first-ever unit test, and a documented
HUD-02 deferral.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engagement-hud-parity/01-CONTEXT.md
@.planning/phases/01-engagement-hud-parity/01-RESEARCH.md
@.planning/phases/01-engagement-hud-parity/01-PATTERNS.md
@.planning/phases/01-engagement-hud-parity/01-VALIDATION.md

<interfaces>
<!-- Current SightReadingSessionContext.jsx shape (VERIFIED by direct read). The executor extends
     this file; these are the exact structures to preserve and add to. -->

Existing constants (top of file, lines 9-10):

```javascript
const TOTAL_EXERCISES_PER_SESSION = 10;
const DEFAULT_MAX_SCORE_PER_EXERCISE = 100;
```

Existing session-boundary callbacks (lines 25-35) — these must ALSO reset combo:

```javascript
const startSession = useCallback(() => {
  setState(() => ({
    ...createInitialState(),
    status: "in-progress",
    sessionId: Date.now(),
  }));
}, []);
const resetSession = useCallback(() => {
  setState(() => createInitialState());
}, []);
```

Existing per-exercise advance (lines 66-83) — must NOT reset combo (D-05):

```javascript
const goToNextExercise = useCallback(() => {
  setState((prev) => {
    /* advances currentExerciseIndex only */
  });
}, []);
```

Existing useMemo value object (lines 105-130) — combo/isOnFire/incrementCombo/resetCombo get added to
BOTH the returned object and the dependency array.

Existing hook + constants export (lines 140-154): `useSightReadingSession()` throws if used outside
provider; `SIGHT_READING_SESSION_CONSTANTS = { TOTAL_EXERCISES_PER_SESSION, DEFAULT_MAX_SCORE_PER_EXERCISE }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the combo lifecycle unit test (RED)</name>
  <files>src/contexts/SightReadingSessionContext.test.jsx</files>
  <read_first>
    - src/contexts/SightReadingSessionContext.jsx (target of assertions — current shape)
    - src/components/games/shared/hud/ComboPill.test.jsx (repo Vitest + @testing-library/react conventions: plain `render`/`screen`, no extra setup)
    - .planning/phases/01-engagement-hud-parity/01-RESEARCH.md ("Validation Architecture" → what to assert) and 01-PATTERNS.md (the new-test-file section)
  </read_first>
  <action>
    Create `src/contexts/SightReadingSessionContext.test.jsx`. This context has zero existing tests, so
    build a small harness-component pattern (standard React testing, no new libraries):

    - Define a `Probe` component that calls `useSightReadingSession()` and renders `combo` and `isOnFire`
      into DOM (e.g. `<span data-testid="combo">{combo}</span>`, `<span data-testid="onfire">{String(isOnFire)}</span>`),
      plus buttons that call `incrementCombo`, `resetCombo`, `startSession`, `resetSession`, and
      `goToNextExercise` on click.
    - Render `<SightReadingSessionProvider><Probe/></SightReadingSessionProvider>`.

    Write these test cases (they will FAIL now because combo/isOnFire/incrementCombo/resetCombo do not
    yet exist on the context — this is the intended RED state):
    1. `"combo starts at 0 and isOnFire false"` — initial `combo` text is `"0"`, `onfire` text is `"false"`.
    2. `"incrementCombo raises combo by one"` — after 3 increment clicks, `combo` is `"3"`.
    3. `"on-fire activates at threshold 5"` — after 5 increment clicks, `onfire` is `"true"`; after 4 it is `"false"`.
    4. `"resetCombo returns combo to 0 and clears on-fire"` — increment to 5 then reset → `combo` `"0"`, `onfire` `"false"`.
    5. `"combo reset on session boundary"` — increment, then click `startSession` → `combo` `"0"`; repeat with `resetSession` → `combo` `"0"`.
    6. `"combo persists across goToNextExercise (session-wide)"` — increment to 2, click `goToNextExercise` → `combo` still `"2"` (D-05).

    Use `fireEvent.click` (or `userEvent`) for the button interactions. No fake timers needed here.

  </action>
  <verify>
    <automated>npx vitest run src/contexts/SightReadingSessionContext.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/contexts/SightReadingSessionContext.test.jsx` exists.
    - `grep -c "it(\|test(" src/contexts/SightReadingSessionContext.test.jsx` returns >= 6.
    - `grep -q "SightReadingSessionProvider" src/contexts/SightReadingSessionContext.test.jsx` succeeds.
    - Running the command executes the suite (no import/parse error) and reports FAILING assertions on
      combo/isOnFire (RED gate — proves the tests target not-yet-implemented behavior). It must NOT error
      with "cannot find module" or a syntax error.
  </acceptance_criteria>
  <done>Test file committed with 6 combo-lifecycle cases; suite runs and is RED on the combo assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session-wide combo state in the context (GREEN)</name>
  <files>src/contexts/SightReadingSessionContext.jsx</files>
  <read_first>
    - src/contexts/SightReadingSessionContext.jsx (the file being modified)
    - src/contexts/SightReadingSessionContext.test.jsx (the RED test from Task 1 — the spec to satisfy)
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx (lines 662-679 for comboRef/setCombo/isOnFireRef pattern; lines 1636-1731 for the increment/reset branches; line 314 for ON_FIRE_THRESHOLD = 5)
    - .planning/phases/01-engagement-hud-parity/01-PATTERNS.md (the SightReadingSessionContext.jsx section — verbatim code)
  </read_first>
  <behavior>
    - combo/isOnFire initialize to 0/false.
    - incrementCombo(): comboRef += 1, setCombo(comboRef.current); when comboRef.current >= 5 and not already on fire, set isOnFire true.
    - resetCombo(): comboRef = 0, setCombo(0); if on fire, clear isOnFire false.
    - startSession() and resetSession() additionally zero the comboRef + combo + isOnFire.
    - goToNextExercise() leaves combo untouched (session-wide, D-05).
  </behavior>
  <action>
    Edit `src/contexts/SightReadingSessionContext.jsx`:

    1. Add `useEffect` is NOT needed. Ensure `useRef` is imported — extend the existing named import
       from `"react"` (currently `createContext, useCallback, useContext, useMemo, useState`) to also
       include `useRef`.

    2. Add a module-level constant near the existing constants (after line 10), reused verbatim per D-06:
    ```javascript
    const ON_FIRE_THRESHOLD = 5; // reuse NotesRecognitionGame's constant (D-06)
    ```

    3. Inside `SightReadingSessionProvider`, right after `const [state, setState] = useState(() => createInitialState());`
       (line 23), add the sibling state + refs (NotesRecognitionGame ref+state double-write pattern):
    ```javascript
    const [combo, setCombo] = useState(0);
    const comboRef = useRef(0);
    const [isOnFire, setIsOnFire] = useState(false);
    const isOnFireRef = useRef(false);

    const incrementCombo = useCallback(() => {
      comboRef.current += 1;
      setCombo(comboRef.current);
      if (comboRef.current >= ON_FIRE_THRESHOLD && !isOnFireRef.current) {
        isOnFireRef.current = true;
        setIsOnFire(true);
      }
    }, []);

    const resetCombo = useCallback(() => {
      comboRef.current = 0;
      setCombo(0);
      if (isOnFireRef.current) {
        isOnFireRef.current = false;
        setIsOnFire(false);
      }
    }, []);
    ```

    4. In `startSession` (lines 25-31) AND `resetSession` (lines 33-35), add these three lines inside the
       callback body (they own the session-boundary reset — D-05, RESEARCH Pitfall/Anti-pattern):
    ```javascript
    comboRef.current = 0;
    setCombo(0);
    isOnFireRef.current = false;
    setIsOnFire(false);
    ```
       Do NOT add any combo reset to `goToNextExercise` — combo must survive exercise advance (D-05).

    5. Extend the `useMemo` value object (lines 105-123) to also return `combo, isOnFire, incrementCombo, resetCombo`,
       and add `combo, isOnFire, incrementCombo, resetCombo` to the `useMemo` dependency array (lines 124-130).
       (`incrementCombo`/`resetCombo` are empty-deps `useCallback`s — stable identities, no churn.)

    Do not change `recordExerciseResult` or the exercise-scoped state blob.

  </action>
  <verify>
    <automated>npx vitest run src/contexts/SightReadingSessionContext.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "const ON_FIRE_THRESHOLD = 5" src/contexts/SightReadingSessionContext.jsx` succeeds.
    - `grep -q "const incrementCombo = useCallback" src/contexts/SightReadingSessionContext.jsx` and `grep -q "const resetCombo = useCallback"` both succeed.
    - `grep -q "useRef" src/contexts/SightReadingSessionContext.jsx` succeeds (import + usage).
    - The `goToNextExercise` callback body contains NO combo reset: inspecting lines of `goToNextExercise`, there is no `setCombo(0)`/`comboRef.current = 0` inside it.
    - All 6 tests in `SightReadingSessionContext.test.jsx` PASS (GREEN).
  </acceptance_criteria>
  <done>Context exposes combo/isOnFire/incrementCombo/resetCombo; session boundaries reset combo; goToNextExercise preserves it; unit suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Formalize HUD-02 deferral in REQUIREMENTS.md (D-02)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — confirm current state of the HUD-02 entry and traceability table)
    - .planning/phases/01-engagement-hud-parity/01-CONTEXT.md (D-01 rationale + D-02 amendment requirement)
  </read_first>
  <action>
    Verify and, if any part is missing, complete the HUD-02 deferral in `.planning/REQUIREMENTS.md` so it
    reflects D-01/D-02:
    - The HUD-02 bullet under "Engagement HUD" MUST be struck through (`~~...~~`) and carry a **DEFERRED**
      marker with the D-01 rationale summarized: sight-reading is high-cognitive-load; a fail/game-over
      loop punishes rather than motivates; lives carry no business upside (subscription-gated content, not
      an energy/hearts economy); positive-only (combo + on-fire) chosen; the gentle encouragement screen is
      retained. It MUST reference `phases/01-engagement-hud-parity/01-CONTEXT.md` D-01/D-02.
    - The Traceability table row for HUD-02 MUST read Phase `—` and Status `Deferred`.
    - The Coverage summary MUST count HUD-02 as the 1 deferred item (11 active + 1 deferred).

    If all of the above is already present (it was amended during context gathering), make no functional
    change beyond confirming it and adding nothing spurious. Do NOT re-add HUD-02 as an active requirement.
    Do NOT touch any other requirement.

  </action>
  <verify>
    <automated>grep -n "HUD-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DEFERRED" .planning/REQUIREMENTS.md` succeeds and the match is on/near a `HUD-02` line.
    - `grep -q "01-CONTEXT.md" .planning/REQUIREMENTS.md` succeeds (rationale cross-reference present).
    - The Traceability table contains a line matching `HUD-02` with `Deferred`.
    - `grep -c "HUD-02" .planning/REQUIREMENTS.md` shows HUD-02 does NOT appear in any phase's active
      requirement list (only in the deferred bullet + traceability row).
  </acceptance_criteria>
  <done>HUD-02 is documented as deferred with the D-01 rationale and D-02 traceability; no active requirement re-added.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                                                            |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan adds only in-memory client React state (combo counter) to an existing context and edits a planning doc. No new trust boundary is crossed: no network calls, no persistence, no new user-input parsing, no auth/session code. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01-01   | Tampering              | Client combo state (`comboRef`/`setCombo`) | accept      | Combo is an ephemeral, non-authoritative, display-only counter derived from already-validated `performanceResults`. A user tampering with it via devtools affects only their own on-screen celebration — no server state, no score-of-record, no content gate. No PII. ASVS L1: no applicable control. |
| T-01-02   | Information Disclosure | `SightReadingSessionContext`               | accept      | Holds only per-session game aggregates already in memory; adds no new sensitive data and no export path.                                                                                                                                                                                               |

</threat_model>

<verification>
- `npx vitest run src/contexts/SightReadingSessionContext.test.jsx` — GREEN (6/6).
- `grep` acceptance checks in Tasks 2 and 3 pass.
- `npm run test:run` — full suite remains green (no regression from the context shape change; existing
  consumers destructure new fields as additive).
</verification>

<success_criteria>

- Context exposes `combo`, `isOnFire`, `incrementCombo`, `resetCombo`.
- On-fire flips at combo == 5 (`ON_FIRE_THRESHOLD`, D-06).
- Session boundaries (`startSession`/`resetSession`) reset combo; `goToNextExercise` does not (D-05).
- HUD-02 is documented as deferred with D-01 rationale (D-02).
  </success_criteria>

<output>
After completion, create `.planning/phases/01-engagement-hud-parity/01-01-SUMMARY.md`.
</output>
