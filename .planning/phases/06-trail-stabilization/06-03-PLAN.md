---
phase: 06-trail-stabilization
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Temporary documentation files removed from repo root"
    - "Debug files removed from repo"
    - "Repository root is clean of phase-specific artifacts"
  artifacts:
    - path: "IMPLEMENTATION_STATUS.md"
      provides: "N/A - should be deleted"
      status: "removed"
    - path: "TEST_PLAN.md"
      provides: "N/A - should be deleted"
      status: "removed"
  key_links: []
---

<objective>
Remove temporary documentation and debug files from the repository.

Purpose: Clean up implementation artifacts that were useful during development but should not remain in the repo. Information is preserved in git history if needed.
Output: Clean repository root with only production files remaining.
</objective>

<execution_context>
@C:\Users\pagis\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\pagis\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-trail-stabilization/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview and verify files to remove</name>
  <files>N/A</files>
  <action>
Before deleting, verify the files are truly temporary and safe to remove.

1. List files to be removed:
   ```bash
   ls -la IMPLEMENTATION_STATUS.md PHASE2_COMPLETE.md REDESIGN_COMPLETE.md TEST_PLAN.md verify-redesign.mjs unlock-nodes-test.sql 2>/dev/null
   ```

2. Verify each file is temporary documentation or debug script:
   - `IMPLEMENTATION_STATUS.md` - Phase tracking document (temporary)
   - `PHASE2_COMPLETE.md` - Completion marker (temporary)
   - `REDESIGN_COMPLETE.md` - Completion marker (temporary)
   - `TEST_PLAN.md` - Testing document (temporary, info preserved in 06-RESEARCH.md)
   - `verify-redesign.mjs` - Debug verification script (temporary)
   - `unlock-nodes-test.sql` - Debug SQL script (temporary)

3. Run `git status` to confirm these are untracked files (not committed):
   ```bash
   git status --short | grep "??"
   ```

All files should show `??` prefix (untracked). If any show `M` (modified), investigate first.
  </action>
  <verify>
All 6 files exist and are untracked (`??` status).
No files are modified or staged.
  </verify>
  <done>Files verified as untracked temporary artifacts, safe to remove.</done>
</task>

<task type="auto">
  <name>Task 2: Remove temporary files and commit cleanup</name>
  <files>
    IMPLEMENTATION_STATUS.md
    PHASE2_COMPLETE.md
    REDESIGN_COMPLETE.md
    TEST_PLAN.md
    verify-redesign.mjs
    unlock-nodes-test.sql
  </files>
  <action>
Remove temporary files and create cleanup commit.

1. Remove documentation files:
   ```bash
   rm IMPLEMENTATION_STATUS.md PHASE2_COMPLETE.md REDESIGN_COMPLETE.md TEST_PLAN.md
   ```

2. Remove debug scripts:
   ```bash
   rm verify-redesign.mjs unlock-nodes-test.sql
   ```

3. Verify removal:
   ```bash
   git status
   ```
   Should show clean working tree (no untracked temp files).

4. Since files were never tracked, no commit needed for removal.
   However, we should document the cleanup in a commit message if there are any remaining changes.

5. If git status shows clean:
   - No commit needed
   - Proceed to verification
  </action>
  <verify>
`git status` shows clean working tree.
`ls *.md` in repo root shows only README.md (if exists) and CLAUDE.md.
No temp files remain: verify-redesign.mjs, unlock-nodes-test.sql are gone.
  </verify>
  <done>Temporary files removed. Repository root is clean of phase-specific artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and build check</name>
  <files>N/A</files>
  <action>
Run final verification to ensure everything is stable.

1. Verify clean git status:
   ```bash
   git status
   ```

2. Run lint check:
   ```bash
   npm run lint
   ```

3. Run production build:
   ```bash
   npm run build
   ```

4. Verify build succeeds without errors.

5. Run `git log --oneline -5` to confirm commit history shows:
   - Foundation commit (feat(trail): add redesigned treble clef...)
   - Integration commit (feat(trail): integrate memory game...)
   - Migrations commit (feat(db): add unit tracking...)
   - Any bug fix commits from Plan 02
  </action>
  <verify>
`git status` shows clean working tree.
`npm run lint` passes.
`npm run build` succeeds.
Git history shows logical commit structure.
  </verify>
  <done>Phase 6 complete. Trail system stabilized, tested, and repository cleaned.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `git status` shows clean working tree
2. `npm run build` succeeds
3. No temp files in repo root (IMPLEMENTATION_STATUS.md, TEST_PLAN.md, etc.)
4. Git history shows 3+ commits from this phase
</verification>

<success_criteria>
- All temporary documentation files removed
- All debug scripts removed
- Repository root is clean
- Production build succeeds
- Requirements covered: CLEAN-01, CLEAN-02
</success_criteria>

<output>
After completion, create `.planning/phases/06-trail-stabilization/06-03-SUMMARY.md`
</output>
