---
phase: 14
slug: console-logging-cleanup
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-31
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase UI Classification

**Type: No-UI phase.**

Phase 14 is a pure code-quality and tooling change. It produces zero user-facing UI changes. No new screens, components, layouts, or interactions are introduced. All work is in:
- Source file edits (adding DEV guards, deleting stale calls)
- ESLint config (`eslint.config.js`) — one new rule

The design contract below reflects the **existing project design system**, declared explicitly so downstream agents (planner, executor, auditor) have a stable reference. No new design decisions are required.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (no shadcn) | `components.json` not present — confirmed |
| Preset | not applicable | no shadcn |
| Component library | none (custom Tailwind components) | CLAUDE.md |
| Icon library | not specified for this phase | n/a |
| Font | Outfit (primary), Nunito (rounded), Comic Neue (comic) | tailwind.config.js |
| Styling approach | Tailwind CSS 3 + glassmorphism pattern | CLAUDE.md |

---

## Spacing Scale

Declared values from existing design system (tailwind.config.js — CSS custom properties):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none — phase introduces no new UI elements that would require spacing exceptions.

Source: tailwind.config.js `extend.spacing` CSS custom property tokens.

---

## Typography

Existing project typography scale (tailwind.config.js `extend.fontSize`):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (base) | 400 (regular) | 1.7 |
| Label | 14px (sm) | 600 (semibold) | 1.6 |
| Heading | 20px (xl) | 600 (semibold) | 1.8 |
| Display | 30px (3xl) | 600 (semibold) | 1.9 |

Source: tailwind.config.js existing fontSize scale. No new typography is introduced in this phase.

---

## Color

Existing project color contract (CLAUDE.md glassmorphism design system):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | indigo-900 / purple-900 / violet-900 gradient | Page background (`bg-gradient-to-br from-indigo-900 via-purple-900 to-violet-900`) |
| Secondary (30%) | `bg-white/10 backdrop-blur-md border-white/20` | Glass cards, sidebar, nav |
| Accent (10%) | indigo-400 / -300 variants | Progress bar fills, accent numbers only |
| Destructive | `hsl(var(--destructive))` | Destructive actions only (no destructive actions in this phase) |

Accent reserved for: progress bar fills (`bg-indigo-400`), accent numeric values (`text-indigo-300`, `text-green-300`). No new accent usages introduced in this phase.

Source: CLAUDE.md "Glass Card Pattern (primary)" section.

---

## Copywriting Contract

Phase 14 introduces no user-facing copy. The table below declares the absence explicitly:

| Element | Copy | Note |
|---------|------|------|
| Primary CTA | n/a — no new CTAs | Phase is code-only |
| Empty state heading | n/a — no new empty states | Phase is code-only |
| Empty state body | n/a — no new empty states | Phase is code-only |
| Error state | n/a — no new error states | Phase is code-only |
| Destructive confirmation | n/a — no destructive user actions | Phase is code-only |

The only "user-visible" effect of this phase is the absence of debug output in the browser console in production — this is invisible to end users and requires no copy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No component registry is used in this phase. No new components are installed.

---

## Implementation Constraints (for executor reference)

These are sourced from CONTEXT.md and RESEARCH.md — not new design decisions.

| Constraint | Value | Source |
|------------|-------|--------|
| DEV guard pattern | `if (import.meta.env.DEV) { ... }` | CONTEXT.md D-01, apiAuth.js:241 |
| No shared logger utility | inline guards only | CONTEXT.md D-02 |
| Scope: log types | `console.log` and `console.debug` only | CONTEXT.md D-03 |
| ESLint rule | `"no-console": ["warn", { allow: ["warn", "error", "info"] }]` | CONTEXT.md D-04, RESEARCH.md Pattern 4 |
| ESLint severity | `"warn"` (not `"error"`) | RESEARCH.md — balances DX with enforcement |
| Config file target | `eslint.config.js` (ESLint 9 flat config) | RESEARCH.md Pitfall 4 |
| Disable directive for gated calls | `// eslint-disable-next-line no-console` | RESEARCH.md Pitfall 2 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — no user-facing copy introduced; absence explicitly declared
- [ ] Dimension 2 Visuals: PASS — no new visual elements; existing design system referenced
- [ ] Dimension 3 Color: PASS — existing glassmorphism palette unchanged
- [ ] Dimension 4 Typography: PASS — existing type scale unchanged
- [ ] Dimension 5 Spacing: PASS — existing 8-point scale unchanged
- [ ] Dimension 6 Registry Safety: PASS — no registries used; no new components

**Approval:** pending
