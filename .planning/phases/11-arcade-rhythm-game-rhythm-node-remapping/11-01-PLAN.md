---
phase: 11-arcade-rhythm-game-rhythm-node-remapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/rhythm-games/ArcadeRhythmGame.jsx
  - src/components/games/rhythm-games/ArcadeRhythmGame.test.js
autonomous: true
requirements:
  - ARCR-01
  - ARCR-02
  - ARCR-03
  - ARCR-04
  - ARCR-05

must_haves:
  truths:
    - "Falling tiles descend in sync with beat schedule using requestAnimationFrame"
    - "Tapping the hit zone scores PERFECT/GOOD/MISS based on audioContext.currentTime timing"
    - "Missing a tile loses a life; 0 lives triggers GameOverScreen"
    - "Consecutive hits increment a combo counter; combo >= 5 triggers on-fire mode"
    - "Completing 10 patterns triggers VictoryScreen with star rating and XP"
  artifacts:
    - path: "src/components/games/rhythm-games/ArcadeRhythmGame.jsx"
      provides: "Falling-tile arcade rhythm game component"
      min_lines: 400
    - path: "src/components/games/rhythm-games/ArcadeRhythmGame.test.js"
      provides: "Unit tests for game logic (lives, combo, on-fire, phase transitions)"
      min_lines: 80
  key_links:
    - from: "ArcadeRhythmGame.jsx"
      to: "RhythmPatternGenerator.js"
      via: "getPattern() import"
      pattern: "import.*RhythmPatternGenerator"
    - from: "ArcadeRhythmGame.jsx"
      to: "rhythmScoringUtils.js"
      via: "scoreTap() import"
      pattern: "import.*scoreTap"
    - from: "ArcadeRhythmGame.jsx"
      to: "FloatingFeedback.jsx"
      via: "component import"
      pattern: "import.*FloatingFeedback"
    - from: "ArcadeRhythmGame.jsx"
      to: "CountdownOverlay.jsx"
      via: "component import"
      pattern: "import.*CountdownOverlay"
    - from: "ArcadeRhythmGame.jsx"
      to: "VictoryScreen.jsx"
      via: "renders on SESSION_COMPLETE"
      pattern: "VictoryScreen"
    - from: "ArcadeRhythmGame.jsx"
      to: "GameOverScreen.jsx"
      via: "renders when lives=0"
      pattern: "GameOverScreen"
---

<objective>
Build the ArcadeRhythmGame component — a falling-tile rhythm game where colored tiles descend toward a glowing hit zone and the child taps to score.

Purpose: Adds the most visually exciting rhythm game type, used for boss nodes (D-13) and ~10% of regular rhythm exercises (D-12). Reuses all existing rhythm infrastructure (pattern generation, tap scoring, feedback display).

Output: `ArcadeRhythmGame.jsx` (playable game component) + `ArcadeRhythmGame.test.js` (unit tests for game logic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-arcade-rhythm-game-rhythm-node-remapping/11-CONTEXT.md
@.planning/phases/11-arcade-rhythm-game-rhythm-node-remapping/11-RESEARCH.md
@.planning/phases/11-arcade-rhythm-game-rhythm-node-remapping/11-UI-SPEC.md

<interfaces>
<!-- Key types, components, and functions the executor needs. Extracted from codebase. -->

From src/components/games/rhythm-games/RhythmReadingGame.jsx (FSM pattern to follow):
```javascript
const GAME_PHASES = {
  SETUP: 'setup',
  READY: 'ready',
  PLAYING: 'playing',
  FEEDBACK: 'feedback',
  SESSION_COMPLETE: 'session-complete',
};
// ArcadeRhythmGame replaces READY with COUNTDOWN (CountdownOverlay, not looping metronome)
```

From src/components/games/rhythm-games/utils/rhythmScoringUtils.js:
```javascript
export function scoreTap(tapTime, beatTimes, nextBeatIndex, bpm) {
  // Returns: { quality: 'PERFECT'|'GOOD'|'MISS'|null, noteIdx, newNextBeatIndex }
}
export function calculateTimingThresholds(bpm) {
  // Returns: { perfectWindow, goodWindow }
}
```

From src/components/games/rhythm-games/components/FloatingFeedback.jsx:
```javascript
// Props: { quality, feedbackKey, reducedMotion }
// quality: 'PERFECT' | 'GOOD' | 'MISS'
// feedbackKey: number (incremented each feedback to re-trigger animation)
```

From src/components/games/rhythm-games/components/CountdownOverlay.jsx:
```javascript
// Props: { countdownValue, reducedMotion }
// countdownValue: 3 | 2 | 1 | 'GO' | null
```

From src/components/games/GameOverScreen.jsx:
```javascript
// Props: { livesLost, onReset, score, correctAnswers, totalQuestions, ...trailProps }
```

From src/components/games/VictoryScreen.jsx:
```javascript
// Trail props: nodeId, exerciseIndex, totalExercises, exerciseType, onNextExercise
// Session props: score, correctAnswers, totalQuestions, stars
```

From src/components/games/rhythm-games/RhythmPatternGenerator.js:
```javascript
// getPattern(config) returns beat schedule with isRest flags
// buildBeatTimes(beats, tempo, audioCtx) returns scheduledBeatTimes array
```

From src/components/games/notes-master-games/NotesRecognitionGame.jsx (lives/combo/on-fire pattern):
```javascript
const INITIAL_LIVES = 3;       // line 380
const ON_FIRE_THRESHOLD = 5;   // line 381
// State: lives, combo, isOnFire — copy pattern, do not import
```

From src/contexts/AudioContextProvider.jsx:
```javascript
export function useAudioContext() {
  // Returns: { audioContext, isReady, resumeAudioContext }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ArcadeRhythmGame test file</name>
  <files>src/components/games/rhythm-games/ArcadeRhythmGame.test.js</files>
  <read_first>
    src/components/games/rhythm-games/RhythmReadingGame.jsx
    src/components/games/notes-master-games/NotesRecognitionGame.jsx
    src/components/games/rhythm-games/utils/rhythmScoringUtils.js
    src/components/games/rhythm-games/components/FloatingFeedback.jsx
    src/components/games/rhythm-games/components/CountdownOverlay.jsx
    src/components/games/GameOverScreen.jsx
    src/components/games/VictoryScreen.jsx
    src/test/setupTests.js
  </read_first>
  <behavior>
    - Test 1: GAME_PHASES constant has SETUP, COUNTDOWN, PLAYING, FEEDBACK, SESSION_COMPLETE keys
    - Test 2: INITIAL_LIVES is 3, ON_FIRE_THRESHOLD is 5, SCREEN_TRAVEL_TIME is 3.0
    - Test 3: Component renders without crashing when given minimal location.state (nodeId, nodeConfig with tempo/timeSignature/difficulty)
    - Test 4: Lives decrement on MISS — starting at 3, after 3 misses should be 0
    - Test 5: Combo increments on PERFECT or GOOD hit, resets to 0 on MISS
    - Test 6: isOnFire becomes true when combo reaches ON_FIRE_THRESHOLD (5)
    - Test 7: GameOverScreen renders when lives reach 0
    - Test 8: VictoryScreen renders when session completes (10 patterns scored)
    - Test 9: Ghost tiles (isRest=true) do not trigger life loss when they exit the hit zone
  </behavior>
  <action>
    Create `src/components/games/rhythm-games/ArcadeRhythmGame.test.js` with Vitest + React Testing Library tests.

    Mock dependencies:
    - `react-router-dom`: mock `useLocation` returning `{ state: { nodeId: 'rhythm_1_7', nodeConfig: { tempo: 80, timeSignature: '4/4', difficulty: 'easy' }, exerciseIndex: 0, totalExercises: 1, exerciseType: 'arcade_rhythm' } }` and `useNavigate` returning `vi.fn()`
    - `react-i18next`: mock `useTranslation` returning `{ t: (k) => k, i18n: { language: 'en' } }`
    - `../../contexts/AudioContextProvider`: mock `useAudioContext` returning `{ audioContext: { currentTime: 0, state: 'running', resume: vi.fn() }, isReady: true }`
    - `./RhythmPatternGenerator`: mock `getPattern` returning a simple pattern array, mock `buildBeatTimes` returning an array of scheduled times
    - `./utils/rhythmScoringUtils`: mock `scoreTap` with controllable return values
    - `../../contexts/AccessibilityContext`: mock `useAccessibility` returning `{ reducedMotion: false }`
    - `../../contexts/SettingsContext`: mock `useSettings` returning minimal settings object
    - `../../hooks/useLandscapeLock`: mock returning void
    - `../../hooks/useRotatePrompt`: mock returning `{ showRotatePrompt: false }`
    - `../../contexts/SessionTimeoutContext`: mock `useSessionTimeout` returning `{ pauseTimer: vi.fn(), resumeTimer: vi.fn() }`

    Test the exported constants (GAME_PHASES, INITIAL_LIVES, ON_FIRE_THRESHOLD, SCREEN_TRAVEL_TIME) directly.

    For state-machine tests, render the component and use `screen.getByText` / `screen.queryByText` to verify UI state transitions. For lives/combo tests, extract the game logic into a testable helper or test via rendered output (hearts count, combo badge text).

    All tests should be RED initially (component does not exist yet).
  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/games/rhythm-games/ArcadeRhythmGame.test.js` exists
    - File contains `import` from `./ArcadeRhythmGame`
    - File contains at least 7 `it(` or `test(` blocks
    - File contains `INITIAL_LIVES` and `ON_FIRE_THRESHOLD` and `SCREEN_TRAVEL_TIME` assertions
    - File contains `GameOverScreen` and `VictoryScreen` render assertions
    - Tests initially FAIL (RED) because ArcadeRhythmGame.jsx does not exist yet
  </acceptance_criteria>
  <done>Test file exists with 7+ test cases covering ARCR-01 through ARCR-05. All tests fail because the component is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build ArcadeRhythmGame component</name>
  <files>src/components/games/rhythm-games/ArcadeRhythmGame.jsx</files>
  <read_first>
    src/components/games/rhythm-games/RhythmReadingGame.jsx
    src/components/games/notes-master-games/NotesRecognitionGame.jsx
    src/components/games/rhythm-games/RhythmPatternGenerator.js
    src/components/games/rhythm-games/utils/rhythmScoringUtils.js
    src/components/games/rhythm-games/components/FloatingFeedback.jsx
    src/components/games/rhythm-games/components/CountdownOverlay.jsx
    src/components/games/GameOverScreen.jsx
    src/components/games/VictoryScreen.jsx
    src/components/games/shared/AudioInterruptedOverlay.jsx
    src/hooks/useLandscapeLock.js
    src/hooks/useRotatePrompt.js
    src/contexts/SessionTimeoutContext.jsx
    src/contexts/AudioContextProvider.jsx
    src/contexts/AccessibilityContext.jsx
    .planning/phases/11-arcade-rhythm-game-rhythm-node-remapping/11-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/games/rhythm-games/ArcadeRhythmGame.jsx` — a single self-contained file mirroring the RhythmReadingGame FSM structure. Key implementation details:

    **Exported constants:**
    ```javascript
    export const GAME_PHASES = {
      SETUP: 'setup',
      COUNTDOWN: 'countdown',
      PLAYING: 'playing',
      FEEDBACK: 'feedback',
      SESSION_COMPLETE: 'session-complete',
    };
    export const INITIAL_LIVES = 3;
    export const ON_FIRE_THRESHOLD = 5;
    export const SCREEN_TRAVEL_TIME = 3.0; // seconds
    ```

    **Imports to use:**
    - `RhythmPatternGenerator` — `getPattern()` for beat schedule, `buildBeatTimes()` for timing
    - `scoreTap` from `./utils/rhythmScoringUtils`
    - `FloatingFeedback` from `./components/FloatingFeedback`
    - `CountdownOverlay` from `./components/CountdownOverlay`
    - `VictoryScreen` from `../VictoryScreen`
    - `GameOverScreen` from `../GameOverScreen`
    - `AudioInterruptedOverlay` from `../shared/AudioInterruptedOverlay`
    - `useAudioContext` from `../../contexts/AudioContextProvider`
    - `useAccessibility` from `../../contexts/AccessibilityContext`
    - `useSessionTimeout` from `../../contexts/SessionTimeoutContext`
    - `useLandscapeLock` from `../../hooks/useLandscapeLock`
    - `useRotatePrompt` from `../../hooks/useRotatePrompt`
    - `Heart`, `Zap`, `Flame`, `ArrowLeft` from `lucide-react`
    - `useTranslation` from `react-i18next`
    - `useLocation`, `useNavigate` from `react-router-dom`

    **Component structure per UI-SPEC:**
    - Full-screen landscape layout: `fixed inset-0 bg-gradient-to-br from-indigo-900 via-purple-900 to-violet-900 flex flex-col`
    - Header (h-12): BackButton (ArrowLeft icon), exercise counter (`X/10`), lives hearts group, combo badge
    - Main area (flex-1 relative overflow-hidden): tile lane + hit zone line + FloatingFeedback + CountdownOverlay
    - Lives: 3 Heart icons — full heart: `fill-red-400 text-red-400`, empty: `text-white/30`
    - Combo badge (shown when combo >= 2): `bg-yellow-400/20 border border-yellow-400/40 rounded-full px-2 py-0.5 text-yellow-300 text-sm font-bold`
    - On-fire combo badge: `bg-orange-400/20 border border-orange-400/40 text-orange-300` with Flame icon prepended

    **Tile lane (per UI-SPEC + D-01):**
    - Single column (`w-full` relative container, `overflow-hidden`)
    - Each tile is `absolute` positioned, translated via `ref.style.transform = translateY(...)` in rAF loop
    - Tile width: full lane width (`w-[calc(100%-2rem)]` centered with `mx-auto`)
    - Tile heights by duration: whole=80px (`h-20`), half=64px (`h-16`), quarter=56px (`h-14`), eighth=44px (`h-11`)
    - Tile border-radius: `rounded-xl`
    - Duration-coded tile colors (per UI-SPEC):
      - Quarter: `bg-blue-400/80 border border-blue-300/60`
      - Half: `bg-emerald-400/80 border border-emerald-300/60`
      - Eighth: `bg-orange-400/80 border border-orange-300/60`
      - Whole: `bg-violet-400/80 border border-violet-300/60`
      - Dotted: same color as base duration + white dot indicator overlay
      - Ghost (rest): `bg-white/15 border border-dashed border-white/30`
    - On-fire mode: tiles get `drop-shadow-[0_0_12px_rgba(249,115,22,0.8)]` orange glow (skip if reducedMotion)

    **Hit zone line (per UI-SPEC + D-03):**
    - Container: `absolute bottom-0 left-0 right-0 h-12` — full-screen tap target (per D-01: tap anywhere)
    - Visual line: 3px horizontal line at vertical center
    - Default glow: `box-shadow: 0 0 12px 4px rgba(99, 102, 241, 0.8)` (indigo)
    - On-fire glow: `box-shadow: 0 0 16px 6px rgba(249, 115, 22, 0.9)` (orange), 150ms transition
    - Pulses with `animate-pulse` (skip if reducedMotion)

    **rAF animation loop (CRITICAL — per RESEARCH Pattern 2):**
    - Use `tileRefs` (array of DOM refs) and `tilesRef` (array of tile definitions)
    - Each frame: compute `progress = (audioContext.currentTime - tile.spawnTime) / SCREEN_TRAVEL_TIME`
    - Set `ref.style.transform = translateY(${yPercent}vh)` where yPercent maps progress [0,1] to screen height
    - When progress >= 1.0 and tile not yet scored:
      - Non-rest tile: trigger MISS (life loss, combo reset)
      - Rest tile (ghost): silently pass through, no penalty (per D-04)
    - Add tile index to `scoredRef` Set to prevent double-scoring (per RESEARCH Pitfall 2)

    **Tile spawn scheduling (per RESEARCH Pattern 3):**
    - Derive beat times from `buildBeatTimes(beats, tempo, audioCtx)`
    - Each tile's `spawnTime = beatTime - SCREEN_TRAVEL_TIME`
    - During COUNTDOWN phase, tiles with spawnTime before countdown end are visible at top of screen, slowly descending (per D-08)
    - All tiles for a single pattern are pre-computed at exercise start

    **Tap scoring (per RESEARCH Pattern 8):**
    - `handleHitZoneTap()` captures `audioContext.currentTime` (NOT `Date.now()` — per STATE.md decision)
    - Calls `scoreTap(tapTime, activeBeatTimesRef.current, nextBeatIndexRef.current, tempo)`
    - Returns `{ quality, noteIdx, newNextBeatIndex }`
    - On PERFECT: tile burst animation (6-8 small divs as particles spread outward over 600ms via CSS keyframes, skip if reducedMotion), combo++, show green FloatingFeedback
    - On GOOD: tile dissolves (opacity 0 over 200ms, immediate if reducedMotion), combo++, show yellow FloatingFeedback
    - On MISS from tap (tap too early/late): tile grays out (`bg-white/15`), combo=0, isOnFire=false, lives--, show red FloatingFeedback

    **Lives / Combo / On-fire (per RESEARCH Pattern 4 + D-06 + D-07):**
    - `const [lives, setLives] = useState(INITIAL_LIVES)` — 3 hearts
    - `const [combo, setCombo] = useState(0)`
    - `const [isOnFire, setIsOnFire] = useState(false)`
    - On MISS: `setCombo(0)`, `setIsOnFire(false)`, `setLives(prev => prev - 1)`, screen shake on main container (CSS `@keyframes shake` for 400ms, skip if reducedMotion)
    - On PERFECT or GOOD: `setCombo(prev => { const next = prev + 1; if (next >= ON_FIRE_THRESHOLD) setIsOnFire(true); return next; })`
    - When lives reach 0: transition to GameOverScreen with `livesLost={true}`

    **Screen shake CSS keyframe (add as inline style or in component):**
    ```css
    @keyframes shake {
      0%, 100% { transform: translateX(0); }
      25% { transform: translateX(-4px); }
      50% { transform: translateX(4px); }
      75% { transform: translateX(-2px); }
    }
    ```

    **Particle burst CSS keyframe (for PERFECT hits):**
    Create 6-8 absolutely-positioned small colored divs (8px circles) at the hit point. Each flies outward in a different direction over 600ms using CSS keyframes with random spread angles. Remove after animation completes. Skip entirely if reducedMotion.

    **Game session flow:**
    1. SETUP phase: Extract config from `location.state` (nodeId, nodeConfig, exerciseIndex, totalExercises, exerciseType). Show "Play" button (per UI-SPEC copywriting). Call `pauseTimer()` from SessionTimeoutContext.
    2. COUNTDOWN phase: Run CountdownOverlay (3, 2, 1, GO!). Generate first pattern via `getPattern()`. Build beat times. Pre-compute tile definitions. Begin slow tile descent. Play metronome clicks (use `usePianoSampler` for click sounds or reuse the audio scheduling from RhythmReadingGame).
    3. PLAYING phase: Full-speed rAF loop. Hit zone active. Tap events scored. Lives/combo updated per hit.
    4. FEEDBACK phase: After all beats in a pattern are scored or exited, show pattern score briefly (400ms), then auto-advance to next pattern. If patterns completed < 10, generate next pattern and return to COUNTDOWN (or skip countdown for patterns 2-10, jumping straight to PLAYING).
    5. SESSION_COMPLETE phase: All 10 patterns done. Calculate final score, stars (same formula as RhythmReadingGame). Render VictoryScreen with trail props.

    **Trail integration (per RESEARCH Pattern 5):**
    - Extract trail state from `location.state`: `nodeId`, `nodeConfig`, `exerciseIndex`, `totalExercises`, `exerciseType`
    - `hasAutoStartedRef` pattern: if trail state present, auto-start after mount
    - `handleNextExercise` callback: full switch statement with ALL exercise type cases including `arcade_rhythm` (use `navigate('/rhythm-mode/arcade-rhythm-game', { state: navState, replace: true }); window.location.reload();` for same-route navigation)
    - Pass `handleNextExercise`, `nodeId`, `exerciseIndex`, `totalExercises`, `exerciseType` to VictoryScreen

    **Accessibility:**
    - `useAccessibility()` for `reducedMotion` — skip all particle bursts, screen shake, glow animations, tile trail effects
    - Hearts group gets `aria-label="${lives} lives remaining"` (sr-only)
    - On-fire announcement: `aria-live="polite"` hidden span: "On fire! Keep going!" at combo threshold
    - FloatingFeedback already handles aria-live

    **Hooks to call (same as RhythmReadingGame):**
    - `useLandscapeLock()` — lock to landscape on mount
    - `useRotatePrompt()` — show rotate overlay on portrait mobile
    - `useSessionTimeout()` — `pauseTimer()` during SETUP, `resumeTimer()` during PLAYING
    - `useAudioContext()` — get audioContext for timing

    **Config extraction (per RESEARCH Pattern 6):**
    ```javascript
    const tempo = nodeConfig?.tempo ?? nodeConfig?.config?.tempo ?? 90;
    const timeSignatureStr = nodeConfig?.timeSignature ?? nodeConfig?.config?.timeSignature ?? '4/4';
    const difficulty = nodeConfig?.difficulty ?? nodeConfig?.config?.difficulty ?? 'beginner';
    ```

    **Export:** `export default ArcadeRhythmGame;` and named exports for `GAME_PHASES`, `INITIAL_LIVES`, `ON_FIRE_THRESHOLD`, `SCREEN_TRAVEL_TIME`.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/games/rhythm-games/ArcadeRhythmGame.jsx` exists with 400+ lines
    - File exports `GAME_PHASES` with keys SETUP, COUNTDOWN, PLAYING, FEEDBACK, SESSION_COMPLETE
    - File exports `INITIAL_LIVES` (value 3), `ON_FIRE_THRESHOLD` (value 5), `SCREEN_TRAVEL_TIME` (value 3.0)
    - File imports `scoreTap` from `./utils/rhythmScoringUtils`
    - File imports `FloatingFeedback` from `./components/FloatingFeedback`
    - File imports `CountdownOverlay` from `./components/CountdownOverlay`
    - File imports `VictoryScreen` from `../VictoryScreen`
    - File imports `GameOverScreen` from `../GameOverScreen`
    - File imports `AudioInterruptedOverlay` from `../shared/AudioInterruptedOverlay`
    - File contains `requestAnimationFrame` call in the animation loop
    - File contains `audioContext.currentTime` (not `Date.now()`) for tap timing
    - File contains `ref.style.transform` for tile positioning (not React state per frame)
    - File contains `scoredRef` Set to prevent double-scoring
    - File contains `tile.isRest` check to prevent rest tiles from triggering MISS
    - File contains `handleNextExercise` with `case 'arcade_rhythm'` routing
    - File contains `reducedMotion` checks for animations
    - File contains `aria-label` on hearts group
    - All tests in `ArcadeRhythmGame.test.js` pass (GREEN)
    - `npm run lint` passes with no errors in ArcadeRhythmGame.jsx
  </acceptance_criteria>
  <done>ArcadeRhythmGame.jsx is a complete, playable falling-tile game component. It renders a full-screen landscape game with descending color-coded tiles, glowing hit zone, 3-lives system, combo counter, on-fire mode, and integrates with VictoryScreen/GameOverScreen. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame.test.js` — all tests pass
- `npm run lint` — no lint errors in new files
- Component can be imported without errors: `node -e "import('./src/components/games/rhythm-games/ArcadeRhythmGame.jsx')"` (via Vite)
</verification>

<success_criteria>
- ArcadeRhythmGame.jsx exists with full game logic (FSM, rAF animation, tap scoring, lives, combo, on-fire)
- Test file covers lives depletion, combo counting, on-fire activation, phase transitions, rest tile handling
- All tests pass GREEN
- No lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-arcade-rhythm-game-rhythm-node-remapping/11-01-SUMMARY.md`
</output>
