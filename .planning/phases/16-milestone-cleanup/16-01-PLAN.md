---
phase: 16-milestone-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useAudioEngine.js
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "ESLint reports zero warnings on useAudioEngine.js (the eslint-disable-line comment suppresses the console.debug call on line 271)"
    - "npm run build succeeds end-to-end including the prebuild trail validation step"
    - "ROADMAP progress table shows all v3.0 phases with accurate statuses and plan counts"
  artifacts:
    - path: "src/hooks/useAudioEngine.js"
      provides: "Correctly placed eslint-disable-line no-console on the console.debug call"
      contains: "console.debug.*eslint-disable-line no-console"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase progress tracking"
      contains: "16. Milestone Cleanup"
  key_links:
    - from: "src/hooks/useAudioEngine.js"
      to: ".eslintrc.cjs"
      via: "no-console rule enforcement"
      pattern: "eslint-disable-line no-console"
---

<objective>
Close all v3.0 audit tech debt: fix the misplaced ESLint disable comment in useAudioEngine.js, verify the build pipeline passes end-to-end, and ensure ROADMAP/STATE/REQUIREMENTS docs accurately reflect completion status.

Purpose: Mark v3.0 milestone as complete with zero outstanding tech debt items.
Output: Clean ESLint output on useAudioEngine.js, passing build, accurate docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v3.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ESLint disable comment placement in useAudioEngine.js</name>
  <files>src/hooks/useAudioEngine.js</files>
  <read_first>
    - src/hooks/useAudioEngine.js (lines 265-280 — see current misplaced comment)
  </read_first>
  <action>
In `src/hooks/useAudioEngine.js`, the `// eslint-disable-line no-console` comment is on line 272 (inside the object literal) but needs to be on line 271 (the line with `console.debug`).

Current state (lines 271-272):

```javascript
          console.debug("[createMetronomeClick]", {
            // eslint-disable-line no-console
```

Change to:

```javascript
          console.debug("[createMetronomeClick]", { // eslint-disable-line no-console
```

This moves the disable directive to the same line as the `console.debug` call. The comment must be an inline trailing comment on the `console.debug` line, not on the next line inside the object.

The opening brace `{` stays on the same line as `console.debug` — the comment goes after the brace.
</action>
<verify>
<automated>cd C:/Development/PianoApp2 && npx eslint src/hooks/useAudioEngine.js 2>&1 | grep -c "no-console" | grep "^0$"</automated>
</verify>
<acceptance_criteria> - `npx eslint src/hooks/useAudioEngine.js` produces zero warnings containing "no-console" - `npx eslint src/hooks/useAudioEngine.js` produces zero warnings containing "Unused eslint-disable directive" - Line containing `console.debug("[createMetronomeClick]"` also contains `// eslint-disable-line no-console` on the same line - The next line after the console.debug line does NOT contain `eslint-disable` - `npm run test:run` still passes (no functional regression)
</acceptance_criteria>
<done>ESLint reports zero no-console warnings and zero unused-directive warnings on useAudioEngine.js. The eslint-disable-line comment is on the same line as console.debug.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build pipeline and update planning docs for milestone completion</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — verify all phase statuses)
    - .planning/STATE.md (check current position and progress counters)
    - .planning/REQUIREMENTS.md (check CLEAN-01 status)
  </read_first>
  <action>
1. **Verify build passes**: Run `npm run build` and confirm it succeeds end-to-end (prebuild trail validation + vite build). This confirms the SVG import issue from Phase 7 (commit 06ed77b) is resolved and the prebuild hook works.

2. **Update ROADMAP.md**: After plan execution completes, update the Phase 16 entry:
   - Change `- [ ] 16-01-PLAN.md` to `- [x] 16-01-PLAN.md`
   - Change `**Plans:** 0/1 plans complete` to `**Plans:** 1/1 plans complete`
   - In the Progress table, change Phase 16 row from `| 16. Milestone Cleanup | v3.0 | 0/1 | Pending |` to `| 16. Milestone Cleanup | v3.0 | 1/1 | Complete | {today's date} |`
   - Verify these exact values in the progress table are correct:
     - Phase 12: `2/2`, `Complete`, `2026-03-30`
     - Phase 13: `2/2`, `Complete`, `2026-03-31`
     - Phase 14: `1/1`, `Complete`, `2026-03-31`
     - Phase 15: `5/5`, `Complete`, `2026-03-31`
     - Phase 16: `1/1`, `Complete`, `{execution date}`
   - Update the `**Total:**` line at bottom of progress table to say: `**Total: 20 milestones shipped (v1.0-v2.9), v3.0 complete -- 5 phases, 15 requirements**`

3. **Update STATE.md**: Update to reflect Phase 16 completion:
   - Set `status: complete` (milestone done)
   - Set `stopped_at: Completed 16-01-PLAN.md`
   - Set `completed_phases: 5` (was 3 in old STATE, should reflect all 5 phases of v3.0 — 12 through 16)
   - Note: total_plans should be updated to reflect actual completed plan count
   - Set `Current focus` to reflect v3.0 complete
   - Clear `Next action` or set to indicate milestone is done

4. **Update REQUIREMENTS.md**: Change CLEAN-01 from `- [ ]` to `- [x]` and update the traceability table row for CLEAN-01 from `Pending` to `Complete`.
   </action>
   <verify>
   <automated>cd C:/Development/PianoApp2 && npm run build 2>&1 | tail -3 | grep -c "built in" | grep "^1$"</automated>
   </verify>
   <acceptance_criteria> - `npm run build` exits with code 0 (prebuild validation + vite build both pass) - ROADMAP.md Phase 16 row in progress table contains `1/1` and `Complete` - ROADMAP.md Phase 13 row contains `2/2` (not `1/2`) - ROADMAP.md does not contain the string `In Progress` for any v3.0 phase (12-16) except the v3.0 milestone header itself if still mid-execution - REQUIREMENTS.md contains `[x] **CLEAN-01**` - REQUIREMENTS.md traceability table has CLEAN-01 status as `Complete` - STATE.md `completed_phases` value is `5`
   </acceptance_criteria>
   <done>Build passes end-to-end. ROADMAP accurately reflects all v3.0 phase statuses with correct plan counts. REQUIREMENTS.md shows CLEAN-01 complete. STATE.md reflects milestone completion.</done>
   </task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. `npx eslint src/hooks/useAudioEngine.js` — zero warnings
2. `npm run build` — exits 0
3. `grep "eslint-disable-line no-console" src/hooks/useAudioEngine.js` — returns the console.debug line
4. `grep "CLEAN-01" .planning/REQUIREMENTS.md` — shows `[x]` and `Complete`
5. `grep "16. Milestone Cleanup" .planning/ROADMAP.md` — shows `Complete`
   </verification>

<success_criteria>

- ESLint produces zero no-console or unused-directive warnings on useAudioEngine.js
- npm run build succeeds with prebuild trail validation passing
- ROADMAP progress table has accurate statuses for all 5 v3.0 phases
- REQUIREMENTS.md CLEAN-01 marked complete
- STATE.md reflects v3.0 milestone completion
  </success_criteria>

<output>
After completion, create `.planning/phases/16-milestone-cleanup/16-01-SUMMARY.md`
</output>
