---
phase: 23-kid-friendly-dashboard-redesign
plan: 03
type: execute
wave: 1
depends_on: ["23-02"]
files_modified:
  - src/components/layout/Dashboard.jsx
  - src/components/ui/Fireflies.jsx
autonomous: true
gap_closure: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Level badge displays 'Level X' text (not 'LV.X') in a horizontal pill layout with avatar on the left"
    - "Fireflies render as static dots (no animation loop) when reducedMotion is true via app AccessibilityContext"
  artifacts:
    - path: "src/components/layout/Dashboard.jsx"
      provides: "Horizontal avatar+pill level badge layout, 'Level {level}' text"
      contains: "Level"
    - path: "src/components/ui/Fireflies.jsx"
      provides: "Accessibility-aware sparkle animation that stops when reducedMotion is true"
      contains: "useAccessibility"
  key_links:
    - from: "src/components/ui/Fireflies.jsx"
      to: "src/contexts/AccessibilityContext.jsx"
      via: "useAccessibility hook import"
      pattern: "useAccessibility"
    - from: "src/components/layout/Dashboard.jsx"
      to: "src/components/ui/Fireflies.jsx"
      via: "JSX rendering in hero section"
      pattern: "<Fireflies"
---

<objective>
Close 2 UAT gaps from Phase 23 dashboard redesign: (1) refactor the hero level badge from a tiny absolute-positioned "LV.X" corner chip to a horizontal avatar-left pill-right "LEVEL X" layout matching the reference design, and (2) make Fireflies.jsx respect the app's AccessibilityContext reducedMotion setting by rendering static dots instead of running framer-motion animation loops.

Purpose: UAT revealed the level badge text and layout do not match the user's reference screenshot, and the hero sparkle animation runs even when reduced motion is enabled in app settings.
Output: Updated Dashboard.jsx hero section and Fireflies.jsx component.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-kid-friendly-dashboard-redesign/23-UAT.md
@.planning/phases/23-kid-friendly-dashboard-redesign/23-02-SUMMARY.md

@src/components/layout/Dashboard.jsx
@src/components/ui/Fireflies.jsx
@src/contexts/AccessibilityContext.jsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/contexts/AccessibilityContext.jsx:
```javascript
export const useAccessibility = () => { ... };
// Returns: { reducedMotion: boolean, ... }
```

Current Fireflies signature (line 31):
```javascript
export default function Fireflies({ count = 5, positions, className = "" })
```

Current Dashboard hero avatar+badge (lines 591-618 for avatarUrl case, lines 621-628 for fallback):
- Avatar: 16x16 rounded-full in a Link to="/avatars"
- Badge: absolute -bottom-1 -right-2, text "LV.{level}"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor hero level badge to horizontal avatar+pill layout</name>
  <files>src/components/layout/Dashboard.jsx</files>
  <action>
Refactor the hero section (lines 581-638) to replace the centered flex-col avatar-with-tiny-corner-badge pattern with a horizontal avatar+pill layout:

**Layout structure (replaces lines 581-638 content inside the z-20 div):**
Keep the small app name span at top. Below it, create a horizontal flex row:

1. Avatar circle (left side): Keep the existing 16x16 (h-16 w-16) rounded-full avatar with border-2 border-indigo-400/60, including the accessory layer rendering. Wrap in the same Link to="/avatars". Remove the absolute-positioned badge span from inside the Link.

2. Pill badge (right side, overlapping avatar): Create a flex row container for the avatar+pill pair. The pill is a rounded-full element with bg-indigo-600/80 backdrop-blur-sm border border-indigo-400/40. It should visually overlap the avatar's right edge using a negative margin (e.g., -ml-3 for LTR, -mr-3 for RTL). The pill contains the text "Level {level}" (not "LV.{level}") in uppercase tracking-wider text-xs font-bold text-white. For RTL, reverse the flex direction so the pill extends left from the avatar.

3. Keep the "Hi, [Name]!" greeting h1 below, centered.

**Specific changes:**
- Lines 616-618 (avatarUrl badge): Remove entirely. Replace with pill in new layout.
- Lines 625-627 (fallback badge): Remove entirely. Replace with pill in new layout.
- Line 617: Change text from `LV.{level}` to `Level {level}` (and apply uppercase via CSS).
- Line 626: Same change for the fallback (no-avatar) case.

**Both avatar cases (with image and fallback emoji) must use the same horizontal layout pattern.** Extract the shared pill element or duplicate it in both branches.

The pill text should read "LEVEL {level}" visually (use `uppercase` Tailwind class on a span containing `Level {level}`). The pill should have horizontal padding (px-4 py-1) to create the elongated shape. The pill height should roughly match the avatar height for visual balance.

Do NOT change any data hooks, modal logic, or anything outside the hero header section.
  </action>
  <verify>
    <automated>cd "C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2" && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>
- Level badge text reads "LEVEL X" (uppercase) instead of "LV.X"
- Avatar is on the left, pill badge extends to the right (or left in RTL)
- Both avatar-present and fallback-emoji cases use the same horizontal layout
- No other Dashboard functionality changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Fireflies reduced motion to use AccessibilityContext</name>
  <files>src/components/ui/Fireflies.jsx</files>
  <action>
Replace the framer-motion `useReducedMotion()` hook with the app's `useAccessibility()` context hook from `src/contexts/AccessibilityContext.jsx`.

**Changes to Fireflies.jsx:**

1. Remove `useReducedMotion` from the framer-motion import (line 2). Keep `motion` if needed, but see step 3.

2. Add import: `import { useAccessibility } from '../../contexts/AccessibilityContext';`

3. Replace line 32 (`const prefersReducedMotion = useReducedMotion();`) with:
   `const { reducedMotion } = useAccessibility();`

4. **When `reducedMotion` is true, render static dots instead of animated motion.divs.** In the return JSX (lines 82-121):
   - If `reducedMotion` is true: render plain `<div>` elements (not `motion.div`) at the same positions with the same size/opacity/style classes, but WITHOUT the `variants`, `animate`, and `custom` props. This ensures zero animation loop runs.
   - If `reducedMotion` is false: render the existing `motion.div` elements with animation as-is.

5. Update the `useMemo` dependency: replace `prefersReducedMotion` with `reducedMotion` in the resolved computation (line 58-59 amplitude calculation and line 80 deps array).

The key fix: when reduced motion is on, no `motion.div` is rendered at all, so framer-motion has nothing to animate. The dots appear as static decorative elements.
  </action>
  <verify>
    <automated>cd "C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2" && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>
- Fireflies imports useAccessibility from AccessibilityContext (not useReducedMotion from framer-motion)
- When reducedMotion is true: static div elements rendered at firefly positions (no animation loop)
- When reducedMotion is false: animated motion.div elements rendered as before
- No framer-motion useReducedMotion import remains
  </done>
</task>

</tasks>

<verification>
1. `npx vite build --mode development` completes without errors
2. Visual check: Dashboard hero shows horizontal avatar + "LEVEL X" pill layout
3. Visual check: Enable reduced motion in Accessibility settings, confirm fireflies are static dots (no movement)
4. Visual check: Disable reduced motion, confirm fireflies animate as before
</verification>

<success_criteria>
- Level badge displays "LEVEL X" in a horizontal pill extending from the avatar (not "LV.X" in a tiny corner badge)
- Fireflies component uses app's AccessibilityContext and renders static elements when reducedMotion is true
- Build passes, no regressions in other Dashboard functionality
</success_criteria>

<output>
After completion, create `.planning/phases/23-kid-friendly-dashboard-redesign/23-03-SUMMARY.md`
</output>
