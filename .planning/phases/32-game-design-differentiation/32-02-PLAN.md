---
phase: 32-game-design-differentiation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/units/rhythmUnit1Redesigned.js
  - src/data/units/rhythmUnit2Redesigned.js
  - src/data/units/rhythmUnit3Redesigned.js
  - src/data/units/rhythmUnit4Redesigned.js
  - src/data/units/rhythmUnit5Redesigned.js
  - src/data/units/rhythmUnit6Redesigned.js
  - src/data/units/rhythmUnit7Redesigned.js
  - src/data/units/rhythmUnit8Redesigned.js
  - src/config/subscriptionConfig.js
  - src/data/units/rhythmUnit7Redesigned.test.js
  - src/data/units/rhythmUnit8Redesigned.test.js
autonomous: true
requirements: [PLAY-04]
must_haves:
  truths:
    - "Mix-Up nodes rhythm_1_5, rhythm_2_5, rhythm_3_5, rhythm_6_5, rhythm_7_5, rhythm_8_5 no longer exist in any unit file"
    - "Speed Round nodes now prerequisite the Practice node (rhythm_X_4) instead of the removed Mix-Up"
    - "All 8 rhythm units have continuous prerequisite chains with no broken links"
    - "The build validator (npm run build) passes with no errors"
    - "Free tier subscription config no longer references rhythm_1_5"
  artifacts:
    - path: "src/data/units/rhythmUnit1Redesigned.js"
      provides: "6-node Unit 1 without Mix-Up"
      contains: "rhythm_1_6"
    - path: "src/config/subscriptionConfig.js"
      provides: "Updated free tier without rhythm_1_5"
  key_links:
    - from: "rhythm_X_6 (Speed Round)"
      to: "rhythm_X_4 (Practice)"
      via: "prerequisites array"
      pattern: 'prerequisites.*rhythm_\\d_4'
---

<objective>
Remove the 6 "Rhythm Patterns" (Mix-Up) nodes from rhythm Units 1, 2, 3, 6, 7, 8 and cascade the renumbering across all 8 unit files.

Purpose: PLAY-04 requires rhythm pattern nodes to be differentiated from practice nodes. Per D-11/D-12, the Mix-Up slot was aspirational filler with no distinct game type -- removal satisfies the "unique content or replaced" criterion. Shrinks affected units from 7 to 6 nodes, creating a cleaner Discovery -> Practice -> Discovery -> Practice -> Speed Round -> Boss flow.

Output: 8 updated unit files, updated subscription config, updated unit tests, clean build.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-game-design-differentiation/32-CONTEXT.md

<interfaces>
<!-- Current unit structure pattern (all 8 units follow this) -->

Each unit file has:

```javascript
const UNIT_ID = N;
const UNIT_NAME = "...";
const CATEGORY = "rhythm";
const START_ORDER = X; // Comment: After Unit N-1's Y nodes (range)
```

Node order field: `order: START_ORDER + (orderInUnit - 1)`

Current START_ORDER values and node counts:

- Unit 1: START_ORDER=100, 7 nodes (100-106)
- Unit 2: START_ORDER=107, 7 nodes (107-113)
- Unit 3: START_ORDER=114, 7 nodes (114-120)
- Unit 4: START_ORDER=121, 7 nodes (121-127) -- NO Mix-Up to remove
- Unit 5: START_ORDER=128, 7 nodes (128-134) -- NO Mix-Up to remove
- Unit 6: START_ORDER=135, 7 nodes (135-141)
- Unit 7: START_ORDER=142, 7 nodes (142-148)
- Unit 8: START_ORDER=149, 7 nodes (149-155)

Mix-Up node IDs to DELETE: rhythm_1_5, rhythm_2_5, rhythm_3_5, rhythm_6_5, rhythm_7_5, rhythm_8_5

Speed Round nodes that need prereq re-wire:

- rhythm_1_6: prereq rhythm_1_5 -> rhythm_1_4
- rhythm_2_6: prereq rhythm_2_5 -> rhythm_2_4
- rhythm_3_6: prereq rhythm_3_5 -> rhythm_3_4
- rhythm_6_6: prereq rhythm_6_5 -> rhythm_6_4
- rhythm_7_6: prereq rhythm_7_5 -> rhythm_7_4
- rhythm_8_6: prereq rhythm_8_5 -> rhythm_8_4

From subscriptionConfig.js:

```javascript
export const FREE_RHYTHM_NODE_IDS = [
  "rhythm_1_1",
  "rhythm_1_2",
  "rhythm_1_3",
  "rhythm_1_4",
  "rhythm_1_5", // <-- DELETE this line
  "rhythm_1_6",
];
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Mix-Up nodes from Units 1-3 and cascade START_ORDER</name>
  <read_first>
    - src/data/units/rhythmUnit1Redesigned.js
    - src/data/units/rhythmUnit2Redesigned.js
    - src/data/units/rhythmUnit3Redesigned.js
    - src/data/units/rhythmUnit4Redesigned.js
  </read_first>
  <files>
    src/data/units/rhythmUnit1Redesigned.js,
    src/data/units/rhythmUnit2Redesigned.js,
    src/data/units/rhythmUnit3Redesigned.js,
    src/data/units/rhythmUnit4Redesigned.js,
    src/data/units/rhythmUnit5Redesigned.js
  </files>
  <action>
**For each of Units 1, 2, 3: perform the same 4-step edit pattern.**

**rhythmUnit1Redesigned.js:**

1. DELETE the entire `rhythm_1_5` node object (the Mix-Up node, currently orderInUnit 5, roughly lines 259-314). Delete the full object including its comment header.
2. On the `rhythm_1_6` (Speed Round) node: change `prerequisites: ["rhythm_1_5"]` to `prerequisites: ["rhythm_1_4"]`.
3. Renumber the Speed Round node: `orderInUnit: 6` -> `orderInUnit: 5`, `order: START_ORDER + 5` -> `order: START_ORDER + 4`.
4. Renumber the Boss node (`boss_rhythm_1`): `orderInUnit: 7` -> `orderInUnit: 6`, `order: START_ORDER + 6` -> `order: START_ORDER + 5`.
5. Update the file header comment: change "7 nodes" to "6 nodes" in the description.
6. START_ORDER stays at 100 (Unit 1 is the anchor).

**rhythmUnit2Redesigned.js:**

1. DELETE the entire `rhythm_2_5` node object (Mix-Up).
2. On `rhythm_2_6` (Speed Round): change `prerequisites: ["rhythm_2_5"]` to `prerequisites: ["rhythm_2_4"]`.
3. Renumber Speed Round: `orderInUnit: 6` -> `5`, `order: START_ORDER + 5` -> `START_ORDER + 4`.
4. Renumber Boss (`boss_rhythm_2`): `orderInUnit: 7` -> `6`, `order: START_ORDER + 6` -> `START_ORDER + 5`.
5. Change `const START_ORDER = 107;` to `const START_ORDER = 106;` with comment `// After Unit 1's 6 nodes (100-105)`.
6. Update header comment: "7 nodes" -> "6 nodes".

**rhythmUnit3Redesigned.js:**

1. DELETE the entire `rhythm_3_5` node object (Mix-Up).
2. On `rhythm_3_6` (Speed Round): change `prerequisites: ["rhythm_3_5"]` to `prerequisites: ["rhythm_3_4"]`.
3. Renumber Speed Round: `orderInUnit: 6` -> `5`, `order: START_ORDER + 5` -> `START_ORDER + 4`.
4. Renumber Boss (`boss_rhythm_3`): `orderInUnit: 7` -> `6`, `order: START_ORDER + 6` -> `START_ORDER + 5`.
5. Change `const START_ORDER = 114;` to `const START_ORDER = 112;` with comment `// After Unit 2's 6 nodes (106-111)`.
6. Update header comment: "7 nodes" -> "6 nodes".

**rhythmUnit4Redesigned.js (START_ORDER only -- no node removal):**
Change `const START_ORDER = 121;` to `const START_ORDER = 118;` with comment `// After Unit 3's 6 nodes (112-117)`.

**rhythmUnit5Redesigned.js (START_ORDER only -- no node removal):**
Change `const START_ORDER = 128;` to `const START_ORDER = 125;` with comment `// After Unit 4's 7 nodes (118-124)`.
</action>
<verify>
<automated>cd C:/Development/PianoApp2 && node -e "
const {rhythmUnit1Nodes} = await import('./src/data/units/rhythmUnit1Redesigned.js');
const {rhythmUnit2Nodes} = await import('./src/data/units/rhythmUnit2Redesigned.js');
const {rhythmUnit3Nodes} = await import('./src/data/units/rhythmUnit3Redesigned.js');
const u1ids = rhythmUnit1Nodes.map(n=>n.id);
const u2ids = rhythmUnit2Nodes.map(n=>n.id);
const u3ids = rhythmUnit3Nodes.map(n=>n.id);
console.assert(rhythmUnit1Nodes.length===6, 'U1 should have 6 nodes');
console.assert(rhythmUnit2Nodes.length===6, 'U2 should have 6 nodes');
console.assert(rhythmUnit3Nodes.length===6, 'U3 should have 6 nodes');
console.assert(!u1ids.includes('rhythm_1_5'), 'rhythm_1_5 should be gone');
console.assert(!u2ids.includes('rhythm_2_5'), 'rhythm_2_5 should be gone');
console.assert(!u3ids.includes('rhythm_3_5'), 'rhythm_3_5 should be gone');
const u1speed = rhythmUnit1Nodes.find(n=>n.id==='rhythm_1_6');
console.assert(u1speed.prerequisites[0]==='rhythm_1_4', 'U1 speed prereq should be rhythm_1_4');
console.log('Units 1-3 validated OK');
" --input-type=module</automated>
</verify>
<acceptance_criteria> - `rhythmUnit1Redesigned.js` does NOT contain `rhythm_1_5` - `rhythmUnit2Redesigned.js` does NOT contain `rhythm_2_5` - `rhythmUnit3Redesigned.js` does NOT contain `rhythm_3_5` - `rhythmUnit1Redesigned.js` contains `prerequisites: ["rhythm_1_4"]` on the rhythm_1_6 node - `rhythmUnit2Redesigned.js` contains `const START_ORDER = 106;` - `rhythmUnit3Redesigned.js` contains `const START_ORDER = 112;` - `rhythmUnit4Redesigned.js` contains `const START_ORDER = 118;` - `rhythmUnit5Redesigned.js` contains `const START_ORDER = 125;`
</acceptance_criteria>
<done>Units 1-3 have Mix-Up nodes removed with re-wired prerequisites and renumbered orders; Units 4-5 have cascaded START_ORDER values</done>
</task>

<task type="auto">
  <name>Task 2: Remove Mix-Up nodes from Units 6-8, update subscriptionConfig, and fix tests</name>
  <read_first>
    - src/data/units/rhythmUnit6Redesigned.js
    - src/data/units/rhythmUnit7Redesigned.js
    - src/data/units/rhythmUnit8Redesigned.js
    - src/config/subscriptionConfig.js
    - src/data/units/rhythmUnit7Redesigned.test.js
    - src/data/units/rhythmUnit8Redesigned.test.js
  </read_first>
  <files>
    src/data/units/rhythmUnit6Redesigned.js,
    src/data/units/rhythmUnit7Redesigned.js,
    src/data/units/rhythmUnit8Redesigned.js,
    src/config/subscriptionConfig.js,
    src/data/units/rhythmUnit7Redesigned.test.js,
    src/data/units/rhythmUnit8Redesigned.test.js
  </files>
  <action>
**rhythmUnit6Redesigned.js:**
1. DELETE the entire `rhythm_6_5` node object (Mix-Up).
2. On `rhythm_6_6` (Speed Round): change `prerequisites: ["rhythm_6_5"]` to `prerequisites: ["rhythm_6_4"]`.
3. Renumber Speed Round: `orderInUnit: 6` -> `5`, `order: START_ORDER + 5` -> `START_ORDER + 4`.
4. Renumber Boss (`boss_rhythm_6`): `orderInUnit: 7` -> `6`, `order: START_ORDER + 6` -> `START_ORDER + 5`.
5. Change `const START_ORDER = 135;` to `const START_ORDER = 132;` with comment `// After Unit 5's 7 nodes (125-131)`.
6. Update header comment: "7 nodes" -> "6 nodes".

**rhythmUnit7Redesigned.js:**

1. DELETE the entire `rhythm_7_5` node object (Mix-Up).
2. On `rhythm_7_6` (Speed Round): change `prerequisites: ["rhythm_7_5"]` to `prerequisites: ["rhythm_7_4"]`.
3. Renumber Speed Round: `orderInUnit: 6` -> `5`, `order: START_ORDER + 5` -> `START_ORDER + 4`.
4. Renumber Boss (`boss_rhythm_7`): `orderInUnit: 7` -> `6`, `order: START_ORDER + 6` -> `START_ORDER + 5`.
5. Change `const START_ORDER = 142;` to `const START_ORDER = 138;` with comment `// After Unit 6's 6 nodes (132-137)`.
6. Update header comment: "7 nodes" -> "6 nodes".

**rhythmUnit8Redesigned.js:**

1. DELETE the entire `rhythm_8_5` node object (Mix-Up).
2. On `rhythm_8_6` (Speed Round): change `prerequisites: ["rhythm_8_5"]` to `prerequisites: ["rhythm_8_4"]`.
3. Renumber Speed Round: `orderInUnit: 6` -> `5`, `order: START_ORDER + 5` -> `START_ORDER + 4`.
4. Renumber Boss (`boss_rhythm_8`): `orderInUnit: 7` -> `6`, `order: START_ORDER + 6` -> `START_ORDER + 5`.
5. Change `const START_ORDER = 149;` to `const START_ORDER = 144;` with comment `// After Unit 7's 6 nodes (138-143)`.
6. Update header comment: "7 nodes" -> "6 nodes".

**subscriptionConfig.js:**

1. Remove `'rhythm_1_5',` from the `FREE_RHYTHM_NODE_IDS` array (line 52). The array should become:

```javascript
export const FREE_RHYTHM_NODE_IDS = [
  "rhythm_1_1",
  "rhythm_1_2",
  "rhythm_1_3",
  "rhythm_1_4",
  "rhythm_1_6",
];
```

2. Update comment `/** Rhythm Unit 1 -- 6 free nodes */` to `/** Rhythm Unit 1 -- 5 free nodes */`.
3. Update `FREE_TIER_SUMMARY.rhythm.count` from `6` to `5`.
4. Update `FREE_TIER_SUMMARY.total` from `25` to `24`.
5. Update the verification header comment to reflect the new node range: `nodes rhythm_1_1 through rhythm_1_6` stays correct (rhythm_1_6 is the Speed Round, still exists). But add a note that rhythm_1_5 was removed in Phase 32.

**rhythmUnit7Redesigned.test.js:**

1. Change `expect(rhythmUnit7Nodes).toHaveLength(7)` to `expect(rhythmUnit7Nodes).toHaveLength(6)`.
2. Change `expect(new Set(ids).size).toBe(7)` to `expect(new Set(ids).size).toBe(6)`.
3. Update the expected IDs array to remove `"rhythm_7_5"`:

```javascript
expect(ids).toEqual([
  "rhythm_7_1",
  "rhythm_7_2",
  "rhythm_7_3",
  "rhythm_7_4",
  "rhythm_7_6",
  "boss_rhythm_7",
]);
```

4. Update the expected orders: with START_ORDER=138, 6 nodes -> `[138, 139, 140, 141, 142, 143]`.
5. Update the exercise type assertion to remove the mix_up entry:

```javascript
expect(types).toEqual([
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_7_1 (discovery)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_7_2 (practice)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_7_3 (discovery)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_7_4 (practice)
  EXERCISE_TYPES.ARCADE_RHYTHM, // rhythm_7_6 (speed_round)
  EXERCISE_TYPES.MIXED_LESSON, // boss_rhythm_7 (mini_boss)
]);
```

6. Update the `for (let i = 0; i < 6; i++)` loop for regular nodes to `for (let i = 0; i < 5; i++)` (5 regular + 1 boss).
7. Update the xpReward range assertion: the loop should iterate over indices 0-4 (5 regular nodes), and the boss is at index 5: `expect(rhythmUnit7Nodes[5].xpReward).toBe(150);`.

**rhythmUnit8Redesigned.test.js:**

1. Change `expect(rhythmUnit8Nodes).toHaveLength(7)` to `expect(rhythmUnit8Nodes).toHaveLength(6)`.
2. Change `expect(new Set(ids).size).toBe(7)` to `expect(new Set(ids).size).toBe(6)`.
3. Update expected IDs array to remove `"rhythm_8_5"`:

```javascript
const expectedIds = [
  "rhythm_8_1",
  "rhythm_8_2",
  "rhythm_8_3",
  "rhythm_8_4",
  "rhythm_8_6",
  "boss_rhythm_8",
];
```

4. Update expected orders: with START_ORDER=144, 6 nodes -> `[144, 145, 146, 147, 148, 149]`.
5. Update the exercise type assertion for regular nodes: `const regularNodes = rhythmUnit8Nodes.slice(0, 5);` (was 6), and the expected types become:

```javascript
expect(types).toEqual([
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_8_1 (discovery)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_8_2 (practice)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_8_3 (discovery)
  EXERCISE_TYPES.MIXED_LESSON, // rhythm_8_4 (practice)
  EXERCISE_TYPES.ARCADE_RHYTHM, // rhythm_8_6 (speed_round)
]);
```

6. Update boss test: boss is now at index 5 (last element), so `rhythmUnit8Nodes[rhythmUnit8Nodes.length - 1]` stays correct. But the `"boss has 1 MIXED_LESSON exercise with 12 questions"` assertion stays.
7. Update `regularNodes = rhythmUnit8Nodes.slice(0, 5)` everywhere it appears (was `slice(0, 6)`).
8. Update tempo comparison: `rhythmUnit8Nodes[5]` for last regular node's default tempo (was index 5, still index 4 after removal — the speed round which was index 5 is now index 4): `const lastRegularTempo = rhythmUnit8Nodes[4].rhythmConfig.tempo.default;`
   </action>
   <verify>
   <automated>cd C:/Development/PianoApp2 && npm run verify:trail && npx vitest run src/data/units/rhythmUnit7Redesigned.test.js src/data/units/rhythmUnit8Redesigned.test.js</automated>
   </verify>
   <acceptance_criteria> - `rhythmUnit6Redesigned.js` does NOT contain `rhythm_6_5` - `rhythmUnit7Redesigned.js` does NOT contain `rhythm_7_5` - `rhythmUnit8Redesigned.js` does NOT contain `rhythm_8_5` - `rhythmUnit6Redesigned.js` contains `const START_ORDER = 132;` - `rhythmUnit7Redesigned.js` contains `const START_ORDER = 138;` - `rhythmUnit8Redesigned.js` contains `const START_ORDER = 144;` - `subscriptionConfig.js` does NOT contain `rhythm_1_5` - `subscriptionConfig.js` contains `total: 24` - `rhythmUnit7Redesigned.test.js` contains `toHaveLength(6)` - `rhythmUnit8Redesigned.test.js` contains `toHaveLength(6)` - `npm run verify:trail` exits 0 - `npx vitest run src/data/units/rhythmUnit7Redesigned.test.js src/data/units/rhythmUnit8Redesigned.test.js` exits 0
   </acceptance_criteria>
   <done>Units 6-8 have Mix-Up nodes removed, subscriptionConfig updated, both unit test files updated and passing, trail validator passes</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries. Node removal is a data-layer change. Orphaned DB rows are tolerated per D-14.

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                             |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-32-02   | Information Disclosure | subscriptionConfig.js rhythm_1_5 removal | accept      | DB `is_free_node()` still includes rhythm_1_5 (harmless — node no longer exists). No DB migration per D-14. |
| T-32-03   | Denial of Service      | Orphaned student_skill_progress rows     | accept      | getNodeById() returns undefined for removed IDs; UI skips them. No crash path.                              |

</threat_model>

<verification>
1. `npm run build` succeeds (prebuild validator checks prereq chains, cycles, duplicate IDs)
2. `npm run verify:trail` passes
3. `npx vitest run src/data/units/rhythmUnit7Redesigned.test.js src/data/units/rhythmUnit8Redesigned.test.js` passes
4. grep confirms no unit file contains any of the 6 removed node IDs as `id:` values
5. grep confirms all Speed Round prerequisites point to `rhythm_X_4` not `rhythm_X_5`
</verification>

<success_criteria>
All 6 Mix-Up nodes are fully deleted, prerequisites are re-wired, START_ORDER cascade is correct across all 8 units, subscription config is updated, tests pass, and `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/32-game-design-differentiation/32-02-SUMMARY.md`
</output>
