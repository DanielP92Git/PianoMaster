---
phase: 33-rhythm-issues-cleanup
plan: 05
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - supabase/migrations/20260201000002_add_rate_limiting.sql
autonomous: false
requirements_addressed: []
contingent: false
user_setup:
  - service: supabase
    why: "Apply rate-limit migration to remote Supabase project (deploys check_rate_limit RPC + rate_limits table)"
    env_vars: []
    dashboard_config:
      - task: "Verify migration applied successfully via Supabase Dashboard → Database → Functions; confirm check_rate_limit() exists"
        location: "https://supabase.com/dashboard → Project → Database → Functions"
must_haves:
  truths:
    - "Migration 20260201000002_add_rate_limiting.sql is applied to the remote Supabase project (production)"
    - "After deploy, browser console no longer shows 'Rate limit function not found in database' warning when student completes a node"
    - "Migration is idempotent (DROP POLICY IF EXISTS guards added per RESEARCH §7) so it can be re-run safely without policy collision errors"
    - "D-07 (rate-limit migration in scope) honored"
    - "UAT issue 6 marked resolved-by-deploy"
  artifacts:
    - path: "supabase/migrations/20260201000002_add_rate_limiting.sql"
      provides: "Hardened migration with DROP POLICY IF EXISTS guards"
      contains: "DROP POLICY IF EXISTS"
  key_links:
    - from: "Remote Supabase project"
      to: "frontend rate-limit check call site"
      via: "check_rate_limit RPC"
      pattern: "check_rate_limit"
---

<objective>
Apply the rate-limit migration to the remote Supabase project, with idempotency hardening per RESEARCH §7. This closes the console 404 + warning surfaced by UAT Issue 6.

Purpose: D-07 puts the rate-limit migration explicitly in scope. RESEARCH §7 confirmed migration is well-designed (SECURITY DEFINER, advisory lock, RLS) but flagged 3 `CREATE POLICY` statements that throw on re-run. Hardening makes the migration safe to re-run if a partial-apply ever occurs (R5 Risk Register).

This plan is `autonomous: false` because:

1. `supabase db push` to a remote project is a hard-to-reverse ops action; the user must explicitly approve before deploy.
2. The user must verify the migration applied via dashboard or CLI, then retest the console.

Output: Migration file hardened in repo + applied to remote Supabase + console clean post-deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-rhythm-issues-cleanup/33-CONTEXT.md
@.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md
@.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md
@.planning/phases/33-rhythm-issues-cleanup/33-UAT.md
@supabase/migrations/20260201000002_add_rate_limiting.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DROP POLICY IF EXISTS guards to migration file</name>
  <files>supabase/migrations/20260201000002_add_rate_limiting.sql</files>
  <read_first>
    - C:/Development/PianoApp2/supabase/migrations/20260201000002_add_rate_limiting.sql (full file — locate the 3 CREATE POLICY statements per RESEARCH §7 idempotency table)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md (§7 — Rate-Limit Migration Audit; idempotency table; suggested hardening block)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md (§11 — Rate-limit migration deploy)
  </read_first>
  <action>
    Open `supabase/migrations/20260201000002_add_rate_limiting.sql`.

    Per RESEARCH §7, locate each of the 3 `CREATE POLICY` statements:
    1. `CREATE POLICY "Students can view own rate limits" ON rate_limits ...`
    2. `CREATE POLICY "Students can insert own rate limits" ON rate_limits ...`
    3. `CREATE POLICY "Students can update own rate limits" ON rate_limits ...`

    Immediately BEFORE each `CREATE POLICY`, insert a matching `DROP POLICY IF EXISTS ... ON rate_limits;` statement. Resulting block per policy:

    ```sql
    -- D-07 (Phase 33): DROP IF EXISTS guard so partial-apply recovery doesn't fail
    DROP POLICY IF EXISTS "Students can view own rate limits" ON rate_limits;
    CREATE POLICY "Students can view own rate limits"
      ON rate_limits
      FOR SELECT
      ...
    ```

    Repeat for the other two policies (matching the exact policy name string verbatim from the existing CREATE POLICY).

    Do NOT change:
    - Table DDL (already idempotent via IF NOT EXISTS)
    - Index DDL (already idempotent)
    - GRANT statements (already idempotent)
    - The `check_rate_limit` function body (already uses CREATE OR REPLACE)
    - The advisory lock logic (RESEARCH §7 confirms it's well-designed)
    - COMMENT statements

    Add a top-of-file comment after the existing migration header (if any):
    ```sql
    -- 2026-05-03 (Phase 33 Plan 33-05): Added DROP POLICY IF EXISTS guards to make policies
    -- idempotent — protects against re-run after partial-apply. Per .planning RESEARCH §7.
    ```

  </action>
  <verify>
    <automated>grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260201000002_add_rate_limiting.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DROP POLICY IF EXISTS" supabase/migrations/20260201000002_add_rate_limiting.sql` returns 3 (one per policy)
    - `grep -c "CREATE POLICY" supabase/migrations/20260201000002_add_rate_limiting.sql` still returns 3 (we did not delete any CREATE POLICY)
    - Each DROP POLICY name string matches its corresponding CREATE POLICY name string exactly
    - File is still valid SQL: visual inspection confirms no syntax errors
  </acceptance_criteria>
  <done>Migration file has DROP POLICY IF EXISTS guards before all 3 CREATE POLICY statements; ready for local dry-run</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Local dry-run + remote deploy of migration</name>
  <what-built>
    Task 1 produced a hardened migration file. Now it must be applied to the remote Supabase project. This requires Supabase CLI authentication and explicit user approval per CLAUDE.md "Service worker auth endpoints + Supabase REST API are never cached" / production data risk.
  </what-built>
  <how-to-verify>
    The user (or Claude with user approval and `dangerouslyDisableSandbox: true` if running interactively) executes the deploy sequence:

    **Step 2.A — Local dry-run (verifies migration applies cleanly):**

    ```bash
    npx supabase --version
    # Confirm Supabase CLI is installed and authenticated. If not, install via:
    # npm install --save-dev supabase
    # Then: npx supabase login
    ```

    ```bash
    npx supabase start
    # Starts local Supabase stack
    npx supabase db reset
    # Applies all migrations (including 20260201000002) from scratch to local DB
    ```

    Expected: `db reset` completes with no errors. The migration applies in order, and the policies/function/table all exist in the local DB. Verify via:
    ```bash
    npx supabase db diff
    # Should be empty after reset (local matches migration history)
    ```

    **Step 2.B — Remote deploy (HARD-TO-REVERSE):**

    Confirm the user explicitly wants to deploy NOW. Then:
    ```bash
    npx supabase db push
    # Pushes new migrations to the linked remote project (production)
    ```

    Expected: Migration `20260201000002_add_rate_limiting.sql` shown as applied. No policy-collision errors (DROP POLICY IF EXISTS guards prevent them).

    **Step 2.C — Verify in dashboard:**

    Visit https://supabase.com/dashboard → Project → Database → Functions → confirm `check_rate_limit` exists.
    Visit Database → Tables → confirm `rate_limits` exists.
    Visit Database → Policies → confirm 3 policies on `rate_limits`.

    **Step 2.D — Frontend retest (UAT issue 6):**

    Reload the deployed app. Open browser DevTools console. Sign in as student → /trail → complete any node. Confirm:
    - NO "Rate limit function not found in database. Allowing request." warning in console
    - NO 404 from the rate-limit RPC call

    Mark `[x] resolved-by-deploy` on UAT issue 6 in `33-UAT.md`.

    **Step 2.E — Stop on errors.**

    If `npx supabase db push` fails with any error (auth, network, schema collision):
    - Do NOT retry destructively
    - Capture the error, share with user
    - Investigate locally first via `npx supabase db reset`
    - Per RESEARCH §7, if collision is on a CREATE POLICY despite the DROP POLICY IF EXISTS guards, something else is malformed — investigate, don't force-push

  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "Migration applied successfully. UAT issue 6 marked resolved-by-deploy. Console clean."
    - "Migration applied but UAT issue 6 still shows warning. <error log>"
    - "Migration failed. <error message>. Need to investigate before retry."
  </resume-signal>
</task>

</tasks>

<verification>
- Migration file modified in repo with DROP POLICY IF EXISTS guards (Task 1)
- Migration applied to remote Supabase via `npx supabase db push` (Task 2)
- `check_rate_limit` RPC visible in dashboard
- Browser console no longer warns about missing rate-limit function (Task 2.D)
- UAT issue 6 marked `[x] resolved-by-deploy` in 33-UAT.md
</verification>

<success_criteria>
Plan complete when:

1. Migration file hardened (DROP POLICY IF EXISTS × 3)
2. Local dry-run via `supabase db reset` passes cleanly
3. Remote deploy via `supabase db push` succeeds
4. Frontend console post-deploy shows no rate-limit warning
5. UAT issue 6 marked resolved-by-deploy
   </success_criteria>

<output>
After completion, create `.planning/phases/33-rhythm-issues-cleanup/33-05-SUMMARY.md` documenting:
- Migration file diff summary (3 DROP POLICY IF EXISTS lines added)
- Local dry-run result
- Remote deploy timestamp + project ref
- Console verification result post-deploy
- UAT issue 6 mark status
</output>
