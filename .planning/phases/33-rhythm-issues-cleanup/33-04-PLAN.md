---
phase: 33-rhythm-issues-cleanup
plan: 04
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - src/data/units/rhythmUnit2Redesigned.js
  - src/data/units/rhythmUnit4Redesigned.js
  - src/data/units/rhythmUnit6Redesigned.js
  - src/data/units/rhythmUnit8Redesigned.js
  - src/data/patterns/rhythmPatterns.js
autonomous: true
requirements_addressed: [DATA-01, DATA-03]
contingent: false
must_haves:
  truths:
    - "rhythm_4_6 'Speed Silence' durations array contains 'w' (whole note) — matches its 'whole-rest' tag and prereq's vocabulary (RESEARCH §3 Unit 4)"
    - "rhythm_2_3 'Long and Short' no longer has the inappropriate discovery_intro question with focusDuration: 'q' (RESEARCH §3 Unit 2)"
    - "rhythm_6_4 'Fast and Faster' name matches content (renamed to 'Mixed Speeds' OR description updated to clarify duration-variety not tempo-contrast — RESEARCH §3 Unit 6)"
    - "Rest-bearing patterns are removed from the 'quarter-half' tag pool in rhythmPatterns.js (D-12) — node 1_3 cannot receive rest-needing patterns regardless of which renderer path is taken"
    - "After every edit, npm run verify:trail exits 0 — no broken prereqs, no cycles, no XP drift"
    - "D-08 (full audit), D-11 (section/content title fixes via audit), D-12 (rest-pool cleanup at tag-pool level) honored"
    - "UAT issue 2/9 ready for resolved-by-deploy mark (rests removed from quarter-half pool)"
    - "UAT issue 5 (section/content title) addressed for the 4 specific drifts identified by RESEARCH §3"
  artifacts:
    - path: "src/data/units/rhythmUnit4Redesigned.js"
      provides: "rhythm_4_6 with durations: ['q','h','w','qr','hr','wr'] (added 'w')"
      contains: 'id: "rhythm_4_6"'
    - path: "src/data/units/rhythmUnit2Redesigned.js"
      provides: "rhythm_2_3 with discovery_intro question removed (or focusDuration corrected)"
      contains: 'id: "rhythm_2_3"'
    - path: "src/data/units/rhythmUnit6Redesigned.js"
      provides: "rhythm_6_4 name/description updated to match content"
      contains: 'id: "rhythm_6_4"'
    - path: "src/data/patterns/rhythmPatterns.js"
      provides: "Rest-bearing patterns removed from quarter-half tag pool"
      contains: "RHYTHM_PATTERNS"
  key_links:
    - from: "src/data/patterns/rhythmPatterns.js"
      to: "src/data/units/rhythmUnit1Redesigned.js (rhythm_1_3 patternTags: ['quarter-half'])"
      via: "tag pool cleanup propagates through resolveByTags() to all consumers"
      pattern: "patternTags.*quarter-half"
---

<objective>
Execute the data audit edits identified in RESEARCH §3 (Data Audit Findings). Fix the 4 specific drifts, run the rest-pool cleanup at the pattern-tag level (D-12), and verify trail validator stays green.

Purpose: D-08 mandates full audit; D-11 mandates section/content title fixes via audit (not user-named list); D-12 fixes node 1_3 rests at the pattern-tag pool level. RESEARCH §3 narrows the actionable surface to:

- rhythm_4_6: durations missing 'w' despite 'whole-rest' tag (Unit 4 audit)
- rhythm_2_3: inappropriate discovery_intro question (Unit 2 audit)
- rhythm_6_4: name/content mismatch — "Fast and Faster" implies tempo, content is duration variety (Unit 6 audit)
- rhythm_8_1: focusDurations drift — '8' plays plain eighths but pedagogy wants pairs/syncopation. Per RESEARCH Open Question 4, defer to user; flag for Plan 33-07 if Issue 4 confirmed-bug for 8_1 specifically.
- D-12 rest-pool cleanup in `rhythmPatterns.js`

Output: 4 unit files + rhythmPatterns.js modified. validateTrail green. UAT issues 2/9 and 5 ready for resolved-by-deploy.

This plan is `autonomous: true` because all edits are deterministic data fixes per RESEARCH §3 specifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-rhythm-issues-cleanup/33-CONTEXT.md
@.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md
@.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md
@.planning/phases/33-rhythm-issues-cleanup/33-UAT.md
@src/data/PEDAGOGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rhythm_4_6 durations + rhythm_2_3 discovery_intro removal + rhythm_6_4 rename</name>
  <files>
    src/data/units/rhythmUnit2Redesigned.js
    src/data/units/rhythmUnit4Redesigned.js
    src/data/units/rhythmUnit6Redesigned.js
  </files>
  <read_first>
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md (§3 Unit 2 table for rhythm_2_3, §3 Unit 4 table for rhythm_4_6, §3 Unit 6 table + §3 rename suggestions list for rhythm_6_4)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md (§9 — rhythm unit data file template; preserve field order)
    - C:/Development/PianoApp2/src/data/units/rhythmUnit2Redesigned.js (full file — locate rhythm_2_3 node; per RESEARCH §3 line 167 the discovery_intro has focusDuration: 'q' but the node is "contrast" not introduction)
    - C:/Development/PianoApp2/src/data/units/rhythmUnit4Redesigned.js (full file — locate rhythm_4_6; per RESEARCH §3 Unit 4 it has `durations: ['q','h','qr','hr','wr']` missing 'w' despite `patternTags: ['quarter-rest','half-rest','whole-rest']`)
    - C:/Development/PianoApp2/src/data/units/rhythmUnit6Redesigned.js (full file — locate rhythm_6_4; per RESEARCH §3 rename suggestions: name "Fast and Faster" + description "Practice mixing fast and slow rhythms" but durations are `[q,h,8,16]` — drift between name and content)
    - C:/Development/PianoApp2/src/data/units/rhythmUnit1Redesigned.js (rhythm_1_1 lines 36-88 — for canonical field order template per PATTERNS §9)
  </read_first>
  <action>
    **Edit 1.A — `src/data/units/rhythmUnit4Redesigned.js`, node `rhythm_4_6`:**

    Locate the rhythmConfig of `rhythm_4_6`. Current state (per RESEARCH §3 Unit 4):
    ```javascript
    durations: ["q", "h", "qr", "hr", "wr"],
    ```
    Change to:
    ```javascript
    durations: ["q", "h", "w", "qr", "hr", "wr"], // D-08: add 'w' — whole notes were introduced by rhythm_4_5 prereq; tag 'whole-rest' implies whole-note context
    ```
    No other field in this node changes. Preserve field order.

    **Edit 1.B — `src/data/units/rhythmUnit2Redesigned.js`, node `rhythm_2_3`:**

    Locate the `exercises` array of `rhythm_2_3`. Per RESEARCH §3 Unit 2 line 167, the MIXED_LESSON `questions` array contains a `discovery_intro` question with `focusDuration: 'q'` — but `rhythm_2_3` is a "contrast" node ("Long and Short"), NOT an introduction node. Per D-08 audit, REMOVE that question from the questions array. Keep all other questions in the array unchanged.

    Identify the exact discovery_intro entry by searching the rhythm_2_3 node block for `{ type: "discovery_intro"` and remove the entire object (and its trailing comma if present). Preserve all other questions and their order. Add a comment above the questions array:
    ```javascript
    // D-08 (Phase 33): removed inappropriate discovery_intro — rhythm_2_3 is a contrast node, not an intro
    ```

    **Edit 1.C — `src/data/units/rhythmUnit6Redesigned.js`, node `rhythm_6_4`:**

    Locate `rhythm_6_4`. Current state (per RESEARCH §3 rename suggestions):
    - `name: "Fast and Faster"`
    - `description: "Practice mixing fast and slow rhythms"` (or similar)
    - `durations: ["q","h","8","16"]`, `patternTags: ["sixteenth", "quarter-eighth"]`

    The drift: name implies tempo contrast; content is duration variety (no half/whole, no tempo split). Two options offered by RESEARCH §3 — pick (a) the SAFER edit:

    Update `name` to: `"Mixed Speeds"`
    Update `description` to: `"Practice patterns that mix quarter, half, eighth, and sixteenth notes"`

    Do NOT change `durations`, `patternTags`, or any structural field. Add a comment above the rename:
    ```javascript
    // D-11 (Phase 33): renamed from "Fast and Faster" — name implied tempo, content is duration variety
    ```

    Note: `rhythm_8_1` was also flagged in RESEARCH §3 ("Off-Beat Surprise" intro plays plain eighths). Per RESEARCH Open Question 4, defer that decision to user via UAT issue 4. Plan 33-07 handles it CONDITIONAL on UAT issue 4 marked confirmed-bug for 8_1 specifically. Do NOT edit rhythm_8_1 in this task.

  </action>
  <verify>
    <automated>npm run verify:trail</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 3 'id: "rhythm_4_6"' src/data/units/rhythmUnit4Redesigned.js | grep -c '"w"'` returns at least 1 (note: this matches occurrences across multiple lines; verify by inspection that the durations array specifically contains "w")
    - `grep -c '"q", "h", "w", "qr", "hr", "wr"' src/data/units/rhythmUnit4Redesigned.js` returns 1
    - In rhythm_2_3's exercises questions array, discovery_intro entry no longer exists. Verify: locate `id: "rhythm_2_3"` block in rhythmUnit2Redesigned.js and confirm no `discovery_intro` entry under that node's questions array. (`grep -A 50 'id: "rhythm_2_3"' src/data/units/rhythmUnit2Redesigned.js | grep -c 'discovery_intro'` should return 0 within the rhythm_2_3 block — manual inspection confirms scope.)
    - `grep -c 'name: "Mixed Speeds"' src/data/units/rhythmUnit6Redesigned.js` returns 1
    - `grep -c 'name: "Fast and Faster"' src/data/units/rhythmUnit6Redesigned.js` returns 0
    - `npm run verify:trail` exits 0
    - `npm run lint` on the 3 modified files exits 0
  </acceptance_criteria>
  <done>3 unit files corrected per RESEARCH §3 audit; trail validator green; UAT issue 5 (rename) addressed for 3 of 4 RESEARCH-flagged drifts (8_1 deferred to Plan 33-07)</done>
</task>

<task type="auto">
  <name>Task 2: D-12 rest-pool cleanup in rhythmPatterns.js</name>
  <files>src/data/patterns/rhythmPatterns.js</files>
  <read_first>
    - C:/Development/PianoApp2/src/data/patterns/rhythmPatterns.js (full file — locate every pattern with `tags: [..., 'quarter-half', ...]`; identify which of those have a binary that requires rests)
    - C:/Development/PianoApp2/src/data/patterns/RhythmPatternGenerator.js (lines 177-208 per RESEARCH §1 Issue 2 — `patternNeedsRests` helper; understand what "needs rests" means for a binary)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md (§3 Unit 1 table — rhythm_1_3 pattern audit lists qh_44_001/002/003 and qhw_44_001 as candidates; §1 Issue 2 — verify `patternNeedsRests` filter behavior)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md (§4 — central duration filter pattern; D-12 is the data-side counterpart)
  </read_first>
  <action>
    **Goal:** Per D-12, remove rest-bearing patterns from the `quarter-half` tag pool so node 1_3 (`patternTags: ['quarter-half']`, `durations: ['q','h']`) can never receive a pattern that needs rests, regardless of renderer path.

    **Step 2.1 — Audit current `quarter-half` tag pool:**

    Search `rhythmPatterns.js` for every pattern object whose `tags` array contains the string `"quarter-half"`. For each, inspect its `pattern` (binary array — 1 = onset, 0 = sustain or rest depending on context).

    Per RESEARCH §3 Unit 1 audit, these specific pattern IDs are flagged as REST-RISK in the quarter-half pool:
    - `qh_44_002` (onsets at positions 2, 3 — leading rest at position 1)
    - `qh_44_003` (onsets at positions 1, 2 — trailing rest at position 4)
    - `qhw_44_001` (single onset at position 1 — needs whole-note OR whole-rest to fill)

    The mechanism `patternNeedsRests(p.pattern, durations)` (RhythmPatternGenerator.js:177-208) determines this dynamically. The filter is already working in `resolveByTags()` (RhythmPatternGenerator.js:243-245). However, per D-12 the user wants belt-and-suspenders cleanup at the data layer too: REMOVE the `quarter-half` tag from any rest-requiring pattern (or remove the pattern entry if its only purpose is rest-bearing).

    **Step 2.2 — Decision tree per pattern:**

    For each rest-risk pattern in the quarter-half pool:
    - If the pattern is ALSO tagged with `quarter-rest`, `half-rest`, `whole-rest` (i.e., used by Unit 4+ rest-introduction nodes): KEEP the pattern, but REMOVE the `"quarter-half"` tag from its tags array.
    - If the pattern is ONLY tagged `quarter-half` (no rest-aware tag): REMOVE the pattern entry entirely.

    **Step 2.3 — Concrete edit examples:**

    For `qh_44_002` (and `qh_44_003`, `qhw_44_001`), search rhythmPatterns.js to find the entry. If the entry looks like:
    ```javascript
    { id: "qh_44_002", pattern: [0, 1, 0, 1], tags: ["quarter-half"], timeSignature: "4/4" },
    ```
    The pattern has a leading rest. Either:
    - (a) Delete the entry (if no other tag references it) — preferred for D-12 minimal scope
    - (b) Move it to the `quarter-rest` or `half-rest` tag pool if pedagogically appropriate

    Per RESEARCH R8 (Risk Register): after removal, count remaining `quarter-half` patterns. If <4, RESEARCH recommends adding 2-3 rest-free quarter-half patterns. For Phase 33 scope, prefer (a) deletion + count check; if <4 remaining, add a comment flagging for follow-up but don't author new patterns in this plan (keeps scope tight per beta-launch framing in CONTEXT specifics).

    **Step 2.4 — Scope (quarter-half only):**

    This task restricts the data-layer cleanup to the `quarter-half` tag pool only. Per beta-launch focus and D-08 audit intent ("audit catches it system-wide"), the runtime `resolveByTags` filter already provides system-wide coverage; this plan's data-layer cleanup is narrowed to the directly-implicated UAT issue (rhythm_1_3 / Issue 2/9) and its tag pool.

    Do NOT audit `quarter-only`, `dotted-half`, `six-eight`, `three-four`, `quarter-eighth`, `quarter-half-whole`, or other tag pools in this plan (D-16 limits scope). The `quarter-only` rest-pool audit is captured in the Backlog section below.

    **Step 2.5 — Add audit comment.**

    At the top of rhythmPatterns.js (after existing module comment), add:
    ```javascript
    // D-12 (Phase 33 Plan 33-04): rest-bearing patterns removed from quarter-half tag pool.
    // resolveByTags()'s patternNeedsRests filter is the runtime guard; this is belt-and-suspenders cleanup.
    // Audit date: 2026-05-03. See .planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md §3 Unit 1.
    // Backlog: quarter-only rest-pool audit deferred to follow-up if UAT issue 2/9 surfaces it system-wide.
    ```

  </action>
  <verify>
    <automated>npm run verify:trail && npx vitest run src/data/patterns/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:trail` exits 0 (validateTrail.mjs passes — no node has empty `resolveByTags` result)
    - `grep -B1 -A3 '"quarter-half"' src/data/patterns/rhythmPatterns.js` does NOT show any pattern entry whose binary has a leading or trailing zero where the durations would force a rest. Manual inspection confirms.
    - Existing rhythmPatterns tests (if any in src/data/patterns/__tests__/ or sibling .test.js files) still pass
    - Comment block "D-12 (Phase 33 Plan 33-04)" present at top of file
    - quarter-only tag pool NOT audited in this plan (deferred to backlog per Step 2.4 scope rule)
    - Manual UAT: user replays UAT issue 2/9 (rhythm_1_3 across 5 sessions). Marks `[x] resolved-by-deploy` if no rests appear.
  </acceptance_criteria>
  <done>D-12 cleanup applied to quarter-half tag pool; trail validator green; UAT issue 2/9 ready for retest. Backlog: quarter-only rest-pool audit deferred to follow-up if UAT surfaces it.</done>
</task>

</tasks>

<verification>
- `npm run verify:trail` exits 0 after every commit
- `npm run lint` exits 0 on all modified files
- `npm run test:run` exits 0 (no pattern-related tests fail)
- Manual UAT issues 2/9 (rhythm_1_3 rests) and 5 (section/content titles for 4_6, 2_3, 6_4) ready for resolved-by-deploy mark

Source-audit coverage:

- D-08 (full audit): Tasks 1+2 collectively cover the 4 RESEARCH §3 drifts (rhythm_2_3, rhythm_4_6, rhythm_6_4, plus the rest-pool cleanup that prevents node 1_3 issue at root)
- D-11 (section/content title fix via audit): Task 1 renames rhythm_6_4
- D-12 (rest-pool cleanup at tag-pool level): Task 2
- DATA-01 (node 1_3 only learned values): Task 2 + Plan 33-06 D-09 filter (defense in depth)
- DATA-03 (section titles match content): Task 1 (3 of 4 drifts; rhythm_8_1 deferred to 33-07)
- rhythm_8_1 / Issue 4 / D-15 deferred to Plan 33-07 (contingent on UAT)
  </verification>

<success_criteria>
Plan complete when:

1. rhythm_4_6 durations now `['q','h','w','qr','hr','wr']`
2. rhythm_2_3 discovery_intro question removed from MIXED_LESSON questions
3. rhythm_6_4 renamed to "Mixed Speeds" with updated description
4. quarter-half tag pool in rhythmPatterns.js has no rest-bearing patterns (quarter-only audit deferred to backlog per Task 2 Step 2.4)
5. `npm run verify:trail` green
6. UAT issues 2/9 and 5 ready for resolved-by-deploy after user retest
   </success_criteria>

## Backlog

- **quarter-only rest-pool audit deferred to follow-up if UAT issue 2/9 surfaces it system-wide.** Per Task 2 Step 2.4 scope rule, this plan's data-layer cleanup is restricted to the `quarter-half` tag pool. Runtime `resolveByTags` filter already provides system-wide coverage. Re-triage if Issue 2/9 retest shows rests in any node beyond rhythm_1_3.

<output>
After completion, create `.planning/phases/33-rhythm-issues-cleanup/33-04-SUMMARY.md` documenting:
- Specific node edits (rhythm_4_6 durations, rhythm_2_3 question removal, rhythm_6_4 rename)
- Pattern entries removed/retagged in rhythmPatterns.js (list IDs)
- Remaining quarter-half pool size after cleanup (per RESEARCH R8 follow-up note)
- validateTrail status
- UAT issues 2/9 and 5 mark status (pending user retest at deploy time)
</output>
