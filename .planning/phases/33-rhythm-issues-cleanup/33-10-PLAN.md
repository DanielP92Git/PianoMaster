---
phase: 33-rhythm-issues-cleanup
plan: 10
type: execute
wave: 6
depends_on: [33-03, 33-04, 33-05, 33-06, 33-07, 33-08, 33-09]
files_modified:
  - .planning/phases/33-rhythm-issues-cleanup/33-UAT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements_addressed: []
contingent: false
must_haves:
  truths:
    - "Final manual UAT pass executed against the post-Wave-2/3 deployed build"
    - "Every confirmed-survivor entry in 33-UAT.md is marked [x] resolved-by-deploy"
    - "Any UAT entry that surfaces a NEW bug not covered by Plans 33-03..33-09 is captured as a backlog item or triggers a follow-up plan (per D-06: manual playthrough is the gate)"
    - "STATE.md reflects Phase 33 complete; ROADMAP.md updates Phase 33 entry; REQUIREMENTS.md traceability table updated to mark addressed items"
    - "Service worker cache version bumped to pianomaster-v8 if any UI shipped (Plans 33-08 contingent)"
    - "D-06 (manual playthrough is the gate; phase done-bar = all confirmed-survivor bugs fixed + manual UAT passes) honored"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 33 status: complete; v3.3 milestone progress updated"
      contains: "Phase 33"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 33 entry updated (Goal filled in, Plans listed)"
      contains: "Phase 33"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated: addressed reqs marked complete"
      contains: "Phase 33"
  key_links:
    - from: "33-UAT.md sign-off"
      to: "STATE.md Current Position"
      via: "Phase 33 marked complete after final UAT"
      pattern: "Phase 33.*complete|completed"
---

<objective>
Final pass: re-run UAT against the post-Wave-2/3 deployed build, verify every confirmed-survivor entry is now resolved-by-deploy, capture any newly-surfaced bugs as backlog or follow-up, update planning docs, and bump service worker cache version if UI shipped.

Purpose: D-06 locks "phase done-bar = all confirmed-survivor bugs fixed + manual UAT passes." Wave 4 closes the loop by re-running UAT against the deployed build that includes all Wave 2/3 fixes. STATE.md, ROADMAP.md, and REQUIREMENTS.md updates surface phase completion to downstream tooling.
Output: Marked 33-UAT.md (final), updated planning docs, service worker version bump if applicable, ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-rhythm-issues-cleanup/33-UAT.md
@.planning/phases/33-rhythm-issues-cleanup/33-CONTEXT.md
@.planning/phases/33-rhythm-issues-cleanup/33-VALIDATION.md
@public/sw.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Final UAT pass on confirmed-survivor entries</name>
  <what-built>
    Wave 2 plans (33-03, 33-04, 33-05, 33-06) and any fired Wave 3 plans (33-07, 33-08, 33-09) shipped fixes for the confirmed-bug subset from Wave 1. The deployed build now contains every fix.
  </what-built>
  <how-to-verify>
    The user re-runs the UAT script for every entry that was marked `[x] confirmed-bug` in Wave 1, plus any entry that the user wants to spot-check.

    1. **Verify the deployed build SHA** matches a build that contains all Wave 2/3 commits. Capture the SHA in a new line at top of `33-UAT.md`:
       ```
       Final UAT build SHA: <new SHA>
       ```

    2. **For every Wave 1 confirmed-bug entry**, re-execute the issue's "Steps" against the new build and confirm the issue is no longer reproducible.

    3. **For each retested entry**, change the mark from `[x] confirmed-bug` to ALSO check `[x] resolved-by-deploy`. Add a Note line:
       ```
       Final UAT: marked resolved-by-deploy at <new SHA>
       ```

    4. **If ANY entry still reproduces a bug:**
       - Halt phase completion
       - Capture the surviving bug in a new section at the bottom of `33-UAT.md` titled `## Survivors after Wave 2/3`
       - Decide: do we ship a Wave 5 fix plan, or capture as v3.4 backlog?
       - Get user direction before continuing

    5. **Subjective UAT entries (Issues 12, 13):** if user gave a 1-5 rating in Wave 1 and rated low, retest and re-rate. If still low and contingent plans (33-08, 33-09) ran, capture as "perception unchanged — UX layer insufficient" in survivors section. If user is satisfied, mark resolved-by-deploy.

    6. **Backlog items spotted during final pass** (not covered by Phase 33 plans): capture in working-memory, listed in 33-10-SUMMARY for triage during next milestone planning.

  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "Final UAT complete. All confirmed-survivor entries now resolved-by-deploy. No new bugs."
    - "Final UAT complete. <N> entries resolved; <M> still surviving — captured in survivors section."
    - "Surfaced new bug not covered by Phase 33 plans: <description>. Need to triage."
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bump service worker cache + update planning docs (STATE / ROADMAP / REQUIREMENTS)</name>
  <files>
    public/sw.js
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - C:/Development/PianoApp2/public/sw.js (locate `pianomaster-v7` cache name; per CLAUDE.md PWA section, bump to v8 only if UI shipped)
    - C:/Development/PianoApp2/.planning/STATE.md (current state — confirm Phase 33 is the in-progress phase)
    - C:/Development/PianoApp2/.planning/ROADMAP.md (Phase 33 entry currently shows "Goal: [To be planned]" — update to actual goal)
    - C:/Development/PianoApp2/.planning/REQUIREMENTS.md (traceability table at bottom — mark addressed reqs)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-UAT.md (post-final-UAT marks)
    - Plan summaries .planning/phases/33-rhythm-issues-cleanup/33-{01,02,...}-SUMMARY.md (whichever exist)
  </read_first>
  <action>
    **Step 2.1 — Service worker cache bump (conditional).**

    If Plan 33-08 fired (BossIntroOverlay shipped) OR any other UI changed in this phase: bump `pianomaster-v7` → `pianomaster-v8` in `public/sw.js`. Add comment:
    ```javascript
    // 2026-05-03 (Phase 33): bumped from v7 to v8 — Phase 33 UI changes (BossIntroOverlay)
    const CACHE_NAME = "pianomaster-v8";
    ```
    If NO UI shipped (only data + audio + migration), leave cache name as v7. Skip this step.

    **Step 2.2 — Update STATE.md:**

    Update the YAML frontmatter:
    ```yaml
    last_updated: "<today's ISO timestamp>"
    last_activity: "<today> -- Phase 33 complete"
    progress:
      total_phases: 10        # add 1 for Phase 33
      completed_phases: 5     # add 1 for Phase 33
      total_plans: <prior_total + 10>  # 10 plans for Phase 33
      completed_plans: <prior_completed + 10>
      percent: <recompute>
    ```

    Update body sections:
    - **Current Position:** "Phase: 33 (rhythm-issues-cleanup) — complete" → next phase TBD or v3.3 ready for milestone close
    - **Status:** "Phase 33 complete; v3.3 milestone close-out pending"
    - **Decisions:** add line: "Phase 33: verify-first triage closed all confirmed-bug UAT entries; rate-limit migration deployed; ArcadeRhythmGame migrated to tag-based resolver; data audit cleaned 4 unit drifts + quarter-half rest pool"
    - **Roadmap Evolution:** add line: "Phase 33 complete — beta-launch ready"
    - **Pending Todos:** clear or update
    - **Session Continuity / Next action:** "Run /gsd-verify-work for v3.3 milestone close-out"

    **Step 2.3 — Update ROADMAP.md:**

    Locate the `### Phase 33: Rhythm issues cleanup` block. Update:
    - **Goal:** replace "[To be planned]" with the actual goal from CONTEXT.md domain section, e.g.: "Triage and resolve the 13 rhythm UAT issues from Phases 30-32 — verify-first manual UAT, fix only confirmed survivors. Ship rate-limit migration, data audit cleanup, ArcadeRhythmGame tag-based resolution, and contingent boss/speed UX layer where user UAT confirmed flatness."
    - **Requirements:** AUDIO-01, AUDIO-02, AUDIO-03, DATA-01, DATA-03, DATA-04, PLAY-02, PLAY-03 (those addressed by Phase 33 plans; refer to each plan's `requirements_addressed` frontmatter)
    - **Plans:** "10 of 10 plans complete"
    - **Plans list:** the 10 PLAN files
      - [x] 33-01-PLAN.md — Pre-flight UAT scaffold + verify:trail + boss measureCount audit
      - [x] 33-02-PLAN.md — Wave 1 manual UAT execution
      - [x] 33-03-PLAN.md — D-13 prewarm hook + dictation race fix
      - [x] 33-04-PLAN.md — D-08/D-11/D-12 data audit edits
      - [x] 33-05-PLAN.md — D-07 rate-limit migration deploy
      - [x] 33-06-PLAN.md — Stash Chunk A salvage + D-09 + D-10
      - [x] 33-07-PLAN.md — D-14/D-15 audio buffer hardening (contingent)
      - [x] 33-08-PLAN.md — D-18 boss intro overlay + victory VFX (contingent)
      - [x] 33-09-PLAN.md — D-19 cumulative speed-pool tags (contingent)
      - [x] 33-10-PLAN.md — Final UAT + state/roadmap update

    Mark plans `[x]` only for those that actually shipped. Contingent plans that were skipped per Wave 1 outcome should be marked with a note like `[skipped per UAT]`.

    Update the Progress table at the bottom — add Phase 33 row.

    Update v3.3 milestone summary line:
    `**v3.3 Rhythm Trail Fix & Polish** — Phases 29-33 (in progress)` → if v3.3 close-out is also complete, update to `(shipped <date>)`. Otherwise leave "in progress" and let the v3.3 milestone close-out (separate workflow) update.

    **Step 2.4 — Update REQUIREMENTS.md traceability table (deterministic branching by contingent-plan outcome):**

    Find the table at line 61-77. For each requirement addressed by Phase 33 plans, apply the following branching rule per contingent plan outcome:

    **Branching rule per contingent plan outcome:**
    - **Plan SHIPPED** (UAT marked confirmed-bug → fix applied): mark requirement **Complete**; add Phase 33 to its Phase column (e.g. `30, 33` instead of just `30`).
    - **Plan SKIPPED because UAT marked resolved-by-deploy**: mark requirement **Complete**; the Phase column note reads `verified at Phase 33 retest`.
    - **Plan SKIPPED because UAT marked cannot-reproduce**: leave requirement **Pending** and append a Phase 33 note in the column: `cannot-reproduce — re-triage in next milestone`.

    **Per-requirement application:**
    - **AUDIO-01** (Phase 30, contingent on Plan 33-07): apply branching rule based on 33-07 outcome.
    - **AUDIO-02** (Phase 30, addressed unconditionally by Plan 33-03 D-13 hook): mark **Complete**; Phase column = `30, 33`.
    - **AUDIO-03** (Phase 30, contingent on Plan 33-07 rhythm_8_1 edit): apply branching rule based on 33-07 outcome.
    - **DATA-01** (Phase 29, addressed unconditionally by Plan 33-04 D-12 + Plan 33-06 D-09): mark **Complete**; Phase column = `29, 33`.
    - **DATA-03** (Phase 29, addressed unconditionally by Plan 33-04 rename audit): mark **Complete**; Phase column = `29, 33`.
    - **DATA-04** (Phase 29, addressed unconditionally by Plan 33-06 D-10 variety rule): mark **Complete**; Phase column = `29, 33`.
    - **PLAY-02** (Phase 32, addressed unconditionally by Plan 33-06 Stash A; Plan 33-09 contingent additive): apply branching rule for the 33-09 contribution; the 33-06 contribution is unconditional → at minimum mark **Complete** with Phase column `32, 33`.
    - **PLAY-03** (Phase 32, contingent on Plan 33-08): apply branching rule based on 33-08 outcome.

    For requirements NOT addressed by Phase 33 (e.g. CODE-01..03, PLAY-01, PLAY-04, DATA-02), leave as-is — they belong to other phases.

    Also: for any prior-phase requirement that was retested during final UAT (Task 1) and is now confirmed working without a Phase 33 plan firing, that row also gets a `verified at Phase 33 retest` note in the Phase column.

    **Step 2.5 — Final mark of 33-UAT.md sign-off section.**

    In `33-UAT.md`, fill in the Sign-off section (added in Plan 33-01):
    - [x] All 10 active issue sections marked
    - [x] Issue 11 acknowledged as DROPPED-PER-CONTEXT
    - [x] User's confirmed-bug subset triggered Wave 2/3 plans as planned
    - [x] Final UAT complete — all confirmed-survivor entries resolved-by-deploy

  </action>
  <verify>
    <automated>npm run verify:trail && grep -c 'Phase 33.*complete\|completed_phases: 5' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:trail` exits 0 (final regression check)
    - STATE.md frontmatter `last_activity` field shows "Phase 33 complete"
    - STATE.md body Current Position shows "complete" for Phase 33
    - ROADMAP.md Phase 33 entry has actual Goal text (not "[To be planned]")
    - ROADMAP.md Plans list shows 10 entries; shipped plans marked `[x]`, contingent-skipped plans noted
    - REQUIREMENTS.md traceability table: every requirement listed in any plan's `requirements_addressed` frontmatter has a deterministic status update per the Step 2.4 branching rule (SHIPPED → Complete + Phase column appended; SKIPPED-resolved-by-deploy → Complete + `verified at Phase 33 retest` note; SKIPPED-cannot-reproduce → Pending + `cannot-reproduce — re-triage in next milestone` note). Total updated row count must equal the count of requirements addressed across all Phase 33 plans plus any retest-verified prior-phase requirements.
    - 33-UAT.md sign-off section has all 4 boxes checked
    - If UI shipped: public/sw.js has `pianomaster-v8`. If no UI shipped: still v7. Verify via `grep -c 'pianomaster-v[78]' public/sw.js`.
    - All four planning docs committed to git in a single docs commit
  </acceptance_criteria>
  <done>STATE / ROADMAP / REQUIREMENTS / 33-UAT.md all updated and committed; ready for /gsd-verify-work</done>
</task>

</tasks>

<verification>
- Final UAT pass complete (Task 1)
- All confirmed-survivor entries marked resolved-by-deploy
- Planning docs reflect Phase 33 completion (Task 2)
- Service worker cache bumped if applicable
- `npm run verify:trail` green
- Phase ready for `/gsd-verify-work` to validate against goal-backward must_haves

Source-audit coverage:

- D-06 (manual playthrough is the gate): honored by Task 1
- All other D-NN decisions traced through prior plans (33-01..33-09); this plan closes the loop
  </verification>

<success_criteria>
Plan complete when:

1. Final UAT executed against deployed build; all confirmed-survivor entries marked resolved-by-deploy in 33-UAT.md
2. Service worker cache bumped (if UI shipped via Plan 33-08)
3. STATE.md, ROADMAP.md, REQUIREMENTS.md updated to reflect Phase 33 completion
4. Single docs commit captures all updates
5. Phase ready for `/gsd-verify-work`
   </success_criteria>

<output>
After completion, create `.planning/phases/33-rhythm-issues-cleanup/33-10-SUMMARY.md` documenting:
- Final UAT outcome per issue (✓ resolved | ✗ surviving | ⊘ skipped)
- Service worker cache version (v7 or v8)
- STATE.md / ROADMAP.md / REQUIREMENTS.md changes summary
- Backlog items surfaced during final UAT (for v3.4 triage)
- Final phase metrics: plans shipped vs skipped, decisions D-01..D-20 disposition
- Recommended next step: `/gsd-verify-work 33` then v3.3 milestone close-out
</output>
