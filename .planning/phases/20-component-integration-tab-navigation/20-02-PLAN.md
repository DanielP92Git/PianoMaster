---
phase: 20-component-integration-tab-navigation
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/components/layout/Dashboard.jsx
  - src/components/dashboard/UnifiedStatsCard.jsx
  - src/components/dashboard/XPRing.jsx
  - src/components/dashboard/XPProgressCard.jsx
  - src/components/games/VictoryScreen.jsx
autonomous: false
requirements: [PROG-01, PROG-02, PROG-03]

must_haves:
  truths:
    - "A student at level 15 sees level 16 on their progress bar, not MAX LEVEL"
    - "A prestige student sees a golden badge and golden progress bar on the dashboard"
    - "Level-up celebration in VictoryScreen shows the new title earned"
    - "Prestige entry at level 30 shows golden celebration with special headline"
    - "XPRing uses golden gradient for prestige players"
    - "Dashboard level pill shows prestige title (Maestro N) with golden border"
    - "Progress bar never shows 100% / MAX LEVEL for any level including 30"
  artifacts:
    - path: "src/components/layout/Dashboard.jsx"
      provides: "Prestige-aware level pill, isPrestige prop computation, isMaxLevel removal"
    - path: "src/components/dashboard/UnifiedStatsCard.jsx"
      provides: "isPrestige prop replaces isMaxLevel for golden styling"
    - path: "src/components/dashboard/XPRing.jsx"
      provides: "Golden gradient SVG for prestige players"
    - path: "src/components/dashboard/XPProgressCard.jsx"
      provides: "Standalone XP card updated (isMaxLevel removal)"
    - path: "src/components/games/VictoryScreen.jsx"
      provides: "Enhanced level-up celebration with title display and prestige golden styling"
  key_links:
    - from: "src/components/layout/Dashboard.jsx"
      to: "src/utils/xpSystem.js"
      via: "calculateLevel().isPrestige for badge styling"
      pattern: "isPrestige"
    - from: "src/components/dashboard/XPRing.jsx"
      to: "src/utils/xpSystem.js"
      via: "isPrestige prop triggers golden gradient"
      pattern: "isPrestige.*amber"
    - from: "src/components/games/VictoryScreen.jsx"
      to: "src/locales/en/common.json"
      via: "victory.youAreNow i18n key for level-up title"
      pattern: "youAreNow"
---

<objective>
Update all UI components to support 30 levels and prestige tiers — replace isMaxLevel with prestige-aware logic, add golden visual treatment for prestige players, and enhance the VictoryScreen level-up celebration to show the earned title.

Purpose: Make the extended progression system visible and rewarding in the UI. Students see progress beyond level 15, prestige players see golden styling, and every level-up shows the new title.
Output: Updated Dashboard, UnifiedStatsCard, XPRing, XPProgressCard, and VictoryScreen.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-component-integration-tab-navigation/20-CONTEXT.md
@.planning/phases/20-component-integration-tab-navigation/20-RESEARCH.md
@.planning/phases/20-component-integration-tab-navigation/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the new xpSystem.js API -->

From src/utils/xpSystem.js (after Plan 01):
```javascript
export const MAX_STATIC_LEVEL = 30;
export const PRESTIGE_XP_PER_TIER = 3000;
export const PRESTIGE_BASE_XP = 51000;

export const XP_LEVELS = [
  // 30 entries: levels 1-30
  { level: 1,  xpRequired: 0,     title: 'Beginner',        icon: '🌱' },
  // ...
  { level: 30, xpRequired: 51000, title: 'Transcendent',     icon: '🏅' },
];

// Returns { level, title, icon, xpRequired, isPrestige: bool, prestigeTier: number }
export const calculateLevel = (totalXp) => { ... };

// Returns PRESTIGE_XP_PER_TIER for levels >= MAX_STATIC_LEVEL
export const getNextLevelXP = (currentLevel) => { ... };

// Returns { currentLevel, nextLevelXP, xpInCurrentLevel, xpNeededForNext, progressPercentage, isPrestige: bool }
export const getLevelProgress = (totalXp) => { ... };
```

From src/locales/en/common.json (after Plan 01):
```json
{
  "xpLevels": {
    "prestigeTitle": "Maestro {{tier}}"
  },
  "victory": {
    "youAreNow": "You are now a {{title}}!",
    "prestigeUnlocked": "Prestige Unlocked!",
    "prestigeEntry": "You've reached the highest level! Welcome to Maestro tier!"
  }
}
```

Current file states to modify:

Dashboard.jsx line 199: `const isMaxLevel = level >= 15;`
Dashboard.jsx lines 640-653: Level pill with cyan-orange gradient
Dashboard.jsx line 692: `progressPercentage={isMaxLevel ? 100 : progressPercentage}`
Dashboard.jsx line 696: `isMaxLevel={isMaxLevel}`

UnifiedStatsCard.jsx line 23: `isMaxLevel = false` prop
UnifiedStatsCard.jsx line 111: `isMaxLevel={isMaxLevel}` passed to XPRing

XPRing.jsx line 14: `isMaxLevel = false` prop
XPRing.jsx line 101: `{isMaxLevel ? xpCurrent : ...}`

XPProgressCard.jsx line 50: `const isMaxLevel = level >= 15;`
XPProgressCard.jsx lines 79, 93, 100, 108: isMaxLevel conditionals

VictoryScreen.jsx lines 894-901: Level-up celebration block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard.jsx and UnifiedStatsCard — prestige-aware level pill and stats</name>
  <files>src/components/layout/Dashboard.jsx, src/components/dashboard/UnifiedStatsCard.jsx, src/components/dashboard/XPRing.jsx, src/components/dashboard/XPProgressCard.jsx</files>
  <action>
**Dashboard.jsx changes:**

1. Add imports at top: `import { ..., MAX_STATIC_LEVEL, PRESTIGE_XP_PER_TIER, PRESTIGE_BASE_XP } from '../../utils/xpSystem';`

2. Replace `const isMaxLevel = level >= 15;` (line 199) with:
   ```javascript
   const levelData = calculateLevel(totalXP);
   const isPrestige = levelData.isPrestige;
   ```
   Note: `calculateLevel` is already imported. Use it on `totalXP` (already available from xpData).

3. Update `rawLevelTitle` computation (line 195-198). For prestige levels, the title comes from calculateLevel, not the XP_LEVELS array:
   ```javascript
   const rawLevelTitle = levelData.title || 'Beginner';
   const levelTitle = levelData.isPrestige
     ? t('xpLevels.prestigeTitle', { tier: levelData.prestigeTier })
     : t('xpLevels.' + rawLevelTitle, { defaultValue: rawLevelTitle });
   ```

4. Update level pill (lines 640-653) to show prestige golden styling:
   - Change the border gradient: if `isPrestige`, use `linear-gradient(to right, #fbbf24, #f59e0b, #fbbf24)` (golden); else keep existing cyan-orange
   - Change box-shadow: if `isPrestige`, use `0 2px 12px rgba(251,191,36,0.5)`; else keep existing
   - Change text: if `isPrestige`, show `t('xpLevels.prestigeTitle', { tier: levelData.prestigeTier })` instead of `t('dashboard.header.level', ...)`

5. Update UnifiedStatsCard props (lines 688-704):
   - Remove `isMaxLevel={isMaxLevel}`
   - Add `isPrestige={isPrestige}`
   - Change `progressPercentage={isMaxLevel ? 100 : progressPercentage}` to `progressPercentage={progressPercentage}` (always show real progress)

**UnifiedStatsCard.jsx changes:**

1. Replace `isMaxLevel = false` prop with `isPrestige = false`
2. Pass `isPrestige` to XPRing instead of `isMaxLevel` (line 111)
3. No other structural changes needed — UnifiedStatsCard is presentational

**XPRing.jsx changes:**

1. Replace `isMaxLevel = false` prop with `isPrestige = false`
2. Add a second gradient definition for prestige (golden):
   ```jsx
   <linearGradient id="xp-ring-grad-prestige" x1="0" y1="0" x2="1" y2="1">
     <stop offset="0%" stopColor="#FBBF24" />
     <stop offset="50%" stopColor="#F59E0B" />
     <stop offset="100%" stopColor="#D97706" />
   </linearGradient>
   ```
3. Add a prestige glow filter:
   ```jsx
   <filter id="xp-ring-glow-prestige">
     <feGaussianBlur stdDeviation="4" result="blur" />
     <feFlood floodColor="#FBBF24" floodOpacity="0.6" result="color" />
     <feComposite in="color" in2="blur" operator="in" result="glow" />
     <feMerge>
       <feMergeNode in="glow" />
       <feMergeNode in="SourceGraphic" />
     </feMerge>
   </filter>
   ```
4. Progress arc: use `url(#xp-ring-grad-prestige)` and `url(#xp-ring-glow-prestige)` when `isPrestige` is true
5. XP text (line 101): always show `${xpCurrent}/${xpTotal}` format (remove isMaxLevel ternary that showed only total)

**XPProgressCard.jsx changes:**

1. Replace `const isMaxLevel = level >= 15;` (line 50) with:
   ```javascript
   const levelData = calculateLevel(totalXP);
   const isPrestige = levelData.isPrestige;
   ```
   Add `calculateLevel` to imports if not present, and add `totalXP` from xpData.

2. Replace all `isMaxLevel` usage:
   - Line 79: `isMaxLevel ?` maxLevel text to `isPrestige ?` prestige text (show "Maestro tier" label)
   - Line 93: `isMaxLevel ? 100 :` to just use `progressPercentage` always
   - Line 100: `isMaxLevel ?` total XP only to `isPrestige ?` prestige XP format
   - Line 108: `!isMaxLevel &&` next level section to always show (or show prestige next tier)

3. Update progress bar gradient for prestige:
   ```jsx
   className={`h-3 rounded-full ${isPrestige ? 'bg-gradient-to-r from-amber-400 via-yellow-300 to-amber-500' : 'bg-gradient-to-r from-blue-400 to-indigo-600'}`}
   ```

4. Add golden box-shadow for prestige bar:
   ```jsx
   style={isPrestige ? { boxShadow: '0 0 8px rgba(251,191,36,0.6)' } : undefined}
   ```
  </action>
  <verify>
    <automated>npx vitest run src/utils/xpSystem.test.js -x && node -e "const fs=require('fs'); const d=fs.readFileSync('src/components/layout/Dashboard.jsx','utf8'); console.log(d.includes('isPrestige') ? 'PASS: Dashboard has isPrestige' : 'FAIL: Missing isPrestige'); console.log(!d.includes('isMaxLevel') ? 'PASS: Dashboard has no isMaxLevel' : 'FAIL: Still has isMaxLevel'); const r=fs.readFileSync('src/components/dashboard/XPRing.jsx','utf8'); console.log(r.includes('isPrestige') ? 'PASS: XPRing has isPrestige' : 'FAIL: Missing isPrestige'); console.log(r.includes('prestige') && r.includes('amber') ? 'PASS: XPRing has golden gradient' : 'FAIL: Missing golden gradient')"</automated>
  </verify>
  <done>Dashboard.jsx uses isPrestige (not isMaxLevel) with golden level pill for prestige. UnifiedStatsCard and XPRing accept isPrestige prop with golden gradient. XPProgressCard uses prestige-aware styling. Progress bars always show incremental progress (never 100% / MAX LEVEL).</done>
</task>

<task type="auto">
  <name>Task 2: VictoryScreen — enhanced level-up celebration with title and prestige</name>
  <files>src/components/games/VictoryScreen.jsx</files>
  <action>
Read VictoryScreen.jsx fully (it is a large file). Find the level-up celebration block (around lines 894-901).

**Changes to VictoryScreen.jsx:**

1. The level-up block currently shows:
   ```jsx
   {xpData.leveledUp && (
     <div className="... bg-gradient-to-r from-purple-500 to-pink-500 ...">
       <div className="text-xs ...">{t('victory.levelUp')}</div>
       <div className="text-base font-bold text-white">
         {levelProgressData?.currentLevel?.title ? t(`xpLevels.${levelProgressData.currentLevel.title}`) : t('victory.levelLabel', { level: xpData.newLevel })}
       </div>
     </div>
   )}
   ```

2. Replace with prestige-aware version:
   ```jsx
   {xpData.leveledUp && (
     <div className={`${reducedMotion ? '' : 'animate-bounce'} mt-2 rounded-lg px-3 py-2 text-center shadow-lg ${
       levelProgressData?.isPrestige || xpData.newLevel >= 30
         ? 'bg-gradient-to-r from-amber-500 to-yellow-500'
         : 'bg-gradient-to-r from-purple-500 to-pink-500'
     }`}>
       <div className="text-xs font-semibold text-white/90">
         {xpData.newLevel === 30
           ? t('victory.prestigeUnlocked')
           : t('victory.levelUp')}
       </div>
       <div className="text-base font-bold text-white">
         {levelProgressData?.currentLevel?.isPrestige
           ? t('xpLevels.prestigeTitle', { tier: levelProgressData.currentLevel.prestigeTier })
           : t('victory.youAreNow', {
               title: levelProgressData?.currentLevel?.title
                 ? t(`xpLevels.${levelProgressData.currentLevel.title}`, { defaultValue: levelProgressData.currentLevel.title })
                 : t('victory.levelLabel', { level: xpData.newLevel })
             })}
       </div>
       {xpData.newLevel === 30 && (
         <div className="mt-1 text-xs text-white/80">
           {t('victory.prestigeEntry')}
         </div>
       )}
     </div>
   )}
   ```

3. Also update the mini XP progress bar (around lines 873-890) to support prestige golden styling:
   - The bar currently uses `bg-gradient-to-r from-blue-500 to-indigo-600`
   - Add prestige check: if `levelProgressData?.isPrestige`, use `bg-gradient-to-r from-amber-400 via-yellow-300 to-amber-500`
   - The surrounding box currently uses `bg-blue-50/80` — for prestige use `bg-amber-50/80`
   - The text uses `text-blue-900` and `text-blue-700` — for prestige use `text-amber-900` and `text-amber-700`

4. For the XP progress bar display text (line 888), it currently shows:
   ```jsx
   {t('victory.xpProgress', { current: levelProgressData.xpInCurrentLevel, total: levelProgressData.nextLevelXP - levelProgressData.currentLevel.xpRequired })}
   ```
   For prestige levels, `nextLevelXP - currentLevel.xpRequired` would be incorrect (prestige levels do not have xpRequired in the array). Use a simpler calculation:
   ```jsx
   {t('victory.xpProgress', {
     current: levelProgressData.xpInCurrentLevel,
     total: levelProgressData.isPrestige
       ? PRESTIGE_XP_PER_TIER
       : levelProgressData.nextLevelXP - levelProgressData.currentLevel.xpRequired
   })}
   ```
   Import PRESTIGE_XP_PER_TIER from xpSystem.js at the top of the file.

IMPORTANT: Do NOT modify any other part of VictoryScreen. The file is large and complex. Only touch the level-up celebration block (lines ~873-901) and the progress bar section.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const v=fs.readFileSync('src/components/games/VictoryScreen.jsx','utf8'); console.log(v.includes('prestigeUnlocked') ? 'PASS: VictoryScreen has prestige celebration' : 'FAIL: Missing prestige celebration'); console.log(v.includes('youAreNow') ? 'PASS: VictoryScreen shows title on level-up' : 'FAIL: Missing youAreNow'); console.log(v.includes('amber-500') ? 'PASS: VictoryScreen has golden styling' : 'FAIL: Missing golden styling'); console.log(v.includes('prestigeEntry') ? 'PASS: VictoryScreen has prestige entry message' : 'FAIL: Missing prestigeEntry')"</automated>
  </verify>
  <done>VictoryScreen level-up block shows: (1) "You are now a [Title]!" for normal level-ups, (2) golden gradient + "Prestige Unlocked!" for level 30, (3) "Maestro N" for prestige level-ups, (4) golden XP progress bar for prestige levels. All existing VictoryScreen functionality preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Extended progression system UI</name>
  <files>src/components/layout/Dashboard.jsx, src/components/dashboard/XPRing.jsx, src/components/games/VictoryScreen.jsx</files>
  <action>
Automated work complete. Verify the extended XP progression system visually:
1. Run `npm run dev` and open the app
2. LEVEL PROGRESS: Check dashboard — level pill should show current level title (if student is below 15, shows existing title; if 15+, shows new title like "Composer" for level 16)
3. XP PROGRESS BAR: Verify the XP ring and progress bar show incremental progress (never "MAX LEVEL" at any level)
4. LEVEL-UP CELEBRATION (requires DB): If possible, set a student's XP near a level threshold in the DB, complete an exercise, and verify:
   - Level-up block shows "You are now a [Title]!" with the correct title
   - For prestige (level 30+): golden styling and "Prestige Unlocked!" headline
5. GOLDEN PRESTIGE STYLING: For prestige testing (optional — requires XP >= 54000):
   - Dashboard level pill should have golden border gradient
   - XP ring should use golden/amber gradient
   - Progress bar should be golden/amber
6. RTL: Switch to Hebrew and verify level names are translated
7. Run `npm run test:run` — full test suite passes
  </action>
  <verify>npm run test:run</verify>
  <done>User confirms: dashboard level pill, XP ring, progress bars, and VictoryScreen level-up celebration all render correctly for normal levels and prestige tiers. No "MAX LEVEL" shown. Golden styling appears for prestige. Hebrew translations work.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/utils/xpSystem.test.js -x` — all unit tests pass
2. `npm run test:run` — full suite green
3. No `isMaxLevel` references remain in Dashboard.jsx, UnifiedStatsCard.jsx, XPRing.jsx
4. XPProgressCard.jsx no longer references `level >= 15`
5. VictoryScreen shows title on level-up and golden styling for prestige
6. Human verification of visual appearance
</verification>

<success_criteria>
- isMaxLevel concept removed from all dashboard components — replaced by isPrestige
- Progress bars always show incremental progress (never 100% / frozen)
- Prestige players see golden badge, golden XP ring, golden progress bar
- Level-up celebration shows "You are now a [Title]!" for all level-ups
- Level 30 triggers special prestige celebration with golden styling
- All translations work in EN and HE including RTL
</success_criteria>

<output>
After completion, create `.planning/phases/20-component-integration-tab-navigation/20-02-SUMMARY.md`
</output>
