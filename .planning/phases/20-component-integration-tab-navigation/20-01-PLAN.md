---
phase: 20-component-integration-tab-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/xpSystem.js
  - src/utils/xpSystem.test.js
  - supabase/migrations/20260307000001_extend_xp_levels.sql
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03]

must_haves:
  truths:
    - "XP_LEVELS array has 30 entries with sequential XP thresholds"
    - "calculateLevel returns prestige object for XP beyond level 30"
    - "getLevelProgress returns correct progress for levels 16-30 and prestige tiers"
    - "getNextLevelXP returns non-zero for levels 1-29 and prestige XP for 30+"
    - "Every level 1-30 has a unique non-empty title and emoji icon"
    - "Postgres award_xp function supports 30 levels and prestige tiers"
    - "All 30 level titles plus prestige key exist in EN and HE locale files"
  artifacts:
    - path: "src/utils/xpSystem.js"
      provides: "30-level XP system with prestige computation"
      exports: ["XP_LEVELS", "calculateLevel", "getLevelProgress", "getNextLevelXP", "MAX_STATIC_LEVEL", "PRESTIGE_XP_PER_TIER", "PRESTIGE_BASE_XP"]
    - path: "src/utils/xpSystem.test.js"
      provides: "Unit tests for all 30 levels and prestige logic"
      min_lines: 80
    - path: "supabase/migrations/20260307000001_extend_xp_levels.sql"
      provides: "DB migration: constraint fix, 30-level award_xp, data fixup"
      contains: "award_xp"
    - path: "src/locales/en/common.json"
      provides: "English translations for 15 new level names + prestige key"
      contains: "Composer"
    - path: "src/locales/he/common.json"
      provides: "Hebrew translations for 15 new level names + prestige key"
      contains: "מלחין"
  key_links:
    - from: "src/utils/xpSystem.js"
      to: "supabase/migrations/20260307000001_extend_xp_levels.sql"
      via: "Identical XP thresholds in JS array and Postgres array"
      pattern: "10500.*12200.*14100"
    - from: "src/utils/xpSystem.js"
      to: "src/locales/en/common.json"
      via: "Level title strings used as i18n keys"
      pattern: "xpLevels\\.Composer"
---

<objective>
Extend the XP level system from 15 to 30 levels with prestige tiers, create the Postgres migration, and add i18n keys for all level names.

Purpose: Provide the core data layer and level computation logic that all UI components in Plan 02 depend on. Fix the pre-existing DB/JS level threshold mismatch (DB caps at 10, JS has 15).
Output: TDD-tested xpSystem.js with 30 levels + prestige, Postgres migration, EN/HE locale keys.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-component-integration-tab-navigation/20-CONTEXT.md
@.planning/phases/20-component-integration-tab-navigation/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/utils/xpSystem.js (CURRENT — will be modified):
```javascript
export const XP_LEVELS = [
  { level: 1, xpRequired: 0, title: 'Beginner', icon: '🌱' },
  // ... 15 entries total
  { level: 15, xpRequired: 9000, title: 'Legend', icon: '💎' }
];

export const calculateLevel = (totalXp) => { /* returns XP_LEVELS[i] */ };
export const getNextLevelXP = (currentLevel) => { /* returns 0 if >= 15 */ };
export const getLevelProgress = (totalXp) => { /* returns { currentLevel, nextLevelXP, xpInCurrentLevel, xpNeededForNext, progressPercentage } */ };
export const calculateNodeXP = (stars, baseXP, bonuses) => { /* returns number */ };
export const awardXP = async (studentId, xpAmount) => { /* calls supabase.rpc('award_xp') */ };
export const getStudentXP = async (studentId) => { /* returns { totalXP, currentLevel, levelData, progress } */ };
export const calculateSessionXP = (session) => { /* returns { stars, baseXP, bonusXP, bonuses, comebackMultiplier, totalXP } */ };
```

From src/locales/en/common.json (xpLevels section — 15 entries):
```json
{
  "xpLevels": {
    "Beginner": "Beginner",
    "Music Sprout": "Music Sprout",
    // ... 15 entries keyed by English title
    "Legend": "Legend"
  }
}
```

From supabase/migrations/20260126000001_fix_award_xp_security.sql:
- award_xp(UUID, INTEGER) RETURNS TABLE(new_total_xp, new_level, leveled_up)
- Uses v_level_thresholds INTEGER[] with 10 entries
- Loop: FOR i IN 1..10

From supabase/migrations/20260124000001_add_skill_trail_system.sql:
- CHECK constraint: students_level_valid CHECK (current_level >= 1 AND current_level <= 10)

Consumers of xpSystem.js that Plan 02 will update:
- src/components/layout/Dashboard.jsx: `isMaxLevel = level >= 15`
- src/components/dashboard/XPProgressCard.jsx: `isMaxLevel = level >= 15`
- src/components/dashboard/UnifiedStatsCard.jsx: receives `isMaxLevel` prop
- src/components/dashboard/XPRing.jsx: receives `isMaxLevel` prop
- src/components/games/VictoryScreen.jsx: level-up celebration block (lines 894-901)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD xpSystem.js — extend to 30 levels with prestige computation</name>
  <files>src/utils/xpSystem.test.js, src/utils/xpSystem.js</files>
  <behavior>
    - XP_LEVELS has exactly 30 entries
    - XP_LEVELS thresholds are strictly increasing (each > previous)
    - Every level 1-30 has a non-empty string title and non-empty string icon
    - All 30 titles are unique
    - calculateLevel(0) returns level 1 with isPrestige: false
    - calculateLevel(9000) returns level 15 ("Legend") with isPrestige: false
    - calculateLevel(10500) returns level 16 ("Composer") with isPrestige: false
    - calculateLevel(51000) returns level 30 ("Transcendent") with isPrestige: false, prestigeTier: 0
    - calculateLevel(54000) returns level 31 with isPrestige: true, prestigeTier: 1, title: "Maestro 1"
    - calculateLevel(57000) returns level 32 with isPrestige: true, prestigeTier: 2, title: "Maestro 2"
    - calculateLevel(51001) returns level 30 (not prestige tier 1 — need full 3000 XP beyond 51000)
    - getLevelProgress(500) returns progressPercentage between 0-100, isPrestige: false
    - getLevelProgress(9000) returns progressPercentage for level 15 toward level 16 (NOT 100% / max)
    - getLevelProgress(51000) returns isPrestige: false, progress toward first prestige tier
    - getLevelProgress(54000) returns isPrestige: true with correct tier progress
    - getNextLevelXP(1) returns 100 (level 2 threshold)
    - getNextLevelXP(15) returns 10500 (level 16 threshold, NOT 0)
    - getNextLevelXP(29) returns 51000 (level 30 threshold)
    - getNextLevelXP(30) returns PRESTIGE_XP_PER_TIER (3000 for prestige)
    - getNextLevelXP(31) returns 3000 (same fixed prestige cost)
    - MAX_STATIC_LEVEL exported and equals 30
    - PRESTIGE_XP_PER_TIER exported and equals 3000
    - PRESTIGE_BASE_XP exported and equals 51000
  </behavior>
  <action>
RED phase: Create src/utils/xpSystem.test.js with tests for all behaviors listed above. Group into describe blocks: "XP_LEVELS array", "calculateLevel", "getLevelProgress", "getNextLevelXP", "prestige constants". Run tests — they must FAIL (calculateLevel returns no isPrestige, only 15 levels, getNextLevelXP returns 0 at 15).

GREEN phase: Modify src/utils/xpSystem.js:

1. Keep existing levels 1-15 UNCHANGED in XP_LEVELS array. Append levels 16-30 with these exact values (from RESEARCH.md):
   - 16: 10500 "Composer" '📝'
   - 17: 12200 "Conductor" '🎙️'
   - 18: 14100 "Concert Master" '🎻'
   - 19: 16200 "Prodigy" '🌟'
   - 20: 18500 "Orchestrator" '🎺'
   - 21: 21000 "Music Sage" '📖'
   - 22: 23700 "Melodist" '🎤'
   - 23: 26500 "Symphonist" '🎷'
   - 24: 29400 "Music Architect" '🏛️'
   - 25: 32500 "Philharmonic" '🌈'
   - 26: 35800 "Opus Creator" '🖋️'
   - 27: 39300 "Concerto Star" '💫'
   - 28: 43000 "Music Luminary" '🔆'
   - 29: 46900 "Grand Virtuoso" '🎭'
   - 30: 51000 "Transcendent" '🏅'

2. Export new constants:
   ```javascript
   export const MAX_STATIC_LEVEL = 30;
   export const PRESTIGE_XP_PER_TIER = 3000;
   export const PRESTIGE_BASE_XP = XP_LEVELS[XP_LEVELS.length - 1].xpRequired; // 51000
   ```

3. Modify calculateLevel(totalXp) to return objects with `isPrestige` and `prestigeTier` fields:
   - For normal levels: { ...XP_LEVELS[i], isPrestige: false, prestigeTier: 0 }
   - For prestige: compute `xpBeyondMax = totalXp - PRESTIGE_BASE_XP`, `prestigeTier = Math.floor(xpBeyondMax / PRESTIGE_XP_PER_TIER)`, return { level: MAX_STATIC_LEVEL + prestigeTier, title: `Maestro ${toRoman(prestigeTier)}`, icon: '👑', isPrestige: true, prestigeTier } when prestigeTier > 0
   - NOTE: Use simple Arabic numerals (Maestro 1, Maestro 2) not Roman numerals — simpler for 8-year-olds per CONTEXT.md

4. Modify getNextLevelXP(currentLevel):
   - If currentLevel >= MAX_STATIC_LEVEL, return PRESTIGE_XP_PER_TIER (not 0)
   - Otherwise return XP_LEVELS[currentLevel].xpRequired (unchanged)

5. Modify getLevelProgress(totalXp):
   - Remove the `if (currentLevel >= 15)` early return that sets progressPercentage: 100
   - Add prestige path: if currentLevelData.isPrestige, compute progress within current prestige tier
   - Add level 30 (not yet prestige): compute progress toward first prestige tier (PRESTIGE_BASE_XP + PRESTIGE_XP_PER_TIER)
   - Normal levels 1-29: keep existing logic unchanged
   - Add `isPrestige` field to all return objects

6. Add isPrestige/prestigeTier to the default export object.

Run tests — all must PASS.

IMPORTANT: Do NOT modify calculateNodeXP, awardXP, getStudentXP, calculateSessionXP, or getXPLeaderboard — they are consumers, not computation functions. Do NOT rename existing levels 1-15.
  </action>
  <verify>
    <automated>npx vitest run src/utils/xpSystem.test.js -x</automated>
  </verify>
  <done>All xpSystem.test.js tests pass. XP_LEVELS has 30 entries. calculateLevel returns prestige objects for XP > 54000. getLevelProgress returns correct progress for all level ranges including prestige. getNextLevelXP never returns 0.</done>
</task>

<task type="auto">
  <name>Task 2: Postgres migration — 30-level award_xp, constraint fix, data fixup</name>
  <files>supabase/migrations/20260307000001_extend_xp_levels.sql</files>
  <action>
Create a new migration file `supabase/migrations/20260307000001_extend_xp_levels.sql` with these steps in order:

1. **Drop old CHECK constraint** (currently caps at 10):
   ```sql
   ALTER TABLE students DROP CONSTRAINT IF EXISTS students_level_valid;
   ```

2. **Add new constraint** (no upper bound — prestige levels go beyond 30):
   ```sql
   ALTER TABLE students ADD CONSTRAINT students_level_valid CHECK (current_level >= 1);
   ```

3. **Replace award_xp function** with 30-level thresholds + prestige:
   - Drop existing: `DROP FUNCTION IF EXISTS award_xp(UUID, INTEGER);`
   - Create new function with:
     - Security check: `IF auth.uid() IS NULL THEN RAISE EXCEPTION 'Not authenticated'` and `IF auth.uid() != p_student_id THEN RAISE EXCEPTION 'Unauthorized'`
     - v_level_thresholds INTEGER[] with 30 entries matching xpSystem.js EXACTLY:
       `ARRAY[0, 100, 250, 450, 700, 1000, 1400, 1900, 2500, 3200, 4000, 5000, 6200, 7500, 9000, 10500, 12200, 14100, 16200, 18500, 21000, 23700, 26500, 29400, 32500, 35800, 39300, 43000, 46900, 51000]`
     - v_prestige_xp_per_tier := 3000
     - Loop `FOR i IN 1..30` to find level from thresholds
     - After loop: if v_new_level = 30, compute prestige tier from XP beyond 51000
     - Returns TABLE(new_total_xp INTEGER, new_level INTEGER, leveled_up BOOLEAN)
   - GRANT EXECUTE to authenticated

4. **Data fixup** — correct existing students whose current_level is capped at 10:
   ```sql
   -- Recalculate current_level for all students based on total_xp
   WITH level_calc AS (
     SELECT id, total_xp,
       CASE
         WHEN total_xp >= 51000 THEN 30 + FLOOR((total_xp - 51000)::numeric / 3000)::integer
         WHEN total_xp >= 46900 THEN 29
         WHEN total_xp >= 43000 THEN 28
         WHEN total_xp >= 39300 THEN 27
         WHEN total_xp >= 35800 THEN 26
         WHEN total_xp >= 32500 THEN 25
         WHEN total_xp >= 29400 THEN 24
         WHEN total_xp >= 26500 THEN 23
         WHEN total_xp >= 23700 THEN 22
         WHEN total_xp >= 21000 THEN 21
         WHEN total_xp >= 18500 THEN 20
         WHEN total_xp >= 16200 THEN 19
         WHEN total_xp >= 14100 THEN 18
         WHEN total_xp >= 12200 THEN 17
         WHEN total_xp >= 10500 THEN 16
         WHEN total_xp >= 9000 THEN 15
         WHEN total_xp >= 7500 THEN 14
         WHEN total_xp >= 6200 THEN 13
         WHEN total_xp >= 5000 THEN 12
         WHEN total_xp >= 4000 THEN 11
         WHEN total_xp >= 3200 THEN 10
         WHEN total_xp >= 2500 THEN 9
         WHEN total_xp >= 1900 THEN 8
         WHEN total_xp >= 1400 THEN 7
         WHEN total_xp >= 1000 THEN 6
         WHEN total_xp >= 700 THEN 5
         WHEN total_xp >= 450 THEN 4
         WHEN total_xp >= 250 THEN 3
         WHEN total_xp >= 100 THEN 2
         ELSE 1
       END AS correct_level
     FROM students
   )
   UPDATE students SET current_level = level_calc.correct_level
   FROM level_calc WHERE students.id = level_calc.id AND students.current_level != level_calc.correct_level;
   ```

CRITICAL: The 30 XP thresholds in the Postgres array MUST exactly match the xpSystem.js XP_LEVELS array. The PRESTIGE_XP_PER_TIER (3000) must match the JS constant. Any mismatch causes the leveled_up flag to fire incorrectly.

Add a comment at the top: `-- Phase 20: Extend XP levels from 10 (DB) / 15 (JS) to 30 + infinite prestige tiers`
  </action>
  <verify>
    <automated>cat supabase/migrations/20260307000001_extend_xp_levels.sql | head -5 && echo "---" && grep -c "WHEN total_xp" supabase/migrations/20260307000001_extend_xp_levels.sql</automated>
  </verify>
  <done>Migration file exists with: DROP old constraint, ADD new constraint (no upper bound), CREATE award_xp with 30 thresholds + prestige, GRANT to authenticated, data fixup UPDATE. All 30 thresholds match xpSystem.js exactly.</done>
</task>

<task type="auto">
  <name>Task 3: i18n keys — 15 new level names + prestige + celebration keys in EN and HE</name>
  <files>src/locales/en/common.json, src/locales/he/common.json</files>
  <action>
Add i18n keys to both locale files. Read each file first, then modify the xpLevels section and victory section.

**English (src/locales/en/common.json) — add to xpLevels object:**
```json
"Composer": "Composer",
"Conductor": "Conductor",
"Concert Master": "Concert Master",
"Prodigy": "Prodigy",
"Orchestrator": "Orchestrator",
"Music Sage": "Music Sage",
"Melodist": "Melodist",
"Symphonist": "Symphonist",
"Music Architect": "Music Architect",
"Philharmonic": "Philharmonic",
"Opus Creator": "Opus Creator",
"Concerto Star": "Concerto Star",
"Music Luminary": "Music Luminary",
"Grand Virtuoso": "Grand Virtuoso",
"Transcendent": "Transcendent",
"prestigeTitle": "Maestro {{tier}}"
```

**English — add to victory object:**
```json
"youAreNow": "You are now a {{title}}!",
"prestigeUnlocked": "Prestige Unlocked!",
"prestigeEntry": "You've reached the highest level! Welcome to Maestro tier!"
```

**Hebrew (src/locales/he/common.json) — add to xpLevels object:**
```json
"Composer": "מלחין",
"Conductor": "מנצח",
"Concert Master": "קונצרטמייסטר",
"Prodigy": "ילד פלא",
"Orchestrator": "מעבד תזמורתי",
"Music Sage": "חכם המוזיקה",
"Melodist": "מלודיסט",
"Symphonist": "סימפוניסט",
"Music Architect": "אדריכל המוזיקה",
"Philharmonic": "פילהרמוני",
"Opus Creator": "יוצר אופוס",
"Concerto Star": "כוכב הקונצרטו",
"Music Luminary": "גדול המוזיקה",
"Grand Virtuoso": "וירטואוז גדול",
"Transcendent": "נעלה",
"prestigeTitle": "מאסטרו {{tier}}"
```

**Hebrew — add to victory object:**
```json
"youAreNow": "!{{title}} עכשיו אתה",
"prestigeUnlocked": "!יוקרה נפתחה",
"prestigeEntry": "!הגעת לרמה הגבוהה ביותר! ברוכים הבאים לרמת מאסטרו"
```

IMPORTANT: Do NOT modify any other keys in these files. Only ADD the new keys within the existing xpLevels and victory objects.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/locales/en/common.json'); const he=require('./src/locales/he/common.json'); const needed=['Composer','Conductor','Concert Master','Prodigy','Orchestrator','Music Sage','Melodist','Symphonist','Music Architect','Philharmonic','Opus Creator','Concerto Star','Music Luminary','Grand Virtuoso','Transcendent','prestigeTitle']; const missing=needed.filter(k=>!en.xpLevels[k]||!he.xpLevels[k]); console.log(missing.length===0?'PASS: All 16 new xpLevels keys present in EN and HE':'FAIL: Missing keys: '+missing.join(', ')); const vKeys=['youAreNow','prestigeUnlocked','prestigeEntry']; const vMissing=vKeys.filter(k=>!en.victory[k]||!he.victory[k]); console.log(vMissing.length===0?'PASS: All victory keys present':'FAIL: Missing victory keys: '+vMissing.join(', '))"</automated>
  </verify>
  <done>Both EN and HE common.json contain all 16 new xpLevels keys (15 level names + prestigeTitle) and 3 new victory keys (youAreNow, prestigeUnlocked, prestigeEntry). Existing keys unchanged.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/utils/xpSystem.test.js -x` — all unit tests pass
2. Migration file has correct SQL syntax with 30 thresholds matching JS
3. EN and HE locale files have all new keys
4. `npm run test:run` — full test suite passes (no regressions)
</verification>

<success_criteria>
- xpSystem.js exports 30-level XP_LEVELS array + prestige constants
- calculateLevel, getLevelProgress, getNextLevelXP all handle levels 1-30 and prestige
- Postgres migration ready to deploy (constraint fix + 30-level function + data fixup)
- All level names localized in EN and HE
- No changes to existing levels 1-15
- No changes to consumer functions (awardXP, calculateSessionXP, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/20-component-integration-tab-navigation/20-01-SUMMARY.md`
</output>
