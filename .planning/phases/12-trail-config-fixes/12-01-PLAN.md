---
phase: 12-trail-config-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/units/rhythmUnit1Redesigned.js
  - src/data/units/rhythmUnit2Redesigned.js
  - src/data/units/rhythmUnits.difficulty.test.js
  - scripts/validateTrail.mjs
autonomous: true
requirements: [TCFG-02]

must_haves:
  truths:
    - "All rhythm unit exercise configs use only 'beginner', 'intermediate', or 'advanced' for difficulty"
    - "Build fails if any exercise config has an invalid difficulty value"
    - "Build fails if any rhythmPatterns array contains unrecognized duration names"
    - "A regression test prevents difficulty value mismatch from recurring"
  artifacts:
    - path: "src/data/units/rhythmUnit1Redesigned.js"
      provides: "Corrected difficulty values for rhythm unit 1"
      contains: "difficulty: 'beginner'"
    - path: "src/data/units/rhythmUnit2Redesigned.js"
      provides: "Corrected difficulty values for rhythm unit 2"
      contains: "difficulty: 'beginner'"
    - path: "src/data/units/rhythmUnits.difficulty.test.js"
      provides: "Cross-unit regression test for difficulty values"
      exports: ["describe block importing all 8 units"]
    - path: "scripts/validateTrail.mjs"
      provides: "Build-time validation for difficulty and rhythmPatterns"
      contains: "validateExerciseDifficultyValues"
  key_links:
    - from: "scripts/validateTrail.mjs"
      to: "src/data/units/rhythmUnit*Redesigned.js"
      via: "SKILL_NODES import (aggregated from all units)"
      pattern: "VALID.*beginner.*intermediate.*advanced"
---

<objective>
Fix rhythm trail node difficulty value mismatch and add guardrails to prevent recurrence.

Purpose: Units 1-2 use `'easy'` for difficulty but the pattern generator only recognizes `'beginner'`/`'intermediate'`/`'advanced'`. This means trail nodes in units 1-2 silently fall back to beginner difficulty regardless of intent. Fixing the data and adding build-time + test-time validation closes this gap permanently.

Output: Corrected data files, a regression test covering all 8 units, and an enhanced build validator that catches invalid difficulty values and rhythm pattern names.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-trail-config-fixes/12-CONTEXT.md
@.planning/phases/12-trail-config-fixes/12-RESEARCH.md

<interfaces>
<!-- Key types and constants the executor needs -->

From src/components/games/rhythm-games/RhythmPatternGenerator.js:
```javascript
export const DIFFICULTY_LEVELS = {
  BEGINNER: "beginner",
  INTERMEDIATE: "intermediate",
  ADVANCED: "advanced",
};
```

From scripts/validateTrail.mjs (existing pattern to follow):
```javascript
function validateExerciseTypes() {
  console.log('\nChecking exercise types...');
  const validTypes = new Set(Object.values(EXERCISE_TYPES));
  let invalidCount = 0;
  for (const node of SKILL_NODES) {
    for (const exercise of (node.exercises || [])) {
      if (!validTypes.has(exercise.type)) {
        console.error(`  ERROR: Unknown exercise type "${exercise.type}" in node "${node.id}"`);
        hasErrors = true;
        invalidCount++;
      }
    }
  }
  if (invalidCount === 0) console.log('  Exercise types: OK');
  else console.error(`  Found ${invalidCount} unknown exercise type(s)`);
}
```

From scripts/validateTrail.mjs (main execution block, line 266-295):
```javascript
// MAIN EXECUTION
console.log('='.repeat(50));
console.log('Trail Validation');
console.log('='.repeat(50));
console.log(`Validating ${SKILL_NODES.length} trail nodes...`);

validatePrerequisiteChains();
validateNodeTypes();
validateDuplicateIds();
validateXPEconomy();
validateExerciseTypes();

console.log('\n' + '='.repeat(50));
if (hasErrors) {
  console.error('Validation FAILED. Build aborted.');
  // ...
}
```

Rhythm unit data file export names (all 8 units):
```javascript
export const rhythmUnit1Nodes = [...]; // src/data/units/rhythmUnit1Redesigned.js
export const rhythmUnit2Nodes = [...]; // src/data/units/rhythmUnit2Redesigned.js
export const rhythmUnit3Nodes = [...]; // src/data/units/rhythmUnit3Redesigned.js
export const rhythmUnit4Nodes = [...]; // src/data/units/rhythmUnit4Redesigned.js
export const rhythmUnit5Nodes = [...]; // src/data/units/rhythmUnit5Redesigned.js
export const rhythmUnit6Nodes = [...]; // src/data/units/rhythmUnit6Redesigned.js
export const rhythmUnit7Nodes = [...]; // src/data/units/rhythmUnit7Redesigned.js
export const rhythmUnit8Nodes = [...]; // src/data/units/rhythmUnit8Redesigned.js
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix difficulty values in units 1-2 and create regression test (TCFG-02, D-01, D-06)</name>
  <files>
    src/data/units/rhythmUnit1Redesigned.js,
    src/data/units/rhythmUnit2Redesigned.js,
    src/data/units/rhythmUnits.difficulty.test.js
  </files>
  <read_first>
    src/data/units/rhythmUnit1Redesigned.js,
    src/data/units/rhythmUnit2Redesigned.js,
    src/data/units/rhythmUnit7Redesigned.test.js
  </read_first>
  <action>
**Data fix (per D-01):** In `rhythmUnit1Redesigned.js`, replace all 5 occurrences of `difficulty: 'easy'` with `difficulty: 'beginner'`. The occurrences are at lines 69, 121, 172, 223, 274. Do NOT change lines that already have `'intermediate'` (lines ~326, ~378).

In `rhythmUnit2Redesigned.js`, replace all 5 occurrences of `difficulty: 'easy'` with `difficulty: 'beginner'`. The occurrences are at lines 69, 121, 172, 223, 274. Do NOT change lines that already have `'intermediate'`.

**Regression test (per D-06):** Create `src/data/units/rhythmUnits.difficulty.test.js` with:

```javascript
import { describe, it, expect } from 'vitest';
import { rhythmUnit1Nodes } from './rhythmUnit1Redesigned.js';
import { rhythmUnit2Nodes } from './rhythmUnit2Redesigned.js';
import { rhythmUnit3Nodes } from './rhythmUnit3Redesigned.js';
import { rhythmUnit4Nodes } from './rhythmUnit4Redesigned.js';
import { rhythmUnit5Nodes } from './rhythmUnit5Redesigned.js';
import { rhythmUnit6Nodes } from './rhythmUnit6Redesigned.js';
import { rhythmUnit7Nodes } from './rhythmUnit7Redesigned.js';
import { rhythmUnit8Nodes } from './rhythmUnit8Redesigned.js';

const VALID_DIFFICULTIES = ['beginner', 'intermediate', 'advanced'];
const allUnits = [
  { name: 'Unit 1', nodes: rhythmUnit1Nodes },
  { name: 'Unit 2', nodes: rhythmUnit2Nodes },
  { name: 'Unit 3', nodes: rhythmUnit3Nodes },
  { name: 'Unit 4', nodes: rhythmUnit4Nodes },
  { name: 'Unit 5', nodes: rhythmUnit5Nodes },
  { name: 'Unit 6', nodes: rhythmUnit6Nodes },
  { name: 'Unit 7', nodes: rhythmUnit7Nodes },
  { name: 'Unit 8', nodes: rhythmUnit8Nodes },
];

describe('Rhythm unit difficulty values (D-06 regression guard)', () => {
  it.each(allUnits)('$name: all exercise configs use valid difficulty values', ({ nodes }) => {
    nodes.forEach(node => {
      (node.exercises || []).forEach(ex => {
        if (ex.config?.difficulty !== undefined) {
          expect(
            VALID_DIFFICULTIES,
            `Node "${node.id}" exercise has difficulty "${ex.config.difficulty}" — expected one of ${VALID_DIFFICULTIES.join(', ')}`
          ).toContain(ex.config.difficulty);
        }
      });
    });
  });

  it('no exercise config uses legacy "easy" or "medium" or "hard" difficulty', () => {
    const INVALID = ['easy', 'medium', 'hard'];
    const violations = [];
    allUnits.forEach(({ name, nodes }) => {
      nodes.forEach(node => {
        (node.exercises || []).forEach(ex => {
          if (INVALID.includes(ex.config?.difficulty)) {
            violations.push(`${name} node "${node.id}": "${ex.config.difficulty}"`);
          }
        });
      });
    });
    expect(violations).toEqual([]);
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnits.difficulty.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rhythmUnit1Redesigned.js` contains zero occurrences of `difficulty: 'easy'` (grep returns empty)
    - `rhythmUnit2Redesigned.js` contains zero occurrences of `difficulty: 'easy'` (grep returns empty)
    - `rhythmUnit1Redesigned.js` contains exactly 5 occurrences of `difficulty: 'beginner'`
    - `rhythmUnit2Redesigned.js` contains exactly 5 occurrences of `difficulty: 'beginner'`
    - `rhythmUnits.difficulty.test.js` exists and imports all 8 rhythm unit node arrays
    - `rhythmUnits.difficulty.test.js` contains `VALID_DIFFICULTIES` array with exactly `['beginner', 'intermediate', 'advanced']`
    - `npx vitest run src/data/units/rhythmUnits.difficulty.test.js` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All rhythm unit data files use only valid difficulty values. Regression test confirms all 8 units pass validation.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance build validator with difficulty and rhythmPatterns checks (D-07)</name>
  <files>
    scripts/validateTrail.mjs
  </files>
  <read_first>
    scripts/validateTrail.mjs
  </read_first>
  <action>
Add two new validation functions to `scripts/validateTrail.mjs`, placed AFTER the existing `validateExerciseTypes()` function definition (before the `// MAIN EXECUTION` comment at line 266). Follow the exact same pattern as `validateExerciseTypes()`.

**Function 1: `validateExerciseDifficultyValues()`**

```javascript
function validateExerciseDifficultyValues() {
  console.log('\nChecking exercise difficulty values...');
  const VALID = new Set(['beginner', 'intermediate', 'advanced']);
  let invalidCount = 0;
  for (const node of SKILL_NODES) {
    for (const exercise of (node.exercises || [])) {
      const d = exercise.config?.difficulty;
      if (d !== undefined && !VALID.has(d)) {
        console.error(`  ERROR: Invalid difficulty "${d}" in node "${node.id}"`);
        hasErrors = true;
        invalidCount++;
      }
    }
  }
  if (invalidCount === 0) console.log('  Exercise difficulty values: OK');
  else console.error(`  Found ${invalidCount} invalid difficulty value(s)`);
}
```

**Function 2: `validateRhythmPatternNames()`**

```javascript
function validateRhythmPatternNames() {
  console.log('\nChecking rhythmPatterns duration names...');
  const VALID = new Set([
    'whole', 'half', 'quarter', 'eighth', 'sixteenth',
    'dotted-half', 'dotted-quarter', 'dotted-eighth',
    'quarter-triplet', 'eighth-triplet', 'sixteenth-triplet'
  ]);
  let invalidCount = 0;
  for (const node of SKILL_NODES) {
    for (const exercise of (node.exercises || [])) {
      for (const pattern of (exercise.config?.rhythmPatterns || [])) {
        if (!VALID.has(pattern)) {
          console.error(`  ERROR: Unknown rhythmPattern "${pattern}" in node "${node.id}"`);
          hasErrors = true;
          invalidCount++;
        }
      }
    }
  }
  if (invalidCount === 0) console.log('  Rhythm pattern names: OK');
  else console.error(`  Found ${invalidCount} invalid rhythm pattern name(s)`);
}
```

**Wire into main execution block:** Add calls to both functions after the existing `validateExerciseTypes();` call (line 279). The main execution block should become:

```javascript
validatePrerequisiteChains();
validateNodeTypes();
validateDuplicateIds();
validateXPEconomy();
validateExerciseTypes();
validateExerciseDifficultyValues();
validateRhythmPatternNames();
```
  </action>
  <verify>
    <automated>npm run verify:trail</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validateTrail.mjs` contains function `validateExerciseDifficultyValues`
    - `scripts/validateTrail.mjs` contains function `validateRhythmPatternNames`
    - `scripts/validateTrail.mjs` main execution block calls `validateExerciseDifficultyValues()` after `validateExerciseTypes()`
    - `scripts/validateTrail.mjs` main execution block calls `validateRhythmPatternNames()` after `validateExerciseDifficultyValues()`
    - `npm run verify:trail` exits 0 (all validation passes, including the new checks)
    - `npm run build` exits 0 (prebuild hook runs validator successfully)
  </acceptance_criteria>
  <done>Build validator catches invalid difficulty values and unrecognized rhythmPatterns names. Current data passes all checks. Future violations will break the build.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/data/units/rhythmUnits.difficulty.test.js` -- all difficulty regression tests pass
2. `npm run verify:trail` -- trail validation passes including new difficulty + rhythmPatterns checks
3. `grep -c "difficulty: 'easy'" src/data/units/rhythmUnit1Redesigned.js src/data/units/rhythmUnit2Redesigned.js` -- both return 0
4. `npm run build` -- full build succeeds (prebuild validator passes)
</verification>

<success_criteria>
- Zero occurrences of `difficulty: 'easy'` in any rhythm unit data file
- New regression test covering all 8 units passes
- Build validator rejects invalid difficulty values and unknown rhythm pattern names
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-trail-config-fixes/12-01-SUMMARY.md`
</output>
