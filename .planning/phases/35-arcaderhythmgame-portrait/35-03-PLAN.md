---
phase: 35
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md
autonomous: false
requirements:
  - ARCADE-01
tags:
  - rhythm
  - arcade
  - spike
  - manual-uat
  - decision-record
must_haves:
  truths:
    - "35-SPIKE.md exists at .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md"
    - "35-SPIKE.md opens with a Verdict box at the top of the file containing exactly one of: SHIP-VERTICAL or ROTATE-PROMPT (per UI-SPEC SPIKE.md Structure Contract)"
    - "Verdict was reached by the project owner after playing on at least: iPhone SE portrait (375×667), one larger phone portrait (e.g., iPhone 14), AND iPad portrait (D-03)"
    - "Both 2-bar and 4-bar pattern variants were tested per D-03 (worst-case tile density check)"
    - "On-the-fence outcomes default to ROTATE-PROMPT per D-07 (the safer path)"
    - "35-SPIKE.md cites the next-step CONTEXT.md decision: D-12 (rotate-prompt cleanup) OR D-13 (ship-vertical promotion)"
  artifacts:
    - path: .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md
      provides: "Audit-trail spike record satisfying ARCADE-01 SC #1"
      contains: "Verdict"
  key_links:
    - from: .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md
      to: src/components/games/rhythm-games/ArcadeRhythmGame.jsx
      via: "Verdict drives Plan 04 branch selection (D-12 rotate-prompt OR D-13 ship-vertical)"
      pattern: "Verdict:"
---

<objective>
Run the portrait feel-test on real phones using the `?spike-portrait` flag instrument shipped in Plan 02, then write `35-SPIKE.md` capturing the verdict, observations, and next step. This plan is the audit trail for ARCADE-01 SC #1 ("A throwaway prototype has been run on a real phone in portrait, and the spike outcome is documented as a recorded decision").

This plan is `autonomous: false` because the feel-test is fundamentally subjective (D-04: "Subjective feel-test is the primary criterion") and requires the project owner to physically play on real devices for ~10–15 min per device per D-03. Claude cannot perform this work.

Claude's role in this plan is bounded to:

1. Coaching the owner through the test surface (devices, patterns, time per device)
2. Authoring `35-SPIKE.md` based on the owner's reported observations using the structure mandated by UI-SPEC's SPIKE.md Structure Contract
3. Surfacing D-07 (on-the-fence defaults to rotate-prompt) at decision time

Per D-04 + D-07, the verdict is binary: SHIP-VERTICAL or ROTATE-PROMPT. There is no "maybe" or "needs more testing" — that response IS rotate-prompt by tie-break rule.

Per UI-SPEC SPIKE.md Structure Contract, the file uses a verdict-box-at-top format (not Phase 34 prose style) so that planner/executor reading the file see the outcome immediately. The Verdict section MUST appear before any findings.

Purpose: Generate the decision artifact that drives Plan 04's branch selection.
Output: 1 NEW file (`.planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md
@.planning/phases/35-arcaderhythmgame-portrait/35-UI-SPEC.md
@CLAUDE.md
@src/components/games/rhythm-games/ArcadeRhythmGame.jsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner runs portrait feel-test on real devices</name>
  <files>(none — manual feel-test on real devices; no file artifacts produced by this task; the spike record file 35-SPIKE.md is created by Task 2 below from the owner-reported verdict)</files>
  <read_first>
    - .planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md (D-03 test surface — devices and patterns; D-04 subjective criterion; D-05 owner-only testing; D-07 tie-break rule)
    - .planning/phases/35-arcaderhythmgame-portrait/35-UI-SPEC.md (Portrait Viewport Contract — lane geometry at 375×667; tile density concern at 4-bar 4/4)
  </read_first>
  <what-built>
    Plan 02 shipped the `?spike-portrait` URL flag instrument. The owner now needs to:
    1. Start the dev server: `npm run dev` (port 5174)
    2. Open the dev URL on each test device (or use Chrome DevTools device emulation if no physical device available — but D-03 explicitly calls for "real phones", so prefer physical devices)
    3. For each device, navigate to `/rhythm-mode/arcade-rhythm-game?spike-portrait` and confirm the rotate prompt does NOT appear (it should not — the spike declares `useDeclareNeedsLandscape(false)` when the flag is on)
    4. Play the game for 10–15 min per device per D-03 timebox
    5. Test BOTH short patterns (2-bar) AND long patterns (4-bar) per D-03 worst-case density check
    6. Form a subjective verdict per D-04: "does this feel like an arcade rhythm game I'd ship to 8yos?"
  </what-built>
  <how-to-verify>
    **Test surface (D-03 — all three required):**
    1. iPhone SE portrait (375×667) — the floor target per UI-SPEC
    2. iPhone 14 portrait (390×844) — secondary target per UI-SPEC
    3. iPad portrait (any iPad size, ≥768px wide) — informs tablet behavior per D-03
       - Note: per Plan 02, the spike instrument currently does NOT yet split phone-vs-tablet declarations (that comes in Plan 04). On iPad portrait with `?spike-portrait`, the declaration is `false` — same as phone. This is correct for the spike: the owner is checking that iPad portrait also feels playable in vertical-lane (D-09 confirms tablet should always play). Plan 04 codifies the per-viewport declaration.

    **Patterns to test (D-03):**
    - At least one 2-bar pattern (low tile density)
    - At least one 4-bar pattern (high tile density — this is the worst-case feel test per UI-SPEC "Tile density concern")

    **What to observe (UI-SPEC Portrait Viewport Contract):**
    - Tile travel time: ~750ms per beat at BPM 80; tile lane height ~571px on iPhone SE portrait
    - Density check: 3–5 tiles simultaneously visible on 4-bar dense patterns — does this read as "readable arcade rhythm" or "overwhelming"?
    - Hit zone tap target: currently `h-12` (48px). Does it feel reachable with thumb in portrait? (UI-SPEC notes planner may promote to `h-16` (64px) on phone-portrait if owner reports miss-frustration)
    - Header at 375px: counter + lives + combo all fit without wrapping?
    - Orientation rotation mid-game: if device is rotated to landscape during play, the game should continue (not break) — extra screen space = wider tiles

    **Steps:**
    1. Boot dev server: `npm run dev`
    2. On each device, navigate to `http://[your-dev-ip]:5174/rhythm-mode/arcade-rhythm-game?spike-portrait`
    3. Verify NO rotate prompt appears (confirms the spike instrument is wired correctly)
    4. Play 2-bar pattern for ~5 min — note "felt right" / "felt wrong" observations
    5. Play 4-bar pattern for ~5 min — note tile density readability
    6. Test orientation rotation mid-game (rotate device to landscape, then back to portrait) — note any breakage
    7. Repeat for next device
    8. After all 3 devices tested, form binary verdict: SHIP-VERTICAL or ROTATE-PROMPT (D-07: any uncertainty defaults to ROTATE-PROMPT)
    9. Report observations and verdict to Claude — Claude drafts `35-SPIKE.md` in Task 2

    **Expected outcome (either is valid):**
    - SHIP-VERTICAL: portrait feels good; tile density at 4-bar is readable; hit zone is reachable. Plan 04 will promote this to default and remove the flag (D-13 cleanup).
    - ROTATE-PROMPT: portrait feels off (density overwhelming, hit zone awkward, or just "not arcade enough"). Plan 04 will declare `needsLandscape=true` on phone always and remove the flag (D-12 cleanup).

  </how-to-verify>
  <acceptance_criteria>
    **Acceptance is owner-reported because the spike is fundamentally subjective per D-04. The following must be confirmed verbally or in writing by the owner before this task can be marked complete:**
    - Owner has played the spike build on iPhone SE portrait (or equivalent ≤375px viewport) for ~10–15 min
    - Owner has played the spike build on a larger phone portrait (iPhone 14 or equivalent) for ~10–15 min
    - Owner has played the spike build on iPad portrait for ~10–15 min
    - At least one 2-bar pattern AND at least one 4-bar pattern were exercised
    - Owner has formed a binary verdict: SHIP-VERTICAL or ROTATE-PROMPT (or has acknowledged "I'm on the fence" → defaults to ROTATE-PROMPT per D-07)
    - Owner has reported observations to Claude (what felt right / what felt wrong) — these populate the Observations section of 35-SPIKE.md in Task 2

    **Note:** This task is exempt from grep-verifiable acceptance per the planning_context "Plan 03 manual UAT items are exempt from grep-verifiability — those are the explicit subjective feel-test per D-04". Plan 04's prerequisite (35-SPIKE.md exists with parseable Verdict header) IS grep-verifiable and is enforced in Task 2 below + in Plan 04's `<read_first>`.

</acceptance_criteria>
<action>This task IS the checkpoint — Claude does NOT take any automated action here. The work is performed by the project owner per the <how-to-verify> steps above (boot dev server, navigate each test device to the spike URL, play 2-bar + 4-bar patterns for ~10-15 min per device, form a binary verdict per D-04, apply D-07 tie-break if uncertain). Claude waits for the owner to reply with the verdict + observations via the <resume-signal> below, then proceeds to Task 2.</action>
<verify>
<automated>MISSING — this is a manual feel-test (D-04 subjective criterion); no automated verification is possible in this task. Plan 04 verifies the resulting 35-SPIKE.md artifact created in Task 2 via grep-parseable Verdict header.</automated>
</verify>
<done>Owner has played the spike build on all 3 required device classes (iPhone SE portrait, larger phone portrait, iPad portrait), tested both 2-bar and 4-bar patterns, formed a binary verdict (SHIP-VERTICAL or ROTATE-PROMPT, with D-07 tie-break applied if uncertain), and reported the verdict + observations to Claude via the resume-signal so Task 2 can author 35-SPIKE.md.</done>
<resume-signal>Reply with the verdict (SHIP-VERTICAL or ROTATE-PROMPT) and a 2–4 bullet summary of what felt right/wrong on each device. Claude will then draft 35-SPIKE.md in Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author 35-SPIKE.md per UI-SPEC Structure Contract</name>
  <files>.planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md</files>
  <read_first>
    - .planning/phases/35-arcaderhythmgame-portrait/35-UI-SPEC.md (specifically the "SPIKE.md Structure Contract" section — verdict box format, required sections, ordering rule "verdict box MUST appear before any findings")
    - .planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md (D-04 subjective criterion, D-06 verdict-recorded-in-SPIKE-md, D-07 tie-break rule, D-12 rotate-prompt cleanup, D-13 ship-vertical promotion)
  </read_first>
  <action>
    Create `.planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` using the structure mandated by UI-SPEC's SPIKE.md Structure Contract. The 5 required sections in order are: (1) Verdict box, (2) Test Surface, (3) Observations, (4) Decision Criteria Check, (5) Next Step.

    **Use the owner's verdict and observations from Task 1's resume signal.** Do NOT make up observations — only record what the owner reported. If the owner reported uncertainty, apply D-07 and write `Verdict: ROTATE-PROMPT` with rationale "Owner reported uncertainty; D-07 tie-break defaults to rotate-prompt (lower-risk path)."

    **Template to fill in (verbatim structure; substitute bracketed placeholders with owner-reported content):**

    ```markdown
    # Phase 35 — ArcadeRhythmGame Portrait Spike

    ## Verdict: [SHIP-VERTICAL | ROTATE-PROMPT]

    **Rationale:** [1–2 sentences from owner reporting why this verdict was reached]

    **Date:** [YYYY-MM-DD — today's date]

    **Decided by:** Project owner (per CONTEXT.md D-05 — kid testers deferred to post-beta)

    ---

    ## Test Surface

    | Device | Viewport | Time Spent | Patterns Tested |
    |--------|----------|------------|-----------------|
    | iPhone SE portrait | 375×667 | [N min] | 2-bar, 4-bar |
    | [Larger phone — e.g., iPhone 14] portrait | [e.g., 390×844] | [N min] | 2-bar, 4-bar |
    | iPad portrait | [e.g., 820×1180] | [N min] | 2-bar, 4-bar |

    Spike instrument: `?spike-portrait` URL flag (Plan 35-02), gated on `import.meta.env.DEV`.

    ---

    ## Observations

    ### What felt right
    - [Owner-reported bullet 1]
    - [Owner-reported bullet 2]
    - [...]

    ### What felt wrong
    - [Owner-reported bullet 1]
    - [Owner-reported bullet 2]
    - [...]

    ### Tile density at 4-bar (UI-SPEC primary failure mode)
    [Owner's specific note on whether 3–5 simultaneous tiles read as "readable arcade rhythm" or "overwhelming" per UI-SPEC "Tile density concern" section]

    ### Hit zone tap target (UI-SPEC Spacing Scale exception)
    [Owner's note on whether the current `h-12` (48px) feels reachable, or whether `h-16` (64px) on phone-portrait would be a worthwhile polish bump per UI-SPEC]

    ---

    ## Decision Criteria Check

    - **D-04 (Subjective feel-test is primary criterion):** Confirmed — verdict reached via owner's subjective judgment after real-device play.
    - **D-07 (On-the-fence defaults to ROTATE-PROMPT):** [Used / Not used — if "Used", note that the verdict is ROTATE-PROMPT because owner reported uncertainty; if "Not used", note that owner reported confident SHIP-VERTICAL or confident ROTATE-PROMPT verdict]

    ---

    ## Next Step

    [If SHIP-VERTICAL:]
    Plan 35-04 promotes the spike behavior to default per CONTEXT.md D-13:
    - Remove `?spike-portrait` flag from `ArcadeRhythmGame.jsx`
    - Make `useDeclareNeedsLandscape(...)` viewport-aware (false on both phone and tablet)
    - Replace lines 122–127 TODO + the "W2 Android PWA regression guard" comment with a permanent comment citing this 35-SPIKE.md file
    - Bundle minor polish surfaced above (e.g., hit zone bump if applicable) into Plan 04, OR split into a Phase 35.5 follow-up if the punch list is large

    [If ROTATE-PROMPT:]
    Plan 35-04 ships the rotate-prompt path per CONTEXT.md D-12:
    - Remove `?spike-portrait` flag from `ArcadeRhythmGame.jsx`
    - Make `useDeclareNeedsLandscape(viewportWidth < 768)` (true on phone <768, false on tablet ≥768) — phone shows rotate prompt, tablet plays in any orientation
    - Replace lines 122–127 TODO with a permanent comment citing this 35-SPIKE.md file and the rotate-prompt decision rationale
    - Single-commit shipping change

    ---

    _Phase: 35-ArcadeRhythmGame Portrait_
    _Spike completed: [YYYY-MM-DD]_
    _Plan: 35-03_
    ```

    **After authoring, validate the file:**
    1. Confirm the verdict line matches exactly one of: `## Verdict: SHIP-VERTICAL` or `## Verdict: ROTATE-PROMPT`
    2. Confirm all 5 required sections (Verdict, Test Surface, Observations, Decision Criteria Check, Next Step) are present in order
    3. Confirm the Verdict section is the FIRST `##` heading after the file title (UI-SPEC structural requirement)

    Commit message:
    ```
    docs(35-03): record portrait spike verdict in 35-SPIKE.md (ARCADE-01)

    Owner ran the ?spike-portrait feel-test on iPhone SE portrait, [larger
    phone] portrait, and iPad portrait per D-03. Verdict: [SHIP-VERTICAL or
    ROTATE-PROMPT]. Plan 35-04 will [promote spike behavior to default per
    D-13 / ship rotate-prompt path per D-12].
    ```

  </action>
  <verify>
    <automated>test -f .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md && echo OK || (echo "35-SPIKE.md missing" && exit 1)</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md','utf8');const errs=[];if(!/^## Verdict: (SHIP-VERTICAL|ROTATE-PROMPT)/m.test(t))errs.push('Missing or malformed Verdict header (must be ## Verdict: SHIP-VERTICAL or ## Verdict: ROTATE-PROMPT at start of a line)');if(!/## Test Surface/.test(t))errs.push('Missing Test Surface section');if(!/## Observations/.test(t))errs.push('Missing Observations section');if(!/## Decision Criteria Check/.test(t))errs.push('Missing Decision Criteria Check section');if(!/## Next Step/.test(t))errs.push('Missing Next Step section');if(!/D-04/.test(t))errs.push('Missing D-04 citation in Decision Criteria Check');if(!/D-07/.test(t))errs.push('Missing D-07 citation in Decision Criteria Check');const verdictIdx=t.search(/^## Verdict:/m);const obsIdx=t.search(/^## Observations/m);if(verdictIdx<0||obsIdx<0||verdictIdx>=obsIdx)errs.push('Verdict section must precede Observations section per UI-SPEC ordering rule');if(errs.length){console.error(errs.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` exit 0
    - Verdict header matches the strict pattern Plan 04 will read: `grep -cE "^## Verdict: (SHIP-VERTICAL|ROTATE-PROMPT)" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns exactly `1`
    - All 5 required sections present (in order): `grep -cE "^## (Verdict|Test Surface|Observations|Decision Criteria Check|Next Step)" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least `5`
    - Decision criteria citations present: `grep -c "D-04" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least `1` AND `grep -c "D-07" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least `1`
    - Verdict section appears BEFORE Observations section (UI-SPEC ordering rule, enforced by the `verdictIdx < obsIdx` check in the `<verify>` node script above)
    - Test Surface table includes all 3 required devices (iPhone SE portrait, larger phone portrait, iPad portrait): `grep -c "iPhone SE portrait" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least `1` AND `grep -c "iPad portrait" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least `1`
    - Next Step section cites the matching CONTEXT.md decision: if Verdict is SHIP-VERTICAL, the file contains `D-13`; if ROTATE-PROMPT, the file contains `D-12`. Verify with: `grep -E "D-1[23]" .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md` returns at least one match
  </acceptance_criteria>
  <done>35-SPIKE.md authored with verdict box at top, all 5 sections in order, parseable verdict header, decision-criteria citations present; committed; Plan 04 unblocked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                    |
| ------------------ | ------------------------------------------------------------------------------ |
| (none — docs only) | No new code shipped; markdown record authored from owner-reported observations |

## STRIDE Threat Register

| Threat ID | Category | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                         |
| --------- | -------- | --------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-05   | (none)   | .planning/phases/35-arcaderhythmgame-portrait/35-SPIKE.md | accept      | No new threat surface — developer-authored markdown record, not shipped to users. The spike instrument used during testing (Plan 02) is dev-only and tree-shaken in production. No PII, no auth, no DB writes, no client-runtime impact from this plan. |

</threat_model>

<verification>
- 35-SPIKE.md exists with parseable Verdict header (SHIP-VERTICAL or ROTATE-PROMPT)
- All 5 required sections present in the order mandated by UI-SPEC SPIKE.md Structure Contract
- Verdict box appears before Observations (so any reader sees the outcome immediately)
- Owner-reported observations recorded in Observations section
- D-04 + D-07 citations present in Decision Criteria Check
- Next Step section names the matching CONTEXT.md decision (D-12 for ROTATE-PROMPT, D-13 for SHIP-VERTICAL)
- ARCADE-01 fully covered (combined with Plan 02's spike instrument)
</verification>

<success_criteria>

- ARCADE-01 SC #1 satisfied: "spike outcome is documented as a recorded decision"
- Plan 04 has an unambiguous, parseable verdict to read in its `<read_first>`
- Audit trail exists for future reference (e.g., post-beta retro: "why did we pick rotate-prompt over vertical?")
- Owner has personally validated portrait playability on real phones (D-04 subjective gate honored)
  </success_criteria>

<output>
After completion, create `.planning/phases/35-arcaderhythmgame-portrait/35-03-SUMMARY.md`
</output>
</content>
