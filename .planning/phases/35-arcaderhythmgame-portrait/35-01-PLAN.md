---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ARCADE-02
tags:
  - rhythm
  - arcade
  - docs
  - roadmap-correction
must_haves:
  truths:
    - "ROADMAP.md Phase 35 Success Criteria #3 reads 'vertical-lane layout' (not 'horizontal-lanes layout')"
    - "Phase 35 contract is internally consistent: SC #3, 35-CONTEXT.md D-08/D-11, and the actual ArcadeRhythmGame.jsx code all describe the same vertical-lane reality"
    - "No other ROADMAP wording (Goal, SC #1, SC #2, dependency line) is altered"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Corrected Phase 35 SC #3 wording per D-11"
      contains: "vertical-lane layout"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md
      via: "D-11 mandates this docs correction; CONTEXT.md is the source of truth"
      pattern: "vertical-lane"
---

<objective>
Fix the ROADMAP.md Phase 35 Success Criteria #3 wording per D-11 BEFORE any substantive work lands, so the planner/researcher/executor on later plans inherit a consistent contract. Per D-11 (35-CONTEXT.md): SC #3 currently says "horizontal-lanes layout" — a documentation slip from the original backlog memo. The actual code has always been single vertical-lane (tiles fall top-to-bottom in one full-width lane), and both spike outcomes (SHIP-VERTICAL and ROTATE-PROMPT) preserve that. Correcting this early prevents anyone reading SC #3 from imagining a non-existent horizontal-lane render path.

Purpose: Eliminate the contract ambiguity surfaced during /gsd-discuss-phase 35 (D-08, D-11) before Plan 02–04 land.
Output: 1 single-line edit to `.planning/ROADMAP.md`, committed as a docs-only change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ROADMAP.md Phase 35 SC #3 wording per D-11</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (specifically the Phase 35 section starting at the line "### Phase 35: ArcadeRhythmGame Portrait" — read the full Phase 35 block including all 3 Success Criteria so you see the current SC #3 wording in context before editing)
    - .planning/phases/35-arcaderhythmgame-portrait/35-CONTEXT.md (specifically the "Tablet Behavior" section — D-08, D-09, D-10, D-11 — to confirm the corrected wording)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md`. Locate the Phase 35 Success Criteria block (currently around line 109–114). The current SC #3 reads (verbatim):

    ```
    3. On tablet-landscape (and on phone-landscape if the rotate-prompt path is chosen), ArcadeRhythmGame continues to render with the existing horizontal-lanes layout — no regression for the orientation it was originally designed for
    ```

    Replace it with (verbatim — note the THREE wording changes: "horizontal-lanes" → "vertical-lane", "the orientation" → "the rendering", and append a parenthetical citing 35-CONTEXT.md D-11):

    ```
    3. On tablet-landscape (and on phone-landscape if the rotate-prompt path is chosen), ArcadeRhythmGame continues to render with the existing single vertical-lane layout — no regression for the rendering it was originally designed for (wording corrected per 35-CONTEXT.md D-11; the original "horizontal-lanes" framing was a documentation slip — the code has always been single vertical-lane)
    ```

    Do NOT modify:
    - Phase 35 "Goal" line (line ~106)
    - Phase 35 "Depends on" line (line ~107)
    - Phase 35 "Requirements" line (line ~108)
    - Success Criteria #1 (line ~111)
    - Success Criteria #2 (line ~112)
    - "Plans: TBD" line (line ~114)
    - "UI hint: yes" line (line ~115)
    - Any other phase, milestone, or progress table row

    Commit message (single docs commit before Plans 02–04 land):

    ```
    docs(35): correct ROADMAP Phase 35 SC #3 wording per D-11

    The original SC #3 said "horizontal-lanes layout" but ArcadeRhythmGame
    has always been single vertical-lane. Updates the wording to match the
    actual code state and prevents downstream planning from inheriting
    confused requirements. See 35-CONTEXT.md D-08, D-11.
    ```

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/ROADMAP.md','utf8');const errs=[];if(/horizontal-lanes layout/.test(t))errs.push('SC #3 still says horizontal-lanes layout');if(!/single vertical-lane layout/.test(t))errs.push('SC #3 missing single vertical-lane layout wording');if(!/D-11/.test(t))errs.push('Missing D-11 citation in SC #3 parenthetical');if(errs.length){console.error(errs.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "horizontal-lanes layout" .planning/ROADMAP.md` returns `0` (the old wording is gone everywhere in the file)
    - `grep -c "single vertical-lane layout" .planning/ROADMAP.md` returns `1` (corrected wording present exactly once, in SC #3)
    - `grep -c "D-11" .planning/ROADMAP.md` returns at least `1` (citation appears in the new SC #3 parenthetical)
    - `grep -c "### Phase 35: ArcadeRhythmGame Portrait" .planning/ROADMAP.md` returns `1` (we did not accidentally duplicate or delete the Phase 35 header)
    - `grep -c "**Goal**: Decide and ship a portrait experience" .planning/ROADMAP.md` returns `1` (the Goal line is unchanged) — note: if grep matches differ due to existing wording, treat as advisory; the binding criterion is that NO line other than SC #3 changed (verify with `git diff -U0 .planning/ROADMAP.md` showing only the SC #3 block in the diff)
    - `git diff --stat .planning/ROADMAP.md` shows exactly one file changed; total insertions+deletions ≤ 4 lines
  </acceptance_criteria>
  <done>ROADMAP SC #3 reads "single vertical-lane layout" with D-11 citation; no other roadmap content altered; docs-only commit landed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                             |
| ------------------ | --------------------------------------------------------------------------------------- |
| (none — docs only) | No new code, no user input, no network/DB surface; markdown edit to a planning artifact |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | -------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-01   | (none)   | .planning/ROADMAP.md | accept      | No new threat surface — docs-only edit. No PII, no auth, no DB writes, no client-runtime impact. The file is internal planning content, not shipped to users. |

</threat_model>

<verification>
- ROADMAP.md SC #3 wording corrected ("horizontal-lanes layout" → "single vertical-lane layout")
- D-11 citation added in the SC #3 parenthetical
- No other ROADMAP content modified (single-block diff)
- Docs commit landed before Plan 02 starts
</verification>

<success_criteria>

- ROADMAP.md Phase 35 SC #3 is internally consistent with 35-CONTEXT.md D-08/D-11 and ArcadeRhythmGame.jsx code reality
- Downstream plans (02, 03, 04) inherit corrected SC #3 wording when they read ROADMAP.md
- Single, focused docs commit lands before substantive code/planning work begins
  </success_criteria>

<output>
After completion, create `.planning/phases/35-arcaderhythmgame-portrait/35-01-SUMMARY.md`
</output>
</content>
