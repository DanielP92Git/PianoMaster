---
phase: 22-service-layer-trail-wiring
plan: 04
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/data/units/rhythmUnit5Redesigned.js
  - src/data/units/rhythmUnit6Redesigned.js
  - src/data/units/rhythmUnit7Redesigned.js
  - src/data/units/rhythmUnit8Redesigned.js
autonomous: true
requirements:
  - PAT-03
  - PAT-05
must_haves:
  truths:
    - "All 28 nodes in Units 5-8 have patternTags in rhythmConfig and no patterns field"
    - "All 28 nodes in Units 5-8 use the correct exercise type per Phase 20 audit"
    - "rhythm_7_4 has focusDurations: [] (one-concept violation fixed per audit)"
    - "boss_rhythm_6 and boss_rhythm_8 retain their current arcade_rhythm exercise type (no violation per audit)"
    - "No node in Units 5-8 has exercise.config.rhythmPatterns field"
  artifacts:
    - path: "src/data/units/rhythmUnit5Redesigned.js"
      provides: "7 migrated rhythm nodes with patternTags"
      contains: "patternTags"
    - path: "src/data/units/rhythmUnit6Redesigned.js"
      provides: "7 migrated rhythm nodes with patternTags"
      contains: "patternTags"
    - path: "src/data/units/rhythmUnit7Redesigned.js"
      provides: "7 migrated rhythm nodes with patternTags"
      contains: "patternTags"
    - path: "src/data/units/rhythmUnit8Redesigned.js"
      provides: "7 migrated rhythm nodes with patternTags"
      contains: "patternTags"
  key_links:
    - from: "src/data/units/rhythmUnit7Redesigned.js"
      to: "rhythm_7_4 focusDurations fix"
      via: "one-concept violation remediation"
      pattern: "focusDurations.*\\[\\]"
---

<objective>
Migrate all 28 nodes in Units 5-8 from the old duration-allowlist format to tag-based pattern resolution with correct exercise types per the Phase 20 audit.

Purpose: PAT-03 requires all node configs to use patternTags. This plan handles the second half (Units 5-8) which includes the more complex durations (dotted notes, 3/4, 6/8, syncopation). Also fixes the one-concept violation on rhythm_7_4.
Output: 4 migrated unit files with all nodes using patternTags and correct exercise types
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-service-layer-trail-wiring/22-CONTEXT.md
@.planning/phases/22-service-layer-trail-wiring/22-RESEARCH.md
@.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md

<interfaces>
<!-- Same templates and conventions as Plan 03 -->

Tag-to-Unit mapping for Units 5-8 (from RESEARCH.md):
| Unit | Duration Vocabulary | Applicable patternTags |
| 5 | q,h,w,8,qr,hr,wr + hd | dotted-half |
| 5 | 3/4 nodes | three-four |
| 5 | + qd | dotted-quarter |
| 6 | + 16 | sixteenth |
| 7 | 6/8 | six-eight |
| 8 | syncopation | syncopation, dotted-syncopation |

EXERCISE_TYPES constants (from src/data/constants.js):

```javascript
MIXED_LESSON: "mixed_lesson",
PULSE: "pulse",
ARCADE_RHYTHM: "arcade_rhythm",
```

Question sequence templates (same as Plan 03):

- DISCOVERY: 8 questions, notation-weighted (4x rhythm_tap, 2x visual_recognition, 2x syllable_matching)
- PRACTICE: 8 questions, balanced (3x rhythm_tap, 3x visual_recognition, 2x syllable_matching)
- MIX_UP: 8 questions, variety-focused (2x rhythm_tap, 3x visual_recognition, 3x syllable_matching)
- SPEED_ROUND: single arcade_rhythm exercise
- MINI_BOSS: 12 questions, comprehensive (4x each type)
- BOSS: keep existing arcade_rhythm (no change needed)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate rhythm Units 5-6 (14 nodes) to patternTags + correct exercise types</name>
  <files>
    src/data/units/rhythmUnit5Redesigned.js
    src/data/units/rhythmUnit6Redesigned.js
  </files>
  <read_first>
    src/data/units/rhythmUnit5Redesigned.js (full file — 407 lines)
    src/data/units/rhythmUnit6Redesigned.js (full file — 395 lines)
    .planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md (Unit 5 and Unit 6 audit tables)
  </read_first>
  <action>
    Apply the same transformation pattern as Plan 03 Task 2 to all 14 nodes in Units 5-6.

    **For EVERY node:** Delete `patterns: [...]` from rhythmConfig, add `patternTags: [...]`, apply correct exercise type template, remove any exercise-level `rhythmPatterns` field.

    ---

    **UNIT 5 TAG ASSIGNMENTS:**

    - `rhythm_5_1` (discovery, introduces hd): `patternTags: ['dotted-half']`, durations: `['q', 'h', 'w', '8', 'qr', 'hr', 'wr', 'hd']`. Change from `rhythm` to DISCOVERY template.
    - `rhythm_5_2` (practice, reinforces hd): `patternTags: ['dotted-half']`. Change from `rhythm` to PRACTICE template.
    - `rhythm_5_3` (discovery, introduces 3/4): `patternTags: ['three-four']`, timeSignature should be `'3/4'`. Change from `rhythm_tap` to DISCOVERY template.
    - `rhythm_5_4` (discovery, introduces qd): `patternTags: ['dotted-quarter']`. Change from `rhythm_dictation` to DISCOVERY template.
    - `rhythm_5_5` (practice, reinforces dotted): `patternTags: ['dotted-half', 'dotted-quarter']`. Change from `rhythm_tap` to PRACTICE template.
    - `rhythm_5_6` (speed_round): `patternTags: ['dotted-half', 'dotted-quarter', 'three-four']`. Change from `rhythm` to SPEED_ROUND arcade_rhythm.
    - `boss_rhythm_5` (mini_boss): `patternTags: ['dotted-half', 'dotted-quarter', 'three-four']`. Currently 2x `arcade_rhythm` (4/4 + 3/4). Collapse to single MINI_BOSS template (12 questions covering both time signatures via patternTags).

    **UNIT 6 TAG ASSIGNMENTS:**

    - `rhythm_6_1` (discovery, introduces 16): `patternTags: ['sixteenth']`. Change from `rhythm` to DISCOVERY template.
    - `rhythm_6_2` (practice, reinforces 16): `patternTags: ['sixteenth']`. Change from `rhythm` to PRACTICE template.
    - `rhythm_6_3` (discovery, 16th + 8th contrast): `patternTags: ['sixteenth', 'quarter-eighth']`. Change from `rhythm_tap` to DISCOVERY template.
    - `rhythm_6_4` (practice, fast notes): `patternTags: ['sixteenth', 'quarter-eighth']`. Change from `rhythm_dictation` to PRACTICE template.
    - `rhythm_6_5` (mix_up, all rhythms): `patternTags: ['sixteenth', 'quarter-eighth', 'quarter-half-whole-eighth']`. Change from `rhythm_tap` to MIX_UP template.
    - `rhythm_6_6` (speed_round): `patternTags: ['sixteenth', 'quarter-eighth']`. Change from `rhythm` to SPEED_ROUND arcade_rhythm.
    - `boss_rhythm_6` (boss): **NO CHANGE NEEDED** per audit ("None needed"). Already uses `arcade_rhythm`. Only add `patternTags: ['sixteenth', 'quarter-eighth', 'quarter-half-whole-eighth']` and delete `patterns` field. Keep existing exercise structure.

    ---

    **QUESTION SEQUENCE TEMPLATES** (same as Plan 03):

    **DISCOVERY (8 questions):**
    ```javascript
    questions: [
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'rhythm_tap' },
      { type: 'visual_recognition' }, { type: 'syllable_matching' }, { type: 'rhythm_tap' },
    ]
    ```

    **PRACTICE (8 questions):**
    ```javascript
    questions: [
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' },
    ]
    ```

    **MIX_UP (8 questions):**
    ```javascript
    questions: [
      { type: 'visual_recognition' }, { type: 'rhythm_tap' }, { type: 'syllable_matching' },
      { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
    ]
    ```

    **SPEED_ROUND:**
    ```javascript
    exercises: [{ type: EXERCISE_TYPES.ARCADE_RHYTHM, config: { difficulty: 'intermediate' } }]
    ```

    **MINI_BOSS (12 questions):**
    ```javascript
    questions: [
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
      { type: 'rhythm_tap' }, { type: 'visual_recognition' }, { type: 'syllable_matching' },
    ]
    ```

  </action>
  <verify>
    <automated>npm run verify:trail 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No `patterns:` field exists in rhythmConfig of any node in Units 5-6
    - All 14 nodes contain `patternTags:` in rhythmConfig
    - No exercise in Units 5-6 has `config.rhythmPatterns` field
    - `boss_rhythm_5` uses EXERCISE_TYPES.MIXED_LESSON with 12 questions (changed from arcade_rhythm)
    - `boss_rhythm_6` retains EXERCISE_TYPES.ARCADE_RHYTHM (unchanged per audit)
    - All speed_round nodes use EXERCISE_TYPES.ARCADE_RHYTHM
    - `npm run verify:trail` passes
  </acceptance_criteria>
  <done>All 14 nodes in Units 5-6 use patternTags and correct exercise types. boss_rhythm_5 migrated from arcade_rhythm to mixed_lesson. boss_rhythm_6 kept as arcade_rhythm (no violation).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate rhythm Units 7-8 (14 nodes) + fix rhythm_7_4 one-concept violation</name>
  <files>
    src/data/units/rhythmUnit7Redesigned.js
    src/data/units/rhythmUnit8Redesigned.js
  </files>
  <read_first>
    src/data/units/rhythmUnit7Redesigned.js (full file — 399 lines)
    src/data/units/rhythmUnit8Redesigned.js (full file — 420 lines)
    .planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md (Unit 7 and Unit 8 audit tables + one-concept violation for rhythm_7_4)
  </read_first>
  <action>
    Apply the same transformation pattern to all 14 nodes in Units 7-8. Additionally fix the one-concept violation on rhythm_7_4.

    ---

    **UNIT 7 TAG ASSIGNMENTS:**

    - `rhythm_7_1` (discovery, introduces 6/8): `patternTags: ['six-eight']`, timeSignature: `'6/8'`. Change from `rhythm` to DISCOVERY template.
    - `rhythm_7_2` (practice, reinforce 6/8): `patternTags: ['six-eight']`. Change from `rhythm` to PRACTICE template.
    - `rhythm_7_3` (discovery, quarters in 6/8): `patternTags: ['six-eight']`. Change from `rhythm_tap` to DISCOVERY template.
    - `rhythm_7_4` (practice, mixing in 6/8): `patternTags: ['six-eight']`. Change from `rhythm_dictation` to PRACTICE template.
      **ONE-CONCEPT FIX:** Change `focusDurations: ['8']` to `focusDurations: []`. Move `'8'` into `contextDurations` if not already there. The eighth note is already known from Unit 3; its use in 6/8 is contextual reinforcement, not a new concept. The `contextDurations` array should be `['qd', 'q', '8']` (all three durations as context).
    - `rhythm_7_5` (mix_up): `patternTags: ['six-eight']`. Change from `rhythm_tap` to MIX_UP template.
    - `rhythm_7_6` (speed_round): `patternTags: ['six-eight']`. Change from `rhythm` to SPEED_ROUND arcade_rhythm.
    - `boss_rhythm_7` (mini_boss): `patternTags: ['six-eight']`. Change from `arcade_rhythm` to MINI_BOSS template (12 questions).

    **UNIT 8 TAG ASSIGNMENTS:**

    - `rhythm_8_1` (discovery, introduces syncopation): `patternTags: ['syncopation']`. Change from `rhythm` to DISCOVERY template.
    - `rhythm_8_2` (practice, reinforce syncopation): `patternTags: ['syncopation']`. Change from `rhythm` to PRACTICE template.
    - `rhythm_8_3` (discovery, introduces dotted syncopation): `patternTags: ['dotted-syncopation']`. Change from `rhythm_tap` to DISCOVERY template.
    - `rhythm_8_4` (practice, reinforce all syncopation): `patternTags: ['syncopation', 'dotted-syncopation']`. Change from `rhythm_dictation` to PRACTICE template.
    - `rhythm_8_5` (mix_up): `patternTags: ['syncopation', 'dotted-syncopation']`. Change from `rhythm_tap` to MIX_UP template.
    - `rhythm_8_6` (speed_round): `patternTags: ['syncopation', 'dotted-syncopation']`. Change from `rhythm` to SPEED_ROUND arcade_rhythm.
    - `boss_rhythm_8` (boss): **NO CHANGE TO EXERCISE STRUCTURE** per audit ("None needed" — 3x arcade_rhythm is intended design). Only add `patternTags: ['syncopation', 'dotted-syncopation', 'six-eight']` to rhythmConfig and delete `patterns` field. For each of the 3 exercises, remove any exercise-level `rhythmPatterns` field.

    ---

    **QUESTION SEQUENCE TEMPLATES** (same as previous tasks):

    Use the exact same DISCOVERY, PRACTICE, MIX_UP, SPEED_ROUND, and MINI_BOSS templates from Task 1 above.

    **boss_rhythm_8 special case:** Keep the existing 3-exercise structure (3x ARCADE_RHYTHM). Only modify rhythmConfig (delete patterns, add patternTags) and remove exercise-level rhythmPatterns fields.

  </action>
  <verify>
    <automated>npm run verify:trail 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No `patterns:` field exists in rhythmConfig of any node in Units 7-8
    - All 14 nodes contain `patternTags:` in rhythmConfig
    - No exercise in Units 7-8 has `config.rhythmPatterns` field
    - `rhythm_7_4` has `focusDurations: []` (not `focusDurations: ['8']`)
    - `rhythm_7_4` has `contextDurations` containing `'8'` (e.g., `['qd', 'q', '8']`)
    - `boss_rhythm_7` uses EXERCISE_TYPES.MIXED_LESSON with 12 questions (changed from arcade_rhythm)
    - `boss_rhythm_8` retains 3x EXERCISE_TYPES.ARCADE_RHYTHM (unchanged per audit)
    - All speed_round nodes use EXERCISE_TYPES.ARCADE_RHYTHM
    - `npm run verify:trail` passes
  </acceptance_criteria>
  <done>All 14 nodes in Units 7-8 use patternTags and correct exercise types. rhythm_7_4 one-concept violation fixed (focusDurations emptied). boss_rhythm_7 migrated to mixed_lesson. boss_rhythm_8 kept as 3x arcade_rhythm (intended design).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries applicable. Data file migration with no user input, authentication, or network access.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan                                                             |
| --------- | -------- | --------- | ----------- | --------------------------------------------------------------------------- |
| —         | —        | —         | accept      | No threats identified. Changes are static data configuration in unit files. |

</threat_model>

<verification>
1. `npm run verify:trail` passes with no errors
2. `grep -r "patterns:" src/data/units/rhythmUnit{5,6,7,8}Redesigned.js` returns 0 matches
3. `grep -c "patternTags:" src/data/units/rhythmUnit{5,6,7,8}Redesigned.js` returns 7 per file
4. `grep "focusDurations" src/data/units/rhythmUnit7Redesigned.js | grep "rhythm_7_4" -A2` shows empty array
</verification>

<success_criteria>

- All 28 nodes in Units 5-8 migrated with patternTags
- Old patterns fields completely removed
- Exercise types match Phase 20 audit for all 28 nodes
- rhythm_7_4 one-concept violation fixed
- boss_rhythm_6 and boss_rhythm_8 retain arcade_rhythm (no violations)
- Build validator passes cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/22-service-layer-trail-wiring/22-04-SUMMARY.md`
</output>
