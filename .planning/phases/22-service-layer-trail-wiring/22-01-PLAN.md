---
phase: 22-service-layer-trail-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/constants.js
  - src/components/games/rhythm-games/RhythmPatternGenerator.js
  - src/components/games/rhythm-games/RhythmPatternGenerator.test.js
  - src/data/units/rhythmUnit1Redesigned.test.js
autonomous: true
requirements: [PAT-04, PAT-05, CURR-05]

must_haves:
  truths:
    - "resolveByTags(['quarter-only']) returns only patterns whose tags include 'quarter-only'"
    - "resolveByTags(['quarter-only'], { difficulty: 'beginner' }) returns only beginner patterns"
    - "resolveByTags(['quarter-only'], { measureCount: 2 }) returns only 2-bar patterns"
    - "resolveByIds(['quarter_only_01']) returns exactly one pattern with that id"
    - "resolveByTags(['quarter-only']) returns zero patterns containing 'h', 'w', '8', '16' durations"
    - "EXERCISE_TYPES.RHYTHM_PULSE equals 'rhythm_pulse'"
  artifacts:
    - path: "src/data/constants.js"
      provides: "RHYTHM_PULSE exercise type constant"
      contains: "RHYTHM_PULSE"
    - path: "src/components/games/rhythm-games/RhythmPatternGenerator.js"
      provides: "resolveByTags and resolveByIds exports"
      exports: ["resolveByTags", "resolveByIds"]
    - path: "src/components/games/rhythm-games/RhythmPatternGenerator.test.js"
      provides: "Unit tests for resolver functions"
      min_lines: 40
    - path: "src/data/units/rhythmUnit1Redesigned.test.js"
      provides: "Test scaffold for pulse exercise and patternTags migration shape"
      min_lines: 20
  key_links:
    - from: "src/components/games/rhythm-games/RhythmPatternGenerator.js"
      to: "src/data/patterns/rhythmPatterns.js"
      via: "import RHYTHM_PATTERNS"
      pattern: "import.*RHYTHM_PATTERNS.*from.*rhythmPatterns"
---

<objective>
Add the RHYTHM_PULSE exercise type constant, implement resolveByTags() and resolveByIds() pattern resolver functions, and create test scaffolds for this phase.

Purpose: Establishes the foundation that all subsequent plans depend on -- the new exercise type constant for pulse exercises and the pattern resolution API that game components will use instead of legacy duration allowlists.

Output: Constants updated, resolver functions exported, tests passing for resolver correctness and learning-order enforcement (PAT-05).
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-service-layer-trail-wiring/22-CONTEXT.md
@.planning/phases/22-service-layer-trail-wiring/22-RESEARCH.md

<interfaces>
<!-- From src/data/patterns/rhythmPatterns.js (Phase 21 output) -->
export const PATTERN_TAGS = Object.freeze([
  'quarter-only', 'quarter-half', 'quarter-half-whole', 'quarter-eighth',
  'with-quarter-rest', 'with-half-rest', 'with-whole-rest',
  'dotted-half', 'dotted-quarter', 'with-sixteenth',
  'compound-basic', 'compound-mixed',
  'syncopation-basic', 'syncopation-dotted', 'three-four',
]);

export function getPatternsByTag(tag) { ... }
export function getPatternById(id) { ... }
export function getPatternsByTagAndDifficulty(tag, difficulty) { ... }

// Pattern object shape:
// { id, description, beats: string[][], durationSet: string[], tags: string[],
// timeSignature: string, difficulty: string, measureCount: number }

<!-- From src/data/constants.js -->

export const EXERCISE_TYPES = {
NOTE_RECOGNITION: 'note_recognition',
SIGHT_READING: 'sight_reading',
RHYTHM: 'rhythm',
MEMORY_GAME: 'memory_game',
BOSS_CHALLENGE: 'boss_challenge',
NOTE_CATCH: 'note_catch',
RHYTHM_TAP: 'rhythm_tap',
RHYTHM_DICTATION: 'rhythm_dictation',
ARCADE_RHYTHM: 'arcade_rhythm',
PITCH_COMPARISON: 'pitch_comparison',
INTERVAL_ID: 'interval_id',
};

<!-- From src/components/games/rhythm-games/RhythmPatternGenerator.js (first 6 lines) -->

export const DURATION_CONSTANTS = { ... };
export const TIME_SIGNATURES = { ... };
export const DIFFICULTY_LEVELS = { ... };
// Contains HybridPatternService class — do NOT refactor, keep existing code intact (D-09)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RHYTHM_PULSE constant and create resolver test scaffold</name>
  <files>src/data/constants.js, src/components/games/rhythm-games/RhythmPatternGenerator.test.js, src/data/units/rhythmUnit1Redesigned.test.js</files>
  <read_first>
    - src/data/constants.js
    - src/data/patterns/rhythmPatterns.js (first 50 lines for PATTERN_TAGS and helper exports)
    - src/data/units/rhythmUnit1Redesigned.js (first 80 lines for current node shape)
    - src/data/units/rhythmUnit7Redesigned.test.js (existing test patterns to follow)
  </read_first>
  <behavior>
    - Test: EXERCISE_TYPES.RHYTHM_PULSE === 'rhythm_pulse'
    - Test: resolveByTags(['quarter-only']) returns array with length > 0
    - Test: resolveByTags(['quarter-only']) returns only patterns where every tag includes 'quarter-only'
    - Test: resolveByTags(['quarter-only'], { difficulty: 'beginner' }) returns only patterns with difficulty === 'beginner'
    - Test: resolveByTags(['quarter-only'], { measureCount: 2 }) returns only patterns with measureCount === 2
    - Test: resolveByIds(['quarter_only_01']) returns array of length 1 with id 'quarter_only_01'
    - Test: resolveByIds(['nonexistent_id']) returns empty array
    - Test: resolveByTags(['quarter-only']) returns NO patterns whose durationSet contains 'h', 'w', '8', '16', 'qd', 'hd' (PAT-05 enforcement)
    - Test (unit1): rhythmUnit1Nodes[0] has exercises[0].type === EXERCISE_TYPES.RHYTHM_PULSE (will fail RED until Plan 02 migrates)
    - Test (unit1): rhythmUnit1Nodes[0] has exercises[0].config.pulseOnly === true (will fail RED until Plan 02)
    - Test (unit1): every rhythm node exercise has config.patternTags or config.pulseOnly (will fail RED until Plan 02)
    - Test (unit1): no rhythm node exercise has config.rhythmPatterns (will fail RED until Plan 02)
  </behavior>
  <action>
1. In `src/data/constants.js`, add `RHYTHM_PULSE: 'rhythm_pulse'` after the `ARCADE_RHYTHM` line in the EXERCISE_TYPES object. Keep the existing comment block structure. The exact line to add:
   ```
   RHYTHM_PULSE: 'rhythm_pulse',
   ```

2. Create `src/components/games/rhythm-games/RhythmPatternGenerator.test.js` with tests for the resolver functions (imports from `./RhythmPatternGenerator.js`). Tests should import `resolveByTags` and `resolveByIds`. All resolver tests will FAIL (RED) until Task 2 implements the functions. Structure:
   - `describe('resolveByTags')` — 6 tests covering: basic tag lookup, difficulty filter, measureCount filter, multi-tag union, empty result for nonsense tag, PAT-05 learning order enforcement (quarter-only returns no patterns with half/whole/eighth durations in durationSet)
   - `describe('resolveByIds')` — 3 tests covering: single ID lookup, multiple IDs, nonexistent ID

3. Create `src/data/units/rhythmUnit1Redesigned.test.js` modeled after `rhythmUnit7Redesigned.test.js`. Include:
   - Standard structure tests (count, IDs, order, prerequisites) matching Unit 7 test patterns
   - Pulse exercise test: `rhythmUnit1Nodes[0].exercises[0].type === EXERCISE_TYPES.RHYTHM_PULSE`
   - Pulse exercise config test: `exercises[0].config.pulseOnly === true` and `exercises[0].config.tempo === 65` and `exercises[0].config.beats === 8`
   - Migration shape test: every non-pulse exercise has `config.patternTags` array and does NOT have `config.rhythmPatterns`
   - These migration tests will fail RED until Plan 02 migrates Unit 1 — this is expected TDD behavior
     </action>
     <verify>
     <automated>cd C:\Development\PianoApp2 && node -e "import('./src/data/constants.js').then(c => { if (c.EXERCISE_TYPES.RHYTHM_PULSE === 'rhythm_pulse') { console.log('RHYTHM_PULSE: OK'); process.exit(0); } else { console.error('RHYTHM_PULSE: MISSING'); process.exit(1); } })" && grep -c "resolveByTags" src/components/games/rhythm-games/RhythmPatternGenerator.test.js && grep -c "RHYTHM_PULSE" src/data/units/rhythmUnit1Redesigned.test.js</automated>
     </verify>
     <acceptance_criteria>
   - src/data/constants.js contains the line `RHYTHM_PULSE: 'rhythm_pulse'`
   - src/components/games/rhythm-games/RhythmPatternGenerator.test.js exists with at least 9 test cases (6 resolveByTags + 3 resolveByIds)
   - src/data/units/rhythmUnit1Redesigned.test.js exists with at least 8 test cases
   - RhythmPatternGenerator.test.js imports `resolveByTags` and `resolveByIds` from `./RhythmPatternGenerator.js`
   - rhythmUnit1Redesigned.test.js imports from `./rhythmUnit1Redesigned.js` and checks `EXERCISE_TYPES.RHYTHM_PULSE`
     </acceptance_criteria>
     <done>RHYTHM_PULSE constant added to EXERCISE_TYPES. Test files created with RED tests for resolver functions and unit migration shape. Constants test passes.</done>
     </task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resolveByTags() and resolveByIds() in RhythmPatternGenerator.js</name>
  <files>src/components/games/rhythm-games/RhythmPatternGenerator.js</files>
  <read_first>
    - src/components/games/rhythm-games/RhythmPatternGenerator.js (full file — understand existing exports, HybridPatternService, import structure)
    - src/data/patterns/rhythmPatterns.js (first 50 lines for exports: RHYTHM_PATTERNS, PATTERN_TAGS, helper functions)
    - src/components/games/rhythm-games/RhythmPatternGenerator.test.js (just-created tests to satisfy)
  </read_first>
  <behavior>
    - resolveByTags(['quarter-only']) returns array of pattern objects matching the tag
    - resolveByTags with difficulty filter returns only matching difficulty
    - resolveByTags with measureCount filter returns only matching measureCount
    - resolveByIds(['quarter_only_01']) returns the exact pattern
    - All tests from Task 1's RhythmPatternGenerator.test.js pass GREEN
  </behavior>
  <action>
1. At the TOP of `src/components/games/rhythm-games/RhythmPatternGenerator.js`, add this import (per D-07, D-09 — keep all existing code intact):
   ```javascript
   import { RHYTHM_PATTERNS } from '../../../data/patterns/rhythmPatterns.js';
   ```

2. AFTER all existing code (below the HybridPatternService class and its export), add these two new named exports:

   ```javascript
   /**
    * Resolve curated patterns by tags, with optional difficulty and measureCount filters.
    * Returns full pattern objects (D-08) — game component picks randomly from pool (D-10).
    * @param {string[]} tags - One or more PATTERN_TAGS values
    * @param {Object} [options]
    * @param {string} [options.difficulty] - 'beginner'|'intermediate'|'advanced'
    * @param {number} [options.measureCount] - 1, 2, or 4
    * @returns {Object[]} Full pattern objects matching all filters
    */
   export function resolveByTags(tags, { difficulty, measureCount } = {}) {
     const tagSet = new Set(tags);
     let pool = RHYTHM_PATTERNS.filter((p) =>
       p.tags.some((t) => tagSet.has(t))
     );
     if (difficulty) {
       pool = pool.filter((p) => p.difficulty === difficulty);
     }
     if (measureCount != null) {
       pool = pool.filter((p) => p.measureCount === measureCount);
     }
     return pool;
   }

   /**
    * Resolve curated patterns by exact IDs.
    * @param {string[]} ids - Pattern IDs from rhythmPatterns.js
    * @returns {Object[]} Matching full pattern objects
    */
   export function resolveByIds(ids) {
     const idSet = new Set(ids);
     return RHYTHM_PATTERNS.filter((p) => idSet.has(p.id));
   }
   ```

3. Do NOT modify any existing code in the file — no changes to DURATION_CONSTANTS, TIME_SIGNATURES, DIFFICULTY_LEVELS, or HybridPatternService. The existing default export and all named exports remain untouched (D-09).

4. Run the test file to confirm all resolver tests pass GREEN:
   `npx vitest run src/components/games/rhythm-games/RhythmPatternGenerator.test.js`
   </action>
   <verify>
   <automated>cd C:\Development\PianoApp2 && npx vitest run src/components/games/rhythm-games/RhythmPatternGenerator.test.js --reporter=verbose</automated>
   </verify>
   <acceptance_criteria> - RhythmPatternGenerator.js contains `import { RHYTHM_PATTERNS } from '../../../data/patterns/rhythmPatterns.js'` - RhythmPatternGenerator.js contains `export function resolveByTags(` - RhythmPatternGenerator.js contains `export function resolveByIds(` - All existing exports (DURATION_CONSTANTS, TIME_SIGNATURES, DIFFICULTY_LEVELS, default export) are unchanged - `npx vitest run src/components/games/rhythm-games/RhythmPatternGenerator.test.js` exits with code 0 - PAT-05 test passes: resolveByTags(['quarter-only']) returns no patterns with non-quarter durations in durationSet
   </acceptance_criteria>
   <done>resolveByTags() and resolveByIds() are exported from RhythmPatternGenerator.js. All resolver tests pass. Existing generator code is untouched. PAT-05 learning-order enforcement verified by test.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries affected. This plan modifies client-side data constants and adds pure functions with no I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan                                                                                                           |
| --------- | -------- | --------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| —         | —        | —         | accept      | Security NOT APPLICABLE for this phase — all changes are client-side data files and game logic per research determination |

</threat_model>

<verification>
1. `npx vitest run src/components/games/rhythm-games/RhythmPatternGenerator.test.js` passes (all resolver tests GREEN)
2. `node -e "import('./src/data/constants.js').then(c => console.log(c.EXERCISE_TYPES.RHYTHM_PULSE))"` prints `rhythm_pulse`
3. `npm run verify:trail` still passes (RHYTHM_PULSE added to EXERCISE_TYPES does not break existing validation)
</verification>

<success_criteria>

- EXERCISE_TYPES.RHYTHM_PULSE = 'rhythm_pulse' exists in constants.js
- resolveByTags() and resolveByIds() are exported from RhythmPatternGenerator.js
- All resolver tests pass including PAT-05 learning-order enforcement
- Test scaffolds exist for Unit 1 migration (will fail RED until Plan 02)
- Existing generator logic and exports unchanged
- npm run verify:trail passes
  </success_criteria>

<output>
After completion, create `.planning/phases/22-service-layer-trail-wiring/22-01-SUMMARY.md`
</output>
