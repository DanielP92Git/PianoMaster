---
phase: 22-service-layer-trail-wiring
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/games/rhythm-games/MetronomeTrainer.jsx
  - src/components/trail/TrailNodeModal.jsx
autonomous: true
requirements: [CURR-05]

must_haves:
  truths:
    - "MetronomeTrainer renders a pulsing circle instead of VexFlow notation when pulseOnly is true"
    - "MetronomeTrainer plays piano C4 on each beat instead of metronome click when pulseOnly is true"
    - "MetronomeTrainer limits session to config.beats count (8) when pulseOnly is true"
    - "Count-in still plays before tapping starts in pulse mode"
    - "Timing scoring (PERFECT/GOOD/MISS) works identically in pulse mode"
    - "TrailNodeModal routes rhythm_pulse exercise type to metronome-trainer"
    - "MetronomeTrainer handleNextExercise routes rhythm_pulse to metronome-trainer"
    - "Pulsing circle animation respects reduced-motion preference"
  artifacts:
    - path: "src/components/games/rhythm-games/MetronomeTrainer.jsx"
      provides: "pulseOnly mode with pulsing circle visual and C4 piano sound"
      contains: "pulseOnly"
    - path: "src/components/trail/TrailNodeModal.jsx"
      provides: "rhythm_pulse routing entry"
      contains: "rhythm_pulse"
  key_links:
    - from: "src/components/trail/TrailNodeModal.jsx"
      to: "/rhythm-mode/metronome-trainer"
      via: "case 'rhythm_pulse' navigate"
      pattern: "rhythm_pulse.*metronome-trainer"
    - from: "src/components/games/rhythm-games/MetronomeTrainer.jsx"
      to: "useAudioEngine playPianoSound"
      via: "audioEngine.playPianoSound"
      pattern: "playPianoSound.*C4"
---

<objective>
Implement the pulseOnly mode in MetronomeTrainer (pulsing circle visual, piano C4 sound, limited beats, no notation) and add rhythm_pulse routing to both TrailNodeModal and MetronomeTrainer's handleNextExercise switch.

Purpose: Delivers the CURR-05 pulse exercise experience -- the very first thing a child encounters on the rhythm trail. A large pulsing circle and piano beats create a welcoming, low-pressure introduction to rhythm before any notation appears.

Output: MetronomeTrainer supports pulseOnly mode, routing tables handle rhythm_pulse exercise type.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-service-layer-trail-wiring/22-CONTEXT.md
@.planning/phases/22-service-layer-trail-wiring/22-RESEARCH.md

<interfaces>
<!-- MetronomeTrainer key state (from RESEARCH.md verified code) -->
// Lines 86-89: Trail config consumption
const nodeId = location.state?.nodeId || null;
const nodeConfig = location.state?.nodeConfig || null;
const rhythmPatterns = nodeConfig?.rhythmPatterns ?? null; // legacy — becomes dead

// Line 222: handleNextExercise switch routes by exercise type
// Current cases: note_recognition, sight_reading, memory_game, rhythm, boss_challenge,
// rhythm_reading, rhythm_dictation, pitch_comparison, interval_id, arcade_rhythm

<!-- useAudioEngine playPianoSound signature (verified line 483) -->

const playPianoSound = useCallback((volume = 0.6, pitch = 0) => { ... }, [...]);
// pitch can be note name string 'C4' or semitone offset from G4

<!-- TapArea component API (verified) -->

export function TapArea({ onTap, feedback, isActive, title, className }) { ... }
// feedback shape: { accuracy: 'PERFECT'|'GOOD'|'FAIR'|'MISS', points: number }

<!-- AccessibilityContext (for reduced-motion) -->

const { reducedMotion } = useContext(AccessibilityContext);
// Already consumed in MetronomeTrainer — check existing usage

<!-- TrailNodeModal routing table (lines 220-248) -->

case 'rhythm': navigate('/rhythm-mode/metronome-trainer', { state: navState });
case 'rhythm_tap': navigate('/rhythm-mode/rhythm-reading-game', { state: navState });
case 'rhythm_dictation': navigate('/rhythm-mode/rhythm-dictation-game', { state: navState });
case 'arcade_rhythm': navigate('/rhythm-mode/arcade-rhythm-game', { state: navState });
// 'rhythm_pulse' NOT YET present — must be added
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MetronomeTrainer pulseOnly mode</name>
  <files>src/components/games/rhythm-games/MetronomeTrainer.jsx</files>
  <read_first>
    - src/components/games/rhythm-games/MetronomeTrainer.jsx (full file — understand game phases, beat scheduling, count-in logic, existing conditional rendering, useAudioEngine usage)
    - src/hooks/useAudioEngine.js (lines 480-510 for playPianoSound signature)
    - src/components/games/rhythm-games/components/TapArea.jsx (props interface)
    - src/contexts/AccessibilityContext.jsx (reducedMotion usage)
  </read_first>
  <action>
Modify MetronomeTrainer.jsx to support a `pulseOnly` mode. The existing game logic (count-in, beat scheduling, tap detection, timing scoring, star calculation, VictoryScreen handoff) remains intact. The changes are:

1. **Read pulseOnly flag** — Near the existing `nodeConfig` destructuring (around line 86-89), add:

   ```javascript
   const pulseOnly = nodeConfig?.pulseOnly ?? false;
   const pulseBeatCount = nodeConfig?.beats ?? 8;
   const pulsePitch = nodeConfig?.pitch ?? "C4";
   ```

2. **Replace metronome sound with piano C4 in pulse mode** — Find where the metronome click/tick sound is triggered on each beat (look for `createCustomMetronomeSound` or similar audio call in the beat scheduling callback). When `pulseOnly` is true, instead of playing the metronome click, call:

   ```javascript
   audioEngine.playPianoSound(0.6, pulsePitch);
   ```

   Keep the metronome sound for non-pulse mode unchanged.

3. **Limit session length in pulse mode** — In the beat scheduling logic, when `pulseOnly` is true, the total number of beats should be `pulseBeatCount` (default 8, which is 2 measures of 4/4). After the last beat + final tap window, transition to the completion/results phase. Do NOT play a pattern first then expect tapping — in pulse mode, the piano note IS the pattern and the child taps along simultaneously. Each beat: play C4, child taps, timing is scored.

4. **Skip notation rendering in pulse mode** — Find the VexFlow / notation display section (likely a conditional JSX block that renders the music staff). When `pulseOnly` is true, do NOT render notation. Instead, render the pulse circle visual:

   ```jsx
   {
     pulseOnly && (
       <div className="flex flex-col items-center justify-center gap-6">
         <div
           className={`flex h-32 w-32 items-center justify-center rounded-full bg-gradient-to-br from-indigo-400 to-purple-500 shadow-lg shadow-purple-500/30 transition-transform ${
             reducedMotion ? "" : "duration-200"
           } ${isOnBeat ? (reducedMotion ? "scale-105" : "scale-125 shadow-purple-400/50") : "scale-100"}`}
         >
           <span className="text-4xl font-bold text-white">
             {currentBeatNumber}
           </span>
         </div>
       </div>
     );
   }
   ```

   Where `isOnBeat` is a state boolean that flips true for ~200ms on each beat (use a brief timeout), and `currentBeatNumber` is the beat number within the current measure (1-4 for 4/4). Use `useState` for `isOnBeat`, set it true when the beat fires, then `setTimeout` to set it false after 200ms.

   For `reducedMotion`: if true, use a minimal scale change (1.0 to 1.05) with no glow. If false, use full pulsing (1.0 to 1.25) with shadow glow.

5. **Count-in behavior** — The existing count-in logic should work as-is for pulse mode (D-05). The count-in plays beat numbers (1-2-3-4) visually and audibly before tapping starts. In pulse mode, also play the C4 piano sound during count-in instead of the metronome click, so the child hears the sound they will be tapping to.

6. **No pattern playback phase** — In normal MetronomeTrainer mode, there may be a "listen" phase where the pattern plays first, then a "tap" phase where the child reproduces it. In pulse mode, SKIP any listen/preview phase. Go straight from count-in to tapping. The metronome (piano C4) keeps playing on every beat while the child taps along.

7. **Score and star calculation** — No changes needed. The existing scoring (PERFECT/GOOD/MISS per tap) and star calculation at session end work identically for pulse mode. VictoryScreen receives the same data structure.

8. **Update handleNextExercise routing switch** — In the switch statement around line 222, add a case for `rhythm_pulse`:
   ```javascript
   case 'rhythm_pulse':
     navigate('/rhythm-mode/metronome-trainer', {
       state: navState,
       replace: true,
     });
     window.location.reload();
     break;
   ```
   Place this case near the existing `'rhythm'` case since they route to the same component.
   </action>
   <verify>
   <automated>cd C:\Development\PianoApp2 && npx vitest run src/components/games/rhythm-games/ --reporter=verbose 2>&1 | tail -20</automated>
   </verify>
   <acceptance_criteria> - MetronomeTrainer.jsx contains `const pulseOnly = nodeConfig?.pulseOnly ?? false` - MetronomeTrainer.jsx contains `playPianoSound` call with `pulsePitch` or `'C4'` argument - MetronomeTrainer.jsx contains `pulseOnly &&` in JSX (conditional pulsing circle render) - MetronomeTrainer.jsx contains `reducedMotion` check within the pulse circle CSS classes - MetronomeTrainer.jsx contains `case 'rhythm_pulse':` in handleNextExercise switch - MetronomeTrainer.jsx does NOT render VexFlow notation when pulseOnly is true - No existing test failures introduced (npx vitest run exits with code 0 for rhythm-games directory)
   </acceptance_criteria>
   <done>MetronomeTrainer supports pulseOnly mode: pulsing circle visual (respects reduced-motion), piano C4 sound on each beat, 8-beat session, count-in preserved, same scoring, handleNextExercise routes rhythm_pulse.</done>
   </task>

<task type="auto">
  <name>Task 2: Add rhythm_pulse routing to TrailNodeModal</name>
  <files>src/components/trail/TrailNodeModal.jsx</files>
  <read_first>
    - src/components/trail/TrailNodeModal.jsx (lines 200-260 for the exercise type routing switch and navigateToExercise function)
  </read_first>
  <action>
In `src/components/trail/TrailNodeModal.jsx`, find the `navigateToExercise` function's switch statement (around line 220). Add a new case for `rhythm_pulse` that routes to the MetronomeTrainer:

```javascript
case 'rhythm_pulse':
  navigate('/rhythm-mode/metronome-trainer', { state: navState });
  break;
```

Place this case right after the existing `case 'rhythm':` block (line ~222) since both route to the same component. The `navState` object already includes `nodeConfig` which carries the `pulseOnly: true` flag — no extra state needed.

Also verify that the existing `case 'rhythm':` case is still present and routes to `/rhythm-mode/metronome-trainer`. Do NOT remove the `'rhythm'` case — it is still used by legacy non-trail games (D-09).
</action>
<verify>
<automated>cd C:\Development\PianoApp2 && grep -n "rhythm_pulse" src/components/trail/TrailNodeModal.jsx</automated>
</verify>
<acceptance_criteria> - TrailNodeModal.jsx contains `case 'rhythm_pulse':` in the navigateToExercise switch - TrailNodeModal.jsx contains `navigate('/rhythm-mode/metronome-trainer'` in the rhythm_pulse case - The existing `case 'rhythm':` case is still present and unchanged - The existing `case 'rhythm_tap':` case is still present and routes to rhythm-reading-game
</acceptance_criteria>
<done>TrailNodeModal routes rhythm_pulse exercise type to MetronomeTrainer. All exercise types used after the unit file migration have corresponding routing entries.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries affected. This plan modifies client-side UI components with no auth, API, or data access changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan                                       |
| --------- | -------- | --------- | ----------- | ----------------------------------------------------- |
| —         | —        | —         | accept      | Security NOT APPLICABLE — client-side UI changes only |

</threat_model>

<verification>
1. MetronomeTrainer.jsx contains `pulseOnly` conditional logic (grep confirms)
2. TrailNodeModal.jsx contains `case 'rhythm_pulse':` routing (grep confirms)
3. MetronomeTrainer.jsx handleNextExercise contains `case 'rhythm_pulse':` (grep confirms)
4. `npx vitest run src/components/games/rhythm-games/` passes (no regressions)
5. `npm run build` passes (no TypeScript/compilation errors from new code)
</verification>

<success_criteria>

- Child tapping pulse exercise on Node 1 sees pulsing circle, hears piano C4, no notation displayed
- Reduced-motion users see minimal animation (scale 1.05, no glow)
- Count-in works before tapping starts
- Session ends after 8 beats with standard scoring and VictoryScreen
- rhythm_pulse routes correctly in both TrailNodeModal and MetronomeTrainer handleNextExercise
- No regressions in existing MetronomeTrainer behavior for non-pulse modes
  </success_criteria>

<output>
After completion, create `.planning/phases/22-service-layer-trail-wiring/22-03-SUMMARY.md`
</output>
