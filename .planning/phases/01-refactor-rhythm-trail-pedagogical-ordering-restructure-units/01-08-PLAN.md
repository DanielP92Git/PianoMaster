---
phase: 01-refactor-rhythm-trail-pedagogical-ordering-restructure-units
plan: 08
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/data/units/rhythmUnit9.js
  - src/data/units/rhythmUnit9.test.js
  - src/data/units/rhythmUnit10.js
  - src/data/units/rhythmUnit10.test.js
  - src/data/expandedNodes.js
  - src/data/skillTrail.js
  - src/data/units/rhythmUnits.difficulty.test.js
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-05]
tags:
  [
    rhythm-trail,
    unit-data,
    six-eight,
    review-boss,
    wire-up,
    aggregator,
    units-map,
  ]
must_haves:
  truths:
    - "U9 (6/8 Meter) and U10 (Rhythm Review BOSS only) data files exist"
    - "expandedNodes.js imports the 10 new unit files (rhythmUnit1.js..rhythmUnit10.js) and removes the OLD rhythmUnit{1..7}Redesigned.js imports"
    - "expandedNodes.js HIDDEN-V1 marker block is preserved verbatim from Plan 02 (rhythm_synco_* references), with binding-renamed warning per duplicate-identifier risk"
    - "skillTrail.js UNITS map has RHYTHM_1..RHYTHM_10 entries plus RHYTHM_SYNCO entry"
    - "rhythmUnits.difficulty.test.js imports updated to reference new unit files"
    - "npm run verify:trail PASSES — all 4 new validator rules go green; no broken prereqs; no cycles; no duplicate IDs; XP economy intact"
  artifacts:
    - path: "src/data/units/rhythmUnit9.js"
      provides: "U9 = 6/8 Meter (timeSignature='6/8')"
      contains: "rhythm_9_1"
    - path: "src/data/units/rhythmUnit10.js"
      provides: "U10 = Rhythm Review (single cumulative BOSS)"
      contains: "boss_rhythm_10"
    - path: "src/data/expandedNodes.js"
      provides: "Aggregator pointing at new U1-U10 + preserved HIDDEN-V1 marker"
      contains: "rhythmUnit1Nodes"
    - path: "src/data/skillTrail.js"
      provides: "UNITS map with RHYTHM_1..10 + RHYTHM_SYNCO entries"
      contains: "RHYTHM_10"
  key_links:
    - from: "src/data/expandedNodes.js"
      to: "src/data/units/rhythmUnit{1..10}.js"
      via: "default-import + spread into EXPANDED_RHYTHM_NODES"
      pattern: "import rhythmUnit"
    - from: "src/data/skillTrail.js UNITS map"
      to: "src/locales/en/trail.json units.names"
      via: "name string matches i18n key (per Plan 03)"
      pattern: "name:"
---

<objective>
Complete Wave 2 by (1) authoring U9 (6/8 Meter) and U10 (Rhythm Review BOSS-only), (2) wiring the 10 new unit files into `expandedNodes.js`, (3) updating `skillTrail.js` UNITS map with RHYTHM_1..10 + RHYTHM_SYNCO entries, and (4) updating `rhythmUnits.difficulty.test.js` imports. After this plan ships, `npm run verify:trail` goes green for the FIRST TIME since Wave 0 — the data restructure is complete at the data layer.

Purpose: REQ-01 (U10 cumulative review boss as terminus), REQ-03 (U9 meter concept), REQ-04 (intro screen for 6/8), REQ-05 (lockstep wire-up). Implements D-11 (single review boss), D-01 (UNITS map RHYTHM_8/9/10), 01-RESEARCH.md Open Question #3 (RHYTHM_SYNCO entry).
Output: 7 files modified/created. CLAUDE.md update is in Plan 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-CONTEXT.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-RESEARCH.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-PATTERNS.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-02-PLAN.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-05-PLAN.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-07-PLAN.md

<interfaces>
Aggregator pattern from 01-PATTERNS.md §3b — preserve top-level structure of expandedNodes.js (treble + bass + ear imports/spreads stay untouched; only rhythm imports + spreads change).

UNITS map shape (skillTrail.js lines 315-340 per 01-PATTERNS.md §2):

```javascript
RHYTHM_N: {
  id: "rhythm_unit_N",
  category: NODE_CATEGORIES.RHYTHM,
  name: "<Display Name from D-01 — matches trail.json key from Plan 03>",
  description: "<short>",
  order: N,
  theme: "<thematic tag>",
  reward: { type: "accessory", id: "<badge_id>", name: "<Badge Name>" },
}
```

</interfaces>
</context>

<threat_model>
This plan introduces no security-relevant changes (data wire-up only — no auth, no RLS, no SQL). N/A.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rhythmUnit9.js (6/8 Meter) + test</name>
  <files>src/data/units/rhythmUnit9.js, src/data/units/rhythmUnit9.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit7Redesigned.js (OLD U7 was 6/8 — reuse its content + patternTags)
    - src/data/units/rhythmUnit8.js (just-completed analog from Plan 07 Task 3 — 3/4 meter unit)
    - 01-CONTEXT.md §D-01 (U9 = 6/8 Meter)
  </read_first>
  <behavior>
    - File `src/data/units/rhythmUnit9.js`, 6 nodes, UNIT_NAME = "Six-Eight Time", START_ORDER = 180
    - ALL nodes timeSignature='6/8'
    - rhythm_9_1: DISCOVERY focusDurations=['6_8'], durations=['qd','q','8'], prerequisites=['boss_rhythm_8']
    - First DISCOVERY's first question: `{ type: 'discovery_intro', focusDuration: '6_8' }`
  </behavior>
  <action>
    Mirror `src/data/units/rhythmUnit8.js` (Plan 07 Task 3) exactly with substitutions:
    - `UNIT_ID = 9`, `UNIT_NAME = "Six-Eight Time"`, `START_ORDER = 180`
    - First node prereq `['boss_rhythm_8']`
    - timeSignature='6/8' on every node
    - focusDurations on rhythm_9_1 = `['6_8']`
    - durations typically `['qd','q','8']` (dotted-quarter is the "big beat" in 6/8)
    - XP arc: 80, 85, 85, 90, 95, 170

    Pattern tags: reuse from OLD `rhythmUnit7Redesigned.js`. Likely `"six-eight-basic"`, `"six-eight-qd-eighths"`. Verify in RhythmPatternGenerator.js — validator catches missing tags.

    rhythm_9_1's first question: `{ type: 'discovery_intro', focusDuration: '6_8' }` — matches the `cards["6_8"]` key authored in Plan 03 common.json.

    Test asserts:
    - length=6, IDs rhythm_9_1..5 + boss_rhythm_9
    - All timeSignature='6/8'
    - focusDurations on rhythm_9_1 = `['6_8']`
    - prereq chain from boss_rhythm_8
    - boss has patternTagMode='any', isBoss=true

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit9.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Files exist
    - `grep -c "timeSignature: \"6/8\"" src/data/units/rhythmUnit9.js` returns 6
    - `grep -c "focusDurations: \[\"6_8\"\]" src/data/units/rhythmUnit9.js` returns at least 1
    - `grep -c "prerequisites: \[\"boss_rhythm_8\"\]" src/data/units/rhythmUnit9.js` returns 1
    - `npx vitest run src/data/units/rhythmUnit9.test.js` reports all tests passing
  </acceptance_criteria>
  <done>
    U9 (6/8 Meter) data + test exist and pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write rhythmUnit10.js (Rhythm Review — single BOSS) + test</name>
  <files>src/data/units/rhythmUnit10.js, src/data/units/rhythmUnit10.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit6Redesigned.js lines 461-520 (OLD boss_rhythm_6 was cumulative all-rhythm BOSS — extract the EXACT arcade_rhythm config shape and nodeType: BOSS pattern)
    - scripts/validateTrail.mjs `validateGameTypePolicy` (lines 571-602) — confirms BOSS → arcade_rhythm (NOT mixed_lesson)
    - 01-CONTEXT.md §D-11 (single cumulative BOSS; no intro/practice/speed)
    - 01-PATTERNS.md §1 mini-boss/boss section + footnote about U10 borrowing from rhythmUnit8Redesigned.js boss section
  </read_first>
  <behavior>
    - File `src/data/units/rhythmUnit10.js`, ONE node only: boss_rhythm_10
    - boss_rhythm_10: nodeType=NODE_TYPES.BOSS (final boss, not MINI_BOSS), category='boss', isBoss=true, prerequisites=['boss_rhythm_9']
    - patternTagMode='any', patternTags = verbatim union of patternTags used across U1-U9
    - measureCount=4
    - exercise type=EXERCISE_TYPES.ARCADE_RHYTHM (BOSS policy per validateGameTypePolicy)
    - Test asserts: length=1, ID boss_rhythm_10, BOSS nodeType, prereq=boss_rhythm_9, patternTagMode='any', reviewsUnits=[1..9]
  </behavior>
  <action>
    Use the Write tool. File:

    ```javascript
    // src/data/units/rhythmUnit10.js
    // Phase 1 v3.5 — Unit 10: Rhythm Review (D-11).
    // Single cumulative BOSS node — terminus of rhythm trail.
    // Pulls patterns from all U1–U9 via patternTagMode: "any".
    // No intro/practice/speed nodes — boss-only per D-11.

    import { NODE_TYPES, RHYTHM_COMPLEXITY, NEW_CONTENT_TYPES } from "../nodeTypes.js";
    import { EXERCISE_TYPES } from "../constants.js";

    const UNIT_ID = 10;
    const UNIT_NAME = "Rhythm Review";
    const CATEGORY = "boss";
    const START_ORDER = 200;

    const rhythmUnit10Nodes = [
      {
        id: "boss_rhythm_10",
        name: "Rhythm Master",
        description: "Master every rhythm concept from quarters to 6/8!",
        unlockHint: "Complete all rhythm units to unlock this final challenge!",
        category: "boss",
        unit: UNIT_ID,
        unitName: UNIT_NAME,
        order: START_ORDER,
        orderInUnit: 1,
        prerequisites: ["boss_rhythm_9"],
        nodeType: NODE_TYPES.BOSS,
        rhythmConfig: {
          complexity: RHYTHM_COMPLEXITY.VARIED,
          durations: ["q","h","w","8","16","qr","hr","wr","hd","qd"],
          focusDurations: [],
          contextDurations: [],
          // patternTags: filled below — exact union from U1-U9 source files
          patternTags: [/* see action body */],
          patternTagMode: "any",
          tempo: { min: 80, max: 100, default: 90 },
          pitch: "C4",
          timeSignature: "4/4",
          measureCount: 4,
        },
        newContent: NEW_CONTENT_TYPES.RHYTHM,
        newContentDescription: "Master review across all 9 rhythm concepts.",
        exercises: [
          {
            type: EXERCISE_TYPES.ARCADE_RHYTHM,   // BOSS policy per validateGameTypePolicy
            config: {
              // Mirror the arcade_rhythm config shape from OLD boss_rhythm_6 (rhythmUnit6Redesigned.js lines 461-520)
              // Typical fields: questionCount, tempo, measureCount inherits from rhythmConfig
              questionCount: 12,
            },
          },
        ],
        skills: ["rhythm_mastery"],
        xpReward: 250,
        accessoryUnlock: "rhythm_master_badge",
        isBoss: true,
        isReview: true,
        reviewsUnits: [1, 2, 3, 4, 5, 6, 7, 8, 9],
      },
    ];

    export default rhythmUnit10Nodes;
    ```

    **Filling `patternTags` verbatim**: after Plans 05/06/07 commit, run:
    ```bash
    grep -h "patternTags:" src/data/units/rhythmUnit{1..9}.js | grep -oE '"[a-z_-]+(?:-?[a-z_-]+)*"' | sort -u
    ```
    Copy the deduplicated list into the `patternTags: []` array.

    **Arcade config shape**: Read `src/data/units/rhythmUnit6Redesigned.js` lines 461-520 verbatim. The OLD `boss_rhythm_6` had `nodeType: BOSS` + `exercises[0].type: ARCADE_RHYTHM`. Mirror its `config` object byte-for-byte (adjusting only the values that need to change — patternTags above, measureCount stays 4).

    Test file `src/data/units/rhythmUnit10.test.js`:
    ```javascript
    import { describe, it, expect } from "vitest";
    import rhythmUnit10Nodes from "./rhythmUnit10.js";
    import { NODE_TYPES } from "../nodeTypes.js";
    import { EXERCISE_TYPES } from "../constants.js";

    describe("Rhythm Unit 10 — Rhythm Review (Phase 1 v3.5 — D-11)", () => {
      it("contains exactly one boss node", () => {
        expect(rhythmUnit10Nodes).toHaveLength(1);
      });
      it("the single node is boss_rhythm_10 with BOSS nodeType", () => {
        const node = rhythmUnit10Nodes[0];
        expect(node.id).toBe("boss_rhythm_10");
        expect(node.nodeType).toBe(NODE_TYPES.BOSS);
        expect(node.isBoss).toBe(true);
        expect(node.isReview).toBe(true);
      });
      it("prereq is boss_rhythm_9 (chains from U9)", () => {
        expect(rhythmUnit10Nodes[0].prerequisites).toEqual(["boss_rhythm_9"]);
      });
      it("uses patternTagMode: 'any' for cumulative review", () => {
        expect(rhythmUnit10Nodes[0].rhythmConfig.patternTagMode).toBe("any");
      });
      it("reviewsUnits enumerates 1..9", () => {
        expect(rhythmUnit10Nodes[0].reviewsUnits).toEqual([1,2,3,4,5,6,7,8,9]);
      });
      it("measureCount is 4 (boss policy)", () => {
        expect(rhythmUnit10Nodes[0].rhythmConfig.measureCount).toBe(4);
      });
      it("exercise type is ARCADE_RHYTHM (BOSS policy)", () => {
        expect(rhythmUnit10Nodes[0].exercises[0].type).toBe(EXERCISE_TYPES.ARCADE_RHYTHM);
      });
    });
    ```

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit10.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Files exist
    - `grep -c "boss_rhythm_10" src/data/units/rhythmUnit10.js` returns at least 1
    - `grep -c "nodeType: NODE_TYPES.BOSS" src/data/units/rhythmUnit10.js` returns 1
    - `grep -c "patternTagMode: \"any\"" src/data/units/rhythmUnit10.js` returns 1
    - `grep -c "isReview: true" src/data/units/rhythmUnit10.js` returns 1
    - `grep -c "prerequisites: \[\"boss_rhythm_9\"\]" src/data/units/rhythmUnit10.js` returns 1
    - `grep -c "EXERCISE_TYPES.ARCADE_RHYTHM" src/data/units/rhythmUnit10.js` returns 1
    - `npx vitest run src/data/units/rhythmUnit10.test.js` reports all 7 tests passing
  </acceptance_criteria>
  <done>
    U10 data + test exist. Final review BOSS chains from U9. Cumulative pattern resolution via `patternTagMode: 'any'`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire 10 new unit files into expandedNodes.js (preserve HIDDEN-V1 marker, resolve binding collision)</name>
  <files>src/data/expandedNodes.js</files>
  <read_first>
    - src/data/expandedNodes.js (full read; locate all rhythm imports + HIDDEN-V1 markers updated by Plan 02)
    - 01-PATTERNS.md §3b "Update expandedNodes.js HIDDEN-V1 marker"
    - All 10 new unit files src/data/units/rhythmUnit{1..10}.js (verify default exports)
  </read_first>
  <behavior>
    - 10 import lines for rhythmUnit1.js..rhythmUnit10.js
    - OLD rhythmUnit{1..7}Redesigned.js imports + spreads REMOVED (not commented; clean cut to avoid duplicate-identifier ESM errors)
    - HIDDEN-V1 marker block preserved; the commented-out `rhythmUnit8Nodes` import binding RENAMED to `rhythmUnit8SyncoNodes` to avoid collision with NEW U8's `rhythmUnit8Nodes` binding
    - EXPANDED_NODES spread includes new rhythm imports
    - EXPANDED_RHYTHM_NODES spread includes ONLY the 10 new rhythm imports
    - OLD rhythmUnit{1..7}Redesigned.js source files NOT deleted in this plan (Plan 10 cleanup task does that)
  </behavior>
  <action>
    Use Edit tool against `src/data/expandedNodes.js`. Two coordinated changes:

    **Change A: Replace imports.** Remove ALL OLD `import rhythmUnit{1..7}Nodes from './units/rhythmUnit{1..7}Redesigned.js';` lines. Add 10 new imports:
    ```javascript
    import rhythmUnit1Nodes from './units/rhythmUnit1.js';
    import rhythmUnit2Nodes from './units/rhythmUnit2.js';
    import rhythmUnit3Nodes from './units/rhythmUnit3.js';
    import rhythmUnit4Nodes from './units/rhythmUnit4.js';
    import rhythmUnit5Nodes from './units/rhythmUnit5.js';
    import rhythmUnit6Nodes from './units/rhythmUnit6.js';
    import rhythmUnit7Nodes from './units/rhythmUnit7.js';
    import rhythmUnit8Nodes from './units/rhythmUnit8.js';
    import rhythmUnit9Nodes from './units/rhythmUnit9.js';
    import rhythmUnit10Nodes from './units/rhythmUnit10.js';
    ```

    **Change B: Update HIDDEN-V1 marker.** Plan 02 left a commented-out `import rhythmUnit8Nodes from './units/rhythmUnit8Redesigned.js';`. Since the NEW U8 now occupies the `rhythmUnit8Nodes` binding, the HIDDEN-V1 commented import must be re-bound to `rhythmUnit8SyncoNodes` to make the re-enable path safe:
    ```javascript
    // HIDDEN-V1: Syncopation unit (renamed rhythm_synco_*/boss_rhythm_synco per Phase 1 v3.5 D-10).
    // Renamed binding from rhythmUnit8Nodes → rhythmUnit8SyncoNodes to avoid collision with new U8 (3/4 Meter).
    // To re-enable: (1) uncomment import below; (2) uncomment spreads below; (3) verify UNITS.RHYTHM_SYNCO entry in skillTrail.js (pre-authored by Task 4).
    // import rhythmUnit8SyncoNodes from './units/rhythmUnit8Redesigned.js';
    ```

    Update both commented spread lines:
    ```javascript
    // HIDDEN-V1: ...rhythmUnit8SyncoNodes,
    ```

    **Change C: Update spreads.** The EXPANDED_NODES + EXPANDED_RHYTHM_NODES arrays must spread the 10 new imports. Keep treble + bass + ear spreads untouched:
    ```javascript
    export const EXPANDED_RHYTHM_NODES = [
      ...rhythmUnit1Nodes, ...rhythmUnit2Nodes, ...rhythmUnit3Nodes,
      ...rhythmUnit4Nodes, ...rhythmUnit5Nodes, ...rhythmUnit6Nodes,
      ...rhythmUnit7Nodes, ...rhythmUnit8Nodes, ...rhythmUnit9Nodes,
      ...rhythmUnit10Nodes,
      // HIDDEN-V1: ...rhythmUnit8SyncoNodes,
    ];

    export const EXPANDED_NODES = [
      // ...existing treble/bass/ear spreads preserved...
      ...rhythmUnit1Nodes, ...rhythmUnit2Nodes, ...rhythmUnit3Nodes,
      ...rhythmUnit4Nodes, ...rhythmUnit5Nodes, ...rhythmUnit6Nodes,
      ...rhythmUnit7Nodes, ...rhythmUnit8Nodes, ...rhythmUnit9Nodes,
      ...rhythmUnit10Nodes,
      // HIDDEN-V1: ...rhythmUnit8SyncoNodes,
    ];
    ```

    Verify the module loads (no syntax errors) by importing via Node ESM dynamic import.

  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/data/expandedNodes.js').then(m => { const r = m.EXPANDED_RHYTHM_NODES; if (!r || r.length < 50) { console.error('Expected ≥50 rhythm nodes, got', r?.length); process.exit(1); } console.log('OK:', r.length, 'rhythm nodes wired'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './units/rhythmUnit1.js'" src/data/expandedNodes.js` returns 1
    - `grep -c "from './units/rhythmUnit10.js'" src/data/expandedNodes.js` returns 1
    - `grep -c "Redesigned" src/data/expandedNodes.js` returns at least 1 (the HIDDEN-V1 commented import still references rhythmUnit8Redesigned.js)
    - `grep -E "^import.*rhythmUnit[0-9]+Nodes.*from.*Redesigned" src/data/expandedNodes.js | wc -l` returns 0 (no ACTIVE imports from Redesigned files — only commented HIDDEN-V1)
    - `grep -c "rhythmUnit8SyncoNodes" src/data/expandedNodes.js` returns at least 2 (commented import + commented spread reference)
    - `grep -c "HIDDEN-V1" src/data/expandedNodes.js` returns at least 3 (all three markers preserved)
    - `node --input-type=module -e "import('./src/data/expandedNodes.js').then(m => console.log(m.EXPANDED_RHYTHM_NODES.length))"` prints a number ≥ 50 (10 units × ~5.5 avg nodes)
  </acceptance_criteria>
  <done>
    Aggregator wired. HIDDEN-V1 marker preserved with renamed binding. Module loads. EXPANDED_RHYTHM_NODES contains all new U1-U10 nodes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update skillTrail.js UNITS map (RHYTHM_1..10 + RHYTHM_SYNCO)</name>
  <files>src/data/skillTrail.js</files>
  <read_first>
    - src/data/skillTrail.js (read entirety; locate UNITS map; current RHYTHM_1..RHYTHM_8 entries at lines ~290-345 per 01-PATTERNS.md)
    - 01-PATTERNS.md §3c "Update skillTrail.js UNITS map"
    - 01-CONTEXT.md §D-01 (new unit concept labels)
    - 01-RESEARCH.md §"Pitfall 5" + Open Question #3 (RHYTHM_SYNCO entry recommendation)
  </read_first>
  <behavior>
    - UNITS map has RHYTHM_1, RHYTHM_2, ..., RHYTHM_10 entries with new concept labels
    - UNITS.RHYTHM_8 now represents "Three-Four Time" (not "Off-Beat Magic" syncopation)
    - NEW entry: UNITS.RHYTHM_SYNCO with `id: "rhythm_unit_synco"`, `name: "Off-Beat Magic"`, `description: "Master syncopation and off-beat patterns"`, `order: 99` (high to push to end if listed), preserved for re-enable mechanical path
    - All `name` strings match the EN trail.json `units.names` keys authored in Plan 03
    - Reward badge IDs are unique per unit (avoid duplicates with treble/bass/ear)
  </behavior>
  <action>
    Use Edit tool against `src/data/skillTrail.js`. Replace/update the entire RHYTHM_* block inside `UNITS`:

    ```javascript
    RHYTHM_1: {
      id: "rhythm_unit_1",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Quarter + Quarter Rest",
      description: "Meet the steady quarter pulse and its rest.",
      order: 1,
      theme: "Pulse Foundation",
      reward: { type: "accessory", id: "rhythm_quarter_badge", name: "Quarter Badge" },
    },
    RHYTHM_2: {
      id: "rhythm_unit_2",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Half + Half Rest",
      description: "Two-beat notes and their matching rests.",
      order: 2,
      theme: "Half Extensions",
      reward: { type: "accessory", id: "rhythm_half_badge", name: "Half Badge" },
    },
    RHYTHM_3: {
      id: "rhythm_unit_3",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Whole + Whole Rest",
      description: "Long held notes and full-measure silence.",
      order: 3,
      theme: "Whole Extensions",
      reward: { type: "accessory", id: "rhythm_whole_badge", name: "Whole Badge" },
    },
    RHYTHM_4: {
      id: "rhythm_unit_4",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Eighth Notes",
      description: "First subdivision — two notes per beat.",
      order: 4,
      theme: "Subdivisions Begin",
      reward: { type: "accessory", id: "rhythm_eighth_badge", name: "Eighth Badge" },
    },
    RHYTHM_5: {
      id: "rhythm_unit_5",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Sixteenth Notes",
      description: "Fast subdivision — four notes per beat.",
      order: 5,
      theme: "Super Speed",
      reward: { type: "accessory", id: "rhythm_sixteenth_badge", name: "Sixteenth Badge" },
    },
    RHYTHM_6: {
      id: "rhythm_unit_6",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Dotted Half Notes",
      description: "The dot adds half a beat — dotted half = 3 beats.",
      order: 6,
      theme: "Dotted Family I",
      reward: { type: "accessory", id: "rhythm_dotted_half_badge", name: "Dotted Half Badge" },
    },
    RHYTHM_7: {
      id: "rhythm_unit_7",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Dotted Quarter Notes",
      description: "Dotted quarter = 1½ beats, often paired with an eighth.",
      order: 7,
      theme: "Dotted Family II",
      reward: { type: "accessory", id: "rhythm_dotted_quarter_badge", name: "Dotted Quarter Badge" },
    },
    RHYTHM_8: {
      id: "rhythm_unit_8",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Three-Four Time",
      description: "Three beats per bar — waltz feel.",
      order: 8,
      theme: "Meter I",
      reward: { type: "accessory", id: "rhythm_three_four_badge", name: "Three-Four Badge" },
    },
    RHYTHM_9: {
      id: "rhythm_unit_9",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Six-Eight Time",
      description: "Two big beats — compound meter with eighth subdivisions.",
      order: 9,
      theme: "Meter II",
      reward: { type: "accessory", id: "rhythm_six_eight_badge", name: "Six-Eight Badge" },
    },
    RHYTHM_10: {
      id: "rhythm_unit_10",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Rhythm Review",
      description: "Master review across every rhythm concept.",
      order: 10,
      theme: "Mastery",
      reward: { type: "accessory", id: "rhythm_master_badge", name: "Rhythm Master Badge" },
    },
    RHYTHM_SYNCO: {
      // HIDDEN-V1 sibling entry — describes the hidden Syncopation unit (rhythm_synco_*).
      // Pre-authored so re-enable in expandedNodes.js is mechanical (no UNITS map editing needed at re-enable time).
      id: "rhythm_unit_synco",
      category: NODE_CATEGORIES.RHYTHM,
      name: "Off-Beat Magic",
      description: "Master syncopation and off-beat patterns.",
      order: 99,
      theme: "Syncopation (Hidden)",
      reward: { type: "accessory", id: "rhythm_synco_badge", name: "Syncopation Badge" },
    },
    ```

    DO NOT touch: TREBLE_*, BASS_*, EAR_* UNITS entries. Lookup functions `getNodeById`, `getNodesByCategory`, `getBossNodes`, `getNextNodeInCategory`, `isNodeUnlocked`, `getUnlockedNodes` — preserve verbatim.

    Verify name strings match Plan 03's trail.json keys (Quarter + Quarter Rest, Half + Half Rest, Whole + Whole Rest, Eighth Notes, Sixteenth Notes, Dotted Half Notes, Dotted Quarter Notes, Three-Four Time, Six-Eight Time, Rhythm Review).

  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/data/skillTrail.js').then(m => { const u = m.UNITS; const keys = Object.keys(u).filter(k => k.startsWith('RHYTHM')); if (keys.length < 11) { console.error('Expected ≥11 RHYTHM_ entries, got', keys.length, keys); process.exit(1); } console.log('OK RHYTHM_ entries:', keys.length); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RHYTHM_10:" src/data/skillTrail.js` returns 1
    - `grep -c "RHYTHM_SYNCO:" src/data/skillTrail.js` returns 1
    - `grep -c "rhythm_unit_10" src/data/skillTrail.js` returns 1
    - `grep -c "rhythm_unit_synco" src/data/skillTrail.js` returns 1
    - `grep -c "Three-Four Time" src/data/skillTrail.js` returns 1
    - `grep -c "Rhythm Review" src/data/skillTrail.js` returns 1
    - Node import script (above) reports ≥11 RHYTHM_ keys in UNITS map
  </acceptance_criteria>
  <done>
    UNITS map updated with RHYTHM_1..10 entries (new concept labels) and a sibling RHYTHM_SYNCO entry to keep the re-enable path mechanical.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Update rhythmUnits.difficulty.test.js imports + run full verify gate</name>
  <files>src/data/units/rhythmUnits.difficulty.test.js</files>
  <read_first>
    - src/data/units/rhythmUnits.difficulty.test.js (full read; locate imports near top)
    - 01-RESEARCH.md §"Test Coverage Map" (`rhythmUnits.difficulty.test.js` listed as UPDATE: "imports change; test logic stays")
  </read_first>
  <behavior>
    - Imports updated from `rhythmUnit{1..7}Redesigned.js` to `rhythmUnit{1..10}.js` (10 new imports replacing 7 old ones)
    - `rhythmUnit8Redesigned.js` symbol-based import (hidden syncopation) PRESERVED — that file still exists
    - Difficulty assertion logic preserved
    - `npm run verify:trail` exits 0 (all 4 new validator rules green + all existing rules green)
    - `npm run test:run` reports a fully green rhythm test surface
  </behavior>
  <action>
    1. Open `src/data/units/rhythmUnits.difficulty.test.js`. Find the import block at the top.

    2. Replace each `import rhythmUnitNNodes from './rhythmUnitNRedesigned.js';` for N=1..7 with `import rhythmUnitNNodes from './rhythmUnitN.js';`. Add three NEW imports for U8, U9, U10:
       ```javascript
       import rhythmUnit8Nodes from './rhythmUnit8.js';
       import rhythmUnit9Nodes from './rhythmUnit9.js';
       import rhythmUnit10Nodes from './rhythmUnit10.js';
       ```

    3. PRESERVE the import of the HIDDEN syncopation unit if present (likely uses an alternate binding name; verify by reading the file). If today's test imports the hidden unit as `rhythmUnit8Nodes` from `./rhythmUnit8Redesigned.js`, RENAME that binding to `rhythmUnit8SyncoNodes` to avoid collision with the new U8 binding:
       ```javascript
       // Hidden syncopation unit (renamed binding per Phase 1 v3.5 expandedNodes.js wire-up).
       import rhythmUnit8SyncoNodes from './rhythmUnit8Redesigned.js';
       ```
       Update any usage of the old binding in the test body accordingly.

    4. Update the difficulty assertions (if they enumerate units by name/binding) to include U8/U9/U10. If the test uses a generic structural sweep (e.g. iterating over `allUnits`), the test logic is unchanged — only the imports change.

    5. **Run the full verify gate as Task 5's automated verify:** `npm run verify:trail && npm run test:run`. Both must exit 0. This is the FIRST point in the phase where the entire trail data layer is internally consistent.

    If `npm run verify:trail` fails, the failures point to issues in Plans 05-08 unit data — fix in this task (the executor can re-edit any unit file as needed; this task acts as the integration gate).

    If specific test failures point to known unrelated tests (e.g. `MetronomeTrainer.test.js`), document them and add to the carry-over list for Plan 10. But ALL rhythm-data and trail-validator tests MUST pass.

  </action>
  <verify>
    <automated>npm run verify:trail &amp;&amp; npx vitest run src/data/units/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './rhythmUnit10.js'" src/data/units/rhythmUnits.difficulty.test.js` returns 1
    - `grep -c "from './rhythmUnit8.js'" src/data/units/rhythmUnits.difficulty.test.js` returns 1
    - `grep -c "rhythmUnit8SyncoNodes" src/data/units/rhythmUnits.difficulty.test.js` returns at least 1 (binding renamed)
    - `grep -c "from './rhythmUnit[1-7]Redesigned.js'" src/data/units/rhythmUnits.difficulty.test.js` returns 0 (OLD imports gone)
    - `npm run verify:trail` exits 0
    - `npx vitest run src/data/units/` reports all tests in the `src/data/units/` tree passing
    - `npx vitest run scripts/__tests__/validateTrail.principles.test.mjs` reports 7/7 passing
    - `npx vitest run src/locales/__tests__/scaffolding-card-parity.test.js` reports 2/2 passing
    - `npx vitest run src/config/__tests__/freeNodes.parity.test.js` reports 1/1 passing
  </acceptance_criteria>
  <done>
    Wave 2 complete. `npm run verify:trail` green for the first time. The 10-unit / 55-node rhythm trail data layer is internally consistent. Validator's 4 new rules all green against real data. Test suite green for the data + validator surfaces.
  </done>
</task>

</tasks>

<verification>
- `npm run verify:trail` exits 0
- `npx vitest run src/data/units/` — all green
- `npx vitest run scripts/__tests__/validateTrail.principles.test.mjs src/locales/__tests__/scaffolding-card-parity.test.js src/config/__tests__/freeNodes.parity.test.js` — all green
- Node import script confirms EXPANDED_RHYTHM_NODES has ≥50 nodes
</verification>

<success_criteria>

- 7 files modified/created
- U9 + U10 data + tests exist and pass
- expandedNodes.js wired with 10 new unit files; HIDDEN-V1 preserved with renamed binding
- skillTrail.js UNITS map has RHYTHM_1..10 + RHYTHM_SYNCO
- rhythmUnits.difficulty.test.js imports updated
- npm run verify:trail green
- The data-layer restructure is COMPLETE
  </success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-08-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md
</output>
