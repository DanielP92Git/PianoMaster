---
phase: 01-refactor-rhythm-trail-pedagogical-ordering-restructure-units
plan: 07
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/data/units/rhythmUnit6.js
  - src/data/units/rhythmUnit6.test.js
  - src/data/units/rhythmUnit7.js
  - src/data/units/rhythmUnit7.test.js
  - src/data/units/rhythmUnit8.js
  - src/data/units/rhythmUnit8.test.js
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04]
tags: [rhythm-trail, unit-data, dotted, meter, three-four]
must_haves:
  truths:
    - "U6 (Dotted Half) and U7 (Dotted Quarter) are SEPARATE units per D-04 (strict concept-per-unit reading)"
    - "U8 (3/4 Meter) uses timeSignature='3/4' on EVERY node (validator concept-per-unit detects meter via timeSignature signature)"
    - "U6 first node introduces hd (focusDurations=['hd']); U7 first node introduces qd; U8 first node introduces 3/4 meter via discovery_intro with focusDuration='3_4'"
    - "U8 occupies the previously-freed rhythm_8_* namespace (Plan 02 freed it by renaming hidden syncopation to rhythm_synco_*)"
  artifacts:
    - path: "src/data/units/rhythmUnit6.js"
      provides: "U6 = Dotted Half (concept family {hd})"
      contains: "rhythm_6_1"
    - path: "src/data/units/rhythmUnit7.js"
      provides: "U7 = Dotted Quarter (concept family {qd})"
      contains: "rhythm_7_1"
    - path: "src/data/units/rhythmUnit8.js"
      provides: "U8 = 3/4 Meter (timeSignature='3/4')"
      contains: "rhythm_8_1"
  key_links:
    - from: "src/data/units/rhythmUnit{6,7,8}.js"
      to: "src/data/expandedNodes.js (Plan 08 will wire)"
      via: "default export named *Nodes array"
      pattern: "export default"
---

<objective>
Author U6 (Dotted Half), U7 (Dotted Quarter), and U8 (3/4 Meter). Per D-04, the dotted units are STRICTLY separated (not one "dotted family"). U8 occupies the rhythm_8_* namespace freed by Plan 02's rename.

Purpose: REQ-01 (dotted before meters per D-05; dotted after subdivisions), REQ-03 (concept-per-unit strict reading per D-04), REQ-04 (discovery_intro on intro nodes). Plus the namespace handover from Plan 02's hidden-unit rename.
Output: 6 files (3 unit + 3 test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-CONTEXT.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-RESEARCH.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-PATTERNS.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-05-PLAN.md

<interfaces>
<!-- Reuse canonical node shape from Plan 05 / 01-PATTERNS.md §1 -->

**Meter unit detection (D-14):** `validateConceptPerUnit` identifies meter units by presence of non-`4/4` timeSignature in any node. For U8, ALL nodes use timeSignature='3/4'. The validator's meter-unit branch allows pulse-related focusDurations (q, qd, 8) within meter units — but not duration concepts like h or w. Document the allowed durations in the file header.

**Dotted unit concept family (D-14):**

- dotted_half: {hd}
- dotted_quarter: {qd}

These are STRICTLY separate per D-04 (different beat-math implications). A unit mixing both would fail `validateConceptPerUnit`.

**Pattern tag inventory:** Today's `rhythmUnit5Redesigned.js` has dotted+meter mixed (the OLD U5). Open it to extract patternTags for `dotted-half`, `dotted-quarter`, and `three-four-quarter-half` if they exist. Likely candidates: `"hd-with-q"`, `"qd-with-eighth"`, `"three-four-basic"`.
</interfaces>
</context>

<threat_model>
This plan introduces no security-relevant changes (data restructure only — no auth, no RLS, no SQL). N/A.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rhythmUnit6.js (Dotted Half) + test</name>
  <files>src/data/units/rhythmUnit6.js, src/data/units/rhythmUnit6.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit5Redesigned.js (OLD U5 had dotted-half content; extract patternTags and pattern shapes for hd)
    - src/data/units/rhythmUnit4.js (just-completed analog from Plan 06; copy non-duration arc structure)
    - 01-CONTEXT.md §D-04 (strict separation of dotted units)
  </read_first>
  <behavior>
    - File `src/data/units/rhythmUnit6.js`, 6 nodes, UNIT_NAME = "Dotted Half Notes", START_ORDER = 150
    - rhythm_6_1: DISCOVERY focusDurations=['hd'], durations=['q','h','hd'], prerequisites=['boss_rhythm_5']
    - rhythm_6_2: PRACTICE durations=['q','h','hd']
    - rhythm_6_3: DISCOVERY (mixed contrast) focusDurations=[], durations=['q','h','hd']
    - rhythm_6_4: PRACTICE
    - rhythm_6_5: SPEED_ROUND
    - boss_rhythm_6: MINI_BOSS category='boss', patternTagMode='any'
    - All timeSignature='4/4'
  </behavior>
  <action>
    Mirror `src/data/units/rhythmUnit4.js` (non-duration arc) with substitutions:
    - `UNIT_ID = 6`, `UNIT_NAME = "Dotted Half Notes"`, `START_ORDER = 150`
    - First node prereq `['boss_rhythm_5']`
    - focusDurations on rhythm_6_1 = `['hd']`
    - All nodes have timeSignature='4/4' (dotted-half lives in 4/4 — meter introduction comes later in U8)
    - XP arc: 65, 70, 70, 75, 80, 140

    Pattern tags: from `rhythmUnit5Redesigned.js`, find tags using 'hd' duration. Substitute with tags whose `RHYTHM_PATTERNS` entries include 'hd'.

    rhythm_6_1's `description`: "Dotted half — three beats. The dot adds half the note's value."

    Test asserts: length=6, ID list, focusDurations[0]=['hd'], timeSignature all '4/4', concept family {hd} only.

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit6.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Files exist
    - `grep -nc "focusDurations: \[\"hd\"\]" src/data/units/rhythmUnit6.js` returns at least 1
    - `grep -nc "focusDurations: \[\"qd\"\]" src/data/units/rhythmUnit6.js` returns 0 (qd lives in U7)
    - `grep -nc "prerequisites: \[\"boss_rhythm_5\"\]" src/data/units/rhythmUnit6.js` returns 1
    - `grep -nc "timeSignature: \"4/4\"" src/data/units/rhythmUnit6.js` returns 6 (all nodes)
    - `npx vitest run src/data/units/rhythmUnit6.test.js` reports all tests passing
  </acceptance_criteria>
  <done>
    U6 data + test exist and pass. Strict {hd}-only concept family.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write rhythmUnit7.js (Dotted Quarter) + test</name>
  <files>src/data/units/rhythmUnit7.js, src/data/units/rhythmUnit7.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit5Redesigned.js (OLD U5 had dotted-quarter content)
    - src/data/units/rhythmUnit6.js (just-completed analog from Task 1)
    - 01-CONTEXT.md §D-04
  </read_first>
  <behavior>
    - File `src/data/units/rhythmUnit7.js`, 6 nodes, UNIT_NAME = "Dotted Quarter Notes", START_ORDER = 160
    - rhythm_7_1: DISCOVERY focusDurations=['qd'], durations=['q','8','qd'], prerequisites=['boss_rhythm_6']
    - all timeSignature='4/4'
  </behavior>
  <action>
    Mirror Task 1 with substitutions:
    - `UNIT_ID = 7`, `UNIT_NAME = "Dotted Quarter Notes"`, `START_ORDER = 160`
    - First node prereq `['boss_rhythm_6']`
    - focusDurations on rhythm_7_1 = `['qd']`
    - durations typically include 'q','8','qd' (qd subdivides into q+8)
    - XP arc: 70, 75, 75, 80, 85, 150

    Pattern tags: from `rhythmUnit5Redesigned.js` for qd patterns.

    rhythm_7_1's `description`: "Dotted quarter — 1½ beats. Often paired with an eighth note to fill 2 beats."

    Test asserts {qd}-only family.

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit7.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Files exist
    - `grep -nc "focusDurations: \[\"qd\"\]" src/data/units/rhythmUnit7.js` returns at least 1
    - `grep -nc "focusDurations: \[\"hd\"\]" src/data/units/rhythmUnit7.js` returns 0 (hd lives in U6, strict separation per D-04)
    - `grep -nc "prerequisites: \[\"boss_rhythm_6\"\]" src/data/units/rhythmUnit7.js` returns 1
    - `npx vitest run src/data/units/rhythmUnit7.test.js` reports all tests passing
  </acceptance_criteria>
  <done>
    U7 data + test exist and pass. Strict {qd}-only family per D-04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write rhythmUnit8.js (3/4 Meter — occupies freed rhythm_8_* namespace) + test</name>
  <files>src/data/units/rhythmUnit8.js, src/data/units/rhythmUnit8.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit5Redesigned.js (OLD U5 node rhythm_5_3 had timeSignature='3/4' — extract patternTags + pattern shapes for 3/4)
    - src/data/units/rhythmUnit7.js (just-completed analog)
    - 01-CONTEXT.md §D-01 (U8 = 3/4 Meter)
    - 01-RESEARCH.md §"Pitfall 1" (patternTags must match timeSignature — validator catches mismatches)
    - 01-RESEARCH.md §"Pitfall 5" (RHYTHM_8 UNITS map semantic collision — handled in Plan 08)
  </read_first>
  <behavior>
    - File `src/data/units/rhythmUnit8.js`, 6 nodes, UNIT_NAME = "Three-Four Time", START_ORDER = 170
    - rhythm_8_1: DISCOVERY focusDurations=['3_4'] (meter concept ID per D-08), timeSignature='3/4', durations=['q','h','hd'], prerequisites=['boss_rhythm_7']
    - ALL nodes have timeSignature='3/4'
    - boss_rhythm_8: MINI_BOSS category='boss', patternTagMode='any', timeSignature='3/4'
    - First DISCOVERY's first question is `{ type: 'discovery_intro', focusDuration: '3_4' }` (matches the `cards["3_4"]` key in common.json from Plan 03)
  </behavior>
  <action>
    Mirror Task 1/2 with critical meter adjustments:
    - `UNIT_ID = 8`, `UNIT_NAME = "Three-Four Time"`, `START_ORDER = 170`
    - First node prereq `['boss_rhythm_7']`
    - **EVERY** node sets `timeSignature: '3/4'` (this is the concept signal for `validateConceptPerUnit`)
    - **focusDurations on rhythm_8_1 = `['3_4']`** — this is the METER concept ID, treated by `DiscoveryIntroQuestion.jsx`'s card lookup map (per Plan 03 common.json `cards["3_4"]` entry)
    - durations may include `['q','h','hd']` (waltz-friendly note values that fit 3/4)
    - measureCount stays 1/2/4 per existing policy
    - XP arc: 75, 80, 80, 85, 90, 160

    Pattern tags: find tags from `rhythmUnit5Redesigned.js`'s node `rhythm_5_3` (the OLD 3/4 node). Likely candidates: `"three-four-basic"`, `"three-four-with-dotted-half"`. Verify in RhythmPatternGenerator.js.

    **CRITICAL:** validate that the tag's `resolveByTags(tag, durations, '3/4')` returns non-null (validator's `validateDurationSafety` will fail otherwise). If existing tags don't support 3/4, add a new tag in this task (escalate scope to user via comment).

    rhythm_8_1's first question: `{ type: 'discovery_intro', focusDuration: '3_4' }`. This is INTENTIONAL even though '3_4' isn't a duration code in `DURATION_INFO` — it's a meter concept ID that `DiscoveryIntroQuestion.jsx` looks up in its `CONCEPT_CARDS` map (per Plan 09 extension). For NON-meter pattern playback (cards 2-3), the renderer falls back to `focusPattern` mode or omits the audio card.

    Test asserts:
    - length=6, IDs `rhythm_8_1..5`, `boss_rhythm_8`
    - All timeSignature='3/4'
    - focusDurations on rhythm_8_1 = `['3_4']`
    - prereq chain from boss_rhythm_7
    - boss has patternTagMode='any', isBoss=true
    - **Explicitly assert NO leftover hidden-unit references:** `expect(rhythmUnit8Nodes.find(n => n.id.startsWith('rhythm_synco_'))).toBeUndefined()` — confirms the namespace handover succeeded.

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit8.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Files exist
    - `grep -nc "timeSignature: \"3/4\"" src/data/units/rhythmUnit8.js` returns 6 (all 6 nodes)
    - `grep -nc "focusDurations: \[\"3_4\"\]" src/data/units/rhythmUnit8.js` returns at least 1
    - `grep -nc "prerequisites: \[\"boss_rhythm_7\"\]" src/data/units/rhythmUnit8.js` returns 1
    - `grep -nc "boss_rhythm_8" src/data/units/rhythmUnit8.js` returns at least 1
    - `grep -nc "rhythm_synco_" src/data/units/rhythmUnit8.js` returns 0 (no hidden-unit cross-contamination)
    - `grep -nc "discovery_intro\".*focusDuration: \"3_4\"" src/data/units/rhythmUnit8.js | head -1` returns a hit (rhythm_8_1 first question)
    - `npx vitest run src/data/units/rhythmUnit8.test.js` reports all tests passing
  </acceptance_criteria>
  <done>
    U8 (3/4 Meter) lives in the freed rhythm_8_* namespace. timeSignature='3/4' on every node. Concept-per-unit rule classifies this as a meter unit.
  </done>
</task>

</tasks>

<verification>
- All 3 unit tests pass: `npx vitest run src/data/units/rhythmUnit{6,7,8}.test.js`
- No cross-contamination between U6/U7 (dotted strict separation)
- U8 namespace fully occupied (no rhythm_synco_* leaks)
</verification>

<success_criteria>

- 6 files created. Dotted strict separation enforced. Meter unit U8 in 3/4. No collision with hidden syncopation namespace.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-07-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md
</output>
