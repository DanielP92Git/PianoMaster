---
phase: 01-refactor-rhythm-trail-pedagogical-ordering-restructure-units
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/data/units/rhythmUnit8Redesigned.js
  - src/data/units/rhythmUnit8Redesigned.test.js
  - src/data/expandedNodes.js
  - src/locales/en/trail.json
  - src/locales/he/trail.json
autonomous: true
requirements: [REQ-05]
tags: [rename, hidden-unit, syncopation, rhythm-trail]
must_haves:
  truths:
    - "D-10: Hidden Unit 8 syncopation rename — rhythm_8_* to rhythm_synco_*, boss_rhythm_8 to boss_rhythm_synco; HIDDEN-V1 marker preserved (entire plan implements D-10)"
    - "The `rhythm_8_*` and `boss_rhythm_8` numeric namespace is no longer used by any active or hidden code path"
    - "The hidden syncopation unit's node IDs are renamed to `rhythm_synco_*` and `boss_rhythm_synco`"
    - "The `HIDDEN-V1` re-enable marker block in `src/data/expandedNodes.js` is preserved (commented import + commented spreads), but references the new `rhythm_synco_*` IDs in its comments"
    - "Running `npm run test:run -- src/data/units/rhythmUnit8Redesigned.test.js` is green with the renamed IDs"
    - "Trail.json EN + HE have no orphan `rhythm_8_*` references in the hidden-unit translation block"
  artifacts:
    - path: "src/data/units/rhythmUnit8Redesigned.js"
      provides: "Hidden syncopation unit with renamed IDs"
      contains: "rhythm_synco_1"
    - path: "src/data/units/rhythmUnit8Redesigned.test.js"
      provides: "Test file updated to assert new IDs"
      contains: "rhythm_synco_1"
    - path: "src/data/expandedNodes.js"
      provides: "Aggregator with HIDDEN-V1 marker pointing at renamed IDs"
      contains: "HIDDEN-V1"
  key_links:
    - from: "src/data/units/rhythmUnit8Redesigned.js"
      to: "src/data/units/rhythmUnit8Redesigned.test.js"
      via: "named export `rhythmUnit8Nodes`"
      pattern: "rhythmUnit8Nodes"
---

<objective>
Free the `rhythm_8_*` / `boss_rhythm_8` numeric namespace by renaming the hidden Unit 8 syncopation file's internal node IDs to `rhythm_synco_*` / `boss_rhythm_synco` per D-10. This MUST land before Wave 2 plans (05-08) write new U8 (3/4 Meter) data, or there will be a duplicate-ID collision at validator time.

Purpose: Implements D-10 (hidden Unit 8 rename) and removes the namespace blocker for new U8 (3/4 Meter). Re-enable path stays intact — the file path and `HIDDEN-V1` marker structure are preserved; only internal IDs change.
Output: 5 files modified. Hidden unit's symbolic export shape (`rhythmUnit8Nodes`) preserved; only ID strings change. Re-enable workflow remains: uncomment two lines in `expandedNodes.js`, optionally add `RHYTHM_SYNCO` UNITS map entry (Plan 08 will pre-author that entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-CONTEXT.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-RESEARCH.md
@.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-PATTERNS.md

<interfaces>
<!-- Existing hidden unit's ID surface — extracted from 01-PATTERNS.md §3 -->

From src/data/units/rhythmUnit8Redesigned.js (current — to be renamed):

- 7 node IDs: rhythm_8_1, rhythm_8_2, rhythm_8_3, rhythm_8_4, rhythm_8_5, rhythm_8_6, boss_rhythm_8
- prerequisites chain: node 1 → boss_rhythm_7 (external), 2..7 → previous in unit
- focusPattern.id strings used: "qhq", "synsyn" — these are PATTERN IDs, NOT node IDs. DO NOT rename.

From src/data/expandedNodes.js (lines 51-52, 89, 123):

```javascript
// HIDDEN-V1: Unit 8 (syncopation, "Off-Beat Magic") temporarily disabled — re-enable for next release
// import rhythmUnit8Nodes from './units/rhythmUnit8Redesigned.js';
// ...
// HIDDEN-V1: ...rhythmUnit8Nodes,
```

</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                  |
| ---------- | ------------------------------------------------------------------------------------------------------------ |
| build-time | A leftover `rhythm_8_*` reference would conflict with new U8 (3/4 Meter) — duplicate ID = validator failure. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                 | Disposition | Mitigation Plan                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------ | ----------------------- | ----------------------------------------------- |
| T-02-01   | Tampering              | Incomplete rename leaves `rhythm_8_3` somewhere → collides with new U8 (3/4 Meter) node when Wave 2 lands | mitigate    | Final grep gate: `grep -rn "rhythm*8*" src/                                                            | grep -v "rhythm*synco*" | grep -v "^#"` returns ZERO matches across src/. |
| T-02-02   | Information Disclosure | Translation orphans in trail.json could surface hidden-unit names in UI if re-enabled accidentally        | accept      | Hidden-unit translation keys are renamed but content is preserved; only re-enable path activates them. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename node IDs inside rhythmUnit8Redesigned.js + its test file</name>
  <files>src/data/units/rhythmUnit8Redesigned.js, src/data/units/rhythmUnit8Redesigned.test.js</files>
  <read_first>
    - src/data/units/rhythmUnit8Redesigned.js (read entirety — all 7 nodes' `id` and `prerequisites` fields must be updated)
    - src/data/units/rhythmUnit8Redesigned.test.js (read entirety — every `expect(...).toEqual` over IDs needs updating)
    - 01-PATTERNS.md §3a "Rename inside rhythmUnit8Redesigned.js" (exact rename map)
    - 01-CONTEXT.md §D-10 (decision source)
  </read_first>
  <behavior>
    - All node IDs in rhythmUnit8Redesigned.js are renamed per the rename map (see action). External chain-link `prerequisites: ["boss_rhythm_7"]` on node 1 is preserved (re-enable chain into trail unchanged).
    - All ID assertions in rhythmUnit8Redesigned.test.js use the new IDs.
    - `focusPattern.id` strings `"qhq"` and `"synsyn"` are PRESERVED verbatim (they are pattern IDs in `RHYTHM_PATTERNS`, not node IDs).
    - `UNIT_NAME = "Syncopation"` is PRESERVED.
    - `UNIT_ID = 8` may stay numeric (semantic-only, file is hidden).
  </behavior>
  <action>
    **Rename map (exact pairs, applied to all `id:` fields AND `prerequisites:` arrays):**

    | Old ID | New ID |
    |--------|--------|
    | `rhythm_8_1` | `rhythm_synco_1` |
    | `rhythm_8_2` | `rhythm_synco_2` |
    | `rhythm_8_3` | `rhythm_synco_3` |
    | `rhythm_8_4` | `rhythm_synco_4` |
    | `rhythm_8_5` | `rhythm_synco_5` |
    | `rhythm_8_6` | `rhythm_synco_6` |
    | `boss_rhythm_8` | `boss_rhythm_synco` |

    **In `src/data/units/rhythmUnit8Redesigned.js`:**
    1. Use the Edit tool to update each node's `id:` field (7 nodes).
    2. Update each node's `prerequisites:` array (node 1 keeps `["boss_rhythm_7"]`; nodes 2..7 update to chain new IDs; boss node prereq becomes `["rhythm_synco_6"]`).
    3. PRESERVE: `UNIT_ID = 8`, `UNIT_NAME = "Syncopation"`, `CATEGORY`, `START_ORDER = 144`, all `focusPattern.id` strings (`"qhq"`, `"synsyn"`), all `name`, `description`, `unitName` display strings, all `rhythmConfig` fields, all `exercises` configs, all `xpReward`, `nodeType` values.
    4. Add a one-line comment at top of file (after existing license/header if any): `// IDs renamed rhythm_8_* → rhythm_synco_* per D-10 (Phase 1 v3.5) to free numeric namespace for new U8 (3/4 Meter). HIDDEN-V1 marker in expandedNodes.js preserved.`

    **In `src/data/units/rhythmUnit8Redesigned.test.js`:**
    1. Update the `expectedIds` array (per 01-PATTERNS.md §3a):
       ```javascript
       const expectedIds = [
         "rhythm_synco_1", "rhythm_synco_2", "rhythm_synco_3",
         "rhythm_synco_4", "rhythm_synco_5", "rhythm_synco_6",
         "boss_rhythm_synco",
       ];
       ```
    2. Update the prereq-chain assertion to walk the new IDs:
       ```javascript
       expect(rhythmUnit8Nodes[0].prerequisites).toEqual(["boss_rhythm_7"]);
       for (let i = 1; i < rhythmUnit8Nodes.length; i++) {
         expect(rhythmUnit8Nodes[i].prerequisites).toEqual([rhythmUnit8Nodes[i - 1].id]);
       }
       ```
       (this assertion is structural — it already works without ID-specific text — but VERIFY by reading the existing test).
    3. Update describe label from `"Rhythm Unit 8 — Syncopation (v2, 7-node monomodal)"` to `"Rhythm Unit Syncopation (HIDDEN, renamed from rhythm_8_*)"` to make the rename visible at test-output time.
    4. PRESERVE: all other test cases (timeSignature invariant, XP arc, category check).

    NOTE: `src/data/units/rhythmUnits.difficulty.test.js` line 9 already imports `rhythmUnit8Nodes` as a SYMBOL — symbol-based not ID-based. Verify and leave untouched.

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit8Redesigned.test.js src/data/units/rhythmUnits.difficulty.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "rhythm_synco_1" src/data/units/rhythmUnit8Redesigned.js` returns at least 2 (id + as prereq for node 2)
    - `grep -nc "boss_rhythm_synco" src/data/units/rhythmUnit8Redesigned.js` returns at least 2 (id + as boss prereq target if chained)
    - `grep -n "\"rhythm_8_" src/data/units/rhythmUnit8Redesigned.js | grep -v "^#"` returns ZERO matches (no leftover old IDs in source)
    - `grep -n "\"boss_rhythm_8\"" src/data/units/rhythmUnit8Redesigned.js | grep -v "^#"` returns ZERO matches
    - `grep -nc "rhythm_synco_1" src/data/units/rhythmUnit8Redesigned.test.js` returns at least 1
    - `grep -nc "qhq\\|synsyn" src/data/units/rhythmUnit8Redesigned.js` returns at least 2 (pattern IDs preserved)
    - `npx vitest run src/data/units/rhythmUnit8Redesigned.test.js` reports all tests passing
    - `npx vitest run src/data/units/rhythmUnits.difficulty.test.js` still passes (no regressions from symbol-based import)
  </acceptance_criteria>
  <done>
    Hidden Unit 8's internal IDs are renamed. The two test files import the same symbol, get the new IDs, and pass. The `rhythm_8_*` numeric namespace is reserved for new U8 (3/4 Meter) Wave 2 plans to occupy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update HIDDEN-V1 marker comments + locale entries to reference renamed IDs</name>
  <files>src/data/expandedNodes.js, src/locales/en/trail.json, src/locales/he/trail.json</files>
  <read_first>
    - src/data/expandedNodes.js (read entirety — find all three HIDDEN-V1 markers at lines 51-52, 89, 123)
    - src/locales/en/trail.json (search for `rhythm_8_` references and `Off-Beat Magic` / `Syncopation` unit name strings; trail.json keys by DISPLAY-NAME string, not node ID — see 01-RESEARCH.md §"Locale-Key Pattern")
    - src/locales/he/trail.json (HE parity)
    - 01-PATTERNS.md §3b "Update expandedNodes.js HIDDEN-V1 marker"
    - 01-RESEARCH.md §"Hidden Unit 8 rename impact" (table — which files need what)
  </read_first>
  <behavior>
    - All HIDDEN-V1 comment-lines in `expandedNodes.js` reference the new `rhythm_synco_*` naming (comment text only; the commented-out import/spread lines stay structurally identical).
    - Any `rhythm_8_*` ID-keyed entries in trail.json (EN + HE) — if they exist for the hidden unit — are removed or renamed. NOTE: trail.json keys are DISPLAY-NAME strings, not IDs, so the more likely surface is a `units.names["Off-Beat Magic"]` or `nodes["Off-Beat Magic Pattern A"]` style entry. The unit-name "Syncopation" / "Off-Beat Magic" entries STAY (they describe the same hidden unit by its display name).
    - No active code references `rhythm_8_*` anywhere in src/ or scripts/.
  </behavior>
  <action>
    **File 1: `src/data/expandedNodes.js`**

    Find the three HIDDEN-V1 marker blocks. Update ONLY the comment text — the commented-out import and spread structure stay structurally identical.

    Current (lines 51-52, per 01-PATTERNS.md):
    ```javascript
    // HIDDEN-V1: Unit 8 (syncopation, "Off-Beat Magic") temporarily disabled — re-enable for next release
    // import rhythmUnit8Nodes from './units/rhythmUnit8Redesigned.js';
    ```

    Replace with:
    ```javascript
    // HIDDEN-V1: Syncopation unit ("Off-Beat Magic", renamed to rhythm_synco_*/boss_rhythm_synco per Phase 1 v3.5 D-10) — temporarily disabled — re-enable for future release.
    // To re-enable: (1) uncomment the import below; (2) uncomment both spread lines in EXPANDED_NODES + EXPANDED_RHYTHM_NODES; (3) add RHYTHM_SYNCO entry to UNITS map in skillTrail.js (pre-authored by Phase 1 Plan 08); (4) update CLAUDE.md node counts.
    // import rhythmUnit8Nodes from './units/rhythmUnit8Redesigned.js';
    ```

    For the two spread placeholders (lines 89, 123), update the inline comment from `HIDDEN-V1: ...rhythmUnit8Nodes,` to `HIDDEN-V1: ...rhythmUnit8Nodes (rhythm_synco_*),`. Spread structure unchanged.

    **DO NOT** uncomment the import or spreads. The unit MUST remain hidden.

    **File 2: `src/locales/en/trail.json`**

    Search for any entries that are keyed by a `rhythm_8_*` ID-pattern (unlikely — `trail.json` keys by display-name string per 01-RESEARCH.md §"Locale-Key Pattern"). If found, rename keys to `rhythm_synco_*`. KEEP the display-name keys `"Off-Beat Magic"` and `"Syncopation"` if present — those describe the hidden unit's user-facing identity which is preserved.

    Specifically check (and remove/rename if present):
    - `nodes` block: any node-name keys that are explicitly tied to `rhythm_8_*` IDs (rare; trail.json convention uses display names)
    - `descriptions` block: same
    - `unlockHints` block: same

    Search command before editing: `grep -n "rhythm_8_\|boss_rhythm_8" src/locales/en/trail.json` — if it returns no matches, no edits needed in this file beyond verification.

    **File 3: `src/locales/he/trail.json`**

    Mirror EN. Same search: `grep -n "rhythm_8_\|boss_rhythm_8" src/locales/he/trail.json`. Rename ID-keyed entries; preserve display-name-keyed entries.

    **Final invariant check** (run after all edits):
    - `grep -rn "rhythm_8_\\|boss_rhythm_8" src/ scripts/ supabase/ public/ 2>/dev/null | grep -v "rhythm_synco" | grep -v "^Binary"` returns ZERO matches across the entire repo (excluding `.planning/` planning docs which legitimately reference the old IDs in historical context).

  </action>
  <verify>
    <automated>npm run verify:trail && grep -rn "rhythm_8_\|boss_rhythm_8" src/ scripts/ supabase/ public/ 2>/dev/null | grep -v rhythm_synco | grep -v "^Binary" | grep -v "^$" ; if [ $? -ne 1 ]; then echo "FAIL: leftover rhythm_8_ reference found"; exit 1; else echo "OK: no leftover rhythm_8_ references"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "rhythm_synco" src/data/expandedNodes.js` returns at least 2 (the two updated comment blocks)
    - `grep -c "HIDDEN-V1" src/data/expandedNodes.js` returns at least 3 (all three markers preserved)
    - `grep -c "^[^/]*import rhythmUnit8Nodes" src/data/expandedNodes.js` returns 0 (import stays commented out)
    - `grep -rn "rhythm_8_\|boss_rhythm_8" src/ scripts/ supabase/ public/ 2>/dev/null | grep -v rhythm_synco | grep -v "^Binary"` returns NO lines
    - `npm run verify:trail` exits 0 (or fails only on the documented Wave 0 known-violation; rename should not introduce NEW failures)
  </acceptance_criteria>
  <done>
    The `rhythm_8_*` numeric namespace is completely free across all src/, scripts/, supabase/, public/. HIDDEN-V1 markers preserve the re-enable path with updated comments. trail.json EN+HE have no stale ID references. Wave 2 plans can now write new U8 (3/4 Meter) without collision.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "rhythm_8_\|boss_rhythm_8" src/ scripts/ supabase/ public/` returns no matches outside `rhythm_synco_*` and `boss_rhythm_synco`
- `npx vitest run src/data/units/rhythmUnit8Redesigned.test.js` passes
- `npm run verify:trail` does not introduce new errors from this plan's changes
</verification>

<success_criteria>

- Hidden unit syncopation renamed: `rhythm_8_*` → `rhythm_synco_*`, `boss_rhythm_8` → `boss_rhythm_synco`
- Re-enable path preserved: `HIDDEN-V1` markers + structure intact; only comment text updated
- Numeric namespace `rhythm_8_*` freed for new U8 (3/4 Meter) in Wave 2
- All tests touching this file pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-rhythm-trail-pedagogical-ordering-restructure-units/01-02-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md
</output>
