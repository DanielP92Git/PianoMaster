---
phase: 18-streak-protection
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/hooks/useStreakWithAchievements.js
  - src/utils/xpSystem.js
  - src/components/games/VictoryScreen.jsx
  - src/components/streak/StreakDisplay.jsx
  - src/components/layout/Dashboard.jsx
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements: [STRK-01, STRK-02, STRK-03, STRK-04]

must_haves:
  truths:
    - "StreakDisplay card shows freeze count below streak counter when freezes > 0"
    - "StreakDisplay shows amber warning state during grace window with encouraging message"
    - "Dashboard shows a 2x XP comeback bonus banner when comeback bonus is active"
    - "VictoryScreen shows 2x badge near XP counter and doubles displayed XP when comeback active"
    - "A toast appears on login when a freeze was consumed since last visit"
    - "Earning a freeze triggers a toast notification"
  artifacts:
    - path: "src/components/streak/StreakDisplay.jsx"
      provides: "Freeze count display and grace warning state"
      contains: "freezeCount"
    - path: "src/components/layout/Dashboard.jsx"
      provides: "Comeback bonus banner"
      contains: "comebackBonus"
    - path: "src/components/games/VictoryScreen.jsx"
      provides: "2x XP badge when comeback active"
      contains: "comebackMultiplier"
    - path: "src/utils/xpSystem.js"
      provides: "Comeback multiplier applied to XP calculation"
      exports: ["calculateNodeXP", "awardXP"]
  key_links:
    - from: "src/hooks/useStreakWithAchievements.js"
      to: "src/services/streakService.js"
      via: "updateStreak() return value includes freezeEarned, comebackBonusActivated"
      pattern: "streakService\\.updateStreak"
    - from: "src/components/streak/StreakDisplay.jsx"
      to: "src/services/streakService.js"
      via: "getStreakState() for full state"
      pattern: "streakService\\.getStreakState"
    - from: "src/components/games/VictoryScreen.jsx"
      to: "src/utils/xpSystem.js"
      via: "calculateSessionXP with comeback multiplier"
      pattern: "calculateSessionXP"
---

<objective>
Wire streak protection into all UI touchpoints: StreakDisplay freeze count and grace warning, Dashboard comeback banner, VictoryScreen 2x XP badge, XP multiplier in xpSystem, toast notifications for freeze events, and all i18n strings.

Purpose: Students see the protective streak system in action — freezes visible, grace state communicated, comeback bonus incentivizing return, and XP properly doubled.
Output: 7 modified files with complete UI integration
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-streak-protection/18-CONTEXT.md
@.planning/phases/18-streak-protection/18-01-SUMMARY.md

@src/components/streak/StreakDisplay.jsx
@src/components/layout/Dashboard.jsx
@src/components/games/VictoryScreen.jsx
@src/hooks/useStreakWithAchievements.js
@src/utils/xpSystem.js
@src/services/streakService.js

<interfaces>
<!-- From Plan 01 - streakService.js new exports: -->
<!--
streakService.getStreakState() → {
  streakCount: number,
  freezeCount: number,
  weekendPassEnabled: boolean,
  inGraceWindow: boolean,
  lastFreezeConsumedAt: string|null,
  comebackBonus: { active: boolean, expiresAt: string|null, daysLeft: number }
}

streakService.updateStreak() → {
  newStreak: number,
  freezeEarned: boolean,
  freezeConsumed: boolean,
  streakBroken: boolean,
  comebackBonusActivated: boolean
}
-->

<!-- From src/utils/xpSystem.js: -->
<!-- calculateNodeXP(stars, baseXP, bonuses) → totalXP (number) -->
<!-- calculateSessionXP(session) → { stars, baseXP, bonusXP, bonuses, totalXP } -->
<!-- awardXP(studentId, xpAmount) → { newTotalXP, newLevel, leveledUp, xpAwarded } -->

<!-- From src/hooks/useStreakWithAchievements.js: -->
<!-- useMutation wrapping streakService.updateStreak() -->
<!-- onSuccess: invalidates ["streak", userId], ["earned-achievements", userId] -->

<!-- Existing toast pattern: -->
<!-- import { toast } from 'react-hot-toast'; toast.success('message') -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useStreakWithAchievements, xpSystem, and VictoryScreen for comeback multiplier and freeze toasts</name>
  <files>src/hooks/useStreakWithAchievements.js, src/utils/xpSystem.js, src/components/games/VictoryScreen.jsx</files>
  <action>
**useStreakWithAchievements.js:**
1. Update the mutation's onSuccess to handle new return fields from updateStreak():
   - If `freezeEarned`: show toast `toast.success(t('streak.freezeEarned'))` with a snowflake emoji (use i18n key, import `useTranslation` — but this is a hook used inside components, so the toast text can be a simple English string if i18n is not available in mutation context; alternatively, pass a callback). Since this hook doesn't have `t()`, use a simple approach: return the flags and let the calling component show the toast. Add `freezeEarned` and `comebackBonusActivated` to the return value alongside `newStreak` and `newAchievements`.
   - Also invalidate `["streak-state", user.id]` query key (new from Plan 01).

**xpSystem.js:**
2. Add a `comebackMultiplier` parameter to `calculateNodeXP()` and `calculateSessionXP()`:
   - `calculateNodeXP(stars, baseXP, bonuses)` → add `bonuses.comebackMultiplier` (default 1). Apply as final step: `totalXP = totalXP * (bonuses.comebackMultiplier || 1)`.
   - `calculateSessionXP(session)` → add `session.comebackMultiplier` (default 1). Apply to `totalXP` at the end. Add `comebackMultiplier` to the returned breakdown object for display.
   - This is backward compatible: existing callers pass no comebackMultiplier, so it defaults to 1 (no change).

**VictoryScreen.jsx:**
3. Fetch comeback bonus state at mount time for XP display:
   - Import `streakService` and add a `useQuery` for `["streak-state", user.id]` calling `streakService.getStreakState()`, enabled when `user?.id` exists.
   - Extract `comebackBonus.active` from the state.
   - When calling `calculateSessionXP()`, pass `comebackMultiplier: comebackBonus.active ? 2 : 1` in the session object.
   - The awardXP call should also pass the doubled amount: `xpBreakdown.totalXP` (already doubled by calculateSessionXP).

4. Add 2x badge in the XP display section:
   - When `comebackBonus?.active`, add a small badge next to the XP earned line:
     ```jsx
     {comebackActive && (
       <span className="ml-1.5 inline-flex items-center rounded-full bg-amber-100 px-2 py-0.5 text-xs font-bold text-amber-700">
         2x
       </span>
     )}
     ```
   - Also add an XP breakdown line for the comeback bonus: "Comeback Bonus: x2" (using i18n key `victory.comebackBonus`).

5. Handle freeze-earned and comeback-activated toasts from streak update:
   - In the existing `useEffect` that calls `updateStreakWithAchievements.mutate()`, check the onSuccess result for `freezeEarned` and `comebackBonusActivated`:
     - `freezeEarned`: `toast.success(t('streak.freezeEarned'))` — show after a short delay so it doesn't overlap with XP animation.
     - `comebackBonusActivated`: no toast here (the banner on Dashboard handles awareness).
  </action>
  <verify>
    <automated>cd "C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2" && npx grep -l "comebackMultiplier" src/utils/xpSystem.js src/components/games/VictoryScreen.jsx 2>/dev/null; echo "---"; grep -c "streak-state" src/components/games/VictoryScreen.jsx || echo 0</automated>
  </verify>
  <done>calculateSessionXP supports comebackMultiplier, VictoryScreen fetches streak state and shows 2x badge when comeback active, useStreakWithAchievements returns freeze/comeback flags and invalidates streak-state query</done>
</task>

<task type="auto">
  <name>Task 2: StreakDisplay freeze/grace UI, Dashboard comeback banner, and i18n strings</name>
  <files>src/components/streak/StreakDisplay.jsx, src/components/layout/Dashboard.jsx, src/locales/en/common.json, src/locales/he/common.json</files>
  <action>
**StreakDisplay.jsx:**
1. Switch from `streakService.getStreak()` to `streakService.getStreakState()` for the query:
   - Query key: `["streak-state"]` (no user ID needed — service gets session internally).
   - Return full state object: `{ streakCount, freezeCount, inGraceWindow, lastFreezeConsumedAt, comebackBonus }`.

2. Add freeze count display in the card variant:
   - Below the streak counter (after the day label line), when `freezeCount > 0`:
     ```jsx
     <div className="flex items-center justify-center gap-1 text-xs text-blue-300">
       <span>❄️</span>
       <span>{freezeCount}</span>
     </div>
     ```
   - When freezeCount === 0, show nothing (clean).

3. Add grace window warning state:
   - When `inGraceWindow` is true, override the visuals to show amber/yellow:
     - `bgColor: "bg-amber-500/20"`, `textColor: "text-amber-400"`, `iconColor: "text-amber-400"`
     - Replace the normal message with: `t('streak.graceWarning')` — "Practice today to keep your streak!"
   - Keep the streak count and milestone progress unchanged — just change the color/message.

4. Freeze-consumed toast on mount:
   - When `lastFreezeConsumedAt` is not null and is within the last 24 hours:
     - Show `toast.success(t('streak.freezeConsumed'))` once per session using a `useRef` guard.
     - Message: "A Streak Freeze saved your streak!"
   - Also show a small annotation below the freeze count: `t('streak.freezeUsedYesterday')` — "Freeze used" (shown for 24h after consumption).

**Dashboard.jsx:**
5. Add comeback bonus banner above the stats cards:
   - Fetch streak state: add `useQuery` for `["streak-state", user.id]` calling `streakService.getStreakState()`.
   - When `comebackBonus.active === true`:
     ```jsx
     <div className="w-full rounded-xl bg-gradient-to-r from-amber-500/20 to-yellow-500/20 border border-amber-400/30 px-4 py-3 text-center">
       <p className="text-sm font-bold text-amber-300">
         {t('streak.comebackBanner', { days: comebackBonus.daysLeft })}
       </p>
       <p className="text-xs text-amber-200/70 mt-0.5">
         {t('streak.comebackDescription')}
       </p>
     </div>
     ```
   - Place this above the existing stat cards grid (after the hero section, before the `<div className="grid grid-cols-2...` block).
   - Banner uses glass card pattern (bg-white/10 variant with amber accent).

**i18n (en/common.json):**
6. Add all new keys under a `"streak"` top-level key:
   ```json
   "streak": {
     "freezeEarned": "You earned a Streak Freeze! ❄️",
     "freezeConsumed": "A Streak Freeze saved your streak! ❄️",
     "freezeCount": "{{count}} freeze(s)",
     "freezeUsedYesterday": "Freeze used",
     "graceWarning": "Practice today to keep your streak!",
     "comebackBanner": "2x XP Active! {{days}} day(s) left",
     "comebackDescription": "Complete exercises to earn double XP",
     "weekendPassLabel": "Weekend Pass",
     "weekendPassDescription": "Streaks won't require Friday & Saturday practice",
     "weekendPassEnabled": "Weekend Pass is on",
     "weekendPassDisabled": "Weekend Pass is off"
   }
   ```
   Also add `"victory.comebackBonus": "Comeback Bonus"` under the existing `"victory"` key.

**i18n (he/common.json):**
7. Add Hebrew translations for all streak keys:
   ```json
   "streak": {
     "freezeEarned": "הרווחת הקפאת רצף! ❄️",
     "freezeConsumed": "הקפאת רצף הצילה את הרצף שלך! ❄️",
     "freezeCount": "{{count}} הקפאה/ות",
     "freezeUsedYesterday": "הקפאה בשימוש",
     "graceWarning": "תתרגל/י היום כדי לשמור על הרצף!",
     "comebackBanner": "XP כפול פעיל! עוד {{days}} ימים",
     "comebackDescription": "השלם/י תרגילים כדי להרוויח XP כפול",
     "weekendPassLabel": "פטור סוף שבוע",
     "weekendPassDescription": "הרצף לא ידרוש תרגול בימי שישי ושבת",
     "weekendPassEnabled": "פטור סוף שבוע פעיל",
     "weekendPassDisabled": "פטור סוף שבוע כבוי"
   }
   ```
   Also add `"victory.comebackBonus": "בונוס חזרה"` under the existing `"victory"` key.
  </action>
  <verify>
    <automated>cd "C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2" && grep -c "freezeCount\|inGraceWindow\|comebackBonus" src/components/streak/StreakDisplay.jsx && grep -c "comebackBanner\|comebackBonus" src/components/layout/Dashboard.jsx && grep -c "streak" src/locales/en/common.json | head -1</automated>
  </verify>
  <done>StreakDisplay shows freeze count and amber grace warning, Dashboard shows comeback banner when active, freeze-consumed toast fires on mount, all i18n keys in en and he</done>
</task>

</tasks>

<verification>
1. StreakDisplay card variant shows freeze snowflake + count when freezeCount > 0
2. StreakDisplay turns amber with grace message when inGraceWindow is true
3. Dashboard shows "2x XP Active! X days left" banner when comebackBonus.active
4. VictoryScreen shows "2x" badge next to XP and doubles the displayed amount
5. calculateSessionXP({...session, comebackMultiplier: 2}).totalXP is double the base
6. Toast fires once for freeze-consumed event within 24h of consumption
7. All 11+ new i18n keys exist in both en and he common.json files
</verification>

<success_criteria>
- Freeze count visible in StreakDisplay card (snowflake + number)
- Grace window shows amber state with "Practice today to keep your streak!"
- Comeback banner visible on Dashboard with days remaining
- VictoryScreen XP section shows "2x" badge and doubled XP amount
- calculateSessionXP with comebackMultiplier=2 returns double totalXP
- All new strings translated in en and he
</success_criteria>

<output>
After completion, create `.planning/phases/18-streak-protection/18-02-SUMMARY.md`
</output>
