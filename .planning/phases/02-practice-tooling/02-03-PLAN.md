---
phase: 02-practice-tooling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/SightReadingSessionContext.jsx
  - src/components/games/sight-reading-game/hooks/useReviewDrill.js
  - src/components/games/sight-reading-game/hooks/useReviewDrill.test.js
  - src/components/games/sight-reading-game/components/ReviewDrillPanel.jsx
autonomous: true
requirements: [PRAC-03, PRAC-04]
must_haves:
  truths:
    - "Session context exposes a session-scoped gradingMode with a lock: lockMode() prevents changes until unlockMode() at a session boundary"
    - "useReviewDrill filters an attempt down to wrong_pitch + missed notes only, and advances to the next mistake when a matching pitch (enharmonic-aware) is played — never touching combo/on-fire"
    - "ReviewDrillPanel renders the current review target, progress (N of M), and exit affordance using the glass card + GameActionButton conventions"
  artifacts:
    - path: "src/contexts/SightReadingSessionContext.jsx"
      provides: "gradingMode/setGradingMode/isModeLocked/lockMode/unlockMode"
      contains: "gradingMode"
    - path: "src/components/games/sight-reading-game/hooks/useReviewDrill.js"
      provides: "mistake list + advance-on-match state machine"
      exports: ["useReviewDrill"]
    - path: "src/components/games/sight-reading-game/components/ReviewDrillPanel.jsx"
      provides: "review UI"
      exports: ["ReviewDrillPanel"]
  key_links:
    - from: "useReviewDrill"
      to: "performanceResults filter"
      via: "timingStatus === missed || wrong_pitch"
      pattern: "wrong_pitch"
    - from: "useReviewDrill.handlePitch"
      to: "advance ref+state"
      via: "ref-mirror discipline, no combo calls"
      pattern: "currentMistakeIndexRef"
---

<objective>
Build the session-scoped grading-mode state (with lock/unlock semantics per D-05) inside
`SightReadingSessionContext`, and the two isolated Review-mistakes building blocks (PRAC-04): the
`useReviewDrill` hook (mistake list + advance-on-matching-pitch state machine, D-16/D-19/D-22) and
the `ReviewDrillPanel` component. All three are isolated from the 4,000-line game file; plans 02-07
and 02-09 wire them in.

Purpose: Establish the mode-lock state contract (D-05) and the review drill's active-recall state
machine (D-16) — both following the exact ref-mirror discipline from Phase 01's combo/on-fire —
before game-file integration. D-18 forbids the drill from ever touching combo/on-fire.
Output: modified `SightReadingSessionContext.jsx`; NEW `useReviewDrill.js` + test; NEW `ReviewDrillPanel.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md
@.planning/phases/01-engagement-hud-parity/01-CONTEXT.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). -->

SightReadingSessionContext.jsx ref-mirror precedent (Phase 01, :27-39) — copy this shape:

```javascript
const [combo, setCombo] = useState(0);
const comboRef = useRef(0);
const incrementCombo = useCallback(() => {
  comboRef.current += 1;
  setCombo(comboRef.current);
  if (comboRef.current >= ON_FIRE_THRESHOLD && !isOnFireRef.current) {
    isOnFireRef.current = true;
    setIsOnFire(true);
  }
}, []);
// value = useMemo(() => ({ ...state, combo, isOnFire, incrementCombo, resetCombo }), [state, ..., combo, isOnFire, incrementCombo, resetCombo]);
// NOTE: `// eslint-disable-next-line react-refresh/only-export-components` above the exported hook/consts is deliberate — preserve it.
```

Review mistake filter (D-19, verified against getDetailedBreakdown semantics):

```javascript
const mistakes = performanceResults.filter(
  (r) => r.timingStatus === "missed" || r.timingStatus === "wrong_pitch"
);
```

Enharmonic matching: `sight-reading-game/__tests__/enharmonicMatching.test.js` documents
`noteToMidi` semantics — reuse the same pitch-comparison approach so C#4 vs Db4 both match.

audioEngine.playPianoSound(volume, pitchName) — `src/hooks/useAudioEngine.js:533` — target-pitch
audition; same call the keyboard input already uses. The hook receives whatever plays the target.

FeedbackSummary.jsx conventions to mirror in ReviewDrillPanel:

- glass card shell (:114-118): `<div className="relative w-full rounded-2xl border border-white/20 bg-white/10 px-4 py-3 shadow-2xl backdrop-blur-md sm:px-5 sm:py-3.5">`
- `GameActionButton` (from `../../shared/hud/GameActionButton`) with `tone="retry"`/`"advance"` for primary CTAs only
- i18n: `const { t } = useTranslation("common");` then `sightReading.*` keys with `{ defaultValue }` fallback for dynamic keys
- FEEDBACK_COLORS from `../constants/feedbackPalette`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-scoped gradingMode + lock state to SightReadingSessionContext</name>
  <read_first>
    - src/contexts/SightReadingSessionContext.jsx (full — 198 lines; combo/isOnFire are the exact composition to mirror)
    - src/components/games/sight-reading-game/constants/gradingModes.js (created in 02-01 — for GRADING_MODES.TEST default; if 02-01 not yet merged, use the literal "test" and add a TODO-free comment — but 02-01 is same-wave/independent; import from constants/gradingModes is preferred)
  </read_first>
  <files>src/contexts/SightReadingSessionContext.jsx</files>
  <action>
Add session-scoped grading-mode state following the EXACT combo/isOnFire composition (useState +
useRef mirror + inclusion in the memoized `value` + its dep array):

1. `const [gradingMode, setGradingModeState] = useState(GRADING_MODES.TEST);` with a
   `gradingModeRef = useRef(GRADING_MODES.TEST)`. Import `GRADING_MODES` from
   `../components/games/sight-reading-game/constants/gradingModes` (verify the correct relative path
   from `src/contexts/`).
2. `const [isModeLocked, setIsModeLocked] = useState(false);` with `isModeLockedRef = useRef(false)`.
3. `setGradingMode(mode)` callback: no-op if `isModeLockedRef.current` is true; otherwise mutate ref
   then setState (ref-first, matching `incrementCombo` order). Allowlist-guard: only accept
   `GRADING_MODES.PRACTICE` or `GRADING_MODES.TEST`, ignore anything else.
4. `lockMode()` callback: `isModeLockedRef.current = true; setIsModeLocked(true);` (idempotent).
5. `unlockMode()` callback: `isModeLockedRef.current = false; setIsModeLocked(false);`
6. Expose `gradingModeRef` on the context value too, so the game's detection callbacks can read the
   mode synchronously without a stale closure (Pattern 1).
7. Add `gradingMode, isModeLocked, gradingModeRef, setGradingMode, lockMode, unlockMode` to the
   memoized `value` object AND to its dependency array.
   Preserve the existing `// eslint-disable-next-line react-refresh/only-export-components` comments.
   Do NOT touch combo/isOnFire logic.
   </action>
   <verify>
   <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx && grep -q "gradingMode" src/contexts/SightReadingSessionContext.jsx && echo OK</automated>
   </verify>
   <acceptance_criteria> - Context contains `gradingMode`, `isModeLocked`, `lockMode`, `unlockMode`, `setGradingMode`, and a `gradingModeRef` - `setGradingMode` body contains a no-op guard on `isModeLockedRef.current` - `setGradingMode` only accepts allowlisted `GRADING_MODES.PRACTICE`/`GRADING_MODES.TEST` - All new names appear in the `useMemo` `value` object and its dependency array (grep both) - Existing combo test still green: `npx vitest run ...SightReadingGame.combo.test.jsx` exits 0
   </acceptance_criteria>
   <done>Session context exposes a lockable, allowlist-guarded gradingMode with a ref mirror; combo/on-fire untouched.</done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2: Create useReviewDrill hook + test</name>
  <read_first>
    - src/contexts/SightReadingSessionContext.jsx (ref-mirror discipline — the pattern source)
    - src/components/games/sight-reading-game/utils/scoreCalculator.js (getDetailedBreakdown :103 — DO NOT re-derive; filter performanceResults directly)
    - src/components/games/sight-reading-game/__tests__/enharmonicMatching.test.js (noteToMidi semantics for pitch comparison)
    - src/hooks/useAudioEngine.js (playPianoSound signature :533)
  </read_first>
  <files>src/components/games/sight-reading-game/hooks/useReviewDrill.js, src/components/games/sight-reading-game/hooks/useReviewDrill.test.js</files>
  <behavior>
    - Given performanceResults, the drill's mistake list = only `missed` + `wrong_pitch` entries (early/late excluded, D-19).
    - `handlePitch(detectedPitch)` advances to the next mistake when detectedPitch matches the current target pitch (enharmonic-aware: C#4 matches Db4).
    - `handlePitch` with a non-matching pitch does NOT advance.
    - After the last mistake is matched, the drill reports done (isComplete true / currentTarget null).
    - The hook NEVER calls incrementCombo/resetCombo and issues no per-frame setState (advance = one setState per matched note).
    - Zero mistakes → mistake list empty, isComplete/idle immediately (drives D-20 "hide the Review button").
    - A `skip()` affordance advances past the current target without a match (escape hatch, RESEARCH.md A5).
  </behavior>
  <action>
Create NEW `hooks/useReviewDrill.js`. Model the ref-mirror discipline on `incrementCombo`
(mutate ref, then setState from the ref). Shape:

- Accept params: `{ performanceResults, patternNotes, playTargetPitch }` where `playTargetPitch` is a
  callback the game supplies (wrapping `audioEngine.playPianoSound(0.6, pitchName)`) — do NOT import
  useAudioEngine here; the hook stays pure/injectable for testing.
- Derive `mistakes` = `performanceResults.filter(r => r.timingStatus === "missed" || r.timingStatus === "wrong_pitch")`. For each mistake, resolve its TARGET pitch = the expected pitch of `patternNotes[r.noteIndex]` (the correct note the child must now play).
- State: `currentMistakeIndex` (+ `currentMistakeIndexRef` mirror), `isComplete`.
- `currentTarget` = mistakes[currentMistakeIndex] mapped to its target pitch + noteIndex, or null.
- `handlePitch(detectedPitch)`: compare detectedPitch to the current target via a MIDI-equivalence
  helper (reuse the enharmonic `noteToMidi` approach); on match, mutate `currentMistakeIndexRef` +1,
  setState, and if past the end set `isComplete`. No combo calls. No canScoreNow gate.
- `skip()`: same advance without a match check.
- `start()` / `reset()`: (re)initialize index to 0 and isComplete false.
- Optionally auto-audition the target on entering each note via `playTargetPitch` (Claude's discretion,
  RESEARCH.md Open Q3 recommends auto-play once on entry; if auto-playing in mic mode, the GAME will
  handle the mic timing — the hook just calls the injected callback).

Create `useReviewDrill.test.js` (NEW) with `renderHook`, covering every <behavior> case including the
enharmonic match and the "no combo calls" assertion (pass spies / assert the hook exposes no combo).
</action>
<verify>
<automated>npx vitest run src/components/games/sight-reading-game/hooks/useReviewDrill.test.js</automated>
</verify>
<acceptance_criteria> - `useReviewDrill.js` exports `useReviewDrill` and contains the filter `r.timingStatus === "missed" || r.timingStatus === "wrong_pitch"` - Hook contains a `currentMistakeIndexRef` and mutates it before setState (grep both `currentMistakeIndexRef.current` and a following setState) - Hook body contains NO reference to `incrementCombo`, `resetCombo`, or `canScoreNow` (grep returns 0) - Hook exposes `handlePitch`, `skip`, `start`/`reset`, `currentTarget`, `isComplete` - `useReviewDrill.test.js` has an enharmonic-match case (e.g. target C#4 advanced by Db4) and a zero-mistakes case - `npx vitest run ...useReviewDrill.test.js` exits 0
</acceptance_criteria>
<done>Review drill filters to wrong/missed, advances on enharmonic-correct pitch, never touches combo, has an escape hatch; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Create ReviewDrillPanel component</name>
  <read_first>
    - src/components/games/sight-reading-game/components/FeedbackSummary.jsx (full — glass card shell :114-118, GameActionButton usage :185-204, i18n pattern :50/:126-128)
    - src/components/games/shared/hud/GameActionButton.jsx (tones)
    - src/components/games/sight-reading-game/constants/feedbackPalette.js (FEEDBACK_COLORS)
    - CLAUDE.md (glassmorphism design system section)
  </read_first>
  <files>src/components/games/sight-reading-game/components/ReviewDrillPanel.jsx</files>
  <action>
Create NEW `components/ReviewDrillPanel.jsx` — the presentational review UI. Props:
`{ current, total, targetNote, instruction, onPlayTarget, onSkip, onExit, isComplete }`.

- Wrap in the FeedbackSummary glass card shell verbatim
  (`relative w-full rounded-2xl border border-white/20 bg-white/10 px-4 py-3 shadow-2xl backdrop-blur-md ...`).
- Show a title (`t("sightReading.review.title")`), progress (`t("sightReading.review.progress", { current, total, defaultValue: "{{current}} of {{total}}" })`), the instruction copy (`t("sightReading.review.instruction")`), and — while not complete — a lightweight text-style "Play it" button (`t("sightReading.review.playIt")`) calling `onPlayTarget` and a "Skip" text button (`t("sightReading.review.skip")`) calling `onSkip`. Use the secondary text-button styling (`text-xs font-medium text-white/80` + hover) for these, NOT `GameActionButton` (reserved for primary CTAs).
- When `isComplete`, show a done state (`t("sightReading.review.done")`) and a primary `GameActionButton tone="advance"` calling `onExit` (back to feedback).
- Also render an always-available exit affordance (text button) so a child can leave mid-drill.
- Reuse `FEEDBACK_COLORS` for any status coloring. All copy through `sightReading.review.*` keys —
  never inline English. `useTranslation("common")`.
  The panel is purely presentational — the drill state machine lives in `useReviewDrill` (Task 2); this
  component receives props and calls callbacks.
  </action>
  <verify>
  <automated>npx vitest run src/components/games/sight-reading-game && grep -q "export function ReviewDrillPanel\|export const ReviewDrillPanel" src/components/games/sight-reading-game/components/ReviewDrillPanel.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria> - `ReviewDrillPanel.jsx` exports `ReviewDrillPanel` - Uses `useTranslation("common")` and references keys under `sightReading.review.*` (grep `sightReading.review`) - Contains the glass card shell classes `rounded-2xl border border-white/20 bg-white/10` - Uses `GameActionButton` for the done/exit primary CTA and text-style buttons (not GameActionButton) for Play it/Skip - No inline hardcoded English user-facing strings (all via `t(...)`) - Existing sight-reading suite still green: `npx vitest run src/components/games/sight-reading-game` exits 0
  </acceptance_criteria>
  <done>ReviewDrillPanel renders target/progress/instruction with correct conventions and i18n; existing tests pass.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------------------------------- |
| localStorage → session mode     | The gradingMode default/allowlist is enforced here (context) and at the localStorage read site (02-07). |
| detected pitch → review advance | Review input is compared to a target pitch; no scoring, no persistence crosses this boundary.           |

## STRIDE Threat Register

| Threat ID | Category            | Component               | Disposition | Mitigation Plan                                                                                                                                           |
| --------- | ------------------- | ----------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering/Elevation | Review drill re-scoring | mitigate    | `useReviewDrill` performs no scoring and no persistence (D-17); it cannot move stars/XP. Enforced by construction — no score/service imports in the hook. |
| T-02-02   | Tampering           | gradingMode value       | mitigate    | `setGradingMode` allowlist-guards to PRACTICE/TEST only and no-ops when locked; junk values are ignored (default remains Test).                           |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game` green (new useReviewDrill test + existing suite).
- `npm run lint` clean on all four files.
</verification>

<success_criteria>

- Session context has lockable, allowlist-guarded gradingMode + ref mirror.
- useReviewDrill filters to wrong/missed, advances on enharmonic-correct pitch, never touches combo, has skip escape hatch.
- ReviewDrillPanel renders per conventions with full i18n.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-03-SUMMARY.md`
</output>
