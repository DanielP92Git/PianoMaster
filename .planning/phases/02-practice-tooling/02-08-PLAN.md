---
phase: 02-practice-tooling
plan: 08
type: execute
wave: 4
depends_on: [02, 06, 07]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/sight-reading-game/SightReadingGame.replay.test.jsx
autonomous: true
requirements: [PRAC-01, PRAC-02]
must_haves:
  truths:
    - "During DISPLAY the player can tap a replay button beside Start Playing to hear the exercise again, unlimited times, with no double-play (pending auto-play timeout cleared)"
    - "In FEEDBACK, one Compare button plays the child's reconstructed rendition, then the correct one, chained on the end-of-pattern signal, with a moving staff outline on each pass"
    - "If the child missed everything, comparison skips the empty 'yours' pass and plays only the correct one"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "handleReplayPreview + startComparison + playbackHighlightIndex state + wired compare button"
      contains: "handleReplayPreview"
  key_links:
    - from: "handleReplayPreview"
      to: "previewPlaybackTimeoutRef clear"
      via: "clearTimeout before play (D-11)"
      pattern: "previewPlaybackTimeoutRef"
    - from: "startComparison"
      to: "buildPlayedRendition + rhythmPlayback.play chained on onBeatChange(-1)"
      via: "yours-then-correct sequential passes"
      pattern: "buildPlayedRendition"
    - from: "comparison playback"
      to: "VexFlowStaffDisplay playbackHighlightIndex"
      via: "setPlaybackHighlightIndex(mapIndex)"
      pattern: "playbackHighlightIndex"
---

<objective>
Wire the two on-demand playback features into `SightReadingGame.jsx`: the DISPLAY-phase replay button
(PRAC-01, D-07/D-10/D-11 — byte-for-byte the auto-play, clearing the pending auto-play timeout to
avoid double-play) and the FEEDBACK-phase played-vs-correct comparison (PRAC-02, D-13/D-14 — build the
synthetic rendition, play yours-then-correct chained on the end-of-pattern signal, drive the additive
staff outline). Reuses `useRhythmPlayback` unchanged, `buildPlayedRendition` (02-02), the
`playbackHighlightIndex` prop (02-02), and the FeedbackSummary Compare button (02-06).

Purpose: Turn the read/display and feedback phases into on-demand listening tools. Second serial
game-file wave (after 02-07's mode anchor).
Output: modified `SightReadingGame.jsx` + new `SightReadingGame.replay.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md
@.planning/phases/02-practice-tooling/02-02-SUMMARY.md
@.planning/phases/02-practice-tooling/02-06-SUMMARY.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). Re-Grep exact lines before editing (3,995-line file). -->

previewPlaybackTimeoutRef: declared :583; scheduled :2250-2255; ALREADY cleared with this exact
3-line idiom at :2213-2216 and :2978-2981:

```javascript
if (previewPlaybackTimeoutRef.current) {
  clearTimeout(previewPlaybackTimeoutRef.current);
  previewPlaybackTimeoutRef.current = null;
}
```

Auto-play call it schedules:

```javascript
rhythmPlayback.play(pattern.notes, (index) => {
  setCurrentNoteIndex(index);
});
```

DISPLAY guidance region (:3710-3720): the Start Playing button lives here; replay is a sibling `<button>`.
useRhythmPlayback.play(pattern, onBeatChange): self-stops prior playback; fires `onBeatChange(-1)`
EXACTLY once at pattern end (:173-179) — the chaining signal. Consumes {type,startTime,endTime,frequency}.
buildPlayedRendition(patternNotes, performanceResults) (02-02): returns objects carrying `noteIndex`.
VexFlowStaffDisplay accepts `playbackHighlightIndex` (02-02) — pass game state to it.
FeedbackSummary accepts `onCompare` (02-06) — pass `startComparison`.
performanceResultsRef / currentPatternRef exist for synchronous reads.

Replay handler shape (RESEARCH.md Code Example 1):

```javascript
const handleReplayPreview = useCallback(() => {
  const pattern = currentPatternRef.current;
  if (!pattern || gamePhaseRef.current !== GAME_PHASES.DISPLAY) return;
  if (previewPlaybackTimeoutRef.current) {
    clearTimeout(previewPlaybackTimeoutRef.current);
    previewPlaybackTimeoutRef.current = null;
  }
  rhythmPlayback.play(pattern.notes, (index) => setCurrentNoteIndex(index));
}, [rhythmPlayback]);
```

Comparison chaining shape (RESEARCH.md Code Example 3) — yours then correct, chained on index===-1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replay button + handler (PRAC-01)</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep: `previewPlaybackTimeoutRef`, `beginPerformanceWithPattern`, the DISPLAY `guidanceRegion`/`startPlaying` button; targeted Read each)
    - src/components/games/sight-reading-game/hooks/useRhythmPlayback.js (play/stop contract)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
1. Add `handleReplayPreview` (copy the verified shape from the interfaces block): guard on
   `gamePhaseRef.current === GAME_PHASES.DISPLAY`; CLEAR `previewPlaybackTimeoutRef` first (D-11 — copy
   the existing 3-line clear idiom already at :2213/:2978), then call
   `rhythmPlayback.play(pattern.notes, (index) => setCurrentNoteIndex(index))` directly (no new
   setTimeout). This is byte-for-byte the auto-play (D-10) — no count-in, no click track.
2. Render the replay button as a sibling `<button>` inside the DISPLAY guidance region (:3710-3720),
   next to Start Playing. Use SECONDARY styling (not the green primary CTA) — the header
   `bg-white/10 hover:bg-white/20` family — to signal "secondary action". Label
   `t("sightReading.controls.replay")`. It renders only in DISPLAY (the region is already
   DISPLAY-gated) and disappears when performance begins.
Unlimited taps, both modes (D-08) — no cap logic.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game && grep -q "handleReplayPreview" src/components/games/sight-reading-game/SightReadingGame.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.jsx` defines `handleReplayPreview` that clears `previewPlaybackTimeoutRef` before calling `rhythmPlayback.play`
    - The DISPLAY guidance region renders a replay `<button>` labeled `t("sightReading.controls.replay")` with secondary (non-green) styling
    - Handler guards on `gamePhaseRef.current === GAME_PHASES.DISPLAY`
    - No replay cap logic added (grep confirms no counter)
    - Existing sight-reading suite green: `npx vitest run src/components/games/sight-reading-game` exits 0
  </acceptance_criteria>
  <done>DISPLAY-phase replay plays the exercise again, unlimited, with the double-play guard.</done>
</task>

<task type="auto">
  <name>Task 2: Comparison playback (PRAC-02) — startComparison + staff highlight + wire Compare button</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep: `<VexFlowStaffDisplay`, `<FeedbackSummary`, `performanceResultsRef`, `currentPatternRef`, `setCurrentNoteIndex`)
    - src/components/games/sight-reading-game/utils/comparisonPattern.js (buildPlayedRendition — 02-02)
    - src/components/games/sight-reading-game/hooks/useRhythmPlayback.js (onBeatChange(-1) end signal :173-179)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
1. Add state `const [playbackHighlightIndex, setPlaybackHighlightIndex] = useState(-1);` and pass it to
   `<VexFlowStaffDisplay playbackHighlightIndex={playbackHighlightIndex} ... />`.
2. Add `startComparison` (copy RESEARCH.md Code Example 3): build `yours = buildPlayedRendition(currentPatternRef.current.notes, performanceResultsRef.current)`. Play pass 1 (yours), then on the `onBeatChange(-1)` end signal chain pass 2 (the correct `pattern.notes`); on each non-`-1` index call `setPlaybackHighlightIndex(mapIndex(index))` — for the "yours" pass map synthetic index → `yours[i].noteIndex` (since missed notes are omitted, indices differ); for the correct pass the index IS the staff index. On pass-2 completion, `setPlaybackHighlightIndex(-1)`.
   - EDGE CASE: if `yours` is empty (everything missed), SKIP pass 1 and play only the correct pass.
   - Optionally insert a soft divider/label between passes (Claude's discretion; `sightReading.compare.yours`/`.correct` labels exist for a "now playing" indicator; a metronome tick via `audioEngine.createMetronomeClick` is available). Keep it simple if unsure.
3. Wire `<FeedbackSummary onCompare={startComparison} ... />` (the button + hierarchy already exist from 02-06).
4. Ensure audio context resume on the gesture (the Compare tap is a user gesture — follow the existing resume pattern used by `beginPerformanceWithPattern` :2989-2990 if the codebase requires it before playback).
Reuse `useRhythmPlayback` unmodified — do NOT compute durations and chain with bare setTimeout (drifts/breaks on stop()).
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game && grep -q "buildPlayedRendition" src/components/games/sight-reading-game/SightReadingGame.jsx && grep -q "playbackHighlightIndex" src/components/games/sight-reading-game/SightReadingGame.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.jsx` imports `buildPlayedRendition` from `./utils/comparisonPattern`
    - `startComparison` builds `yours`, plays it then the correct pattern chained on `index === -1`
    - An empty-`yours` branch skips pass 1 (grep for a length/empty check before pass 1)
    - `playbackHighlightIndex` state is created and passed to `<VexFlowStaffDisplay ...>`
    - `<FeedbackSummary` render includes `onCompare={startComparison}` (or equivalent)
    - The chaining uses the `-1` end signal, NOT a `setTimeout` duration computation (grep confirms no new setTimeout in startComparison)
    - Existing sight-reading suite green
  </acceptance_criteria>
  <done>Compare plays yours-then-correct with a moving outline; empty-yours plays only correct; no timer drift.</done>
</task>

<task type="auto">
  <name>Task 3: Replay double-play regression test</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx (mock harness to copy)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.replay.test.jsx</files>
  <action>
Create `SightReadingGame.replay.test.jsx` reusing the combo test's mock set, additionally mocking
`useRhythmPlayback` so `play`/`stop` are spies, and using `vi.useFakeTimers()`. Cases:
- After entering DISPLAY, tapping replay before the 500ms auto-play timer fires, then flushing timers,
  results in `rhythmPlayback.play` being called EXACTLY once (D-11 double-play guard — the replay
  cleared the pending timeout).
- The replay button is present in DISPLAY and absent outside DISPLAY (e.g. in FEEDBACK).
- Multiple replay taps each call `play` (unlimited, D-08).
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.replay.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.replay.test.jsx` exists and uses `vi.useFakeTimers()`
    - Contains an assertion that `play` is called exactly once after replay-tap-then-timer-flush
    - Contains an assertion the replay button is absent outside DISPLAY
    - `npx vitest run ...replay.test.jsx` exits 0
  </acceptance_criteria>
  <done>Double-play guard and DISPLAY-only visibility proven by green test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                              |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| in-memory results → comparison playback | Child's rendition reconstructed from in-memory note-name events; never captured, stored, or transmitted. |

## STRIDE Threat Register

| Threat ID | Category                       | Component           | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ------------------------------ | ------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-03   | Information Disclosure (COPPA) | Comparison playback | mitigate    | No raw-audio capture anywhere; `startComparison` consumes only `buildPlayedRendition`'s note-name reconstruction (D-12). No new persistence or PII. |
| T-02-04   | Spoofing                       | Client playback     | accept      | Playback is local Web Audio only; no server interaction.                                                                                            |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game` green (replay + existing).
- `npm run test:run` + `npm run lint` before phase gate.
- Manual (deferred): audible correctness of replay timbre and yours-then-correct comparison; moving outline visible.
</verification>

<success_criteria>

- DISPLAY replay: unlimited, no double-play, secondary styling.
- FEEDBACK comparison: yours-then-correct chained on end signal, moving outline, empty-yours handled.
- Regression test green.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-08-SUMMARY.md`
</output>
