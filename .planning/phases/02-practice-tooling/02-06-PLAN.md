---
phase: 02-practice-tooling
plan: 06
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - src/components/games/sight-reading-game/components/FeedbackSummary.jsx
  - src/components/games/sight-reading-game/components/SightReadingLayout.jsx
autonomous: true
requirements: [PRAC-02, PRAC-03, PRAC-04]
must_haves:
  truths:
    - "FeedbackSummary shows a secondary 'learn' row (Hear yours vs correct / Review mistakes) above the existing primary 'navigate' row (Try Again / Next) per D-23"
    - "On a clean run (zero wrong_pitch + missed), the Review button is HIDDEN (not disabled), collapsing the top row (D-20)"
    - "FeedbackSummary shows a 'practice run — not scored' notice when in practice mode, while accuracy bars/stars still render (D-06/D-01)"
    - "SightReadingLayout's phase contract includes 'review' and routes a REVIEW guidance region"
  artifacts:
    - path: "src/components/games/sight-reading-game/components/FeedbackSummary.jsx"
      provides: "two-row layout + onCompare/onReview/gradingMode props + not-scored notice"
      contains: "onCompare"
    - path: "src/components/games/sight-reading-game/components/SightReadingLayout.jsx"
      provides: "review phase contract + guidance routing"
      contains: "review"
  key_links:
    - from: "FeedbackSummary review row"
      to: "breakdown wrongPitch+missed"
      via: "hide when count === 0"
      pattern: "wrongPitch"
    - from: "FeedbackSummary calculateOverallScore"
      to: "gradingMode prop"
      via: "mode threaded into fallback score call"
      pattern: "gradingMode"
---

<objective>
Restructure the feedback panel into the owner-selected two-row layout (D-23 — the only non-delegated
UI decision this phase): a secondary "learn" row (`♫ Hear yours vs correct` · `↺ Review mistakes`)
above the existing primary "navigate" row (`Try Again` · `Next`), with the Review button hidden on a
clean run (D-20) and a "practice — not scored" notice under practice mode (D-06). Also extend
`SightReadingLayout`'s phase contract to include `review` so plan 02-09 can host the review panel.
Both components receive new props but are wired by the game in later waves; the buttons call props
(guarded with `&&`).

Purpose: Own the two shared presentational files (FeedbackSummary + Layout) in a single plan so the
game-file integration plans (02-07/08/09) only pass handlers, never re-edit these files.
Output: modified `FeedbackSummary.jsx` (two-row + props) and `SightReadingLayout.jsx` (review phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). -->

FeedbackSummary.jsx current primary-action block (:185-204):

```javascript
<div className="flex w-full max-w-xs gap-2.5">
  <GameActionButton tone="retry" onClick={onTryAgain} className="flex-1">
    {t("sightReading.tryAgain")}
  </GameActionButton>
  {showNextButton && onNextPattern && (
    <GameActionButton
      tone="advance"
      onClick={onNextPattern}
      disabled={nextButtonDisabled}
      className="flex-1"
    >
      {nextButtonLabel}
    </GameActionButton>
  )}
</div>
```

- `breakdown` object (wrongPitch/missed counts) already computed :76-79 via `getDetailedBreakdown`.
- breakdown chip typography scale for row-1 secondary buttons: `text-xs font-medium text-white/80` (:171).
- fallback score call at :67-69 uses `calculateOverallScore(...)` — thread `gradingMode`.
- i18n: `useTranslation("common")`, keys under `sightReading.*`.

SightReadingLayout.jsx contract comment (:1-34):

```
* Props (contract):
* - phase: "setup" | "display" | "count-in" | "performance" | "feedback"
```

Unhandled-phase fallback (:56-62): an unknown `phase="review"` falls into `showGuidanceOverlay`
(fixed-top overlay) by default — make REVIEW handling explicit instead of relying on the fallback.

New i18n keys available (authored in 02-04): `sightReading.controls.compare`,
`sightReading.controls.review`, `sightReading.summary.practiceNotScored`.
GRADING_MODES from `../constants/gradingModes` (values "practice"/"test").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure FeedbackSummary into the two-row D-23 layout + new props + notice</name>
  <read_first>
    - src/components/games/sight-reading-game/components/FeedbackSummary.jsx (full — current single-row block :185-204, breakdown :76-79, fallback score :67-69, i18n usage)
    - src/components/games/sight-reading-game/utils/scoreCalculator.js (calculateOverallScore now accepts mode — from 02-01)
    - src/components/games/sight-reading-game/constants/gradingModes.js (GRADING_MODES)
  </read_first>
  <files>src/components/games/sight-reading-game/components/FeedbackSummary.jsx</files>
  <action>
1. Add new props (all optional, backward-compatible): `onCompare`, `onReview`, `gradingMode = "test"`.
   Optionally `hasMistakes` — but PREFER deriving it from the already-computed `breakdown`:
   `const hasMistakes = (breakdown.wrongPitch + breakdown.missed) > 0;` (D-20 uses this).
2. Thread `gradingMode` into the fallback `calculateOverallScore(...)` call at :67-69 as its third arg
   so the displayed number matches the active grading mode.
3. Add a NEW secondary "row 1" ABOVE the existing action block, using lighter-weight styling than
   `GameActionButton` (model on the breakdown-chip scale `text-xs font-medium text-white/80` + hover),
   containing:
   - a Compare button (`♫` + `t("sightReading.controls.compare")`) rendered only when `onCompare` is provided; calls `onCompare`.
   - a Review button (`↺` + `t("sightReading.controls.review")`) rendered only when `onReview` is provided AND `hasMistakes` (D-20: hide, not disable, on a clean run); calls `onReview`.
   If neither renders, the row collapses to nothing (no empty container).
4. Keep the existing primary action block (:185-204) UNCHANGED as "row 2" (Try Again / Next).
5. Add the D-06 notice: when `gradingMode === GRADING_MODES.PRACTICE` (or `=== "practice"`), render
   `<p ...>{t("sightReading.summary.practiceNotScored")}</p>` near the stars/accuracy area. The
   accuracy bars and stars STILL render unconditionally (D-01: "still displays… simply do not
   persist") — the notice is additive text, not a conditional replacement.
Visual hierarchy: row 1 (learn) lighter, row 2 (navigate) bold CTAs — the "learn above / navigate
below" split D-23 specifies.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game && grep -q "onCompare" src/components/games/sight-reading-game/components/FeedbackSummary.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `FeedbackSummary.jsx` destructures `onCompare`, `onReview`, `gradingMode` props
    - Contains a `hasMistakes` derivation from `breakdown.wrongPitch + breakdown.missed`
    - Compare button gated on `onCompare &&`; Review button gated on `onReview && hasMistakes` (grep both guards)
    - `calculateOverallScore` fallback call includes a `gradingMode` argument
    - A `practiceNotScored` notice renders under `gradingMode === "practice"`/GRADING_MODES.PRACTICE, and the accuracy bars/stars render regardless (grep the notice guard + confirm stars markup unchanged)
    - The existing `onTryAgain`/`onNextPattern` GameActionButton block is preserved as row 2
    - `npx vitest run src/components/games/sight-reading-game` exits 0
  </acceptance_criteria>
  <done>Two-row D-23 layout with compare/review row (review hidden on clean run) + practice notice; primary row unchanged; existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add REVIEW phase to SightReadingLayout contract + guidance routing</name>
  <read_first>
    - src/components/games/sight-reading-game/components/SightReadingLayout.jsx (full — contract comment :1-34, guidance-overlay logic :56-62)
  </read_first>
  <files>src/components/games/sight-reading-game/components/SightReadingLayout.jsx</files>
  <action>
1. Extend the props contract comment (:1-34) to include `"review"`:
   `* - phase: "setup" | "display" | "count-in" | "performance" | "feedback" | "review"`
2. Make REVIEW guidance explicit rather than relying on the silent `showGuidanceOverlay` fallback
   (:56-62). Decide and implement: REVIEW should host its guidance region (the ReviewDrillPanel, passed
   in by the game via the existing `guidance` slot) in a dedicated, visible region — treat REVIEW like
   an active in-exercise phase (similar to how DISPLAY/PERFORMANCE render their guidance), not the
   fixed-top overlay used for incidental hints. Add a branch (e.g. `phase === "review"`) that renders
   the `guidance` content in the primary guidance region so the panel is prominent.
This file is "dumb" — it does NOT import ReviewDrillPanel; the game renders the panel and passes it as
`guidance`. This task only teaches the layout that `review` is a real phase with a guidance region.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game && grep -q "review" src/components/games/sight-reading-game/components/SightReadingLayout.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The layout's props contract comment lists `"review"` in the phase union
    - There is an explicit `phase === "review"` branch controlling guidance rendering (not relying on the fallback)
    - No import of ReviewDrillPanel added (layout stays presentational — grep confirms no such import)
    - `npx vitest run src/components/games/sight-reading-game` exits 0
  </acceptance_criteria>
  <done>Layout recognizes the review phase and gives it a real guidance region.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------------- |
| (none new) | Presentational components; new buttons invoke parent callbacks. No trust boundary crossed here. |

## STRIDE Threat Register

| Threat ID | Category            | Component                   | Disposition   | Mitigation Plan                                                                                                                           |
| --------- | ------------------- | --------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering/Elevation | Practice not-scored notice  | mitigate (UX) | The notice makes non-persistence legible (D-06); actual persistence gating is enforced in 02-05/02-07. No new write path introduced here. |
| T-02-04   | Spoofing            | Client-rendered mode notice | accept        | Notice is display-only; not a security control.                                                                                           |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game` green.
- Buttons render conditionally (compare when handler present; review only with mistakes).
- `npm run lint` clean.
</verification>

<success_criteria>

- FeedbackSummary: two-row D-23 layout, review hidden on clean run, practice notice with bars/stars still shown, mode threaded into score.
- SightReadingLayout recognizes `review` as a real phase with a guidance region.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-06-SUMMARY.md`
</output>
