---
phase: 02-practice-tooling
plan: 07
type: execute
wave: 3
depends_on: [01, 03, 04, 05, 06]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/sight-reading-game/SightReadingGame.practiceMode.test.jsx
  - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx
autonomous: true
requirements: [PRAC-03]
must_haves:
  truths:
    - "A labeled Practice/Test pill sits in the header control row; it is live before exercise #1 and greys out (locked) from the first COUNT_IN until a session boundary"
    - "Selecting Practice widens timing tolerance (via useTimingAnalysis mode) and grades pitch-only (via calculateOverallScore mode); the choice persists to localStorage and defaults to Test"
    - "A Practice run submits NO score (updateStudentScore skipped) and suppresses all VictoryScreen persistence (suppressPersistence passed through)"
    - "Mode unlocks only at returnToSetup / openSettingsModal / handleStartNewSession; the mid-exercise Try Again keeps the mode"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "mode pill, ref mirror, localStorage, lock/unlock, grading wiring, persistence gate"
      contains: "gradingMode"
  key_links:
    - from: "SightReadingGame updateStudentScore effect"
      to: "practice early-return"
      via: "gradingModeRef.current === PRACTICE → skipped"
      pattern: "gradingMode"
    - from: "SightReadingGame VictoryScreen render"
      to: "suppressPersistence prop"
      via: "suppressPersistence={isPracticeMode}"
      pattern: "suppressPersistence"
    - from: "useTimingAnalysis call"
      to: "mode arg"
      via: "useTimingAnalysis({ tempo, mode })"
      pattern: "mode"
---

<objective>
Wire Practice/Test grading mode (PRAC-03) into `SightReadingGame.jsx`: the labeled header pill (D-02),
the localStorage-persisted default-Test preference (D-03), the session lock at first COUNT_IN with
unlock at genuine boundaries (D-05), the ref-mirror for detection-callback reads (Pattern 1), and the
grading wiring — `useTimingAnalysis({ tempo, mode })`, `calculateOverallScore(..., mode)`, the
`updateStudentScore` practice early-return (the ONE in-file persistence path), and
`suppressPersistence={isPracticeMode}` passed to `VictoryScreen` (the other three paths gated in 02-05).
Also thread `gradingMode` to `FeedbackSummary` for its not-scored notice.

Purpose: This is the PRAC-03 anchor and the most invasive game-file change; it establishes the mode
lock every later feature respects. It must land before replay/compare (02-08) and review (02-09).
Output: modified `SightReadingGame.jsx` + new `SightReadingGame.practiceMode.test.jsx` + extended combo test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md
@.planning/phases/02-practice-tooling/02-01-SUMMARY.md
@.planning/phases/02-practice-tooling/02-03-SUMMARY.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). Use Grep to reconfirm exact line numbers before editing — the file is 3,995 lines and line drift is possible. -->

Session context (from 02-03) now exposes: `gradingMode`, `gradingModeRef`, `isModeLocked`,
`setGradingMode`, `lockMode`, `unlockMode` via `useSightReadingSession()`.
gradingModes.js (from 02-01): `GRADING_MODES` {PRACTICE:"practice", TEST:"test"}, `GRADING_MODE_STORAGE_KEY`="sightReadingGradingMode".

localStorage preference pattern to mirror (inputMode read :322-326, write effect :602-607):

```javascript
const [inputMode, setInputMode] = useState(() => {
  const stored = localStorage.getItem("sightReadingInputMode");
  return stored === "mic" ? "mic" : "keyboard"; // allowlist, safe default
});
useEffect(() => { localStorage.setItem("sightReadingInputMode", inputMode); ... }, [...]);
```

Header control row (:3606-3696) — icon-button family `rounded-lg p-1.5 sm:p-2` + active-bg ternary
(metronome uses `bg-fuchsia-500`); BPM pill shape `rounded-lg border border-white/20 bg-white/10 px-2 py-1 text-xs font-semibold text-white/90`; metronome button already uses `disabled={gamePhase === GAME_PHASES.COUNT_IN}`.

useTimingAnalysis call site: :689-691 (currently `useTimingAnalysis({ tempo })`).
Summary effect calculateOverallScore call: ~:1343.
updateStudentScore submit effect (:1420-1497) — the "not a student" early-exit idiom:

```javascript
if (!isStudent || !studentId) {
  setScoreSyncStatus("skipped");
  return;
}
```

Mode-unlock funnels (verified, Pitfall 8): `returnToSetup` (:2544), `openSettingsModal` (:2584),
`handleStartNewSession` (:2435 — also the target of Victory `onReset` :3495 and encouragement Try Again :3552).
Mode LOCK site: `beginPerformanceWithPattern`'s `setGamePhase(COUNT_IN)` (:3011, first per session).
Do NOT unlock in `replayPattern` (:2445 — mid-exercise Try Again keeps mode).
VictoryScreen render ~:3489-3495; encouragement screen ~:3540-3552.
combo test harness model: `SightReadingGame.combo.test.jsx`; `updateStudentScore` already mocked as a spy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mode state plumbing — pill, localStorage, ref, lock/unlock at boundaries</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep for: `useSightReadingSession`, `sightReadingInputMode`, the header region `headerRegion`, `beginPerformanceWithPattern`, `returnToSetup`, `openSettingsModal`, `handleStartNewSession`, `replayPattern`; then targeted Read of each)
    - src/contexts/SightReadingSessionContext.jsx (the gradingMode/lockMode/unlockMode API from 02-03)
    - src/components/games/sight-reading-game/constants/gradingModes.js
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
1. Consume the mode API from the session context: destructure `gradingMode, gradingModeRef, isModeLocked, setGradingMode, lockMode, unlockMode` from `useSightReadingSession()`.
2. Initialize mode from localStorage on mount (mirror the inputMode allowlist-with-default shape):
   read `GRADING_MODE_STORAGE_KEY`; if value === `GRADING_MODES.PRACTICE` call `setGradingMode(PRACTICE)`, else leave/ set Test (default per D-03). Add a write effect persisting `gradingMode` to `GRADING_MODE_STORAGE_KEY` whenever it changes (mirror :602-607). Junk/unknown values → Test (allowlist).
3. Add the header mode pill in the control row (:3606-3696), as a LABELED pill (D-02 — text, not icon-only): base shape from the BPM pill (`rounded-lg border border-white/20 ... px-2 py-1 text-xs font-semibold`) combined with an active-state color swap (like metronome's `bg-fuchsia-500`/`bg-white/10` ternary) indicating the current mode. Label text = `t("sightReading.controls.modePractice")` / `t("sightReading.controls.modeTest")`; aria-label `t("sightReading.controls.modeToggleLabel")`. onClick toggles `setGradingMode` between PRACTICE/TEST. `disabled={isModeLocked}` with the greyed `cursor-not-allowed opacity-60` treatment (D-05/D-06 legibility).
4. LOCK the mode: at the first `setGamePhase(GAME_PHASES.COUNT_IN)` in `beginPerformanceWithPattern` (:3011), call `lockMode()`. Because the pill reads `isModeLocked` (session-scoped, spans exercises), it stays locked for the whole session, not just during COUNT_IN.
5. UNLOCK at the three funnels: call `unlockMode()` at the top of `returnToSetup`, `openSettingsModal`, and `handleStartNewSession`. Do NOT unlock in `replayPattern` (mid-exercise Try Again keeps the mode, D-05).
Keep the pill's active/locked states visually legible per D-06.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx && grep -q "gradingMode" src/components/games/sight-reading-game/SightReadingGame.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.jsx` destructures `gradingMode`, `gradingModeRef`, `isModeLocked`, `setGradingMode`, `lockMode`, `unlockMode` from the session hook
    - Reads `GRADING_MODE_STORAGE_KEY` on mount with a Test default and has a write effect persisting it (grep the key twice)
    - Header renders a pill with `t("sightReading.controls.modePractice")`/`modeTest` and `disabled={isModeLocked}`
    - `lockMode()` is called at the first COUNT_IN transition in `beginPerformanceWithPattern`
    - `unlockMode()` is called in `returnToSetup`, `openSettingsModal`, and `handleStartNewSession`, and NOT in `replayPattern` (grep each)
    - `npx vitest run ...combo.test.jsx` exits 0
  </acceptance_criteria>
  <done>Labeled mode pill persists to localStorage (default Test), locks at first COUNT_IN, unlocks only at session boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Grading wiring + persistence gate (timing mode, score mode, updateStudentScore skip, suppressPersistence, FeedbackSummary notice)</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep: `useTimingAnalysis(`, `calculateOverallScore(`, the `updateStudentScore` submit effect, `<VictoryScreen`, `<FeedbackSummary`)
    - src/hooks/useVictoryState.js + src/components/games/VictoryScreen.jsx (suppressPersistence prop from 02-05)
    - src/components/games/sight-reading-game/components/FeedbackSummary.jsx (gradingMode prop from 02-06)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
Define `const isPracticeMode = gradingMode === GRADING_MODES.PRACTICE;` near the mode consumption.
1. Timing: change the `useTimingAnalysis({ tempo })` call (:689-691) to `useTimingAnalysis({ tempo, mode: gradingMode })`. Windows rebuild per pattern load; D-05 lock means mode is stable through an exercise.
2. Score: pass mode into `calculateOverallScore(...)` at the summary effect (~:1343) — add `gradingMode` as the third argument.
3. Persistence gate (the ONE in-file path): in the `updateStudentScore` submit effect (:1420-1497), add an early-return immediately AFTER the `!isStudent || !studentId` check, using the SAME "skipped" idiom: `if (gradingModeRef.current === GRADING_MODES.PRACTICE) { setScoreSyncStatus("skipped"); return; }`. Use the REF (not state) — this effect/closure needs the synchronous value. Add `gradingMode` to the effect deps so it re-evaluates if mode changed at a boundary.
4. VictoryScreen: pass `suppressPersistence={isPracticeMode}` where `<VictoryScreen ... />` is rendered (the other three D-01 paths are gated inside it via 02-05).
5. FeedbackSummary: pass `gradingMode={gradingMode}` so its not-scored notice (02-06) shows in Practice.
6. Miss-sweep fallback (RESEARCH.md Pitfall 5): the miss-finalization sweep uses raw `NOTE_LATE_MS` as a fallback in two spots — `missToleranceMs` (~:2065, used only when a timing window is missing) and the completion threshold (~:2162-2168). The primary miss path reads `windowEnd` from the mode-scaled windows (so Practice leniency already flows through), but scale the COMPLETION-THRESHOLD fallback by `PRACTICE_TIMING.toleranceMultiplier` when `gradingModeRef.current === GRADING_MODES.PRACTICE` — otherwise the last note's widened Practice window can outlive the completion check and finalize as a miss incorrectly. The `missToleranceMs` fallback may be scaled likewise or left as-is (documented accepted, since the primary path dominates).
7. Penalty affordance (conscious decision — CONTEXT.md "Claude's Discretion" / RESEARCH.md Pitfall 9): in Practice mode, suppress BOTH the anti-cheat penalty modal (do not trigger `showPenaltyModal` when `gradingModeRef.current === GRADING_MODES.PRACTICE`) AND the `penaltyPoints` display in the summary — a punishment affordance contradicts an explicitly unscored, psychologically-safe mode ("safe mode" coherence). Keep the anti-cheat TRACKING itself (`guessPenaltyRef` accumulation) running unchanged in both modes — zero cost, avoids divergent code paths.
Do NOT alter Test-mode behavior — with Test selected every path (including penalty modal + display) runs exactly as today.
  </action>
  <verify>
    <automated>grep -q "mode: gradingMode" src/components/games/sight-reading-game/SightReadingGame.jsx && grep -q "suppressPersistence=" src/components/games/sight-reading-game/SightReadingGame.jsx && grep -q "GRADING_MODES.PRACTICE" src/components/games/sight-reading-game/SightReadingGame.jsx && npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `useTimingAnalysis({ tempo, mode: gradingMode })` present (grep `mode: gradingMode`)
    - `calculateOverallScore` call includes `gradingMode` argument
    - The `updateStudentScore` effect contains `gradingModeRef.current === GRADING_MODES.PRACTICE` guard with `setScoreSyncStatus("skipped")` early-return
    - `<VictoryScreen` render includes `suppressPersistence={isPracticeMode}` (or equivalent boolean)
    - `<FeedbackSummary` render includes `gradingMode={gradingMode}`
    - Structural greps above pass and the existing combo test stays green; full behavioral coverage (Practice-skips-submit etc.) lands in Task 3 (`SightReadingGame.practiceMode.test.jsx`)
    - The penalty modal trigger and `penaltyPoints` display are guarded on Practice mode (grep shows a `GRADING_MODES.PRACTICE`/`gradingModeRef` guard at the `showPenaltyModal` trigger site)
  </acceptance_criteria>
  <done>Practice widens timing + grades pitch-only + persists nothing (all 4 paths closed); Test unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Author practiceMode test + extend combo test (D-04 side effect)</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx (full mock harness — stateful session-context mock, updateStudentScore spy, capturedOnNoteEvent)
    - src/components/games/sight-reading-game/SightReadingGame.practiceMode.test.jsx (does not exist yet — create)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.practiceMode.test.jsx, src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx</files>
  <action>
Create `SightReadingGame.practiceMode.test.jsx` reusing the combo test's exact mock set (the stateful
`useSightReadingSession` mock — extend it to expose `gradingMode`/`setGradingMode`/`lockMode`/
`unlockMode`/`isModeLocked`/`gradingModeRef`; `updateStudentScore` already mocked as a spy). Cover:
- Practice mode: after reaching FEEDBACK, `updateStudentScore` is NOT called (assert spy not called).
- Test mode (default): `updateStudentScore` IS called at FEEDBACK (regression guard).
- Mode locks at first COUNT_IN: after the game enters COUNT_IN, `isModeLocked` is true / the pill is disabled.
- Mode persists to localStorage: toggling calls setItem with `sightReadingGradingMode`.
- `unlockMode` is invoked on `handleStartNewSession`/`returnToSetup` path; NOT on the mid-exercise Try Again.

Extend `SightReadingGame.combo.test.jsx` with one case: under a Practice-mode render, the wider timing
windows delay miss finalization → the live combo break fires later (or not within the test's note
count) than under Test mode — the D-04 side-effect regression guard from the Test Map.
</action>
<verify>
<automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.practiceMode.test.jsx src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx</automated>
</verify>
<acceptance_criteria> - `SightReadingGame.practiceMode.test.jsx` exists with a case asserting `updateStudentScore` NOT called in Practice and a case asserting it IS called in Test - Test file asserts localStorage persistence with key `sightReadingGradingMode` - `SightReadingGame.combo.test.jsx` has a new Practice-mode delayed-combo-break case - Both files green: `npx vitest run` on both exits 0
</acceptance_criteria>
<done>Practice/Test persistence divergence, lock, localStorage, and delayed-combo-break all covered by green tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------ |
| localStorage → grading mode        | Persisted mode is read here; allowlist-validated (Test default) at the read site.    |
| client → Supabase (students_score) | Practice runs must not cross; the updateStudentScore effect early-returns "skipped". |

## STRIDE Threat Register

| Threat ID | Category            | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                                                   |
| --------- | ------------------- | -------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering/Elevation | XP/score farming via lenient Practice mode         | mitigate    | The updateStudentScore effect early-returns in Practice (reads gradingModeRef); VictoryScreen `suppressPersistence` closes streak/trail/free-play/daily (02-05). All four D-01 paths gated. Server RLS unchanged. |
| T-02-02   | Tampering           | localStorage `sightReadingGradingMode` set to junk | mitigate    | Read site allowlists to PRACTICE/TEST, defaults Test on anything else (ASVS V5). A tampered value can at most force an unscored Practice run — no elevation.                                                      |
| T-02-04   | Spoofing            | Client-only mode gate                              | accept      | Documented — product-integrity gate, not a security boundary; `students_score` RLS is ground truth.                                                                                                               |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game` green (practiceMode + combo + existing).
- `npm run test:run` full suite green + `npm run lint` clean before phase gate.
- Manual (device, deferred consistent with prior milestones): Practice run does not move trail/streak/XP; pill greys at count-in.
</verification>

<success_criteria>

- Labeled mode pill, default Test, localStorage-persisted, locked during session.
- Practice widens tolerance, grades pitch-only, persists nothing (all four paths); Test unchanged.
- Tests green.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-07-SUMMARY.md`
</output>
