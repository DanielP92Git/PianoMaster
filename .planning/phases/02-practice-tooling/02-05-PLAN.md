---
phase: 02-practice-tooling
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useVictoryState.js
  - src/components/games/VictoryScreen.jsx
autonomous: true
requirements: [PRAC-03]
must_haves:
  truths:
    - "When suppressPersistence is true, useVictoryState awards no XP (trail OR free-play), advances no trail progress, updates no streak, and completes no daily challenge"
    - "VictoryScreen forwards suppressPersistence to the hook and shows a 'practice run — not scored' notice when suppressed"
    - "Default behavior (suppressPersistence=false / omitted) is byte-for-byte unchanged for every other game that renders VictoryScreen"
  artifacts:
    - path: "src/hooks/useVictoryState.js"
      provides: "suppressPersistence-gated persistence"
      contains: "suppressPersistence"
    - path: "src/components/games/VictoryScreen.jsx"
      provides: "suppressPersistence prop forwarding + not-scored notice"
      contains: "suppressPersistence"
  key_links:
    - from: "VictoryScreen"
      to: "useVictoryState"
      via: "suppressPersistence prop forwarded 1:1"
      pattern: "suppressPersistence"
    - from: "useVictoryState persistence sites"
      to: "early guards"
      via: "if (suppressPersistence) return"
      pattern: "suppressPersistence"
---

<objective>
Close the three "hidden" D-01 persistence paths that live OUTSIDE `SightReadingGame.jsx`: the streak
update, the trail-progress + trail-XP block, and the free-play XP block — all inside the shared
`useVictoryState` hook — plus the daily-challenge completion inside `VictoryScreen`. Add ONE optional
`suppressPersistence` prop (default false, fully backward-compatible) threaded from `VictoryScreen`
into `useVictoryState`, and gate every persistence site on it. Also render the D-06 "practice run —
not scored" notice when suppressed. Plan 02-07 passes `suppressPersistence={isPracticeMode}` from the
game.

Purpose: RESEARCH.md Pitfall 2 — gating only the per-exercise `updateStudentScore` (done in 02-07)
leaves Practice runs still writing XP/streak/trail/daily-challenge. This plan closes those four.
Output: modified `useVictoryState.js` and `VictoryScreen.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). -->

useVictoryState signature (:92-105):

```javascript
export function useVictoryState({
  score, totalPossibleScore, onReset, timedMode, timeRemaining, initialTime, onExit,
  nodeId = null, exerciseIndex = null, totalExercises = null, exerciseType = null,
  onNextExercise = null,
}) {
```

Streak-update path (:328-342):

```javascript
useEffect(() => {
  if (scorePercentage >= 80 && !hasCalledStreakUpdate.current) {
    hasCalledStreakUpdate.current = true;
    updateStreakWithAchievements.mutate(undefined, {
      onSuccess: ({ newStreak }) => {
        /* toast */
      },
    });
  }
}, [scorePercentage, updateStreakWithAchievements, t]);
```

Trail progress + awardXP block: ~:387-493 (updateExerciseProgress/updateNodeProgress + awardXP).
Free-play XP path (:500-519):

```javascript
} else {
  if (user?.id && scorePercentage > 0) {
    const freePlayXP = calculateFreePlayXP(scorePercentage, comebackActive ? 2 : 1);
    if (freePlayXP > 0) { const xpResult = await awardXP(user.id, freePlayXP); ... }
  }
  setIsProcessingTrail(false);
```

VictoryScreen.jsx (:15-81) forwards every prop 1:1 into useVictoryState; imports
`completeDailyChallenge` from `../../services/dailyChallengeService` (:12), invoked inside a
`challengeMode` guard (:83-90). Imports `Trophy`, `Zap` from lucide-react for celebration markup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the three useVictoryState persistence sites on suppressPersistence</name>
  <read_first>
    - src/hooks/useVictoryState.js (full — need the exact signature :92, streak effect :328, trail block ~:387-493, free-play XP :500-519, and every early-return style already used)
  </read_first>
  <files>src/hooks/useVictoryState.js</files>
  <action>
1. Add `suppressPersistence = false` to the destructured hook signature (matching the existing
   `= null`/default-param style used for every optional field).
2. Streak effect (:328): add `if (suppressPersistence) return;` as the FIRST line inside the effect
   (before the `scorePercentage >= 80` check). Add `suppressPersistence` to the effect deps.
3. Trail progress + awardXP block (~:387-493): guard the trail-progress writes and `awardXP` so that
   when `suppressPersistence` is true, NONE of `updateExerciseProgress`/`updateNodeProgress`/`awardXP`
   run. Keep any non-persisting display/state cleanup (e.g. `setIsProcessingTrail(false)`) running so
   the UI still settles — only the persistence calls are skipped.
4. Free-play XP block (:500-519): wrap the `awardXP` call in `if (!suppressPersistence) { ... }` (still
   run `setIsProcessingTrail(false)`).
Every guard is additive; with the default `suppressPersistence=false`, all existing behavior is
byte-for-byte unchanged for every other game using this hook.
  </action>
  <verify>
    <automated>npx vitest run src/hooks && grep -c "suppressPersistence" src/hooks/useVictoryState.js</automated>
  </verify>
  <acceptance_criteria>
    - `useVictoryState.js` signature contains `suppressPersistence = false`
    - The streak `useEffect` contains `if (suppressPersistence) return` and lists `suppressPersistence` in its deps
    - The trail-progress/awardXP block is guarded so `awardXP`/`updateExerciseProgress`/`updateNodeProgress` do not run when suppressed (grep shows `suppressPersistence` guarding these)
    - The free-play `awardXP` is wrapped in a `!suppressPersistence` guard
    - `grep -c "suppressPersistence" src/hooks/useVictoryState.js` returns ≥ 4
    - `npx vitest run src/hooks` exits 0 (existing hook tests green; no dedicated useVictoryState/VictoryScreen test exists today — behavioral coverage of the suppression flag lands in 02-07 Task 3)
  </acceptance_criteria>
  <done>All three in-hook persistence paths are gated; default path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Forward suppressPersistence through VictoryScreen + gate daily challenge + add not-scored notice</name>
  <read_first>
    - src/components/games/VictoryScreen.jsx (:1-90 — prop destructure, forwarding into useVictoryState, completeDailyChallenge import + challengeMode effect, Trophy/Zap celebration markup)
    - src/locales/en/common.json (confirm `sightReading.summary.practiceNotScored` — authored in 02-04; reference the key even if 02-04 lands same wave, i18next falls back to key name gracefully)
  </read_first>
  <files>src/components/games/VictoryScreen.jsx</files>
  <action>
1. Add `suppressPersistence = false` to VictoryScreen's props destructure (:15-81) and forward it 1:1
   into the `useVictoryState({ ... })` call.
2. Gate the daily-challenge completion: the `useEffect` that calls `completeDailyChallenge` (inside the
   `challengeMode` guard, :83-90) must additionally short-circuit when `suppressPersistence` is true —
   add `if (suppressPersistence) return;` at the top of that effect and add `suppressPersistence` to
   its deps. This is the FOURTH D-01 persistence path.
3. Render a D-06 "practice run — not scored" notice near the existing Trophy/Zap celebration markup,
   shown only when `suppressPersistence` is true:
   `{suppressPersistence && (<p className="text-sm text-white/70">{t("sightReading.summary.practiceNotScored")}</p>)}`
   (use the screen's existing `t` / translation setup; if VictoryScreen doesn't already use
   `useTranslation`, add `const { t } = useTranslation("common");`).
Keep every default-path render unchanged for other games (notice hidden when suppressPersistence false).
  </action>
  <verify>
    <automated>npx vitest run src/components/games && grep -q "suppressPersistence" src/components/games/VictoryScreen.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `VictoryScreen.jsx` destructures `suppressPersistence = false` and forwards it into `useVictoryState(...)`
    - The `completeDailyChallenge` effect contains `if (suppressPersistence) return` and lists `suppressPersistence` in deps
    - A notice referencing `sightReading.summary.practiceNotScored` renders only when `suppressPersistence` is true (grep the key + a `suppressPersistence &&` guard)
    - Existing games test suite still green: `npx vitest run src/components/games` exits 0
  </acceptance_criteria>
  <done>VictoryScreen forwards the flag, gates the daily challenge, and shows the not-scored notice under suppression.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                                    |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| client → Supabase (XP/streak/trail/daily) | Practice runs must not cross this boundary; suppressPersistence blocks the client-side calls. Server RLS remains ground truth. |

## STRIDE Threat Register

| Threat ID | Category            | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                      |
| --------- | ------------------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-02-01   | Tampering/Elevation | XP/score farming via Practice reaching persistence | mitigate    | Gate all four out-of-game persistence paths (streak, trail XP/progress, free-play XP, daily challenge) behind `suppressPersistence`. Combined with 02-07's `updateStudentScore` gate, this closes all four D-01 paths (RESEARCH.md Pitfall 2). Server-side `students_score` RLS is unchanged and remains the backstop for Test mode. |

</threat_model>

<verification>
- `npx vitest run src/components/games src/hooks` green.
- Default path (suppressPersistence false) unchanged for other games — confirm existing VictoryScreen/useVictoryState tests still pass.
- `npm run lint` clean.
</verification>

<success_criteria>

- suppressPersistence gates streak, trail XP/progress, free-play XP, and daily-challenge completion.
- VictoryScreen forwards the prop and shows the not-scored notice under suppression.
- Backward-compatible default for all other games.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-05-SUMMARY.md`
</output>
