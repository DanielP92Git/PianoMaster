---
phase: 02-practice-tooling
plan: 09
type: execute
wave: 5
depends_on: [03, 06, 08]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/sight-reading-game/SightReadingGame.review.test.jsx
autonomous: true
requirements: [PRAC-04]
must_haves:
  truths:
    - "After an exercise with mistakes, a Review button enters a REVIEW phase that steps through only wrong_pitch + missed notes as an active play-it drill; a clean run hides the button"
    - "Review input routes to the drill BEFORE canScoreNow (both mic and keyboard paths), advances on a matching pitch, and never scores, never re-scores the attempt, never touches combo/on-fire"
    - "GAME_PHASES.REVIEW is added to the session-timeout activePhases, the keyboard band, and the audio-interruption phase lists so the drill does not break child-safety timeout or lose keyboard input"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "REVIEW phase, review input routing, useReviewDrill wiring, ReviewDrillPanel render, mic lifecycle"
      contains: "REVIEW"
  key_links:
    - from: "handleNoteEvent / handleKeyboardNoteInput"
      to: "reviewDrill.handlePitch"
      via: "branch on gamePhaseRef===REVIEW before canScoreNow"
      pattern: "GAME_PHASES.REVIEW"
    - from: "FeedbackSummary onReview"
      to: "enter REVIEW phase"
      via: "setGamePhase(REVIEW) + mic startListeningSync"
      pattern: "onReview"
---

<objective>
Wire the Review-mistakes drill (PRAC-04) into `SightReadingGame.jsx`: add `GAME_PHASES.REVIEW` and
register it in the three phase lists (session-timeout activePhases, keyboard band, audio-interruption)
so the drill doesn't break child-safety timeout or drop keyboard input (Pitfall 4); route REVIEW input
to `useReviewDrill.handlePitch` BEFORE the `canScoreNow` guards in both the mic and keyboard paths
(Pitfall 3 / D-22); manage the mic lifecycle on enter/exit; render `ReviewDrillPanel` in the review
guidance region; and wire the FeedbackSummary Review button (02-06) to enter the phase. The drill is
active recall (D-16), unscored (D-17), never re-scores, and never touches combo/on-fire (D-18).

Purpose: Final serial game-file wave. Closes PRAC-04 and completes the phase.
Output: modified `SightReadingGame.jsx` + new `SightReadingGame.review.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-tooling/02-CONTEXT.md
@.planning/phases/02-practice-tooling/02-RESEARCH.md
@.planning/phases/02-practice-tooling/02-PATTERNS.md
@.planning/phases/02-practice-tooling/02-03-SUMMARY.md
@.planning/phases/02-practice-tooling/02-06-SUMMARY.md

<interfaces>
<!-- Verified anchors (this worktree, 2026-07-10). Re-Grep exact lines before editing (3,995-line file). -->

GAME_PHASES (:80-86): SETUP/COUNT_IN/DISPLAY/PERFORMANCE/FEEDBACK. Add `REVIEW: "review"`.
Three phase lists to update (Pitfall 4):

1. Session-timeout activePhases (:261-276): `[COUNT_IN, DISPLAY, PERFORMANCE]` → add REVIEW (else resumeTimer runs mid-drill and child-safety timeout can fire).
2. showPlayableKeyboardBand (:3748-3752): `(DISPLAY || COUNT_IN || PERFORMANCE) && shouldShowKeyboard` → add `|| REVIEW` (else keyboard users have no input surface).
3. Audio-interruption pause effect (~:3369-3376, excludes only SETUP/FEEDBACK): treat REVIEW as active.

Input routing choke points (branch BEFORE canScoreNow):

- mic: `handleNoteEvent` (:932)
- keyboard: `handleKeyboardNoteInput` (:1976-2029) + PC-keydown listener (:2019)
  canScoreNow (:1042-1051) returns false for any phase except COUNT_IN/PERFORMANCE — it silently eats REVIEW input.

useReviewDrill (02-03): `{ handlePitch, skip, start/reset, currentTarget, isComplete }`, params
`{ performanceResults, patternNotes, playTargetPitch }`. Never calls combo.
ReviewDrillPanel (02-03): props `{ current, total, targetNote, instruction, onPlayTarget, onSkip, onExit, isComplete }`.
FeedbackSummary `onReview` (02-06): rendered only when there are mistakes (D-20 hide-on-clean).
SightReadingLayout (02-06): `review` phase renders `guidance` in a prominent region — pass ReviewDrillPanel as guidance.
audioEngine.playPianoSound(0.6, pitchName) (:1979 usage) — for target audition; startListeningSync/stopListeningSync manage mic.
performanceResultsRef / currentPatternRef for synchronous reads.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GAME_PHASES.REVIEW + register in the three phase lists</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep: `GAME_PHASES = {`, `activePhases`, `showPlayableKeyboardBand`, the audio-interruption effect; targeted Read each)
    - src/components/games/sight-reading-game/components/SightReadingLayout.jsx (confirm `review` phase handling from 02-06)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
1. Add `REVIEW: "review"` to `GAME_PHASES` (:80-86).
2. Session-timeout `activePhases` (:261-276): add `GAME_PHASES.REVIEW` to the array so `pauseTimer()`
   runs during the drill (child-safety timeout must NOT fire mid-drill).
3. `showPlayableKeyboardBand` (:3748-3752): add `|| gamePhase === GAME_PHASES.REVIEW` so keyboard users
   have an input surface in review.
4. Audio-interruption pause effect (~:3369-3376): include REVIEW as an active phase (same treatment as
   PERFORMANCE) so an interruption pauses the drill correctly.
No behavior change to existing phases.
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game && grep -c "GAME_PHASES.REVIEW\|REVIEW:" src/components/games/sight-reading-game/SightReadingGame.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `GAME_PHASES` contains `REVIEW: "review"`
    - The session-timeout `activePhases` array includes `GAME_PHASES.REVIEW`
    - `showPlayableKeyboardBand` includes a `gamePhase === GAME_PHASES.REVIEW` disjunct
    - The audio-interruption effect treats REVIEW as active (grep shows REVIEW referenced there)
    - Existing sight-reading suite green
  </acceptance_criteria>
  <done>REVIEW is a first-class phase registered in all three lists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useReviewDrill, input routing, mic lifecycle, and ReviewDrillPanel render</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx (Grep: `handleNoteEvent`, `handleKeyboardNoteInput`, PC keydown listener, `canScoreNow`, `startListeningSync`, `stopListeningSync`, `<FeedbackSummary`, the guidance region wiring to SightReadingLayout)
    - src/components/games/sight-reading-game/hooks/useReviewDrill.js (API from 02-03)
    - src/components/games/sight-reading-game/components/ReviewDrillPanel.jsx (props from 02-03)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
1. Instantiate the drill: `const reviewDrill = useReviewDrill({ performanceResults, patternNotes: currentPattern?.notes, playTargetPitch: (pitch) => audioEngine.playPianoSound(0.6, pitch) });` (adjust to the hook's exact param names from 02-03).
2. Enter REVIEW: wire `<FeedbackSummary onReview={handleEnterReview} ... />`. `handleEnterReview`:
   `reviewDrill.start()`, `setGamePhase(GAME_PHASES.REVIEW)`; in mic input mode, call `startListeningSync()`
   (the button tap is the required user gesture; resume audio context if needed). To avoid the target
   audition being picked up as mic input (same phantom-note concern that dropped the Phase 01 fire
   sound), gate the drill handler for ~500ms after an auto-audition OR delay the auto-audition until
   after mic is listening (Claude's discretion per RESEARCH.md Open Q3).
3. Route REVIEW input BEFORE `canScoreNow` (Pitfall 3 / D-22): at the TOP of `handleNoteEvent` (mic,
   :932) and `handleKeyboardNoteInput` (:1976) and the PC-keydown listener (:2019), add:
   `if (gamePhaseRef.current === GAME_PHASES.REVIEW) { reviewDrill.handlePitch(<detectedPitch>); return; }`
   before any `canScoreNow`/`handleNoteDetected` call. Use the correct pitch field for each path.
4. Exit REVIEW: `handleExitReview` → in mic mode `stopListeningSync()`, then `setGamePhase(GAME_PHASES.FEEDBACK)`
   (return to the feedback panel; the recorded score is untouched — D-17). Wire it to ReviewDrillPanel's
   `onExit` and also trigger it automatically when `reviewDrill.isComplete`.
5. Render `ReviewDrillPanel` as the review guidance content, passed into `SightReadingLayout`'s guidance
   slot when `gamePhase === GAME_PHASES.REVIEW`, mapping drill state to props
   (`current`, `total`, `targetNote`, `instruction`, `onPlayTarget`, `onSkip`, `onExit`, `isComplete`).
6. D-18 guarantee: the review path must NEVER call `incrementCombo`/`resetCombo` and must NEVER write a
   score. The drill hook already avoids combo; ensure the routing branch `return`s before any scoring
   handler so no score/combo side effects occur.
  </action>
  <verify>
    <automated>grep -q "GAME_PHASES.REVIEW" src/components/games/sight-reading-game/SightReadingGame.jsx && grep -q "reviewDrill.handlePitch" src/components/games/sight-reading-game/SightReadingGame.jsx && grep -q "onReview=" src/components/games/sight-reading-game/SightReadingGame.jsx && npx vitest run src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.jsx` instantiates `useReviewDrill(...)` and imports `ReviewDrillPanel`
    - `handleNoteEvent`, `handleKeyboardNoteInput`, and the PC-keydown listener each contain a `gamePhaseRef.current === GAME_PHASES.REVIEW` branch that calls `reviewDrill.handlePitch(...)` and `return`s BEFORE `canScoreNow`/`handleNoteDetected`
    - `<FeedbackSummary` render includes `onReview={...}` entering the REVIEW phase
    - Enter/exit manage mic (`startListeningSync`/`stopListeningSync`) in mic mode
    - `ReviewDrillPanel` is rendered for the review phase via the layout guidance slot
    - No `incrementCombo`/`resetCombo` call reachable from the REVIEW input branch (grep the branch body)
    - Structural greps above pass and the existing combo test stays green; full behavioral coverage lands in Task 3 (`SightReadingGame.review.test.jsx`)
  </acceptance_criteria>
  <done>Review drill enters from the feedback panel, routes input before canScoreNow, drills wrong/missed notes, never scores or touches combo, manages mic, and exits back to feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Review drill component/integration test</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.combo.test.jsx (mock harness)
    - src/components/games/sight-reading-game/hooks/useReviewDrill.test.js (drill unit behavior from 02-03)
  </read_first>
  <files>src/components/games/sight-reading-game/SightReadingGame.review.test.jsx</files>
  <action>
Create `SightReadingGame.review.test.jsx` reusing the combo test mock harness. Cases:
- With mistakes present in performanceResults, the Review button is shown; clicking it enters REVIEW
  (renders ReviewDrillPanel).
- On a clean run (zero wrong_pitch + missed), the Review button is NOT rendered (D-20).
- In REVIEW, a captured note event routes to the drill (advances) and does NOT call the combo spies
  (`incrementCombo`/`resetCombo`) and does NOT call `updateStudentScore` (D-17/D-18) — assert all three
  spies not called during review input.
- REVIEW is included in the session-timeout active set (assert `pauseTimer` invoked while in REVIEW,
  reusing the timeout mock approach if present; otherwise assert the phase list contains REVIEW).
  </action>
  <verify>
    <automated>npx vitest run src/components/games/sight-reading-game/SightReadingGame.review.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `SightReadingGame.review.test.jsx` exists
    - Contains a clean-run case asserting the Review button is absent (D-20)
    - Contains a case asserting review input does NOT call `incrementCombo`/`resetCombo`/`updateStudentScore`
    - `npx vitest run ...review.test.jsx` exits 0
  </acceptance_criteria>
  <done>Review entry, clean-run hide, and no-score/no-combo guarantees proven by green test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                         |
| ----------------------------------- | ----------------------------------------------------------------------------------- |
| detected pitch → review advance     | Review input is compared to a target pitch locally; no scoring/persistence crosses. |
| REVIEW phase → child-safety timeout | REVIEW must keep the session-timeout paused (pauseTimer) like other active phases.  |

## STRIDE Threat Register

| Threat ID | Category                         | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                         |
| --------- | -------------------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering/Elevation              | Review re-scoring to farm stars  | mitigate    | Review routes before `canScoreNow`/`handleNoteDetected` and calls no score writer (D-17); the recorded score is final on entering feedback. Test asserts `updateStudentScore` not called during review. |
| T-02-06   | Denial of Service (child-safety) | Session-timeout firing mid-drill | mitigate    | REVIEW added to `activePhases` so `pauseTimer()` holds; audio-interruption effect treats REVIEW as active.                                                                                              |
| T-02-04   | Spoofing                         | Client review drill              | accept      | Local-only recall drill; no server interaction.                                                                                                                                                         |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game` green (review + all prior).
- Full phase gate: `npm run test:run` (~1,975+ tests) green + `npm run lint` clean + `npm run build` (prebuild trail validation) before `/gsd-verify-work`.
- Manual (deferred): mic-mode review does not phantom-detect the target audition; keyboard review works; child-safety timeout does not fire mid-drill.
</verification>

<success_criteria>

- REVIEW is a registered phase; input routes to the drill before canScoreNow (mic + keyboard).
- Drill steps only wrong/missed notes, never scores/re-scores/touches combo; clean run hides the button.
- Mic lifecycle managed; ReviewDrillPanel rendered; tests green.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-practice-tooling/02-09-SUMMARY.md`
</output>
