---
phase: 16-backend-email-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: []
autonomous: false
requirements: [MAIL-01, MAIL-02]

must_haves:
  truths:
    - "Brevo dashboard shows the new support Gmail address as a verified sender"
    - "SENDER_EMAIL env var on Supabase is updated to the new support Gmail"
    - "Consent emails arrive from the new support Gmail address"
    - "Weekly report emails arrive from the new support Gmail address"
    - "The send-feedback function sends email to and from the new support Gmail"
  artifacts: []
  key_links:
    - from: "Supabase SENDER_EMAIL env var"
      to: "send-consent-email, send-weekly-report, process-account-deletions, send-feedback"
      via: "Deno.env.get('SENDER_EMAIL')"
      pattern: "SENDER_EMAIL"
---

<objective>
Update Brevo sender email to the new shared support Gmail and verify all existing transactional emails continue working. This is a configuration-only plan with no code changes.

Purpose: Consolidate all outgoing email under a single support Gmail address (per D-19), ensuring zero disruption to consent emails, weekly reports, and account deletion notifications.
Output: Verified sender in Brevo, updated env var on Supabase, confirmed email delivery for all functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-backend-email-infrastructure/16-CONTEXT.md
@.planning/phases/16-backend-email-infrastructure/16-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify new Gmail sender in Brevo and update Supabase SENDER_EMAIL</name>
  <read_first>
    supabase/functions/.env.example
  </read_first>
  <action>
This task requires manual steps in two external dashboards that Claude cannot access.

**Pre-requisite (per D-20):** The user must have already created the new support Gmail account.

**Step 1 -- Add sender in Brevo:**
1. Navigate to https://app.brevo.com/senders/list
2. Click "Add a new sender"
3. Enter the new support Gmail address as the sender email
4. Enter "PianoMaster" as the sender name
5. Brevo sends a verification email to the Gmail address
6. Open Gmail, find the Brevo verification email, click the verification link
7. Return to Brevo senders list and confirm the new address shows "verified" (green checkmark)

**Step 2 -- Update Supabase env var (per D-19):**
1. Navigate to Supabase Dashboard > Project Settings > Edge Functions > Secrets
2. Find `SENDER_EMAIL`
3. Update its value to the new support Gmail address
4. Save

**Rollback plan (per D-22):** If any email delivery fails after the update, revert `SENDER_EMAIL` to its previous value in the Supabase dashboard immediately.
  </action>
  <verify>User confirms Brevo senders list shows new Gmail verified AND Supabase SENDER_EMAIL updated</verify>
  <resume-signal>Type "done" when Brevo sender is verified AND Supabase SENDER_EMAIL is updated, or "blocked" with details if something went wrong</resume-signal>
  <acceptance_criteria>
    - User confirms Brevo senders list shows new Gmail as verified
    - User confirms Supabase SENDER_EMAIL env var is updated
  </acceptance_criteria>
  <done>New support Gmail is verified in Brevo and SENDER_EMAIL env var on Supabase points to it</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test all email functions with new sender</name>
  <read_first>
    supabase/functions/send-consent-email/index.ts,
    supabase/functions/send-weekly-report/index.ts,
    supabase/functions/send-feedback/index.ts
  </read_first>
  <action>
SENDER_EMAIL has been updated to new support Gmail. All 4 email Edge Functions (send-consent-email, send-weekly-report, process-account-deletions, send-feedback) now use this address as both sender and, for send-feedback, the recipient. The user must verify that all email delivery continues working.

**Test 1 -- Consent email (MAIL-02):**
1. In the app, trigger a consent email flow (or use the Supabase dashboard to invoke `send-consent-email` directly)
2. Check the parent email inbox -- email should arrive FROM the new support Gmail address
3. Verify the email content renders correctly (HTML template unchanged)

**Test 2 -- Send feedback (MAIL-01 + BACK-01):**
1. Using curl or the browser console, POST to the send-feedback Edge Function with a valid JWT:
```
curl -X POST "https://PROJECT_REF.supabase.co/functions/v1/send-feedback" \
  -H "Authorization: Bearer VALID_JWT" \
  -H "Content-Type: application/json" \
  -H "apikey: ANON_KEY" \
  -d '{"type":"bug","message":"This is a test feedback submission from smoke test verification."}'
```
2. Check the support Gmail inbox -- feedback email should arrive with subject `[Bug] PianoMaster Feedback`
3. Verify email body contains: `Type: Bug`, `Student: xxxxxxxx...` (8-char prefix), `Version: unknown` (no client version sent via curl), `Submitted: <UTC timestamp>`, and the message text

**Test 3 -- Weekly report (MAIL-02):**
- This is cron-triggered (Monday 08:00 UTC). If testing before the next cron run: check recent Brevo logs at https://app.brevo.com/logs to confirm no sender-related errors. The next scheduled run will confirm delivery.
- If the cron has already fired since the env var update: check the recipient inbox for the weekly report email from the new sender address.

**Test 4 -- Rate limiting (SPAM-02):**
1. Send 3 more feedback submissions via curl (total 4 including Test 2)
2. The 4th submission should return `{"success":false,"error":"rate_limit"}` with HTTP 429

**Test 5 -- Input validation (SPAM-04):**
1. POST with message too short: `{"type":"bug","message":"hi"}` -- expect 400
2. POST with invalid type: `{"type":"invalid","message":"This is a valid length message for testing."}` -- expect 400
  </action>
  <verify>User confirms all 5 tests pass: consent email arrives, feedback email arrives with correct format, rate limiting triggers at 4th request, input validation rejects bad data</verify>
  <resume-signal>Type "approved" if all tests pass, or describe which test(s) failed with error details</resume-signal>
  <acceptance_criteria>
    - Consent email arrives from new support Gmail sender address
    - Feedback email arrives in support Gmail inbox with correct subject and body format
    - 4th feedback submission within an hour returns HTTP 429
    - Short message (< 10 chars) returns HTTP 400
    - Invalid type enum returns HTTP 400
    - No errors in Brevo logs related to sender verification
  </acceptance_criteria>
  <done>All transactional emails work with new sender. Feedback pipeline is end-to-end functional: auth, validation, rate limiting, DB insert, and Brevo delivery all confirmed.</done>
</task>

</tasks>

<verification>
1. Brevo senders list shows new support Gmail as verified
2. Supabase SENDER_EMAIL env var updated
3. Consent email arrives from new sender
4. Feedback email arrives in support inbox with correct format
5. Rate limiting blocks 4th submission
6. Input validation rejects bad data
7. No Brevo log errors for existing email functions
</verification>

<success_criteria>
- New support Gmail verified as sender in Brevo (MAIL-01)
- SENDER_EMAIL env var updated on Supabase dashboard (MAIL-01)
- All existing email functions (consent, weekly report, account deletion) send from new address without errors (MAIL-02)
- send-feedback delivers email to support inbox with correct plain-text format (BACK-01, BACK-02)
- Rate limiting and input validation confirmed working in production (SPAM-01, SPAM-02, SPAM-04)
</success_criteria>

<output>
After completion, create `.planning/phases/16-backend-email-infrastructure/16-02-SUMMARY.md`
</output>
