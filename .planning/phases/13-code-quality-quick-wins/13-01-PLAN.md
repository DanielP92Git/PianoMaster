---
phase: 13-code-quality-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/noteUtils.js
  - src/utils/noteUtils.test.js
  - src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx
  - src/components/games/sight-reading-game/components/KlavierKeyboard.jsx
  - src/services/skillProgressService.js
  - src/services/skillProgressService.test.js
  - src/hooks/useVictoryState.js
  - src/services/apiDatabase.js
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "noteNameToMidi has exactly one definition in the codebase, in src/utils/noteUtils.js"
    - "calculateStarsFromPercentage has exactly one definition in the codebase, exported from skillProgressService.js"
    - "verifyStudentDataAccess has exactly one definition in the codebase, in authorizationUtils.js"
    - "All callers of these three functions import from the canonical location"
  artifacts:
    - path: "src/utils/noteUtils.js"
      provides: "Canonical noteNameToMidi with sharp and flat support"
      exports: ["noteNameToMidi"]
    - path: "src/utils/noteUtils.test.js"
      provides: "Unit tests for noteNameToMidi"
      min_lines: 20
    - path: "src/services/skillProgressService.test.js"
      provides: "Unit tests for calculateStarsFromPercentage"
      min_lines: 15
  key_links:
    - from: "src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx"
      to: "src/utils/noteUtils.js"
      via: "named import"
      pattern: "import.*noteNameToMidi.*from.*utils/noteUtils"
    - from: "src/components/games/sight-reading-game/components/KlavierKeyboard.jsx"
      to: "src/utils/noteUtils.js"
      via: "named import"
      pattern: "import.*noteNameToMidi.*from.*utils/noteUtils"
    - from: "src/hooks/useVictoryState.js"
      to: "src/services/skillProgressService.js"
      via: "named import"
      pattern: "import.*calculateStarsFromPercentage.*from.*skillProgressService"
    - from: "src/services/apiDatabase.js"
      to: "src/services/authorizationUtils.js"
      via: "named import"
      pattern: "import.*verifyStudentDataAccess.*from.*authorizationUtils"
---

<objective>
Consolidate three duplicated utility functions to single canonical implementations.

Purpose: Eliminate code duplication that causes divergent behavior (noteNameToMidi has two versions with different flat-note handling) and maintenance burden (changes must be made in multiple places).
Output: Three utilities each have a single source of truth with all consumers importing from the canonical location. Two new test files validate the consolidated functions.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-code-quality-quick-wins/13-CONTEXT.md
@.planning/phases/13-code-quality-quick-wins/13-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/utils/lazyWithRetry.js:
```javascript
export function lazyWithRetry(importFn) // wraps React.lazy with stale-chunk recovery
```

From src/services/skillProgressService.js (line 19-24):
```javascript
const _calculateStarsFromPercentage = (percentage) => {
  if (percentage >= 95) return 3;
  if (percentage >= 80) return 2;
  if (percentage >= 60) return 1;
  return 0;
};
```

From src/services/authorizationUtils.js (line 19+):
```javascript
export async function verifyStudentDataAccess(studentId) {
  // Returns { userId, isOwner, isTeacher } or throws
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate noteNameToMidi to canonical noteUtils.js (QUAL-01)</name>
  <files>src/utils/noteUtils.js, src/utils/noteUtils.test.js, src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx, src/components/games/sight-reading-game/components/KlavierKeyboard.jsx</files>
  <read_first>
    - src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx (current noteNameToMidi implementation at line 30-74, NOTE_TO_SEMITONE at line 30, STEM_REFERENCE_MIDI at line 76-79 which calls noteNameToMidi at module level)
    - src/components/games/sight-reading-game/components/KlavierKeyboard.jsx (current noteNameToMidi at line 22, NOTE_NAMES at line 6 used by both noteNameToMidi and midiToNoteName at line 34)
    - src/utils/noteUtils.js (should not exist yet — verify)
  </read_first>
  <behavior>
    - noteNameToMidi("C4") returns 60
    - noteNameToMidi("A4") returns 69
    - noteNameToMidi("Eb4") returns 63 (flat support)
    - noteNameToMidi("F#3") returns 54 (sharp support)
    - noteNameToMidi("c4") returns 60 (case-insensitive)
    - noteNameToMidi("Cb4") returns 59 (enharmonic: Cb = B)
    - noteNameToMidi(null) returns null
    - noteNameToMidi("") returns null
    - noteNameToMidi("X4") returns null (invalid note letter)
    - noteNameToMidi("C") returns null (no octave)
    - noteNameToMidi("C-1") returns null (negative octave not matched by regex)
  </behavior>
  <action>
    **Per D-01:** Create `src/utils/noteUtils.js` as the canonical home for `noteNameToMidi`.

    **Step 1 — Write test file `src/utils/noteUtils.test.js`:**
    Create unit tests covering all behaviors listed above. Import `{ noteNameToMidi }` from `../utils/noteUtils`. Use `describe("noteNameToMidi", () => { ... })` with individual `it()` cases. Run tests — they MUST fail (file does not exist yet).

    **Step 2 — Create `src/utils/noteUtils.js`:**
    Use the VexFlowStaffDisplay implementation (NOT the KlavierKeyboard version — the KlavierKeyboard version does not handle flat notes). The file must contain:

    ```javascript
    const NOTE_TO_SEMITONE = {
      C: 0, "C#": 1, D: 2, "D#": 3, E: 4,
      F: 5, "F#": 6, G: 7, "G#": 8, A: 9, "A#": 10, B: 11,
    };

    /**
     * Convert a note name to MIDI number.
     * Supports sharps (#), flats (b), and case-insensitive note letters.
     * @param {string} pitch - e.g. "C4", "Eb4", "F#3", "c4"
     * @returns {number|null} MIDI number or null if invalid
     */
    export const noteNameToMidi = (pitch) => {
      if (!pitch) return null;
      const match = pitch.match(/^([A-Ga-g])([#b]?)(\d)$/);
      if (!match) return null;

      const [, letter, accidental, octaveStr] = match;
      const baseLetter = letter.toUpperCase();
      const noteKey =
        accidental === "b"
          ? (() => {
              const flatMap = {
                CB: "B", DB: "C#", EB: "D#", FB: "E",
                GB: "F#", AB: "G#", BB: "A#",
              };
              return flatMap[`${baseLetter}B`] || baseLetter;
            })()
          : `${baseLetter}${accidental === "#" ? "#" : ""}`;
      const semitone = NOTE_TO_SEMITONE[noteKey];
      if (semitone === undefined) return null;

      const octave = parseInt(octaveStr, 10);
      if (Number.isNaN(octave)) return null;

      return (octave + 1) * 12 + semitone;
    };
    ```

    Run tests — they MUST pass.

    **Step 3 — Rewire VexFlowStaffDisplay.jsx:**
    - Add `import { noteNameToMidi } from "../../../../utils/noteUtils";` at the top of the import section
    - Delete the local `NOTE_TO_SEMITONE` const (around line 30-42)
    - Delete the local `noteNameToMidi` function (around line 45-74)
    - Keep `STEM_REFERENCE_MIDI` (line 76-79) — it calls `noteNameToMidi("B4")` and `noteNameToMidi("D3")` at module level. Because ES imports are hoisted, the imported `noteNameToMidi` is available before `STEM_REFERENCE_MIDI` is evaluated. No change needed to STEM_REFERENCE_MIDI.
    - Keep all other code in the file unchanged

    **Step 4 — Rewire KlavierKeyboard.jsx:**
    - Add `import { noteNameToMidi } from "../../../../utils/noteUtils";` at the top of the import section
    - Delete the local `noteNameToMidi` function (around line 22-32)
    - KEEP the local `NOTE_NAMES` array (around line 6) — it is used by the local `midiToNoteName` function (line 34) which is NOT being extracted (out of scope per D-01)
    - Keep all other code in the file unchanged
  </action>
  <verify>
    <automated>npx vitest run src/utils/noteUtils.test.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/noteUtils.js exists and contains `export const noteNameToMidi`
    - src/utils/noteUtils.test.js exists and all tests pass
    - VexFlowStaffDisplay.jsx contains `import { noteNameToMidi } from "../../../../utils/noteUtils"`
    - VexFlowStaffDisplay.jsx does NOT contain `const NOTE_TO_SEMITONE` (local copy removed)
    - VexFlowStaffDisplay.jsx does NOT contain `const noteNameToMidi` (local function removed)
    - VexFlowStaffDisplay.jsx STILL contains `STEM_REFERENCE_MIDI` (kept, uses imported function)
    - KlavierKeyboard.jsx contains `import { noteNameToMidi } from "../../../../utils/noteUtils"`
    - KlavierKeyboard.jsx does NOT contain a local `noteNameToMidi` function definition
    - KlavierKeyboard.jsx STILL contains `NOTE_NAMES` array (used by midiToNoteName)
    - `npm run build` succeeds with no import errors
  </acceptance_criteria>
  <done>noteNameToMidi has a single canonical implementation in src/utils/noteUtils.js. Both consumers (VexFlowStaffDisplay, KlavierKeyboard) import from the canonical location. Unit tests cover sharp notes, flat notes, lowercase, and edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export calculateStarsFromPercentage from skillProgressService (QUAL-02)</name>
  <files>src/services/skillProgressService.js, src/services/skillProgressService.test.js, src/hooks/useVictoryState.js</files>
  <read_first>
    - src/services/skillProgressService.js (line 19-24: _calculateStarsFromPercentage — the canonical implementation, currently private with underscore prefix)
    - src/hooks/useVictoryState.js (line 61-71: local calculateStars function — the duplicate; line 205 and 335: the two call sites using `calculateStars(scorePercentage)`)
  </read_first>
  <behavior>
    - calculateStarsFromPercentage(100) returns 3
    - calculateStarsFromPercentage(95) returns 3
    - calculateStarsFromPercentage(94) returns 2
    - calculateStarsFromPercentage(80) returns 2
    - calculateStarsFromPercentage(79) returns 1
    - calculateStarsFromPercentage(60) returns 1
    - calculateStarsFromPercentage(59) returns 0
    - calculateStarsFromPercentage(0) returns 0
  </behavior>
  <action>
    **Per D-02:** Export `calculateStarsFromPercentage` from `skillProgressService.js` and import in `useVictoryState.js`.

    **Step 1 — Write test file `src/services/skillProgressService.test.js`:**
    Create unit tests covering all behaviors above. Import `{ calculateStarsFromPercentage }` from `./skillProgressService`. Use `describe("calculateStarsFromPercentage", () => { ... })` with individual `it()` cases for each threshold boundary. Run tests — they MUST fail (function not yet exported).

    **Step 2 — Export the function in skillProgressService.js:**
    Change line 19 from:
    ```javascript
    const _calculateStarsFromPercentage = (percentage) => {
    ```
    to:
    ```javascript
    export const calculateStarsFromPercentage = (percentage) => {
    ```
    Then find ALL internal references to `_calculateStarsFromPercentage` within the same file and rename them to `calculateStarsFromPercentage`. Search for `_calculateStarsFromPercentage(` in the file — each hit must be renamed.

    Run tests — they MUST pass.

    **Step 3 — Rewire useVictoryState.js:**
    - Add to imports section: `import { calculateStarsFromPercentage } from "../services/skillProgressService";`
    - Delete the local `calculateStars` function (around lines 61-71 — the block that starts with `const calculateStars = (scorePercentage) => {`)
    - Replace ALL call sites of `calculateStars(` with `calculateStarsFromPercentage(` — there are 2 call sites (around lines 205 and 335). Search for `calculateStars(` to find them.
    - Verify no references to old name `calculateStars` remain in the file (except comments if any)
  </action>
  <verify>
    <automated>npx vitest run src/services/skillProgressService.test.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - skillProgressService.js line 19 (or nearby) contains `export const calculateStarsFromPercentage`
    - skillProgressService.js does NOT contain `_calculateStarsFromPercentage` anywhere
    - skillProgressService.test.js exists and all 8 threshold tests pass
    - useVictoryState.js contains `import { calculateStarsFromPercentage } from "../services/skillProgressService"`
    - useVictoryState.js does NOT contain `const calculateStars` (local function removed)
    - useVictoryState.js does NOT contain `calculateStars(` calls (replaced with calculateStarsFromPercentage)
    - `npm run build` succeeds with no import errors
  </acceptance_criteria>
  <done>calculateStarsFromPercentage is exported from skillProgressService.js as the single canonical implementation. useVictoryState.js imports from there. Unit tests cover all star thresholds (0%, 59%, 60%, 79%, 80%, 94%, 95%, 100%).</done>
</task>

<task type="auto">
  <name>Task 3: Remove verifyStudentDataAccess duplicate from apiDatabase.js (QUAL-03)</name>
  <files>src/services/apiDatabase.js</files>
  <read_first>
    - src/services/apiDatabase.js (line 1-40: imports + local verifyStudentDataAccess definition; line 57, 180, 388, 407, 420, 642: the 6 callers — verify none capture the return value)
    - src/services/authorizationUtils.js (line 19+: canonical verifyStudentDataAccess that returns {userId, isOwner, isTeacher})
  </read_first>
  <action>
    **Per D-03:** No circular dependency exists. apiDatabase.js does not import from authorizationUtils.js, so adding the import is safe.

    **Step 1 — Add import:**
    Add this import line near the top of apiDatabase.js, after the existing `import supabase from './supabase';` line:
    ```javascript
    import { verifyStudentDataAccess } from "./authorizationUtils";
    ```

    **Step 2 — Delete the local copy:**
    Remove the entire local `verifyStudentDataAccess` function definition from apiDatabase.js. This is the block starting around line 3 (after the supabase import) through approximately line 39. It includes a comment block and the async function definition. The function:
    - Has a JSDoc comment about verifying student data access
    - Gets the current user via `supabase.auth.getUser()`
    - Checks if user.id === studentId
    - Checks teacher_student_connections for teacher access
    - Returns `true` (unlike the canonical which returns `{userId, isOwner, isTeacher}`)

    Delete the entire function and its comment block.

    **Step 3 — Verify callers are safe:**
    The 6 callers in apiDatabase.js all use `await verifyStudentDataAccess(studentId)` without capturing the return value. The canonical returns `{userId, isOwner, isTeacher}` which is truthy, so the behavior is identical. Do NOT change any caller code — just verify by reading each call site that none uses `const result = await verifyStudentDataAccess(...)` or destructures the return.

    **Step 4 — Build verification:**
    Run `npm run build` to confirm no import resolution errors.
  </action>
  <verify>
    <automated>npm run build && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - apiDatabase.js contains `import { verifyStudentDataAccess } from "./authorizationUtils"`
    - apiDatabase.js does NOT contain `async function verifyStudentDataAccess` or `const verifyStudentDataAccess`
    - apiDatabase.js still contains 6 calls to `await verifyStudentDataAccess(` (callers unchanged)
    - `npm run build` succeeds
    - `npm run test:run` passes (full test suite)
  </acceptance_criteria>
  <done>verifyStudentDataAccess has a single definition in authorizationUtils.js. apiDatabase.js imports from the canonical location. All 6 internal callers work correctly because they only check for thrown exceptions, not return values.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

1. `npm run build` — zero errors (no broken imports from refactoring)
2. `npm run test:run` — full test suite green
3. Duplicate check:
   - `grep -rn "noteNameToMidi" src/ --include="*.js" --include="*.jsx"` — should show only noteUtils.js (definition), noteUtils.test.js (tests), VexFlowStaffDisplay.jsx (import), KlavierKeyboard.jsx (import)
   - `grep -rn "calculateStars" src/ --include="*.js" --include="*.jsx"` — should show only skillProgressService.js (definition), skillProgressService.test.js (tests), useVictoryState.js (import + calls using new name)
   - `grep -rn "verifyStudentDataAccess" src/services/apiDatabase.js` — should show only the import line and call sites (no function definition)
</verification>

<success_criteria>
- noteNameToMidi exists in exactly 1 file as a definition (src/utils/noteUtils.js), with 2 consumer imports
- calculateStarsFromPercentage exists in exactly 1 file as a definition (skillProgressService.js), with 1 consumer import
- verifyStudentDataAccess exists in exactly 1 file as a definition (authorizationUtils.js), apiDatabase.js imports it
- All unit tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-quality-quick-wins/13-01-SUMMARY.md`
</output>
