---
phase: 13-code-quality-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AchievementsLegacy.jsx
  - src/pages/Achievements.jsx
  - supabase/migrations/DEBUG_check_teacher_status.sql
  - supabase/migrations/README_USER_PREFERENCES.md
  - supabase/migrations/TEST_direct_insert.sql
  - src/App.jsx
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements: [QUAL-04, QUAL-05, QUAL-07, XP-01]

must_haves:
  truths:
    - "AchievementsLegacy.jsx does not exist in the codebase"
    - "No DEBUG_, TEST_, or README_ files exist in supabase/migrations/"
    - "TeacherDashboard loads via React.lazy (not eager import) in App.jsx"
    - "No locale string values contain 'Total Points' or 'total points' — all say 'Total XP' or 'total XP'"
  artifacts:
    - path: "src/pages/Achievements.jsx"
      provides: "Clean wrapper without stale rollback comment"
      contains: "AchievementsRedesign"
    - path: "src/App.jsx"
      provides: "TeacherDashboard as lazy-loaded chunk"
      contains: "lazyWithRetry"
    - path: "src/locales/en/common.json"
      provides: "English locale with XP terminology"
      contains: "Total XP"
    - path: "src/locales/he/common.json"
      provides: "Hebrew locale with XP terminology"
      contains: "XP"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/layout/TeacherDashboard"
      via: "lazyWithRetry dynamic import"
      pattern: "lazyWithRetry.*import.*TeacherDashboard"
---

<objective>
Remove dead code, lazy-load the teacher bundle, and migrate remaining "points" locale strings to "XP".

Purpose: Reduce codebase noise from dead files, shrink main JS bundle by splitting TeacherDashboard into a separate chunk (students never download it), and complete the XP terminology migration in locale files.
Output: 4 dead files deleted, 1 stale comment removed, TeacherDashboard lazy-loaded, 7 locale string values updated across 2 files.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-code-quality-quick-wins/13-CONTEXT.md
@.planning/phases/13-code-quality-quick-wins/13-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From src/utils/lazyWithRetry.js:
```javascript
export function lazyWithRetry(importFn) // wraps React.lazy with stale-chunk recovery
```

From src/App.jsx — existing lazy import pattern (lines 50-65):
```javascript
const TrailMapPage = lazyWithRetry(() => import("./pages/TrailMapPage"));
const Achievements = lazyWithRetry(() => import("./pages/Achievements"));
const PracticeModes = lazyWithRetry(() => import("./pages/PracticeModes"));
// ... etc
```

From src/App.jsx — line 29 (the eager import to replace):
```javascript
import TeacherDashboard from "./components/layout/TeacherDashboard";
```

From src/App.jsx — line 43 (lazyWithRetry already imported):
```javascript
import { lazyWithRetry } from "./utils/lazyWithRetry";
```

From src/pages/Achievements.jsx (entire file):
```javascript
import AchievementsRedesign from "./AchievementsRedesign";
// To rollback: swap this import to AchievementsLegacy
export default function Achievements() {
  return <AchievementsRedesign />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code and lazy-load TeacherDashboard (QUAL-04, QUAL-05, QUAL-07)</name>
  <files>src/pages/AchievementsLegacy.jsx, src/pages/Achievements.jsx, supabase/migrations/DEBUG_check_teacher_status.sql, supabase/migrations/README_USER_PREFERENCES.md, supabase/migrations/TEST_direct_insert.sql, src/App.jsx</files>
  <read_first>
    - src/pages/AchievementsLegacy.jsx (confirm it exists and has no live imports — it is dead code)
    - src/pages/Achievements.jsx (line 2: stale rollback comment to remove; full file is only 5 lines)
    - src/App.jsx (line 29: eager TeacherDashboard import; line 43: lazyWithRetry already imported; lines 50-65: existing lazy import pattern to follow; line 309 area: existing Suspense boundary wrapping routes)
  </read_first>
  <action>
    **QUAL-04 — Delete AchievementsLegacy.jsx (per D-04):**

    1. Delete the file `src/pages/AchievementsLegacy.jsx` entirely.
    2. Edit `src/pages/Achievements.jsx` — remove line 2 (the comment `// To rollback: swap this import to AchievementsLegacy`). The resulting file should be:
    ```javascript
    import AchievementsRedesign from "./AchievementsRedesign";
    export default function Achievements() {
      return <AchievementsRedesign />;
    }
    ```

    **QUAL-05 — Delete non-migration files (per D-05):**

    Delete these 3 files from `supabase/migrations/`:
    - `supabase/migrations/DEBUG_check_teacher_status.sql`
    - `supabase/migrations/README_USER_PREFERENCES.md`
    - `supabase/migrations/TEST_direct_insert.sql`

    **QUAL-07 — Lazy-load TeacherDashboard (per D-06):**

    Edit `src/App.jsx`:
    1. Delete line 29: `import TeacherDashboard from "./components/layout/TeacherDashboard";`
    2. Add the lazy import in the lazy-loaded section (after the existing lazy game components, around line 72 area):
    ```javascript
    const TeacherDashboard = lazyWithRetry(() => import("./components/layout/TeacherDashboard"));
    ```
    3. Do NOT add a new Suspense boundary — one already exists at line 309 wrapping all routes including `/teacher/*`.
    4. Do NOT add a prefetch line (unlike TrailMapPage which has `import("./pages/TrailMapPage")` for prefetch). Teacher routes are role-gated and not worth prefetching for student users.
    5. `lazyWithRetry` is already imported at line 43 — no new import needed.
  </action>
  <verify>
    <automated>npm run build && bash -c "test ! -f src/pages/AchievementsLegacy.jsx && test ! -f supabase/migrations/DEBUG_check_teacher_status.sql && test ! -f supabase/migrations/README_USER_PREFERENCES.md && test ! -f supabase/migrations/TEST_direct_insert.sql && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/AchievementsLegacy.jsx does NOT exist (deleted)
    - src/pages/Achievements.jsx does NOT contain the string "rollback" or "AchievementsLegacy"
    - src/pages/Achievements.jsx contains `import AchievementsRedesign from "./AchievementsRedesign"` and `export default function Achievements`
    - supabase/migrations/DEBUG_check_teacher_status.sql does NOT exist (deleted)
    - supabase/migrations/README_USER_PREFERENCES.md does NOT exist (deleted)
    - supabase/migrations/TEST_direct_insert.sql does NOT exist (deleted)
    - src/App.jsx does NOT contain `import TeacherDashboard from "./components/layout/TeacherDashboard"` (eager import removed)
    - src/App.jsx contains `const TeacherDashboard = lazyWithRetry(() => import("./components/layout/TeacherDashboard"))` (lazy import added)
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>
  <done>AchievementsLegacy.jsx deleted with stale rollback comment removed. Three non-migration files deleted from supabase/migrations/. TeacherDashboard loads via lazyWithRetry as a separate chunk — students never download the teacher bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining "points" locale strings to "XP" terminology (XP-01)</name>
  <files>src/locales/en/common.json, src/locales/he/common.json</files>
  <read_first>
    - src/locales/en/common.json (lines 185-194: achievements section with "points" key; lines 548-562: accessory unlock section with pointsEarned* keys)
    - src/locales/he/common.json (lines 185-194: Hebrew achievements section; lines 547-561: Hebrew accessory unlock section)
  </read_first>
  <action>
    **Per D-07:** JS teacher code already uses `total_xp`. Only locale string VALUES need updating. Do NOT rename any keys (key renaming deferred per CONTEXT.md deferred ideas).

    **en/common.json — 4 value changes:**

    | Line | Key path | Current value | New value |
    |------|----------|--------------|-----------|
    | 188 | `pages.achievements.achievements.points` | `"Total Points"` | `"Total XP"` |
    | 190 | `pages.achievements.achievements.pointsBreakdown` | `"{{gameplay}} game + {{achievements}} badge points"` | `"{{gameplay}} game + {{achievements}} badge XP"` |
    | 551 | `avatars.unlockModal.pointsEarnedDescription` | `"Earn {{amount}} total points (lifetime)"` | `"Earn {{amount}} total XP (lifetime)"` |
    | 557 | `avatars.unlockModal.pointsEarnedCompleted` | `"You've earned {{amount}} total points! ✨"` | `"You've earned {{amount}} total XP! ✨"` |

    Also update en/common.json line 555:
    | 555 | `avatars.unlockModal.pointsEarnedTipDetailed` | `"Earn {{amount}} more points to unlock. This counts your total lifetime points earned, not just what's available to spend."` | `"Earn {{amount}} more points to unlock. This counts your total lifetime XP earned, not just what's available to spend."` |

    Wait — re-reading D-07: "change remaining 'Total Points' / 'total points' references to 'Total XP' / 'total XP'". The `pointsEarnedTipDetailed` uses "points" not "Total Points". Let me be precise:
    - Line 551 `pointsEarnedDescription`: contains "total points" -> change to "total XP"
    - Line 555 `pointsEarnedTipDetailed`: contains "more points" and "lifetime points" -> change "points" to "XP" in both spots: `"Earn {{amount}} more XP to unlock. This counts your total lifetime XP earned, not just what's available to spend."`
    - Line 557 `pointsEarnedCompleted`: contains "total points" -> change to "total XP"

    Full en/common.json changes (5 strings):
    1. Line 188: `"Total Points"` -> `"Total XP"`
    2. Line 190: `"{{gameplay}} game + {{achievements}} badge points"` -> `"{{gameplay}} game + {{achievements}} badge XP"`
    3. Line 551: `"Earn {{amount}} total points (lifetime)"` -> `"Earn {{amount}} total XP (lifetime)"`
    4. Line 555: `"Earn {{amount}} more points to unlock. This counts your total lifetime points earned, not just what's available to spend."` -> `"Earn {{amount}} more XP to unlock. This counts your total lifetime XP earned, not just what's available to spend."`
    5. Line 557: `"You've earned {{amount}} total points! ✨"` -> `"You've earned {{amount}} total XP! ✨"`

    **he/common.json — 4 value changes:**

    1. Line 188: `"סך כל הנקודות"` -> `"סך כל ה-XP"`
    2. Line 190: `"{{gameplay}} נקודות משחק + {{achievements}} נקודות הישגים"` -> `"{{gameplay}} XP ממשחקים + {{achievements}} XP מהישגים"`
    3. Line 550: `"צברו {{amount}} נקודות סה\"כ (לכל החיים)"` -> `"צברו {{amount}} XP סה\"כ (לכל החיים)"`
    4. Line 554: `"צברו עוד {{amount}} נקודות כדי לפתוח. זה סופר את כל הנקודות שצברתם מאז ההתחלה, לא רק את הזמין להוצאה."` -> `"צברו עוד {{amount}} XP כדי לפתוח. זה סופר את כל ה-XP שצברתם מאז ההתחלה, לא רק את הזמין להוצאה."`
    5. Line 556: `"צברתם {{amount}} נקודות סה\"כ! ✨"` -> `"צברתם {{amount}} XP סה\"כ! ✨"`

    Also update he/common.json line 552:
    6. Line 552: `"{{amount}} סה\"כ נק׳"` -> `"{{amount}} XP סה\"כ"`

    Wait — `pointsEarnedShort` at he/common.json:552 uses `נק׳` (abbreviation for "points" in Hebrew). The `xpEarnedShort` at line 553 already has the XP version. Per D-07, the `points*` keys should have their values updated. So update line 552 as well.

    Similarly, en/common.json line 553 `pointsEarnedShort`: `"{{amount}} pts total"` -> `"{{amount}} XP total"`.

    **Final en/common.json changes (6 strings):**
    1. Line 188: `"Total Points"` -> `"Total XP"`
    2. Line 190: `"{{gameplay}} game + {{achievements}} badge points"` -> `"{{gameplay}} game + {{achievements}} badge XP"`
    3. Line 551: `"Earn {{amount}} total points (lifetime)"` -> `"Earn {{amount}} total XP (lifetime)"`
    4. Line 553: `"{{amount}} pts total"` -> `"{{amount}} XP total"`
    5. Line 555: `"Earn {{amount}} more points to unlock. This counts your total lifetime points earned, not just what's available to spend."` -> `"Earn {{amount}} more XP to unlock. This counts your total lifetime XP earned, not just what's available to spend."`
    6. Line 557: `"You've earned {{amount}} total points! ✨"` -> `"You've earned {{amount}} total XP! ✨"`

    **Final he/common.json changes (6 strings):**
    1. Line 188: `"סך כל הנקודות"` -> `"סך כל ה-XP"`
    2. Line 190: `"{{gameplay}} נקודות משחק + {{achievements}} נקודות הישגים"` -> `"{{gameplay}} XP ממשחקים + {{achievements}} XP מהישגים"`
    3. Line 550: `"צברו {{amount}} נקודות סה\"כ (לכל החיים)"` -> `"צברו {{amount}} XP סה\"כ (לכל החיים)"`
    4. Line 552: `"{{amount}} סה\"כ נק׳"` -> `"{{amount}} XP סה\"כ"`
    5. Line 554: `"צברו עוד {{amount}} נקודות כדי לפתוח. זה סופר את כל הנקודות שצברתם מאז ההתחלה, לא רק את הזמין להוצאה."` -> `"צברו עוד {{amount}} XP כדי לפתוח. זה סופר את כל ה-XP שצברתם מאז ההתחלה, לא רק את הזמין להוצאה."`
    6. Line 556: `"צברתם {{amount}} נקודות סה\"כ! ✨"` -> `"צברתם {{amount}} XP סה\"כ! ✨"`

    After making changes, verify by grepping: `grep -i "total points\|נקודות" src/locales/en/common.json src/locales/he/common.json` should return no matches related to the changed keys.
  </action>
  <verify>
    <automated>bash -c "! grep -i 'total points' src/locales/en/common.json && ! grep 'סך כל הנקודות' src/locales/he/common.json && npm run build && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - en/common.json line 188 value is `"Total XP"` (not "Total Points")
    - en/common.json line 190 value ends with `"badge XP"` (not "badge points")
    - en/common.json line 551 value contains `"total XP (lifetime)"` (not "total points")
    - en/common.json line 553 value is `"{{amount}} XP total"` (not "pts total")
    - en/common.json line 555 value contains `"more XP to unlock"` and `"lifetime XP earned"` (not "points")
    - en/common.json line 557 value contains `"total XP!"` (not "total points!")
    - he/common.json line 188 value is `"סך כל ה-XP"` (not "סך כל הנקודות")
    - he/common.json line 190 value contains `"XP"` (not "נקודות")
    - he/common.json line 550 value contains `"XP סה\"כ"` (not "נקודות סה\"כ")
    - he/common.json line 552 value contains `"XP"` (not "נק׳")
    - he/common.json line 554 value contains `"XP"` twice (not "נקודות")
    - he/common.json line 556 value contains `"XP סה\"כ"` (not "נקודות סה\"כ")
    - `grep -i "total points" src/locales/en/common.json` returns no matches
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All locale strings containing "points" / "נקודות" in the achievements and accessories namespaces have been updated to use "XP" terminology. No locale keys were renamed (deferred per D-07). Both English and Hebrew files updated consistently.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

1. `npm run build` — zero errors (no broken imports from deletions or lazy loading)
2. `npm run test:run` — full test suite green
3. Dead code check:
   - `test ! -f src/pages/AchievementsLegacy.jsx` — file does not exist
   - `ls supabase/migrations/ | grep -E "^(DEBUG_|TEST_|README_)"` — returns empty
4. Lazy load check:
   - `grep "lazyWithRetry.*TeacherDashboard" src/App.jsx` — matches
   - `grep "^import TeacherDashboard" src/App.jsx` — no match (eager import removed)
5. XP terminology check:
   - `grep -i "total points" src/locales/en/common.json` — no matches
   - `grep "סך כל הנקודות" src/locales/he/common.json` — no matches
</verification>

<success_criteria>
- AchievementsLegacy.jsx deleted, rollback comment removed from Achievements.jsx
- Three non-migration files deleted from supabase/migrations/
- TeacherDashboard lazy-loaded via lazyWithRetry in App.jsx (separate chunk in build output)
- All "Total Points" / "total points" / "נקודות" locale values updated to XP terminology
- Build succeeds, test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-quality-quick-wins/13-02-SUMMARY.md`
</output>
