---
phase: 11-legal-gate-design-and-processor-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/subscriptionConfig.js
  - src/services/__tests__/subscriptionPayload.test.js
  - src/components/subscription/ParentalConsentModal.jsx
  - src/locales/en/subscription.json
  - src/locales/he/subscription.json
  - src/i18n/index.js
autonomous: true
requirements:
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "A single config file defines exactly which trail nodes are free and which boss nodes are paywalled — changing the free tier boundary requires editing one file"
    - "An automated test fails if any child PII field (student_id, name, age, grade, birth_date) appears in a billing-related payload object"
    - "A parental consent modal component shows a placeholder processor name (to be replaced once processor is chosen), discloses what data is shared, describes recurring billing terms, and explains cancellation rights — in both English and Hebrew"
    - "The subscription i18n namespace is registered and loads correctly — strings render as text, not as key paths"
  artifacts:
    - path: "src/config/subscriptionConfig.js"
      provides: "Free node ID sets, boss node IDs, isFreeNode() function"
      exports: ["FREE_NODE_IDS", "PAYWALL_BOSS_NODE_IDS", "isFreeNode", "FREE_TIER_SUMMARY"]
    - path: "src/services/__tests__/subscriptionPayload.test.js"
      provides: "PII boundary regression guard test"
      contains: "CHILD_PII_FIELDS"
    - path: "src/components/subscription/ParentalConsentModal.jsx"
      provides: "Consent modal with i18n integration"
      contains: "useTranslation('subscription')"
    - path: "src/locales/en/subscription.json"
      provides: "English consent strings"
      contains: "consent.title"
    - path: "src/locales/he/subscription.json"
      provides: "Hebrew consent strings"
      contains: "consent.title"
  key_links:
    - from: "src/components/subscription/ParentalConsentModal.jsx"
      to: "src/locales/en/subscription.json"
      via: "i18next useTranslation('subscription') namespace"
      pattern: "useTranslation\\('subscription'\\)"
    - from: "src/i18n/index.js"
      to: "src/locales/en/subscription.json"
      via: "inline resource import and ns array registration"
      pattern: "ns:.*subscription"
    - from: "src/config/subscriptionConfig.js"
      to: "src/data/units/trebleUnit1Redesigned.js"
      via: "Node IDs copied from unit files (static, not imported)"
      pattern: "treble_1_1"
---

<objective>
Create the subscription config file with verified free-tier node IDs, the PII boundary regression test, and the bilingual parental consent modal component.

Purpose: Establish the single source of truth for content gating, guard against child PII leaking to the payment processor, and satisfy COPPA 2025 disclosure requirements — all before any billing code is written.

Output: 6 files — config, test, modal component, 2 translation files, updated i18n init.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-legal-gate-design-and-processor-setup/11-CONTEXT.md
@.planning/phases/11-legal-gate-design-and-processor-setup/11-RESEARCH.md
@src/i18n/index.js
@src/components/ui/Modal.jsx
@src/data/units/trebleUnit1Redesigned.js
@src/data/units/bassUnit1Redesigned.js
@src/data/units/rhythmUnit1Redesigned.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subscription config and PII boundary test</name>
  <files>
    src/config/subscriptionConfig.js
    src/services/__tests__/subscriptionPayload.test.js
  </files>
  <action>
**Create `src/config/subscriptionConfig.js`:**

Create the `src/config/` directory (it does not exist yet). Create the subscription config file with these exact node IDs verified from the redesigned unit files:

- Free treble nodes (7): `treble_1_1` through `treble_1_7`
- Free bass nodes (6): `bass_1_1` through `bass_1_6`
- Free rhythm nodes (6): `rhythm_1_1` through `rhythm_1_6`
- Paywall boss nodes (3): `boss_treble_1`, `boss_bass_1`, `boss_rhythm_1`

Export:
- `FREE_TREBLE_NODE_IDS` — array of 7 treble node ID strings
- `FREE_BASS_NODE_IDS` — array of 6 bass node ID strings
- `FREE_RHYTHM_NODE_IDS` — array of 6 rhythm node ID strings
- `PAYWALL_BOSS_NODE_IDS` — array of 3 boss node IDs
- `FREE_NODE_IDS` — `new Set([...treble, ...bass, ...rhythm])` for O(1) lookup
- `FREE_TIER_SUMMARY` — object with `{ treble: { count: 7 }, bass: { count: 6 }, rhythm: { count: 6 }, total: 19, bossNodeCount: 3 }`
- `isFreeNode(nodeId)` — returns `FREE_NODE_IDS.has(nodeId)`

Add a JSDoc header comment documenting the verification date and source files. Per locked decision: this is static config with explicit IDs, NO dynamic resolution from unit files.

**Create `src/services/__tests__/subscriptionPayload.test.js`:**

Create the `src/services/__tests__/` directory (it does not exist yet). Create a Vitest test file with:

- `CHILD_PII_FIELDS` constant: `['student_id', 'student_name', 'child_name', 'name', 'age', 'grade', 'birth_date']`
- Test 1: "billing checkout payload contains only parent email, amount, currency" — create a mock checkout payload with `{ email: 'parent@example.com', priceId: 'pri_test_123', currency: 'USD', quantity: 1 }` and assert none of the CHILD_PII_FIELDS are present
- Test 2: "webhook subscription record does not include child PII fields" — create a mock record with `{ parent_email, paddle_subscription_id, status, current_period_end }` and assert `['child_name', 'student_name', 'age', 'grade', 'birth_date']` are absent
- Test 3: "subscriptionConfig lists only valid node IDs from unit files" — import `FREE_TREBLE_NODE_IDS`, `FREE_BASS_NODE_IDS`, `FREE_RHYTHM_NODE_IDS`, `PAYWALL_BOSS_NODE_IDS` from the config and assert: treble has 7 items, bass has 6 items, rhythm has 6 items, boss has 3 items, all IDs match the expected format (`/^treble_1_\d+$/`, `/^bass_1_\d+$/`, `/^rhythm_1_\d+$/`, `/^boss_(treble|bass|rhythm)_1$/`)

Import from `vitest`: `describe, it, expect`.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/subscriptionPayload.test.js</automated>
    <manual>Confirm src/config/subscriptionConfig.js exports the correct node counts</manual>
  </verify>
  <done>subscriptionConfig.js exports 19 free node IDs + 3 boss node IDs with isFreeNode(). PII boundary test passes with 3 test cases.</done>
</task>

<task type="auto">
  <name>Task 2: Create parental consent modal with bilingual i18n</name>
  <files>
    src/components/subscription/ParentalConsentModal.jsx
    src/locales/en/subscription.json
    src/locales/he/subscription.json
    src/i18n/index.js
  </files>
  <action>
**Create `src/locales/en/subscription.json`:**

```json
{
  "consent": {
    "title": "Your Child's Privacy Comes First",
    "intro": "Before you subscribe, here's everything you need to know about how your information is handled.",
    "processor": {
      "label": "Who processes your payment:",
      "body": "Payments are processed by {{processorName}}. They are responsible for securely handling your payment information."
    },
    "dataShared": {
      "label": "What is shared:",
      "body": "Only your email address is shared with the payment processor. Your child's name, age, grade, and student information are never sent."
    },
    "billing": {
      "label": "Recurring billing:",
      "body": "You'll be billed monthly or yearly (your choice) until you cancel. Your first charge happens when you subscribe."
    },
    "cancellation": {
      "label": "Cancellation:",
      "body": "You can cancel anytime from within the app. Your child keeps full access until the end of the current billing period."
    },
    "understood": "I understand — continue"
  }
}
```

**Create `src/locales/he/subscription.json`:**

```json
{
  "consent": {
    "title": "פרטיות ילדך בראש סדר העדיפויות",
    "intro": "לפני שתרשמו למנוי, הנה כל מה שצריך לדעת על אופן הטיפול במידע שלכם.",
    "processor": {
      "label": "מי מעבד את התשלום שלך:",
      "body": "התשלומים מעובדים על ידי {{processorName}}. הם אחראים לטיפול מאובטח במידע התשלום שלך."
    },
    "dataShared": {
      "label": "מה משותף:",
      "body": "רק כתובת האימייל שלך משותפת עם מעבד התשלומים. שם הילד, גיל, כיתה ומידע סטודנט אינם נשלחים לעולם."
    },
    "billing": {
      "label": "חיוב חוזר:",
      "body": "תחויב מדי חודש או מדי שנה (לבחירתך) עד שתבטל. החיוב הראשון יתבצע בעת ההרשמה למנוי."
    },
    "cancellation": {
      "label": "ביטול:",
      "body": "ניתן לבטל בכל עת מתוך האפליקציה. לילד יישאר גישה מלאה עד סוף תקופת החיוב הנוכחית."
    },
    "understood": "הבנתי — המשך"
  }
}
```

**Update `src/i18n/index.js`:**

Add the new subscription namespace:
1. Add imports: `import enSubscription from '../locales/en/subscription.json';` and `import heSubscription from '../locales/he/subscription.json';`
2. Add to resources: `en: { common: enCommon, trail: enTrail, subscription: enSubscription }` and same for `he`
3. Add to `ns` array: `ns: ['common', 'trail', 'subscription']`

**Create `src/components/subscription/ParentalConsentModal.jsx`:**

Create the `src/components/subscription/` directory (does not exist yet). Build the consent modal:

```jsx
import React from 'react';
import { useTranslation } from 'react-i18next';
import { Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter } from '../ui/Modal';
```

Component: `ParentalConsentModal({ isOpen, onClose, processorName = 'Your payment processor' })`

NOTE: The processor name is intentionally a placeholder. Once the Israeli-market payment processor is chosen, update this default and the subscription config.

- Use `useTranslation('subscription')`
- Render Modal with `closeOnOverlayClick={false}` and `closeOnEscape={false}` — per locked decision, user MUST acknowledge (click the "I understand" button), not dismiss by clicking outside
- Modal title: `t('consent.title')`
- Intro paragraph with `text-gray-600 mb-4` styling
- Four disclosure items as a `<ul>` with `space-y-3 text-sm text-gray-700`:
  - Each item has a `<strong>` label + body text
  - Processor item uses `t('consent.processor.body', { processorName })` for interpolation
- Footer with single primary button: `t('consent.understood')` that calls `onClose`
- Use existing design system classes: `text-gray-900`, `text-gray-600`, `text-gray-700`
- Button styling: match app's existing button patterns (check other modals for reference, likely `bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2 rounded-lg font-medium`)

Export as both named and default export.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/subscriptionPayload.test.js && node -e "import('./src/i18n/index.js').then(m => console.log('i18n loaded'))" 2>/dev/null || echo "i18n check: visual verification needed"</automated>
    <manual>Verify ParentalConsentModal renders correctly by importing in a test or checking for syntax errors. Verify i18n namespace registration by checking src/i18n/index.js includes 'subscription' in ns array.</manual>
  </verify>
  <done>ParentalConsentModal component exists with 4 COPPA disclosure points. English and Hebrew translation files created. i18n index registers 'subscription' namespace. Processor name is interpolated (not hardcoded) via {{processorName}} i18n variable.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/services/__tests__/subscriptionPayload.test.js` — all 3 tests pass
2. `src/config/subscriptionConfig.js` exists and exports `FREE_NODE_IDS` (Set with 19 entries) and `isFreeNode()`
3. `src/components/subscription/ParentalConsentModal.jsx` exists and imports from `'../ui/Modal'`
4. `src/i18n/index.js` has `'subscription'` in the `ns` array and both resource bundles imported
5. `src/locales/en/subscription.json` contains all 4 consent disclosure keys
6. `src/locales/he/subscription.json` contains matching Hebrew translations
7. `grep -c "processorName" src/components/subscription/ParentalConsentModal.jsx` returns at least 1 (interpolation, not hardcoded)
</verification>

<success_criteria>
- subscriptionConfig.js is the single source of truth for free-tier gating with 19 free + 3 boss node IDs
- PII boundary test catches any child data field in billing payloads
- Parental consent modal covers all 4 COPPA disclosure points with i18n
- Processor name is a variable, not hardcoded JSX text
- Modal cannot be dismissed without clicking "I understand"
</success_criteria>

<output>
After completion, create `.planning/phases/11-legal-gate-design-and-processor-setup/11-01-SUMMARY.md`
</output>
