---
phase: 11-legal-gate-design-and-processor-setup
plan: 03
type: execute
wave: 1
depends_on: ["11-02"]
files_modified:
  - src/config/subscriptionConfig.js
  - src/components/subscription/ParentalConsentModal.jsx
autonomous: false
requirements: [PAY-01, COMP-03]
gap_closure: true

must_haves:
  truths:
    - "PAYMENT_PROCESSOR.name is set to the confirmed processor name (not 'TBD') and selected is true"
    - "ParentalConsentModal renders the real processor name — a parent reading it today sees 'Payments are processed by [real name]', not 'Your payment processor'"
    - "PAYMENT_PROCESSOR.sandboxUrl and liveUrl are populated with real URLs"
  artifacts:
    - path: "src/config/subscriptionConfig.js"
      provides: "Confirmed PAYMENT_PROCESSOR with name, selected: true, sandbox/live URLs, payout currency"
      contains: "selected: true"
    - path: "src/components/subscription/ParentalConsentModal.jsx"
      provides: "Default processorName updated to real processor name"
  key_links:
    - from: "src/components/subscription/ParentalConsentModal.jsx"
      to: "src/config/subscriptionConfig.js"
      via: "processorName default matches PAYMENT_PROCESSOR.name"
      pattern: "PAYMENT_PROCESSOR\\.name"
---

<objective>
Close the two remaining verification gaps for Phase 11 by confirming Paddle registration (human action) and updating the processor placeholder and consent modal default with real values.

Purpose: PAY-01 requires a registered payment processor with sandbox active and Israeli bank payout confirmed. COMP-03 requires the parental consent modal to name the processor explicitly (COPPA 2025 mandates "specific identities" of third parties, not category descriptions). Both gaps trace to a single root cause: Paddle account registration has not occurred.

Output: `subscriptionConfig.js` has `PAYMENT_PROCESSOR.selected = true` with real name/URLs, and `ParentalConsentModal.jsx` defaults to the real processor name.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-legal-gate-design-and-processor-setup/11-VERIFICATION.md
@.planning/phases/11-legal-gate-design-and-processor-setup/11-01-SUMMARY.md
@.planning/phases/11-legal-gate-design-and-processor-setup/11-02-SUMMARY.md
@src/config/subscriptionConfig.js
@src/components/subscription/ParentalConsentModal.jsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm Paddle account registration and payout path</name>
  <action>
    Complete the Paddle Registration Checklist from RESEARCH.md:

    1. Register a Paddle sandbox account at https://sandbox-vendors.paddle.com/signup
       - Record the sandbox vendor URL once complete
    2. Register a Paddle live account at https://vendors.paddle.com/signup
       - Complete domain, business, and identity verification
       - Record the live vendor URL once complete
    3. During live account onboarding, navigate to payout settings and confirm:
       - Israeli bank account is accepted as a payout destination
       - What payout currency is available (ILS or USD — either is acceptable)
    4. Provide the following information so Claude can update the code:
       - Confirmed processor name (expected: "Paddle")
       - Sandbox vendor URL (e.g., https://sandbox-vendors.paddle.com/...)
       - Live vendor URL (e.g., https://vendors.paddle.com/...)
       - Confirmed payout currency (ILS or USD)

    If Paddle does NOT support Israeli bank payouts, state the alternative processor you selected and provide the equivalent URLs and payout currency.
  </action>
  <resume-signal>
    Provide the confirmed processor details:
    - Processor name: ___
    - Sandbox URL: ___
    - Live URL: ___
    - Payout currency: ___

    Or state: "Paddle confirmed" if using Paddle with defaults (sandbox-vendors.paddle.com / vendors.paddle.com).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update processor config and consent modal with confirmed values</name>
  <files>
    src/config/subscriptionConfig.js
    src/components/subscription/ParentalConsentModal.jsx
  </files>
  <action>
    Using the processor details confirmed in Task 1:

    1. In `src/config/subscriptionConfig.js`:
       - Set `PAYMENT_PROCESSOR.name` to the confirmed processor name (e.g., `'Paddle'`)
       - Set `PAYMENT_PROCESSOR.selected` to `true`
       - Set `PAYMENT_PROCESSOR.sandboxUrl` to the confirmed sandbox URL (e.g., `'https://sandbox-vendors.paddle.com'`)
       - Set `PAYMENT_PROCESSOR.liveUrl` to the confirmed live URL (e.g., `'https://vendors.paddle.com'`)
       - Set `PAYMENT_PROCESSOR.payoutCurrency` to the confirmed payout currency (e.g., `'USD'` or `'ILS'`)
       - Update the TODO comment above PAYMENT_PROCESSOR to a "Confirmed" comment with date and payout details

    2. In `src/components/subscription/ParentalConsentModal.jsx`:
       - Change the `processorName` default prop from `'Your payment processor'` to the confirmed processor name (e.g., `'Paddle'`)
       - Update the JSDoc NOTE comment to reflect that the processor is now confirmed

    3. Run existing tests to confirm nothing breaks:
       - `npx vitest run src/services/__tests__/subscriptionPayload.test.js`
       - All 3 PII boundary tests must still pass
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/subscriptionPayload.test.js</automated>
    <manual>
      1. Open src/config/subscriptionConfig.js and verify PAYMENT_PROCESSOR.selected is true and name is not 'TBD'
      2. Open src/components/subscription/ParentalConsentModal.jsx and verify processorName default is not 'Your payment processor'
    </manual>
  </verify>
  <done>
    PAYMENT_PROCESSOR.name is the confirmed processor (not 'TBD'), selected is true, sandbox/live URLs are populated, payout currency is set. ParentalConsentModal.jsx default processorName matches the confirmed processor name. All 3 existing PII boundary tests pass.
  </done>
</task>

</tasks>

<verification>
1. `PAYMENT_PROCESSOR.selected === true` in subscriptionConfig.js
2. `PAYMENT_PROCESSOR.name` is a real processor name (not 'TBD')
3. `processorName` default in ParentalConsentModal.jsx matches `PAYMENT_PROCESSOR.name`
4. `npx vitest run src/services/__tests__/subscriptionPayload.test.js` — 3/3 pass
5. COPPA disclosure: the consent modal would render "Payments are processed by [real name]" not "Payments are processed by Your payment processor"
</verification>

<success_criteria>
- PAY-01 gap closed: processor confirmed, sandbox URL and live URL populated, payout currency documented
- COMP-03 gap closed: parental consent modal defaults to the real processor name, satisfying COPPA 2025 "specific identities" requirement
- No regressions: all 3 existing PII boundary tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-legal-gate-design-and-processor-setup/11-03-SUMMARY.md`
</output>
