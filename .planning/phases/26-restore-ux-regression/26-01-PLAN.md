---
phase: 26-restore-ux-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/rhythm-games/utils/rhythmTimingUtils.js
  - src/components/games/rhythm-games/utils/rhythmScoringUtils.js
  - src/components/games/rhythm-games/MetronomeTrainer.jsx
  - src/hooks/useDocumentTitle.js
  - src/locales/en/common.json
  - src/locales/he/common.json
  - src/locales/en/trail.json
  - src/locales/he/trail.json
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Discovery/Practice/MIX_UP/REVIEW nodes use 100ms PERFECT threshold base instead of 50ms"
    - "CHALLENGE/SPEED_ROUND/MINI_BOSS/BOSS nodes keep 50ms PERFECT threshold base"
    - "MetronomeTrainer UI shows 'Listen & Tap' in English and 'חזור אחריי' in Hebrew — 'Metronome' does not appear in any visible label"
    - "Tapping incorrectly shows 'Almost!' in English — the string 'MISS' does not appear in rhythm game UI"
    - "Pulse exercise type shows 'דופק' in Hebrew trail modal"
  artifacts:
    - path: "src/components/games/rhythm-games/utils/rhythmTimingUtils.js"
      provides: "Two-tier timing thresholds"
      contains: "EASY_NODE_TYPES"
    - path: "src/components/games/rhythm-games/utils/rhythmScoringUtils.js"
      provides: "nodeType param in scoreTap"
      contains: "nodeType"
    - path: "src/locales/en/common.json"
      provides: "Updated i18n keys"
      contains: "Almost!"
    - path: "src/locales/he/trail.json"
      provides: "Pulse Hebrew key"
      contains: "דופק"
  key_links:
    - from: "rhythmTimingUtils.js"
      to: "rhythmScoringUtils.js"
      via: "calculateTimingThresholds(tempo, nodeType)"
      pattern: "calculateTimingThresholds.*nodeType"
    - from: "rhythmScoringUtils.js"
      to: "RhythmReadingGame.jsx"
      via: "scoreTap 5th param"
      pattern: "scoreTap.*nodeType"
---

<objective>
Restore timing forgiveness (UX-01), game rename (UX-02), "Almost!" feedback (UX-03), and pulse i18n for the rhythm trail.

Purpose: These UX improvements were implemented in Phase 23 but reverted by commit 13cff54. This plan re-implements the timing, naming, and feedback changes on the current codebase.
Output: Updated timing utils with two-tier thresholds, renamed game labels, encouraging feedback text, and pulse exercise Hebrew translation.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-restore-ux-regression/26-CONTEXT.md
@.planning/phases/26-restore-ux-regression/26-UI-SPEC.md

<interfaces>
<!-- From src/data/nodeTypes.js — NODE_TYPES used to define EASY_NODE_TYPES -->
export const NODE_TYPES = {
  DISCOVERY: 'discovery',
  PRACTICE: 'practice',
  MIX_UP: 'mix_up',
  SPEED_ROUND: 'speed_round',
  REVIEW: 'review',
  CHALLENGE: 'challenge',
  MINI_BOSS: 'mini_boss',
  BOSS: 'boss'
};

<!-- From src/components/games/rhythm-games/utils/rhythmScoringUtils.js — current scoreTap signature -->

export function scoreTap(tapTime, scheduledBeatTimes, nextBeatIndex, tempo)

<!-- From src/components/games/rhythm-games/utils/rhythmTimingUtils.js — current calculateTimingThresholds -->

export function calculateTimingThresholds(tempo)
const BASE_TIMING_THRESHOLDS = { PERFECT: 50, GOOD: 75, FAIR: 125 };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-tier timing thresholds and nodeType-aware scoring (UX-01)</name>
  <files>
    src/components/games/rhythm-games/utils/rhythmTimingUtils.js
    src/components/games/rhythm-games/utils/rhythmScoringUtils.js
    src/components/games/rhythm-games/MetronomeTrainer.jsx
    src/components/games/rhythm-games/utils/__tests__/rhythmTimingUtils.test.js
  </files>
  <read_first>
    src/components/games/rhythm-games/utils/rhythmTimingUtils.js
    src/components/games/rhythm-games/utils/rhythmScoringUtils.js
    src/components/games/rhythm-games/MetronomeTrainer.jsx
    src/data/nodeTypes.js
  </read_first>
  <behavior>
    - Test 1: calculateTimingThresholds(120) with no nodeType returns PERFECT=50 (backward compat)
    - Test 2: calculateTimingThresholds(120, 'discovery') returns PERFECT=100
    - Test 3: calculateTimingThresholds(120, 'practice') returns PERFECT=100
    - Test 4: calculateTimingThresholds(120, 'mix_up') returns PERFECT=100
    - Test 5: calculateTimingThresholds(120, 'review') returns PERFECT=100
    - Test 6: calculateTimingThresholds(120, 'challenge') returns PERFECT=50
    - Test 7: calculateTimingThresholds(120, 'boss') returns PERFECT=50
    - Test 8: calculateTimingThresholds(65, 'discovery') PERFECT is approximately 140ms (100 * tempo scaling)
    - Test 9: scoreTap with nodeType='discovery' uses wider thresholds
  </behavior>
  <action>
    **Per D-03, D-04, D-05:**

    1. In `rhythmTimingUtils.js`, add after the existing `BASE_TIMING_THRESHOLDS` constant:

    ```javascript
    // Two-tier system (D-03): easy nodes get 100ms PERFECT base, hard nodes keep 50ms
    export const EASY_NODE_TYPES = new Set([
      'discovery', 'practice', 'mix_up', 'review'
    ]);

    export const BASE_TIMING_THRESHOLDS_EASY = {
      PERFECT: 100,  // ±100ms at 120 BPM
      GOOD: 150,     // ±150ms at 120 BPM
      FAIR: 250,     // ±250ms at 120 BPM
    };
    ```

    2. Update `calculateTimingThresholds(tempo)` signature to `calculateTimingThresholds(tempo, nodeType = null)`:
    ```javascript
    export function calculateTimingThresholds(tempo, nodeType = null) {
      const base = (nodeType && EASY_NODE_TYPES.has(nodeType))
        ? BASE_TIMING_THRESHOLDS_EASY
        : BASE_TIMING_THRESHOLDS;
      const scalingFactor = Math.pow(BASE_TEMPO / tempo, 0.3);
      return {
        PERFECT: Math.round(base.PERFECT * scalingFactor),
        GOOD: Math.round(base.GOOD * scalingFactor),
        FAIR: Math.round(base.FAIR * scalingFactor),
      };
    }
    ```

    3. In `rhythmScoringUtils.js`, update `scoreTap` to accept a 5th parameter `nodeType`:
    ```javascript
    export function scoreTap(tapTime, scheduledBeatTimes, nextBeatIndex, tempo, nodeType = null) {
      const thresholds = calculateTimingThresholds(tempo, nodeType);
      // ... rest unchanged
    }
    ```

    4. In `MetronomeTrainer.jsx`:
       - DELETE the local `BASE_TIMING_THRESHOLDS` constant (lines 36-41) and local `calculateTimingThresholds` function (lines 47-63)
       - ADD import: `import { calculateTimingThresholds } from './utils/rhythmTimingUtils';`
       - Find all calls to the local `calculateTimingThresholds(tempo)` and replace with `calculateTimingThresholds(tempo, nodeType)` where `nodeType` is derived from: `const node = nodeId ? getNodeById(nodeId) : null; const nodeType = node?.nodeType ?? null;`
       - `getNodeById` is already imported in MetronomeTrainer.jsx

    Note: RhythmReadingGame.jsx and other callers of `scoreTap` pass `nodeType` in Plan 02 — the default `null` preserves backward compatibility for now.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/utils/__tests__/rhythmTimingUtils.test.js --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - rhythmTimingUtils.js contains `export const EASY_NODE_TYPES = new Set(`
    - rhythmTimingUtils.js contains `export const BASE_TIMING_THRESHOLDS_EASY`
    - rhythmTimingUtils.js contains `calculateTimingThresholds(tempo, nodeType`
    - rhythmScoringUtils.js contains `scoreTap(tapTime, scheduledBeatTimes, nextBeatIndex, tempo, nodeType`
    - MetronomeTrainer.jsx does NOT contain `const BASE_TIMING_THRESHOLDS =` (local duplicate removed)
    - MetronomeTrainer.jsx contains `import { calculateTimingThresholds } from './utils/rhythmTimingUtils'`
    - Test file exists and all tests pass
  </acceptance_criteria>
  <done>Two-tier timing thresholds work: easy nodes get 100ms PERFECT base, hard nodes keep 50ms. scoreTap accepts nodeType. MetronomeTrainer deduped to shared utils.</done>
</task>

<task type="auto">
  <name>Task 2: i18n updates — game rename, "Almost!" feedback, pulse Hebrew (UX-02, UX-03, pulse i18n)</name>
  <files>
    src/locales/en/common.json
    src/locales/he/common.json
    src/locales/en/trail.json
    src/locales/he/trail.json
    src/hooks/useDocumentTitle.js
  </files>
  <read_first>
    src/locales/en/common.json
    src/locales/he/common.json
    src/locales/en/trail.json
    src/locales/he/trail.json
    src/hooks/useDocumentTitle.js
    src/components/games/rhythm-games/components/FloatingFeedback.jsx
    src/components/games/rhythm-games/components/TapArea.jsx
  </read_first>
  <action>
    **Per D-06, D-09, D-24 and UI-SPEC i18n contract:**

    **English common.json updates:**
    1. At JSON path `games.rhythmReading.tapArea.accuracy.miss`: change value from `"MISS"` to `"Almost!"`
    2. At JSON path `games.metronomeTrainer.tapArea.accuracy.miss`: change value from `"MISS"` to `"Almost!"`
    3. At JSON path `games.metronomeTrainer.headerTitle`: change from `"Metronome Rhythm Trainer"` to `"Listen & Tap"`
    4. At JSON path `games.modes.metronomeTrainer.name`: change from `"Metronome Rhythm Trainer"` to `"Listen & Tap"`
    5. At JSON path `gameSettings.titles.metronomeTrainer`: change from `"Metronome Rhythm Trainer"` to `"Listen & Tap"`
    6. ADD new keys at `games.rhythmReading.syllableToggle.show`: `"Show syllables"`
    7. ADD new keys at `games.rhythmReading.syllableToggle.hide`: `"Hide syllables"`

    **Hebrew common.json updates:**
    1. At JSON path `games.rhythmReading.tapArea.accuracy.miss` (currently `"פספוס"`): change to `"!כמעט"` (Almost! in Hebrew)
    2. At JSON path `games.metronomeTrainer.tapArea.accuracy.miss` (currently `"פספסת"`): change to `"!כמעט"`
    3. At JSON path `games.metronomeTrainer.headerTitle` (currently `"מאמן המטרונום"`): change to `"חזור אחריי"`
    4. At JSON path `games.modes.metronomeTrainer.name`: change to `"חזור אחריי"`
    5. At JSON path `gameSettings.titles.metronomeTrainer`: change to `"חזור אחריי"`
    6. ADD `games.rhythmReading.syllableToggle.show`: `"הצג הברות"`
    7. ADD `games.rhythmReading.syllableToggle.hide`: `"הסתר הברות"`

    **English trail.json:**
    1. ADD `exerciseTypes.pulse`: `"Pulse"` (verify key exists; if already present, no change needed)

    **Hebrew trail.json:**
    1. ADD `exerciseTypes.pulse`: `"דופק"` (this is the missing key per integration gap 4)

    **useDocumentTitle.js:**
    1. Find the line `if (path.includes("/metronome-trainer"))` (around line 47-48)
    2. Change the `defaultValue` from `"Metronome Trainer"` to `"Listen & Tap"`
    3. The `t('games.metronomeTrainer.title')` key is what resolves — but the `defaultValue` fallback also needs updating

    NOTE: FloatingFeedback.jsx and TapArea.jsx already use i18n keys for their labels. The i18n value changes above are sufficient — no component code changes needed for UX-03.

  </action>
  <verify>
    <automated>node -e "const en=require('./src/locales/en/common.json'); const he=require('./src/locales/he/common.json'); const enT=require('./src/locales/en/trail.json'); const heT=require('./src/locales/he/trail.json'); console.log('EN miss:', en.games.rhythmReading.tapArea.accuracy.miss); console.log('HE miss:', he.games.rhythmReading.tapArea.accuracy.miss); console.log('EN header:', en.games.metronomeTrainer.headerTitle); console.log('HE header:', he.games.metronomeTrainer.headerTitle); console.log('EN pulse:', enT.exerciseTypes.pulse); console.log('HE pulse:', heT.exerciseTypes.pulse); console.log('EN syllable show:', en.games.rhythmReading.syllableToggle?.show); const ok = en.games.rhythmReading.tapArea.accuracy.miss === 'Almost!' && en.games.metronomeTrainer.headerTitle === 'Listen & Tap' && heT.exerciseTypes.pulse === 'דופק'; console.log('ALL PASS:', ok)"</automated>
  </verify>
  <acceptance_criteria>
    - en/common.json `games.rhythmReading.tapArea.accuracy.miss` equals `"Almost!"`
    - en/common.json `games.metronomeTrainer.headerTitle` equals `"Listen & Tap"`
    - en/common.json `gameSettings.titles.metronomeTrainer` equals `"Listen & Tap"`
    - he/common.json `games.metronomeTrainer.headerTitle` equals `"חזור אחריי"`
    - he/trail.json `exerciseTypes.pulse` equals `"דופק"`
    - en/trail.json `exerciseTypes.pulse` equals `"Pulse"`
    - en/common.json contains `"syllableToggle"` key with `"show"` and `"hide"` subkeys
    - useDocumentTitle.js contains `"Listen & Tap"` as defaultValue for metronome-trainer route
    - The string `"Metronome Rhythm Trainer"` does NOT appear in en/common.json `headerTitle` or `gameSettings.titles.metronomeTrainer`
  </acceptance_criteria>
  <done>All i18n keys updated: "MISS" becomes "Almost!", MetronomeTrainer becomes "Listen & Tap" in both languages, pulse has Hebrew key "דופק", syllable toggle keys added for Plan 02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                          |
| -------------- | -------------------------------------------------------------------- |
| i18n JSON → UI | User-facing text from translation files rendered in React components |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                      |
| --------- | ---------------------- | ----------------- | ----------- | ------------------------------------------------------------------------------------ |
| T-26-01   | Tampering              | i18n JSON files   | accept      | i18n files are bundled at build time, not fetched at runtime; no user input involved |
| T-26-02   | Information Disclosure | Timing thresholds | accept      | Timing values are not sensitive data; they affect game difficulty only               |

</threat_model>

<verification>
1. `npm run build` passes (no lint/type errors from changes)
2. `npx vitest run src/components/games/rhythm-games/utils/ --reporter=verbose` — all timing/scoring tests pass
3. Grep: no occurrence of `"Metronome Rhythm Trainer"` in `games.metronomeTrainer.headerTitle` or `gameSettings.titles.metronomeTrainer` in en/common.json
4. Grep: no occurrence of `"MISS"` as a value in `accuracy.miss` keys in en/common.json
</verification>

<success_criteria>

- Two-tier timing works: easy nodes 100ms PERFECT, hard nodes 50ms PERFECT at 120 BPM
- scoreTap accepts and forwards nodeType parameter
- MetronomeTrainer uses shared calculateTimingThresholds (no local duplicate)
- All user-facing "MISS" text replaced with "Almost!" (EN) / "כמעט!" (HE)
- All user-facing "Metronome Rhythm Trainer" replaced with "Listen & Tap" (EN) / "חזור אחריי" (HE)
- Pulse exercise type has Hebrew translation "דופק"
- useDocumentTitle shows "Listen & Tap" for metronome-trainer route
  </success_criteria>

<output>
After completion, create `.planning/phases/26-restore-ux-regression/26-01-SUMMARY.md`
</output>
