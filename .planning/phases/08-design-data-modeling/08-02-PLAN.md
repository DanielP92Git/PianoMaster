---
phase: 08-design-data-modeling
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/notes-master-games/NotesRecognitionGame.jsx
autonomous: true
gap_closure: true
requirements:
  - PIPE-05
  - PIPE-06

must_haves:
  truths:
    - "SightReadingGame derives mic timing from its tempo setting via calcMicTimingFromBpm and passes the result to useMicNoteInput"
    - "NotesRecognitionGame derives mic timing from its tempo setting via calcMicTimingFromBpm and passes the result to useMicNoteInput"
    - "Both game components have per-note scoring dedup — the same pitch cannot score twice within the note minimum duration window"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "BPM-derived timing passed to useMicNoteInput + per-note scoring dedup"
      contains: "calcMicTimingFromBpm"
    - path: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      provides: "BPM-derived timing passed to useMicNoteInput + per-note scoring dedup"
      contains: "calcMicTimingFromBpm"
  key_links:
    - from: "src/hooks/micInputPresets.js"
      to: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      via: "import { calcMicTimingFromBpm }"
      pattern: "calcMicTimingFromBpm"
    - from: "src/hooks/micInputPresets.js"
      to: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      via: "import { calcMicTimingFromBpm }"
      pattern: "calcMicTimingFromBpm"
    - from: "SightReadingGame handleNoteEvent"
      to: "handleNoteDetectedRef scoring"
      via: "per-note dedup ref blocks repeat scores"
      pattern: "lastScoredPitch"
---

<objective>
Wire BPM-adaptive timing from game settings into useMicNoteInput calls, and add per-note scoring dedup in both game components.

Purpose: Plan 01 created the timing utility and FSM. This plan connects them to game components (PIPE-05) and adds the scoring-layer dedup guard (PIPE-06) so that no single played note scores twice.

Output: Updated SightReadingGame.jsx and NotesRecognitionGame.jsx with BPM-derived timing and per-note dedup.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-data-modeling/08-01-SUMMARY.md
@src/hooks/micInputPresets.js
@src/components/games/sight-reading-game/SightReadingGame.jsx
@src/components/games/notes-master-games/NotesRecognitionGame.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire BPM timing into SightReadingGame and add per-note scoring dedup</name>
  <files>src/components/games/sight-reading-game/SightReadingGame.jsx</files>
  <action>
**Part A: Replace static MIC_INPUT_PRESETS with BPM-derived timing (PIPE-05)**

1. Add import: `import { calcMicTimingFromBpm, MIC_INPUT_PRESETS } from "../../../hooks/micInputPresets";` (keep MIC_INPUT_PRESETS as fallback).

2. Find the useMicNoteInput call (around line 840-847) that currently spreads `...MIC_INPUT_PRESETS.sightReading`.

3. Before the useMicNoteInput call, add a `useMemo` that computes timing params:
```js
const micTiming = useMemo(() => {
  const bpm = gameSettings?.tempo || gameSettings?.bpm;
  const shortestDuration = gameSettings?.shortestDuration || 'q';
  if (bpm) {
    return calcMicTimingFromBpm(bpm, shortestDuration);
  }
  // Fallback to static preset when no BPM available
  return MIC_INPUT_PRESETS.sightReading;
}, [gameSettings?.tempo, gameSettings?.bpm, gameSettings?.shortestDuration]);
```

4. Replace the `...MIC_INPUT_PRESETS.sightReading` spread with `...micTiming` in the useMicNoteInput call.

**Part B: Add per-note scoring dedup (PIPE-06)**

1. Add a ref near the other refs at the top of the component:
```js
const lastScoredRef = useRef({ pitch: null, time: 0 });
```

2. In the `handleNoteEvent` callback (around line 805), BEFORE calling `handleNoteDetectedRef.current(event.pitch, ...)`, add a dedup guard:
```js
const now = performance.now();
const last = lastScoredRef.current;
const minScoreInterval = micTiming.minInterOnMs || 80;
if (last.pitch === event.pitch && now - last.time < minScoreInterval * 2) {
  return; // Block double-scoring same pitch within dedup window
}
lastScoredRef.current = { pitch: event.pitch, time: now };
```

The `minScoreInterval * 2` factor means the dedup window is twice the minimum inter-onset time — sufficient to block rapid re-triggers for the same held note without blocking legitimate repeated notes in a melody.

**IMPORTANT:** Do NOT remove any existing debug/telemetry logging. The dedup guard should be added before the `handleNoteDetectedRef.current(...)` call but after the telemetry logging.

**IMPORTANT:** Keep `noteFrequencies` in the useMicNoteInput call — it is used by the legacy compat path.
  </action>
  <verify>
Grep the file for `calcMicTimingFromBpm` — must appear in import and useMemo.
Grep the file for `lastScoredRef` — must appear in ref declaration and handleNoteEvent.
Grep the file for `MIC_INPUT_PRESETS.sightReading` — must NOT appear as a spread in useMicNoteInput (replaced by `...micTiming`).

Run: `npx vitest run --reporter=verbose 2>&1 | tail -20` — existing tests must pass.
  </verify>
  <done>
SightReadingGame passes BPM-derived timing to useMicNoteInput instead of static presets.
Per-note scoring dedup blocks same-pitch double-scoring within the adaptive dedup window.
Fallback to MIC_INPUT_PRESETS.sightReading when no BPM available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire BPM timing into NotesRecognitionGame and add per-note scoring dedup</name>
  <files>src/components/games/notes-master-games/NotesRecognitionGame.jsx</files>
  <action>
**Part A: Add BPM-derived timing to useMicNoteInput call (PIPE-05)**

1. Add import: `import { calcMicTimingFromBpm, MIC_INPUT_PRESETS } from "../../../hooks/micInputPresets";`

2. Before the useMicNoteInput call (around line 1673-1684), add a `useMemo` that computes timing params:
```js
const micTiming = useMemo(() => {
  // NotesRecognitionGame may not have BPM settings — use a moderate default
  const bpm = settings?.tempo || settings?.bpm || 90;
  return calcMicTimingFromBpm(bpm, 'q'); // Quarter note default for recognition
}, [settings?.tempo, settings?.bpm]);
```

3. Spread `...micTiming` into the useMicNoteInput call. The current call has `isActive: false` and `onNoteEvent: handleMicNoteEvent`. Add the spread:
```js
} = useMicNoteInput({
  isActive: false,
  onNoteEvent: handleMicNoteEvent,
  ...micTiming,
});
```

**Part B: Add per-note scoring dedup (PIPE-06)**

1. Add a ref near the other refs:
```js
const lastScoredRef = useRef({ pitch: null, time: 0 });
```

2. In `handleMicNoteEvent` (around line 1653), after the `if (event.type !== 'noteOn') return;` guard, add dedup:
```js
const now = performance.now();
const last = lastScoredRef.current;
const minScoreInterval = micTiming.minInterOnMs || 80;
if (last.pitch === event.pitch && now - last.time < minScoreInterval * 2) {
  return; // Block double-scoring same pitch
}
lastScoredRef.current = { pitch: event.pitch, time: now };
```

This must be placed BEFORE the `handleAnswerSelect` call.

**IMPORTANT:** Do NOT change how analyserNode/sampleRate are passed at call time. The comment in the existing code explains they are null at render time and passed at call time via overrides. The timing params (onFrames, offMs, etc.) are fine to pass at render time.
  </action>
  <verify>
Grep the file for `calcMicTimingFromBpm` — must appear in import and useMemo.
Grep the file for `lastScoredRef` — must appear in ref declaration and handleMicNoteEvent.
Grep the file for `...micTiming` — must appear in useMicNoteInput call.

Run: `npx vitest run --reporter=verbose 2>&1 | tail -20` — existing tests must pass.
  </verify>
  <done>
NotesRecognitionGame passes BPM-derived timing to useMicNoteInput instead of no timing overrides.
Per-note scoring dedup blocks same-pitch double-scoring.
analyserNode/sampleRate call-time override pattern preserved.
  </done>
</task>

</tasks>

<verification>
1. SightReadingGame imports and uses calcMicTimingFromBpm — no more `...MIC_INPUT_PRESETS.sightReading` spread directly in hook call
2. NotesRecognitionGame imports and uses calcMicTimingFromBpm — timing params now passed to useMicNoteInput
3. Both games have lastScoredRef with per-note dedup in their scoring handlers
4. `npm run test:run` passes
5. No changes to hook public APIs — only game-level wiring changed
</verification>

<success_criteria>
- BPM context flows from game settings to detection hooks in both games (PIPE-05)
- Per-note scoring dedup prevents double-scoring in both games (PIPE-06)
- Existing tests pass — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-data-modeling/08-02-SUMMARY.md`
</output>
