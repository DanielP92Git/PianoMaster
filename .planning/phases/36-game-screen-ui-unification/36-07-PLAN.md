---
phase: 36-game-screen-ui-unification
plan: 07
type: execute
wave: 5
depends_on: ["36-04", "36-05", "36-06"]
files_modified:
  - src/components/games/shared/hud/LivesDisplay.test.jsx
  - src/components/games/shared/hud/ComboPill.test.jsx
  - src/components/games/shared/hud/LivesDisplay.jsx
  - src/components/games/shared/hud/ComboPill.jsx
  - src/components/games/shared/hud/OnFireBadge.jsx
  - src/components/games/shared/hud/OnFireSplash.jsx
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "RED contract tests for LivesDisplay and ComboPill exist BEFORE the components, then go GREEN once the components are created (within this plan)"
    - "LivesDisplay, ComboPill, OnFireBadge, OnFireSplash exist as named-export verbatim lifts under shared/hud/ with reduced-motion read internally (D-10)"
    - "ComboPill encapsulates its own shake/scale animation by diffing the combo prop (no comboShake prop from parent)"
    - "OnFireBadge reads BOTH useMotionTokens().reduce and useAccessibility().reducedMotion (dual-source pitfall)"
  artifacts:
    - path: "src/components/games/shared/hud/LivesDisplay.jsx"
      provides: "Animated hearts row"
      contains: "export function LivesDisplay"
    - path: "src/components/games/shared/hud/ComboPill.jsx"
      provides: "Combo pill with internal shake/scale + optional Flame (isOnFire)"
      contains: "export function ComboPill"
    - path: "src/components/games/shared/hud/OnFireBadge.jsx"
      provides: "Inline flame badge"
      contains: "export function OnFireBadge"
    - path: "src/components/games/shared/hud/OnFireSplash.jsx"
      provides: "Full-screen flame overlay"
      contains: "export function OnFireSplash"
  key_links:
    - from: "LivesDisplay.test.jsx"
      to: "src/components/games/shared/hud/LivesDisplay.jsx"
      via: 'import { LivesDisplay } from "./LivesDisplay"'
      pattern: "from \"\\./LivesDisplay\""
    - from: "ComboPill.jsx"
      to: "framer-motion + lucide-react (Zap, Flame)"
      via: "internal animation state from prop diff"
      pattern: "prevComboRef"
---

<objective>
Wave 3 (D-05) start: extract the engagement-layer base components. First write RED contract tests
for LivesDisplay and ComboPill (the two with unit-testable logic), then create the four base
engagement components as verbatim lifts: LivesDisplay, ComboPill, OnFireBadge, OnFireSplash.
ComboPill internalizes its shake/scale animation (D-10), and OnFireBadge handles the
reduced-motion dual-source pitfall. The remaining two engagement components (SpeedBonusFlash,
TierUpPopup) and the NotesRecognition engagement refactor are in Plan 36-08.

This plan starts only after Wave 2 base-shell rollout fully lands (D-05: each wave lands before
the next). Creating the LivesDisplay/ComboPill tests here (not in Wave 0) keeps the full suite
green across Waves 1–2 — these components don't exist until now, so their tests would otherwise
red the per-wave gate.

Purpose: Establish the engagement base components against a test contract.
Output: 2 RED→GREEN test files + 4 engagement components under shared/hud/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md

<interfaces>
Verbatim source-of-truth lines in NotesRecognitionGame.jsx:
- LivesDisplay JSX: lines 2479–2509 (hearts, exit/enter animation)
- ComboPill JSX: lines 2435–2469 (Zap, tint tiers, shake/scale)
- OnFireBadge JSX: lines 2415–2432 (flame.png, animate-pulse)
- OnFireSplash JSX: lines 2228–2247 (fixed z-[70] flame overlay)

Component contracts (from 36-PATTERNS.md):

- `export function LivesDisplay({ lives, totalLives = 3 })` — outer wrapper carries
  `aria-label={`${lives} lives remaining`}` and `role="group"`.
- `export function ComboPill({ combo, isOnFire = false })` — internal `prevComboRef` diff drives
  shake (combo decreased) / scale (combo increased); when `isOnFire`, renders `Flame` instead of `Zap`.
- `export function OnFireBadge({ active })` — reads BOTH `useMotionTokens().reduce` and
  `useAccessibility().reducedMotion`; CSS guard `className={reduce || reducedMotion ? "" : "animate-pulse"}`.
- `export function OnFireSplash({ show })`.

flame.png import from hud/ depth (4 levels): `import flameIcon from "../../../../assets/icons/flame.png"`.
useMotionTokens import: `../../../../utils/useMotionTokens`. AccessibilityContext: `../../../../contexts/AccessibilityContext`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LivesDisplay.test.jsx + ComboPill.test.jsx (RED gate)</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "Test Mock Scaffolding" (lines ~786–808) incl. the AccessibilityContext mock note (lines ~570–575); LivesDisplay.jsx contract (lines ~192–243); ComboPill.jsx contract (lines ~247–317).
    - .planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md — Component Inventory LivesDisplay + ComboPill rows; Animation Contract heart/combo rows.
  </read_first>
  <action>
Create two test files importing from the not-yet-existing sibling components (intended RED until
Task 2). Use the framer-motion / react-i18next / useMotionTokens mocks from 36-PATTERNS.md. For
LivesDisplay, ALSO mock `../../../../contexts/AccessibilityContext` (LivesDisplay itself only uses
useMotionTokens, but ComboPill/OnFireBadge will — keep mocks minimal per component).

**LivesDisplay.test.jsx** — `import { LivesDisplay } from "./LivesDisplay";` Tests under
`describe("LivesDisplay", ...)`:

1. `it("renders totalLives hearts")` — `<LivesDisplay lives={2} totalLives={3} />`; assert 3 Heart
   nodes render (query the container for the heart wrapper elements; with the framer mock, count
   the rendered heart svg/icon placeholders or wrapper divs).
2. `it("marks the correct number of hearts as active vs spent")` — `<LivesDisplay lives={1} totalLives={3} />`;
   assert exactly 1 active heart carries `fill-red-400` and 2 carry `text-white/30`.
3. `it("exposes an aria-label with the remaining lives")` — assert `screen.getByLabelText("2 lives remaining")` for `lives={2}`.

**ComboPill.test.jsx** — `import { ComboPill } from "./ComboPill";` Tests under
`describe("ComboPill", ...)`:

1. `it("renders the combo count")` — `<ComboPill combo={5} />`; assert `screen.getByText("5")`.
2. `it("applies tier tint classes")` — render combo 1, 4, 9; assert the pill className contains,
   respectively, `border-white/20`, `border-amber-400/30`, `border-yellow-400/40`.
3. `it("shows the Flame icon instead of Zap when isOnFire")` — render `<ComboPill combo={6} isOnFire />`;
   assert the Flame icon placeholder is present and the Zap one is not. (Mock lucide-react so Zap
   and Flame render distinguishable test ids, e.g. `vi.mock("lucide-react", () => ({ Zap: () => <span data-testid="zap"/>, Flame: () => <span data-testid="flame"/>, Heart: () => <span data-testid="heart"/> }))`.)
   </action>
   <acceptance_criteria> - `src/components/games/shared/hud/LivesDisplay.test.jsx` exists; contains `describe("LivesDisplay"`, `import { LivesDisplay } from "./LivesDisplay"`, `fill-red-400`, `text-white/30`, and a `getByLabelText` assertion. - `src/components/games/shared/hud/ComboPill.test.jsx` exists; contains `describe("ComboPill"`, `import { ComboPill } from "./ComboPill"`, the three tier fragments, and a Flame/Zap distinction test. - `npx vitest run src/components/games/shared/hud/LivesDisplay.test.jsx src/components/games/shared/hud/ComboPill.test.jsx` exits NON-zero (RED) — failing on missing `./LivesDisplay` / `./ComboPill` modules, not test syntax errors.
   </acceptance_criteria>
   <verify>
   <automated>npx vitest run src/components/games/shared/hud/LivesDisplay.test.jsx src/components/games/shared/hud/ComboPill.test.jsx ; echo "EXPECT NON-ZERO (RED) until Task 2 creates the components"</automated>
   </verify>
   <done>LivesDisplay + ComboPill contract tests exist; RED because components are absent (Task 2 fixes).</done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2: Create LivesDisplay.jsx + ComboPill.jsx (turn Task 1 tests GREEN)</name>
  <behavior>
    - LivesDisplay({lives,totalLives=3}): renders totalLives hearts; i<lives active (fill-red-400), else spent (text-white/30); outer aria-label "{lives} lives remaining" role=group; exit/enter animation gated by reduce.
    - ComboPill({combo,isOnFire=false}): renders combo; tint tiers neutral/amber/yellow at >=0/>=3/>=8; internal prevComboRef diff → shake when combo decreased, scale when increased; Flame icon when isOnFire else Zap.
  </behavior>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — LivesDisplay.jsx (lines ~192–243) and ComboPill.jsx (lines ~247–317) VERBATIM bodies, including the `prevComboRef`/`useEffect` shake-detection pattern.
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx lines 2435–2509 (the combo + lives inline source).
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "Pattern 3: Value-In, Animation Encapsulated" (lines ~243–268) and Pitfall 2.
  </read_first>
  <action>
Create the two components under `src/components/games/shared/hud/` as verbatim lifts from
36-PATTERNS.md.

**LivesDisplay.jsx** — imports:

```jsx
import { AnimatePresence, motion } from "framer-motion";
import { Heart } from "lucide-react";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

`export function LivesDisplay({ lives, totalLives = 3 })`. Body verbatim from PATTERNS lines
214–242: outer `<div className="flex items-center gap-0.5" aria-label={`${lives} lives remaining`} role="group">`,
`Array.from({ length: totalLives })` mapping active (`fill-red-400 text-red-400`) vs spent
(`text-white/30`) hearts with the `reduce`-gated exit/enter motion. Mark Heart icons `aria-hidden="true"`.

**ComboPill.jsx** — imports:

```jsx
import { useRef, useEffect, useState } from "react";
import { motion } from "framer-motion";
import { Zap, Flame } from "lucide-react";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

`export function ComboPill({ combo, isOnFire = false })`. Body verbatim from PATTERNS lines
270–313: `prevComboRef`/`useEffect` diff sets `shaking` (combo decreased) or `scaling` (combo
increased), each cleared by a 300ms timeout; `motion.div animate` chooses shake `x:[0,-6,6,-4,4,0]`
or scale `scale:[1,1.18,1]`; tint tiers by `combo >= 8 / >= 3`; render `Flame` when `isOnFire`,
else `Zap` with per-tier tint.

CRITICAL (D-10, Pitfall 2): ComboPill takes NO `comboShake`/`shouldShake` prop — it detects the
change internally. Do not add such a prop.
</action>
<acceptance_criteria> - `src/components/games/shared/hud/LivesDisplay.jsx` contains `export function LivesDisplay`, `fill-red-400`, `aria-label`, and `role="group"`. - `src/components/games/shared/hud/ComboPill.jsx` contains `export function ComboPill`, `prevComboRef`, both `Zap` and `Flame` imports, and the tier fragments `border-amber-400/30` and `border-yellow-400/40`. - ComboPill.jsx does NOT contain the substring `comboShake` or `shouldShake` (no parent-driven animation prop). - `npx vitest run src/components/games/shared/hud/LivesDisplay.test.jsx src/components/games/shared/hud/ComboPill.test.jsx` exits 0 (GREEN).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/shared/hud/LivesDisplay.test.jsx src/components/games/shared/hud/ComboPill.test.jsx</automated>
</verify>
<done>LivesDisplay + ComboPill exist as verbatim lifts with internalized animation; Task 1 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Create OnFireBadge.jsx + OnFireSplash.jsx</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — OnFireBadge.jsx (lines ~320–364) and OnFireSplash.jsx (lines ~368–407) VERBATIM bodies, incl. the flame.png path note (line ~364) and the dual-source reduced-motion pattern.
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx lines 2228–2247 (OnFireSplash) and 2415–2432 (OnFireBadge); line 4 (`import flameIcon from "../../../assets/icons/flame.png"`).
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "Reduced-Motion Dual-Source Pitfall" (lines ~638–657).
  </read_first>
  <action>
Create both components under `src/components/games/shared/hud/` as verbatim lifts.

**OnFireBadge.jsx** — imports:

```jsx
import { AnimatePresence, motion } from "framer-motion";
import flameIcon from "../../../../assets/icons/flame.png";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
import { useAccessibility } from "../../../../contexts/AccessibilityContext";
```

`export function OnFireBadge({ active })`. Read BOTH `const { reduce } = useMotionTokens();` and
`const { reducedMotion } = useAccessibility();`. Body verbatim from PATTERNS lines 341–361:
AnimatePresence-wrapped `motion.div` with `className={reduce || reducedMotion ? "" : "animate-pulse"}`
and `<img src={flameIcon} alt="" className="h-10 w-10" />`.

**OnFireSplash.jsx** — imports:

```jsx
import { AnimatePresence, motion } from "framer-motion";
import flameIcon from "../../../../assets/icons/flame.png";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

`export function OnFireSplash({ show })`. Body verbatim from PATTERNS lines 381–404:
`fixed inset-0 z-[70]` flame overlay, `h-24 w-24 sm:h-28 sm:w-28 drop-shadow-[0_0_16px_rgba(251,146,60,0.6)]`,
scale-bounce gated by `reduce`.

The flame.png path MUST be `../../../../assets/icons/flame.png` (4 levels from hud/, one deeper
than NotesRecognitionGame's 3-level path).
</action>
<acceptance_criteria> - `src/components/games/shared/hud/OnFireBadge.jsx` contains `export function OnFireBadge`, both `useMotionTokens` and `useAccessibility` imports, `from "../../../../assets/icons/flame.png"`, and `animate-pulse`. - `src/components/games/shared/hud/OnFireSplash.jsx` contains `export function OnFireSplash`, `from "../../../../assets/icons/flame.png"`, and `z-[70]`. - Both files import flame.png via the exact path `../../../../assets/icons/flame.png` (not the 3-level NotesRecognition path). - `npx vitest run src/components/games/shared/hud` exits 0 (no import resolution failures; flame.png resolves).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/shared/hud</automated>
</verify>
<done>OnFireBadge + OnFireSplash exist as verbatim lifts with the corrected 4-level flame.png path; dual-source reduced-motion handled in OnFireBadge.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational engagement components fed by numeric/boolean props.

## STRIDE Threat Register

| Threat ID | Category | Component                       | Disposition | Mitigation Plan                                                                                                                          |
| --------- | -------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-07   | N/A      | hud/ engagement base components | accept      | No new attack surface — presentational components fed by existing in-component state; no new user input, network calls, or PII handling. |

</threat_model>

<verification>
- `npx vitest run src/components/games/shared/hud` → GREEN (all 4 base + engagement tests pass).
- `npm run lint` → no new errors on the 4 component files.
- `npm run test:run` → full suite green (Wave 3 entry gate).
</verification>

<success_criteria>
LivesDisplay, ComboPill, OnFireBadge, OnFireSplash exist as verbatim lifts under shared/hud/ with
reduced-motion internalized and ComboPill animation encapsulated; their contract tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-07-SUMMARY.md`.
</output>
