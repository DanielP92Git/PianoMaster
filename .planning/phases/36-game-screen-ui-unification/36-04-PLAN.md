---
phase: 36-game-screen-ui-unification
plan: 04
type: execute
wave: 4
depends_on: ["36-03"]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/notes-master-games/MemoryGame.jsx
autonomous: true
requirements: [REQ-02, REQ-04, REQ-06, REQ-07]
must_haves:
  truths:
    - "SightReadingGame renders the shared ProgressBar (session exercise N of M) and shared ScorePill in place of its custom inline progress bar and score label"
    - "MemoryGame renders the shared ProgressBar (pairs found) and shared ScorePill in place of its inline rounded-lg score display"
    - "Neither game adopts lives/combo/on-fire (base shell only per D-01/D-02); existing mechanics unchanged"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "Base-shell adoption for sight-reading"
      contains: "shared/hud/ProgressBar"
    - path: "src/components/games/notes-master-games/MemoryGame.jsx"
      provides: "Base-shell adoption for memory game"
      contains: "shared/hud/ScorePill"
  key_links:
    - from: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      to: "src/components/games/shared/hud/ProgressBar.jsx"
      via: "named import + session counters"
      pattern: "from \"\\.\\./shared/hud/ProgressBar\""
    - from: "src/components/games/notes-master-games/MemoryGame.jsx"
      to: "src/components/games/shared/hud/ScorePill.jsx"
      via: "named import + score value"
      pattern: "from \"\\.\\./shared/hud/ScorePill\""
---

<objective>
Wave 2 (D-01) base-shell rollout, cluster A: SightReadingGame and MemoryGame adopt the shared
`ProgressBar` and `ScorePill`. These are the two non-rhythm games that already render their own
inline progress/score UI, so adoption is a REPLACE (not an add). Base shell only — no
lives/combo/on-fire (D-02: slow/staff and grid games stay calm).

Purpose: Bring SightReading and Memory up to the NotesRecognition base-shell standard.
Output: Two games consuming shared ProgressBar + ScorePill; their custom inline equivalents removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@CLAUDE.md

<interfaces>
Shared components (created in Plan 36-02):
- `export function ProgressBar({ current, total })` — from `../shared/hud/ProgressBar`
- `export const ScorePill = React.forwardRef(({ value, label="XP", comboTint=0, ... }, ref))` — from `../shared/hud/ScorePill`

Both game dirs are at `src/components/games/<subdir>/`, so the import path is `../shared/hud/X`.

Per-game wiring (from 36-RESEARCH.md Per-Game Adoption State Map — verify exact names in the file):

- SightReadingGame: ProgressBar current=`currentExerciseNumber`, total=`sessionTotalExercises`
  (both from `useSightReadingSession`); ScorePill value=`sessionTotalScore`, label="Score".
- MemoryGame: ProgressBar current=`matchedIndexes.length / 2`, total=`cards.length / 2`;
  ScorePill value=`score`, label="XP".
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: SightReadingGame adopts shared ProgressBar + ScorePill</name>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx — read the top-bar/HUD render region (RESEARCH cites lines ~3497–3530) AND the `useSightReadingSession` destructure to confirm `currentExerciseNumber`, `sessionTotalExercises`, `sessionTotalScore` exist with those names. Do not assume — confirm in the file.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "SightReadingGame (base shell only)" section (lines ~418–434).
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — Wave 2 common pattern + state-wiring table (lines ~640–660).
  </read_first>
  <action>
Edit `src/components/games/sight-reading-game/SightReadingGame.jsx`:

1. Add imports:

```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```

2. Replace the existing custom inline progress bar (the `h-1 bg-indigo-300` CSS-transition bar
   with its exercise counter text) with:

```jsx
<ProgressBar current={currentExerciseNumber} total={sessionTotalExercises} />
```

Use the actual session-counter variable names you confirmed in read_first. If the session
exposes a 0-indexed current, pass it as-is (ProgressBar internally renders `current+1` in the
counter). If the exercise number is already 1-indexed, pass `currentExerciseNumber - 1` so the
counter reads correctly — verify which by checking the existing counter text expression. 3. Replace the inline score label with:

```jsx
<ScorePill value={sessionTotalScore} label={t("games.score")} />
```

If `t("games.score")` does not exist in BOTH `src/locales/en/common.json` and
`src/locales/he/common.json`, add it in lockstep (EN "Score" + the matching Hebrew) — REQ-07.
(RESEARCH notes `games.score` likely already exists; confirm before adding.) 4. Do NOT add lives/combo/on-fire. Do NOT touch the staff `progressFraction` scroll (a different
concept from session progress — both coexist). Do NOT touch the mic listen button.
</action>
<acceptance_criteria> - SightReadingGame.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`. - SightReadingGame.jsx renders `<ProgressBar current={` and `<ScorePill value={`. - The old `h-1` inline progress bar markup is removed (grep for `h-1` in the former progress-bar block returns 0 in that region). - If a new label key was added: `src/locales/he/common.json` contains the same key path as `src/locales/en/common.json` (parity). - `npx vitest run src/components/games/sight-reading-game` exits 0.
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/sight-reading-game</automated>
</verify>
<done>SightReadingGame consumes shared ProgressBar + ScorePill; custom inline progress bar removed; no engagement added; locale parity preserved.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryGame adopts shared ProgressBar + ScorePill</name>
  <read_first>
    - src/components/games/notes-master-games/MemoryGame.jsx — read the header/score render region (RESEARCH cites lines ~882–900) and confirm `matchedIndexes`, `cards`, and `score` variable names.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "MemoryGame (base shell only)" section (lines ~512–527) including Pitfall 6 (rounded-lg → rounded-full is an intentional, accepted visual change per REQ-04).
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — Wave 2 state-wiring table (line ~659, MemoryGame row).
  </read_first>
  <action>
Edit `src/components/games/notes-master-games/MemoryGame.jsx`:

1. Add imports:

```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```

2. Add a `<ProgressBar current={matchedIndexes.length / 2} total={cards.length / 2} />` in the
   header area to show pairs-found progress (MemoryGame currently lacks a progress bar).
3. Replace the inline `rounded-lg border border-white/20 bg-white/10` score display with:

```jsx
<ScorePill value={score} label="XP" />
```

The corner-radius change to `rounded-full` is intentional design-system alignment (REQ-04,
accepted per Pitfall 6) — do not try to preserve `rounded-lg`. 4. Keep the existing Timer/XP header behavior otherwise. Do NOT add lives/combo/on-fire. Do NOT
touch the card grid mechanics.
</action>
<acceptance_criteria> - MemoryGame.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`. - MemoryGame.jsx renders `<ProgressBar current={matchedIndexes.length / 2}` and `<ScorePill value={score}`. - The former inline `rounded-lg` score pill block is removed (no `rounded-lg` remains on the score display element). - `npx vitest run src/components/games/notes-master-games/MemoryGame` exits 0 (if a MemoryGame test exists; otherwise run the notes-master-games dir).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/notes-master-games</automated>
</verify>
<done>MemoryGame consumes shared ProgressBar + ScorePill (rounded-full); no engagement added; grid mechanics untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational HUD adoption fed by each game's existing session state.

## STRIDE Threat Register

| Threat ID | Category | Component                        | Disposition | Mitigation Plan                                                                                                                                        |
| --------- | -------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-36-04   | N/A      | SightReading/Memory HUD adoption | accept      | No new attack surface — swapping inline presentational markup for shared presentational components; no new user input, network calls, or PII handling. |

</threat_model>

<verification>
- `npx vitest run src/components/games/sight-reading-game src/components/games/notes-master-games` → green.
- `npm run lint` → no new errors on the two files.
- Locale parity check if any key added (EN ↔ HE same paths).
- MANUAL (folded into final checkpoint 36-11): both games show a NotesRecognition-style progress
  bar + score pill; RTL + reduced-motion correct.
</verification>

<success_criteria>
SightReadingGame and MemoryGame consume the shared base shell; their custom inline progress/score
UI is removed; no engagement layer added; existing tests green; locale parity intact.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-04-SUMMARY.md`.
</output>
