---
phase: 36-game-screen-ui-unification
plan: 06
type: execute
wave: 4
depends_on: ["36-03"]
files_modified:
  - src/components/games/rhythm-games/MixedLessonGame.jsx
  - src/components/games/rhythm-games/MetronomeTrainer.jsx
autonomous: true
requirements: [REQ-02, REQ-04, REQ-06, REQ-07]
must_haves:
  truths:
    - "MixedLessonGame's divergent renderProgressBar() is deleted and replaced by the shared ProgressBar (REQ-04, D-03)"
    - "MixedLessonGame renders the shared ScorePill (correct-answer count)"
    - "MetronomeTrainer (trail/exercise mode) renders the shared ProgressBar and ScorePill"
    - "Both keep landscape-lock and metronome/lesson mechanics unchanged; no lives/combo/on-fire (D-02)"
  artifacts:
    - path: "src/components/games/rhythm-games/MixedLessonGame.jsx"
      provides: "Unified progress bar (REQ-04) + base-shell adoption"
      contains: "shared/hud/ProgressBar"
    - path: "src/components/games/rhythm-games/MetronomeTrainer.jsx"
      provides: "Base-shell adoption for metronome trail mode"
      contains: "shared/hud/ProgressBar"
  key_links:
    - from: "src/components/games/rhythm-games/MixedLessonGame.jsx"
      to: "src/components/games/shared/hud/ProgressBar.jsx"
      via: "named import replacing renderProgressBar()"
      pattern: "from \"\\.\\./shared/hud/ProgressBar\""
    - from: "src/components/games/rhythm-games/MetronomeTrainer.jsx"
      to: "src/components/games/shared/hud/ScorePill.jsx"
      via: "named import + exerciseProgress.totalScore"
      pattern: "from \"\\.\\./shared/hud/ScorePill\""
---

<objective>
Wave 2 (D-01/D-03) base-shell rollout, cluster C: MixedLessonGame and MetronomeTrainer adopt the
shared `ProgressBar` and `ScorePill`. MixedLessonGame additionally satisfies REQ-04 by deleting
its divergent green `renderProgressBar()` and unifying onto the shared gradient bar. Base shell
only — no engagement (D-02). Landscape-lock and mechanics preserved (REQ-06).

Purpose: Unify MixedLesson's progress bar (REQ-04) and bring both games to the base-shell standard.
Output: Two games consuming shared ProgressBar + ScorePill; MixedLesson's custom bar removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@CLAUDE.md

<interfaces>
Shared components (from Plan 36-02): `ProgressBar({current,total})`, `ScorePill({value,label,...})`.
Import path from `rhythm-games/`: `../shared/hud/X`.

Per-game wiring (from 36-RESEARCH.md / 36-PATTERNS.md — confirm names in each file):

- MixedLessonGame: ProgressBar current=`currentIndex`, total=`questions.length`; ScorePill
  value=`results.filter(Boolean).length`, label="Score". DELETE the `renderProgressBar()`
  function (RESEARCH lines 656–675 / PATTERNS lines 662–673).
- MetronomeTrainer: ProgressBar current=`exerciseProgress.currentExercise`,
  total=`exerciseProgress.totalExercises`; ScorePill value=`exerciseProgress.totalScore`,
  label="Score". Trail/exercise mode only.

LOCALE KEY PITFALL (RESEARCH Pitfall 4): the shared ProgressBar uses
`t("noteRecognition.questionProgress", {current, total})` for its counter. MixedLessonGame's old
bar used `t("mixedLesson.progressLabel")` as an aria-label. DECISION: adopt the shared component's
`noteRecognition.questionProgress` counter as canonical (semantically equivalent "Question N of N").
The shared ProgressBar replaces MixedLesson's bar AND its aria-label. Do NOT add a labelKey prop.
Both locales already have `noteRecognition.questionProgress` — no new key needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MixedLessonGame — replace renderProgressBar() with shared ProgressBar (REQ-04) + add ScorePill</name>
  <read_first>
    - src/components/games/rhythm-games/MixedLessonGame.jsx — read the `renderProgressBar` function (RESEARCH cites lines ~656–675) and ALL its call sites; confirm `currentIndex`, `questions`, `results`; confirm `useDeclareNeedsLandscape(...)` location.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "MixedLessonGame" section (lines ~464–496) incl. visual deltas and Pitfall 4.
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "MixedLessonGame — additionally delete renderProgressBar()" (lines ~661–673).
  </read_first>
  <action>
Edit `src/components/games/rhythm-games/MixedLessonGame.jsx`:
1. Add imports:
```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```
2. DELETE the entire `renderProgressBar` function (the `h-2 bg-green-400` CSS-transition bar with
   the `{currentIndex}/{questions.length}` span). Replace every call site `{renderProgressBar()}`
   with:
```jsx
<ProgressBar current={currentIndex} total={questions.length} />
```
   This unifies the bar onto the shared gradient (`h-4`, indigo→violet→fuchsia, Framer spring,
   5 checkpoint dots) — REQ-04. The old `mixedLesson.progressLabel` aria-label is intentionally
   dropped in favor of the shared `noteRecognition.questionProgress` counter (Pitfall 4 decision;
   no new locale key).
3. Add `<ScorePill value={results.filter(Boolean).length} label={t("games.score")} />` to the HUD
   (MixedLesson currently shows per-question score only). Add `games.score` to both locales in
   lockstep if missing (REQ-07).
4. Do NOT add lives/combo/on-fire. Do NOT touch the mixed-lesson question renderers or the
   crossfade `key` logic. Do NOT move/remove `useDeclareNeedsLandscape(...)`.
  </action>
  <acceptance_criteria>
    - MixedLessonGame.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`.
    - MixedLessonGame.jsx no longer defines `renderProgressBar` (grep, comment-filtered: `grep -v '^\s*//' | grep -c 'renderProgressBar'` returns 0) and no longer contains `bg-green-400` in the former progress-bar block.
    - MixedLessonGame.jsx renders `<ProgressBar current={currentIndex} total={questions.length}` and `<ScorePill value={results`.
    - MixedLessonGame.jsx STILL contains `useDeclareNeedsLandscape(`.
    - `npx vitest run src/components/games/rhythm-games/MixedLessonGame` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/MixedLessonGame</automated>
  </verify>
  <done>MixedLessonGame's divergent bar removed and unified onto shared ProgressBar (REQ-04); shared ScorePill added; mechanics + landscape lock intact.</done>
</task>

<task type="auto">
  <name>Task 2: MetronomeTrainer (trail mode) adopts shared ProgressBar + ScorePill</name>
  <read_first>
    - src/components/games/rhythm-games/MetronomeTrainer.jsx — read the compact header region (RESEARCH cites lines ~1397–1421) and confirm `exerciseProgress.currentExercise`, `.totalExercises`, `.totalScore`; confirm `useDeclareNeedsLandscape(...)` location.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "MetronomeTrainer" section (lines ~497–510).
  </read_first>
  <action>
Edit `src/components/games/rhythm-games/MetronomeTrainer.jsx`:
1. Add imports:
```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```
2. In trail/exercise mode, replace the inline
   `exerciseProgress.currentExercise of totalExercises` counter text with
   `<ProgressBar current={exerciseProgress.currentExercise} total={exerciseProgress.totalExercises} />`.
   If `currentExercise` is 1-indexed, pass `exerciseProgress.currentExercise - 1` so the internal
   `current+1` counter reads correctly — verify against the existing text expression.
3. Add `<ScorePill value={exerciseProgress.totalScore} label={t("games.score")} />` (live-updating
   as exercises complete). Add `games.score` to both locales in lockstep if missing (REQ-07).
4. Only adopt in trail/exercise mode — do NOT alter the free-practice metronome layout. Do NOT add
   lives/combo/on-fire. Do NOT move/remove `useDeclareNeedsLandscape(...)`.
  </action>
  <acceptance_criteria>
    - MetronomeTrainer.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`.
    - MetronomeTrainer.jsx renders `<ProgressBar current={exerciseProgress` and `<ScorePill value={exerciseProgress.totalScore`.
    - MetronomeTrainer.jsx STILL contains `useDeclareNeedsLandscape(`.
    - `npx vitest run src/components/games/rhythm-games/MetronomeTrainer` exits 0 (or rhythm-games dir).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/MetronomeTrainer</automated>
  </verify>
  <done>MetronomeTrainer trail mode consumes shared base shell; free-practice layout + landscape lock untouched; no engagement added.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational HUD adoption fed by each game's existing session state.

## STRIDE Threat Register

| Threat ID | Category | Component                          | Disposition | Mitigation Plan                                                                                                               |
| --------- | -------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-36-06   | N/A      | MixedLesson/Metronome HUD adoption | accept      | No new attack surface — presentational component swap; no new user input, network, or PII. Landscape-lock preserved (REQ-06). |

</threat_model>

<verification>
- `npx vitest run src/components/games/rhythm-games/MixedLessonGame src/components/games/rhythm-games/MetronomeTrainer` → green.
- `npm run lint` → no new errors.
- `useDeclareNeedsLandscape(` present in both files (grep).
- MANUAL (folded into 36-11): MixedLesson bar now matches NotesRecognition; metronome trail mode
  shows shared progress + score; landscape prompt intact; RTL + reduced-motion correct.
</verification>

<success_criteria>
MixedLessonGame's divergent progress bar is unified onto the shared ProgressBar (REQ-04) and both
games consume the shared base shell; landscape lock + mechanics preserved; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-06-SUMMARY.md`.
</output>
