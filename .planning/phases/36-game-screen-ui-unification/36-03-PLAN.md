---
phase: 36-game-screen-ui-unification
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified: []
autonomous: false
requirements: [REQ-01, REQ-06]
must_haves:
  truths:
    - "The owner has confirmed NotesRecognitionGame's base shell (progress bar, score pill, timer) renders and animates identically to before extraction"
    - "Rollout to other games (Wave 2) does not begin until this gold-standard reference is confirmed regression-free"
  artifacts: []
  key_links: []
---

<objective>
Wave 1 gate (D-05/D-06): Confirm the gold-standard reference (NotesRecognitionGame) came out of
the base-shell extraction with zero visual/behavioral regression BEFORE the base shell is rolled
out to eight other games. There are no full-render unit tests for NotesRecognitionGame's HUD, so
visual parity is owner-verified (per 36-VALIDATION.md "Manual-Only Verifications"). This is the
smallest-blast-radius checkpoint: catch any extraction defect on one screen before it multiplies.

Purpose: Human verification that the base-shell extraction is regression-free.
Output: Owner approval to proceed to Wave 2 rollout (or a defect list to fix in 36-02 first).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner verifies NotesRecognitionGame base-shell zero-regression</name>
  <what-built>
Plan 36-02 extracted ProgressBar, ScorePill, and TimerDisplay from NotesRecognitionGame into
`src/components/games/shared/hud/` and refactored NotesRecognitionGame to consume them. The
engagement layer (lives, combo, on-fire, speed-bonus, tier-up) was intentionally left inline and
unchanged this wave. Automated contract tests for ProgressBar and ScorePill pass; the existing
NotesRecognition test passes.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` (port 5174) and open the app.
2. Start a NotesRecognition session (Notes Master mode → a note-recognition node, or timed mode).
3. Confirm the BASE SHELL renders and animates exactly as before:
   - Progress bar: 16px gradient bar (indigo→violet→fuchsia), 5 checkpoint dots that activate as
     you progress, "Question N of N" counter beneath it.
   - Score pill (XP): value updates; combo tint shifts amber at combo ≥ 3 and yellow at ≥ 8;
     the floating "+score" animates upward on a correct answer.
   - Timed mode: the Time pill shows the clock and counts.
4. Confirm the ENGAGEMENT layer (still inline) is also intact: hearts, combo pill, on-fire flame,
   speed-bonus FAST! flash, and the DOUBLE/TRIPLE XP tier-up popup flying to the score pill.
5. Toggle reduced-motion (app Settings → Accessibility) and confirm animations are suppressed
   appropriately (progress bar snaps, no shake/fly).
6. Switch language to Hebrew and confirm the HUD mirrors correctly (RTL) with numbers still LTR.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2 base-shell rollout, or describe any visual/behavioral difference for Plan 36-02 to fix first.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — verification-only checkpoint, no code change.

## STRIDE Threat Register

| Threat ID | Category | Component  | Disposition | Mitigation Plan                                                      |
| --------- | -------- | ---------- | ----------- | -------------------------------------------------------------------- |
| T-36-03   | N/A      | checkpoint | accept      | No attack surface — human verification step, no code or data change. |

</threat_model>

<verification>
Owner confirms base-shell parity and engagement-layer intactness on NotesRecognitionGame.
</verification>

<success_criteria>
Owner approves the reference extraction as zero-regression; Wave 2 rollout is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-03-SUMMARY.md` recording the approval (or the defect list returned).
</output>
