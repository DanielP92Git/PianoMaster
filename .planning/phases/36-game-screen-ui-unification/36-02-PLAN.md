---
phase: 36-game-screen-ui-unification
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/components/games/shared/hud/ProgressBar.jsx
  - src/components/games/shared/hud/ScorePill.jsx
  - src/components/games/shared/hud/TimerDisplay.jsx
  - src/components/games/notes-master-games/NotesRecognitionGame.jsx
autonomous: true
requirements: [REQ-01, REQ-04, REQ-06]
must_haves:
  truths:
    - "ProgressBar, ScorePill, TimerDisplay exist as named-export components under shared/hud/ with the contract Plan 36-01's tests assert"
    - "NotesRecognitionGame imports ProgressBar/ScorePill/TimerDisplay from shared/hud and no longer declares them inline"
    - "NotesRecognitionGame renders identically (base-shell elements) before and after the swap; engagement-layer JSX is untouched in this wave"
    - "Plan 36-01's ProgressBar and ScorePill tests are now GREEN"
  artifacts:
    - path: "src/components/games/shared/hud/ProgressBar.jsx"
      provides: "X-of-N progress bar with gradient fill + 5 checkpoint dots"
      contains: "export function ProgressBar"
    - path: "src/components/games/shared/hud/ScorePill.jsx"
      provides: "Configurable score pill with comboTint + floating score, forwardRef for fly-to"
      contains: "React.forwardRef"
    - path: "src/components/games/shared/hud/TimerDisplay.jsx"
      provides: "Timed-mode clock pill"
      contains: "export function TimerDisplay"
  key_links:
    - from: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      to: "src/components/games/shared/hud/ProgressBar.jsx"
      via: "named import"
      pattern: "from \"\\.\\./shared/hud/ProgressBar\""
    - from: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      to: "src/components/games/shared/hud/ScorePill.jsx"
      via: "named import + scorePillRef attached"
      pattern: "from \"\\.\\./shared/hud/ScorePill\""
---

<objective>
Wave 1 (D-05): Extract the base-shell components (ProgressBar, ScorePill, TimerDisplay) into
`src/components/games/shared/hud/` as verbatim lifts from NotesRecognitionGame, then refactor
NotesRecognitionGame to consume them — with ZERO visual/behavioral regression on the base shell.
This proves the extraction on the gold-standard reference before any rollout (D-06 smallest blast
radius). The engagement-layer JSX (lives, combo, on-fire, speed-bonus, tier-up) stays INLINE in
NotesRecognitionGame this wave; it is extracted in Wave 3 (Plan 36-08).

Turning Plan 36-01's RED tests GREEN is the automated proof that the extracted components match
the locked contract.

Purpose: Establish the shared base-shell components against the reference; prove zero regression.
Output: 3 new hud/ components + a NotesRecognitionGame that imports them for the base shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@CLAUDE.md

<interfaces>
Verbatim source-of-truth lines in NotesRecognitionGame.jsx (the extraction source):
- TimerDisplay inline component: lines 295–308 (props: `formattedTime`)
- StageCard inline component: lines 310–318 — LEAVE INLINE (not in D-12 list; local layout wrapper)
- ProgressBar inline component: lines 320–371 (props: `current`, `total`)
- ScorePill inline JSX + floating score: lines 2357–2412 (uses `scorePillRef`, `progress.score`, `combo`, `floatingScore`, `floatingScoreKey`)

Import path from hud/ to utils (4 levels up): `../../../../utils/useMotionTokens`
Named-export convention (no default): see src/components/games/shared/AudioInterruptedOverlay.jsx.
ScorePill MUST use `React.forwardRef` so the parent keeps `scorePillRef` for the Wave 3 TierUpPopup fly-to (Pitfall 1 in RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProgressBar.jsx, ScorePill.jsx, TimerDisplay.jsx (turn 36-01 tests GREEN)</name>
  <behavior>
    - ProgressBar({current,total}): fill scaleX = min(1, current/total); 5 checkpoint dots active (bg-white/80) when progressPercent >= dot%; counter "Question N of N".
    - ScorePill({value,label="XP",comboTint=0,floatingScore=null,floatingScoreKey=0}, ref): renders label+value; tint tiers 0/1/2 map to white/amber/yellow glass classes; floating +score only when floatingScore !== null; forwardRef on outer wrapper.
    - TimerDisplay({formattedTime}): Clock3 pill showing label t("games.time") + formattedTime || "00:00".
  </behavior>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — ProgressBar.jsx (lines ~41–113), ScorePill.jsx (lines ~117–188), TimerDisplay.jsx (lines ~512–536). These contain the VERBATIM bodies to lift.
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx lines 295–371 (TimerDisplay + ProgressBar inline source) and 2357–2412 (ScorePill inline source).
    - src/components/games/shared/AudioInterruptedOverlay.jsx — named-export + JSDoc header convention.
  </read_first>
  <action>
Create three files under `src/components/games/shared/hud/`. Copy the verbatim bodies from
36-PATTERNS.md (they were lifted line-for-line from NotesRecognitionGame).

**ProgressBar.jsx** — named export `export function ProgressBar({ current, total })`. Imports:

```jsx
import { useTranslation } from "react-i18next";
import { motion } from "framer-motion";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

Body verbatim from 36-PATTERNS.md lines 70–110: `h-4 rounded-full bg-white/10` track, gradient
fill `from-indigo-400 via-violet-400 to-fuchsia-400` animated `animate={{ scaleX: progressPercent/100 }}`
with `transition={soft}` `initial={false}`, 5 checkpoint dots, counter
`t("noteRecognition.questionProgress", { current: Math.min(total, Math.max(1, current+1)), total })`.
`soft` from useMotionTokens already encapsulates reduced-motion — no extra guard.

**ScorePill.jsx** — `export const ScorePill = React.forwardRef(function ScorePill({ value, label = "XP", comboTint = 0, floatingScore = null, floatingScoreKey = 0 }, ref) { ... })`. Imports:

```jsx
import React from "react";
import { AnimatePresence, motion } from "framer-motion";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

Use the `TINT` array (3 tiers) and body verbatim from 36-PATTERNS.md lines 150–185. Attach
`ref` to the outer `<div className="relative">`. The floating `+{floatingScore}` motion.span is
keyed by `floatingScoreKey` and only renders when `floatingScore !== null`.

**TimerDisplay.jsx** — `export function TimerDisplay({ formattedTime })`. Imports:

```jsx
import { useTranslation } from "react-i18next";
import { Clock3 } from "lucide-react";
```

Body verbatim from 36-PATTERNS.md lines 524–535.

Do NOT extract StageCard — it stays inline in NotesRecognitionGame (local layout wrapper, not in
the D-12 component list).
</action>
<acceptance_criteria> - `src/components/games/shared/hud/ProgressBar.jsx` contains `export function ProgressBar` and `from-indigo-400 via-violet-400 to-fuchsia-400` and `noteRecognition.questionProgress`. - `src/components/games/shared/hud/ScorePill.jsx` contains `React.forwardRef` and all three tint fragments `border-amber-400/30`, `border-yellow-400/40`, `border-white/20`. - `src/components/games/shared/hud/TimerDisplay.jsx` contains `export function TimerDisplay` and `games.time`. - `npx vitest run src/components/games/shared/hud/ProgressBar.test.jsx src/components/games/shared/hud/ScorePill.test.jsx` exits 0 (both GREEN).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/shared/hud/ProgressBar.test.jsx src/components/games/shared/hud/ScorePill.test.jsx</automated>
</verify>
<done>Three base-shell components exist as verbatim lifts; Plan 36-01's ProgressBar + ScorePill tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor NotesRecognitionGame to consume the base-shell components</name>
  <read_first>
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx — read lines 1–60 (imports), 290–384 (inline component declarations to delete), and 2334–2522 (HUD render section where ProgressBar/ScorePill/TimerDisplay are used). Do NOT read the whole 2600-line file; target these ranges.
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "NotesRecognitionGame.jsx (Wave 1 reference refactor)" section (lines ~579–636) for exact import block and ScorePill call-site wiring.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "HUD Layout in Render" (lines ~388–409) for placement.
  </read_first>
  <action>
Refactor `src/components/games/notes-master-games/NotesRecognitionGame.jsx` to import the three
base-shell components and delete their inline declarations. ENGAGEMENT JSX (lives, combo,
on-fire, speed-bonus, tier-up) and the `comboShake`/engagement state stay INLINE this wave —
do not touch them (they move in Plan 36-08).

1. Add imports (top of file, with the other component imports):

```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
import { TimerDisplay } from "../shared/hud/TimerDisplay";
```

2. DELETE the inline `TimerDisplay` declaration (lines 295–308) and inline `ProgressBar`
   declaration (lines 320–371). KEEP the inline `StageCard` (lines 310–318) — it stays local.

3. At the ScorePill render site (was lines 2357–2412), replace the inline pill + floating-score
   JSX with the component, keeping the existing `scorePillRef` attached via the forwarded ref and
   computing the tint tier in the parent:

```jsx
const comboTier = combo >= 8 ? 2 : combo >= 3 ? 1 : 0;
<ScorePill
  ref={scorePillRef}
  value={progress.score}
  label="XP"
  comboTint={comboTier}
  floatingScore={floatingScore}
  floatingScoreKey={floatingScoreKey}
/>;
```

4. At the ProgressBar render site, keep the existing call exactly:
   `<ProgressBar current={progress.totalQuestions} total={settings.timedMode ? 10 : 20} />`
   (use the existing current/total expressions already in the file — do not invent new ones).

5. At the TimerDisplay render site (timed mode), keep:
   `{settings.timedMode ? <TimerDisplay formattedTime={formattedTime} /> : null}`
   (or the existing conditional already present — just swap inline for the import).

Do NOT remove `comboShake`, `speedBonusKey`, `floatingScoreKey`, `scorePillRef`, or any
engagement state — engagement extraction is Wave 3. `floatingScoreKey` and `scorePillRef` are
still owned by the parent and passed into ScorePill now.

Run the existing NotesRecognition test to confirm the public export (`buildInitialTrailPool`)
is unaffected.
</action>
<acceptance_criteria> - NotesRecognitionGame.jsx contains `from "../shared/hud/ProgressBar"`, `from "../shared/hud/ScorePill"`, `from "../shared/hud/TimerDisplay"`. - NotesRecognitionGame.jsx no longer contains an inline `const ProgressBar = ` or `const TimerDisplay = ` declaration (grep returns 0 for `^const ProgressBar` and `^const TimerDisplay`). `grep -c` MUST filter comments: `grep -v '^\s*//' | grep -c`. - NotesRecognitionGame.jsx still contains `const StageCard` (StageCard NOT extracted). - NotesRecognitionGame.jsx ScorePill site renders `<ScorePill ref={scorePillRef}` with `comboTint={comboTier}`. - `npx vitest run src/components/games/notes-master-games/NotesRecognitionGame.autogrow.test.js` exits 0. - `npx vitest run src/components/games/shared/hud` exits 0 (36-01 base-shell tests green).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/notes-master-games/NotesRecognitionGame.autogrow.test.js src/components/games/shared/hud</automated>
</verify>
<done>NotesRecognitionGame imports the three base-shell components, inline ProgressBar/TimerDisplay deleted, StageCard kept inline, engagement untouched, existing tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary. Presentational component extraction; NotesRecognitionGame's existing
game state, scoring, and trail integration are unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component                  | Disposition | Mitigation Plan                                                                                                                                  |
| --------- | -------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-36-02   | N/A      | hud/ base-shell components | accept      | No new attack surface — presentational components fed by existing in-component numeric state; no new user input, network calls, or PII handling. |

</threat_model>

<verification>
- `npx vitest run src/components/games/shared/hud` → GREEN (ProgressBar + ScorePill tests pass).
- `npm run test:run` → full suite green (Wave 2 merge gate; LivesDisplay/ComboPill tests do NOT exist yet, so no spurious red).
- `npm run lint` → no new errors on the touched files.
- MANUAL (gated in Plan 36-03 checkpoint): play a NotesRecognition session and confirm the
  progress bar, score pill (incl. combo tint + floating +score), and timed-mode timer render and
  animate identically to before.
</verification>

<success_criteria>
The base-shell components exist under shared/hud and NotesRecognitionGame consumes them with no
inline base-shell declarations remaining (StageCard excepted). Automated contract tests pass;
existing NotesRecognition test passes; engagement layer is byte-for-byte unchanged this wave.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-02-SUMMARY.md`.
</output>
