---
phase: 36-game-screen-ui-unification
plan: 09
type: execute
wave: 6
depends_on: ["36-07"]
files_modified:
  - src/components/games/rhythm-games/ArcadeRhythmGame.jsx
autonomous: true
requirements: [REQ-03, REQ-05, REQ-06]
must_haves:
  truths:
    - "ArcadeRhythmGame renders the shared ComboPill and LivesDisplay instead of its inline combo badge and hearts"
    - "The combo >= 2 conditional-render guard stays in ArcadeRhythmGame (parent owns the threshold, not the component)"
    - "On-fire shows via ComboPill's isOnFire prop (Flame icon), not a separate OnFireBadge"
    - "The sacred useDeclareNeedsLandscape(isPhoneViewport) line is present and unchanged; INITIAL_LIVES/ON_FIRE_THRESHOLD/GAME_PHASES remain exported; lives→GameOver path preserved (REQ-05)"
  artifacts:
    - path: "src/components/games/rhythm-games/ArcadeRhythmGame.jsx"
      provides: "De-duplicated engagement HUD onto shared components"
      contains: "shared/hud/ComboPill"
  key_links:
    - from: "src/components/games/rhythm-games/ArcadeRhythmGame.jsx"
      to: "src/components/games/shared/hud/ComboPill.jsx"
      via: "named import; combo>=2 guard + isOnFire prop"
      pattern: "from \"\\.\\./shared/hud/ComboPill\""
    - from: "src/components/games/rhythm-games/ArcadeRhythmGame.jsx"
      to: "src/components/games/shared/hud/LivesDisplay.jsx"
      via: "named import; totalLives=INITIAL_LIVES"
      pattern: "from \"\\.\\./shared/hud/LivesDisplay\""
---

<objective>
Wave 3 (D-05/D-09): De-duplicate ArcadeRhythmGame's inline lives + combo + on-fire onto the shared
`LivesDisplay` and `ComboPill` (REQ-03), preserving its existing behavior exactly — including the
`combo >= 2` render guard, the Flame-icon-in-combo-pill on-fire style, the lives→GameOverScreen
path (REQ-05), the exported constants its tests import, and (critically) the landscape lock. The
shared LivesDisplay adds entry/exit Framer animations ArcadeRhythm currently lacks — this is an
accepted enhancement, not a regression (D-09 / RESEARCH A2).

Purpose: Single source of truth for lives/combo/on-fire; remove ArcadeRhythm's duplicate inline JSX.
Output: ArcadeRhythmGame consuming shared ComboPill + LivesDisplay with behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@.planning/phases/36-game-screen-ui-unification/35-CONTEXT.md
@CLAUDE.md

<interfaces>
Shared components (from Plan 36-07): `ComboPill({ combo, isOnFire = false })`,
`LivesDisplay({ lives, totalLives = 3 })`. Import path from `rhythm-games/`: `../shared/hud/X`.

ArcadeRhythmGame source (from 36-RESEARCH.md "ArcadeRhythmGame" section, lines ~564–617):

- Inline combo badge: lines 1157–1169 — renders only `combo >= 2`; Flame Lucide icon when isOnFire;
  Zap icon; tier tint.
- Inline lives: lines 1171–1186 — `Array.from({ length: INITIAL_LIVES })` hearts, no animation.
- SACRED: `useDeclareNeedsLandscape(isPhoneViewport)` at line 141 (imported from NeedsLandscapeContext,
  line 16). MUST remain before the return, unchanged (Pitfall 3).
- Exported constants used by ArcadeRhythmGame.test.js: `INITIAL_LIVES`, `ON_FIRE_THRESHOLD`,
  `GAME_PHASES` (Pitfall 7) — keep them exported from this file.

Behavioral preservation (D-09): the `combo >= 2` guard stays in the PARENT (not inside ComboPill);
on-fire routes through `ComboPill`'s `isOnFire` flag (NOT a separate OnFireBadge).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-dup ArcadeRhythmGame lives/combo/on-fire onto shared components</name>
  <read_first>
    - src/components/games/rhythm-games/ArcadeRhythmGame.jsx — read lines 1–60 (imports incl. NeedsLandscapeContext + lucide), lines 130–150 (the useDeclareNeedsLandscape call + isPhoneViewport), and lines 1119–1188 (the inline HUD header to replace). Confirm `combo`, `isOnFire`, `lives`, `INITIAL_LIVES` names.
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "ArcadeRhythmGame (de-dup)" section (lines ~710–736) for the exact replacement snippets and sacred-line note.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "ArcadeRhythmGame" section + Pitfall 3 (landscape) + Pitfall 7 (exported constants).
  </read_first>
  <action>
Edit `src/components/games/rhythm-games/ArcadeRhythmGame.jsx`:

1. Add imports:

```jsx
import { ComboPill } from "../shared/hud/ComboPill";
import { LivesDisplay } from "../shared/hud/LivesDisplay";
```

2. Replace the inline combo badge JSX (lines ~1157–1169) — the
   `{combo >= 2 && <div className="rounded-full border ...">...<Zap/></div>}` block — with:

```jsx
{
  combo >= 2 && <ComboPill combo={combo} isOnFire={isOnFire} />;
}
```

KEEP the `combo >= 2 &&` guard in the parent (D-09 — the threshold is game-specific, not the
component's concern). On-fire flows through `isOnFire` (ComboPill shows the Flame icon) — do
NOT add a separate OnFireBadge here.

3. Replace the inline lives JSX (lines ~1171–1186) — the
   `Array.from({ length: INITIAL_LIVES }).map(... <Heart .../>)` block — with:

```jsx
<LivesDisplay lives={lives} totalLives={INITIAL_LIVES} />
```

The shared LivesDisplay adds Framer entry/exit animations ArcadeRhythm lacked — accepted
enhancement per D-09, not a regression.

4. If `Zap`/`Heart`/`Flame` Lucide imports are now unused in ArcadeRhythmGame, remove only the
   ones that are genuinely unused elsewhere in the file (check first — `Flame` may be used in
   other ArcadeRhythm UI). Do not remove imports still referenced.

5. DO NOT TOUCH: `useDeclareNeedsLandscape(isPhoneViewport)` (line ~141) and its import. DO NOT
   remove or relocate the `INITIAL_LIVES`, `ON_FIRE_THRESHOLD`, `GAME_PHASES` exports. DO NOT
   change the lives-decrement / GameOverScreen logic (REQ-05).

Run the existing ArcadeRhythmGame test to confirm constants, lives logic, and the lives=0 →
GameOverScreen behavior still pass.
</action>
<acceptance_criteria> - ArcadeRhythmGame.jsx contains `from "../shared/hud/ComboPill"` and `from "../shared/hud/LivesDisplay"`. - ArcadeRhythmGame.jsx renders `{combo >= 2 && <ComboPill combo={combo} isOnFire={isOnFire} />}` (the `combo >= 2` guard remains in the parent) and `<LivesDisplay lives={lives} totalLives={INITIAL_LIVES} />`. - ArcadeRhythmGame.jsx STILL contains `useDeclareNeedsLandscape(isPhoneViewport)` (sacred line, exact). - ArcadeRhythmGame.jsx STILL exports `INITIAL_LIVES`, `ON_FIRE_THRESHOLD`, and `GAME_PHASES`. - The former inline hearts `Array.from({ length: INITIAL_LIVES }).map` HUD block and the inline combo `<Zap` badge block are removed from the header region. - `npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame` exits 0 (constants, lives logic, GameOver-at-0 all pass).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame</automated>
</verify>
<done>ArcadeRhythmGame uses shared ComboPill (combo>=2 guard + isOnFire) and LivesDisplay; sacred landscape line + exported constants + GameOver path preserved; tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational de-dup; ArcadeRhythm's game logic, lives, GameOver routing,
and landscape behavior unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component                      | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | -------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-09   | N/A      | ArcadeRhythm engagement de-dup | accept      | No new attack surface — presentational component swap; no new user input, network, or PII. Landscape-lock (useDeclareNeedsLandscape) and lives→GameOver routing preserved (REQ-05, REQ-06). |

</threat_model>

<verification>
- `npx vitest run src/components/games/rhythm-games/ArcadeRhythmGame` → green (existing 12 tests).
- `npm run lint` → no new errors (incl. no unused-import errors from removed Lucide icons).
- `useDeclareNeedsLandscape(isPhoneViewport)` present (grep, exact).
- MANUAL (gated in 36-11): on iPhone SE + iPad — ArcadeRhythm still triggers the landscape prompt;
  combo pill appears only at combo ≥ 2, turns to Flame on-fire; hearts deplete to GameOver; behavior
  matches pre-de-dup (plus the new heart animations).
</verification>

<success_criteria>
ArcadeRhythmGame's inline lives/combo/on-fire are replaced by shared ComboPill + LivesDisplay with
behavior preserved exactly (D-09); sacred landscape line, exported constants, and GameOver path
intact; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-09-SUMMARY.md`.
</output>
