---
phase: 36-game-screen-ui-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/shared/hud/ProgressBar.test.jsx
  - src/components/games/shared/hud/ScorePill.test.jsx
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "A failing (RED) ProgressBar test exists that asserts fill %, checkpoint-dot active states, and counter text against the shared contract"
    - "A failing (RED) ScorePill test exists that asserts value, label, and comboTint tier classes (0/1/2) against the shared contract"
    - "Both tests are RED only because the components do not exist yet (import resolution), not because of bad assertions"
  artifacts:
    - path: "src/components/games/shared/hud/ProgressBar.test.jsx"
      provides: "Contract test for ProgressBar (guards Wave 1 extraction)"
      contains: 'describe("ProgressBar"'
    - path: "src/components/games/shared/hud/ScorePill.test.jsx"
      provides: "Contract test for ScorePill (guards Wave 1 extraction)"
      contains: "comboTint"
  key_links:
    - from: "ProgressBar.test.jsx"
      to: "src/components/games/shared/hud/ProgressBar.jsx (created in Wave 1)"
      via: 'import { ProgressBar } from "./ProgressBar"'
      pattern: "from \"\\./ProgressBar\""
    - from: "ScorePill.test.jsx"
      to: "src/components/games/shared/hud/ScorePill.jsx (created in Wave 1)"
      via: 'import { ScorePill } from "./ScorePill"'
      pattern: "from \"\\./ScorePill\""
---

<objective>
Create the base-shell HUD component contract tests BEFORE the components are extracted, so the
Wave 1 NotesRecognitionGame reference refactor is provably zero-regression against a fixed
contract. This is the "Wave 0" gate the RESEARCH recommended, scoped to the two base-shell
components extracted in Wave 1 (ProgressBar, ScorePill).

These tests are RED on completion of this plan — that is intended. They import from
`./ProgressBar` and `./ScorePill`, which do not exist until Plan 36-02. Plan 36-02 turns them
GREEN by creating the components to match this contract.

Engagement-layer component tests (LivesDisplay, ComboPill) are deliberately NOT created here —
their components are extracted in Wave 3, and creating their tests now would leave the full
suite RED across Waves 1–2 (defeating the per-wave "full suite green" gate REQ-06). They are
created at the start of the Wave 3 engagement plan (36-07) instead.

Purpose: Lock the ProgressBar/ScorePill API as a test contract before extraction.
Output: Two RED test files under `src/components/games/shared/hud/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-VALIDATION.md

<interfaces>
Component contracts the tests assert against (these are what Plan 36-02 will create):

ProgressBar — `export function ProgressBar({ current, total })`

- Renders a fill element whose width/scaleX corresponds to `Math.min(100, (current/total)*100)`.
- Renders 5 checkpoint dots at 0/25/50/75/100; a dot is "active" (class `bg-white/80`) when
  `progressPercent >= dotPercent`, otherwise `bg-white/10`.
- Renders counter text from `t("noteRecognition.questionProgress", { current: Math.min(total, Math.max(1, current+1)), total })`.

ScorePill — `export const ScorePill = React.forwardRef(function ScorePill({ value, label="XP", comboTint=0, floatingScore=null, floatingScoreKey=0 }, ref))`

- Renders `label` and `value`.
- Applies tint classes by `comboTint`: 0 → `border-white/20 bg-white/10`; 1 → `border-amber-400/30 bg-amber-500/15`; 2 → `border-yellow-400/40 bg-yellow-500/20`.
- Shows a `+{floatingScore}` element only when `floatingScore !== null`.
  </interfaces>

Test mock scaffolding is specified verbatim in 36-PATTERNS.md "Test Mock Scaffolding" section
(lines ~786–808) and the ProgressBar.test.jsx pattern (lines ~540–575).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProgressBar.test.jsx (RED contract test)</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "ProgressBar.test.jsx (test)" section (lines ~540–575) for the exact mock scaffolding, and the "ProgressBar.jsx" core pattern (lines ~70–110) for the contract being asserted.
    - .planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md — Component Inventory ProgressBar row + Animation Contract row (the contract under test).
    - src/components/games/rhythm-games/ArcadeRhythmGame.test.js (first ~60 lines) — the project's vitest + Testing Library + vi.mock conventions to mirror.
  </read_first>
  <action>
Create `src/components/games/shared/hud/ProgressBar.test.jsx`. Import from the not-yet-existing
sibling `./ProgressBar` (the import resolution failure is the intended RED).

Use this exact mock scaffolding (from 36-PATTERNS.md). `motion.div` must forward `className`,
`style`, and children so width/percentage assertions work:

```jsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import React from "react";
import { ProgressBar } from "./ProgressBar";

vi.mock("framer-motion", () => ({
  motion: {
    div: ({ children, className, style }) => (
      <div className={className} style={style}>
        {children}
      </div>
    ),
    span: ({ children, className }) => (
      <span className={className}>{children}</span>
    ),
  },
  AnimatePresence: ({ children }) => <>{children}</>,
  useReducedMotion: vi.fn(() => false),
}));

vi.mock("react-i18next", () => ({
  useTranslation: vi.fn(() => ({
    t: (k, opts) => (opts ? `${opts.current} of ${opts.total}` : k),
  })),
}));

// useMotionTokens lives 4 levels up from hud/: ../../../../utils/useMotionTokens
vi.mock("../../../../utils/useMotionTokens", () => ({
  useMotionTokens: vi.fn(() => ({ soft: { duration: 0 }, reduce: false })),
}));
```

Write three tests under `describe("ProgressBar", ...)`:

1. `it("animates fill to the correct fraction of current/total")` — render `<ProgressBar current={3} total={10} />`; query the gradient fill element (the one with class containing `from-indigo-400`) and assert it exists. (Width is driven by `animate={{ scaleX }}` which the mock drops, so assert the fill node renders and the counter math is correct rather than pixel width.)
2. `it("marks checkpoint dots active up to current progress")` — render `<ProgressBar current={5} total={10} />` (50%); assert that at least the 0/25/50 dots carry the active class fragment `bg-white/80` and the 75/100 dots carry `bg-white/10`. Query dots by their `rounded-full border` class via container.querySelectorAll.
3. `it("shows the question counter text")` — render `<ProgressBar current={2} total={10} />`; assert `screen.getByText("3 of 10")` is present (counter uses `Math.min(total, Math.max(1, current+1))` = 3).
   </action>
   <acceptance_criteria> - File `src/components/games/shared/hud/ProgressBar.test.jsx` exists and contains `describe("ProgressBar"` and `import { ProgressBar } from "./ProgressBar"`. - File contains all three `it(` blocks named per the action. - `npx vitest run src/components/games/shared/hud/ProgressBar.test.jsx` exits NON-zero (RED) — failing because `./ProgressBar` does not resolve, NOT because of a syntax error in the test file. (The error message references the missing `./ProgressBar` module.)
   </acceptance_criteria>
   <verify>
   <automated>npx vitest run src/components/games/shared/hud/ProgressBar.test.jsx ; echo "EXPECT NON-ZERO EXIT (RED) — missing ./ProgressBar module"</automated>
   </verify>
   <done>ProgressBar.test.jsx exists with 3 contract tests; RED because the component is absent (Wave 1 will create it).</done>
   </task>

<task type="auto">
  <name>Task 2: Create ScorePill.test.jsx (RED contract test)</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "ScorePill.jsx" section (lines ~117–188) for the prop contract, forwardRef, and TINT class table; and the shared "Test Mock Scaffolding" (lines ~786–808).
    - .planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md — Component Inventory ScorePill row + Color section combo-tint tiers.
  </read_first>
  <action>
Create `src/components/games/shared/hud/ScorePill.test.jsx`, importing from the not-yet-existing
`./ScorePill` (intended RED). Reuse the same framer-motion / react-i18next / useMotionTokens
mocks as Task 1 (ScorePill does NOT use AccessibilityContext, so no mock needed for it).

Write four tests under `describe("ScorePill", ...)`:

1. `it("renders value and default XP label")` — `<ScorePill value={120} />`; assert `screen.getByText("120")` and `screen.getByText("XP")`.
2. `it("renders a custom label")` — `<ScorePill value={7} label="Score" />`; assert `screen.getByText("Score")`.
3. `it("applies tint classes per comboTint tier")` — render at `comboTint={0}`, `{1}`, `{2}`; assert the pill container className contains, respectively, `border-white/20`+`bg-white/10`, `border-amber-400/30`+`bg-amber-500/15`, `border-yellow-400/40`+`bg-yellow-500/20`. (Query the inner pill div via container.querySelector with `.rounded-full.border`.)
4. `it("shows the floating +score only when floatingScore is set")` — render `<ScorePill value={5} floatingScore={null} />` and assert no `+`-prefixed float node; re-render `<ScorePill value={5} floatingScore={3} />` and assert `screen.getByText("+3")`.

Note for the executor: the component will use `React.forwardRef`. The test does not need to
attach a ref, but importing must succeed once 36-02 creates the file.
</action>
<acceptance_criteria> - File `src/components/games/shared/hud/ScorePill.test.jsx` exists; contains `describe("ScorePill"`, `import { ScorePill } from "./ScorePill"`, and the string `comboTint`. - File asserts all three tint tiers with the exact class fragments `border-amber-400/30`, `bg-amber-500/15`, `border-yellow-400/40`, `bg-yellow-500/20`, `border-white/20`. - `npx vitest run src/components/games/shared/hud/ScorePill.test.jsx` exits NON-zero (RED) — failing on the missing `./ScorePill` module, not a test syntax error.
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/shared/hud/ScorePill.test.jsx ; echo "EXPECT NON-ZERO EXIT (RED) — missing ./ScorePill module"</automated>
</verify>
<done>ScorePill.test.jsx exists with 4 contract tests covering value, label, all three tint tiers, and floating score; RED pending Wave 1.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary. Test files exercise presentational components with mocked dependencies;
no network, storage, auth, or user-input surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component       | Disposition | Mitigation Plan                                                                                                                                                         |
| --------- | -------- | --------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-01   | N/A      | hud/ test stubs | accept      | No new attack surface — test-only code with mocked deps; no user input, network calls, or PII handling. Presentational HUD contract fed by pre-validated numeric props. |

</threat_model>

<verification>
- `npx vitest run src/components/games/shared/hud/ProgressBar.test.jsx` → RED (missing component).
- `npx vitest run src/components/games/shared/hud/ScorePill.test.jsx` → RED (missing component).
- Both RED states are import-resolution failures, confirmed by the error naming the missing `./ProgressBar` / `./ScorePill` module (not a test parse error).
- Full suite (`npm run test:run`) will report these two files RED after this plan — documented and expected; they go GREEN in Plan 36-02.
</verification>

<success_criteria>
Two contract test files exist under `src/components/games/shared/hud/`, each with the assertions
described, each RED solely due to the not-yet-created component. The ProgressBar/ScorePill API is
now pinned as an executable contract that Plan 36-02 must satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-01-SUMMARY.md`.
</output>
