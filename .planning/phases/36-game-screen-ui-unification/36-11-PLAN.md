---
phase: 36-game-screen-ui-unification
plan: 11
type: execute
wave: 7
depends_on: ["36-08", "36-09", "36-10"]
files_modified: []
autonomous: false
requirements: [REQ-02, REQ-03, REQ-04, REQ-05, REQ-06, REQ-07]
must_haves:
  truths:
    - "Owner has walked through every restructured game screen and confirmed visual + behavioral consistency (portrait + landscape where applicable)"
    - "Full automated suite is green; reduced-motion, RTL/Hebrew, and landscape-lock manual checks pass across all adopting games"
  artifacts: []
  key_links: []
---

<objective>
Phase gate: the owner walkthrough that closes Phase 36. All automated tests are green by this
point; this checkpoint covers the manual-only verifications enumerated in 36-VALIDATION.md that no
unit test can cover — visual parity, landscape lock on real devices, RTL/Hebrew parity, and
reduced-motion suppression — across all ten game screens. This is the REQ-06 "no regressions" +
"owner walkthrough" acceptance gate.

Purpose: Owner sign-off that the unified HUD is consistent and regression-free everywhere.
Output: Phase-complete approval, or a defect list routed to gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-VALIDATION.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated suite as the pre-walkthrough gate</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-VALIDATION.md — Sampling Rate + Manual-Only Verifications.
  </read_first>
  <action>
Run the full test suite and the linter as the objective gate before the owner walkthrough. If
anything is red, stop and report — do not proceed to the human checkpoint with a red suite.
```bash
npm run test:run
npm run lint
```
  </action>
  <acceptance_criteria>
    - `npm run test:run` exits 0 (full suite green, including all 6 hud/ component tests and every touched game's existing tests).
    - `npm run lint` exits 0 (no new lint errors across the 9 new components + 10 modified game files).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run && npm run lint</automated>
  </verify>
  <done>Full suite + lint green; ready for owner walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner full-phase walkthrough (all 10 games + cross-cutting checks)</name>
  <what-built>
The HUD was extracted into 9 shared components under `src/components/games/shared/hud/`
(ProgressBar, ScorePill, TimerDisplay, LivesDisplay, ComboPill, OnFireBadge, OnFireSplash,
SpeedBonusFlash, TierUpPopup) and adopted across all game screens per the locked matrix:
- Base shell (all): NotesRecognition, SightReading, RhythmReading, RhythmDictation, MixedLesson,
  Metronome (trail), Memory, NoteComparison, Interval.
- Engagement (fast-recall): NoteComparison + Interval gained combo/on-fire (no lives);
  ArcadeRhythm de-duplicated its lives/combo/on-fire onto the shared components.
- MixedLesson's divergent progress bar was unified onto the shared ProgressBar.
  </what-built>
  <how-to-verify>
Run `npm run dev` (port 5174). For EACH game below, start a session and confirm the HUD looks and
behaves like the NotesRecognition standard:

1. NotesRecognition (reference) — full HUD incl. hearts→GameOver, combo shake/scale, on-fire
   badge+splash, FAST! flash, DOUBLE/TRIPLE XP fly-to landing on the score pill.
2. SightReading — shared progress bar + score pill (no combo/lives).
3. RhythmReading — shared progress bar + score pill; staff scroll still works.
4. RhythmDictation — shared progress bar + score pill (correct count); choice cards work.
5. MixedLesson — progress bar now matches NotesRecognition (gradient, dots) — NOT the old green bar.
6. Metronome (enter via a trail/exercise node) — shared progress bar + score pill.
7. Memory — shared progress bar (pairs) + rounded-full score pill.
8. NoteComparison — progress bar + score + combo pill + on-fire flame at combo ≥ 5; a wrong
   answer resets combo with NO life lost and NO early game-over; session ends on VictoryScreen.
9. Interval — same as NoteComparison.
10. ArcadeRhythm — combo pill appears only at combo ≥ 2 and shows the Flame on-fire; hearts
    deplete to GameOver; behavior matches before (plus new heart animations).

Cross-cutting:

- LANDSCAPE: on a phone-sized viewport (or iPhone SE), confirm ArcadeRhythm and the rhythm games
  still trigger the landscape prompt (useDeclareNeedsLandscape intact).
- REDUCED-MOTION: enable it (Settings → Accessibility) and confirm combo shake, on-fire splash,
  tier-up fly, speed-bonus flash, and the on-fire background shift are all suppressed in every
  adopting game.
- RTL/HEBREW: switch language to Hebrew and confirm the progress bar, score pill, and nav mirror
  correctly across the adopted games, with numbers still reading LTR.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 36 complete, or list any screen + the specific visual/behavioral defect for gap-closure planning.</resume-signal>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — verification-only checkpoint, no code change.

## STRIDE Threat Register

| Threat ID | Category | Component  | Disposition | Mitigation Plan                                                                 |
| --------- | -------- | ---------- | ----------- | ------------------------------------------------------------------------------- |
| T-36-11   | N/A      | checkpoint | accept      | No attack surface — automated gate + human walkthrough; no code or data change. |

</threat_model>

<verification>
- `npm run test:run` + `npm run lint` green (Task 1).
- Owner confirms all 10 game screens + landscape + reduced-motion + RTL checks (Task 2).
</verification>

<success_criteria>
Full automated suite green and owner-approved walkthrough across every restructured game screen,
with landscape-lock, reduced-motion, and RTL parity confirmed. Phase 36 acceptance criteria met.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-11-SUMMARY.md` recording the approval or defect list.
</output>
