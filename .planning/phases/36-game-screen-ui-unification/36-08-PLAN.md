---
phase: 36-game-screen-ui-unification
plan: 08
type: execute
wave: 6
depends_on: ["36-07"]
files_modified:
  - src/components/games/shared/hud/SpeedBonusFlash.jsx
  - src/components/games/shared/hud/TierUpPopup.jsx
  - src/components/games/notes-master-games/NotesRecognitionGame.jsx
autonomous: true
requirements: [REQ-01, REQ-05, REQ-06]
must_haves:
  truths:
    - "SpeedBonusFlash and TierUpPopup exist as named-export verbatim lifts under shared/hud/"
    - "NotesRecognitionGame consumes all six engagement components (LivesDisplay, ComboPill, OnFireBadge, OnFireSplash, SpeedBonusFlash, TierUpPopup) and no longer declares their inline JSX"
    - "The comboShake state is deleted from NotesRecognitionGame (animation now internal to ComboPill, D-10)"
    - "TierUpPopup fly-to still targets the score pill via scorePillRef (forwardRef preserved); NotesRecognition's lives→GameOverScreen path is unchanged (REQ-05)"
  artifacts:
    - path: "src/components/games/shared/hud/SpeedBonusFlash.jsx"
      provides: "FAST! flash with remount key"
      contains: "export function SpeedBonusFlash"
    - path: "src/components/games/shared/hud/TierUpPopup.jsx"
      provides: "DOUBLE/TRIPLE XP fly-to popup"
      contains: "export function TierUpPopup"
  key_links:
    - from: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      to: "src/components/games/shared/hud/TierUpPopup.jsx"
      via: "target computed from scorePillRef rect"
      pattern: "from \"\\.\\./shared/hud/TierUpPopup\""
    - from: "src/components/games/notes-master-games/NotesRecognitionGame.jsx"
      to: "src/components/games/shared/hud/ComboPill.jsx"
      via: "named import; comboShake state removed"
      pattern: "from \"\\.\\./shared/hud/ComboPill\""
---

<objective>
Wave 3 (D-05): Create the final two engagement components (SpeedBonusFlash, TierUpPopup), then
refactor NotesRecognitionGame to consume ALL SIX engagement components, deleting their inline JSX
and the parent-managed `comboShake` state (D-10). This completes the NotesRecognition reference's
full migration to shared HUD components. The lives→GameOverScreen path stays exactly as-is
(REQ-05); the `scorePillRef` forwardRef chain feeds TierUpPopup's fly-to (Pitfall 1).

Purpose: Finish extracting the engagement layer and migrate the reference fully onto it.
Output: 2 new hud/ components + a NotesRecognitionGame consuming all 9 shared HUD components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@CLAUDE.md

<interfaces>
Verbatim source lines in NotesRecognitionGame.jsx:
- SpeedBonusFlash JSX: lines 2535–2550 (h-7 wrapper, FAST! badge, remount key)
- TierUpPopup JSX: lines 2571–2610 (fly-to from scorePillRef rect)
- Engagement render layout: lines 2208–2610 (OnFireSplash at root z-[70]; HUD row with ScorePill,
  OnFireBadge, ComboPill; LivesDisplay right; SpeedBonusFlash reserved h-7; TierUpPopup overlay)

Component contracts (from 36-PATTERNS.md):

- `export function SpeedBonusFlash({ show, flashKey = 0 })` — keeps its own `h-7` wrapper; parent
  passes `show={showSpeedBonus}` AND `flashKey={speedBonusKey}` (Pitfall 5 remount).
- `export function TierUpPopup({ multiplier, target = {x:0,y:0} })` — `target` computed in parent
  from `scorePillRef.current.getBoundingClientRect()`.

Already created (Plan 36-02 + 36-07): ProgressBar, ScorePill (forwardRef), TimerDisplay,
LivesDisplay, ComboPill, OnFireBadge, OnFireSplash. Import path from notes-master-games/: `../shared/hud/X`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SpeedBonusFlash.jsx + TierUpPopup.jsx</name>
  <read_first>
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — SpeedBonusFlash.jsx (lines ~411–452) and TierUpPopup.jsx (lines ~456–508) VERBATIM bodies, incl. the h-7 wrapper + flashKey note and the target/forwardRef note.
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx lines 2535–2550 (SpeedBonusFlash) and 2571–2610 (TierUpPopup).
    - .planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md — Animation Contract SpeedBonusFlash + TierUpPopup rows; Typography note (tier-up uses text-3xl at ALL viewports, NO text-4xl).
  </read_first>
  <action>
Create both components under `src/components/games/shared/hud/` as verbatim lifts.

**SpeedBonusFlash.jsx** — imports:

```jsx
import { AnimatePresence, motion } from "framer-motion";
import { useTranslation } from "react-i18next";
```

`export function SpeedBonusFlash({ show, flashKey = 0 })`. Body verbatim from PATTERNS lines
431–448: KEEP the outer `<div className="pointer-events-none flex h-7 items-center justify-center">`
wrapper (reserves layout space — do not omit), AnimatePresence `motion.span` keyed by `flashKey`,
text `t("games.engagement.fast")`.

**TierUpPopup.jsx** — imports:

```jsx
import { AnimatePresence, motion } from "framer-motion";
import { useTranslation } from "react-i18next";
import { useMotionTokens } from "../../../../utils/useMotionTokens";
```

`export function TierUpPopup({ multiplier, target = { x: 0, y: 0 } })`. Body verbatim from PATTERNS
lines 478–504: `fixed inset-0 z-[70]` overlay, `rounded-2xl bg-gradient-to-br from-amber-500/90 to-yellow-500/90`,
`text-3xl font-black text-white` (NO `text-4xl` — UI-SPEC typography cap), reduce-gated fly-to
using `target.x`/`target.y`, text `t("games.engagement.triplePoints")` (>=3) / `t("games.engagement.doublePoints")`.
</action>
<acceptance_criteria> - `src/components/games/shared/hud/SpeedBonusFlash.jsx` contains `export function SpeedBonusFlash`, `h-7`, `flashKey`, and `games.engagement.fast`. - `src/components/games/shared/hud/TierUpPopup.jsx` contains `export function TierUpPopup`, `z-[70]`, `target.x`, `games.engagement.doublePoints`, and `games.engagement.triplePoints`. - TierUpPopup.jsx does NOT contain `text-4xl` (UI-SPEC typography cap). - `npx vitest run src/components/games/shared/hud` exits 0 (no import resolution failures).
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/shared/hud</automated>
</verify>
<done>SpeedBonusFlash + TierUpPopup exist as verbatim lifts; all 9 hud/ components now present.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor NotesRecognitionGame to consume all six engagement components</name>
  <read_first>
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx — read lines 1–60 (imports), 373–420 (engagement constants + reduced-motion sources + comboShake state), and 2208–2610 (full engagement render section). Target these ranges; do not re-read the whole file.
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "NotesRecognitionGame.jsx (Wave 1 reference refactor)" section (lines ~579–636) for the engagement import block, state removals (comboShake), and call-site wiring.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — Pitfall 1 (TierUpPopup fly-to / forwardRef), Pitfall 2 (stale comboShake), Pitfall 5 (SpeedBonusFlash remount key); "HUD Layout in Render" (lines ~388–409).
  </read_first>
  <action>
Refactor `src/components/games/notes-master-games/NotesRecognitionGame.jsx` to consume the
engagement components and delete their inline JSX.

1. Add imports:

```jsx
import { LivesDisplay } from "../shared/hud/LivesDisplay";
import { ComboPill } from "../shared/hud/ComboPill";
import { OnFireBadge } from "../shared/hud/OnFireBadge";
import { OnFireSplash } from "../shared/hud/OnFireSplash";
import { SpeedBonusFlash } from "../shared/hud/SpeedBonusFlash";
import { TierUpPopup } from "../shared/hud/TierUpPopup";
```

2. Replace inline JSX blocks with components, keeping the existing parent state names:

```jsx
<OnFireSplash show={showFireSplash} />                       // at render root, before StageCard
<OnFireBadge active={isOnFire} />                            // in HUD row
<ComboPill combo={combo} />                                  // in HUD row (no comboShake prop)
<LivesDisplay lives={lives} totalLives={INITIAL_LIVES} />    // right side
<SpeedBonusFlash show={showSpeedBonus} flashKey={speedBonusKey} />  // reserved h-7 slot
<TierUpPopup multiplier={tierUpMultiplier} target={tierUpTarget} /> // fixed overlay
```

3. DELETE the `comboShake` state declaration AND its `setComboShake(true)` call (and any timer
   that cleared it) from `handleAnswerSelect`/wherever it lives — ComboPill now detects the combo
   decrease internally (D-10, Pitfall 2).

4. KEEP in the parent (do NOT remove): `scorePillRef` (still attached to ScorePill via ref and
   used to compute `tierUpTarget` from `getBoundingClientRect()`), `speedBonusKey`,
   `floatingScoreKey`, `tierUpMultiplier`, `tierUpTarget`, `isOnFire`, `showFireSplash`,
   `showSpeedBonus`, `lives`, `combo`, and the lives→GameOverScreen logic (REQ-05 — unchanged).

5. The ScorePill site from Plan 36-02 already attaches `ref={scorePillRef}` — leave it. Confirm
   `tierUpTarget` is still computed from `scorePillRef.current.getBoundingClientRect()` so the
   fly-to lands on the pill.

Run the existing NotesRecognition autogrow test to confirm the public export is unaffected.
</action>
<acceptance_criteria> - NotesRecognitionGame.jsx contains all six engagement imports from `../shared/hud/` (LivesDisplay, ComboPill, OnFireBadge, OnFireSplash, SpeedBonusFlash, TierUpPopup). - NotesRecognitionGame.jsx renders `<ComboPill combo={combo} />` with NO `comboShake`/`shouldShake` attribute, and the file no longer contains the identifier `comboShake` (grep, comment-filtered: `grep -v '^\s*//' | grep -c 'comboShake'` returns 0). - NotesRecognitionGame.jsx renders `<SpeedBonusFlash show={showSpeedBonus} flashKey={speedBonusKey} />` and `<TierUpPopup multiplier={tierUpMultiplier} target={tierUpTarget} />`. - NotesRecognitionGame.jsx still computes `tierUpTarget` from `scorePillRef` (`getBoundingClientRect` present) and still references `GameOverScreen` (lives path intact). - `npx vitest run src/components/games/notes-master-games/NotesRecognitionGame.autogrow.test.js src/components/games/shared/hud` exits 0.
</acceptance_criteria>
<verify>
<automated>npx vitest run src/components/games/notes-master-games/NotesRecognitionGame.autogrow.test.js src/components/games/shared/hud</automated>
</verify>
<done>NotesRecognitionGame consumes all six engagement components; comboShake state deleted; TierUpPopup fly-to + GameOver path preserved; existing test green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational engagement extraction; NotesRecognition game logic,
scoring, lives, and GameOver routing unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component                            | Disposition | Mitigation Plan                                                                                                                       |
| --------- | -------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-08   | N/A      | NotesRecognition engagement refactor | accept      | No new attack surface — presentational component swap; no new user input, network, or PII. Lives→GameOver routing unchanged (REQ-05). |

</threat_model>

<verification>
- `npx vitest run src/components/games/shared/hud src/components/games/notes-master-games` → green.
- `npm run lint` → no new errors.
- `comboShake` absent from NotesRecognitionGame.jsx (grep, comment-filtered).
- MANUAL (gated in 36-11 final checkpoint): full NotesRecognition session — hearts removal,
  combo shake on wrong / scale on correct, on-fire badge+splash, FAST! flash, DOUBLE/TRIPLE XP
  fly-to landing on the score pill, lives→GameOver — all identical to pre-extraction; reduced-motion
  suppresses animations.
</verification>

<success_criteria>
NotesRecognitionGame consumes all nine shared HUD components; engagement inline JSX and comboShake
state removed; fly-to and GameOver behavior preserved; tests green. The reference is fully migrated.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-08-SUMMARY.md`.
</output>
