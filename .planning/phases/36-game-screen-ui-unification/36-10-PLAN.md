---
phase: 36-game-screen-ui-unification
plan: 10
type: execute
wave: 6
depends_on: ["36-07"]
files_modified:
  - src/components/games/ear-training-games/NoteComparisonGame.jsx
  - src/components/games/ear-training-games/IntervalGame.jsx
autonomous: true
requirements: [REQ-02, REQ-05, REQ-06, REQ-07]
must_haves:
  truths:
    - "NoteComparisonGame and IntervalGame render the shared ProgressBar, ScorePill, ComboPill, and OnFireBadge"
    - "Both add combo + isOnFire state; wrong answers reset combo but cost NO life and trigger NO early GameOver (D-08); sessions always end on VictoryScreen"
    - "ScorePill comboTint reflects the combo tier (engagement layer present); no LivesDisplay is added (REQ-05 documented exclusion)"
  artifacts:
    - path: "src/components/games/ear-training-games/NoteComparisonGame.jsx"
      provides: "Base shell + combo/on-fire engagement (no lives)"
      contains: "shared/hud/ComboPill"
    - path: "src/components/games/ear-training-games/IntervalGame.jsx"
      provides: "Base shell + combo/on-fire engagement (no lives)"
      contains: "shared/hud/ComboPill"
  key_links:
    - from: "src/components/games/ear-training-games/NoteComparisonGame.jsx"
      to: "src/components/games/shared/hud/ComboPill.jsx"
      via: "new combo state + named import"
      pattern: "from \"\\.\\./shared/hud/ComboPill\""
    - from: "src/components/games/ear-training-games/IntervalGame.jsx"
      to: "src/components/games/shared/hud/OnFireBadge.jsx"
      via: "new isOnFire state + named import"
      pattern: "from \"\\.\\./shared/hud/OnFireBadge\""
---

<objective>
Wave 3 (D-01/D-02/D-08): Bring the two ear-training games up to the full NotesRecognition feel —
shared ProgressBar + ScorePill (base shell) PLUS ComboPill + OnFireBadge (engagement). Both games
gain new `combo` and `isOnFire` state. Per D-08, ear-training stays no-lives / always-finish:
wrong answers reset combo but cost no life and never trigger an early GameOver — the session
always ends on VictoryScreen. This is REQ-05's "games where game-over does not apply are
documented (not forced)" case. No LivesDisplay, no SpeedBonusFlash, no TierUpPopup.

IMPORTANT PATH NOTE: 36-PATTERNS.md (line ~700) shows `../../shared/hud/` for ear-training — that
is an ERROR. `ear-training-games/` is at the same depth as the other game dirs, so the correct
import path is `../shared/hud/X` (one `..`). Use `../shared/hud/`.

Purpose: Give fast-recall ear-training the arcade engagement layer without lives.
Output: Both ear-training games consuming base shell + ComboPill + OnFireBadge; no-lives model documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@CLAUDE.md

<interfaces>
Shared components: `ProgressBar({current,total})`, `ScorePill({value,label,comboTint,...})`,
`ComboPill({combo,isOnFire})`, `OnFireBadge({active})`. Import path from `ear-training-games/`:
`../shared/hud/X` (NOT `../../shared/hud/` — PATTERNS typo).

New engagement state (both files, identical — 36-PATTERNS.md lines ~681–696):

```jsx
const [combo, setCombo] = useState(0);
const [isOnFire, setIsOnFire] = useState(false);
// correct: setCombo(prev => { const next = prev + 1; if (next >= ON_FIRE_THRESHOLD && !isOnFire) setIsOnFire(true); return next; });
// wrong:   setCombo(0);   // NO lives deducted (D-08)
```

ON_FIRE_THRESHOLD = 5 (mirror NotesRecognition's constant; define locally in each ear-training file).

Per-game wiring (36-RESEARCH.md lines ~528–562 — confirm names in each file):

- NoteComparisonGame: ProgressBar current=`currentQuestion`, total=`TOTAL_QUESTIONS` (=10);
  ScorePill value=`correctCount`, label=`t("games.score")`.
- IntervalGame: ProgressBar current=`questionScores.length`, total=`TOTAL_QUESTIONS`;
  ScorePill value=`correctCount`, label=`t("games.score")`.
  comboTint for ScorePill = `combo >= 8 ? 2 : combo >= 3 ? 1 : 0`.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: NoteComparisonGame — base shell + combo/on-fire (no lives)</name>
  <read_first>
    - src/components/games/ear-training-games/NoteComparisonGame.jsx — read the inline header (RESEARCH cites lines ~430–443) and the answer-correct / answer-wrong handlers; confirm `currentQuestion`, `TOTAL_QUESTIONS`, `correctCount`, and where correct/wrong are handled.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "NoteComparisonGame" section (lines ~528–546) incl. the D-08 no-lives rule.
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — "NoteComparisonGame and IntervalGame" Wave 3 section (lines ~679–708).
  </read_first>
  <action>
Edit `src/components/games/ear-training-games/NoteComparisonGame.jsx`:
1. Add imports (note the SINGLE `..` path):
```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
import { ComboPill } from "../shared/hud/ComboPill";
import { OnFireBadge } from "../shared/hud/OnFireBadge";
```
2. Add state: `const [combo, setCombo] = useState(0);` and `const [isOnFire, setIsOnFire] = useState(false);`
   Define `const ON_FIRE_THRESHOLD = 5;` near the top of the component (or module scope).
3. In the answer-CORRECT handler: increment combo and trigger on-fire at threshold (snippet above).
   In the answer-WRONG handler: `setCombo(0);` — and DO NOT deduct a life or end the session early
   (D-08). Leave the existing always-finish-all-questions flow intact (ends on VictoryScreen).
   Reset `combo`/`isOnFire` to 0/false when a new session starts.
4. Replace the inline header counter / score with the HUD components:
```jsx
<ProgressBar current={currentQuestion} total={TOTAL_QUESTIONS} />
<ScorePill value={correctCount} label={t("games.score")} comboTint={combo >= 8 ? 2 : combo >= 3 ? 1 : 0} />
<ComboPill combo={combo} />
<OnFireBadge active={isOnFire} />
```
   (Place ComboPill/OnFireBadge in the HUD row near the score, matching NotesRecognition's layout.)
5. Do NOT add LivesDisplay, SpeedBonusFlash, or TierUpPopup. Add `games.score` to both locales in
   lockstep if missing (REQ-07).
  </action>
  <acceptance_criteria>
    - NoteComparisonGame.jsx contains imports from `../shared/hud/ProgressBar`, `../shared/hud/ScorePill`, `../shared/hud/ComboPill`, `../shared/hud/OnFireBadge` (single `..`).
    - NoteComparisonGame.jsx declares `const [combo, setCombo]` and `const [isOnFire, setIsOnFire]`, and renders `<ComboPill combo={combo} />` and `<OnFireBadge active={isOnFire} />`.
    - NoteComparisonGame.jsx does NOT import or render `LivesDisplay`, `SpeedBonusFlash`, or `TierUpPopup` (no-lives model, D-08/REQ-05).
    - The answer-wrong handler resets combo (`setCombo(0)`) and contains no life-decrement / GameOver call.
    - `npx vitest run src/components/games/ear-training-games/NoteComparisonGame` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/ear-training-games/NoteComparisonGame</automated>
  </verify>
  <done>NoteComparisonGame has base shell + combo/on-fire engagement, no lives, always ends on VictoryScreen; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: IntervalGame — base shell + combo/on-fire (no lives)</name>
  <read_first>
    - src/components/games/ear-training-games/IntervalGame.jsx — read the inline header (RESEARCH cites lines ~535–545) and the correct/wrong handlers; confirm `questionScores`, `TOTAL_QUESTIONS`, `correctCount`.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "IntervalGame" section (lines ~548–562).
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — Wave 3 ear-training section (lines ~679–708).
  </read_first>
  <action>
Edit `src/components/games/ear-training-games/IntervalGame.jsx`, mirroring Task 1 exactly:
1. Add the four `../shared/hud/` imports (single `..`).
2. Add `combo` + `isOnFire` state and `ON_FIRE_THRESHOLD = 5`; wire correct (increment + on-fire)
   and wrong (`setCombo(0)`, NO life loss, NO early GameOver) handlers; reset on new session.
3. Render:
```jsx
<ProgressBar current={questionScores.length} total={TOTAL_QUESTIONS} />
<ScorePill value={correctCount} label={t("games.score")} comboTint={combo >= 8 ? 2 : combo >= 3 ? 1 : 0} />
<ComboPill combo={combo} />
<OnFireBadge active={isOnFire} />
```
4. No LivesDisplay/SpeedBonusFlash/TierUpPopup. Locale parity for any new label (REQ-07).
  </action>
  <acceptance_criteria>
    - IntervalGame.jsx contains imports from `../shared/hud/ProgressBar`, `../shared/hud/ScorePill`, `../shared/hud/ComboPill`, `../shared/hud/OnFireBadge` (single `..`).
    - IntervalGame.jsx declares `const [combo, setCombo]` and `const [isOnFire, setIsOnFire]`, and renders `<ComboPill combo={combo} />` and `<OnFireBadge active={isOnFire} />`.
    - IntervalGame.jsx does NOT import or render `LivesDisplay`, `SpeedBonusFlash`, or `TierUpPopup`.
    - The answer-wrong handler resets combo and contains no life-decrement / GameOver call.
    - `npx vitest run src/components/games/ear-training-games/IntervalGame` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/ear-training-games/IntervalGame</automated>
  </verify>
  <done>IntervalGame has base shell + combo/on-fire engagement, no lives, always ends on VictoryScreen; tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational HUD adoption + local combo/on-fire UI state; no change to
audio discrimination logic, scoring persistence, network, or PII.

## STRIDE Threat Register

| Threat ID | Category | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                     |
| --------- | -------- | -------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-10   | N/A      | ear-training engagement adoption | accept      | No new attack surface — presentational components + local UI state (combo/isOnFire) derived from existing answer outcomes; no new user input, network, or PII. No lives/GameOver path added (D-08). |

</threat_model>

<verification>
- `npx vitest run src/components/games/ear-training-games` → green.
- `npm run lint` → no new errors.
- Grep confirms no `LivesDisplay`/`SpeedBonusFlash`/`TierUpPopup` in either ear-training file.
- Locale parity if a key added.
- MANUAL (gated in 36-11): both ear-training games show progress bar + score + combo pill +
  on-fire flame at combo ≥ 5; wrong answer resets combo with no life lost and no early game-over;
  session ends on VictoryScreen; RTL + reduced-motion correct.
</verification>

<success_criteria>
NoteComparisonGame and IntervalGame consume base shell + ComboPill + OnFireBadge, gain combo/on-fire
state, keep the no-lives always-finish model (D-08, REQ-05 documented), tests green, locale parity intact.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-10-SUMMARY.md`.
</output>
