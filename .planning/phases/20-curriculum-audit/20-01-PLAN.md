---
phase: 20-curriculum-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md
autonomous: true
requirements:
  - CURR-01
  - CURR-02
  - CURR-03
  - CURR-04

must_haves:
  truths:
    - "Every one of the 56 rhythm nodes has a row in the audit table"
    - "Each node's single introduced musical concept is identified"
    - "Nodes violating one-concept rule are flagged with specific violation description"
    - "A game-type policy section documents which nodeType maps to which exercise type"
    - "Every node that uses the wrong exercise type per policy is flagged for remediation"
    - "The remediation column specifies what must change for each violating node"
  artifacts:
    - path: ".planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md"
      provides: "Complete curriculum audit reference document"
      contains: "## Game-Type Policy"
  key_links:
    - from: ".planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md"
      to: "Phase 22 implementation"
      via: "Remediation table drives unit file changes"
      pattern: "Remediation"
---

<objective>
Audit all 56 rhythm nodes across 8 units and produce a reference document that locks every pedagogical decision before any source file is modified.

Purpose: Phase 22 (Service Layer & Trail Wiring) needs an exact specification of what changes to make in each unit file. Without this audit, implementation would require in-the-moment pedagogical decisions that should be made deliberately.

Output: `.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md` containing game-type policy, one-concept rule definition, and a 56-row node-by-node remediation table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-curriculum-audit/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the curriculum audit reference document</name>
  <files>.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md</files>
  <read_first>
    src/data/units/rhythmUnit1Redesigned.js
    src/data/units/rhythmUnit2Redesigned.js
    src/data/units/rhythmUnit3Redesigned.js
    src/data/units/rhythmUnit4Redesigned.js
    src/data/units/rhythmUnit5Redesigned.js
    src/data/units/rhythmUnit6Redesigned.js
    src/data/units/rhythmUnit7Redesigned.js
    src/data/units/rhythmUnit8Redesigned.js
    src/data/nodeTypes.js
    src/data/constants.js
    .planning/phases/20-curriculum-audit/20-CONTEXT.md
  </read_first>
  <action>
Create `.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md` with the following structure and content. Read every rhythm unit file to extract exact current state. The document must contain these sections IN ORDER:

## 1. Header

```markdown
# Rhythm Trail Curriculum Audit

**Phase:** 20-curriculum-audit
**Date:** [today's date]
**Status:** LOCKED -- Phase 22 implementation must follow this document exactly

**Scope:** 56 rhythm nodes across 8 units (7 nodes per unit)
```

## 2. Game-Type Policy (per D-03 through D-14)

Write a section `## Game-Type Policy` with a table mapping each NODE_TYPE to its required exercise type:

| Node Type   | Required Exercise Type | Rationale                                                                                                                                               |
| ----------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DISCOVERY   | `mixed_lesson`         | Notation-weighted: heavier on `rhythm_tap` (notation-showing) questions, with `visual_recognition` and `syllable_matching` for variety (per D-03, D-14) |
| PRACTICE    | `mixed_lesson`         | Balanced mix of all question types for reinforcement (per D-04)                                                                                         |
| MIX_UP      | `mixed_lesson`         | Varied question types keep engagement high (per D-05)                                                                                                   |
| REVIEW      | `mixed_lesson`         | Spaced repetition across multiple question formats (per D-06)                                                                                           |
| MINI_BOSS   | `mixed_lesson`         | Longer session covering all unit concepts (per D-09)                                                                                                    |
| CHALLENGE   | `arcade_rhythm`        | Pressure-based engagement challenge (per D-07)                                                                                                          |
| SPEED_ROUND | `arcade_rhythm`        | Timed arcade challenge at elevated tempo (per D-08)                                                                                                     |
| BOSS        | `arcade_rhythm`        | Ultimate mastery test in arcade format (per D-10)                                                                                                       |

Add a summary paragraph: "Nodes with types DISCOVERY, PRACTICE, MIX_UP, REVIEW, and MINI_BOSS use `mixed_lesson`. Nodes with types CHALLENGE, SPEED_ROUND, and BOSS use `arcade_rhythm`. Per D-11, D-12, D-13."

## 3. One-Concept Rule (per D-15 through D-17)

Write a section `## One-Concept Rule` that states:

- A "musical concept" is defined as: a new duration value (e.g., quarter note, half note, eighth note, quarter rest, dotted half, sixteenth note) OR a new time signature (e.g., 3/4, 6/8). (per D-15)
- Game mode changes (e.g., first time encountering arcade mode, first time doing rhythm_tap) do NOT count as a concept introduction. (per D-16)
- A node that introduces two or more musical concepts is a VIOLATION. Example: introducing both "half note" and "quarter rest" on the same node. (per D-17)
- How to identify what a node introduces: check `focusDurations` array (non-empty = new duration), `newContentDescription` field (mentions new duration/time sig), and whether `timeSignature` differs from all prior nodes.

## 4. Node-by-Node Audit Table (per D-01)

Write a section `## Node-by-Node Audit` with one subsection per unit (`### Unit N: [Unit Name]`).

Each unit subsection contains a table with these EXACT columns (per D-01):

- **Node ID** — e.g., `rhythm_1_1`
- **Name** — e.g., "Meet Quarter Notes"
- **nodeType** — e.g., `discovery`
- **Current Exercises** — comma-separated list of current exercise types, e.g., `mixed_lesson` or `rhythm, visual_recognition, syllable_matching`
- **Concept Introduced** — What new musical concept this node introduces (from `focusDurations` + `newContentDescription` + `timeSignature` changes). Use "None (reinforcement)" for nodes with empty `focusDurations` and no new time sig.
- **Violations** — List any violations: wrong exercise type per game-type policy, multiple concepts introduced, or "None"
- **Remediation** — What must change. Either "None needed" or specific change like "Change exercises from `[rhythm]` to `[mixed_lesson]` with notation-weighted question sequence" or "Change exercises from `[arcade_rhythm]` to `[mixed_lesson]` (longer session covering all unit concepts)"

For each of the 56 nodes, determine:

1. **Concept check:** Read `focusDurations` array. If it has entries, those are new durations. Also check if `timeSignature` is new (first occurrence of 3/4, 6/8, etc. in trail order). If more than one new concept, flag as "VIOLATION: Multiple concepts".

2. **Exercise type check:** Compare current `exercises[].type` against the game-type policy table above. The required type depends on the node's `nodeType`:
   - `discovery`, `practice`, `mix_up`, `review`, `mini_boss` -> must be `mixed_lesson`
   - `challenge`, `speed_round` -> must be `arcade_rhythm`
   - `boss` -> must be `arcade_rhythm`
     If current type does not match required type, flag as violation and specify remediation.

3. **Multi-exercise check:** Some nodes currently have multiple exercises (e.g., Unit 1 Node 4 has `rhythm_dictation`, `visual_recognition`, `syllable_matching` as 3 separate exercises). For nodes that should use `mixed_lesson`, remediation is to collapse these into a single `mixed_lesson` exercise with an interleaved `questions` array. For nodes that should use `arcade_rhythm`, remediation is to use a single `arcade_rhythm` exercise.

IMPORTANT concrete values for the current state of each unit (extract from actual file reads):

**Unit 1 (Rhythm Starters, 7 nodes, IDs rhythm_1_1 through boss_rhythm_1):**

- rhythm_1_1: discovery, mixed_lesson, focus=q
- rhythm_1_2: practice, mixed_lesson, focus=none
- rhythm_1_3: discovery, mixed_lesson, focus=h
- rhythm_1_4: practice, [rhythm_dictation, visual_recognition, syllable_matching], focus=none
- rhythm_1_5: mix_up, [rhythm_tap, visual_recognition, syllable_matching], focus=none
- rhythm_1_6: speed_round, [rhythm, visual_recognition, syllable_matching], focus=none
- boss_rhythm_1: mini_boss, arcade_rhythm, focus=none

**Unit 2 (Beat Builders, 7 nodes, IDs rhythm_2_1 through boss_rhythm_2):**

- rhythm_2_1: discovery, rhythm, focus=w
- rhythm_2_2: practice, rhythm, focus=none
- rhythm_2_3: discovery, [rhythm_tap, visual_recognition, syllable_matching], focus=none (contrast node)
- rhythm_2_4: practice, rhythm_dictation, focus=none
- rhythm_2_5: mix_up, rhythm_tap, focus=none
- rhythm_2_6: speed_round, rhythm, focus=none
- boss_rhythm_2: mini_boss, arcade_rhythm, focus=none

**Unit 3 (Fast Note Friends, 7 nodes, IDs rhythm_3_1 through boss_rhythm_3):**

- rhythm_3_1: discovery, rhythm, focus=8
- rhythm_3_2: practice, rhythm, focus=none
- rhythm_3_3: discovery, rhythm_tap, focus=none (contrast node)
- rhythm_3_4: practice, rhythm_dictation, focus=none
- rhythm_3_5: mix_up, rhythm_tap, focus=none
- rhythm_3_6: speed_round, rhythm, focus=none
- boss_rhythm_3: mini_boss, arcade_rhythm, focus=none

**Unit 4 (Quiet Moments, 7 nodes, IDs rhythm_4_1 through boss_rhythm_4):**

- rhythm_4_1: discovery, rhythm, focus=qr
- rhythm_4_2: practice, rhythm, focus=none
- rhythm_4_3: discovery, rhythm_tap, focus=hr
- rhythm_4_4: practice, rhythm_dictation, focus=none
- rhythm_4_5: discovery, rhythm_tap, focus=wr
- rhythm_4_6: speed_round, rhythm, focus=none
- boss_rhythm_4: mini_boss, arcade_rhythm, focus=none

**Unit 5 (Magic Dots, 7 nodes, IDs rhythm_5_1 through boss_rhythm_5):**

- rhythm_5_1: discovery, rhythm, focus=hd
- rhythm_5_2: practice, rhythm, focus=none
- rhythm_5_3: discovery, rhythm_tap, focus=none BUT introduces 3/4 time signature (new concept!)
- rhythm_5_4: discovery, rhythm_dictation, focus=qd
- rhythm_5_5: practice, rhythm_tap, focus=none
- rhythm_5_6: speed_round, rhythm, focus=none
- boss_rhythm_5: mini_boss, [arcade_rhythm x2], focus=none

**Unit 6 (Speed Champions, 7 nodes, IDs rhythm_6_1 through boss_rhythm_6):**

- rhythm_6_1: discovery, rhythm, focus=16
- rhythm_6_2: practice, rhythm, focus=none
- rhythm_6_3: discovery, rhythm_tap, focus=none (mixing node)
- rhythm_6_4: practice, rhythm_dictation, focus=none
- rhythm_6_5: mix_up, rhythm_tap, focus=none
- rhythm_6_6: speed_round, rhythm, focus=none
- boss_rhythm_6: boss, arcade_rhythm, focus=none

**Unit 7 (Big Beats, 7 nodes, IDs rhythm_7_1 through boss_rhythm_7):**

- rhythm_7_1: discovery, rhythm, focus=qd (but this is dotted-quarter as 6/8 beat AND introduces 6/8 time sig)
- rhythm_7_2: practice, rhythm, focus=none
- rhythm_7_3: discovery, rhythm_tap, focus=q (quarter notes in 6/8 context)
- rhythm_7_4: practice, rhythm_dictation, focus=8 (eighth notes in 6/8)
- rhythm_7_5: mix_up, rhythm_tap, focus=none
- rhythm_7_6: speed_round, rhythm, focus=none
- boss_rhythm_7: mini_boss, arcade_rhythm, focus=none

**Unit 8 (Off-Beat Magic, 7 nodes, IDs rhythm_8_1 through boss_rhythm_8):**

- rhythm_8_1: discovery, rhythm, focus=8 (syncopation context)
- rhythm_8_2: practice, rhythm, focus=none
- rhythm_8_3: discovery, rhythm_tap, focus=qd (dotted quarter syncopation)
- rhythm_8_4: practice, rhythm_dictation, focus=none
- rhythm_8_5: mix_up, rhythm_tap, focus=none
- rhythm_8_6: speed_round, rhythm, focus=none
- boss_rhythm_8: boss, [arcade_rhythm x3], focus=none

For EACH node, produce a row. Key violation patterns to flag:

- Any discovery/practice/mix_up/review/mini_boss node NOT using `mixed_lesson` -> exercise type violation
- Any speed_round/challenge/boss node NOT using `arcade_rhythm` -> exercise type violation
- Nodes with `focusDurations` containing 2+ entries -> one-concept violation
- Unit 7 Node 1 (rhythm_7_1): introduces BOTH dotted-quarter as 6/8 beat AND 6/8 time signature. Per D-15 and D-16, check whether this is truly 2 concepts or whether the dotted-quarter is just the beat unit of 6/8 (which could be argued as one concept: "compound meter"). Use Claude's discretion: the dotted-quarter was already introduced in Unit 5, so the new concept here is specifically "6/8 time signature" -- the dotted-quarter is context, not new. Flag as "No violation" with a note.
- Unit 7 Node 4 (rhythm_7_4): nodeType is `practice` but introduces a new concept (eighth notes in 6/8 via `focusDurations: ['8']`). This is a violation -- Practice nodes should reinforce, not introduce. Flag with remediation: either remove eighth notes from focusDurations (make it pure practice of existing durations) or change nodeType to discovery.

## 5. Remediation Summary

Write a section `## Remediation Summary` listing:

- Total nodes: 56
- Nodes with exercise type violations: [count]
- Nodes with one-concept violations: [count]
- Nodes with no violations: [count]

Then a condensed checklist of all remediations grouped by violation type:

### Exercise Type Remediations

List every node that needs exercise type change, grouped as:

- "Change to `mixed_lesson`:" followed by node IDs
- "Change to `arcade_rhythm`:" followed by node IDs (if any)

### One-Concept Remediations

List any nodes with concept violations and what to do.

### Multi-Exercise Consolidation

List nodes that have multiple separate exercises that should be consolidated into a single `mixed_lesson` or `arcade_rhythm`.

## 6. Question Mix Guidelines (Claude's Discretion)

Write a section `## Question Mix Guidelines` with recommended question distributions for `mixed_lesson` nodes by nodeType:

| nodeType  | rhythm_tap | visual_recognition | syllable_matching | Total Questions |
| --------- | ---------- | ------------------ | ----------------- | --------------- |
| DISCOVERY | 3-4        | 2                  | 2                 | 7-8             |
| PRACTICE  | 2-3        | 2-3                | 2-3               | 8               |
| MIX_UP    | 2          | 3                  | 3                 | 8               |
| MINI_BOSS | 3-4        | 3-4                | 3-4               | 10-12           |

Add note: "Discovery nodes are notation-weighted per D-14 -- more rhythm_tap (which shows notation) questions. Practice/MIX_UP nodes have balanced distribution. MINI_BOSS nodes have longer sessions covering all unit concepts per D-09."

## 7. Unit Narrative Notes (Claude's Discretion)

Write a brief 2-3 sentence narrative per unit explaining the pedagogical flow: what concepts are introduced, in what order, and the learning arc. This helps Phase 22 implementers understand intent.
</action>
<verify>
<automated>bash -c "FILE='.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md' && test -f \"$FILE\" && echo 'File exists: PASS' && grep -c 'rhythm_' \"$FILE\" | xargs -I{} bash -c 'if [ {} -ge 56 ]; then echo \"Node coverage: PASS ({}+ mentions)\"; else echo \"Node coverage: FAIL ({} mentions)\"; fi' && grep -q '## Game-Type Policy' \"$FILE\" && echo 'Game-Type Policy section: PASS' && grep -q '## One-Concept Rule' \"$FILE\" && echo 'One-Concept Rule section: PASS' && grep -q '## Node-by-Node Audit' \"$FILE\" && echo 'Audit table section: PASS' && grep -q '## Remediation Summary' \"$FILE\" && echo 'Remediation Summary: PASS' && grep -q 'mixed_lesson' \"$FILE\" && echo 'mixed_lesson references: PASS' && grep -q 'arcade_rhythm' \"$FILE\" && echo 'arcade_rhythm references: PASS' && grep -q '## Question Mix' \"$FILE\" && echo 'Question Mix section: PASS'"</automated>
</verify>
<acceptance_criteria> - File `.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md` exists - File contains `## Game-Type Policy` section with 8-row nodeType mapping table - File contains `## One-Concept Rule` section defining concept as "duration value OR time signature" - File contains `## Node-by-Node Audit` with subsections for all 8 units - Every rhythm node ID appears: rhythm_1_1 through rhythm_8_6 and boss_rhythm_1 through boss_rhythm_8 - Each node row has columns: Node ID, Name, nodeType, Current Exercises, Concept Introduced, Violations, Remediation - File contains `## Remediation Summary` with violation counts and grouped remediation checklist - File contains `## Question Mix Guidelines` table with recommended distributions - Nodes currently using `rhythm` exercise type on discovery/practice/mix_up nodes are flagged as violations needing `mixed_lesson` - MINI_BOSS nodes currently using `arcade_rhythm` are flagged as violations needing `mixed_lesson` (per D-09) - SPEED_ROUND and BOSS nodes using `arcade_rhythm` are marked as correct (no violation) - Header contains "LOCKED" status indicator - File references decision IDs (D-XX) in policy sections
</acceptance_criteria>
<done>
A complete 56-node audit document exists at `.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md` with game-type policy, one-concept rule, node-by-node table, remediation summary, and question mix guidelines. Every node has been reviewed, every violation documented, and every remediation specified. The document is marked LOCKED as the single source of truth for Phase 22 implementation.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No trust boundaries apply -- this is a documentation-only phase producing a planning artifact. No user input is processed, no API calls are made, no data is stored.

## STRIDE Threat Register

| Threat ID | Category | Component          | Disposition | Mitigation Plan                                  |
| --------- | -------- | ------------------ | ----------- | ------------------------------------------------ |
| T-20-01   | N/A      | Documentation only | accept      | No code changes in this phase; no attack surface |

</threat_model>

<verification>
1. File `.planning/phases/20-curriculum-audit/20-CURRICULUM-AUDIT.md` exists and is non-empty
2. All 56 node IDs appear in the document (8 units x 7 nodes)
3. Game-type policy section maps all 8 node types to exercise types
4. One-concept rule section defines musical concept scope
5. Remediation summary provides actionable counts and checklists
6. Question mix guidelines provide distributions for each nodeType
</verification>

<success_criteria>

- Every rhythm node (56 total) has been reviewed with concept identification and violation check
- Game-type policy is written and references decisions D-03 through D-14
- One-concept rule definition references decisions D-15 through D-17
- Remediation table specifies exact changes needed for every violating node
- Document is committed as the reference that Phase 22 follows
  </success_criteria>

<output>
After completion, create `.planning/phases/20-curriculum-audit/20-01-SUMMARY.md`
</output>
