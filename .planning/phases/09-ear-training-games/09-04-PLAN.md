---
phase: 09-ear-training-games
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - src/App.jsx
  - src/components/trail/TrailNodeModal.jsx
  - src/locales/en/common.json
  - src/locales/he/common.json
  - src/components/games/rhythm-games/MetronomeTrainer.jsx
  - src/components/games/rhythm-games/RhythmReadingGame.jsx
  - src/components/games/rhythm-games/RhythmDictationGame.jsx
  - src/components/games/notes-master-games/NotesRecognitionGame.jsx
  - src/components/games/notes-master-games/MemoryGame.jsx
  - src/components/games/notes-master-games/NoteSpeedCards.jsx
  - src/components/games/sight-reading-game/SightReadingGame.jsx
autonomous: true
requirements: [PITCH-01, PITCH-02, PITCH-04, PITCH-05, INTV-01, INTV-04, INTV-05]

must_haves:
  truths:
    - "Navigating to /ear-training-mode/note-comparison-game renders NoteComparisonGame"
    - "Navigating to /ear-training-mode/interval-game renders IntervalGame"
    - "Tapping a trail node with pitch_comparison exercise opens NoteComparisonGame (not ComingSoon)"
    - "Tapping a trail node with interval_id exercise opens IntervalGame (not ComingSoon)"
    - "All game UI text appears in both English and Hebrew with correct RTL layout"
    - "Trail multi-exercise nodes can chain from any existing game to pitch_comparison or interval_id"
    - "Both new game routes are landscape-compatible (in LANDSCAPE_ROUTES)"
  artifacts:
    - path: "src/App.jsx"
      provides: "Lazy route imports for NoteComparisonGame and IntervalGame, LANDSCAPE_ROUTES entries, AudioContextProvider-wrapped routes"
      contains: "ear-training-mode"
    - path: "src/components/trail/TrailNodeModal.jsx"
      provides: "Updated pitch_comparison and interval_id routing to actual game components"
      contains: "ear-training-mode/note-comparison-game"
    - path: "src/locales/en/common.json"
      provides: "English i18n keys for both ear training games"
      contains: "noteComparison"
    - path: "src/locales/he/common.json"
      provides: "Hebrew i18n keys for both ear training games"
      contains: "noteComparison"
  key_links:
    - from: "src/App.jsx"
      to: "NoteComparisonGame.jsx"
      via: "lazyWithRetry import + Route element"
      pattern: "lazyWithRetry.*ear-training-games/NoteComparisonGame"
    - from: "src/App.jsx"
      to: "IntervalGame.jsx"
      via: "lazyWithRetry import + Route element"
      pattern: "lazyWithRetry.*ear-training-games/IntervalGame"
    - from: "TrailNodeModal.jsx"
      to: "/ear-training-mode/note-comparison-game"
      via: "navigate call in pitch_comparison case"
      pattern: "navigate.*ear-training-mode/note-comparison-game"
    - from: "TrailNodeModal.jsx"
      to: "/ear-training-mode/interval-game"
      via: "navigate call in interval_id case"
      pattern: "navigate.*ear-training-mode/interval-game"
---

<objective>
Wire both ear training games into the app: register routes in App.jsx, update TrailNodeModal routing, add i18n keys for EN+HE, and update handleNextExercise in ALL existing game components so trail multi-exercise nodes can chain to/from ear training games.

Purpose: Without this wiring, neither game is reachable from the trail or directly navigable. This plan makes both games fully integrated and playable end-to-end.
Output: Updated App.jsx, TrailNodeModal.jsx, locale files, and all existing game component handleNextExercise functions.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ear-training-games/09-CONTEXT.md
@.planning/phases/09-ear-training-games/09-RESEARCH.md
@.planning/phases/09-ear-training-games/09-UI-SPEC.md
@.planning/phases/09-ear-training-games/09-02-SUMMARY.md
@.planning/phases/09-ear-training-games/09-03-SUMMARY.md

<interfaces>
<!-- Existing code patterns to match -->

From src/App.jsx (lazy import pattern, lines 68-81):
```javascript
const MetronomeTrainer = lazyWithRetry(() => import("./components/games/rhythm-games/MetronomeTrainer"));
// ... (other lazy imports follow same pattern)
```

From src/App.jsx (LANDSCAPE_ROUTES, lines 191-198):
```javascript
const LANDSCAPE_ROUTES = [
  "/notes-master-mode/notes-recognition-game",
  "/notes-master-mode/memory-game",
  "/notes-master-mode/sight-reading-game",
  "/rhythm-mode/metronome-trainer",
  "/notes-master-mode/note-speed-cards",
  "/rhythm-mode/rhythm-reading-game",
  "/rhythm-mode/rhythm-dictation-game",
];
```

From src/App.jsx (route registration pattern, lines 355-369):
```jsx
<Route path="/rhythm-mode/rhythm-reading-game"
  element={<AudioContextProvider><RhythmReadingGame /></AudioContextProvider>} />
<Route path="/rhythm-mode/rhythm-dictation-game"
  element={<AudioContextProvider><RhythmDictationGame /></AudioContextProvider>} />
```

From src/components/trail/TrailNodeModal.jsx (current routing, lines 240-244):
```javascript
case 'pitch_comparison':
  navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.pitch_comparison') } });
  break;
case 'interval_id':
  navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.interval_id') } });
  break;
```

From src/components/games/rhythm-games/MetronomeTrainer.jsx (handleNextExercise, lines 219-238):
```javascript
// Missing cases: rhythm_reading, rhythm_dictation, pitch_comparison, interval_id
switch (nextExercise.type) {
  case 'note_recognition': ... break;
  case 'sight_reading': ... break;
  case 'memory_game': ... break;
  case 'rhythm': ... break;
  case 'boss_challenge': ... break;
  default: navigate('/trail');
}
```

From 09-UI-SPEC.md (i18n key structure, full JSON):
```json
{
  "games": {
    "noteComparison": {
      "title": "Note Comparison", "question": "Which note is HIGHER?",
      "higher": "HIGHER", "lower": "LOWER",
      "reveal": { "higher": "HIGHER ▲", "lower": "LOWER ▼" },
      "listening": "Listen...", "playAgain": "Play Again",
      "startGame": "Start Listening", "correct": "Correct!", "wrong": "Try again!",
      "errors": { "audioUnavailable": "Audio not available. Tap to try again.", "generationFailed": "Something went wrong. Go back and try again." }
    },
    "intervalGame": {
      "title": "Interval Training", "question": "How far apart are the notes?",
      "step": "Step", "stepHint": "next door",
      "skip": "Skip", "skipHint": "jump one",
      "leap": "Leap", "leapHint": "far apart",
      "listening": "Listen...", "playAgain": "Play Again",
      "startGame": "Start Listening", "correct": "Correct!", "wrong": "Try again!",
      "errors": { "audioUnavailable": "Audio not available. Tap to try again.", "generationFailed": "Something went wrong. Go back and try again." }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register routes in App.jsx + update TrailNodeModal routing + add i18n keys EN+HE</name>
  <files>
    src/App.jsx,
    src/components/trail/TrailNodeModal.jsx,
    src/locales/en/common.json,
    src/locales/he/common.json
  </files>
  <read_first>
    src/App.jsx,
    src/components/trail/TrailNodeModal.jsx,
    src/locales/en/common.json,
    src/locales/he/common.json,
    .planning/phases/09-ear-training-games/09-UI-SPEC.md
  </read_first>
  <action>
    **1. App.jsx — Lazy imports** (add after the RhythmDictationGame import, around line 81):
    ```javascript
    const NoteComparisonGame = lazyWithRetry(() => import("./components/games/ear-training-games/NoteComparisonGame"));
    const IntervalGame = lazyWithRetry(() => import("./components/games/ear-training-games/IntervalGame"));
    ```

    **2. App.jsx — LANDSCAPE_ROUTES** (add to the array, around line 198):
    ```javascript
    "/ear-training-mode/note-comparison-game",
    "/ear-training-mode/interval-game",
    ```

    **3. App.jsx — Route registration** (add after the rhythm-dictation-game Route, inside the protected routes section):
    ```jsx
    <Route
      path="/ear-training-mode/note-comparison-game"
      element={<AudioContextProvider><NoteComparisonGame /></AudioContextProvider>}
    />
    <Route
      path="/ear-training-mode/interval-game"
      element={<AudioContextProvider><IntervalGame /></AudioContextProvider>}
    />
    ```

    **4. TrailNodeModal.jsx — Update routing** (replace lines 240-244):

    Change `pitch_comparison` case from:
    ```javascript
    navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.pitch_comparison') } });
    ```
    To:
    ```javascript
    navigate('/ear-training-mode/note-comparison-game', { state: navState });
    ```

    Change `interval_id` case from:
    ```javascript
    navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.interval_id') } });
    ```
    To:
    ```javascript
    navigate('/ear-training-mode/interval-game', { state: navState });
    ```

    **5. src/locales/en/common.json — Add i18n keys** (inside the `"games"` object, after the existing `"rhythmDictation"` block):
    ```json
    "noteComparison": {
      "title": "Note Comparison",
      "question": "Which note is HIGHER?",
      "higher": "HIGHER",
      "lower": "LOWER",
      "reveal": {
        "higher": "HIGHER ▲",
        "lower": "LOWER ▼"
      },
      "listening": "Listen...",
      "playAgain": "Play Again",
      "startGame": "Start Listening",
      "correct": "Correct!",
      "wrong": "Try again!",
      "progress": "{{current}} / {{total}}",
      "jumpedOver": "Jumped over {{note}}",
      "jumpedOverMultiple": "{{count}} notes between",
      "errors": {
        "audioUnavailable": "Audio not available. Tap to try again.",
        "generationFailed": "Something went wrong. Go back and try again."
      }
    },
    "intervalGame": {
      "title": "Interval Training",
      "question": "How far apart are the notes?",
      "step": "Step",
      "stepHint": "next door",
      "skip": "Skip",
      "skipHint": "jump one",
      "leap": "Leap",
      "leapHint": "far apart",
      "listening": "Listen...",
      "playAgain": "Play Again",
      "startGame": "Start Listening",
      "correct": "Correct!",
      "wrong": "Try again!",
      "progress": "{{current}} / {{total}}",
      "jumpedOver": "Jumped over {{note}}",
      "jumpedOverMultiple": "{{count}} notes between",
      "errors": {
        "audioUnavailable": "Audio not available. Tap to try again.",
        "generationFailed": "Something went wrong. Go back and try again."
      }
    }
    ```

    Also add game card entries under `"games"."cards"`:
    ```json
    "noteComparison": {
      "name": "Note Comparison",
      "description": "Hear two notes and identify which is higher"
    },
    "intervalGame": {
      "name": "Interval Training",
      "description": "Identify the distance between two notes"
    }
    ```

    **6. src/locales/he/common.json — Add Hebrew i18n keys** (same location, matching keys):
    ```json
    "noteComparison": {
      "title": "השוואת צלילים",
      "question": "איזה צליל גבוה יותר?",
      "higher": "גבוה יותר",
      "lower": "נמוך יותר",
      "reveal": {
        "higher": "גבוה יותר ▲",
        "lower": "נמוך יותר ▼"
      },
      "listening": "...הקשב",
      "playAgain": "נגן שוב",
      "startGame": "התחל להאזין",
      "correct": "!נכון",
      "wrong": "!נסה שוב",
      "progress": "{{total}} / {{current}}",
      "jumpedOver": "דילג על {{note}}",
      "jumpedOverMultiple": "{{count}} צלילים באמצע",
      "errors": {
        "audioUnavailable": "שמע לא זמין. הקש לנסות שוב.",
        "generationFailed": "משהו השתבש. חזור ונסה שוב."
      }
    },
    "intervalGame": {
      "title": "זיהוי קפיצות",
      "question": "כמה רחוקים הצלילים?",
      "step": "שלב",
      "stepHint": "שכן",
      "skip": "דילוג",
      "skipHint": "קפיצה אחת",
      "leap": "קפיצה גדולה",
      "leapHint": "רחוק",
      "listening": "...הקשב",
      "playAgain": "נגן שוב",
      "startGame": "התחל להאזין",
      "correct": "!נכון",
      "wrong": "!נסה שוב",
      "progress": "{{total}} / {{current}}",
      "jumpedOver": "דילג על {{note}}",
      "jumpedOverMultiple": "{{count}} צלילים באמצע",
      "errors": {
        "audioUnavailable": "שמע לא זמין. הקש לנסות שוב.",
        "generationFailed": "משהו השתבש. חזור ונסה שוב."
      }
    }
    ```

    And Hebrew game card entries:
    ```json
    "noteComparison": {
      "name": "השוואת צלילים",
      "description": "שמע שני צלילים וזהה מי גבוה יותר"
    },
    "intervalGame": {
      "name": "זיהוי קפיצות",
      "description": "זהה את המרחק בין שני צלילים"
    }
    ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains `const NoteComparisonGame = lazyWithRetry(` with path `ear-training-games/NoteComparisonGame`
    - App.jsx contains `const IntervalGame = lazyWithRetry(` with path `ear-training-games/IntervalGame`
    - App.jsx LANDSCAPE_ROUTES contains `"/ear-training-mode/note-comparison-game"`
    - App.jsx LANDSCAPE_ROUTES contains `"/ear-training-mode/interval-game"`
    - App.jsx contains `<Route path="/ear-training-mode/note-comparison-game"`
    - App.jsx contains `<Route path="/ear-training-mode/interval-game"`
    - App.jsx Route elements wrap games in `<AudioContextProvider>`
    - TrailNodeModal.jsx `pitch_comparison` case navigates to `/ear-training-mode/note-comparison-game` (NOT `/coming-soon`)
    - TrailNodeModal.jsx `interval_id` case navigates to `/ear-training-mode/interval-game` (NOT `/coming-soon`)
    - en/common.json contains `"noteComparison"` key under `"games"`
    - en/common.json contains `"intervalGame"` key under `"games"`
    - he/common.json contains `"noteComparison"` key under `"games"`
    - he/common.json contains `"intervalGame"` key under `"games"`
    - he/common.json noteComparison.title equals "השוואת צלילים"
    - he/common.json intervalGame.step equals "שלב"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both ear training games are routable from the URL bar and from TrailNodeModal. All UI text has EN and HE translations. Both routes are landscape-compatible. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update handleNextExercise in all existing game components for ear training chaining</name>
  <files>
    src/components/games/rhythm-games/MetronomeTrainer.jsx,
    src/components/games/rhythm-games/RhythmReadingGame.jsx,
    src/components/games/rhythm-games/RhythmDictationGame.jsx,
    src/components/games/notes-master-games/NotesRecognitionGame.jsx,
    src/components/games/notes-master-games/MemoryGame.jsx,
    src/components/games/notes-master-games/NoteSpeedCards.jsx,
    src/components/games/sight-reading-game/SightReadingGame.jsx
  </files>
  <read_first>
    src/components/games/rhythm-games/MetronomeTrainer.jsx,
    src/components/games/rhythm-games/RhythmReadingGame.jsx,
    src/components/games/rhythm-games/RhythmDictationGame.jsx,
    src/components/games/notes-master-games/NotesRecognitionGame.jsx,
    src/components/games/notes-master-games/MemoryGame.jsx,
    src/components/games/notes-master-games/NoteSpeedCards.jsx,
    src/components/games/sight-reading-game/SightReadingGame.jsx
  </read_first>
  <action>
    Update the `handleNextExercise` (or `handleNextTrailExercise` in SightReadingGame) switch statement in ALL 7 existing game components to add cases for `pitch_comparison` and `interval_id`. Also add missing `rhythm_reading` and `rhythm_dictation` cases to games that don't have them yet.

    **For each game component, find the handleNextExercise switch statement and add these cases BEFORE the `default` case:**

    ```javascript
    case 'rhythm_reading':
      navigate('/rhythm-mode/rhythm-reading-game', { state: navState });
      break;
    case 'rhythm_dictation':
      navigate('/rhythm-mode/rhythm-dictation-game', { state: navState });
      break;
    case 'pitch_comparison':
      navigate('/ear-training-mode/note-comparison-game', { state: navState });
      break;
    case 'interval_id':
      navigate('/ear-training-mode/interval-game', { state: navState });
      break;
    ```

    **File-by-file specifics:**

    1. **MetronomeTrainer.jsx** (handleNextExercise ~line 219): Currently has note_recognition, sight_reading, memory_game, rhythm, boss_challenge. Add rhythm_reading, rhythm_dictation, pitch_comparison, interval_id cases before default.

    2. **RhythmReadingGame.jsx** (handleNextExercise ~line 565): Read the file to see which cases exist. Add any missing from: rhythm_reading (self — use `navigate('/rhythm-mode/rhythm-reading-game', { state: navState, replace: true }); window.location.reload();`), rhythm_dictation, pitch_comparison, interval_id.

    3. **RhythmDictationGame.jsx** (handleNextExercise ~line 378): Already has rhythm_reading and rhythm_dictation. Add pitch_comparison and interval_id cases before default.

    4. **NotesRecognitionGame.jsx** (handleNextExercise ~line 612): Read to see existing cases. Add rhythm_reading, rhythm_dictation, pitch_comparison, interval_id before default.

    5. **MemoryGame.jsx** (handleNextExercise ~line 338): Read to see existing cases. Add rhythm_reading, rhythm_dictation, pitch_comparison, interval_id before default.

    6. **NoteSpeedCards.jsx** (handleNextExercise ~line 410): Read to see existing cases. Add rhythm_reading, rhythm_dictation, pitch_comparison, interval_id before default.

    7. **SightReadingGame.jsx** (handleNextTrailExercise ~line 766): Read to see existing cases. Add rhythm_reading, rhythm_dictation, pitch_comparison, interval_id before default.

    **Important notes:**
    - When adding the `rhythm` self-routing case (same game), use `{ state: navState, replace: true }` + `window.location.reload()` pattern (forces remount for same-route navigation) — already established in MetronomeTrainer and RhythmDictationGame.
    - For `rhythm_reading` and `rhythm_dictation` routing from other games (not self), do NOT use `replace: true` + `window.location.reload()` — just navigate normally.
    - All games that already have certain cases should keep them unchanged; only ADD the missing cases.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - MetronomeTrainer.jsx handleNextExercise contains `case 'pitch_comparison'` with navigate to `/ear-training-mode/note-comparison-game`
    - MetronomeTrainer.jsx handleNextExercise contains `case 'interval_id'` with navigate to `/ear-training-mode/interval-game`
    - RhythmReadingGame.jsx handleNextExercise contains `case 'pitch_comparison'`
    - RhythmReadingGame.jsx handleNextExercise contains `case 'interval_id'`
    - RhythmDictationGame.jsx handleNextExercise contains `case 'pitch_comparison'`
    - RhythmDictationGame.jsx handleNextExercise contains `case 'interval_id'`
    - NotesRecognitionGame.jsx handleNextExercise contains `case 'pitch_comparison'`
    - NotesRecognitionGame.jsx handleNextExercise contains `case 'interval_id'`
    - MemoryGame.jsx handleNextExercise contains `case 'pitch_comparison'`
    - MemoryGame.jsx handleNextExercise contains `case 'interval_id'`
    - NoteSpeedCards.jsx handleNextExercise contains `case 'pitch_comparison'`
    - NoteSpeedCards.jsx handleNextExercise contains `case 'interval_id'`
    - SightReadingGame.jsx handleNextTrailExercise contains `case 'pitch_comparison'`
    - SightReadingGame.jsx handleNextTrailExercise contains `case 'interval_id'`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 existing game components can chain to pitch_comparison and interval_id exercises via their handleNextExercise/handleNextTrailExercise switch statements. Trail multi-exercise nodes that mix ear training with other game types will route correctly between all exercise types.</done>
</task>

</tasks>

<verification>
- `npm run build` — production build succeeds with all new routes and imports
- `npm run lint` — no lint errors across all modified files
- `npm run test:run` — full test suite passes (no regressions in existing games)
- App.jsx has both lazy imports + route registrations + LANDSCAPE_ROUTES entries
- TrailNodeModal no longer routes ear training types to /coming-soon
- All 7 existing game handleNextExercise functions contain pitch_comparison and interval_id cases
- Both EN and HE locale files contain noteComparison and intervalGame keys
</verification>

<success_criteria>
- Both ear training games are navigable from URL and from trail nodes
- TrailNodeModal routes pitch_comparison/interval_id to actual game components
- All game UI text appears in English and Hebrew
- All 7 existing game components can chain to ear training exercises
- Build, lint, and full test suite pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-ear-training-games/09-04-SUMMARY.md`
</output>
