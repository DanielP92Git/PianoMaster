---
phase: 08-audio-infrastructure-rhythm-games
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified:
  - src/App.jsx
  - src/components/trail/TrailNodeModal.jsx
  - src/locales/en/common.json
  - src/locales/he/common.json
  - public/sw.js
autonomous: true
requirements: [INFRA-07, INFRA-08, RTAP-05, RDICT-06]

must_haves:
  truths:
    - "Navigating to /rhythm-mode/rhythm-reading-game renders RhythmReadingGame component"
    - "Navigating to /rhythm-mode/rhythm-dictation-game renders RhythmDictationGame component"
    - "Tapping a trail node with exercise type rhythm_tap opens RhythmReadingGame (not ComingSoon)"
    - "Tapping a trail node with exercise type rhythm_dictation opens RhythmDictationGame (not ComingSoon)"
    - "Both game routes appear in LANDSCAPE_ROUTES for proper layout handling"
    - "All rhythm game UI strings appear in both English and Hebrew"
    - "Both new game routes are accessible in the installed PWA after update without stale cache serving old assets"
  artifacts:
    - path: "src/App.jsx"
      provides: "Route registration for both rhythm games"
      contains: "rhythm-reading-game"
    - path: "src/components/trail/TrailNodeModal.jsx"
      provides: "Trail navigation to real game routes"
      contains: "/rhythm-mode/rhythm-reading-game"
    - path: "src/locales/en/common.json"
      provides: "English i18n keys for rhythm games"
      contains: "rhythmReading"
    - path: "src/locales/he/common.json"
      provides: "Hebrew i18n keys for rhythm games"
      contains: "rhythmReading"
    - path: "public/sw.js"
      provides: "Updated cache version"
      contains: "pianomaster-v9"
  key_links:
    - from: "src/App.jsx"
      to: "RhythmReadingGame.jsx"
      via: "lazyWithRetry import + Route element"
      pattern: "lazyWithRetry.*RhythmReadingGame"
    - from: "src/App.jsx"
      to: "RhythmDictationGame.jsx"
      via: "lazyWithRetry import + Route element"
      pattern: "lazyWithRetry.*RhythmDictationGame"
    - from: "src/components/trail/TrailNodeModal.jsx"
      to: "/rhythm-mode/rhythm-reading-game"
      via: "navigate() in exercise type switch"
      pattern: "rhythm-reading-game"
---

<objective>
Wire both rhythm game components into the app routing, update trail navigation from ComingSoon to real game routes, add all i18n keys for rhythm game UI in English and Hebrew, and bump the service worker cache version.

Purpose: Connect the game components (Plans 02-03) to the rest of the app. Without this plan, the games exist as unreachable components. This plan makes them launchable from both the trail and direct URL navigation, with complete i18n support.

Output: Updated App.jsx routes, TrailNodeModal navigation, complete EN+HE i18n, and cache-busted service worker.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-audio-infrastructure-rhythm-games/08-CONTEXT.md
@.planning/phases/08-audio-infrastructure-rhythm-games/08-UI-SPEC.md
@.planning/phases/08-audio-infrastructure-rhythm-games/08-02-SUMMARY.md
@.planning/phases/08-audio-infrastructure-rhythm-games/08-03-SUMMARY.md

<interfaces>
<!-- Key existing code patterns to follow -->

From src/App.jsx (existing patterns):
```javascript
// Line 43: import { lazyWithRetry } from "./utils/lazyWithRetry";
// Line 74: const MetronomeTrainer = lazyWithRetry(() => import("./components/games/rhythm-games/MetronomeTrainer"));

// Lines 185-193: LANDSCAPE_ROUTES array
const LANDSCAPE_ROUTES = [
  "/notes-master-mode/notes-recognition-game",
  "/notes-master-mode/sight-reading-game",
  "/rhythm-mode/metronome-trainer",
  "/notes-master-mode/memory-game",
  "/notes-master-mode/note-speed-cards",
];

// Line 343: Route for MetronomeTrainer (wrapped in AudioContextProvider)
<Route path="/rhythm-mode/metronome-trainer"
  element={<AudioContextProvider><MetronomeTrainer /></AudioContextProvider>}
/>
```

From src/components/trail/TrailNodeModal.jsx (existing routing):
```javascript
// Lines 231-235: Current ComingSoon routing for rhythm types
case 'rhythm_tap':
  navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.rhythm_tap') } });
  break;
case 'rhythm_dictation':
  navigate('/coming-soon', { state: { ...navState, gameName: t('trail:exerciseTypes.rhythm_dictation') } });
  break;
```

From public/sw.js (line 4):
```javascript
const CACHE_NAME = "pianomaster-v8";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register routes, update trail navigation, and bump service worker cache</name>
  <files>src/App.jsx, src/components/trail/TrailNodeModal.jsx, public/sw.js</files>
  <read_first>
    - src/App.jsx (full file — understand lazy import section near lines 49-75, LANDSCAPE_ROUTES near lines 185-193, Route definitions near lines 341-350)
    - src/components/trail/TrailNodeModal.jsx (full file — understand the exercise type switch statement, navState construction)
    - public/sw.js (line 4 — current CACHE_NAME value)
  </read_first>
  <action>
    **1. Add lazy imports to `src/App.jsx` (after line 74, the MetronomeTrainer import):**

    ```javascript
    const RhythmReadingGame = lazyWithRetry(
      () => import("./components/games/rhythm-games/RhythmReadingGame")
    );
    const RhythmDictationGame = lazyWithRetry(
      () => import("./components/games/rhythm-games/RhythmDictationGame")
    );
    ```

    **2. Add both paths to LANDSCAPE_ROUTES array (near line 189, after metronome-trainer):**

    ```javascript
    "/rhythm-mode/rhythm-reading-game",
    "/rhythm-mode/rhythm-dictation-game",
    ```

    Note: RhythmReadingGame is landscape-locked. RhythmDictationGame is portrait-primary but should still be in LANDSCAPE_ROUTES for consistent layout handling.

    **3. Add Route elements (after the MetronomeTrainer route, near line 345):**

    ```jsx
    <Route
      path="/rhythm-mode/rhythm-reading-game"
      element={
        <AudioContextProvider>
          <RhythmReadingGame />
        </AudioContextProvider>
      }
    />
    <Route
      path="/rhythm-mode/rhythm-dictation-game"
      element={
        <AudioContextProvider>
          <RhythmDictationGame />
        </AudioContextProvider>
      }
    />
    ```

    CRITICAL: Wrap each in `<AudioContextProvider>` at route level, identical to MetronomeTrainer. Do NOT add AudioContextProvider inside the game components.

    **4. Update TrailNodeModal.jsx exercise type routing:**

    Replace the `rhythm_tap` and `rhythm_dictation` cases (around lines 231-235) that currently navigate to `/coming-soon`:

    ```javascript
    case 'rhythm_tap':
      navigate('/rhythm-mode/rhythm-reading-game', { state: navState });
      break;
    case 'rhythm_dictation':
      navigate('/rhythm-mode/rhythm-dictation-game', { state: navState });
      break;
    ```

    Keep `navState` construction unchanged — it already contains `{ nodeId, nodeConfig, exerciseIndex, totalExercises, exerciseType }`.

    **5. Bump service worker cache in `public/sw.js` (line 4):**

    Change:
    ```javascript
    const CACHE_NAME = "pianomaster-v8";
    ```
    To:
    ```javascript
    const CACHE_NAME = "pianomaster-v9";
    ```

    Do NOT change `ACCESSORY_CACHE_NAME` or `CACHE_WHITELIST` logic — only the main cache name string.
  </action>
  <verify>
    <automated>grep -n "RhythmReadingGame\|RhythmDictationGame" src/App.jsx && grep -n "rhythm-reading-game\|rhythm-dictation-game" src/components/trail/TrailNodeModal.jsx && grep -n "pianomaster-v9" public/sw.js</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contains `const RhythmReadingGame = lazyWithRetry(`
    - src/App.jsx contains `const RhythmDictationGame = lazyWithRetry(`
    - src/App.jsx contains `path="/rhythm-mode/rhythm-reading-game"`
    - src/App.jsx contains `path="/rhythm-mode/rhythm-dictation-game"`
    - src/App.jsx LANDSCAPE_ROUTES contains `/rhythm-mode/rhythm-reading-game`
    - src/App.jsx LANDSCAPE_ROUTES contains `/rhythm-mode/rhythm-dictation-game`
    - src/App.jsx wraps both routes in `<AudioContextProvider>`
    - TrailNodeModal.jsx `rhythm_tap` case navigates to `/rhythm-mode/rhythm-reading-game` (not `/coming-soon`)
    - TrailNodeModal.jsx `rhythm_dictation` case navigates to `/rhythm-mode/rhythm-dictation-game` (not `/coming-soon`)
    - public/sw.js line 4 reads `const CACHE_NAME = "pianomaster-v9"`
    - public/sw.js still contains `ACCESSORY_CACHE_NAME = "pianomaster-accessories-v2"` (unchanged)
  </acceptance_criteria>
  <done>Both rhythm games are reachable via URL navigation and trail node tapping. Service worker cache version bumped for new assets. INFRA-07 complete.</done>
</task>

<task type="auto">
  <name>Task 2: Add complete i18n keys for rhythm games in English and Hebrew</name>
  <files>src/locales/en/common.json, src/locales/he/common.json</files>
  <read_first>
    - src/locales/en/common.json (full file — understand existing `games` section structure, especially `games.metronomeTrainer` nesting pattern starting around line 702)
    - src/locales/he/common.json (full file — understand existing Hebrew translations, RTL patterns)
    - .planning/phases/08-audio-infrastructure-rhythm-games/08-UI-SPEC.md (Copywriting Contract table and i18n Key Structure section)
    - .planning/phases/08-audio-infrastructure-rhythm-games/08-RESEARCH.md (Pattern 8: i18n Key Structure with minimum required keys)
  </read_first>
  <action>
    Add new i18n keys under the `games` section of both `common.json` files. Follow the existing `games.metronomeTrainer` nesting convention exactly.

    **English (`src/locales/en/common.json`) — add after the `games.metronomeTrainer` object (but still inside `games`):**

    ```json
    "rhythmReading": {
      "title": "Rhythm Reading",
      "progressLabel": "Exercise {{current}} of {{total}}",
      "startGame": "Start Game",
      "tapArea": {
        "tapHere": "TAP",
        "accuracy": {
          "perfect": "PERFECT!",
          "good": "GOOD!",
          "miss": "MISS"
        }
      },
      "countdown": {
        "getReady": "Get Ready!",
        "go": "GO!"
      },
      "errors": {
        "audioUnavailable": "Audio not available. Tap to try again.",
        "patternError": "Something went wrong. Go back and try again."
      }
    },
    "rhythmDictation": {
      "title": "Rhythm Dictation",
      "progressLabel": "Exercise {{current}} of {{total}}",
      "startGame": "Start Game",
      "listening": "Listen...",
      "playAgain": "Play Again",
      "chooseRhythm": "Choose the rhythm you heard",
      "correct": "Correct!",
      "wrong": "Try again!",
      "errors": {
        "audioUnavailable": "Audio not available. Tap to try again.",
        "patternError": "Something went wrong. Go back and try again."
      }
    }
    ```

    **Hebrew (`src/locales/he/common.json`) — add matching keys with Hebrew translations:**

    ```json
    "rhythmReading": {
      "title": "קריאת קצב",
      "progressLabel": "תרגיל {{current}} מתוך {{total}}",
      "startGame": "התחל משחק",
      "tapArea": {
        "tapHere": "הקש",
        "accuracy": {
          "perfect": "!מושלם",
          "good": "!טוב",
          "miss": "פספסת"
        }
      },
      "countdown": {
        "getReady": "!היכונו",
        "go": "!קדימה"
      },
      "errors": {
        "audioUnavailable": "שמע לא זמין. הקש לנסות שוב.",
        "patternError": "משהו השתבש. חזור ונסה שוב."
      }
    },
    "rhythmDictation": {
      "title": "שמיעת קצב",
      "progressLabel": "תרגיל {{current}} מתוך {{total}}",
      "startGame": "התחל משחק",
      "listening": "...הקשב",
      "playAgain": "נגן שוב",
      "chooseRhythm": "בחר את הקצב ששמעת",
      "correct": "!נכון",
      "wrong": "!נסה שוב",
      "errors": {
        "audioUnavailable": "שמע לא זמין. הקש לנסות שוב.",
        "patternError": "משהו השתבש. חזור ונסה שוב."
      }
    }
    ```

    Also add game cards entries under `games.cards` for both games (following the existing `metronomeTrainer` card pattern):

    **English:**
    ```json
    "rhythmReading": {
      "name": "Rhythm Reading",
      "description": "See rhythm notation and tap along in time"
    },
    "rhythmDictation": {
      "name": "Rhythm Dictation",
      "description": "Hear a rhythm and pick the correct notation"
    }
    ```

    **Hebrew:**
    ```json
    "rhythmReading": {
      "name": "קריאת קצב",
      "description": "ראה תווים של קצב והקש בזמן"
    },
    "rhythmDictation": {
      "name": "שמיעת קצב",
      "description": "שמע קצב ובחר את התווים הנכונים"
    }
    ```

    IMPORTANT: JSON files must remain valid JSON after editing. Use a JSON formatter or be very careful with trailing commas. Add the new keys right after `games.metronomeTrainer` (for the game UI keys) and after the last entry in `games.cards` (for the card entries).
  </action>
  <verify>
    <automated>node -e "const en = require('./src/locales/en/common.json'); const he = require('./src/locales/he/common.json'); const checks = [en.games.rhythmReading?.title === 'Rhythm Reading', en.games.rhythmDictation?.title === 'Rhythm Dictation', he.games.rhythmReading?.title === 'קריאת קצב', he.games.rhythmDictation?.title === 'שמיעת קצב', en.games.rhythmReading?.tapArea?.accuracy?.perfect === 'PERFECT!', en.games.rhythmDictation?.playAgain === 'Play Again', he.games.rhythmReading?.tapArea?.accuracy?.perfect === '!מושלם', en.games.cards?.rhythmReading?.name === 'Rhythm Reading', en.games.cards?.rhythmDictation?.name === 'Rhythm Dictation']; console.log(checks.every(Boolean) ? 'ALL_KEYS_PRESENT' : 'MISSING: ' + checks.map((v,i) => v ? '' : i).filter(x=>x!=='').join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - src/locales/en/common.json is valid JSON (node -e "require('./src/locales/en/common.json')" succeeds)
    - src/locales/he/common.json is valid JSON (node -e "require('./src/locales/he/common.json')" succeeds)
    - EN common.json contains `games.rhythmReading.title` = "Rhythm Reading"
    - EN common.json contains `games.rhythmDictation.title` = "Rhythm Dictation"
    - EN common.json contains `games.rhythmReading.tapArea.accuracy.perfect` = "PERFECT!"
    - EN common.json contains `games.rhythmReading.tapArea.accuracy.good` = "GOOD!"
    - EN common.json contains `games.rhythmReading.tapArea.accuracy.miss` = "MISS"
    - EN common.json contains `games.rhythmReading.countdown.go` = "GO!"
    - EN common.json contains `games.rhythmDictation.playAgain` = "Play Again"
    - EN common.json contains `games.rhythmDictation.listening` = "Listen..."
    - EN common.json contains `games.rhythmDictation.correct` = "Correct!"
    - EN common.json contains `games.rhythmDictation.wrong` = "Try again!"
    - HE common.json contains `games.rhythmReading.title` = "קריאת קצב"
    - HE common.json contains `games.rhythmDictation.title` = "שמיעת קצב"
    - HE common.json contains `games.rhythmReading.tapArea.accuracy.perfect` = "!מושלם"
    - HE common.json contains `games.rhythmDictation.playAgain` = "נגן שוב"
    - EN common.json contains `games.cards.rhythmReading.name`
    - EN common.json contains `games.cards.rhythmDictation.name`
  </acceptance_criteria>
  <done>Complete i18n coverage for both rhythm games in English and Hebrew. All UI strings, feedback text, error messages, and game card descriptions are translated. INFRA-08 complete.</done>
</task>

</tasks>

<verification>
Route verification: `grep -c "rhythm-reading-game\|rhythm-dictation-game" src/App.jsx` returns at least 4 (2 routes + 2 LANDSCAPE_ROUTES)
Trail navigation: `grep "coming-soon" src/components/trail/TrailNodeModal.jsx` should NOT show rhythm_tap or rhythm_dictation cases
i18n validation: Both JSON files parse without error
Service worker: `grep "pianomaster-v9" public/sw.js` returns match
Build succeeds: `npm run build`
Full test suite: `npm run test:run`
Lint passes: `npm run lint`
</verification>

<success_criteria>
- Both rhythm games accessible via direct URL and trail node navigation
- Trail nodes with rhythm_tap type open RhythmReadingGame (not ComingSoon)
- Trail nodes with rhythm_dictation type open RhythmDictationGame (not ComingSoon)
- Both routes wrapped in AudioContextProvider at route level
- Complete EN+HE i18n for all rhythm game UI strings per INFRA-08
- Service worker cache version is pianomaster-v9 per INFRA-07
- Both games integrated with VictoryScreen via route navigation per RTAP-05, RDICT-06
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-audio-infrastructure-rhythm-games/08-04-SUMMARY.md`
</output>
