---
phase: 34-responsive-rhythm-renderers-non-arcade
plan: 05
type: execute
wave: 3
depends_on: ["34-01", "34-02", "34-03"]
files_modified:
  - src/components/games/rhythm-games/components/CountdownOverlay.jsx
  - src/components/games/rhythm-games/components/FloatingFeedback.jsx
  - src/components/games/rhythm-games/components/MetronomeDisplay.jsx
  - src/components/games/rhythm-games/components/BossIntroOverlay.jsx
  - src/components/games/rhythm-games/components/RhythmGameSettings.jsx
  - src/components/games/rhythm-games/components/RhythmGameSetup.jsx
  - src/components/games/rhythm-games/components/DictationChoiceCard.jsx
autonomous: true
requirements:
  - WRAPPER-02
  - WRAPPER-03
tags:
  - rhythm
  - responsive
  - supporting-components
  - glassmorphism
must_haves:
  truths:
    - "CountdownOverlay text bumps from text-3xl to text-3xl md:text-5xl lg:text-6xl"
    - "FloatingFeedback text bumps from text-3xl to text-3xl md:text-4xl"
    - "MetronomeDisplay circle sizes bump to md:h-12 md:w-12 md:text-base (replacing the sm: bug)"
    - "RhythmGameSettings glass-converted per D-18 — no more bg-white text-gray-700 light theme on glass page"
    - "BossIntroOverlay receives responsive sanity check only — visual treatment unchanged per D-11"
    - "DictationChoiceCard sized to fit 2x2 cell from Plan 04's grid (no breakage)"
  artifacts:
    - path: src/components/games/rhythm-games/components/RhythmGameSettings.jsx
      provides: "Glass-pattern conversion (D-18)"
      contains: "bg-white/10"
    - path: src/components/games/rhythm-games/components/CountdownOverlay.jsx
      provides: "Tablet-responsive countdown text"
      contains: "md:text-5xl"
    - path: src/components/games/rhythm-games/components/MetronomeDisplay.jsx
      provides: "Tablet-responsive metronome dots"
      contains: "md:h-12"
  key_links:
    - from: 34-AUDIT.md
      to: this plan
      via: "fix-owner mapping table"
      pattern: "Plan 05"
---

<objective>
Apply per-component fixes from `34-AUDIT.md` to the 5 supporting components + 2 setup components in WRAPPER-03 + WRAPPER-02. The biggest single change is the `RhythmGameSettings` glass conversion (D-18). Other changes are responsive size bumps that untie tablets from phone-only Tailwind classes.

Purpose: Wave 3 parallel with Plan 04 (no file overlap — 04 owns renderers + wrappers; 05 owns supporting components in `components/`). After this plan, every supporting component renders correctly across all 4 quadrants.
Output: 7 MODIFIED files (5 supporting + RhythmGameSettings glass + DictationChoiceCard sanity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-CONTEXT.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-RESEARCH.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-PATTERNS.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-AUDIT.md
@CLAUDE.md
@src/components/games/rhythm-games/components/CountdownOverlay.jsx
@src/components/games/rhythm-games/components/FloatingFeedback.jsx
@src/components/games/rhythm-games/components/MetronomeDisplay.jsx
@src/components/games/rhythm-games/components/BossIntroOverlay.jsx
@src/components/games/rhythm-games/components/RhythmGameSettings.jsx
@src/components/games/rhythm-games/components/RhythmGameSetup.jsx
@src/components/games/rhythm-games/components/DictationChoiceCard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump responsive sizing on 3 supporting components (CountdownOverlay, FloatingFeedback, MetronomeDisplay) + BossIntro sanity (WRAPPER-03)</name>
  <files>src/components/games/rhythm-games/components/CountdownOverlay.jsx, src/components/games/rhythm-games/components/FloatingFeedback.jsx, src/components/games/rhythm-games/components/MetronomeDisplay.jsx, src/components/games/rhythm-games/components/BossIntroOverlay.jsx</files>
  <read_first>
    - All 4 component files (single-pass read each)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-PATTERNS.md (sections "CountdownOverlay.jsx", "FloatingFeedback.jsx", "MetronomeDisplay.jsx", "BossIntroOverlay.jsx")
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-RESEARCH.md ("13-Component Audit Punch List" — Supporting Components rows)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-AUDIT.md (per-component findings — ANY deviation from pre-research hypothesis must be honored)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-CONTEXT.md (D-11 — BossIntro responsive sanity ONLY, no visual treatment change)
  </read_first>
  <action>
    **Pre-flight check:** Read 34-AUDIT.md "Pre-Research Hypothesis Confirmation" section. If the audit confirmed all the pre-research findings below, proceed as written. If audit DISCONFIRMED any (e.g. "MetronomeDisplay is actually fine"), skip that change and update the SUMMARY.

    **(1) CountdownOverlay.jsx (line ~35 per PATTERNS.md):**
    Find:
    ```jsx
    <div className={`text-3xl font-bold ${colorClass} ${pulseClass} select-none`}>
    ```
    Change `text-3xl` to `text-3xl md:text-5xl lg:text-6xl`. Result:
    ```jsx
    <div className={`text-3xl md:text-5xl lg:text-6xl font-bold ${colorClass} ${pulseClass} select-none`}>
    ```
    Tailwind class string stays inside a template literal here — that's fine because the LITERAL substrings (`text-3xl md:text-5xl lg:text-6xl font-bold`) are statically detectable by purge. ONLY concatenated dynamic class names (`text-${size}`) break purge.

    **(2) FloatingFeedback.jsx (line ~75-85 per PATTERNS.md — find the className that includes `text-3xl`):**
    Bump `text-3xl` to `text-3xl md:text-4xl`. Preserve the existing `position: absolute` style object.

    **(3) MetronomeDisplay.jsx (lines 39-49 per PATTERNS.md):**
    Current:
    ```jsx
    className={`flex items-center justify-center rounded-full border-2 font-bold transition-all duration-150
      h-9 w-9 text-xs sm:h-10 sm:w-10 sm:text-sm
    ${isCurrentBeat ? ... : ...}`}
    ```
    Add `md:h-12 md:w-12 md:text-base` after the `sm:` chain. Result:
    ```jsx
    className={`flex items-center justify-center rounded-full border-2 font-bold transition-all duration-150
      h-9 w-9 text-xs sm:h-10 sm:w-10 sm:text-sm md:h-12 md:w-12 md:text-base
    ${isCurrentBeat ? ... : ...}`}
    ```
    Per RESEARCH audit row: this fixes the "`sm:` not `md:`" bug where tablets stayed at phone sizing.

    **(4) BossIntroOverlay.jsx (D-11 — responsive sanity ONLY):**
    DO NOT change the visual treatment, motion, colors, or animations. ONLY verify there's no clipping at 4 quadrants.

    Per AUDIT findings: if audit says "BossIntro fine at all quadrants", make NO changes to this file (skip; remove from files_modified in SUMMARY). If audit flags a specific clipping issue, apply the minimal class fix to address it (e.g. adding `max-w-screen-sm md:max-w-screen-md` to a container if it overflows on phone-portrait). Document the AUDIT-driven change in the commit message.

    Commit message: `feat(34-05): responsive size bumps on CountdownOverlay/FloatingFeedback/MetronomeDisplay (WRAPPER-03)`.

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/games/rhythm-games/components/CountdownOverlay.jsx','utf8');const f=fs.readFileSync('src/components/games/rhythm-games/components/FloatingFeedback.jsx','utf8');const m=fs.readFileSync('src/components/games/rhythm-games/components/MetronomeDisplay.jsx','utf8');const errs=[];if(!/md:text-5xl/.test(c))errs.push('CountdownOverlay missing md:text-5xl');if(!/lg:text-6xl/.test(c))errs.push('CountdownOverlay missing lg:text-6xl');if(!/md:text-4xl/.test(f))errs.push('FloatingFeedback missing md:text-4xl');if(!/md:h-12/.test(m))errs.push('MetronomeDisplay missing md:h-12');if(!/md:w-12/.test(m))errs.push('MetronomeDisplay missing md:w-12');if(!/md:text-base/.test(m))errs.push('MetronomeDisplay missing md:text-base');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "text-3xl\\s+md:text-5xl\\s+lg:text-6xl" src/components/games/rhythm-games/components/CountdownOverlay.jsx` matches one line
    - `grep -nE "md:text-4xl" src/components/games/rhythm-games/components/FloatingFeedback.jsx` matches at least one line
    - `grep -nE "md:h-12\\s+md:w-12\\s+md:text-base" src/components/games/rhythm-games/components/MetronomeDisplay.jsx` matches one line
    - BossIntroOverlay diff is empty OR contains only a minimal AUDIT-driven container-size fix (no motion/color changes — `git diff src/components/games/rhythm-games/components/BossIntroOverlay.jsx | grep -E "(motion|amber|gold|Crown)"` returns nothing meaningful)
    - `npm run build` exit 0
    - `npm run test:run` exit 0
  </acceptance_criteria>
  <done>3 supporting components bumped; BossIntro responsive-sanity verified per AUDIT; build + tests green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: RhythmGameSettings glass conversion (D-18) + RhythmGameSetup audit-only + DictationChoiceCard sanity (WRAPPER-02, WRAPPER-03)</name>
  <files>src/components/games/rhythm-games/components/RhythmGameSettings.jsx, src/components/games/rhythm-games/components/RhythmGameSetup.jsx, src/components/games/rhythm-games/components/DictationChoiceCard.jsx</files>
  <read_first>
    - src/components/games/rhythm-games/components/RhythmGameSettings.jsx (full file — glass conversion target; lines 70-160 per PATTERNS.md contain bg-white/text-gray-700/border-gray-300)
    - src/components/games/rhythm-games/components/RhythmGameSetup.jsx (full file — audit only; delegates to UnifiedGameSettings per PATTERNS.md)
    - src/components/games/rhythm-games/components/DictationChoiceCard.jsx (lines 22-31 STATE_CLASSES + lines 144-165 layout)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-PATTERNS.md (sections "RhythmGameSettings.jsx", "RhythmGameSetup.jsx", "DictationChoiceCard.jsx")
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-AUDIT.md (RhythmGameSettings finding — confirm bg-white is actually visible, NOT overridden by Modal surface; RhythmGameSetup finding — confirm out-of-scope per D-10)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-CONTEXT.md (D-10 RhythmGameSetup out of scope unless rhythm-specific issue; D-18 glass conversion locked)
    - CLAUDE.md (Design System § Glass Card Pattern — exact class strings to use)
  </read_first>
  <action>
    **Pre-flight check (D-18 confirmation):**
    Read 34-AUDIT.md "RhythmGameSettings" row. If audit confirms `bg-white text-gray-700` is actually rendered on a glass page (visible bug), proceed with full glass conversion. If audit reports the Modal surface overrides it and the component looks fine, STILL proceed because D-18 is locked — but note in commit message that conversion is decision-driven, not bug-driven.

    **(1) RhythmGameSettings.jsx — glass conversion (D-18, lines 70-160):**

    Read the file once. Identify every occurrence of these legacy classes and replace per the mapping below (per PATTERNS.md "Glass pattern source"):

    | Legacy class | Replacement |
    |---|---|
    | `bg-white` (on container/cards) | `bg-white/10 backdrop-blur-md` |
    | `border-gray-300` | `border-white/20` |
    | `text-gray-700` (label/body text) | `text-white/70` |
    | `text-gray-900` (heading text, if any) | `text-white` |
    | `bg-blue-500 text-white` (selected state, if used) | `bg-indigo-500/30 border-indigo-400 text-white` |
    | `bg-white text-gray-700 border-gray-300` (unselected button) | `bg-white/5 border-white/20 text-white hover:bg-white/10 hover:border-white/40` |
    | `bg-gray-200` (range input track or similar) | `bg-white/15` |
    | `hover:bg-gray-100` | `hover:bg-white/10` |
    | `border-gray-200` (dividers) | `border-white/10` |

    Keep all functional behavior (state, callbacks, props). ONLY classes change. Preserve `rounded-*` and `shadow-*` classes — if a card has `rounded-lg shadow-md`, KEEP those and just swap the background/border/text.

    For the outer container card, use the canonical glass pattern:
    ```
    bg-white/10 backdrop-blur-md border border-white/20 rounded-xl shadow-lg
    ```

    Test: open the modal in dev (any rhythm game → settings cog) and confirm white text on glass surface, no light-theme remnants.

    **(2) RhythmGameSetup.jsx (D-10 audit-only):**

    Per D-10 + AUDIT findings: this component delegates to `UnifiedGameSettings`. If AUDIT found a rhythm-SPECIFIC issue (not a UnifiedGameSettings issue), apply the minimal fix here. If issues are in UnifiedGameSettings, log them in 34-05-SUMMARY.md as deferred items per D-10 — do NOT modify UnifiedGameSettings.

    Default action: NO file change. If file is unchanged, REMOVE it from this plan's `files_modified` list when writing the SUMMARY.

    **(3) DictationChoiceCard.jsx (sanity check after Plan 04 grid swap):**

    Plan 04 changed `RhythmDictationQuestion` to use a 2-col grid (3 cards with last spanning 2 cols). DictationChoiceCard's current `min-h-[96px] w-full` layout (lines 144-165 per PATTERNS.md) needs to fit a 2x2 cell.

    Steps:
    a. Read DictationChoiceCard.jsx fully.
    b. Verify `w-full` is present on the outer card so it fills the grid cell width. Verify `min-h-[96px]` is appropriate for the new cell (cells are roughly equal-width on phone-portrait).
    c. If the card has hard-coded inner widths that conflict with `col-span-2` for the last odd card, add a sanity fix (e.g. `flex-1` on inner content). Otherwise NO change needed.

    Default action if existing `w-full + min-h-[96px]` works: NO file change. Remove from files_modified in SUMMARY.

    Commit message: `feat(34-05): glass-convert RhythmGameSettings (D-18) + audit-driven sanity fixes`.

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/components/games/rhythm-games/components/RhythmGameSettings.jsx','utf8');const errs=[];if(/\\bbg-white\\b(?!\\/)/.test(t))errs.push('Still contains bg-white (no /opacity)');if(/\\btext-gray-700\\b/.test(t))errs.push('Still contains text-gray-700');if(/\\bborder-gray-300\\b/.test(t))errs.push('Still contains border-gray-300');if(!/bg-white\\/(10|5|15|20)/.test(t))errs.push('Missing glass bg-white/N variants');if(!/text-white/.test(t))errs.push('Missing text-white');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK — RhythmGameSettings glass-converted')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\\bbg-white\\b(?!\\/)" src/components/games/rhythm-games/components/RhythmGameSettings.jsx` returns 0 (no bare `bg-white` — only `bg-white/N` opacity variants allowed)
    - `grep -c "text-gray-700" src/components/games/rhythm-games/components/RhythmGameSettings.jsx` returns 0
    - `grep -c "border-gray-300" src/components/games/rhythm-games/components/RhythmGameSettings.jsx` returns 0
    - `grep -cE "bg-white\\/(5|10|15|20)" src/components/games/rhythm-games/components/RhythmGameSettings.jsx` returns at least 1 (glass bg present)
    - `grep -c "text-white" src/components/games/rhythm-games/components/RhythmGameSettings.jsx` returns at least 1
    - RhythmGameSettings preserves all original prop interfaces (no behavioral changes — `git diff` shows class changes only, no new/removed callbacks or state)
    - `npm run build` exit 0
    - `npm run test:run` exit 0
  </acceptance_criteria>
  <done>RhythmGameSettings glass-converted; RhythmGameSetup + DictationChoiceCard handled per AUDIT; build + tests green; committed.</done>
</task>

</tasks>

<verification>
- `npm run build` exit 0
- `npm run test:run` exit 0
- All grep gates pass
- 34-AUDIT.md cross-reference: every component this plan owns has an AUDIT row marked ✓ (fixed) or 🟡 (out of scope, deferred)
</verification>

<success_criteria>

- 5 supporting components rendered correctly across all 4 quadrants (verified by AUDIT findings + grep checks)
- RhythmGameSettings glass-converted per D-18; no light-theme remnants
- BossIntro responsive sanity verified — visual treatment unchanged per D-11
- Setup screens (RhythmGameSetup) audit complete; deferred items logged in SUMMARY per D-10
- Build + full test suite green
  </success_criteria>

<output>
After completion, create `.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-05-SUMMARY.md`
</output>
