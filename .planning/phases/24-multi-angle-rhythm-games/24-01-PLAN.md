---
phase: 24-multi-angle-rhythm-games
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/icons/rhythm/quarter-note.svg
  - src/assets/icons/rhythm/half-note.svg
  - src/assets/icons/rhythm/whole-note.svg
  - src/assets/icons/rhythm/eighth-note.svg
  - src/assets/icons/rhythm/sixteenth-note.svg
  - src/assets/icons/rhythm/dotted-quarter.svg
  - src/assets/icons/rhythm/dotted-half.svg
  - src/assets/icons/rhythm/quarter-rest.svg
  - src/assets/icons/rhythm/half-rest.svg
  - src/assets/icons/rhythm/whole-rest.svg
  - src/components/games/rhythm-games/utils/durationInfo.js
  - src/components/games/rhythm-games/utils/durationInfo.test.js
  - src/components/games/rhythm-games/components/DurationCard.jsx
  - src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx
  - src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx
  - src/components/trail/__tests__/TrailNodeModal.test.jsx
  - src/data/constants.js
  - src/locales/en/trail.json
  - src/locales/he/trail.json
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-4

must_haves:
  truths:
    - "10 SVG sprite files exist in src/assets/icons/rhythm/ and render as React components via ?react import"
    - "DURATION_INFO lookup maps every VexFlow duration code (q, h, w, 8, 16, qd, hd, qr, hr, wr) to svg filename, i18n key, durationUnits, and isRest"
    - "DurationCard renders either an SVG icon or text, with 4 visual states (default, correct, wrong, dimmed)"
    - "EXERCISE_TYPES includes VISUAL_RECOGNITION and SYLLABLE_MATCHING"
    - "i18n keys exist for both exercise type names and all game UI strings in EN and HE"
    - "Wave 0 test stubs exist for VisualRecognitionGame, SyllableMatchingGame, and TrailNodeModal"
  artifacts:
    - path: "src/assets/icons/rhythm/quarter-note.svg"
      provides: "Quarter note SVG sprite"
      contains: "viewBox"
    - path: "src/components/games/rhythm-games/utils/durationInfo.js"
      provides: "DURATION_INFO lookup + question generation"
      exports: ["DURATION_INFO", "ALL_DURATION_CODES", "generateQuestions"]
    - path: "src/components/games/rhythm-games/components/DurationCard.jsx"
      provides: "Shared card sub-component"
      min_lines: 50
    - path: "src/data/constants.js"
      provides: "EXERCISE_TYPES with new entries"
      contains: "VISUAL_RECOGNITION"
    - path: "src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx"
      provides: "Wave 0 test stub for Visual Recognition game"
      contains: "describe"
    - path: "src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx"
      provides: "Wave 0 test stub for Syllable Matching game"
      contains: "describe"
    - path: "src/components/trail/__tests__/TrailNodeModal.test.jsx"
      provides: "Wave 0 test stub for TrailNodeModal new exercise type cases"
      contains: "describe"
  key_links:
    - from: "src/components/games/rhythm-games/components/DurationCard.jsx"
      to: "src/assets/icons/rhythm/*.svg"
      via: "SVG React component imports through durationInfo SVG_COMPONENTS map"
      pattern: "svg\\?react"
    - from: "src/components/games/rhythm-games/utils/durationInfo.js"
      to: "src/data/constants.js"
      via: "No import — durationInfo is pure data, constants is pure data. Both consumed by games."
      pattern: "DURATION_INFO"
---

<objective>
Create the foundation layer for Phase 24's multi-angle rhythm games: SVG sprite artwork, the DURATION_INFO lookup table, the shared DurationCard component, new EXERCISE_TYPES constants, question generation logic, all i18n keys, and Wave 0 test stubs for downstream game components.

Purpose: Establish all shared artifacts that both game components (Plan 02) and trail wiring (Plan 03) depend on, plus create test scaffolds that Plan 02/03 tasks will fill with real assertions.

Output: 10 SVG files, durationInfo.js with tests, DurationCard.jsx, updated constants.js, updated locale files, 3 test stub files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-multi-angle-rhythm-games/24-CONTEXT.md
@.planning/phases/24-multi-angle-rhythm-games/24-RESEARCH.md
@.planning/phases/24-multi-angle-rhythm-games/24-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/data/constants.js (EXERCISE_TYPES — add new entries here):

```javascript
export const EXERCISE_TYPES = {
  NOTE_RECOGNITION: "note_recognition",
  SIGHT_READING: "sight_reading",
  RHYTHM: "rhythm",
  MEMORY_GAME: "memory_game",
  BOSS_CHALLENGE: "boss_challenge",
  NOTE_CATCH: "note_catch",
  RHYTHM_TAP: "rhythm_tap",
  RHYTHM_DICTATION: "rhythm_dictation",
  ARCADE_RHYTHM: "arcade_rhythm",
  PITCH_COMPARISON: "pitch_comparison",
  INTERVAL_ID: "interval_id",
  RHYTHM_PULSE: "rhythm_pulse",
};
```

From src/components/games/rhythm-games/components/DictationChoiceCard.jsx (STATE_CLASSES — reuse verbatim):

```javascript
const STATE_CLASSES = {
  default:
    "bg-white/10 backdrop-blur-md border border-white/20 rounded-xl hover:bg-white/20 hover:border-white/40 cursor-pointer transition-colors duration-150",
  correct:
    "bg-green-500/20 backdrop-blur-md border-2 border-green-400 rounded-xl shadow-[0_0_12px_rgba(74,222,128,0.4)] transition-all duration-300",
  wrong:
    "bg-red-500/20 backdrop-blur-md border-2 border-red-400 rounded-xl transition-all duration-300",
  dimmed:
    "opacity-40 pointer-events-none bg-white/10 border border-white/20 rounded-xl",
};
```

From src/components/games/rhythm-games/utils/rhythmVexflowHelpers.js (syllable maps — import for reference, do NOT duplicate):

```javascript
export const SYLLABLE_MAP_EN = { 16: "ta-a-a-a", 12: "ta-a-a", 8: "ta-a", 6: "ta-a", 4: "ta", 2: "ti", 1: "ti-ka" };
export const SYLLABLE_MAP_HE = { 16: "...", 12: "...", ... };
export const REST_SYLLABLE_EN = "sh";
export const REST_SYLLABLE_HE = "הָס";
```

SVG import convention (CLAUDE.md — ?react suffix REQUIRED):

```javascript
import QuarterNoteIcon from "../../../assets/icons/rhythm/quarter-note.svg?react";
```

Existing test patterns (for Wave 0 stubs):

```javascript
// src/components/games/rhythm-games/ArcadeRhythmGame.test.js — sibling test pattern
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 test stubs for downstream components</name>
  <files>
    src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx
    src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx
    src/components/trail/__tests__/TrailNodeModal.test.jsx
  </files>
  <read_first>
    src/components/games/rhythm-games/ArcadeRhythmGame.test.js
    src/components/games/sight-reading-game/__tests__/sightReadingPatterns.test.js
  </read_first>
  <action>
    Create three minimal test stub files that Plan 02 and Plan 03 tasks will fill with real assertions. These are Wave 0 scaffolds per the Nyquist rule — they ensure test files exist before implementation begins.

    **Create `src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx`:**

    ```jsx
    import { describe, it, expect } from 'vitest';

    describe('VisualRecognitionGame', () => {
      it.todo('renders 4 icon cards when game starts');
      it.todo('shows "Which one is a [duration]?" prompt with correct duration name');
      it.todo('marks correct card green and auto-advances after 800ms');
      it.todo('marks wrong card red, highlights correct card, auto-advances after 1200ms');
      it.todo('shows VictoryScreen after 5 questions with correct score');
      it.todo('tracks progress dots (green=correct, red=wrong)');
      it.todo('integrates with trail via location.state (nodeId, exerciseIndex)');
    });
    ```

    **Create `src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx`:**

    ```jsx
    import { describe, it, expect } from 'vitest';

    describe('SyllableMatchingGame', () => {
      it.todo('renders large SVG note prompt and 4 syllable text cards');
      it.todo('shows correct Kodaly syllable for each duration code');
      it.todo('marks correct card green and auto-advances after 800ms');
      it.todo('marks wrong card red, highlights correct card, auto-advances after 1200ms');
      it.todo('shows VictoryScreen after 5 questions with correct score');
      it.todo('uses dedupSyllables option to avoid ambiguous distractors');
      it.todo('displays Hebrew syllables with Nikud when language is HE');
    });
    ```

    **Create `src/components/trail/__tests__/TrailNodeModal.test.jsx`:**

    Create the `__tests__` directory if it does not exist. This stub covers ONLY the new exercise type cases added in Phase 24 — do not test existing TrailNodeModal behavior.

    ```jsx
    import { describe, it, expect } from 'vitest';

    describe('TrailNodeModal — multi-angle exercise types', () => {
      it.todo('displays "Visual Recognition" exercise type name');
      it.todo('displays "Syllable Matching" exercise type name');
      it.todo('navigates to /rhythm-mode/visual-recognition-game for visual_recognition exercises');
      it.todo('navigates to /rhythm-mode/syllable-matching-game for syllable_matching exercises');
    });
    ```

    All three files use `it.todo()` stubs — these will pass vitest (todos are skipped, not failed) and establish the test contract for downstream plans to implement.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx src/components/trail/__tests__/TrailNodeModal.test.jsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Three test stub files exist with todo placeholders. vitest runs them without failure. Plan 02 and Plan 03 will fill in real assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: SVG sprites + DURATION_INFO lookup + question generator + EXERCISE_TYPES + i18n</name>
  <files>
    src/assets/icons/rhythm/quarter-note.svg
    src/assets/icons/rhythm/half-note.svg
    src/assets/icons/rhythm/whole-note.svg
    src/assets/icons/rhythm/eighth-note.svg
    src/assets/icons/rhythm/sixteenth-note.svg
    src/assets/icons/rhythm/dotted-quarter.svg
    src/assets/icons/rhythm/dotted-half.svg
    src/assets/icons/rhythm/quarter-rest.svg
    src/assets/icons/rhythm/half-rest.svg
    src/assets/icons/rhythm/whole-rest.svg
    src/components/games/rhythm-games/utils/durationInfo.js
    src/components/games/rhythm-games/utils/durationInfo.test.js
    src/data/constants.js
    src/locales/en/trail.json
    src/locales/he/trail.json
    src/locales/en/common.json
    src/locales/he/common.json
  </files>
  <read_first>
    src/data/constants.js
    src/components/games/rhythm-games/utils/rhythmVexflowHelpers.js
    src/locales/en/trail.json
    src/locales/he/trail.json
    src/locales/en/common.json
    src/locales/he/common.json
    .planning/phases/24-multi-angle-rhythm-games/24-UI-SPEC.md
    .planning/phases/24-multi-angle-rhythm-games/24-CONTEXT.md
  </read_first>
  <behavior>
    - Test: DURATION_INFO has exactly 10 entries (q, h, w, 8, 16, qd, hd, qr, hr, wr)
    - Test: Each DURATION_INFO entry has { svgFilename: string, i18nKey: string, durationUnits: number, isRest: boolean }
    - Test: durationUnits values are correct: q=4, h=8, w=16, 8=2, 16=1, qd=6, hd=12, qr=4, hr=8, wr=16
    - Test: isRest is true only for qr, hr, wr
    - Test: generateQuestions(["q"], allCodes, 5) returns 5 questions, each with correct="q" and 4 choices total
    - Test: generateQuestions(["q","h"], allCodes, 5) cycles correct answers q,h,q,h,q
    - Test: Every question's choices array contains the correct answer exactly once
    - Test: No distractor equals the correct answer in any question
    - Test: generateQuestions with syllable dedup mode filters distractors with duplicate syllables (e.g., "h" and "qd" both map to "ta-a")
  </behavior>
  <action>
    **Part A: Create 10 SVG sprite files** in `src/assets/icons/rhythm/`. Per D-11/D-12/D-13/D-14 and UI-SPEC SVG sprite visual spec:

    All sprites share: `viewBox="0 0 64 96"`, `fill="none"` on root svg, white (#ffffff) fill/stroke, stroke-width="3" for stems/flags, stroke-linecap="round".

    1. **quarter-note.svg**: Filled ellipse note head (rx=12, ry=9, rotated -20deg) at bottom + vertical stem line from head to top. Head at cy~82, stem from y~76 to y~20.
    2. **half-note.svg**: Open (stroke-only) ellipse note head + stem. Same positioning. Note head: stroke="white" stroke-width="2.5", no fill.
    3. **whole-note.svg**: Open ellipse note head only, no stem. Center vertically at cy~60. Larger rx/ry (rx=14, ry=10). stroke="white" stroke-width="2.5".
    4. **eighth-note.svg**: Filled ellipse head + stem + single flag. Flag: curved path from stem top curving right and down. Per D-14: single note with flag, not beamed pair.
    5. **sixteenth-note.svg**: Filled ellipse head + stem + double flag (two curved paths). Per D-14: single note with double flag.
    6. **dotted-quarter.svg**: Same as quarter-note but with a dot (filled circle r=3) to the right of the note head, at approximately (cx=42, cy=82).
    7. **dotted-half.svg**: Same as half-note but with augmentation dot to the right of the head.
    8. **quarter-rest.svg**: The standard quarter rest zigzag symbol, centered. Draw using a path element. Include a short horizontal reference line (stroke-width="1", opacity="0.3") to give visual context. White fill.
    9. **half-rest.svg**: Filled rectangle sitting ON a horizontal line (rectangle below the line). Rectangle: ~16x8px, centered. Reference line: thin horizontal at y=48. White fill.
    10. **whole-rest.svg**: Filled rectangle hanging BELOW a horizontal line (rectangle above, hanging down from line). Rectangle: ~16x8px, centered. Reference line: thin horizontal at y=44. White fill.

    **Part B: Create `src/components/games/rhythm-games/utils/durationInfo.js`** — per D-30/D-36:

    Export `DURATION_INFO` object mapping VexFlow duration codes to metadata:
    ```javascript
    export const DURATION_INFO = {
      q:  { svgFilename: 'quarter-note',   i18nKey: 'rhythm.duration.quarter',        durationUnits: 4,  isRest: false },
      h:  { svgFilename: 'half-note',      i18nKey: 'rhythm.duration.half',           durationUnits: 8,  isRest: false },
      w:  { svgFilename: 'whole-note',      i18nKey: 'rhythm.duration.whole',          durationUnits: 16, isRest: false },
      '8':  { svgFilename: 'eighth-note',   i18nKey: 'rhythm.duration.eighth',         durationUnits: 2,  isRest: false },
      '16': { svgFilename: 'sixteenth-note', i18nKey: 'rhythm.duration.sixteenth',     durationUnits: 1,  isRest: false },
      qd: { svgFilename: 'dotted-quarter', i18nKey: 'rhythm.duration.dottedQuarter',  durationUnits: 6,  isRest: false },
      hd: { svgFilename: 'dotted-half',    i18nKey: 'rhythm.duration.dottedHalf',     durationUnits: 12, isRest: false },
      qr: { svgFilename: 'quarter-rest',   i18nKey: 'rhythm.duration.quarterRest',    durationUnits: 4,  isRest: true },
      hr: { svgFilename: 'half-rest',      i18nKey: 'rhythm.duration.halfRest',       durationUnits: 8,  isRest: true },
      wr: { svgFilename: 'whole-rest',      i18nKey: 'rhythm.duration.wholeRest',     durationUnits: 16, isRest: true },
    };
    ```

    Export `ALL_DURATION_CODES = Object.keys(DURATION_INFO)` — convenience for distractor pool.

    Export `generateQuestions(durationPool, distractorPool, questionCount, options)`:
    - `durationPool`: array of duration codes from the node's config (e.g., ["q", "h"])
    - `distractorPool`: array of all possible duration codes (default: ALL_DURATION_CODES)
    - `questionCount`: number (default: 5)
    - `options.dedupSyllables`: boolean — when true, filter distractors to avoid syllable collisions with the correct answer. Use SYLLABLE_MAP_EN durationUnits lookup (since durationInfo has durationUnits, compare durationUnits to get the EN syllable — actually simpler: build a quick syllable lookup inline from DURATION_INFO.durationUnits mapped through a small map { 16: "ta-a-a-a", 12: "ta-a-a", 8: "ta-a", 6: "ta-a", 4: "ta", 2: "ti", 1: "ti-ka" } plus isRest -> "sh"). Do NOT import from rhythmVexflowHelpers.js (that file imports VexFlow which is browser-only; durationInfo.js must be Node.js-safe for the validator).
    - Returns array of `{ correct: string, choices: string[] }` where choices is shuffled array of 4 items (1 correct + 3 distractors).
    - Cycling: `correct = durationPool[i % durationPool.length]`
    - Distractor selection: pick 3 from distractorPool that are not equal to correct. If `dedupSyllables`, also exclude durations with the same syllable text as correct. If not enough distractors after filtering, relax dedup constraint to fill.
    - Shuffle: Fisher-Yates on the 4-item choices array.

    CRITICAL: This file must be pure JS with NO imports from vexflow, no SVG imports, no React — it runs in Node.js via validateTrail.mjs. Per research Pitfall 3.

    **Part C: Create test file** `src/components/games/rhythm-games/utils/durationInfo.test.js`:
    Test all behaviors listed in `<behavior>` block above. Import { DURATION_INFO, ALL_DURATION_CODES, generateQuestions } from './durationInfo.js'.

    **Part D: Add to `src/data/constants.js`** — per D-22:
    Add two entries to the EXERCISE_TYPES object after the `RHYTHM_PULSE` line:
    ```javascript
    // v3.2 multi-angle games
    VISUAL_RECOGNITION: 'visual_recognition',
    SYLLABLE_MATCHING: 'syllable_matching',
    ```

    **Part E: Update locale files** — per D-10, UI-SPEC copywriting contract:

    In `src/locales/en/trail.json` under `exerciseTypes`, add:
    ```json
    "visual_recognition": "Visual Recognition",
    "syllable_matching": "Syllable Matching"
    ```

    In `src/locales/he/trail.json` under `exerciseTypes`, add:
    ```json
    "visual_recognition": "זיהוי חזותי",
    "syllable_matching": "התאמת הברות"
    ```

    In `src/locales/en/common.json`, add a new top-level key `"visualRecognition"` with:
    ```json
    "visualRecognition": {
      "prompt": "Which one is a {{durationName}}?",
      "title": "Visual Recognition"
    },
    "syllableMatching": {
      "prompt": "What syllable is this?",
      "title": "Syllable Matching"
    },
    "rhythm": {
      ...existing rhythm keys if any...,
      "duration": {
        "quarter": "quarter note",
        "half": "half note",
        "whole": "whole note",
        "eighth": "eighth note",
        "sixteenth": "sixteenth note",
        "dottedQuarter": "dotted quarter note",
        "dottedHalf": "dotted half note",
        "quarterRest": "quarter rest",
        "halfRest": "half rest",
        "wholeRest": "whole rest"
      }
    }
    ```
    NOTE: Check if a "rhythm" key already exists in common.json. If so, nest `duration` inside it. If not, create it. Also check for existing keys before adding to avoid overwriting.

    In `src/locales/he/common.json`, add matching Hebrew keys:
    ```json
    "visualRecognition": {
      "prompt": "?{{durationName}} איזה מהם הוא",
      "title": "זיהוי חזותי"
    },
    "syllableMatching": {
      "prompt": "?איזו הברה זו",
      "title": "התאמת הברות"
    },
    "rhythm": {
      "duration": {
        "quarter": "תו רבע",
        "half": "תו חצי",
        "whole": "תו שלם",
        "eighth": "תו שמינית",
        "sixteenth": "תו שש עשרה",
        "dottedQuarter": "תו רבע מנוקד",
        "dottedHalf": "תו חצי מנוקד",
        "quarterRest": "שתקת רבע",
        "halfRest": "שתקת חצי",
        "wholeRest": "שתקת שלם"
      }
    }
    ```

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/utils/durationInfo.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 10 SVG files exist in src/assets/icons/rhythm/ — each contains `viewBox="0 0 64 96"` and `fill` or `stroke` attributes with value "white" or "#ffffff"
    - src/components/games/rhythm-games/utils/durationInfo.js exports DURATION_INFO, ALL_DURATION_CODES, generateQuestions
    - DURATION_INFO has exactly 10 keys: q, h, w, 8, 16, qd, hd, qr, hr, wr
    - Each DURATION_INFO entry has all 4 fields: svgFilename, i18nKey, durationUnits, isRest
    - durationInfo.js contains NO import statements referencing vexflow, .svg, or React
    - src/data/constants.js contains `VISUAL_RECOGNITION: 'visual_recognition'` and `SYLLABLE_MATCHING: 'syllable_matching'`
    - src/locales/en/trail.json contains `"visual_recognition": "Visual Recognition"`
    - src/locales/he/trail.json contains `"visual_recognition": "זיהוי חזותי"`
    - src/locales/en/common.json contains `"visualRecognition"` and `"syllableMatching"` top-level keys
    - src/locales/en/common.json contains `"rhythm"` key with nested `"duration"` containing all 10 duration name keys
    - durationInfo.test.js passes with 0 failures
    - `npm run verify:trail` still passes (constants.js change doesn't break existing validator)
  </acceptance_criteria>
  <done>All 10 SVG sprites created, DURATION_INFO lookup complete with tests passing, EXERCISE_TYPES extended, and all i18n keys added for EN and HE.</done>
</task>

<task type="auto">
  <name>Task 2: DurationCard shared component</name>
  <files>
    src/components/games/rhythm-games/components/DurationCard.jsx
  </files>
  <read_first>
    src/components/games/rhythm-games/components/DictationChoiceCard.jsx
    src/components/games/rhythm-games/utils/durationInfo.js
    src/utils/useMotionTokens.js
    .planning/phases/24-multi-angle-rhythm-games/24-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/games/rhythm-games/components/DurationCard.jsx` — per D-35, D-18, UI-SPEC interaction contract.

    This component renders either an SVG note icon or text (Kodaly syllable) inside a glass-styled card. It handles tap interaction, 4 visual states, and reduced-motion support.

    **Props:**
    - `type`: "icon" | "text" — determines render mode
    - `durationCode`: string (VexFlow duration code, e.g., "q") — used for icon mode to pick the SVG component and for aria-label
    - `text`: string — displayed text for type="text" (syllable like "ta")
    - `state`: "default" | "correct" | "wrong" | "dimmed" — visual state
    - `onSelect`: (cardIndex: number) => void — called on tap
    - `disabled`: boolean — prevents interaction during feedback
    - `cardIndex`: number — passed to onSelect
    - `ariaLabel`: string — accessible label for the card

    **Implementation:**

    1. **SVG Component Map**: Create a constant `SVG_COMPONENTS` object at module level that maps duration codes to their imported SVG React components:
       ```javascript
       import QuarterNoteIcon from '../../../../assets/icons/rhythm/quarter-note.svg?react';
       import HalfNoteIcon from '../../../../assets/icons/rhythm/half-note.svg?react';
       // ... all 10 imports with ?react suffix

       const SVG_COMPONENTS = {
         q: QuarterNoteIcon,
         h: HalfNoteIcon,
         w: WholeNoteIcon,
         '8': EighthNoteIcon,
         '16': SixteenthNoteIcon,
         qd: DottedQuarterIcon,
         hd: DottedHalfIcon,
         qr: QuarterRestIcon,
         hr: HalfRestIcon,
         wr: WholeRestIcon,
       };
       ```

    2. **STATE_CLASSES**: Copy verbatim from DictationChoiceCard.jsx:
       ```javascript
       const STATE_CLASSES = {
         default: "bg-white/10 backdrop-blur-md border border-white/20 rounded-xl hover:bg-white/20 hover:border-white/40 cursor-pointer transition-colors duration-150",
         correct: "bg-green-500/20 backdrop-blur-md border-2 border-green-400 rounded-xl shadow-[0_0_12px_rgba(74,222,128,0.4)] transition-all duration-300",
         wrong: "bg-red-500/20 backdrop-blur-md border-2 border-red-400 rounded-xl transition-all duration-300",
         dimmed: "opacity-40 pointer-events-none bg-white/10 border border-white/20 rounded-xl",
       };
       ```

    3. **Motion tokens**: Use `useMotionTokens()` hook. When reduced-motion is active, add `transition-none` class to the card and skip `active:scale-95` transform. Apply scale transform via `active:scale-[0.95]` Tailwind class (only when motion is allowed).

    4. **Render logic**:
       - For `type="icon"`: Render the SVG component from SVG_COMPONENTS[durationCode] at `w-16 h-24` (64x96 aspect), centered. Wrap in `dir="ltr"` container (music notation always LTR per accessibility contract).
       - For `type="text"`: Render `text` prop in `text-3xl font-bold text-white` (28px display size per UI-SPEC typography). Center text in card.
       - D-17: No text labels on icon cards. No duration name on text cards.

    5. **Accessibility** — per UI-SPEC accessibility contract:
       - `role="button"`, `tabIndex={disabled || state === "dimmed" ? -1 : 0}`
       - `aria-label`: For icon type, use the durationCode to provide descriptive label (will be set by parent via a prop `ariaLabel`). Add `ariaLabel` prop.
       - `aria-pressed={state === "correct" || state === "wrong"}` for selection state
       - Keyboard: onKeyDown handler for Enter/Space (same as DictationChoiceCard pattern)
       - `aria-disabled={disabled}`

    6. **Click handler**: Same pattern as DictationChoiceCard — call `onSelect(cardIndex)` if not disabled and not dimmed.

    7. **Card sizing**: Use `min-h-[80px] min-w-[80px]` for minimum per D-16. Use `flex items-center justify-center p-4` for centering content.

    Export SVG_COMPONENTS as a named export (for SyllableMatchingGame prompt rendering) and DurationCard as both named and default export.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/games/rhythm-games/components/DurationCard.jsx
    - File contains `import QuarterNoteIcon from '../../../../assets/icons/rhythm/quarter-note.svg?react'` (with ?react suffix)
    - File contains all 10 SVG imports (quarter-note, half-note, whole-note, eighth-note, sixteenth-note, dotted-quarter, dotted-half, quarter-rest, half-rest, whole-rest)
    - File contains STATE_CLASSES with all 4 states matching DictationChoiceCard exactly
    - File contains `useMotionTokens` import and usage
    - File contains `role="button"` and `aria-pressed` and `tabIndex` attributes
    - File contains onKeyDown handler checking for Enter and Space keys
    - File exports DurationCard as default export
    - File exports SVG_COMPONENTS as named export
    - `npm run lint` passes on the file
  </acceptance_criteria>
  <done>DurationCard component renders SVG icons and text with glass styling, 4 visual states, tap animation with reduced-motion support, and full accessibility attributes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                 |
| -------- | ----------------------------------------------------------------------------------------------------------- |
| None new | This plan creates pure client-side display components with no network calls, no user input beyond tap index |

## STRIDE Threat Register

| Threat ID | Category                   | Component             | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | -------------------------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-24-01   | I (Information Disclosure) | SVG sprites           | accept      | SVGs contain no sensitive data — just geometric shapes. Public assets.                                                                                                         |
| T-24-02   | T (Tampering)              | generateQuestions     | accept      | Pure function for quiz question generation. No data persistence. Incorrect tampering would only affect local game display.                                                     |
| T-24-03   | S (Spoofing)               | DurationCard onSelect | accept      | cardIndex is a 0-3 integer used locally for UI state. No authentication or authorization decisions based on it. Bounds-checked by the parent component's choices array length. |

</threat_model>

<verification>
1. `npx vitest run src/components/games/rhythm-games/__tests__/ src/components/trail/__tests__/TrailNodeModal.test.jsx` — Wave 0 stubs run (all todo, no failures)
2. `npx vitest run src/components/games/rhythm-games/utils/durationInfo.test.js` — all tests pass
3. `npm run verify:trail` — existing trail validation still passes
4. `npm run lint` — no lint errors in new/modified files
5. All 10 SVG files exist and contain valid SVG markup with `viewBox="0 0 64 96"`
</verification>

<success_criteria>

- Wave 0 test stubs created for VisualRecognitionGame, SyllableMatchingGame, and TrailNodeModal
- 10 SVG sprites created with correct viewBox and white fill/stroke
- DURATION_INFO lookup has all 10 entries with correct metadata
- generateQuestions produces correct quiz question sets (verified by tests)
- DurationCard renders both icon and text modes with glass styling and accessibility
- EXERCISE_TYPES extended with VISUAL_RECOGNITION and SYLLABLE_MATCHING
- All i18n keys added for EN and HE
- No lint errors, no test failures, trail validator still passes
  </success_criteria>

<output>
After completion, create `.planning/phases/24-multi-angle-rhythm-games/24-01-SUMMARY.md`
</output>
