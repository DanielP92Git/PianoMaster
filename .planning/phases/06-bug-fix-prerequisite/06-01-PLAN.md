---
phase: 06-bug-fix-prerequisite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "SightReadingGame.micRestart.test.jsx passes when run via `npx vitest run`"
    - "Clicking 'Try Again' after a failed mic performance calls startListening() on the second attempt"
    - "The phase-enforcement effect reads micIsListeningRef.current (sync ref) instead of isListening (async state)"
    - "Rapid start/stop sequences cannot leave the mic in an inconsistent state due to stale React state"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx"
      provides: "Working test with MemoryRouter wrapper and useLocation mock"
      contains: "MemoryRouter"
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "Fixed mic lifecycle with sync ref guard and reset flag in replayPattern"
      contains: "micEarlyWindowStartRequestedRef.current = false"
  key_links:
    - from: "SightReadingGame.micRestart.test.jsx"
      to: "SightReadingGame.jsx"
      via: "render with MemoryRouter wrapper"
      pattern: "MemoryRouter.*SightReadingGame"
    - from: "replayPattern()"
      to: "micEarlyWindowStartRequestedRef"
      via: "ref reset to false"
      pattern: "micEarlyWindowStartRequestedRef\\.current = false"
    - from: "phase-enforcement useEffect"
      to: "micIsListeningRef.current"
      via: "sync ref read replacing async isListening"
      pattern: "micIsListeningRef\\.current"
---

<objective>
Fix the two core mic lifecycle bugs (test crash + runtime ref reset) and replace async state reads with synchronous ref reads in the mic guard effect.

Purpose: The existing test crashes before reaching any mic logic (missing Router context), the runtime bug prevents mic reactivation on second "Try Again" attempt (stale ref flag), and the phase-enforcement effect reads stale async state. All three must be fixed before subsequent v1.7 phases can rely on the test suite as a baseline.

Output: Passing test, working mic restart on second attempt, race-condition-free mic guard.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bug-fix-prerequisite/06-RESEARCH.md
@src/components/games/sight-reading-game/SightReadingGame.jsx
@src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test infrastructure and runtime mic-restart bug</name>
  <files>
    src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx
    src/components/games/sight-reading-game/SightReadingGame.jsx
  </files>
  <action>
**Test fix (SightReadingGame.micRestart.test.jsx):**

1. Add `MemoryRouter` import from `react-router-dom` at the top of the test file.

2. Extend the existing `vi.mock("react-router-dom", ...)` to also mock `useLocation`:
   ```javascript
   useLocation: () => ({ state: null, pathname: "/" }),
   ```
   This is needed because the component reads `location.state?.nodeId` etc. and MemoryRouter alone with `initialEntries` might still trigger trail auto-start logic.

3. Wrap ALL `render(<SightReadingGame />)` calls with `MemoryRouter`:
   ```jsx
   render(
     <MemoryRouter initialEntries={[{ pathname: "/", state: null }]}>
       <SightReadingGame />
     </MemoryRouter>
   );
   ```

4. Verify the test now renders without the `"useLocation() may be used only in the context of a <Router> component"` crash.

**Runtime fix (SightReadingGame.jsx):**

5. In the `replayPattern()` callback (around line ~2218), add the missing ref reset BEFORE the `setGamePhase(GAME_PHASES.DISPLAY)` call:
   ```javascript
   // Reset mic warm-up flag so the next performance can start the mic.
   // Without this, micEarlyWindowStartRequestedRef stays true from the previous
   // run and the early-window guard skips startListening() on the next attempt.
   micEarlyWindowStartRequestedRef.current = false;
   pendingMicLatencyMsRef.current = null;
   ```
   Place this after the existing resets (after `setExerciseRecorded(false)` or similar) and before `setGamePhase(GAME_PHASES.DISPLAY)`.

6. Add a code comment noting that both `replayPattern()` and `beginPerformanceWithPattern()` reset the flag. The `beginPerformanceWithPattern()` reset is authoritative (runs just before performance); the `replayPattern()` reset is an early reset to prevent stale flag edge cases.

**Verification:** Run `npx vitest run src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx` and confirm all tests pass.
  </action>
  <verify>npx vitest run src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx</verify>
  <done>SightReadingGame.micRestart.test.jsx passes (all tests green). The test renders without Router crash. The replayPattern() function resets micEarlyWindowStartRequestedRef.current to false so the second "Try Again" correctly triggers startListening().</done>
</task>

<task type="auto">
  <name>Task 2: Replace async isListening reads with synchronous ref reads in phase-enforcement effect</name>
  <files>
    src/components/games/sight-reading-game/SightReadingGame.jsx
  </files>
  <action>
**Create sync wrapper functions:**

1. Locate the existing `micIsListeningRef` declaration (around line 843). It is currently synced via a `useEffect` that reads `isListening` state. This async sync is the root of the race condition.

2. Create two wrapper functions that update the ref synchronously at call time:
   ```javascript
   const startListeningSync = useCallback(async () => {
     micIsListeningRef.current = true;
     try {
       await startListening();
     } catch (err) {
       micIsListeningRef.current = false;
       throw err;
     }
   }, [startListening]);

   const stopListeningSync = useCallback(() => {
     micIsListeningRef.current = false;
     stopListening();
   }, [stopListening]);
   ```

3. Remove or comment out the `useEffect` that was syncing `micIsListeningRef` from `isListening` state (the pattern `useEffect(() => { micIsListeningRef.current = Boolean(isListening); }, [isListening])`) since the ref is now maintained synchronously.

**Update phase-enforcement effect:**

4. In the phase-enforcement `useEffect` (around lines 2895-2945), replace all reads of `isListening` with `micIsListeningRef.current`:
   - Line ~2900: `if (isListening)` -> `if (micIsListeningRef.current)`
   - Line ~2923: `&& isListening` -> `&& micIsListeningRef.current`
   - Line ~2937: `if (isListening)` -> `if (micIsListeningRef.current)`

5. Replace all calls to `stopListening()` in the phase-enforcement effect with `stopListeningSync()`.

6. Remove `isListening` from the effect's dependency array. The effect should depend on `[gamePhase, inputMode, stopListeningSync]` (plus any other existing non-isListening deps). Add a comment explaining the ref read is intentional and not reactive.

**Update all other call sites:**

7. Search the entire file for direct calls to `startListening()` and `stopListening()` and replace them with `startListeningSync()` and `stopListeningSync()` respectively. This ensures the ref stays in sync everywhere, not just in the phase-enforcement effect. Common locations:
   - Early window mic warmup logic
   - Performance start logic
   - Error handling / catch blocks
   - Cleanup effects

   **Important:** Do NOT replace the `startListening`/`stopListening` references in the destructuring from `useMicNoteInput` or `usePitchDetection` — those are the original functions used inside the wrappers. Only replace downstream call sites that currently call the originals directly.

8. Verify no lint warnings about exhaustive-deps for the ref read. If the linter flags `micIsListeningRef.current`, add an eslint-disable-next-line comment for the specific rule.

**Verification:** Run the full test suite and confirm no regressions.
  </action>
  <verify>npx vitest run src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx && npm run test:run</verify>
  <done>The phase-enforcement effect reads micIsListeningRef.current (sync) instead of isListening (async state). The sync wrapper functions (startListeningSync, stopListeningSync) update the ref at call time, eliminating the race window. All call sites in SightReadingGame.jsx use the sync wrappers. Tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/games/sight-reading-game/SightReadingGame.micRestart.test.jsx` — all tests pass
2. `npm run test:run` — no regressions in other test files
3. Grep for `isListening` in the phase-enforcement effect — should NOT appear as a read (only `micIsListeningRef.current`)
4. Grep for `micEarlyWindowStartRequestedRef.current = false` — should appear in both `replayPattern()` AND `beginPerformanceWithPattern()`
5. No direct `startListening()` or `stopListening()` calls remain outside the wrapper definitions
</verification>

<success_criteria>
- SightReadingGame.micRestart.test.jsx passes with zero failures
- micEarlyWindowStartRequestedRef is reset in replayPattern(), preventing stale flag on second "Try Again"
- Phase-enforcement effect uses synchronous ref reads, not async React state
- All startListening/stopListening call sites use the sync wrappers
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fix-prerequisite/06-01-SUMMARY.md`
</output>
