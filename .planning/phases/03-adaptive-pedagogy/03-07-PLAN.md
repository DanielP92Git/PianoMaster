---
phase: 03-adaptive-pedagogy
plan: 07
type: execute
wave: 2
depends_on: [02]
files_modified: []
autonomous: false
requirements: [ADAPT-03, ADAPT-04]

must_haves:
  truths:
    - "The note_mastery JSONB column physically exists on the live student_skill_progress table with default '{}' and a GIN index."
    - "The apply is owner-gated (production database holds children's data) and idempotent (IF NOT EXISTS), so re-running is safe."
    - "No new RLS policy is created; the column is confirmed to inherit the existing row-level policies (ADAPT-04, to be re-confirmed by /gsd-secure-phase)."
  artifacts:
    - path: "supabase/migrations/20260712120000_add_note_mastery.sql"
      provides: "the DDL applied in this plan (authored in Plan 02)"
      contains: "note_mastery"
  key_links:
    - from: "20260712120000_add_note_mastery.sql"
      to: "live student_skill_progress table"
      via: "mcp__supabase__apply_migration (project ref hdltcvgqrtxuxgjdvzzu)"
      pattern: "note_mastery"
---

<objective>
Apply the `note_mastery` migration (authored in Plan 02) to the live Supabase project so the column
physically exists before phase verification. This is the mandatory schema push: build and type checks
pass WITHOUT it (this app derives no types from the live DB), which would create a false-positive
verification state where mastery persistence silently writes to a non-existent column in production.

Purpose: Make the ADAPT-03 persistence path real end-to-end and give `/gsd-secure-phase` a live column
to verify RLS against (ADAPT-04).
Output: `note_mastery` column live on `student_skill_progress`, owner-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-adaptive-pedagogy/03-CONTEXT.md
@.planning/phases/03-adaptive-pedagogy/03-RESEARCH.md
@.planning/phases/03-adaptive-pedagogy/03-02-SUMMARY.md

<notes>
- Supabase project ref: `hdltcvgqrtxuxgjdvzzu` (from project memory / CONTEXT canonical refs).
- Prior phases applied migrations via the Supabase MCP tool (`mcp__supabase__apply_migration`); the
  v3.5 rhythm-pedagogy migration was owner-gated (committed to repo, applied separately). This plan
  follows that same owner-gate.
- The Supabase CLI is not on PATH in the planning environment (RESEARCH.md Environment Availability);
  MCP `apply_migration` is the primary method, `supabase db push` (with `SUPABASE_ACCESS_TOKEN`) the
  fallback if run from a machine that has the CLI.
</notes>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner approval to apply note_mastery to production</name>
  <decision>Apply the `note_mastery` column migration to the live production Supabase project now?</decision>
  <context>
This ALTERs `student_skill_progress` (a table holding children's progress data) on production. The
DDL is additive and idempotent (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`), adds no
RLS policy, and cannot drop or rewrite existing data. It must be live before phase verification and
before `/gsd-secure-phase`. Per the established owner-gate convention for this project's production
database, applying requires explicit approval.
  </context>
  <options>
    <option id="approve-apply">
      <name>Approve — apply now via MCP</name>
      <pros>Unblocks end-to-end mastery persistence and the secure-phase RLS check immediately.</pros>
      <cons>Touches production (mitigated: additive + idempotent).</cons>
    </option>
    <option id="defer-owner-runs">
      <name>Defer — owner will run `supabase db push` themselves</name>
      <pros>Owner retains full manual control of the production apply.</pros>
      <cons>Phase verification blocks until the owner completes it out-of-band.</cons>
    </option>
  </options>
  <resume-signal>Select: approve-apply or defer-owner-runs</resume-signal>
  <action>
Pause and present the decision above to the owner. Do NOT apply anything in this task — it is a gate.
On `approve-apply`, proceed to Task 2 (Claude applies via MCP). On `defer-owner-runs`, skip Task 2's
apply and record the owner-pending blocker in the SUMMARY.
  </action>
  <verify>Owner has explicitly selected approve-apply or defer-owner-runs.</verify>
  <done>A decision (approve-apply | defer-owner-runs) is recorded; no DDL applied in this task.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration and verify the column is live</name>
  <read_first>
    - supabase/migrations/20260712120000_add_note_mastery.sql (the exact DDL to apply)
    - .planning/phases/03-adaptive-pedagogy/03-RESEARCH.md (Environment Availability — MCP primary, CLI fallback)
  </read_first>
  <action>
Only after Task 1 resolves `approve-apply`. Apply the migration using the Supabase MCP tool:
`mcp__supabase__apply_migration` with project ref `hdltcvgqrtxuxgjdvzzu`, name `add_note_mastery`,
and the SQL body from `supabase/migrations/20260712120000_add_note_mastery.sql`.

Fallback (only if MCP is unavailable and running on a machine with the Supabase CLI): set
`SUPABASE_ACCESS_TOKEN` and run `supabase db push`. If neither is available, STOP and report — do not
mark the phase verifiable.

Then confirm the column is live: use `mcp__supabase__list_tables` (or `mcp__supabase__execute_sql`
with a schema query such as
`select column_name, data_type from information_schema.columns where table_name = 'student_skill_progress' and column_name = 'note_mastery'`)
and confirm exactly one `note_mastery` / `jsonb` row is returned. Also confirm no new policy was
created (the migration authored none).

If Task 1 resolved `defer-owner-runs`: do not apply; record in the SUMMARY that the apply is
owner-pending and the phase cannot pass verification until the owner confirms the column is live.
</action>
<verify>
<automated>MISSING — live-DB verification is via MCP (Supabase MCP execute_sql / list_tables tools); confirm information_schema shows student_skill_progress.note_mastery as jsonb</automated>
</verify>
<acceptance_criteria> - `information_schema.columns` shows `student_skill_progress.note_mastery` with data_type `jsonb` (via MCP execute_sql) - `mcp__supabase__list_tables` shows the `note_mastery` column on `student_skill_progress` - No new RLS policy exists on `student_skill_progress` beyond the pre-existing set (mastery inherits row-level policies) - SUMMARY records the apply method used (MCP vs CLI) and the verification output
</acceptance_criteria>
<done>note_mastery column is live on production student_skill_progress (jsonb, default '{}', GIN index), inheriting existing RLS, verified via MCP.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                |
| ------------------------------------------ | ------------------------------------------------------------------------------------------ |
| operator/agent → production Postgres (DDL) | A schema-altering migration is applied to the production database holding children's data. |

## STRIDE Threat Register

| Threat ID | Category                       | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                                           |
| --------- | ------------------------------ | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-14   | Tampering / Destructive change | production DDL apply        | mitigate    | DDL is strictly additive and idempotent (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`); cannot drop/alter existing columns or data. Owner-gated behind an explicit approval checkpoint before apply.          |
| T-03-15   | Elevation of Privilege         | new column access scope     | mitigate    | No new policy added; the column inherits `student_skill_progress` row-level RLS (`student_id = auth.uid()` for write; existing SELECT policy for read). `/gsd-secure-phase` verifies inheritance against the live column. |
| T-03-16   | Repudiation                    | untracked production change | mitigate    | Applied from a committed, timestamped migration file (`20260712120000_add_note_mastery.sql`); apply method + verification output recorded in the plan SUMMARY.                                                            |

</threat_model>

<verification>
- Live `information_schema` confirms `note_mastery` (jsonb) on `student_skill_progress`.
- No new RLS policy present; existing row-level policies cover the column.
- Deferred path (owner-runs) is explicitly recorded as blocking phase verification until confirmed.
</verification>

<success_criteria>

- `note_mastery` column exists on the live table (or owner-pending is explicitly recorded as a blocker).
- The apply was owner-approved and idempotent; no new policy created.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adaptive-pedagogy/03-07-SUMMARY.md`.
</output>
