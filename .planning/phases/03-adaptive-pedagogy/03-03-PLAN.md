---
phase: 03-adaptive-pedagogy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/en/common.json
  - src/locales/he/common.json
  - src/components/games/sight-reading-game/components/LevelUpCue.jsx
  - src/components/games/sight-reading-game/components/LevelUpCue.test.jsx
autonomous: true
requirements: [ADAPT-01, I18N-01]

must_haves:
  truths:
    - "A positive-only 'leveling up' cue component renders a brief celebratory overlay when shown and is suppressed to a static/opacity-only render under prefers-reduced-motion."
    - "The cue's copy is driven entirely by i18n keys under sightReading.adaptive.* present in BOTH en and he with exact key parity (RTL inherited from the app's dir handling)."
    - "There is NO 'easing' / 'you're doing worse' string anywhere — easing is silent by design (D-12)."
  artifacts:
    - path: "src/components/games/sight-reading-game/components/LevelUpCue.jsx"
      provides: "escalation cue overlay (framer-motion, reduced-motion aware)"
      contains: "useMotionTokens"
    - path: "src/locales/en/common.json"
      provides: "sightReading.adaptive.* EN strings"
      contains: '"adaptive"'
    - path: "src/locales/he/common.json"
      provides: "sightReading.adaptive.* HE strings (parity)"
      contains: '"adaptive"'
  key_links:
    - from: "LevelUpCue.jsx"
      to: "sightReading.adaptive.levelUp i18n key"
      via: "useTranslation t()"
      pattern: "sightReading\\.adaptive"
---

<objective>
Build the visible, positive-only escalation cue (D-12) and its EN+HE strings. When difficulty/tempo
steps up, a brief "leveling up" overlay celebrates it (same spirit as on-fire); easing down is silent
(no component, no copy). The component is presentation-only and driven by a parent-controlled `show`
prop — Plan 05 wires the trigger.

Purpose: Deliver the child-facing motivational moment and its localized copy independently of the
heavy `SightReadingGame.jsx` wiring, so it can be built and reviewed in isolation.
Output: One new cue component + render test, and parity-gated EN/HE `sightReading.adaptive.*` keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-adaptive-pedagogy/03-CONTEXT.md
@.planning/phases/03-adaptive-pedagogy/03-PATTERNS.md

<interfaces>
<!-- Structural analog: OnFireSplash.jsx (verified). Follow this shape for LevelUpCue but do NOT
     literally reuse OnFireSplash/OnFireBadge — this is a distinct concept (CONTEXT.md note). -->
```javascript
import { AnimatePresence, motion } from "framer-motion";
import { useMotionTokens } from "../../../../utils/useMotionTokens";

export function OnFireSplash({ show }) {
const { reduce } = useMotionTokens();
return (
<AnimatePresence>
{show && (
<motion.div
key="fire-splash"
initial={reduce ? { opacity: 1 } : { opacity: 0, scale: 0.5 }}
animate={reduce ? { opacity: 1 } : { opacity: 1, scale: [1, 1.15, 1] }}
exit={{ opacity: 0, scale: 0.8 }}
transition={{ duration: 0.6, ease: "easeOut" }}
className="pointer-events-none fixed inset-0 z-[70] flex items-center justify-center" >
{/_ icon _/}
</motion.div>
)}
</AnimatePresence>
);
}

````

<!-- Canonical sightReading namespace is the SECOND occurrence in en/common.json (line ~1629);
     the LAST duplicate key wins on JSON.parse, so the parity test reads that one. Add the new
     subtree there, NOT at the line ~710 occurrence. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sightReading.adaptive.* strings (EN + HE, exact parity)</name>
  <files>src/locales/en/common.json, src/locales/he/common.json</files>
  <read_first>
    - src/locales/en/common.json (the sightReading namespace at line ~1629 — the canonical/last occurrence; confirm which is canonical before editing)
    - src/locales/he/common.json (mirror location of the sightReading namespace)
    - src/locales/__tests__/sight-reading-parity.test.js (the gate that will enforce EN↔HE parity on the new keys)
  </read_first>
  <action>
Inside the CANONICAL `sightReading` object in `en/common.json` (the second/last occurrence, ~line
1629 — verify it is the one the parity test resolves), add a new nested `adaptive` subtree:
```json
"adaptive": {
  "levelUp": "Leveling Up!",
  "levelUpSubtitle": "Nice streak — here comes a bigger challenge"
}
````

Add the byte-for-byte key-parallel subtree to the canonical `sightReading` object in
`he/common.json` with Hebrew copy (present-tense, child-friendly; owner may refine wording later):

```json
"adaptive": {
  "levelUp": "עולים רמה!",
  "levelUpSubtitle": "רצף יפה — מגיע אתגר גדול יותר"
}
```

Add NO easing/negative strings (D-12 — easing is silent). Keep exactly these two keys in both files
so the parity gate stays green. Do not touch the line ~710 duplicate occurrence.
</action>
<verify>
<automated>npx vitest run sight-reading-parity.test.js</automated>
</verify>
<acceptance_criteria> - `en/common.json` sightReading.adaptive contains keys `levelUp` and `levelUpSubtitle` - `he/common.json` sightReading.adaptive contains the same two keys with non-empty Hebrew values - No key named with "ease"/"down"/"worse" exists under sightReading.adaptive (grep returns 0) - `npx vitest run src/locales/__tests__/sight-reading-parity.test.js` exits 0 (both parity directions green) - Both JSON files still parse (no trailing-comma/bracket errors) — `node -e "require('./src/locales/en/common.json'); require('./src/locales/he/common.json')"` exits 0
</acceptance_criteria>
<done>sightReading.adaptive.\* present in EN+HE with exact parity; parity gate green; no easing copy.</done>
</task>

<task type="auto">
  <name>Task 2: Build LevelUpCue.jsx (reduced-motion aware) + render test</name>
  <files>src/components/games/sight-reading-game/components/LevelUpCue.jsx, src/components/games/sight-reading-game/components/LevelUpCue.test.jsx</files>
  <read_first>
    - src/components/games/shared/hud/OnFireSplash.jsx (exact structural analog — AnimatePresence, reduce-branched motion, fixed inset-0 z-[70], pointer-events-none)
    - src/utils/useMotionTokens.js (the reduce token source)
    - src/components/games/sight-reading-game/components/FeedbackSummary.test.jsx (sibling render-test style with react-i18next in this folder)
  </read_first>
  <action>
Create `LevelUpCue.jsx` exporting `function LevelUpCue({ show })`. Mirror `OnFireSplash` structure:
`AnimatePresence` wrapping a `motion.div` gated on `show`, `initial`/`animate` branched on
`reduce` from `useMotionTokens()` (reduced-motion → opacity-only, no scale/keyframe animation),
`className="pointer-events-none fixed inset-0 z-[70] flex flex-col items-center justify-center"`.

Use a lucide-react icon already available in the project (e.g. `import { ArrowUp } from "lucide-react"`
or `Sparkles`) rather than a new PNG asset. Render the icon plus two lines of copy via
`useTranslation`: `t("sightReading.adaptive.levelUp")` (bold/primary) and
`t("sightReading.adaptive.levelUpSubtitle")` (secondary). Do NOT hardcode English text. Use the
glass/`text-white` styling conventions from CLAUDE.md. There is NO easing variant — this component
only ever renders on escalation.

Create `LevelUpCue.test.jsx` (JSDOM, @testing-library/react + i18n test wrapper like the sibling
FeedbackSummary.test.jsx): (1) renders the localized levelUp text when `show` is true; (2) renders
nothing when `show` is false; (3) under a mocked reduced-motion (`useMotionTokens` returns
`{ reduce: true }`), the motion.div uses the opacity-only branch (assert no `scale` keyframe array
in the animate prop, or assert the reduced variant renders).
</action>
<verify>
<automated>npx vitest run src/components/games/sight-reading-game/components/LevelUpCue.test.jsx</automated>
</verify>
<acceptance_criteria> - `LevelUpCue.jsx` contains `useMotionTokens` and `fixed inset-0 z-[70]` - `LevelUpCue.jsx` references `sightReading.adaptive.levelUp` via `t(` (grep matches `sightReading.adaptive`) - `LevelUpCue.jsx` contains no hardcoded "Leveling Up" literal (copy comes from i18n) - `LevelUpCue.test.jsx` asserts the reduced-motion branch AND the show=false empty render - `npx vitest run src/components/games/sight-reading-game/components/LevelUpCue.test.jsx` exits 0
</acceptance_criteria>
<done>LevelUpCue renders localized, reduced-motion-aware escalation cue; render test green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                |
| ---------- | -------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Presentational component + static translation resources. No user input, no persistence, no network. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                                                                 |
| --------- | ---------------------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-03-07   | Information Disclosure | escalation cue copy | accept      | Cue shows only a generic "leveling up" message; it exposes no per-child performance data, scores, or PII. No mitigation needed. |

</threat_model>

<verification>
- `npx vitest run src/locales/__tests__/sight-reading-parity.test.js` green.
- `npx vitest run src/components/games/sight-reading-game/components/LevelUpCue.test.jsx` green.
</verification>

<success_criteria>

- LevelUpCue renders a localized, positive-only, reduced-motion-aware escalation overlay driven by `show`.
- `sightReading.adaptive.*` exists in EN+HE with exact parity; no easing/negative copy.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adaptive-pedagogy/03-03-SUMMARY.md`.
</output>
