---
phase: 25-unified-mixed-lesson-engine-for-trail-nodes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx
  - src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx
  - src/components/games/rhythm-games/VisualRecognitionGame.jsx
  - src/components/games/rhythm-games/SyllableMatchingGame.jsx
  - src/components/games/rhythm-games/renderers/__tests__/VisualRecognitionQuestion.test.jsx
  - src/components/games/rhythm-games/renderers/__tests__/SyllableMatchingQuestion.test.jsx
autonomous: true
requirements: [MLE-01, MLE-02]

must_haves:
  truths:
    - "VisualRecognitionQuestion renders a prompt heading and 4 DurationCard icons given a question prop"
    - "SyllableMatchingQuestion renders an SVG prompt panel and 4 DurationCard text syllable cards given a question prop"
    - "Standalone VisualRecognitionGame still works identically — all existing tests pass"
    - "Standalone SyllableMatchingGame still works identically — all existing tests pass"
    - "Renderers are stateless — no useState, no useEffect, no useNavigate"
  artifacts:
    - path: "src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx"
      provides: "Stateless visual recognition question renderer"
      exports: ["default"]
    - path: "src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx"
      provides: "Stateless syllable matching question renderer"
      exports: ["default"]
    - path: "src/components/games/rhythm-games/renderers/__tests__/VisualRecognitionQuestion.test.jsx"
      provides: "Renderer unit tests"
    - path: "src/components/games/rhythm-games/renderers/__tests__/SyllableMatchingQuestion.test.jsx"
      provides: "Renderer unit tests"
  key_links:
    - from: "src/components/games/rhythm-games/VisualRecognitionGame.jsx"
      to: "src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx"
      via: "import and render as child component"
      pattern: "import VisualRecognitionQuestion from"
    - from: "src/components/games/rhythm-games/SyllableMatchingGame.jsx"
      to: "src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx"
      via: "import and render as child component"
      pattern: "import SyllableMatchingQuestion from"
---

<objective>
Extract quiz rendering UI from VisualRecognitionGame and SyllableMatchingGame into stateless renderer components, then refactor the standalone games to use them as thin wrappers.

Purpose: Creates the reusable renderer components that both standalone games and the new MixedLessonGame (Plan 03) will consume. This is the foundation extraction that enables the unified engine.
Output: Two new renderer files in renderers/, two refactored standalone games, all existing tests green.
</objective>

<execution_context>
@C:\Users\pagis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pagis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-unified-mixed-lesson-engine-for-trail-nodes/25-CONTEXT.md
@.planning/phases/25-unified-mixed-lesson-engine-for-trail-nodes/25-RESEARCH.md
@.planning/phases/25-unified-mixed-lesson-engine-for-trail-nodes/25-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/games/rhythm-games/components/DurationCard.jsx:

- DurationCard({ type, durationCode, text, state, onSelect, disabled, cardIndex, ariaLabel })
- type: "icon" | "text"
- state: "default" | "correct" | "wrong" | "dimmed"
- SVG_COMPONENTS export: maps duration codes to SVG React components

From src/components/games/rhythm-games/utils/durationInfo.js:

- DURATION_INFO: { [durationCode]: { i18nKey, durationUnits, isRest } }
- generateQuestions(pool, allCodes, count, options?): { correct: string, choices: string[] }[]
- ALL_DURATION_CODES: string[]

From src/components/games/rhythm-games/utils/rhythmVexflowHelpers.js:

- SYLLABLE_MAP_EN: { [durationUnits]: string }
- SYLLABLE_MAP_HE: { [durationUnits]: string }
- REST_SYLLABLE_EN: string
- REST_SYLLABLE_HE: string
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create stateless renderer components</name>
  <files>
    src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx
    src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx
    src/components/games/rhythm-games/renderers/__tests__/VisualRecognitionQuestion.test.jsx
    src/components/games/rhythm-games/renderers/__tests__/SyllableMatchingQuestion.test.jsx
  </files>
  <read_first>
    src/components/games/rhythm-games/VisualRecognitionGame.jsx
    src/components/games/rhythm-games/SyllableMatchingGame.jsx
    src/components/games/rhythm-games/components/DurationCard.jsx
    src/components/games/rhythm-games/utils/durationInfo.js
    src/components/games/rhythm-games/utils/rhythmVexflowHelpers.js
    .planning/phases/25-unified-mixed-lesson-engine-for-trail-nodes/25-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/games/rhythm-games/renderers/` directory with two stateless renderer components. Per D-01, D-02, D-04:

**VisualRecognitionQuestion.jsx** — Extract from VisualRecognitionGame.jsx lines 265-311 (the prompt text + renderCards logic). Props contract per UI-SPEC section 4:

```jsx
export default function VisualRecognitionQuestion({
  question,    // { correct: string, choices: string[] }
  cardStates,  // ['default','default','default','default']
  isLandscape, // boolean
  onSelect,    // (cardIndex: number) => void
  disabled,    // boolean
})
```

The component must:

- Import `useTranslation` from `react-i18next` (only hook allowed besides `useMotionTokens`)
- Import `DurationCard` from `../components/DurationCard`
- Import `DURATION_INFO` from `../utils/durationInfo`
- Compute `durationName = t(DURATION_INFO[question.correct].i18nKey)` and `promptText = t('visualRecognition.prompt', { durationName })`
- Render a prompt heading in a glass card: `<div className="rounded-xl border border-white/20 bg-white/10 p-6 backdrop-blur-md"><h2 className="text-center text-xl font-bold text-white">{promptText}</h2></div>`
- Render card grid: portrait `grid grid-cols-2 gap-4 w-full max-w-sm`, landscape `grid grid-cols-4 gap-3 w-full max-w-2xl`
- Each card: `<DurationCard type="icon" durationCode={choice} state={cardStates[i]} onSelect={onSelect} disabled={disabled} cardIndex={i} ariaLabel={t(DURATION_INFO[choice].i18nKey)} />`
- Key each card as `${question.correct}-${i}` (not `${currentIndex}-${i}` since renderer has no index)
- Return a Fragment wrapping the prompt div and card grid div
- NO useState, NO useEffect, NO useNavigate, NO useSounds

**SyllableMatchingQuestion.jsx** — Extract from SyllableMatchingGame.jsx lines 286-351 (renderPromptPanel + renderCards). Props contract per UI-SPEC section 5:

```jsx
export default function SyllableMatchingQuestion({
  question,    // { correct: string, choices: string[] }
  cardStates,  // ['default','default','default','default']
  isLandscape, // boolean
  onSelect,    // (cardIndex: number) => void
  disabled,    // boolean
})
```

The component must:

- Import `useCallback` from React, `useTranslation` from `react-i18next`
- Import `DurationCard, { SVG_COMPONENTS }` from `../components/DurationCard`
- Import `DURATION_INFO` from `../utils/durationInfo`
- Import `SYLLABLE_MAP_EN, SYLLABLE_MAP_HE, REST_SYLLABLE_EN, REST_SYLLABLE_HE` from `../utils/rhythmVexflowHelpers`
- Include `getSyllable` callback (copied from SyllableMatchingGame lines 112-123) using `useCallback` with `[i18n.language]` dependency
- Render prompt panel: glass card with `dir="ltr"` wrapper on SVG, `SvgIcon = SVG_COMPONENTS[question.correct]`, icon class `h-24 w-auto` portrait / `h-16 w-auto` landscape, subtext `t('syllableMatching.prompt')` in `text-base text-white/60`
- Render card grid (same grid classes as VisualRecognitionQuestion) with `<DurationCard type="text" text={syllable} ... />` where `syllable = getSyllable(choice)`
- Key each card as `${question.correct}-${i}`
- Return Fragment wrapping prompt panel div and card grid div
- NO useState, NO useEffect, NO useNavigate, NO useSounds

**Tests** — Create renderer unit tests:

`renderers/__tests__/VisualRecognitionQuestion.test.jsx`:

- Mock `react-i18next` with `useTranslation` returning `t` that maps duration i18n keys
- Mock `../components/DurationCard` as a simple div rendering props
- Test: renders prompt text containing the correct duration name
- Test: renders 4 DurationCard elements with type="icon"
- Test: calls onSelect when a card is clicked
- Test: passes disabled prop through to DurationCard

`renderers/__tests__/SyllableMatchingQuestion.test.jsx`:

- Same mock pattern
- Mock `../components/DurationCard` and `SVG_COMPONENTS` (named export)
- Mock `../utils/rhythmVexflowHelpers` with simple syllable maps
- Test: renders SVG icon for the question.correct duration
- Test: renders 4 DurationCard elements with type="text"
- Test: calls onSelect when a card is clicked
- Test: passes disabled prop through to DurationCard
  </action>
  <verify>
  <automated>npx vitest run src/components/games/rhythm-games/renderers/**tests**/VisualRecognitionQuestion.test.jsx src/components/games/rhythm-games/renderers/**tests**/SyllableMatchingQuestion.test.jsx</automated>
  </verify>
  <acceptance_criteria> - File `src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx` exists and contains `export default function VisualRecognitionQuestion(` - File `src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx` exists and contains `export default function SyllableMatchingQuestion(` - VisualRecognitionQuestion.jsx does NOT contain `useState` or `useEffect` or `useNavigate` - SyllableMatchingQuestion.jsx does NOT contain `useState` or `useEffect` or `useNavigate` - VisualRecognitionQuestion.jsx contains `import DurationCard from "../components/DurationCard"` - SyllableMatchingQuestion.jsx contains `import { SYLLABLE_MAP_EN` from `../utils/rhythmVexflowHelpers` - Both renderer test files exist and pass with exit code 0
  </acceptance_criteria>
  <done>Both stateless renderers created with correct props contract, no state hooks, and passing unit tests</done>
  </task>

<task type="auto">
  <name>Task 2: Refactor standalone games to thin wrappers using renderers</name>
  <files>
    src/components/games/rhythm-games/VisualRecognitionGame.jsx
    src/components/games/rhythm-games/SyllableMatchingGame.jsx
  </files>
  <read_first>
    src/components/games/rhythm-games/VisualRecognitionGame.jsx
    src/components/games/rhythm-games/SyllableMatchingGame.jsx
    src/components/games/rhythm-games/renderers/VisualRecognitionQuestion.jsx
    src/components/games/rhythm-games/renderers/SyllableMatchingQuestion.jsx
    src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx
    src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx
  </read_first>
  <action>
Per D-03: Refactor both standalone games to be thin wrappers around the new renderers. No breaking changes to standalone routes.

**VisualRecognitionGame.jsx changes:**

1. Add import: `import VisualRecognitionQuestion from './renderers/VisualRecognitionQuestion';`
2. DELETE the entire `renderCards()` function (lines 290-311)
3. DELETE the `durationName` and `promptText` const declarations (lines 265-266) — these now live in the renderer
4. In the landscape layout section (the `<div className="flex flex-1 flex-col items-center justify-center gap-4">` block around line 333-340):
   - Replace the inline `<div className="rounded-xl ..."><h2 ...>{promptText}</h2></div>` and `{renderCards()}` with:
     ```jsx
     <VisualRecognitionQuestion
       question={currentQuestion}
       cardStates={cardStates}
       isLandscape={isLandscape}
       onSelect={handleSelect}
       disabled={gameState === GAME_STATES.FEEDBACK}
     />
     ```
5. In the portrait layout section (around lines 362-376):
   - Replace the prompt `<div className="mt-6 rounded-xl ...">` and the cards `<div className="mt-6 flex ...">` blocks with:
     ```jsx
     <div className="mt-6">
       <VisualRecognitionQuestion
         question={currentQuestion}
         cardStates={cardStates}
         isLandscape={isLandscape}
         onSelect={handleSelect}
         disabled={gameState === GAME_STATES.FEEDBACK}
       />
     </div>
     ```
   - Adjust container: the renderer returns a Fragment with prompt + grid, so wrap it in a container div that has the `mt-6` spacing and `flex-1` sizing as needed
6. Keep ALL state management, hooks, VictoryScreen, error guard, progress dots, `startGame`, `handleSelect`, `buildDurationPool`, etc. unchanged
7. The `DURATION_INFO` import can remain (used nowhere else after extraction) or be removed — both are fine since the renderer handles it internally. Remove `DurationCard` import since it's now used only by the renderer.

**SyllableMatchingGame.jsx changes:**

1. Add import: `import SyllableMatchingQuestion from './renderers/SyllableMatchingQuestion';`
2. DELETE the entire `renderCards()` function (lines 310-334)
3. DELETE the entire `renderPromptPanel()` function (lines 337-351)
4. DELETE the `getSyllable` callback (lines 112-123) — now lives in the renderer
5. DELETE the `SvgIcon` const (line 286) — now lives in the renderer
6. Remove now-unused imports: `SVG_COMPONENTS` from DurationCard, `SYLLABLE_MAP_EN`, `SYLLABLE_MAP_HE`, `REST_SYLLABLE_EN`, `REST_SYLLABLE_HE` from rhythmVexflowHelpers. Keep `DURATION_INFO` only if still used elsewhere in the file (it isn't after this refactor — remove it). Keep `DurationCard` import only if used elsewhere (it isn't — remove it).
7. In both landscape and portrait layouts, replace `{renderPromptPanel()}` and `{renderCards()}` calls with:
   ```jsx
   <SyllableMatchingQuestion
     question={currentQuestion}
     cardStates={cardStates}
     isLandscape={isLandscape}
     onSelect={handleSelect}
     disabled={gameState === GAME_STATES.FEEDBACK}
   />
   ```
8. Keep ALL state management, hooks, VictoryScreen, error guard, progress dots, everything else unchanged

**Critical verification:** After refactoring, run existing tests to ensure zero regressions. The standalone games must behave identically to before — same DOM structure rendered through the renderers.
</action>
<verify>
<automated>npx vitest run src/components/games/rhythm-games/**tests**/VisualRecognitionGame.test.jsx src/components/games/rhythm-games/**tests**/SyllableMatchingGame.test.jsx</automated>
</verify>
<acceptance_criteria> - VisualRecognitionGame.jsx contains `import VisualRecognitionQuestion from './renderers/VisualRecognitionQuestion'` - VisualRecognitionGame.jsx contains `<VisualRecognitionQuestion` (JSX usage) - VisualRecognitionGame.jsx does NOT contain `function renderCards` (inline rendering removed) - SyllableMatchingGame.jsx contains `import SyllableMatchingQuestion from './renderers/SyllableMatchingQuestion'` - SyllableMatchingGame.jsx contains `<SyllableMatchingQuestion` (JSX usage) - SyllableMatchingGame.jsx does NOT contain `function renderCards` or `function renderPromptPanel` (inline rendering removed) - SyllableMatchingGame.jsx does NOT contain `getSyllable` callback definition - All 4 existing test files pass: VisualRecognitionGame.test.jsx, SyllableMatchingGame.test.jsx, and both renderer tests - `npm run test:run` passes (full suite green)
</acceptance_criteria>
<done>Both standalone games use renderers as thin wrappers. All existing tests pass with zero changes to test files. No breaking changes to standalone routes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                        |
| -------- | ---------------------------------------------------------------------------------- |
| none     | Renderers are pure UI — no data input from external sources, no API calls, no auth |

## STRIDE Threat Register

| Threat ID | Category      | Component      | Disposition | Mitigation Plan                                                                                                           |
| --------- | ------------- | -------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-25-01   | T (Tampering) | Renderer props | accept      | Renderers receive data from parent engine which validates internally; no user-controlled data flows directly to renderers |

</threat_model>

<verification>
1. `npx vitest run src/components/games/rhythm-games/renderers/__tests__/VisualRecognitionQuestion.test.jsx` — renderer tests green
2. `npx vitest run src/components/games/rhythm-games/renderers/__tests__/SyllableMatchingQuestion.test.jsx` — renderer tests green
3. `npx vitest run src/components/games/rhythm-games/__tests__/VisualRecognitionGame.test.jsx` — standalone game still works
4. `npx vitest run src/components/games/rhythm-games/__tests__/SyllableMatchingGame.test.jsx` — standalone game still works
5. `npm run test:run` — full suite green
</verification>

<success_criteria>

- Two stateless renderer components exist in renderers/ directory
- Both standalone games import and use renderers (no inline card/prompt rendering)
- All existing tests pass without modification
- Renderer tests verify props contract
- No useState/useEffect/useNavigate in renderer files
  </success_criteria>

<output>
After completion, create `.planning/phases/25-unified-mixed-lesson-engine-for-trail-nodes/25-01-SUMMARY.md`
</output>
