---
phase: quick-260524-l3r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/patterns/rhythmPatterns.js
  - scripts/validateTrail.mjs
  - src/components/games/rhythm-games/renderers/ComposeRhythmQuestion.jsx
  - src/components/games/rhythm-games/renderers/__tests__/ComposeRhythmQuestion.test.jsx
  - src/components/games/rhythm-games/MixedLessonGame.jsx
  - src/data/units/rhythmUnit8Redesigned.js
  - src/data/units/rhythmUnit8Redesigned.test.js
  - src/locales/en/trail.json
  - src/locales/he/trail.json
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements:
  - quick-260524-l3r

must_haves:
  truths:
    - "Unit 8 exposes exactly 7 nodes (6 regular + boss) following the new modality-per-node design."
    - "Node 1 reframes q-h-q as a Hold-Across warm-up that also re-grounds 4/4 pulse after Unit 7's 6/8."
    - "Node 2 is the headline syncopation discovery (8-q-8) and is the only node that may use the word 'syncopation'/'סינ-קו-פה' in child-facing copy."
    - "Node 5 contains a runnable compose_rhythm question — the child can pick tiles, hear the assembled phrase, and advance."
    - "Boss draws >=50% syncopated bars by using the new curated 'syncopation-heavy' tag (any/all-syncopation patterns plus 4-6 contrast bars)."
    - "Pre-build (`npm run build`) succeeds — validateTrail.mjs accepts compose_rhythm as a renderer and tolerates Node 5's 5-question count."
    - "Unit 8 test file passes against the new 7-node structure with the Combined-values DATA-04 describe block left intact."
    - "EN + HE i18n strings are present for new node names, descriptions, and compose UI copy."
  artifacts:
    - path: "src/data/patterns/rhythmPatterns.js"
      provides: "syncopation-heavy curated tag pool + dropped orphan long-syncopation tag"
      contains: "syncopation-heavy"
    - path: "scripts/validateTrail.mjs"
      provides: "compose_rhythm renderer registration + relaxed minimum question gate"
      contains: "compose_rhythm"
    - path: "src/components/games/rhythm-games/renderers/ComposeRhythmQuestion.jsx"
      provides: "Tile palette + 2-slot composer + Play button for Node 5 creative milestone"
      min_lines: 120
    - path: "src/components/games/rhythm-games/renderers/__tests__/ComposeRhythmQuestion.test.jsx"
      provides: "Unit tests for ComposeRhythmQuestion (render, fill, play, complete)"
      min_lines: 60
    - path: "src/components/games/rhythm-games/MixedLessonGame.jsx"
      provides: "Dispatch + startGame pass-through for compose_rhythm question type"
      contains: "compose_rhythm"
    - path: "src/data/units/rhythmUnit8Redesigned.js"
      provides: "7-node Unit 8 (orders 144-150, XP arc 60/80/85/85/100/90/250, strict syncopation-heavy boss)"
      contains: "boss_rhythm_8"
    - path: "src/data/units/rhythmUnit8Redesigned.test.js"
      provides: "Rewritten assertions for 7-node design + boss 10-question + 250 XP"
    - path: "src/locales/en/trail.json"
      provides: "EN node labels and descriptions for new Unit 8 nodes"
    - path: "src/locales/he/trail.json"
      provides: "HE node labels — 'סינ-קו-פה' restricted to Node 2 (8-q-8) and boss"
    - path: "src/locales/en/common.json"
      provides: "EN compose UI strings (palette, slot.empty, play, instruction)"
    - path: "src/locales/he/common.json"
      provides: "HE compose UI strings"
  key_links:
    - from: "src/components/games/rhythm-games/MixedLessonGame.jsx"
      to: "src/components/games/rhythm-games/renderers/ComposeRhythmQuestion.jsx"
      via: "switch(currentQuestion.type) dispatch case 'compose_rhythm'"
      pattern: 'case ["'']compose_rhythm["'']'
    - from: "src/data/units/rhythmUnit8Redesigned.js"
      to: "src/data/patterns/rhythmPatterns.js"
      via: "boss patternTags: ['syncopation-heavy']"
      pattern: "syncopation-heavy"
    - from: "src/data/units/rhythmUnit8Redesigned.js"
      to: "src/components/games/rhythm-games/MixedLessonGame.jsx"
      via: "Node 5 questions[] includes { type: 'compose_rhythm', tiles: [...], slotCount: 2 }"
      pattern: 'type: ["'']compose_rhythm["'']'
    - from: "scripts/validateTrail.mjs"
      to: "src/components/games/rhythm-games/renderers/ComposeRhythmQuestion.jsx"
      via: "RENDERER_TYPES Set and SKIP_EXERCISE_TYPES include 'compose_rhythm'; min-question gate relaxed when present"
      pattern: "compose_rhythm"
---

<objective>
Refactor Rhythm Unit 8 (syncopation) end-to-end so it lands the pedagogically-correct
"feeling moment" of off-beat accents, varies modality per node to feel fresh, and
ships a new `compose_rhythm` creative milestone — for 8-year-old learners.

Purpose: The current 8-node Unit 8 is structurally sound but pedagogically flat —
identical question manifolds, q-h-q mislabelled as "syncopation," and a bloated
boss whose dilution hides the headline skill. This refactor implements the locked
PEDAGOGY-REVIEW design (7 nodes, monomodal specialization, child-facing word
"syncopation" reserved for 8-q-8, boss >=50% syncopated bars, new compose tile
question type).

Output: 11 file changes — pattern catalog tag curation, validator updates, a new
renderer + test, MixedLessonGame dispatch wiring, the Unit 8 data + test rewrite,
and EN/HE i18n for trail labels and compose UI strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260524-l3r-refactor-rhythm-unit-8-syncopation-pedag/260524-l3r-CONTEXT.md
@.planning/quick/260524-l3r-refactor-rhythm-unit-8-syncopation-pedag/260524-l3r-RESEARCH.md
@.planning/quick/260524-l3r-refactor-rhythm-unit-8-syncopation-pedag/PEDAGOGY-REVIEW.md
@src/data/units/rhythmUnit8Redesigned.js
@src/data/units/rhythmUnit8Redesigned.test.js

<interfaces>
<!-- Key contracts extracted from current source. Executors should use these directly. -->

From src/components/games/rhythm-games/MixedLessonGame.jsx (dispatch ~line 519,
startGame ~line 215):

```js
// startGame pass-through pattern (mirror existing ones, e.g. discovery_intro):
if (entry.type === "compose_rhythm") {
  return {
    type: "compose_rhythm",
    tiles: entry.tiles,           // [{ id, binary, label? }]
    slotCount: entry.slotCount ?? 2,
    tempo: buildRhythmTapConfig().tempo,
  };
}

// renderQuestion dispatch case (stateful tap path — owns lifecycle):
case "compose_rhythm":
  return (
    <ComposeRhythmQuestion
      question={currentQuestion}
      isLandscape={isLandscape}
      onComplete={handleRhythmTapComplete}
      disabled={disabled}
    />
  );
```

From src/components/games/rhythm-games/utils/rhythmTimingUtils.js
(`schedulePatternPlayback`):

```js
schedulePatternPlayback(
  beats,                // [{ durationUnits, isRest }]
  tempo,                // number
  audioContext,
  playNote,             // (time, durationSec) => void
  explicitStartTime?,
  timeSignature?,
);
```

From src/components/games/rhythm-games/utils/rhythmVexflowHelpers.js:

```js
binaryPatternToBeats(binary: number[]): { durationUnits: number, isRest: boolean }[]
```

From src/components/games/rhythm-games/components/RhythmStaffDisplay.jsx —
preferred VexFlow rhythm staff renderer (used by PulseQuestion and RhythmReadingQuestion).
Mirror the prop shape used in those callers.

From scripts/validateTrail.mjs:

- `RENDERER_TYPES` Set (~line 393) — controls which question types are accepted
  by `validateMixedLessons`.
- `SKIP_EXERCISE_TYPES` Set inside `validateMeasureCountPolicy` (~line 611) —
  question types that are not measure-count-driven.
- `validateMixedLessons` enforces a per-node min of 8 questions (4 for discovery,
  10-12 for boss). Node 5's 5-question count requires a relaxation when any
  question entry is `compose_rhythm`.

From `rhythmConfig` keys used in this unit:
`complexity, durations, focusDurations, contextDurations, patternTags,
patternTagMode, measureCount, tempo, pitch, timeSignature`.
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!--  WAVE 0 — Foundations (catalog + validator)                  -->
<!--  Land BEFORE the unit-data rewrite so prebuild can pass.     -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1: Curate rhythmPatterns.js — add `syncopation-heavy` tag and drop orphan `long-syncopation`</name>
  <files>src/data/patterns/rhythmPatterns.js</files>
  <action>
    Per RESEARCH §2 option (a) and CORRECTIONS §2:

    1. Find the single 4/4 pattern that still carries `long-syncopation` (q_44_003, the
       canonical q-h-q). Remove only the `long-syncopation` tag from its `tags` array.
       Keep all other tags intact (it already carries `quarter-half`). Confirm with a
       single ripgrep `long-syncopation` afterwards — expected count = 0.

    2. Identify the 16 existing 4/4 patterns tagged `syncopation` (the 8-q-8 family).
       Add `syncopation-heavy` to each of their `tags` arrays. Do NOT remove
       `syncopation` — both tags coexist.

    3. Add 4-6 NEW contrast patterns to the 4/4 section, each tagged
       `['quarter-half', 'syncopation-heavy']` or `['quarter-eighth', 'syncopation-heavy']`,
       so the boss draws ~70-80% syncopated bars (not 100%). Use simple non-syncopated
       shapes — e.g. `q q q q`, `q q h`, `h q q`, `q h q`, `q 8 8 q q`. Give them
       monotonic IDs continuing the existing 4/4 ID series (verify by greppping the
       highest `q_44_NNN` ID first).

    Verify via inline `node -e` after editing:
    ```
    node -e "const { RHYTHM_PATTERNS } = require('./src/data/patterns/rhythmPatterns.js'); \
      const all = RHYTHM_PATTERNS.filter(p => p.tags?.includes('syncopation-heavy')); \
      const sync = all.filter(p => p.tags?.includes('syncopation')); \
      console.log('total:', all.length, 'syncopated:', sync.length, 'ratio:', (sync.length/all.length).toFixed(2));"
    ```
    Expected: total ~20-22, syncopated 16, ratio 0.70-0.85.

  </action>
  <verify>
    <automated>node -e "const { RHYTHM_PATTERNS } = require('./src/data/patterns/rhythmPatterns.js'); const heavy = RHYTHM_PATTERNS.filter(p => p.tags?.includes('syncopation-heavy')); const sync = heavy.filter(p => p.tags?.includes('syncopation')); const orphan = RHYTHM_PATTERNS.filter(p => p.tags?.includes('long-syncopation')); if (heavy.length < 18 || heavy.length > 24) { console.error('heavy count off:', heavy.length); process.exit(1); } if (sync.length !== 16) { console.error('syncopation count off:', sync.length); process.exit(1); } if (orphan.length !== 0) { console.error('long-syncopation orphan still present:', orphan.length); process.exit(1); } const ratio = sync.length / heavy.length; if (ratio < 0.65 || ratio > 0.9) { console.error('ratio off:', ratio); process.exit(1); } console.log('OK heavy:', heavy.length, 'sync:', sync.length, 'ratio:', ratio.toFixed(2));"</automated>
  </verify>
  <done>
    `long-syncopation` no longer appears in any pattern's `tags`; 16 existing
    syncopation patterns + 4-6 contrast patterns carry `syncopation-heavy`;
    syncopation ratio within the new pool is 0.65-0.9.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update validateTrail.mjs — register compose_rhythm + relax min-question gate</name>
  <files>scripts/validateTrail.mjs</files>
  <action>
    Three surgical edits per RESEARCH §4:

    1. Locate the `RENDERER_TYPES` Set (~line 393) and add `'compose_rhythm'` to the
       allowed renderer question types.

    2. Locate the `SKIP_EXERCISE_TYPES` Set inside `validateMeasureCountPolicy`
       (~line 611) and add `'compose_rhythm'` — compose tiles are pre-authored,
       not measure-driven.

    3. In `validateMixedLessons`, relax the per-node minimum-question check so that
       when ANY question entry in a node has `type === 'compose_rhythm'`, the
       minimum is `>= 5` instead of `>= 8`. Keep boss/discovery thresholds otherwise
       intact. Use a clear conditional and a comment referencing this plan
       (`compose_rhythm creative milestone allows shorter node — see quick task 260524-l3r`).

    Do NOT change anything else — no XP-range loosening, no tag-coverage changes,
    no test-file edits in this task.

  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('./scripts/validateTrail.mjs','utf8'); const r1 = /RENDERER_TYPES[^\}]*compose_rhythm/.test(src); const r2 = /SKIP_EXERCISE_TYPES[^\}]*compose_rhythm/.test(src); const r3 = /compose_rhythm[\s\S]{0,200}>= 5|>= 5[\s\S]{0,200}compose_rhythm/.test(src); if (!r1) { console.error('RENDERER_TYPES missing compose_rhythm'); process.exit(1); } if (!r2) { console.error('SKIP_EXERCISE_TYPES missing compose_rhythm'); process.exit(1); } if (!r3) { console.error('min-question relaxation for compose_rhythm not detected'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    Three checks pass. (Prebuild trail validation will still run against the
    pre-existing Unit 8 file at this point — that is expected and tested in the
    unit-data rewrite task.)
  </done>
</task>

<!-- ============================================================ -->
<!--  WAVE 1 — Compose renderer + dispatch                        -->
<!-- ============================================================ -->

<task type="auto" tdd="true">
  <name>Task 3: Build ComposeRhythmQuestion renderer + tests</name>
  <files>
    src/components/games/rhythm-games/renderers/ComposeRhythmQuestion.jsx,
    src/components/games/rhythm-games/renderers/__tests__/ComposeRhythmQuestion.test.jsx
  </files>
  <behavior>
    - Renders one `RhythmStaffDisplay` mini-preview per tile in `question.tiles`.
    - Renders `question.slotCount` (default 2) empty slot drop-zones.
    - Clicking a tile fills the first empty slot with that tile (tap-first; drag
      not required for MVP).
    - Clicking a filled slot empties it (lets the child correct mistakes).
    - "Play" button is disabled until all slots are filled; when clicked, calls
      `schedulePatternPlayback` with the concatenation of `binaryPatternToBeats`
      across all filled slots at `question.tempo`.
    - When all slots are filled AND the user presses an "I'm done" / "Continue"
      action, calls `onComplete(slotCount, slotCount)` exactly once — informational
      success per RESEARCH §1 recommendation.
    - Respects `disabled` prop (no interactions while parent transitions).
    - Uses i18n keys `compose.palette`, `compose.slot.empty`, `compose.play`,
      `compose.instruction`, `compose.done` (added in Task 7).
    - Glassmorphism styling consistent with PulseQuestion/RhythmReadingQuestion
      (purple gradient backdrop, `bg-white/10 backdrop-blur-md border border-white/20`).
    - Test cases (RED first):
      1. renders `tiles.length` tile previews and `slotCount` slot containers
      2. clicking a tile fills the first empty slot
      3. clicking a filled slot empties it
      4. Play button is disabled until all slots filled, then invokes
         `schedulePatternPlayback` exactly once with concatenated beats
      5. pressing the Done/Continue button after all slots filled invokes
         `onComplete(slotCount, slotCount)` exactly once
  </behavior>
  <action>
    Mirror the existing renderer pattern from `PulseQuestion.jsx` and
    `RhythmReadingQuestion.jsx`:
    - Imports: `useAudioEngine` from the rhythm hooks, `RhythmStaffDisplay`,
      `schedulePatternPlayback`, `binaryPatternToBeats`, `useTranslation`.
    - Mock `useAudioEngine` (returns `{ audioContext, playNote, isReady: true }`),
      `schedulePatternPlayback`, and `RhythmStaffDisplay` (render a `<div data-testid="staff" />`)
      in the test setup — mirror the mocking style used in
      `PulseQuestion.test.jsx`.
    - Tile preview should set a stable `key={tile.id}` and a
      `data-testid={`tile-${tile.id}`}`.
    - Slots: `data-testid={`slot-${index}`}` with `aria-label` showing fill state.
    - Keep total LOC reasonable (~150-250). No drag-and-drop dependency —
      pure tap interaction.
    - Mobile-friendly tap targets (min 44px height per `docs/DESIGN_SYSTEM.md`
      conventions).
    - Translation keys read inside the component via `t('compose.palette')` etc;
      tests can stub i18n with `vi.mock('react-i18next', ...)` returning identity
      `t` (mirror existing renderer tests).

    Write the test file FIRST, watch it fail, then implement the renderer until
    all 5 tests pass. Commit the failing test, then the GREEN implementation, as
    two separate commits if pre-commit hooks allow; otherwise one combined commit
    with both files is acceptable.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/renderers/__tests__/ComposeRhythmQuestion.test.jsx</automated>
  </verify>
  <done>
    Test file exists with >=5 assertions; renderer file exists with the behaviors
    above; `npx vitest run` for this file is green; no console errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire compose_rhythm into MixedLessonGame dispatch + startGame</name>
  <files>src/components/games/rhythm-games/MixedLessonGame.jsx</files>
  <action>
    Two surgical edits matching the interfaces block in `<context>`:

    1. Add `import ComposeRhythmQuestion from "./renderers/ComposeRhythmQuestion";`
       alongside the other renderer imports.

    2. In `startGame()` (~line 215), add the pass-through entry BEFORE the
       generic `rhythm_tap` config builder:
       ```js
       if (entry.type === "compose_rhythm") {
         return {
           type: "compose_rhythm",
           tiles: entry.tiles,
           slotCount: entry.slotCount ?? 2,
           tempo: buildRhythmTapConfig().tempo,
         };
       }
       ```

    3. In `renderQuestion()` (~line 519), add a `case "compose_rhythm":` returning
       `<ComposeRhythmQuestion ... />` with the standard stateful-path props
       (`{ question: currentQuestion, isLandscape, onComplete: handleRhythmTapComplete, disabled }`).

    Critical: remember `feedback_mixedlesson_key_prop.md` — when wrapping in any
    crossfade/key element, the key MUST include `currentIndex`, not just
    `fadeKey`, to prevent freezes on consecutive same-type questions. Inspect
    surrounding dispatch cases for the established key pattern and follow it.

    Do NOT modify any other dispatch cases or scoring logic.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/MixedLessonGame.test.jsx 2>nul || node -e "const fs = require('fs'); const src = fs.readFileSync('./src/components/games/rhythm-games/MixedLessonGame.jsx','utf8'); const i = /import\s+ComposeRhythmQuestion/.test(src); const s = /entry\.type\s*===\s*[\"']compose_rhythm[\"']/.test(src); const d = /case\s+[\"']compose_rhythm[\"']:/.test(src); if (!i || !s || !d) { console.error('missing pieces — import:', i, 'startGame:', s, 'dispatch:', d); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    Import, startGame pass-through, and dispatch case all present;
    MixedLessonGame test suite (if it exists) still passes; static check confirms
    the three pieces.
  </done>
</task>

<!-- ============================================================ -->
<!--  WAVE 2 — Unit 8 data + test rewrite (depends on Waves 0-1)  -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 5: Rewrite Unit 8 data — 7 nodes per PEDAGOGY-REVIEW + CONTEXT decisions</name>
  <files>src/data/units/rhythmUnit8Redesigned.js</files>
  <action>
    Replace the existing 8-node export with the 7-node structure below. All nodes
    keep `category: "rhythm"` (boss keeps `category: "boss"`), `pitch: "C4"`,
    `timeSignature: "4/4"`, `START_ORDER = 144`. XP arc: 60 / 80 / 85 / 85 / 100 / 90 / 250.

    Drop entirely: the `syncopation_dotted_quarter` skill, all references to
    `qd, qd-r, 16, hd, wr, hr, dotted-quarter, dotted-syncopation, dotted-half,
    sixteenth, whole-rest, half-rest, quarter-half-whole, quarter-half-whole-eighth`
    from this unit (boss only carries `[q, h, 8]` per CONTEXT.md decision D).
    Drop `long-syncopation` from every node's `patternTags`.

    Node specs (per PEDAGOGY-REVIEW §4 + RESEARCH recommendations):

    - **`rhythm_8_1` "Hold-Across Warm-Up"** — DISCOVERY, order 144,
      prerequisites `["boss_rhythm_7"]`, `durations: ["q","h"]`,
      `patternTags: ["quarter-half"]`, `tempo {min:60,max:66,default:63}`,
      `measureCount: 1`, `newContentDescription: "Hold across the beat — long notes that hover over the pulse"`,
      `skills: ["syncopation_long_value"]`, `xpReward: 60`. Questions (6-7):
      `discovery_intro(focusDuration:'h')` → `pulse` → `rhythm_tap` →
      `visual_recognition` → `rhythm_tap` → `rhythm_reading`. (Re-grounds 4/4
      pulse after Unit 7's 6/8.) MUST include at least one of
      `visual_recognition`/`syllable_matching` to satisfy
      `validateMultiAngleGames`.

    - **`rhythm_8_2` "Surprise Beat: Listen & Echo"** — DISCOVERY, order 145,
      prerequisites `["rhythm_8_1"]`, `durations: ["8","q"]`,
      `patternTags: ["syncopation"]` (strict — no fallback), `tempo {min:64,max:70,default:67}`,
      `measureCount: 1`, `newContentDescription: "Syncopation: a sound where the beat isn't!"`,
      `skills: ["syncopation_eighth_quarter"]`, `xpReward: 80`. Questions (7,
      listen-first): `discovery_intro(focusDuration:'8')` → `rhythm_tap` →
      `rhythm_tap` → `visual_recognition` → `syllable_matching` → `rhythm_tap`
      → `rhythm_reading`.

    - **`rhythm_8_3` "Read the Off-Beat"** — PRACTICE, order 146,
      prerequisites `["rhythm_8_2"]`, `durations: ["q","h","8"]`,
      `patternTags: ["syncopation","quarter-half"]`, `patternTagMode: "any"`,
      `measureCount: 2`, `tempo {min:70,max:78,default:74}`,
      `skills: ["syncopation_eighth_quarter"]`, `xpReward: 85`. Questions (6, reading-only +
      detective): `rhythm_reading` × 4 interleaved with `visual_recognition` × 2.
      Sequence: `reading, visual_recognition, reading, reading, visual_recognition, reading`.

    - **`rhythm_8_4` "Body Split"** — PRACTICE, order 147, prerequisites
      `["rhythm_8_3"]`, `durations: ["q","8"]`,
      `patternTags: ["syncopation"]` (strict), `patternTagMode: "any"`,
      `measureCount: 2`, `tempo {min:66,max:74,default:70}`,
      `skills: ["syncopation_eighth_quarter"]`, `xpReward: 85`. Questions (6,
      pulse + tap dominant): `pulse` → `rhythm_tap` → `rhythm_tap` →
      `rhythm_tap` → `rhythm_tap` → `rhythm_tap`. (Per CONTEXT.md decision and
      RESEARCH §3: ship without the visual-pulse renderer addition; plain
      `rhythm_tap` is the degraded fallback.) Include one
      `visual_recognition` OR `syllable_matching` if `validateMultiAngleGames`
      complains — replace the final `rhythm_tap` with `visual_recognition`.

    - **`rhythm_8_5` "Build a Syncopation"** — CHALLENGE / PRACTICE
      (`nodeType: NODE_TYPES.PRACTICE` is fine; do not introduce a new node type),
      order 148, prerequisites `["rhythm_8_4"]`, `durations: ["q","h","8"]`,
      `patternTags: ["syncopation","quarter-half"]`, `patternTagMode: "any"`,
      `measureCount: 2`, `tempo {min:72,max:78,default:75}`,
      `skills: ["syncopation_long_value","syncopation_eighth_quarter"]`,
      `xpReward: 100`. Questions (5): one `compose_rhythm` entry then 2
      `rhythm_tap` + 2 `rhythm_reading`. The compose entry MUST include:
      ```js
      {
        type: "compose_rhythm",
        slotCount: 2,
        tiles: [
          { id: "tile_qqqq", binary: [1,0,1,0,1,0,1,0], label: "q q q q" },
          { id: "tile_qhq",  binary: [1,0,1,0,0,0,1,0], label: "q h q" },
          { id: "tile_8q8q", binary: [1,1,0,1,0,1,0,1], label: "8-q-8 q" },
          { id: "tile_q8q8", binary: [1,0,1,1,0,1,0,1], label: "q 8-q-8" },
        ],
      }
      ```
      (Author the binary arrays carefully — each tile must equal 8 sixteenth-units
      = 1 bar of 4/4. Tap notes are `1`, sustain/silence `0` per the project's
      existing convention; verify against `binaryPatternToBeats` semantics
      before finalizing.)

    - **`rhythm_8_6` "Off-Beat Speed Drill"** — SPEED_ROUND, order 149,
      prerequisites `["rhythm_8_5"]`, `durations: ["q","h","8"]`, exercise type
      `ARCADE_RHYTHM` with `difficulty: "advanced"`. `rhythmConfig.patternTags:
      ["syncopation","quarter-eighth"]` (tightened from current 8-tag pool per
      PEDAGOGY-REVIEW §4 Node 6), `patternTagMode: "any"`,
      `tempo {min:80,max:88,default:84}`, `xpReward: 90`,
      `newContent: NEW_CONTENT_TYPES.CHALLENGE_TYPE`,
      `newContentDescription: "Syncopation Speed Drill"`,
      `skills: ["syncopation_long_value","syncopation_eighth_quarter"]`.

    - **`boss_rhythm_8` "Syncopation Boss"** — BOSS, order 150,
      prerequisites `["rhythm_8_6"]`, `category: "boss"`,
      `durations: ["q","h","8"]` (strict — drops everything else per CONTEXT decision D),
      `contextDurations: ["q","h","8"]`,
      `patternTags: ["syncopation-heavy"]`, `patternTagMode: "any"`,
      `measureCount: 4`, `tempo {min:78,max:88,default:84}`,
      `xpReward: 250`, `accessoryUnlock: "advanced_rhythm_badge"`,
      `isBoss: true`,
      `skills: ["syncopation_long_value","syncopation_eighth_quarter"]`
      (drop `68_compound_meter` — this is the syncopation capstone, not a
      grandmaster review). Questions (10, per PEDAGOGY-REVIEW): `rhythm_reading` × 4,
      `rhythm_dictation` × 3, `rhythm_tap` × 2, `visual_recognition` × 1.
      Sequence: `reading, dictation, reading, tap, dictation, reading, dictation,
      tap, visual_recognition, reading`.

    Keep `unlockHint` on boss reasonable for child copy ("Master off-beat magic
    to face the syncopation boss!").

    Preserve `import` style and the file's module shape (named export
    `rhythmUnit8Nodes` + default export).

  </action>
  <verify>
    <automated>npm run verify:trail</automated>
  </verify>
  <done>
    `npm run verify:trail` passes with zero errors. The file exports exactly 7
    nodes with the IDs, orders, prereqs, durations, XP rewards, and questions
    described above. (The unit test file is rewritten in Task 6 — `npm test` is
    expected to fail until then.)
  </done>
</task>

<task type="auto">
  <name>Task 6: Rewrite rhythmUnit8Redesigned.test.js for the 7-node design</name>
  <files>src/data/units/rhythmUnit8Redesigned.test.js</files>
  <action>
    Full rewrite per RESEARCH §4 — 10 of 16 existing assertions break under the
    new design. New assertions (keep `Combined-values node variety (DATA-04)`
    describe block intact — it tests Units 1/2/3, NOT Unit 8):

    Top-level `describe('rhythmUnit8Nodes', ...)` should cover:
    1. exports exactly 7 nodes
    2. all node IDs are unique
    3. node IDs match exactly `['rhythm_8_1','rhythm_8_2','rhythm_8_3','rhythm_8_4','rhythm_8_5','rhythm_8_6','boss_rhythm_8']`
    4. orders are sequential `[144, 145, 146, 147, 148, 149, 150]`
    5. prerequisite chain walks `boss_rhythm_7 → rhythm_8_1 → rhythm_8_2 → ... → rhythm_8_6 → boss_rhythm_8`
    6. all 7 nodes use timeSignature `'4/4'`
    7. all 7 nodes use pitch `'C4'`
    8. regular nodes (slice 0,6) use category `'rhythm'`; boss uses category `'boss'`
    9. first node (`rhythm_8_1`) is the Hold-Across warm-up — assert
       `nodeType === NODE_TYPES.DISCOVERY`, `durations` includes `['q','h']` only,
       `newContentDescription` matches `/hold/i` and does NOT contain `/syncopa/i`
       (decision A: q-h-q is NOT framed as syncopation in child copy)
    10. second node (`rhythm_8_2`) is the headline syncopation discovery — assert
        `nodeType === DISCOVERY`, `durations` equals `['8','q']`,
        `patternTags` equals `['syncopation']` (strict),
        `newContentDescription` matches `/syncopa/i`
    11. node 5 (`rhythm_8_5`) contains exactly one `compose_rhythm` question,
        and its `tiles` array has length 4-6 with `slotCount === 2`
    12. node 6 is `SPEED_ROUND` with `EXERCISE_TYPES.ARCADE_RHYTHM`
    13. boss has `id === 'boss_rhythm_8'`, `isBoss === true`,
        `xpReward === 250`, `accessoryUnlock === 'advanced_rhythm_badge'`
    14. boss exercise is `MIXED_LESSON` with exactly 10 questions
    15. boss `patternTags` equals `['syncopation-heavy']` and
        `durations` equals `['q','h','8']` (no dotted/sixteenth/rest values)
    16. boss `skills` does NOT include `'68_compound_meter'` (capstone is syncopation only)
    17. XP arc — regular nodes XP values in order equal `[60, 80, 85, 85, 100, 90]`
    18. tempo trend — first node default tempo < last regular node default tempo

    Leave the `Combined-values node variety (DATA-04)` describe block (about Units
    1/2/3) completely untouched. If it lives in a SEPARATE file (most likely
    `src/data/units/dataVariety.test.js` or similar — verify with a quick grep before
    starting), then no action is needed in THIS file. If it does live in
    `rhythmUnit8Redesigned.test.js`, preserve its contents verbatim.

    Import shape: keep `import { rhythmUnit8Nodes } from './rhythmUnit8Redesigned.js'`
    plus `import { NODE_TYPES, NEW_CONTENT_TYPES } from '../nodeTypes.js'` and
    `import { EXERCISE_TYPES } from '../constants.js'`.

  </action>
  <verify>
    <automated>npx vitest run src/data/units/rhythmUnit8Redesigned.test.js</automated>
  </verify>
  <done>
    18 unit-8-specific assertions pass; `Combined-values node variety (DATA-04)`
    describe block is unchanged (verify with `git diff -- src/data/units/`); full
    `npm run test:run` for this file is green.
  </done>
</task>

<!-- ============================================================ -->
<!--  WAVE 3 — i18n (touches different files, independent commit) -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 7: Add EN + HE i18n strings — trail labels + compose UI</name>
  <files>
    src/locales/en/trail.json,
    src/locales/he/trail.json,
    src/locales/en/common.json,
    src/locales/he/common.json
  </files>
  <action>
    **`trail.json` (both EN and HE):** Update Unit 8 node display strings.
    First read the current Unit 8 key shape in each file (likely
    `nodes.rhythm_8_1.name` / `.description` and equivalents — DO NOT guess; mirror
    whatever shape the file already uses for other rhythm nodes). Provide:

    - `rhythm_8_1.name` — EN: "Hold-Across Warm-Up" / HE: "החזקה מעל הפעמה"
      (do NOT use "סינ-קו-פה" here — reserved for 8-q-8 per CONTEXT decision A)
    - `rhythm_8_1.description` — EN: "Re-ground in 4/4 with long notes that
      hover over the beat" / HE: equivalent without "סינ-קו-פה"
    - `rhythm_8_2.name` — EN: "Surprise Beat: Listen & Echo" / HE: "פעמה מפתיעה: סינ-קו-פה"
      (this is the ONE child-facing node that may use "סינ-קו-פה" — reserved
      per CONTEXT.md decision A and `feedback_hebrew_nikud.md` memory)
    - `rhythm_8_2.description` — EN: "Hear an off-beat surprise — a sound
      where the beat isn't!" / HE: equivalent
    - `rhythm_8_3.name` — EN: "Read the Off-Beat" / HE: "קוראים את הלא-בפעמה"
    - `rhythm_8_3.description` — EN: "Spot the syncopated bar — detective mode"
      / HE: equivalent
    - `rhythm_8_4.name` — EN: "Body Split" / HE: "פיצול גוף"
    - `rhythm_8_4.description` — EN: "Tap the pulse and the off-beat together"
      / HE: equivalent
    - `rhythm_8_5.name` — EN: "Build a Syncopation" / HE: "בונים סינ-קו-פה"
      (8-q-8 family — "סינ-קו-פה" allowed)
    - `rhythm_8_5.description` — EN: "Pick tiles, build a 2-bar phrase, hear it
      play!" / HE: equivalent
    - `rhythm_8_6.name` — EN: "Off-Beat Speed Drill" / HE: "מרוץ הלא-בפעמה"
    - `rhythm_8_6.description` — EN: "How fast can you tap syncopated rhythms?"
      / HE: equivalent
    - `boss_rhythm_8.name` — EN: "Syncopation Boss" / HE: "מנהיג הסינ-קו-פה"
    - `boss_rhythm_8.description` — EN: "Master off-beat magic — capstone of
      the unit!" / HE: equivalent

    Per `feedback_hebrew_nikud.md` memory: Hebrew Kodaly syllables use confirmed
    diacritics — DO NOT alter "סינ-קו-פה" hyphenation or add nikud without
    asking. The user has explicitly approved this spelling.

    **`common.json` (both EN and HE):** Add a `compose.*` sub-tree with these
    keys (used by ComposeRhythmQuestion in Task 3):
    - `compose.instruction` — EN: "Pick 2 tiles to build your rhythm" / HE:
      "בחר 2 חתיכות כדי לבנות את הקצב שלך"
    - `compose.palette` — EN: "Your tiles" / HE: "החתיכות שלך"
    - `compose.slot.empty` — EN: "Tap a tile" / HE: "לחץ על חתיכה"
    - `compose.slot.filled` — EN: "Tap to remove" / HE: "לחץ להסרה"
    - `compose.play` — EN: "Play your rhythm" / HE: "השמע את הקצב שלך"
    - `compose.done` — EN: "I'm done!" / HE: "סיימתי!"

    If the EN/HE common.json files use a nested structure for game UI, place
    `compose.*` adjacent to existing rhythm-game keys. Mirror existing key
    nesting conventions — do not flatten.

    Do NOT touch any other unit's translations.

  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const en_t = JSON.parse(fs.readFileSync('./src/locales/en/trail.json','utf8')); const he_t = JSON.parse(fs.readFileSync('./src/locales/he/trail.json','utf8')); const en_c = JSON.parse(fs.readFileSync('./src/locales/en/common.json','utf8')); const he_c = JSON.parse(fs.readFileSync('./src/locales/he/common.json','utf8')); const s = JSON.stringify; const t = s({en_t, he_t, en_c, he_c}); const ids = ['rhythm_8_1','rhythm_8_2','rhythm_8_3','rhythm_8_4','rhythm_8_5','rhythm_8_6','boss_rhythm_8']; ids.forEach(id => { if (!s(en_t).includes(id) || !s(he_t).includes(id)) { console.error('missing trail key for', id); process.exit(1); } }); ['palette','slot','play','done','instruction'].forEach(k => { if (!s(en_c).includes('compose') || !s(he_c).includes('compose')) { console.error('missing compose.* in common.json'); process.exit(1); } }); const heAll = s(he_t); const hold = (heAll.match(/rhythm_8_1[\s\S]{0,400}/) || [''])[0]; const offbeat = (heAll.match(/rhythm_8_3[\s\S]{0,400}/) || [''])[0]; const body = (heAll.match(/rhythm_8_4[\s\S]{0,400}/) || [''])[0]; const speed = (heAll.match(/rhythm_8_6[\s\S]{0,400}/) || [''])[0]; const guards = [hold, offbeat, body, speed]; for (const g of guards) { if (g.includes('סינ-קו-פה')) { console.error('סינ-קו-פה leaked into a non-8-q-8 node — decision A violated:', g.slice(0,200)); process.exit(1); } } console.log('OK');"</automated>
  </verify>
  <done>
    All four JSON files parse; every Unit 8 node ID has new EN+HE name+description;
    `compose.*` keys present in both `common.json` files; "סינ-קו-פה" appears
    only in `rhythm_8_2`, `rhythm_8_5`, and `boss_rhythm_8` Hebrew entries (per
    decision A guard).
  </done>
</task>

<!-- ============================================================ -->
<!--  WAVE 4 — Final integration verification                     -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 8: Full verification — build + tests + lint</name>
  <files>(no source changes — verification only)</files>
  <action>
    Run the full verification suite to confirm the refactor is shippable end-to-end:

    1. `npm run lint` — must pass (pre-commit hook will run this anyway, but
       confirm clean state).
    2. `npm run verify:trail` — must pass (validates the new 7-node Unit 8
       structure against the updated validator).
    3. `npm run test:run` — full Vitest suite must pass. Pay particular attention
       to: `rhythmUnit8Redesigned.test.js`, `ComposeRhythmQuestion.test.jsx`,
       MixedLessonGame tests (if present), any pattern-catalog tests
       (`rhythmPatterns.test.js` or equivalent — check for tests asserting
       specific tag counts that the curation may have broken).
    4. `npm run build` — the prebuild hook re-runs `verify:trail`; confirm the
       full production build succeeds.

    If `npm run build` fails with `validateMixedLessons` complaining about Node
    5's question count, re-check Task 2's relaxation logic — the conditional
    must detect a `compose_rhythm`-containing node and apply `>= 5`.

    If any unrelated test fails (e.g. a pattern-count test in
    `rhythmPatterns.test.js`), update its expected count to match the new
    catalog (Task 1 added 4-6 patterns + a `syncopation-heavy` tag). Do NOT
    touch unrelated unit tests or other rhythm units.

    Report the verification status — pass/fail per command, with failure
    diagnostics if any.

  </action>
  <verify>
    <automated>npm run lint && npm run verify:trail && npm run test:run && npm run build</automated>
  </verify>
  <done>
    All four commands exit 0. The refactor is shippable.
  </done>
</task>

</tasks>

<verification>
End-to-end goal verification:
1. `npm run verify:trail` passes with the new 7-node Unit 8.
2. `npm run test:run` passes — including the rewritten unit test and the new
   ComposeRhythmQuestion test.
3. `npm run build` succeeds (prebuild hook validates trail).
4. Manual UI smoke test (deferred to UAT): navigate to Trail → Unit 8 → Node 5,
   confirm compose tiles render, can be tapped into slots, Play plays the
   assembled phrase, advancing to next exercise works.
5. Boss visually emits ~70-80% syncopated bars (sample 5 boss runs, count
   syncopated bars).
</verification>

<success_criteria>

- 11 file changes committed across 8 atomic tasks (one task may produce two
  commits per the TDD pattern in Task 3 — total 8-9 commits).
- Unit 8 ships 7 nodes (down from 8) with the new modality-per-node design.
- `compose_rhythm` is a fully-wired question type — renderer + dispatch +
  validator + tests + i18n + node 5 reference.
- Boss draws strict `[q, h, 8]` durations against the curated
  `syncopation-heavy` pool (>=50% syncopated bars).
- Child-facing copy honors decision A: "syncopation" / "סינ-קו-פה" appears only
  on Node 2 (8-q-8 discovery), Node 5 (compose), and the boss.
- Test file `rhythmUnit8Redesigned.test.js` is rewritten; the
  `Combined-values node variety (DATA-04)` describe block is preserved verbatim.
- `npm run build` succeeds end-to-end.
  </success_criteria>

<output>
After completion, create
`.planning/quick/260524-l3r-refactor-rhythm-unit-8-syncopation-pedag/260524-l3r-01-SUMMARY.md`
covering: files touched, commits made, verification output, any UX caveats for
UAT (e.g. compose-tile RTL handling, body-split visual pulse deferred per
CONTEXT decision D), Hebrew copy hand-off note for the user to review final
wording.
</output>
