---
phase: quick-260514-w1y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/units/rhythmUnit1Redesigned.js
  - src/config/subscriptionConfig.js
autonomous: true
requirements: [MERGE-RHYTHM-1-1-1-2]

must_haves:
  truths:
    - "Rhythm Unit 1 has 5 nodes: merged quarter-notes node, rhythm_1_3, rhythm_1_4, rhythm_1_6, boss_rhythm_1"
    - "rhythm_1_2 no longer exists in the trail data"
    - "Merged node keeps id rhythm_1_1, nodeType discovery, opens with discovery_intro then practices quarter notes"
    - "rhythm_1_3 prerequisite points at rhythm_1_1 (not rhythm_1_2)"
    - "All Unit 1 node order/orderInUnit fields are contiguous with no gaps"
    - "FREE_RHYTHM_NODE_IDS no longer lists rhythm_1_2"
    - "npm run verify:trail passes"
    - "npm run test:run passes"
  artifacts:
    - path: "src/data/units/rhythmUnit1Redesigned.js"
      provides: "Merged Unit 1 node definitions (5 nodes, rhythm_1_2 deleted)"
      contains: 'id: "rhythm_1_1"'
    - path: "src/config/subscriptionConfig.js"
      provides: "FREE_RHYTHM_NODE_IDS without rhythm_1_2"
  key_links:
    - from: "src/data/units/rhythmUnit1Redesigned.js (rhythm_1_3)"
      to: "rhythm_1_1"
      via: "prerequisites array"
      pattern: "prerequisites: \\[\"rhythm_1_1\"\\]"
---

<objective>
Merge rhythm trail Unit 1 nodes `rhythm_1_1` ("Meet Quarter Notes", Discovery, 4 questions) and `rhythm_1_2` ("Practice Quarter Notes", Practice, 8 questions) into a single Discovery node. Both nodes are quarter-note-only on C4 and feel repetitive to the user.

The merged node keeps id `rhythm_1_1`, stays nodeType DISCOVERY, opens with a `discovery_intro` card then a richer ~8-question practice mix. `rhythm_1_2` is deleted entirely. `rhythm_1_3` / `rhythm_1_4` keep their IDs (numbering gap 1_1, 1_3, 1_4 is intentional — preserves student progress rows keyed by node_id).

Purpose: Remove redundant quarter-note grind so Unit 1 progresses faster and feels less repetitive.
Output: Updated `rhythmUnit1Redesigned.js` (5 nodes) and `subscriptionConfig.js` (FREE_RHYTHM_NODE_IDS without rhythm_1_2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Files to modify

@src/data/units/rhythmUnit1Redesigned.js
@src/config/subscriptionConfig.js

# Validator the plan must keep passing — do NOT modify, just satisfy

@scripts/validateTrail.mjs

<notes>
- LOCKED: Node IDs stay stable. Merged node keeps id `rhythm_1_1`. `rhythm_1_2` is DELETED. `rhythm_1_3`/`rhythm_1_4` keep their IDs. The numbering gap is acceptable and intentional.
- LOCKED: No database migration. Orphaned student progress rows on removed `rhythm_1_2` are harmless and left alone.
- LOCKED: Postgres `is_free_node()` mirror still listing `rhythm_1_2` is harmless and intentionally left alone (no-migration decision). Do NOT touch Supabase.
- Validator rule (validateTrail.mjs validateMixedLessons): for nodeType `discovery`, mixed_lesson question count must be 4-10. The merged node's ~8 questions (1 discovery_intro + 7 practice) is in range.
- Validator rule (validateGameTypePolicy): DISCOVERY nodes must use `mixed_lesson` exercise type — the merged node already does.
- `RENDERER_TYPES` valid question types: visual_recognition, syllable_matching, rhythm_tap, pulse, discovery_intro, rhythm_reading, rhythm_dictation.
- `rhythmUnit8Redesigned.test.js` references `rhythm_1_4` (unchanged) — not affected. Test fixtures referencing `rhythm_1_1` as a string id are unaffected since that id is preserved.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge rhythm_1_1 + rhythm_1_2 into one Discovery node and re-sequence Unit 1</name>
  <files>src/data/units/rhythmUnit1Redesigned.js</files>
  <action>
In `rhythmUnit1Nodes`, replace the two separate node objects (`rhythm_1_1` "Meet Quarter Notes" and `rhythm_1_2` "Practice Quarter Notes") with ONE merged node, then delete the old `rhythm_1_2` object entirely.

The merged node:

- `id: "rhythm_1_1"` (unchanged — keeps student progress)
- `name: "Quarter Notes"` (it now both introduces AND practices — drop "Meet"/"Practice" framing; a concise name like "Quarter Notes" fits the merged Discovery role)
- `description`: something that reflects discover + practice, e.g. "Discover and practice steady quarter notes"
- `category: CATEGORY`, `unit: UNIT_ID`, `unitName: UNIT_NAME`
- `order: START_ORDER` (100), `orderInUnit: 1`
- `prerequisites: []`
- `nodeType: NODE_TYPES.DISCOVERY`
- `rhythmConfig`: keep `complexity: RHYTHM_COMPLEXITY.SIMPLE`, `durations: ["q"]`, `focusDurations: ["q"]`, `contextDurations: []`, `patternTags: ["quarter-only"]`, `tempo: { min: 60, max: 75, default: 68 }` (blend the two prior tempo ranges), `pitch: "C4"`, `timeSignature: "4/4"`
- `newContent: NEW_CONTENT_TYPES.RHYTHM`, `newContentDescription: "Quarter Notes (1 beat)"`
- `exercises`: single `EXERCISE_TYPES.MIXED_LESSON` with a `config.questions` array of ~8 entries — opens with `{ type: "discovery_intro", focusDuration: "q" }` then a varied practice mix drawn from the two original nodes' question pools (syllable_matching, visual_recognition, rhythm_tap, rhythm_reading, rhythm_dictation). Suggested 8-question sequence:
  1. `{ type: "discovery_intro", focusDuration: "q" }`
  2. `{ type: "syllable_matching" }`
  3. `{ type: "visual_recognition" }`
  4. `{ type: "rhythm_tap" }`
  5. `{ type: "rhythm_reading" }`
  6. `{ type: "rhythm_dictation" }`
  7. `{ type: "rhythm_tap" }`
  8. `{ type: "visual_recognition" }`
     (Keep it 8; validator allows 4-10 for discovery. Do not add `pulse` unless desired — original 1_1 had it; including it is fine but optional. Use the 8 above.)
- `skills: ["quarter_note"]`
- `xpReward: 45` (the two originals were 40 + 45; pick 45 since this is now a richer single node — keeps Unit 1 XP roughly balanced)
- `accessoryUnlock: null`, `isBoss: false`, `isReview: false`, `reviewsUnits: []`

Update the file header comment block (lines ~1-14): it says "6 nodes" and "Quarter notes (1 beat), then Half notes" — change node count from 6 to 5, and adjust any "Practice Quarter Notes" mention. Update the "Psychological journey" comment if it enumerates the old node sequence.

Re-sequence the remaining nodes so `order` and `orderInUnit` are contiguous with NO gaps:

- merged `rhythm_1_1`: `order: START_ORDER` (100), `orderInUnit: 1`
- `rhythm_1_3`: `order: START_ORDER + 1` (101), `orderInUnit: 2`, and change `prerequisites` from `["rhythm_1_2"]` to `["rhythm_1_1"]`
- `rhythm_1_4`: `order: START_ORDER + 2` (102), `orderInUnit: 3` (prerequisites stay `["rhythm_1_3"]`)
- `rhythm_1_6`: `order: START_ORDER + 3` (103), `orderInUnit: 4` (prerequisites stay `["rhythm_1_4"]`)
- `boss_rhythm_1`: `order: START_ORDER + 4` (104), `orderInUnit: 5` (prerequisites stay `["rhythm_1_6"]`)

Update the section banner comments (`// NODE 1: ...` through `// NODE 6: ...`) to reflect the new 5-node numbering and the merged node's name.

Do NOT change node IDs `rhythm_1_3`, `rhythm_1_4`, `rhythm_1_6`, `boss_rhythm_1`. Do NOT change `rhythm_1_3`/`rhythm_1_4`/`rhythm_1_6`/`boss_rhythm_1` exercise content.
</action>
<verify>
<automated>npm run verify:trail</automated>
</verify>
<done>rhythmUnit1Nodes has exactly 5 entries; `rhythm_1_2` absent; merged `rhythm_1_1` is nodeType discovery with a mixed_lesson opening on discovery_intro followed by 7 practice questions; `rhythm_1_3.prerequisites` is `["rhythm_1_1"]`; order fields are 100,101,102,103,104 and orderInUnit 1-5; `npm run verify:trail` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove rhythm_1_2 from the free-tier node list</name>
  <files>src/config/subscriptionConfig.js</files>
  <action>
In `FREE_RHYTHM_NODE_IDS`, remove the `'rhythm_1_2'` entry. The array becomes:
```js
export const FREE_RHYTHM_NODE_IDS = [
  'rhythm_1_1',
  'rhythm_1_3',
  'rhythm_1_4',
  'rhythm_1_6',
];
```
Update the JSDoc comment above it from "Rhythm Unit 1 — 5 free nodes (rhythm_1_5 removed in Phase 32)" to "Rhythm Unit 1 — 4 free nodes (rhythm_1_5 removed Phase 32; rhythm_1_2 merged into rhythm_1_1)".

Update `FREE_TIER_SUMMARY`: change `rhythm: { count: 5 }` to `rhythm: { count: 4 }`, and change `total: 24` to `total: 23`.

Update the audited-source comment near the top (line ~12) that says "nodes rhythm_1_1 through rhythm_1_6" if you want precision — optional, but if touched, note rhythm_1_2 was merged.

Add a brief inline comment noting the Postgres `is_free_node()` mirror still listing `rhythm_1_2` is harmless and intentionally left alone per the no-migration decision. Do NOT modify any Supabase migration or call any Supabase tool.
</action>
<verify>
<automated>npm run test:run</automated>
</verify>
<done>`FREE_RHYTHM_NODE_IDS` has 4 entries with no `rhythm_1_2`; `FREE_TIER_SUMMARY.rhythm.count` is 4 and `total` is 23; `npm run test:run` passes.</done>
</task>

</tasks>

<verification>
- `npm run verify:trail` exits 0 (no missing prerequisites, no cycles, no mixed_lesson question-count errors, game-type policy OK).
- `npm run test:run` passes — including `rhythmUnit8Redesigned.test.js` (references unchanged `rhythm_1_4`).
- Grep confirms `rhythm_1_2` no longer appears in `src/data/units/rhythmUnit1Redesigned.js` or `src/config/subscriptionConfig.js`.
- Grep confirms `prerequisites: ["rhythm_1_1"]` on the `rhythm_1_3` node.
</verification>

<success_criteria>

- Rhythm Unit 1 contains 5 nodes; `rhythm_1_2` is fully removed.
- Merged `rhythm_1_1` is a Discovery node that both introduces (discovery_intro) and practices quarter notes (~8 questions total).
- `rhythm_1_3` prerequisite chain repaired to point at `rhythm_1_1`.
- All Unit 1 `order`/`orderInUnit` fields are contiguous (100-104 / 1-5).
- `FREE_RHYTHM_NODE_IDS` and `FREE_TIER_SUMMARY` updated; no Supabase migration created.
- `npm run verify:trail` and `npm run test:run` both pass.
  </success_criteria>

<output>
After completion, create `.planning/quick/260514-w1y-merge-rhythm-trail-nodes-1-1-and-1-2-int/260514-w1y-SUMMARY.md`
</output>
