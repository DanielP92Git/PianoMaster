---
phase: 33-rhythm-issues-cleanup
plan: 07
type: execute
wave: 4
depends_on: [33-02, 33-03, 33-04]
files_modified:
  - src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx
  - src/data/units/rhythmUnit8Redesigned.js
autonomous: false
requirements_addressed: [AUDIO-01, AUDIO-03]
contingent: true
gate: "UAT issue 1 OR UAT issue 4 marked confirmed-bug in 33-UAT.md (Wave 1 outcome). If both marked resolved-by-deploy or cannot-reproduce, this plan is SKIPPED per D-16 (limit hardening to renderers that reproduce a bug)."
must_haves:
  truths:
    - "If UAT issue 1 confirmed-bug: DiscoveryIntroQuestion.playDemo uses the shared useEnsureAudioReady hook from Plan 33-03 (D-13 + D-14 + D-16: hardening scope = only renderers that reproduce a bug)"
    - "If UAT issue 4 confirmed-bug for rhythm_3_1 specifically: 4-pair pitch-alternation branch in DiscoveryIntroQuestion plays without first-pair trim (covered by useEnsureAudioReady await-load-pianoSound)"
    - "If UAT issue 4 confirmed-bug for rhythm_8_1 specifically: focusDurations on rhythm_8_1 changed from ['8'] to ['8_pair'] OR DiscoveryIntroQuestion gains a mode flag (Discretion choice; RESEARCH Open Question 4 — defer-to-user)"
    - "D-14 (first-play trim investigation) and D-15 (eighths first-play; covers either case) honored"
    - "UAT issues 1 and/or 4 ready for resolved-by-deploy mark after retest"
  artifacts:
    - path: "src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx"
      provides: "playDemo integrated with useEnsureAudioReady (replaces inline initializeAudioContext/resumeAudioContext)"
      contains: "useEnsureAudioReady"
  key_links:
    - from: "src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx"
      to: "src/hooks/useEnsureAudioReady.js (created in Plan 33-03)"
      via: "import + ensureReady() call before playback scheduling"
      pattern: "useEnsureAudioReady"
---

<objective>
Apply the shared `useEnsureAudioReady` hook (Plan 33-03) to DiscoveryIntroQuestion if UAT confirms first-play trim still reproduces. Optionally fix rhythm_8_1's focusDurations drift if Issue 4 was confirmed for the Unit 8 path.

Purpose: D-14/D-15 hold first-play trim and eighths trim contingent on UAT confirmation. D-16 explicitly limits hardening rollout. RESEARCH §4 traces Issue 1 to the same `loadPianoSound` race that affects Issue 7 — applying useEnsureAudioReady covers it. RESEARCH Open Question 4 defers the rhythm_8_1 design choice (8 vs 8_pair vs new mode flag) to the user.

This plan is `autonomous: false` AND `contingent: true`. It DOES NOT FIRE unless Plan 33-02 (Wave 1 UAT) marks Issue 1 OR Issue 4 as confirmed-bug. Otherwise SKIP this plan entirely per D-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-rhythm-issues-cleanup/33-CONTEXT.md
@.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md
@.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md
@.planning/phases/33-rhythm-issues-cleanup/33-UAT.md
@src/hooks/useEnsureAudioReady.js
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm contingent fire + scope (Issue 1 only / Issue 4 only / both)</name>
  <decision>Should this plan execute, and if so, which subtasks fire?</decision>
  <context>
    Plan 33-07 fires only if Wave 1 UAT in `33-UAT.md` marked Issue 1 OR Issue 4 as `[x] confirmed-bug`. Per D-16, no preemptive hardening rollout.

    Read `.planning/phases/33-rhythm-issues-cleanup/33-UAT.md` to determine the marks. Three execution paths:

    - **Issue 1 confirmed only:** Run Task 2 (apply useEnsureAudioReady to DiscoveryIntroQuestion.playDemo). Skip rhythm_8_1 edit.
    - **Issue 4 confirmed only:** If user notes specify rhythm_3_1 (Unit 3) — run Task 2 (covers loadPianoSound race for the 8_pair branch). If user notes specify rhythm_8_1 (Unit 8) — run Task 3 (data edit only). If user is ambiguous about which unit — run Task 2 + Task 3.
    - **Both confirmed:** Run Task 2 + Task 3.

    For Task 3, RESEARCH Open Question 4 has two design options:
    - **Option A:** Change `rhythm_8_1.focusDurations` from `["8"]` to `["8_pair"]` — pure data edit; reuses existing 4-pair-with-pitch-alternation branch in DiscoveryIntroQuestion (lines 108-141). Lowest risk, smallest diff.
    - **Option B:** Add a `mode: 'pairs' | 'plain' | 'syncopated'` config flag to discovery_intro question and a corresponding branch in DiscoveryIntroQuestion. Higher complexity, future-proof.

    Per CONTEXT specifics ("ship-ready, don't gold-plate"), Option A is preferred. User confirms.

  </context>
  <options>
    <option id="skip">
      <name>SKIP — both Issue 1 and Issue 4 marked resolved-by-deploy / cannot-reproduce</name>
      <pros>D-16 honored; no unnecessary hardening; smallest Phase 33 scope</pros>
      <cons>None — this is the expected path if Phase 30/31/32 already shipped the fixes</cons>
    </option>
    <option id="task2-only">
      <name>Run Task 2 only — Issue 1 confirmed-bug; apply useEnsureAudioReady to DiscoveryIntroQuestion</name>
      <pros>Fixes the loadPianoSound race for the discovery_intro path; mirrors Plan 33-03's RhythmDictationQuestion fix</pros>
      <cons>None</cons>
    </option>
    <option id="task3-only">
      <name>Run Task 3 only — Issue 4 confirmed-bug for rhythm_8_1 specifically; change focusDurations to ['8_pair']</name>
      <pros>Single-line data edit; eliminates ambiguous "plain eighths in syncopation intro" pedagogy drift</pros>
      <cons>Doesn't address the audio-race trim if rhythm_3_1 also reproduces Issue 4</cons>
    </option>
    <option id="task2-and-task3">
      <name>Run Task 2 + Task 3 — both Issues 1 and 4 confirmed-bug; apply hook AND update rhythm_8_1 focusDurations</name>
      <pros>Closes both audio race and pedagogy drift in one plan</pros>
      <cons>Slightly larger diff but each piece is independently small</cons>
    </option>
  </options>
  <resume-signal>
    Reply with: skip / task2-only / task3-only / task2-and-task3
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply useEnsureAudioReady to DiscoveryIntroQuestion.playDemo (D-13 + D-14 integration)</name>
  <files>src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx</files>
  <read_first>
    - C:/Development/PianoApp2/src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx (full file — locate playDemo around line 59 per RESEARCH §4 Issue 1/4 trace; identify imports section)
    - C:/Development/PianoApp2/src/components/games/rhythm-games/renderers/RhythmDictationQuestion.jsx (post-Plan-33-03 — for the same integration pattern reference)
    - C:/Development/PianoApp2/src/hooks/useEnsureAudioReady.js (created in Plan 33-03)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-PATTERNS.md (§3 — DiscoveryIntroQuestion D-13/D-15 integration)
  </read_first>
  <action>
    **Skip this task if Task 1 decision was `skip` or `task3-only`.**

    **Step 2.1 — Add import.**

    At the top of DiscoveryIntroQuestion.jsx (alongside existing audio imports):
    ```javascript
    import { useEnsureAudioReady } from "../../../../hooks/useEnsureAudioReady";
    ```

    **Step 2.2 — Add hook invocation in component body.**

    After the existing `audioEngine = useAudioEngine(...)` and `getOrCreateAudioContext` declarations:
    ```javascript
    const ensureAudioReady = useEnsureAudioReady(audioEngine, getOrCreateAudioContext);
    ```

    **Step 2.3 — Replace playDemo prelude.**

    Locate `playDemo` around line 59. Per RESEARCH §4, the current implementation does:
    ```javascript
    const playDemo = useCallback(async () => {
      await audioEngine.initializeAudioContext();
      await audioEngine.resumeAudioContext();
      // ... build beats array, schedule playback
    });
    ```

    Replace the `initializeAudioContext + resumeAudioContext` lines with:
    ```javascript
    const ready = await ensureAudioReady();
    if (!ready) {
      // Audio pipeline not ready — surface silently. User can re-tap Listen.
      return;
    }
    // ... existing build beats array + schedule playback (UNCHANGED)
    ```

    Keep ALL the rest of playDemo unchanged: the `8_pair` branch (lines 108-141 per RESEARCH §3 Unit 3 confirmed shipped), the per-beat pitch-shift logic, the schedulePatternPlayback call.

    Drop `audioEngine` and `getOrCreateAudioContext` from the useCallback dependency array (now closed over by ensureAudioReady). Keep all other deps.

  </action>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEnsureAudioReady" src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx` returns at least 2 (import + invocation)
    - `grep -c "audioEngine.initializeAudioContext()" src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx` returns 0 (inline call replaced)
    - `npm run lint -- src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx` exits 0
    - Manual UAT: user replays UAT issue 1 (rhythm_1_1 quarter intro) — all 4 quarters audible on first Listen tap. Marks `[x] resolved-by-deploy`.
    - Manual UAT: user replays UAT issue 4 for rhythm_3_1 (Unit 3 8_pair) — all 4 pairs audible with hi-lo alternation. Marks `[x] resolved-by-deploy`.
  </acceptance_criteria>
  <done>DiscoveryIntroQuestion.playDemo uses ensureAudioReady; UAT issues 1 and (3_1 portion of) 4 ready for resolved-by-deploy</done>
</task>

<task type="auto">
  <name>Task 3: Change rhythm_8_1 focusDurations to ['8_pair'] (D-15 data fix path A)</name>
  <files>src/data/units/rhythmUnit8Redesigned.js</files>
  <read_first>
    - C:/Development/PianoApp2/src/data/units/rhythmUnit8Redesigned.js (locate rhythm_8_1 — first node in Unit 8 "Off-Beat Surprise"; per RESEARCH §3 Unit 8 line 233 it has `focusDuration: '8'` triggering the plain-eighths branch in DiscoveryIntroQuestion lines 116-118)
    - C:/Development/PianoApp2/src/components/games/rhythm-games/renderers/DiscoveryIntroQuestion.jsx (lines 108-141 — confirm 8_pair branch with 4-pair pitch-alternation)
    - C:/Development/PianoApp2/.planning/phases/33-rhythm-issues-cleanup/33-RESEARCH.md (§3 Unit 8 + §10 Open Question 4 + §1 Issue 4)
  </read_first>
  <action>
    **Skip this task if Task 1 decision was `skip` or `task2-only`.**

    **Per Task 1 decision being `task3-only` or `task2-and-task3`, AND Option A from RESEARCH Open Question 4:**

    Locate `rhythm_8_1` node in `src/data/units/rhythmUnit8Redesigned.js`. Find its `exercises[0].config.questions` array. Find the `discovery_intro` question entry. It currently has `focusDuration: "8"` (or similar — confirm by grep).

    Change `focusDuration` value from `"8"` to `"8_pair"`. This invokes the existing 4-pair-with-pitch-alternation branch in DiscoveryIntroQuestion (lines 108-141 per RESEARCH §3 Unit 3 confirmed shipped) instead of the plain-4-eighths branch (lines 116-118).

    Note: `rhythmConfig.focusDurations` array (top-level) may also list `"8"`. Per RESEARCH §3 Unit 8 line 233, the drift is in the `discovery_intro` question's `focusDuration` field, NOT necessarily the top-level `focusDurations` array. Confirm via inspection: edit ONLY the discovery_intro question's focusDuration field. The top-level `focusDurations` is for trail UI hints; leave it as-is unless it triggers downstream behavior (verify by grep on `focusDurations` in DiscoveryIntroQuestion).

    Add comment above the edit:
    ```javascript
    // D-15 (Phase 33 Plan 33-07): focusDuration '8_pair' invokes 4-pair pitch-alternation
    // branch in DiscoveryIntroQuestion (matching rhythm_3_1's pedagogy). Per RESEARCH
    // Open Question 4 Option A.
    ```

  </action>
  <verify>
    <automated>grep -A 30 'id: "rhythm_8_1"' src/data/units/rhythmUnit8Redesigned.js | grep -c '"8_pair"' && npm run verify:trail</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 30 'id: "rhythm_8_1"' src/data/units/rhythmUnit8Redesigned.js | grep -c '"8_pair"'` returns at least 1 (focusDuration updated to 8_pair within rhythm_8_1's discovery_intro question)
    - `npm run verify:trail` exits 0
    - `npm run lint -- src/data/units/rhythmUnit8Redesigned.js` exits 0
    - Manual UAT: user replays UAT issue 4 for rhythm_8_1 — Listen plays 4 beamed eighth pairs with hi-lo pitch alternation. Marks `[x] resolved-by-deploy`.
  </acceptance_criteria>
  <done>rhythm_8_1 discovery_intro now uses 8_pair branch; UAT issue 4 (Unit 8 portion) ready for resolved-by-deploy</done>
</task>

</tasks>

<verification>
- If Task 1 = skip: plan exits with no file changes; UAT issues 1 and 4 already marked resolved-by-deploy or cannot-reproduce.
- Otherwise: `npm run lint` and `npm run verify:trail` exit 0
- Manual UAT issues 1 and/or 4 (per Task 1 decision) ready for resolved-by-deploy after retest

Source-audit coverage:

- D-14 (first-play trim investigation): Task 2 (apply useEnsureAudioReady — RESEARCH §4 fix candidate (a))
- D-15 (eighths first-play, covers either case): Task 2 (audio race fix) + Task 3 (rhythm_8_1 data fix)
- D-16 (limit hardening to bug-reproducing renderers): honored by contingent gate (skip if no UAT confirmation)
- AUDIO-01 / AUDIO-03 (pending verification per REQUIREMENTS table — Phase 30 verdict was "Pending"; this plan closes them if UAT confirmed-bug)
  </verification>

<success_criteria>
Plan complete when:

1. Task 1 decision recorded
2. If task2 fired: DiscoveryIntroQuestion.playDemo uses ensureAudioReady; old initializeAudioContext call removed
3. If task3 fired: rhythm_8_1 discovery_intro focusDuration is "8_pair"; validateTrail green
4. UAT issues 1 and/or 4 (per Task 1 scope) marked resolved-by-deploy after retest
   </success_criteria>

<output>
After completion, create `.planning/phases/33-rhythm-issues-cleanup/33-07-SUMMARY.md` documenting:
- Task 1 decision (skip / task2-only / task3-only / task2-and-task3)
- Files modified per the decision
- UAT issues 1 and 4 mark status (per scope)
- Note: if both Tasks 2 and 3 ran, Issue 4 retest must cover BOTH rhythm_3_1 (Unit 3) and rhythm_8_1 (Unit 8) playthroughs
</output>
