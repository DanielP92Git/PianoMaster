---
phase: 02-refactor-score-vs-xp-unify-into-xp-only-scoring-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/xpSystem.js
  - src/services/achievementService.js
  - src/services/apiAccessories.js
  - src/utils/accessoryUnlocks.js
  - src/utils/points.js
  - src/hooks/useTotalPoints.js
  - src/services/scoreComparisonService.js
autonomous: true
requirements: [XP-SERVICE, XP-ACHIEVE, XP-DEAD-CODE]

must_haves:
  truths:
    - "Achievement rewards call awardXP() instead of updateUserPoints()"
    - "Free play games can earn XP via a new calculateFreePlayXP() function"
    - "Accessory unlock 'points_earned' type evaluates against total_xp instead of totalPoints"
    - "points.js, useTotalPoints.js, and scoreComparisonService.js are deleted"
    - "HIGH_SCORER achievement uses XP-based condition instead of total_points"
  artifacts:
    - path: "src/utils/xpSystem.js"
      provides: "calculateFreePlayXP function for non-trail games"
      exports: ["calculateFreePlayXP"]
    - path: "src/services/achievementService.js"
      provides: "XP-based achievement awarding"
      contains: "awardXP"
    - path: "src/utils/accessoryUnlocks.js"
      provides: "XP-based unlock checks"
      contains: "total_xp"
  key_links:
    - from: "src/services/achievementService.js"
      to: "src/utils/xpSystem.js"
      via: "import awardXP"
      pattern: "awardXP"
    - from: "src/services/apiAccessories.js"
      to: "students table"
      via: "total_xp query"
      pattern: "total_xp"
---

<objective>
Refactor the service layer to replace "points" with XP as the sole reward currency, add a free play XP calculation function, and delete dead code.

Purpose: Establish the XP-only foundation that all UI components will consume. Service layer must be solid before UI changes.
Output: Updated service files, new calculateFreePlayXP function, 3 dead files deleted.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/utils/xpSystem.js:
```javascript
export const calculateNodeXP = (stars, baseXP = 50, bonuses = {}) => { ... };
export const calculateSessionXP = (session) => { ... };
export const awardXP = async (studentId, xpAmount) => { ... };
export const getStudentXP = async (studentId) => { ... };
```

From src/services/achievementService.js:
```javascript
// Currently calls this.updateUserPoints(studentId, achievement.points) in awardAchievement()
// updateUserPoints writes to student_profiles.achievement_points column
// Need to replace with awardXP() from xpSystem.js
```

From src/services/apiAccessories.js:
```javascript
// Currently uses calculatePointsSummary from points.js for getUserPointBalance()
// Need to switch to reading students.total_xp directly
```

From src/utils/accessoryUnlocks.js:
```javascript
// checkAccessoryUnlock has case "points_earned" that checks userProgress.totalPoints
// Need to change to check userProgress.totalXP (or similar XP-based field)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calculateFreePlayXP to xpSystem and refactor achievementService</name>
  <files>src/utils/xpSystem.js, src/services/achievementService.js</files>
  <action>
1. In `src/utils/xpSystem.js`, add a new exported function `calculateFreePlayXP(scorePercentage, comebackMultiplier = 1)`:
   - Use a score-based formula (per user's discretion grant): base XP = 10 + Math.floor(scorePercentage * 0.4), so a 100% score gives 50 XP, 50% gives 30 XP, 0% gives 10 XP
   - Apply comebackMultiplier as final step (same pattern as calculateNodeXP)
   - This ensures free play always gives some XP to keep engagement, but less than trail nodes
   - Add it to the default export object

2. In `src/services/achievementService.js`:
   - Add `import { awardXP } from '../utils/xpSystem';` at top
   - In `awardAchievement()` method (line ~200): replace `await this.updateUserPoints(studentId, achievement.points);` with `await awardXP(studentId, achievement.points);`
   - Delete the entire `updateUserPoints()` method (lines 210-241)
   - Update the HIGH_SCORER achievement definition: change `condition: "total_points >= 1000"` to `condition: "total_xp >= 1000"` and update description to "Earn 1000 total XP"
   - In `calculateUserStats()`: rename `total_points` to `total_xp` in the stats object, calculate from student's total_xp via an additional DB query or keep the score sum (Claude's discretion — using score sum as a proxy is acceptable since we're transitioning)
   - In `checkAchievementCondition()`: update the `total_points >=` condition parser to `total_xp >=`
   - In `getAchievementProgress()`: update the `total_points >=` progress calculator to `total_xp >=`
   - The `points` field in ACHIEVEMENTS definitions stays as-is (it represents XP amount to award, the value semantics change from "points" to "XP" but the numbers are the same: 50-500 range)
  </action>
  <verify>
    <automated>npx vitest run src/utils/xpSystem.test.js 2>/dev/null; node -e "const x = require('./src/utils/xpSystem.js'); console.log('calculateFreePlayXP exists:', typeof x.calculateFreePlayXP === 'function')" 2>/dev/null || echo "Module check requires ESM - verify file exports manually"</automated>
  </verify>
  <done>
    - calculateFreePlayXP exported from xpSystem.js with score-based formula
    - achievementService calls awardXP instead of updateUserPoints
    - updateUserPoints method deleted from achievementService
    - HIGH_SCORER achievement uses total_xp condition
    - No remaining references to updateUserPoints in achievementService.js
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch accessory system to XP-based and delete dead code files</name>
  <files>src/utils/accessoryUnlocks.js, src/services/apiAccessories.js, src/utils/points.js, src/hooks/useTotalPoints.js, src/services/scoreComparisonService.js</files>
  <action>
1. In `src/utils/accessoryUnlocks.js`:
   - Rename the `case "points_earned"` to `case "xp_earned"` (keep "points_earned" as a fallback alias on the same case for backward compatibility with existing DB records)
   - Change the check from `userProgress.totalPoints` to `userProgress.totalXP`
   - Update the requirement string from "Earn X total points" to "Earn X total XP"

2. In `src/services/apiAccessories.js`:
   - Remove `import { calculatePointsSummary } from "../utils/points";`
   - In `getUserPointBalance()` function (around line 149): replace the calculatePointsSummary call with a direct query to `students` table for `total_xp`. The balance calculation becomes: query `students.total_xp` for the user, query the sum of `point_ledger.delta` for the user, return `{ earned: total_xp, ledgerDelta: sum_of_deltas, available: total_xp + sum_of_deltas }`
   - This means earned points = total XP, and spending (ledger deltas) still subtracts from the XP pool for accessory purchases

3. Delete the following files (they have no remaining consumers after the changes above):
   - `src/utils/points.js` — calculateGameplayPoints, calculateAchievementPoints, calculatePointsSummary are no longer needed
   - `src/hooks/useTotalPoints.js` — consumers will be removed in Plan 02 (Dashboard, useVictoryState)
   - `src/services/scoreComparisonService.js` — already unused after Phase 1 VictoryScreen redesign (useVictoryState still imports it but the percentile feature is non-critical and can be removed in Plan 02)

Note: Deleting useTotalPoints.js and scoreComparisonService.js will cause import errors in files that still reference them. This is intentional — Plan 02 will clean up those imports. The dead files must be removed first so Plan 02 executors know they're gone.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const gone = ['src/utils/points.js','src/hooks/useTotalPoints.js','src/services/scoreComparisonService.js']; const results = gone.map(f => ({file:f, deleted:!fs.existsSync(f)})); console.log(JSON.stringify(results, null, 2));"</automated>
  </verify>
  <done>
    - accessoryUnlocks.js uses totalXP instead of totalPoints for "xp_earned" (with "points_earned" fallback)
    - apiAccessories.js reads total_xp from students table instead of calculating from scores
    - points.js deleted
    - useTotalPoints.js deleted
    - scoreComparisonService.js deleted
  </done>
</task>

</tasks>

<verification>
- No file in this plan imports from the 3 deleted files
- achievementService.js has no reference to updateUserPoints
- accessoryUnlocks.js references totalXP not totalPoints
- xpSystem.js exports calculateFreePlayXP
</verification>

<success_criteria>
- Service layer uses XP exclusively for all reward operations
- Dead code files are removed from the repo
- Existing xpSystem.test.js still passes (if applicable)
- Build may have import errors in files not touched by this plan (expected, fixed in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-01-SUMMARY.md`
</output>
