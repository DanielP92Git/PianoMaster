---
phase: 02-refactor-score-vs-xp-unify-into-xp-only-scoring-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/apiTeacher.js
  - src/components/layout/TeacherDashboard.jsx
autonomous: true
gap_closure: true
requirements: [XP-TEACHER]

must_haves:
  truths:
    - "Teacher student list shows XP instead of Points in all labels and sort options"
    - "Teacher student detail modal shows Total XP instead of Total Points"
    - "Filter dropdown says XP Range instead of Points Range"
    - "Sort by XP works using total_xp from students table"
    - "Teacher chart components receive total_xp on student objects (fixing currently-broken XP display)"
    - "No calculateGameplayPoints stub or getAchievementPointsTotal import remains"
  artifacts:
    - path: "src/services/apiTeacher.js"
      provides: "Student objects with total_xp and current_level from students table"
      contains: "total_xp"
    - path: "src/components/layout/TeacherDashboard.jsx"
      provides: "XP-based teacher UI with no points references"
  key_links:
    - from: "src/services/apiTeacher.js"
      to: "students table"
      via: "select total_xp, current_level"
      pattern: "total_xp.*current_level"
    - from: "src/components/layout/TeacherDashboard.jsx"
      to: "student.total_xp"
      via: "display and filter/sort logic"
      pattern: "total_xp"
---

<objective>
Close the single remaining verification gap: TeacherDashboard.jsx and its data source (apiTeacher.js) still use "points" instead of "XP". This also fixes a latent bug where the 3 chart components (TopPerformersLeaderboard, ClassPerformanceChart, AnalyticsDashboard) reference `student.total_xp` but the data source provides `total_points`, resulting in charts showing 0 for all students.

Purpose: Complete the XP unification so no user-visible text says "points" anywhere in the app, including teacher views.
Output: Fully XP-based teacher student list, detail modal, filters, and sort -- plus correct data flowing to chart components.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-04-SUMMARY.md
@.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-VERIFICATION.md

<interfaces>
<!-- Data flow: apiTeacher.js builds student objects -> TeacherDashboard.jsx consumes them -->
<!-- Chart components (already migrated to XP in Plan 02-04) also consume these student objects -->

From src/utils/xpSystem.js (already used by chart components):
```javascript
export function calculateLevel(totalXP) // returns { level, title, ... }
```

From src/services/apiTeacher.js (current student object shape at line 286-312):
```javascript
// Current shape (BROKEN - provides total_points, charts expect total_xp):
const transformedStudent = {
  student_id: student.id,
  student_name: "...",
  total_points: scoresByStudent[student.id] || 0,  // <- REPLACE with total_xp
  current_streak: streaksByStudent[student.id] || 0,
  // ... other fields
};
```

From src/services/apiDatabase.js (line 468 - to be removed from TeacherDashboard imports):
```javascript
export async function getAchievementPointsTotal(studentId) // queries student_achievements.points (column being dropped)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch apiTeacher.js from points computation to total_xp from students table</name>
  <files>src/services/apiTeacher.js</files>
  <action>
Modify `getTeacherStudents()` in apiTeacher.js to use `total_xp` and `current_level` directly from the students table instead of computing points via RPC/fallback.

1. **Add total_xp, current_level to the students query** (line 116):
   Change the select from:
   `"id, first_name, last_name, email, username, level, studying_year, created_at"`
   to:
   `"id, first_name, last_name, email, username, level, studying_year, created_at, total_xp, current_level"`

2. **Remove the entire points computation block** (lines 158-249):
   Delete the `teacher_get_student_points` RPC call, the fallback `students_score`/`student_achievements` queries, and the `scoresByStudent` aggregation logic (the `pointsRows` variable, the RPC call, the fallback queries for gameScores/achievements, and the loops that populate `scoresByStudent`). Also remove the `scoresByStudent` declaration at line 204 and its population loops.

3. **Update the transformed student object** (line 295):
   Change `total_points: scoresByStudent[student.id] || 0` to:
   `total_xp: studentData.total_xp || 0,`
   `current_level: studentData.current_level || 1,`

   Note: `studentData` is already available in the `.map()` closure at line 126: `const studentData = studentsData?.find((s) => s.id === studentId) || {};`

4. **Keep everything else unchanged**: streaks, practice sessions, recordings, accuracy metrics all stay as-is.

This is a simplification -- instead of a complex RPC + fallback to compute aggregated points, we read the canonical `total_xp` directly from the students table where `awardXP()` already maintains it.
  </action>
  <verify>
    <automated>cd C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2 && grep -c "total_points\|teacher_get_student_points\|scoresByStudent\|pointsRows\|gameScores\|achievement.*points" src/services/apiTeacher.js; echo "Expected: 0 (no points references remain)"</automated>
  </verify>
  <done>apiTeacher.js getTeacherStudents returns student objects with total_xp and current_level instead of total_points. No points RPC or fallback computation remains. Chart components (TopPerformersLeaderboard, ClassPerformanceChart, AnalyticsDashboard) now receive correct total_xp values.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all points references in TeacherDashboard.jsx with XP</name>
  <files>src/components/layout/TeacherDashboard.jsx</files>
  <action>
Replace every "points" reference in TeacherDashboard.jsx with XP equivalents. There are 6 areas to update:

**A. Remove dead code (top of file):**
1. Remove `getAchievementPointsTotal` from the import at line 63 (keep `getStudentScores` if still used elsewhere in the file)
2. Delete the `calculateGameplayPoints` function (lines 68-73) and its TODO comment

**B. Student detail modal -- remove points computation (lines 1024-1043):**
1. Delete the `gameplayPoints` useMemo (lines 1024-1026) -- no longer needed
2. Delete the `studentAchievementPoints` useQuery (lines 1028-1038) -- queries a column being dropped
3. Delete the `achievementPoints` computation (lines 1040-1043)

**C. Student detail modal -- header stats (lines 1158-1174):**
Replace the "Total Points" stat card:
- Change label from "Total Points" to "Total XP"
- Change value from `(student.total_points || 0).toLocaleString()` to `(student.total_xp || 0).toLocaleString()`
- Replace the sub-text that shows "achievements / games" breakdown with XP level info. Use: `Level ${student.current_level || 1}` (simpler and more meaningful for teachers than the old breakdown)
- Remove references to `isStudentAchievementPointsLoading`, `achievementPoints`, `gameplayPoints` in this block

**D. Filter state and logic:**
1. Rename `pointsRange` to `xpRange` in the initial filter state (line 1492) and in `clearAllFilters` (line 1975)
2. Update the filter logic (lines 1756-1770): change `filters.pointsRange` to `filters.xpRange`, change `student.total_points` to `student.total_xp`, keep the same threshold values (1000, 500)
3. Update the filter UI (lines 2359-2375): change label from "Points" to "XP", change `filters.pointsRange` to `filters.xpRange`, change `handleFilterChange("pointsRange", ...)` to `handleFilterChange("xpRange", ...)`, change option labels from "All Points" to "All XP", "High (1000+)" stays same

**E. Sort logic:**
1. In the sort switch (lines 1789-1791): change case "points" to case "xp", change `a.total_points` to `a.total_xp`, same for `b.total_xp`
2. In the sort dropdown (lines 2232-2233): change `value="points-desc"` to `value="xp-desc"`, change `value="points-asc"` to `value="xp-asc"`, change labels from "Points High-Low" to "XP High-Low" and "Points Low-High" to "XP Low-High"

**F. Student list card (line 2555-2561):**
Change `student.total_points || 0` to `student.total_xp || 0` and change the "pts" suffix span to "XP"

After all changes, verify no "point" references remain (case-insensitive) except for CSS class names like `pointer-events-none` which are unrelated.
  </action>
  <verify>
    <automated>cd C:/Users/pagis/OneDrive/WebDev/Projects/MainPianoApp2/PianoApp2 && grep -in "point" src/components/layout/TeacherDashboard.jsx | grep -vi "pointer-events\|pointer\."; echo "Expected: no output (no points references remain except CSS pointer-events)"</automated>
  </verify>
  <done>TeacherDashboard.jsx shows "XP" everywhere instead of "Points". The calculateGameplayPoints stub and getAchievementPointsTotal import are removed. Filter and sort use xpRange/total_xp. Student detail modal shows "Total XP" with level info. Student list cards show XP instead of pts.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -rn "total_points" src/services/apiTeacher.js src/components/layout/TeacherDashboard.jsx` returns no matches
2. `grep -in "point" src/components/layout/TeacherDashboard.jsx | grep -vi "pointer-events"` returns no matches
3. `grep -n "total_xp" src/services/apiTeacher.js` confirms total_xp is selected from students table and assigned to student objects
4. `grep -n "total_xp" src/components/layout/TeacherDashboard.jsx` confirms XP is used in display, filter, and sort logic
5. `npm run build` succeeds (no import errors from removed getAchievementPointsTotal)
</verification>

<success_criteria>
- No "points" terminology visible in any teacher view (student list, student detail, filters, sort options)
- apiTeacher.js student objects include total_xp and current_level from students table
- Chart components (already migrated in 02-04) now receive correct total_xp data instead of undefined
- calculateGameplayPoints stub removed
- getAchievementPointsTotal import removed (function still exists in apiDatabase.js but is unused -- separate cleanup)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-05-SUMMARY.md`
</output>
