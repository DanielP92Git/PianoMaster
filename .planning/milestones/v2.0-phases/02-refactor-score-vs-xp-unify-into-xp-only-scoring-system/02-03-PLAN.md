---
phase: 02-refactor-score-vs-xp-unify-into-xp-only-scoring-system
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Avatars.jsx
  - src/pages/Achievements.jsx
  - src/components/ui/UnlockRequirementModal.jsx
  - src/components/ui/AccessoryUnlockModal.jsx
  - src/components/ui/Toast.jsx
  - src/locales/en/common.json
  - src/locales/he/common.json
autonomous: true
requirements: [XP-AVATARS, XP-ACHIEVEMENTS, XP-I18N]

must_haves:
  truths:
    - "Avatars page shows XP balance instead of points balance for accessory purchases"
    - "Achievements page shows XP totals instead of points totals"
    - "UnlockRequirementModal shows XP thresholds instead of points thresholds"
    - "All 'points' i18n keys are replaced with XP equivalents in both en and he"
    - "No user-visible text says 'points' — only 'XP'"
  artifacts:
    - path: "src/components/Avatars.jsx"
      provides: "XP-based accessory unlock and purchase UI"
      contains: "totalXP"
    - path: "src/pages/Achievements.jsx"
      provides: "XP-based achievement display"
      contains: "XP"
    - path: "src/locales/en/common.json"
      provides: "English translations with XP instead of points"
    - path: "src/locales/he/common.json"
      provides: "Hebrew translations with XP instead of points"
  key_links:
    - from: "src/components/Avatars.jsx"
      to: "src/hooks/useAccessories.js"
      via: "usePointBalance returns XP-based balance"
      pattern: "pointsBalance"
    - from: "src/pages/Achievements.jsx"
      to: "achievement display"
      via: "XP reward text"
      pattern: "XP"
---

<objective>
Update all user-facing UI components to display XP instead of points, and update both English and Hebrew i18n files to remove all "points" language.

Purpose: Complete the user-facing transition so the child sees "XP" everywhere, never "points". This plan runs parallel to Plan 02 since it touches different files.
Output: Avatars, Achievements, unlock modals, toasts, and i18n all use XP language.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-CONTEXT.md
@.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01 outputs that this plan consumes -->

From src/services/apiAccessories.js (after Plan 01):
```javascript
// getUserPointBalance now returns { earned: total_xp, ledgerDelta, available: total_xp + ledgerDelta }
// The "earned" field is the student's total_xp from the students table
```

From src/utils/accessoryUnlocks.js (after Plan 01):
```javascript
// case "xp_earned" (with "points_earned" fallback)
// checks userProgress.totalXP instead of userProgress.totalPoints
```

From src/services/achievementService.js (after Plan 01):
```javascript
// ACHIEVEMENTS definitions still have `points: N` field — this is now the XP reward amount
// HIGH_SCORER now has condition: "total_xp >= 1000"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Avatars and Achievements pages to display XP</name>
  <files>src/components/Avatars.jsx, src/pages/Achievements.jsx</files>
  <action>
1. **Avatars.jsx**:
   - The `usePointBalance()` hook returns XP-based data now (Plan 01 changed apiAccessories). Variable names can stay as `pointsBalance` internally (renaming all references is optional cosmetic change), but user-facing labels must change.
   - Line 163-165: Rename variables for clarity:
     - `availablePoints` -> `availableXP` (or keep internal name, just change display)
     - `spentPoints` -> keep as is (spending from XP pool)
     - `earnedPoints` -> `earnedXP`
   - Line 176: In the `currentProgress` object, change `totalPoints: pointsBalance?.earned || 0` to `totalXP: pointsBalance?.earned || 0`
   - Update all i18n key references from points terminology to XP terminology:
     - `t("avatars.availablePoints", ...)` -> `t("avatars.availableXP", ...)`
     - `t("avatars.pointsSpent", ...)` -> `t("avatars.xpSpent", ...)`
     - `t("avatars.purchase", ...)` -> update to use XP
     - `t("avatars.notEnoughPoints")` -> `t("avatars.notEnoughXP")`
   - Line 572: Remove `queryClient.invalidateQueries(["total-points", user?.id])` — this query key no longer exists
   - The `subtitle` text referencing "points" should use the i18n key (which will be updated in Task 2)

2. **Achievements.jsx**:
   - Remove `import { calculatePointsSummary } from "../utils/points";` (line 15)
   - The file currently calculates `totalPoints`, `gameplayPoints`, `achievementPoints` from `calculatePointsSummary` (lines 117-118). Replace this with a simpler XP-based display:
     - Query the student's `total_xp` from the existing scores data or add a simple query
     - Or better: use the achievements data directly. Show the sum of XP rewards from earned achievements, and the student's total XP level
     - Replace the points summary display (line ~170-178) with XP total display: show total XP and XP level instead of gameplay+achievement points breakdown
   - Update the category icon mapping: change `points: Star` (line 21) to something XP-themed, or remove the "points" category since HIGH_SCORER is now in the same category but XP-based
   - Update achievement reward display: change `t("pages.achievements.pointsReward", { points: ... })` to `t("pages.achievements.xpReward", { xp: ... })` (lines ~236-237, ~325-326)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const av = fs.readFileSync('src/components/Avatars.jsx','utf8'); const ach = fs.readFileSync('src/pages/Achievements.jsx','utf8'); console.log(JSON.stringify({avatars_no_totalPoints_query: !av.includes('total-points'), achievements_no_pointsSummary: !ach.includes('calculatePointsSummary'), achievements_no_points_import: !ach.includes('points.js')}, null, 2));"</automated>
  </verify>
  <done>
    - Avatars page displays XP balance for accessory purchases
    - Avatars currentProgress uses totalXP
    - Achievements page shows XP rewards instead of points
    - No import of calculatePointsSummary in Achievements
    - No "total-points" query invalidation in Avatars
  </done>
</task>

<task type="auto">
  <name>Task 2: Update unlock modals, toast, and i18n files (en + he)</name>
  <files>src/components/ui/UnlockRequirementModal.jsx, src/components/ui/AccessoryUnlockModal.jsx, src/components/ui/Toast.jsx, src/locales/en/common.json, src/locales/he/common.json</files>
  <action>
1. **UnlockRequirementModal.jsx** (line ~91):
   - The `case "points_earned"` switch block: add `case "xp_earned":` before it (fallthrough)
   - Change `userProgress.totalPoints` to `userProgress.totalXP`
   - Update i18n key references from pointsEarned/pointsEarnedDescription to xpEarned/xpEarnedDescription

2. **AccessoryUnlockModal.jsx** (line ~336):
   - Same pattern: add `case "xp_earned":` as fallthrough before `case "points_earned":`
   - Update display text to say XP instead of points

3. **Toast.jsx** (line ~303):
   - Change `+{points} points earned!` to `+{points} XP earned!` (or use i18n key)

4. **i18n English (src/locales/en/common.json)** — update the following keys:
   - `common.points` -> `"XP"` (or remove if unused)
   - `pages.achievements.points` -> `"Total XP"` (was "Total Points")
   - `pages.achievements.pointsBreakdown` -> remove or change to XP description
   - `pages.achievements.pointsReward` -> rename to `xpReward`: `"+{{xp}} XP"`
   - `avatars.subtitle` -> change "points" to "XP"
   - `avatars.availablePoints` -> rename to `availableXP`: `"{{xp}} XP available"`
   - `avatars.pointsSpent` -> rename to `xpSpent`: `"{{xp}} XP spent on accessories"`
   - `avatars.purchase` -> `"Buy for {{xp}} XP"`
   - `avatars.notEnoughPoints` -> rename to `notEnoughXP`: `"Not enough XP"`
   - `avatars.unlockModal.pointsEarned` -> rename to `xpEarned`: `"XP Earned"`
   - `avatars.unlockModal.pointsEarnedDescription` -> rename to `xpEarnedDescription`: `"Earn {{amount}} total XP (lifetime)"`
   - `avatars.unlockModal.pointsEarnedShort` -> `xpEarnedShort`: `"{{amount}} XP total"`
   - `avatars.unlockModal.pointsEarnedTipDetailed` -> `xpEarnedTipDetailed`: `"Earn {{amount}} more XP to unlock. This counts your total lifetime XP earned, not just what's available to spend."`
   - `avatars.unlockModal.pointsEarnedCompleted` -> `xpEarnedCompleted`: `"You've earned {{amount}} total XP!"`
   - `victory.pointsEarnedLine` -> remove (replaced by xpEarned in VictoryScreen)
   - `victory.totalPoints` -> remove
   - `victory.points` -> remove
   - `achievement.categories.points` -> rename to `"XP"` or `"Progress"`
   - `achievement.descriptions.high_scorer.description` -> `"Earn 1000 total XP"`
   - Keep old keys as-is if they might be referenced from DB records (the AccessoryUnlockModal case), or add both old and new keys

5. **i18n Hebrew (src/locales/he/common.json)** — mirror all changes from English:
   - Same key renames, Hebrew translations for XP-related text
   - XP in Hebrew: use "XP" (it's a universal gaming term, no need to translate)
   - "נקודות" (points) -> "XP" in all relevant keys
   - `"availableXP": "{{xp}} XP זמינים"`
   - `"xpSpent": "{{xp}} XP הושקעו באביזרים"`
   - `"purchase": "קנו ב-{{xp}} XP"`
   - `"notEnoughXP": "אין מספיק XP"`
   - etc. (mirror all English changes)

NOTE: When updating i18n files, keep old keys alongside new ones temporarily for backward compatibility (any DB-stored unlock_requirement types that say "points_earned" might generate UI text through old keys). Mark old keys with a comment if JSON supports it (it doesn't — just keep them for now, cleanup later).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const en = fs.readFileSync('src/locales/en/common.json','utf8'); const he = fs.readFileSync('src/locales/he/common.json','utf8'); console.log(JSON.stringify({en_has_xpEarned: en.includes('xpEarned') || en.includes('xpReward'), en_has_availableXP: en.includes('availableXP') || en.includes('notEnoughXP'), he_has_xp_terms: he.includes('XP')}, null, 2));"</automated>
  </verify>
  <done>
    - UnlockRequirementModal handles both "xp_earned" and "points_earned" types
    - AccessoryUnlockModal handles both types
    - Toast shows XP instead of points
    - English i18n has XP keys for all formerly points-based text
    - Hebrew i18n mirrors English XP changes
    - No user-visible text says "points" where it should say "XP"
  </done>
</task>

</tasks>

<verification>
- grep -r "pointsEarnedLine" src/locales/ — should be absent or deprecated
- grep -r "notEnoughPoints" src/components/ — should use notEnoughXP
- i18n files parse as valid JSON (no syntax errors from manual editing)
- Avatars page functionally works with XP balance
</verification>

<success_criteria>
- Child sees "XP" everywhere, never "points" in the UI
- Accessory purchase system works with XP as currency
- Achievement rewards display as XP
- Both English and Hebrew translations updated
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactor-score-vs-xp-unify-into-xp-only-scoring-system/02-03-SUMMARY.md`
</output>
