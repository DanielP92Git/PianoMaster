---
phase: 36-game-screen-ui-unification
plan: 05
type: execute
wave: 4
depends_on: ["36-03"]
files_modified:
  - src/components/games/rhythm-games/RhythmReadingGame.jsx
  - src/components/games/rhythm-games/RhythmDictationGame.jsx
autonomous: true
requirements: [REQ-02, REQ-06, REQ-07]
must_haves:
  truths:
    - "RhythmReadingGame renders the shared ProgressBar (session exercise N of M) and shared ScorePill"
    - "RhythmDictationGame renders the shared ProgressBar (question N of 10) and shared ScorePill (correct count)"
    - "Both keep their landscape-lock declaration and staff/choice-card mechanics unchanged; no lives/combo/on-fire added (D-02)"
  artifacts:
    - path: "src/components/games/rhythm-games/RhythmReadingGame.jsx"
      provides: "Base-shell adoption for rhythm reading"
      contains: "shared/hud/ProgressBar"
    - path: "src/components/games/rhythm-games/RhythmDictationGame.jsx"
      provides: "Base-shell adoption for rhythm dictation"
      contains: "shared/hud/ProgressBar"
  key_links:
    - from: "src/components/games/rhythm-games/RhythmReadingGame.jsx"
      to: "src/components/games/shared/hud/ProgressBar.jsx"
      via: "named import + session counters"
      pattern: "from \"\\.\\./shared/hud/ProgressBar\""
    - from: "src/components/games/rhythm-games/RhythmDictationGame.jsx"
      to: "src/components/games/shared/hud/ScorePill.jsx"
      via: "named import + correct count"
      pattern: "from \"\\.\\./shared/hud/ScorePill\""
---

<objective>
Wave 2 (D-01) base-shell rollout, cluster B: RhythmReadingGame and RhythmDictationGame adopt the
shared `ProgressBar` and `ScorePill`. These are staff/choice-card rhythm games — base shell only,
no engagement layer (D-02). Their existing landscape-lock (`useDeclareNeedsLandscape`) and staff
scroll / VexFlow choice-card mechanics must be untouched (REQ-06).

Purpose: Bring the two rhythm-reading games up to the base-shell standard.
Output: Both games consuming shared ProgressBar + ScorePill; landscape + mechanics preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-game-screen-ui-unification/36-PATTERNS.md
@.planning/phases/36-game-screen-ui-unification/36-RESEARCH.md
@.planning/phases/36-game-screen-ui-unification/36-UI-SPEC.md
@CLAUDE.md

<interfaces>
Shared components (from Plan 36-02): `ProgressBar({current,total})`, `ScorePill({value,label,...})`.
Import path from `rhythm-games/`: `../shared/hud/X`.

Per-game wiring (from 36-RESEARCH.md — confirm exact names in each file):

- RhythmReadingGame: ProgressBar current=`currentExercise` (0-indexed), total=`TOTAL_EXERCISES`
  constant from session config; ScorePill value = aggregate from `exerciseScores` (running total
  or mean — match what the file already computes), label="Score". The within-pattern staff
  scroll `progressFraction` is a DIFFERENT concept — leave it alone.
- RhythmDictationGame: ProgressBar current=`currentQuestion` (0-indexed; the file shows
  `currentQuestion+1`/`TOTAL_QUESTIONS` text), total=`TOTAL_QUESTIONS` (=10); ScorePill value =
  `questionScores.filter(s => s === 1).length`, label="Score".
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: RhythmReadingGame adopts shared ProgressBar + ScorePill</name>
  <read_first>
    - src/components/games/rhythm-games/RhythmReadingGame.jsx — read the HUD/header region and confirm `currentExercise`, the total-exercises constant, and the `exerciseScores` aggregation; also confirm the `useDeclareNeedsLandscape(...)` call location (it must stay).
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "RhythmReadingGame" section (lines ~436–450).
    - .planning/phases/36-game-screen-ui-unification/36-PATTERNS.md — Wave 2 common pattern + state table (lines ~640–660).
  </read_first>
  <action>
Edit `src/components/games/rhythm-games/RhythmReadingGame.jsx`:
1. Add imports:
```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```
2. Add `<ProgressBar current={currentExercise} total={TOTAL_EXERCISES} />` to the session HUD
   (use the real total constant name from the file). Keep the existing staff scroll
   `progressFraction` rendering — it is the within-measure scroll, not session progress.
3. Add `<ScorePill value={<aggregate score>} label={t("games.score")} />` using the score
   expression the file already computes from `exerciseScores`. If `t("games.score")` is missing
   in either locale, add it to both in lockstep (REQ-07).
4. Do NOT add lives/combo/on-fire. Do NOT move or remove `useDeclareNeedsLandscape(...)`.
  </action>
  <acceptance_criteria>
    - RhythmReadingGame.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`.
    - RhythmReadingGame.jsx renders `<ProgressBar current={` and `<ScorePill value={`.
    - RhythmReadingGame.jsx STILL contains `useDeclareNeedsLandscape(` (landscape lock preserved).
    - Locale parity if a key was added.
    - `npx vitest run src/components/games/rhythm-games/RhythmReadingGame` exits 0 (or the rhythm-games dir if no dedicated test).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/RhythmReadingGame</automated>
  </verify>
  <done>RhythmReadingGame consumes shared base shell; staff scroll + landscape lock intact; no engagement added.</done>
</task>

<task type="auto">
  <name>Task 2: RhythmDictationGame adopts shared ProgressBar + ScorePill</name>
  <read_first>
    - src/components/games/rhythm-games/RhythmDictationGame.jsx — read the right-column HUD region (RESEARCH cites lines ~734–769) and confirm `currentQuestion`, `TOTAL_QUESTIONS`, `questionScores`; confirm `useDeclareNeedsLandscape(...)` location.
    - .planning/phases/36-game-screen-ui-unification/36-RESEARCH.md — "RhythmDictationGame" section (lines ~452–462).
  </read_first>
  <action>
Edit `src/components/games/rhythm-games/RhythmDictationGame.jsx`:
1. Add imports:
```jsx
import { ProgressBar } from "../shared/hud/ProgressBar";
import { ScorePill } from "../shared/hud/ScorePill";
```
2. Replace the inline progress text `{currentQuestion+1}/{TOTAL_QUESTIONS}` with
   `<ProgressBar current={currentQuestion} total={TOTAL_QUESTIONS} />` (ProgressBar renders the
   `current+1` counter internally, so pass the 0-indexed `currentQuestion`).
3. Replace the inline score counter with
   `<ScorePill value={questionScores.filter((s) => s === 1).length} label={t("games.score")} />`.
   Add `games.score` to both locales in lockstep if missing (REQ-07).
4. Do NOT add lives/combo/on-fire. Do NOT touch the VexFlow choice-card rendering. Do NOT move or
   remove `useDeclareNeedsLandscape(...)`.
  </action>
  <acceptance_criteria>
    - RhythmDictationGame.jsx contains `from "../shared/hud/ProgressBar"` and `from "../shared/hud/ScorePill"`.
    - RhythmDictationGame.jsx renders `<ProgressBar current={currentQuestion}` and `<ScorePill value={questionScores`.
    - The old `{currentQuestion+1}/{TOTAL_QUESTIONS}` inline text block is removed from the HUD region.
    - RhythmDictationGame.jsx STILL contains `useDeclareNeedsLandscape(`.
    - `npx vitest run src/components/games/rhythm-games/RhythmDictationGame` exits 0 (or rhythm-games dir).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/games/rhythm-games/RhythmDictationGame</automated>
  </verify>
  <done>RhythmDictationGame consumes shared base shell; choice-card mechanics + landscape lock intact; no engagement added.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary — presentational HUD adoption fed by each game's existing session state.

## STRIDE Threat Register

| Threat ID | Category | Component                            | Disposition | Mitigation Plan                                                                                                                           |
| --------- | -------- | ------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-05   | N/A      | RhythmReading/Dictation HUD adoption | accept      | No new attack surface — presentational component swap; no new user input, network, or PII. Landscape-lock declaration preserved (REQ-06). |

</threat_model>

<verification>
- `npx vitest run src/components/games/rhythm-games/RhythmReadingGame src/components/games/rhythm-games/RhythmDictationGame` → green.
- `npm run lint` → no new errors.
- `useDeclareNeedsLandscape(` present in both files (grep).
- MANUAL (folded into 36-11): both games show shared progress bar + score pill; landscape prompt
  still triggers on phones; RTL + reduced-motion correct.
</verification>

<success_criteria>
RhythmReadingGame and RhythmDictationGame consume the shared base shell; inline progress/score
text removed; landscape lock and mechanics preserved; tests green; locale parity intact.
</success_criteria>

<output>
After completion, create `.planning/phases/36-game-screen-ui-unification/36-05-SUMMARY.md`.
</output>
