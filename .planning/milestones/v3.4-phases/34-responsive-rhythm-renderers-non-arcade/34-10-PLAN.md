---
phase: 34-responsive-rhythm-renderers-non-arcade
plan: 10
type: execute
wave: 6
depends_on: ["34-07", "34-08", "34-09"]
files_modified:
  - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md
autonomous: false
gap_closure: true
requirements:
  - INFRA-04
  - CORE-02
  - CORE-04
  - CORE-05
  - NOTATION-01
  - NOTATION-02
  - WRAPPER-02
  - WRAPPER-03
  - TABLET-01
tags:
  - rhythm
  - responsive
  - uat-delta
  - gap-closure
  - manual-verification
must_haves:
  truths:
    - "Pre-UAT-delta automated gates re-run (test:run, build, lint Phase 34 scope) and recorded green before delta walkthrough"
    - "Each previously-failing row in 34-UAT.md gets a new RESULT entry under a `## UAT Delta (Gap Closure)` section — original row entries are preserved unchanged"
    - "GAP 1: SC #1 free-play row 30-34 retested at iPhone SE portrait — should now pass post-Plan-07"
    - "GAP 2: SC #2 long-pattern rows (reading-long, tap-long, mixed-lesson swap rows) retested using ?measures=4 dev URL param post-Plan-08"
    - "GAP 2: SC #3 long-pattern tablet rows (rhythm-reading-game (long), rhythm-tap-game (long)) retested using ?measures=4 at tablet quadrants — overlay must NOT appear"
    - "GAP 3: SC #5 RhythmGameSetup + RhythmGameSettings rows updated with classification result from Plan 09 (PASS post-fix / DEFERRED with note / RECLASSIFIED visual-only)"
    - "Newly-affected rows: SC #1 trail entry to MixedLessonGame dictation re-verified to confirm Plan 07 wrapper change did NOT regress trail entry"
    - "Final UAT sign-off section updated with delta status — phase ship gate passes if all delta rows pass"
  artifacts:
    - path: .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md
      provides: "UAT delta walkthrough log — gap-closure re-test results appended below original Plan-06 entries"
      contains: "UAT Delta"
  key_links:
    - from: .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md
      to: ROADMAP Phase 34 Success Criteria
      via: "Each delta row references the SC number it re-validates"
      pattern: "SC #"
---

<objective>
Re-walk ONLY the failed rows from 34-UAT.md (plus newly-affected rows that Plans 07-09 might have impacted) on real device + DevTools per D-12. Append results as a `## UAT Delta (Gap Closure)` section to the existing 34-UAT.md — keep original Plan-06 row entries intact for traceability. Final sign-off at the bottom of the delta section.

Failed rows to re-test (from gap-closure-input):

- **GAP 1** → SC #1 row 30-34 (free-play dictation 2x2 grid)
- **GAP 2** → SC #2 reading-long, tap-long, mixed-lesson swap rows (3-4 sub-rows)
- **GAP 2** → SC #3 rhythm-reading-game (long), rhythm-tap-game (long) tablet-portrait + tablet-landscape rows (4 cells)
- **GAP 3** → SC #5 RhythmGameSetup + RhythmGameSettings rows (8 cells: 2 components × 4 quadrants)

Newly-affected rows (might regress due to Plans 07-09):

- SC #1 trail-entry mixed-lesson dictation (Plan 07 changed the standalone wrapper but NOT the renderer; verify trail still passes — should be unchanged)
- SC #5 MetronomeTrainer setup-phase rendering at all 4 quadrants (Plan 09 may have applied wrapper-level fix or none)

Purpose: Phase 34 ship gate. If all delta rows pass, the phase closes via `/gsd-progress`. If anything still fails, route back to a follow-up gap-closure plan.
Output: 1 MODIFIED file (34-UAT.md, with appended delta section). No new code files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-CONTEXT.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-07-PLAN.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-08-PLAN.md
@.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run pre-UAT automated gate post-gap-closure</name>
  <files>.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md</files>
  <read_first>
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md (full file — to find the existing "Pre-UAT Gate" section format and mirror it for the delta)
  </read_first>
  <action>
    Run the three automated gates and capture results:

    1. `npm run test:run` — full Vitest run; record duration + tests-passed count
    2. `npm run build` — production build; record success + duration
    3. `npm run lint` — Phase 34 scope (rhythm-games + utils); record errors and pre-existing warnings count

    **Append** (do NOT replace existing Pre-UAT Gate section) a new section to `34-UAT.md` immediately after the existing "Deferred Items Discovered During UAT" section and BEFORE the existing "UAT Sign-Off" section:

    ```markdown
    ## UAT Delta (Gap Closure) — Pre-Delta Gate

    Re-run after Plans 34-07, 34-08, 34-09 shipped (gap-closure for original UAT failures).

    | Command | Status |
    |---|---|
    | `npm run test:run` | {pass/fail} — {N}/{N} tests, {duration} |
    | `npm run build` | {pass/fail} — `built in {time}` |
    | `npm run lint` (Phase 34 scope) | {pass/fail} — {N} errors, {N} pre-existing warnings |

    All three MUST be green before delta walkthrough begins. If any fail, STOP and re-open the relevant gap-closure plan.
    ```

    Replace `{...}` placeholders with actual values.

    Commit message: `chore(34-10): record pre-delta-UAT automated gate results`.

  </action>
  <verify>
    <automated>npm run test:run && npm run build && npm run lint</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md','utf8');if(!/UAT Delta \(Gap Closure\) — Pre-Delta Gate/.test(t)){console.error('Pre-Delta Gate section not appended');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` exit 0
    - `npm run build` exit 0
    - `npm run lint` (Phase 34 scope) exit 0
    - 34-UAT.md contains literal `## UAT Delta (Gap Closure) — Pre-Delta Gate` heading
    - The new section is placed after "Deferred Items Discovered During UAT" and before the original "UAT Sign-Off" section
    - All three command results are filled in (no `{pass/fail}` placeholders remain)
  </acceptance_criteria>
  <done>Three automated gates green; 34-UAT.md has Pre-Delta Gate section recording results; ready for human delta walkthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Append delta walkthrough skeleton to 34-UAT.md</name>
  <files>.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md</files>
  <read_first>
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md (read the existing structure — preserve original entries; append-only edit)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-07-PLAN.md (GAP 1 fix scope — what to retest)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-08-PLAN.md (GAP 2 fix scope — ?measures URL param usage)
    - .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-09-PLAN.md (GAP 3 fix scope — wrapper fix or deferred)
  </read_first>
  <action>
    Append a new section IMMEDIATELY AFTER the "Pre-Delta Gate" section from Task 1, and BEFORE the original "UAT Sign-Off" section. Use this exact skeleton — the user (human verifier in Task 3) will fill in the cells:

    ```markdown
    ## UAT Delta (Gap Closure) — Walkthrough

    **Verified:** {YYYY-MM-DD when walkthrough runs}
    **Devices used:** iPhone SE (375×667) — DevTools emulator / real device; iPad (1024×768) — DevTools emulator / real device

    Re-tests ONLY the rows that failed in the original UAT (rows 30-34, SC #2 long-pattern rows, SC #3 long-pattern tablet rows, SC #5 RhythmGameSetup + RhythmGameSettings rows) plus newly-affected rows from gap-closure plans.

    ### Delta SC #1 — GAP 1: free-play dictation 2x2 grid (Plan 07)

    Route: `/rhythm-mode/rhythm-dictation-game` (free-play entry — NO trail nav state)
    Device: iPhone SE 375×667 portrait

    Steps:
    1. Open route in free-play (NOT via trail). Wait for first prompt.
    2. Visually confirm: 1 staff at top, 3 cards in 2×2 grid (first 2 cards row 1; third card spans both columns row 2)
    3. Confirm no vertical scroll
    4. Tap each card to confirm tappable; the col-span-2 third card MUST be fully tappable across its full width

    | Field | Value |
    |---|---|
    | Result | ☐ pass / ☐ fail |
    | Notes | |

    ### Delta SC #1 — Regression: trail-entry mixed-lesson dictation (no Plan-07 impact expected)

    Plan 07 modified `RhythmDictationGame.jsx` (the wrapper) but NOT `RhythmDictationQuestion.jsx` (the renderer used by MixedLessonGame). Verify trail entry still passes the original 2x2 grid test (no regression).

    Route: navigate from Trail → mixed-lesson node containing a Dictation question
    Device: iPhone SE 375×667 portrait

    | Field | Value |
    |---|---|
    | Result | ☐ pass / ☐ fail |
    | Notes | |

    ### Delta SC #2 — GAP 2: long-pattern rows (Plan 08, ?measures=4 URL param)

    Use the dev-only URL param to force long patterns:
    - Short → no `?measures` param OR `?measures=1`
    - Long → `?measures=4`

    Devices: phone-portrait (iPhone SE 375×667)

    | Sub-test | Result | Notes |
    |---|---|---|
    | reading short (no param) → no prompt | ☐ pass / ☐ fail | |
    | reading long (`?measures=4`) → prompt | ☐ pass / ☐ fail | |
    | tap short (no param) → no prompt | ☐ pass / ☐ fail | "tap" is via reading game per existing UAT note |
    | tap long (`?measures=4`) → prompt | ☐ pass / ☐ fail | |
    | mixed-lesson swap (long→short) → prompt clears | ☐ pass / ☐ fail | mixed-lesson uses trail node config; swap behavior should still work without ?measures |
    | mixed-lesson swap (short→long) → prompt appears | ☐ pass / ☐ fail | |

    Note: mixed-lesson rows in the original UAT were marked "fail" only because the user could not control pattern length from free-play; mixed-lesson swap behavior is exercised by navigating between trail nodes with different measure counts. If this is impractical, mark "skipped — trail node content does not exhibit the swap" and continue.

    ### Delta SC #3 — GAP 2: long-pattern tablet rows (Plan 08)

    Same `?measures=4` URL param. Tablet quadrants must NEVER show overlay regardless of pattern length.

    Devices: iPad 768×1024 portrait + 1024×768 landscape

    | Route | tablet-portrait | tablet-landscape |
    |---|---|---|
    | rhythm-reading-game (long, `?measures=4`) | ☐ pass / ☐ fail | ☐ pass / ☐ fail |
    | rhythm-tap-game (long, via reading game `?measures=4`) | ☐ pass / ☐ fail | ☐ pass / ☐ fail |

    ### Delta SC #5 — GAP 3: RhythmGameSetup + RhythmGameSettings (Plan 09)

    Route: `/rhythm-mode/metronome-trainer` (the only route that surfaces RhythmGameSetup)

    Per Plan 09 classification result (record in Plan 09 SUMMARY):
    - **Class A** (wrapper-level fix applied): all 4 quadrants should now PASS
    - **Class B** (deferred to UnifiedGameSettings milestone): mark cells as `deferred` with note pointing to deferred-items.md
    - **Class C** (visual-only, not actually broken): mark cells as PASS with note "reclassified per Plan 09 — visually awkward but functional"

    | Component | phone-portrait | phone-landscape | tablet-portrait | tablet-landscape |
    |---|---|---|---|---|
    | RhythmGameSetup | ☐ pass / ☐ fail / ☐ deferred | ☐ pass / ☐ fail / ☐ deferred | ☐ pass / ☐ fail / ☐ deferred | ☐ pass / ☐ fail / ☐ deferred |
    | RhythmGameSettings (dead code, @deprecated) | N/A — no UI consumer; row marked obsolete per Plan 09 Task 2 | N/A | N/A | N/A |

    Plan 09 Classification: ☐ Class A / ☐ Class B / ☐ Class C — see 34-09-SUMMARY.md

    ### Newly-Affected Regression Check — MetronomeTrainer setup phase

    If Plan 09 applied a Class A wrapper fix to MetronomeTrainer's setup-phase render, verify the fix doesn't regress the in-game render path:

    Steps:
    1. Open `/rhythm-mode/metronome-trainer` (free-play entry)
    2. Complete setup → Start → confirm in-game state renders correctly at iPhone SE portrait (countdown overlay, beat circles, tap area all visible and interactive)

    | Field | Value |
    |---|---|
    | Result | ☐ pass / ☐ fail |
    | Notes | |

    ## UAT Delta Sign-Off

    - [ ] All delta rows above pass OR are explicitly classified as deferred (with deferred-items.md entry)
    - [ ] No regressions in original passing rows (random spot-check 3 rows from original UAT — none broken)
    - [ ] Pre-Delta Gate (test:run, build, lint Phase 34 scope) all green
    - [ ] Plan 09 classification result recorded above (A / B / C)

    Signed: \_\_\_\_\_\_ on \_\_\_\_\_\_

    ---
    ```

    Commit message: `chore(34-10): scaffold UAT delta walkthrough section in 34-UAT.md`.

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md','utf8');const required=['UAT Delta (Gap Closure) — Walkthrough','Delta SC #1','Delta SC #2','Delta SC #3','Delta SC #5','UAT Delta Sign-Off'];const missing=required.filter(r=>!t.includes(r));if(missing.length){console.error('Missing delta sections:',missing);process.exit(1)}console.log('OK — UAT delta skeleton complete')"</automated>
  </verify>
  <acceptance_criteria>
    - 34-UAT.md contains literal headings: `## UAT Delta (Gap Closure) — Walkthrough`, `### Delta SC #1`, `### Delta SC #2`, `### Delta SC #3`, `### Delta SC #5`, `## UAT Delta Sign-Off`
    - Original sections (Pre-UAT Gate, original SC #1-5 walkthrough rows, Regression Check, original Deferred Items, original UAT Sign-Off) ARE PRESERVED — `wc -l 34-UAT.md` returns more lines than before, not fewer or equal
    - Delta SC #2 references `?measures=4` URL param: `grep -c "?measures=4" .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md` returns at least 2
    - Delta SC #5 references Plan 09 classification (A / B / C): `grep -c "Class A\\|Class B\\|Class C" .planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md` returns at least 1
  </acceptance_criteria>
  <done>34-UAT.md has UAT Delta walkthrough skeleton appended; ready for human verifier.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT delta walkthrough on iPhone SE + iPad</name>
  <files>.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-UAT.md</files>
  <action>
    Pause for the human verifier to walk the delta scenarios per the verification steps below. Fill in 34-UAT.md cells as you go. Resume only when user types "approved" (all delta rows pass or deferred) or "gap" (further failures need another gap-closure pass).
  </action>
  <verify>34-UAT.md UAT Delta sections all filled with pass / fail / deferred markers; Plan 09 classification recorded; user signs delta sign-off section</verify>
  <done>All UAT Delta rows pass or are explicitly deferred with notes; user signs the UAT Delta Sign-Off; phase ready to ship via /gsd-progress.</done>
  <what-built>
    Plan 07 fixed the standalone RhythmDictationGame wrapper (free-play 2x2 grid). Plan 08 added a dev-only `?measures=N` URL param to RhythmReadingGame (UAT-enabling, removes hardcoded 1-measure default in free-play). Plan 09 investigated GAP 3 root cause and applied minimal wrapper-level fix in MetronomeTrainer if Class A, otherwise logged UnifiedGameSettings issues as deferred per D-10. RhythmGameSettings was marked `@deprecated`. The UAT-delta skeleton in 34-UAT.md (from Task 2) covers exactly the failed rows + newly-affected regression checks.
  </what-built>
  <how-to-verify>
    1. **Start dev server:** `npm run dev` (port 5174)

    2. **Delta SC #1 (GAP 1) — free-play dictation 2x2 grid:**
       - Open Chrome DevTools at 375×667 (iPhone SE portrait)
       - Visit `http://localhost:5174/rhythm-mode/rhythm-dictation-game` (free-play, no trail state)
       - Wait for first prompt
       - Confirm: 1 staff at top, 3 cards in 2×2 grid (third card col-span-2), no vertical scroll, all 3 cards tappable
       - Mark Delta SC #1 free-play row pass / fail in 34-UAT.md

    3. **Delta SC #1 regression — trail entry to mixed-lesson dictation:**
       - In the same DevTools session, navigate to a trail mixed-lesson node containing a Dictation question
       - Confirm 2x2 grid + col-span-2 still works as before (Plan 07 should NOT have changed this)
       - Mark regression row pass / fail

    4. **Delta SC #2 (GAP 2) — long-pattern rows on phone-portrait:**
       - Visit `http://localhost:5174/rhythm-mode/rhythm-reading-game` (no param) → start → assert NO rotate prompt overlay
       - Visit `http://localhost:5174/rhythm-mode/rhythm-reading-game?measures=4` → start → assert rotate prompt overlay APPEARS (4 measures × 4 beats = 16 > 8 threshold)
       - Same for tap (still via reading game route since `/rhythm-mode/rhythm-tap-game` is not a route)
       - Mixed-lesson swap rows: navigate trail to a mixed-lesson node where renderers swap between long and short patterns; observe overlay appearing/disappearing per renderer's content. If trail content does not exhibit the swap, mark "skipped — trail node content does not exhibit the swap"
       - Mark each Delta SC #2 sub-row

    5. **Delta SC #3 (GAP 2) — long-pattern tablet rows:**
       - Switch DevTools to 768×1024 (iPad portrait) and 1024×768 (iPad landscape)
       - At each, visit `http://localhost:5174/rhythm-mode/rhythm-reading-game?measures=4`
       - Confirm rotate prompt NEVER appears (tablet ≥768 viewport gate filters it out)
       - Mark each Delta SC #3 cell

    6. **Delta SC #5 (GAP 3) — RhythmGameSetup + RhythmGameSettings:**
       - Read `34-09-SUMMARY.md` for Plan 09's classification (A / B / C)
       - At each of 4 quadrants, visit `http://localhost:5174/rhythm-mode/metronome-trainer` (the only route surfacing RhythmGameSetup)
       - Per Plan 09 classification:
         - **Class A:** verify wrapper fix delivers PASS at each quadrant
         - **Class B:** mark cells as `deferred` (cross-cutting UnifiedGameSettings issue per D-10)
         - **Class C:** mark cells as PASS with note "visually awkward but functional"
       - For RhythmGameSettings row: mark all 4 cells as N/A (component is dead code per Plan 09 Task 2)
       - Mark Plan 09 classification (A / B / C) in the sign-off

    7. **Newly-affected regression — MetronomeTrainer setup → in-game:**
       - At iPhone SE portrait, run through `/rhythm-mode/metronome-trainer` setup → start → in-game
       - Confirm countdown, beat circles, tap area all render and respond
       - Mark regression row pass / fail

    8. **Sign delta sign-off** at the bottom of 34-UAT.md when all delta rows pass or are explicitly deferred.

    9. **If anything still fails** — type `gap` and describe failures. The orchestrator will route to another gap-closure pass.

    10. **If all delta rows pass or are deferred** — type `approved` to close the phase.

  </how-to-verify>
  <resume-signal>Type `approved` if all delta rows pass / deferred, OR `gap` if further failures need another gap-closure round, OR list specific failed checks</resume-signal>
</task>

</tasks>

<verification>
- 34-UAT.md UAT Delta sections all filled with pass / fail / deferred markers
- All grep gates pass
- Pre-Delta Gate (test:run, build, lint Phase 34 scope) green
- Plan 09 classification (A / B / C) recorded
- User signs UAT Delta Sign-Off
</verification>

<success_criteria>

- All delta rows in 34-UAT.md marked pass OR explicitly deferred with notes
- Phase 34 ship gate met (or one more gap-closure round triggered)
- Original 34-UAT.md content preserved (delta is appended, not replaced)
- 34-UAT.md serves as the final verification artifact for /gsd-progress to close Phase 34
  </success_criteria>

<output>
After completion, create `.planning/phases/34-responsive-rhythm-renderers-non-arcade/34-10-SUMMARY.md` referencing 34-UAT.md as the final verification artifact and recording the delta outcome.
</output>
