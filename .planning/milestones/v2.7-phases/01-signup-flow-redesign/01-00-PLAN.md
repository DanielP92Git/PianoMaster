---
phase: 01-signup-flow-redesign
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/auth/SignupForm.test.jsx
  - src/components/auth/ParentEmailStep.test.jsx
  - src/features/authentication/useSignup.test.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-07
  - D-10
  - D-13

must_haves:
  truths:
    - "Test stubs exist for all behavioral contracts in VALIDATION.md"
    - "All test files run without error (pending tests are skipped, not failing)"
    - "npx vitest run src/components/auth/ exits 0"
  artifacts:
    - path: "src/components/auth/SignupForm.test.jsx"
      provides: "Test stubs for wizard step navigation — student path, teacher path, back navigation, age branching"
      contains: "describe"
    - path: "src/components/auth/ParentEmailStep.test.jsx"
      provides: "Test stubs for skip button behavior and email submission"
      contains: "describe"
    - path: "src/features/authentication/useSignup.test.js"
      provides: "Test stubs for account_status always active and no consent email"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for all behavioral contracts defined in VALIDATION.md. These are stub test files with `describe` blocks and `it.todo()` placeholders for each behavior. They establish the test harness and mocks so that Plans 01-03 can reference them in their `<verify>` commands.

Purpose: Satisfy Nyquist compliance — every task in Plans 01-03 must have an automated test file to verify against. Without Wave 0, all `<automated>` verify commands reference nonexistent files.
Output: Three test files with pending test stubs. All pass (0 failures, N skipped).
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-signup-flow-redesign/01-CONTEXT.md
@.planning/phases/01-signup-flow-redesign/01-VALIDATION.md

<interfaces>
<!-- Current SignupForm.jsx imports (will be rewritten in Plan 03, but tests mock these): -->
From src/components/auth/SignupForm.jsx:
```jsx
import { SocialLogin } from "./SocialLogin";
import { useSignup } from "../../features/authentication/useSignup";
import { AgeGate } from "./AgeGate";
import { ParentEmailStep } from "./ParentEmailStep";
```

<!-- Current ParentEmailStep.jsx API (will be updated in Plan 02): -->
From src/components/auth/ParentEmailStep.jsx:
```jsx
export function ParentEmailStep({ onSubmit, onBack, disabled = false })
// After Plan 02: { onSubmit, onSkip, onBack, disabled }
```

<!-- Current useSignup.js mutation (will be updated in Plan 01): -->
From src/features/authentication/useSignup.js:
```js
mutationFn: async ({ email, password, firstName, lastName, role, dateOfBirth, parentEmail }) => {
  // After Plan 01: { email, password, firstName, lastName, role, birthYear, parentEmail }
  // After Plan 01: account_status always 'active'
}
```

<!-- Test pattern from existing FeedbackForm.test.jsx: -->
```jsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
// vi.mock() for dependencies
// describe blocks with it() tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SignupForm.test.jsx with step navigation stubs</name>
  <files>src/components/auth/SignupForm.test.jsx</files>
  <action>
    Create `src/components/auth/SignupForm.test.jsx` with test stubs for all SignupForm behavioral contracts from VALIDATION.md.

    **Structure the file as follows:**

    1. **Imports:** `describe, it, expect, vi, beforeEach` from vitest, `render, screen, fireEvent` from @testing-library/react.

    2. **Mocks** (required for the component to render at all):
       - Mock `../../features/authentication/useSignup` returning `{ signup: vi.fn(), isPending: false }` — needed because the component calls `useSignup()`.
       - Mock `./SocialLogin` as a simple div with `data-testid="social-login"` that renders the `role` prop as text — so tests can assert role is passed correctly.
       - Mock `./AgeGate` as a component that renders a "Select year" button. When clicked, it calls `props.onSubmit(2016)` (under-13 year). Also render a "Select year 13+" button that calls `props.onSubmit(2010)`. Also render a "Back from age" button that calls `props.onBack()`. This lets tests drive the wizard through the age step.
       - Mock `./ParentEmailStep` as a component that renders: a "Submit email" button calling `props.onSubmit("parent@test.com")`, a "Skip email" button calling `props.onSkip()`, and a "Back from email" button calling `props.onBack()`.
       - Mock `react-router-dom` with `useNavigate` returning `vi.fn()`.

    3. **Test blocks using `it.todo()` for each behavior from VALIDATION.md:**

       ```jsx
       describe("SignupForm Wizard", () => {
         // Step navigation (D-01, D-02, D-03)
         it.todo("renders role selection as the first step");
         it.todo("student role selection navigates to birth year step");
         it.todo("teacher role selection navigates directly to credentials step");

         // Age branching (D-02, D-07)
         it.todo("birth year under-13 navigates to parent email step");
         it.todo("birth year 13+ skips parent email and goes to credentials");

         // Back navigation (D-04)
         it.todo("back from birth-year returns to role selection");
         it.todo("back from parent-email returns to birth-year");
         it.todo("back from credentials (teacher) returns to role");
         it.todo("back from credentials (under-13 student) returns to parent-email");
         it.todo("back from credentials (13+ student) returns to birth-year");

         // Step dots (D-01)
         it.todo("step dots show 4 dots for student path");
         it.todo("step dots show 2 dots for teacher path");

         // Google OAuth (D-08, D-09)
         it.todo("SocialLogin receives role prop on credentials step");
       });
       ```

    4. **One real (non-todo) test** to confirm the test file runs: test that SignupForm renders without crashing and shows the role selection step.

       ```jsx
       it("renders role selection as the first step", () => {
         render(<SignupForm onBackToLogin={vi.fn()} />);
         expect(screen.getByText("Student")).toBeInTheDocument();
         expect(screen.getByText("Teacher")).toBeInTheDocument();
       });
       ```

    **Important:** Use the CURRENT SignupForm import (the component as it exists today). The mocks must work with the current API. Plans 01-03 will update both the component and fill in the test stubs. The one real test validates the current component renders.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/SignupForm.test.jsx 2>&1</automated>
  </verify>
  <done>SignupForm.test.jsx exists with 13 todo stubs + 1 real test. `npx vitest run` shows 1 passed, 12 skipped (todos), 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create ParentEmailStep.test.jsx and useSignup.test.js stubs</name>
  <files>src/components/auth/ParentEmailStep.test.jsx, src/features/authentication/useSignup.test.js</files>
  <action>
    Create two test files with stubs matching the VALIDATION.md behavioral contracts.

    **File 1: `src/components/auth/ParentEmailStep.test.jsx`**

    1. **Imports:** `describe, it, expect, vi` from vitest, `render, screen, fireEvent` from @testing-library/react.

    2. **Import the component:** `import { ParentEmailStep } from "./ParentEmailStep";`

    3. **Test blocks:**
       ```jsx
       describe("ParentEmailStep", () => {
         // Current behavior (before Plan 02 updates)
         it.todo("calls onSubmit with email string when form submitted");
         it.todo("shows validation error for invalid email");

         // Post Plan-02 behavior (D-07 — Skip button)
         it.todo("Skip button calls onSkip with no arguments");
         it.todo("displays optional messaging about weekly reports");
         it.todo("single email field — no confirm email field");
       });
       ```

    4. **One real test** to confirm file runs: test that ParentEmailStep renders an email input.
       ```jsx
       it("renders an email input", () => {
         render(
           <ParentEmailStep
             onSubmit={vi.fn()}
             onBack={vi.fn()}
           />
         );
         expect(screen.getByPlaceholderText(/email/i)).toBeInTheDocument();
       });
       ```

    **File 2: `src/features/authentication/useSignup.test.js`**

    1. **Imports:** `describe, it, vi` from vitest.

    2. **Mocks:** Mock `../../services/supabase` with a basic supabase client stub. Mock `react-router-dom` with `useNavigate` returning `vi.fn()`. Mock `@tanstack/react-query` with `useMutation` and `useQueryClient` stubs.

    3. **Test blocks:**
       ```js
       describe("useSignup", () => {
         // Post Plan-01 behavior (D-13)
         it.todo("sets account_status to 'active' regardless of age");
         it.todo("accepts birthYear integer parameter instead of dateOfBirth");
         it.todo("does NOT call sendParentalConsentEmail");
         it.todo("stores date_of_birth as YYYY-01-01 format from birthYear");
       });
       ```

    4. **No real tests needed** — `useSignup` is a React hook that requires complex rendering context (QueryClientProvider, Router). The todo stubs are sufficient for Wave 0. Plans 01 will fill them in if feasible, otherwise the grep-based verify commands cover the behavioral contract.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/ParentEmailStep.test.jsx src/features/authentication/useSignup.test.js 2>&1</automated>
  </verify>
  <done>ParentEmailStep.test.jsx has 1 real test + 4 todo stubs. useSignup.test.js has 4 todo stubs. Both files run without error.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/auth/` exits 0 — all test files found and run
2. `npx vitest run src/features/authentication/useSignup.test.js` exits 0
3. No test failures (only passes + skipped todos)
4. Each VALIDATION.md behavior has a corresponding test stub
</verification>

<success_criteria>
- Three test files exist: SignupForm.test.jsx, ParentEmailStep.test.jsx, useSignup.test.js
- All files run via vitest without errors
- Combined: 2 real tests pass, ~17 todo stubs skipped
- Plans 01-03 can now reference these files in their <automated> verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-signup-flow-redesign/01-00-SUMMARY.md`
</output>
