---
phase: 09-ear-training-games
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/ear-training-games/earTrainingUtils.js
  - src/components/games/ear-training-games/earTrainingUtils.test.js
  - src/components/games/ear-training-games/components/PianoKeyboardReveal.jsx
  - src/components/games/ear-training-games/PianoKeyboardReveal.test.js
autonomous: true
requirements: [PITCH-03, INTV-02, INTV-03, INTV-04]

must_haves:
  truths:
    - "Note pair generation returns two different notes within the specified semitone range"
    - "Tier-band progression narrows interval distance across 10 questions (wide first, close last)"
    - "Interval classification maps semitone distances to step/skip/leap correctly"
    - "Ascending-first ordering produces ~60% ascending intervals before mixed"
    - "PianoKeyboardReveal renders a 1-octave SVG keyboard with correct key highlight colors"
    - "In-between keys get dim-highlight only when showInBetween prop is true"
  artifacts:
    - path: "src/components/games/ear-training-games/earTrainingUtils.js"
      provides: "Note pair generation, tier logic, interval classification, ascending-first ordering"
      exports: ["generateNotePair", "getTierForQuestion", "classifyInterval", "generateIntervalQuestion", "NOTE_ORDER"]
    - path: "src/components/games/ear-training-games/earTrainingUtils.test.js"
      provides: "Unit tests for all utility functions"
      min_lines: 80
    - path: "src/components/games/ear-training-games/components/PianoKeyboardReveal.jsx"
      provides: "Shared SVG piano keyboard reveal component"
      exports: ["PianoKeyboardReveal"]
    - path: "src/components/games/ear-training-games/PianoKeyboardReveal.test.js"
      provides: "Unit tests for PianoKeyboardReveal"
      min_lines: 30
  key_links:
    - from: "earTrainingUtils.js"
      to: "usePianoSampler.js"
      via: "NOTE_FREQS import for NOTE_ORDER derivation"
      pattern: "import.*NOTE_FREQS.*from.*usePianoSampler"
    - from: "PianoKeyboardReveal.jsx"
      to: "earTrainingUtils.js"
      via: "NOTE_ORDER import for key state derivation"
      pattern: "import.*NOTE_ORDER.*from.*earTrainingUtils"
---

<objective>
Build the shared foundation for both ear training games: utility functions for note pair generation, tier-band progression, interval classification, and ascending-first ordering, plus the PianoKeyboardReveal SVG component used by both NoteComparisonGame and IntervalGame.

Purpose: These shared modules must exist before either game component can be built. They contain all the domain logic (what notes to play, how to classify answers) and the shared visual reveal (piano keyboard SVG).
Output: earTrainingUtils.js, PianoKeyboardReveal.jsx, and their test files.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ear-training-games/09-CONTEXT.md
@.planning/phases/09-ear-training-games/09-RESEARCH.md
@.planning/phases/09-ear-training-games/09-UI-SPEC.md
@.planning/phases/09-ear-training-games/09-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/usePianoSampler.js:
```javascript
export const NOTE_FREQS = {
  'C3': 130.81, 'C#3': 138.59, 'D3': 146.83, 'D#3': 155.56,
  'E3': 164.81, 'F3': 174.61, 'F#3': 185.00, 'G3': 196.00,
  'G#3': 207.65, 'A3': 220.00, 'A#3': 233.08, 'B3': 246.94,
  'C4': 261.63, 'C#4': 277.18, 'D4': 293.66, 'D#4': 311.13,
  'E4': 329.63, 'F4': 349.23, 'F#4': 369.99, 'G4': 392.00,
  'G#4': 415.30, 'A4': 440.00, 'A#4': 466.16, 'B4': 493.88,
};
export function usePianoSampler() → { playNote(noteId, { duration, velocity, startTime }) }
```

From tailwind.config.js (existing animation):
```javascript
floatUp: { "0%": { transform: "translateY(10px)", opacity: "0" }, "100%": { transform: "translateY(0)", opacity: "1" } },
animation: { floatUp: "floatUp 0.6s ease-out forwards" }
```

From 09-UI-SPEC.md (PianoKeyboardReveal dimensions):
- White keys: 28px wide x 120px tall, fill: white, stroke: rgba(255,255,255,0.3), rx="3"
- Black keys: 14px wide x 72px tall, fill: #1e1b4b, stroke: rgba(255,255,255,0.3), rx="2"
- 7 white keys x 28px = 196px SVG canvas width
- Note 1 white key: #60a5fa (blue-400), black: #3b82f6 (blue-500)
- Note 2 white key: #fb923c (orange-400), black: #ea580c (orange-600)
- In-between keys: rgba(255,255,255,0.2)
- Default black key: #1e1b4b
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create earTrainingUtils.js with note pair generation, tier logic, interval classification</name>
  <files>
    src/components/games/ear-training-games/earTrainingUtils.js,
    src/components/games/ear-training-games/earTrainingUtils.test.js
  </files>
  <read_first>
    src/hooks/usePianoSampler.js,
    .planning/phases/09-ear-training-games/09-RESEARCH.md,
    .planning/phases/09-ear-training-games/09-CONTEXT.md
  </read_first>
  <behavior>
    - generateNotePair(tierMinSemitones, tierMaxSemitones) returns { note1, note2, semitones, direction } where note1 !== note2, Math.abs(semitones) is within [min, max], and both notes are valid keys in NOTE_FREQS
    - generateNotePair never returns semitones === 0 (no duplicate notes)
    - generateNotePair clamps indices to [0, 23] (C3-B4 range)
    - getTierForQuestion(0) returns { minSemitones: 6, maxSemitones: 12 } (Tier 1)
    - getTierForQuestion(4) returns { minSemitones: 3, maxSemitones: 5 } (Tier 2)
    - getTierForQuestion(9) returns { minSemitones: 1, maxSemitones: 2 } (Tier 3)
    - classifyInterval(1) returns 'step', classifyInterval(2) returns 'step'
    - classifyInterval(3) returns 'skip', classifyInterval(4) returns 'skip'
    - classifyInterval(5) returns 'leap', classifyInterval(12) returns 'leap'
    - classifyInterval(-3) returns 'skip' (uses absolute value)
    - generateIntervalQuestion(questionIndex, totalQuestions, ascendingRatio=0.6) returns ascending direction for first 60% of questions
    - generateIntervalQuestion(0, 10, 0.6) has direction 'ascending'
    - generateIntervalQuestion(7, 10, 0.6) can have direction 'ascending' or 'descending' (mixed)
    - NOTE_ORDER is an array of 24 strings matching Object.keys(NOTE_FREQS)
    - getNotesInBetween('C4', 'E4') returns ['C#4', 'D4', 'D#4']
    - getDisplayOctaveRoot('C4', 'E4') returns 'C4' (lower note's octave root)
    - getDisplayOctaveRoot('A3', 'C4') returns 'G3' or similar (centers around the pair)
  </behavior>
  <action>
    Create `src/components/games/ear-training-games/earTrainingUtils.js` exporting:

    1. `NOTE_ORDER` — `Object.keys(NOTE_FREQS)` from usePianoSampler.js. Import NOTE_FREQS directly:
       `import { NOTE_FREQS } from '../../../hooks/usePianoSampler';`
       `export const NOTE_ORDER = Object.keys(NOTE_FREQS);` — yields 24 chromatic notes C3-B4.

    2. `COMPARISON_TIERS` — per D-09:
       ```javascript
       export const COMPARISON_TIERS = [
         { questions: [0, 1, 2], minSemitones: 6, maxSemitones: 12 },
         { questions: [3, 4, 5, 6], minSemitones: 3, maxSemitones: 5 },
         { questions: [7, 8, 9], minSemitones: 1, maxSemitones: 2 },
       ];
       ```

    3. `getTierForQuestion(questionIndex)` — returns matching tier object from COMPARISON_TIERS, falls back to COMPARISON_TIERS[0].

    4. `generateNotePair(minSemitones, maxSemitones)` — returns `{ note1, note2, semitones, direction }`:
       - Pick a random semitone delta in [minSemitones, maxSemitones]
       - Randomly decide ascending or descending (for NoteComparison; IntervalGame controls this separately)
       - Pick note1Index randomly, compute note2Index = note1Index + delta (or - delta)
       - Clamp both to [0, NOTE_ORDER.length - 1]
       - If delta === 0 after clamping, retry (max 10 attempts), fallback to { note1: 'C3', note2: 'C4', semitones: 12, direction: 'ascending' }
       - direction = note2Index > note1Index ? 'ascending' : 'descending'

    5. `classifyInterval(semitones)` — per RESEARCH Pattern 6:
       ```javascript
       export function classifyInterval(semitones) {
         const abs = Math.abs(semitones);
         if (abs <= 2) return 'step';
         if (abs <= 4) return 'skip';
         return 'leap';
       }
       ```

    6. `generateIntervalQuestion(questionIndex, totalQuestions, ascendingRatio = 0.6)` — per D-10:
       - If questionIndex < totalQuestions * ascendingRatio, force ascending (note2 > note1)
       - Otherwise, random ascending or descending
       - Determine interval category needed (mix of step/skip/leap evenly across questions)
       - Return `{ note1, note2, semitones, direction, category }` where category is step/skip/leap

    7. `getNotesInBetween(note1, note2)` — returns array of note names strictly between the two notes in chromatic order.

    8. `getDisplayOctaveRoot(note1, note2)` — determines which octave slice to display. Returns the note name of the first white key (C) of the octave containing the lower of the two notes. If the lower note is below F of its octave, use that octave's C. Otherwise, shift to show both notes centered.

    Then create `src/components/games/ear-training-games/earTrainingUtils.test.js` with tests for all behaviors listed above. Use `vi.mock` sparingly — these are pure functions (except generateNotePair uses Math.random).

    For deterministic random tests: spy on `Math.random` with `vi.spyOn(Math, 'random').mockReturnValue(0.5)` to get predictable outputs. Test boundary cases (min/max semitones, first/last note in range).
  </action>
  <verify>
    <automated>npx vitest run src/components/games/ear-training-games/earTrainingUtils.test.js</automated>
  </verify>
  <acceptance_criteria>
    - earTrainingUtils.js contains `export const NOTE_ORDER = Object.keys(NOTE_FREQS)`
    - earTrainingUtils.js contains `export const COMPARISON_TIERS`
    - earTrainingUtils.js contains `export function getTierForQuestion(`
    - earTrainingUtils.js contains `export function generateNotePair(`
    - earTrainingUtils.js contains `export function classifyInterval(`
    - earTrainingUtils.js contains `export function generateIntervalQuestion(`
    - earTrainingUtils.js contains `export function getNotesInBetween(`
    - earTrainingUtils.js contains `export function getDisplayOctaveRoot(`
    - earTrainingUtils.js contains `import { NOTE_FREQS } from '../../../hooks/usePianoSampler'`
    - earTrainingUtils.test.js contains at least 12 test cases (it/test calls)
    - `npx vitest run src/components/games/ear-training-games/earTrainingUtils.test.js` exits 0
  </acceptance_criteria>
  <done>All 8 exported functions pass unit tests. Tier logic maps Q0-2 to wide, Q3-6 to medium, Q7-9 to close. Interval classification: 1-2 semitones = step, 3-4 = skip, 5+ = leap. No duplicate note pairs generated.</done>
</task>

<task type="auto">
  <name>Task 2: Create PianoKeyboardReveal SVG component with highlight states and test file</name>
  <files>
    src/components/games/ear-training-games/components/PianoKeyboardReveal.jsx,
    src/components/games/ear-training-games/PianoKeyboardReveal.test.js
  </files>
  <read_first>
    src/components/games/ear-training-games/earTrainingUtils.js,
    .planning/phases/09-ear-training-games/09-UI-SPEC.md,
    .planning/phases/09-ear-training-games/09-CONTEXT.md
  </read_first>
  <action>
    Create `src/components/games/ear-training-games/components/PianoKeyboardReveal.jsx` — a shared SVG component per D-01, D-02, D-03, D-08.

    **Props interface:**
    ```javascript
    PianoKeyboardReveal({
      note1,           // string: e.g. 'C4'
      note2,           // string: e.g. 'E4'
      showInBetween,   // boolean: true for IntervalGame (D-08), false for NoteComparison
      intervalLabel,   // string | null: e.g. 'SKIP — C4 to E4' (IntervalGame only)
      subLabel,        // string | null: e.g. 'Jumped over D4' (IntervalGame only)
      visible,         // boolean: controls slide-in animation
      reducedMotion,   // boolean: from AccessibilityContext
    })
    ```

    **SVG structure:** Render one octave (7 white keys + 5 black keys). The octave root is determined by `getDisplayOctaveRoot(note1, note2)` from earTrainingUtils.js.

    **Key layout constants (from 09-UI-SPEC.md):**
    - White key: width=28, height=120, fill="white", stroke="rgba(255,255,255,0.3)", rx=3
    - Black key: width=14, height=72, fill="#1e1b4b", stroke="rgba(255,255,255,0.3)", rx=2
    - SVG viewBox: "0 0 196 140" (196 = 7 * 28; 140 = 120 key + 20 label area)
    - Black key x-offsets within an octave (relative to white key positions):
      C#: white[0].x + 21 = 21, D#: 49, F#: 105, G#: 133, A#: 161 (these are approximate; center black keys between their natural neighbors)

    **White key mapping for one octave starting at C:**
    keys = [C, D, E, F, G, A, B] at x = [0, 28, 56, 84, 112, 140, 168]

    **Black key mapping:**
    blacks = [C#, D#, F#, G#, A#] at x = [19, 47, 103, 131, 159] (centered between white key pairs)

    **Key state derivation:** Import `NOTE_ORDER`, `getNotesInBetween` from earTrainingUtils.js. Build a map of note-name to state:
    - note1 → 'note1' (blue)
    - note2 → 'note2' (orange)
    - getNotesInBetween(note1, note2) → 'between' if showInBetween
    - all others → 'default'

    **Fill colors per state (from 09-UI-SPEC.md):**
    ```javascript
    const FILL_COLORS = {
      white: { default: '#ffffff', note1: '#60a5fa', note2: '#fb923c', between: 'rgba(255,255,255,0.2)' },
      black: { default: '#1e1b4b', note1: '#3b82f6', note2: '#ea580c', between: 'rgba(255,255,255,0.15)' },
    };
    ```

    **Note name labels** below highlighted keys: Render text elements at 14px, positioned below the key, colored `#93c5fd` (blue-300) for note1, `#fdba74` (orange-300) for note2. Only show labels for highlighted keys.

    **Direction arrow** (NoteComparisonGame only — not rendered if intervalLabel is provided): An SVG arrow icon between note labels. Arrow points up or down based on note2 > note1 comparison. Color: `#818cf8` (indigo-400). Size 24px.

    Actually, the direction arrow is rendered by the parent game component, not PianoKeyboardReveal itself. The keyboard only renders keys + labels + optional interval label.

    **Interval label area** (when intervalLabel prop provided — IntervalGame): Below the SVG keyboard, render:
    - intervalLabel at 20px bold text-white
    - subLabel at 16px regular text-white/70

    **Container styling:**
    - Outer div: `dir="ltr"` (always LTR for piano), `bg-white/10 backdrop-blur-md border border-white/20 rounded-xl p-4`
    - Slide-in animation: when `visible` transitions from false to true, animate `transform: translateY(100%)` to `translateY(0)` over 300ms ease-out. Use inline style controlled by `visible` prop.
    - If `reducedMotion`: skip translateY, fade opacity 0 to 1 only.
    - Set `aria-hidden="true"` on the SVG element (decorative visual).

    **Test file** `src/components/games/ear-training-games/PianoKeyboardReveal.test.js`:
    - Test: renders 7 white `<rect>` elements and 5 black `<rect>` elements
    - Test: note1 key has fill="#60a5fa" (blue-400 for white key)
    - Test: note2 key has fill="#fb923c" (orange-400 for white key)
    - Test: when showInBetween=true, between keys have fill containing "rgba(255,255,255"
    - Test: when showInBetween=false, between keys have default fill
    - Test: container has dir="ltr"
    - Test: SVG has aria-hidden="true"
    - Test: when visible=true, container does NOT have translateY(100%) style
    - Test: intervalLabel text renders when prop provided
    - Test: subLabel text renders when prop provided
  </action>
  <verify>
    <automated>npx vitest run src/components/games/ear-training-games/PianoKeyboardReveal.test.js</automated>
  </verify>
  <acceptance_criteria>
    - PianoKeyboardReveal.jsx contains `export default function PianoKeyboardReveal(` or `export function PianoKeyboardReveal(`
    - PianoKeyboardReveal.jsx contains `dir="ltr"` or `dir={'ltr'}`
    - PianoKeyboardReveal.jsx contains `aria-hidden="true"` or `aria-hidden={'true'}` or `aria-hidden={true}`
    - PianoKeyboardReveal.jsx contains `#60a5fa` (blue-400 note1 fill)
    - PianoKeyboardReveal.jsx contains `#fb923c` (orange-400 note2 fill)
    - PianoKeyboardReveal.jsx contains `rgba(255,255,255` (in-between key fill)
    - PianoKeyboardReveal.jsx contains `import` from `earTrainingUtils`
    - PianoKeyboardReveal.test.js contains at least 6 test cases
    - `npx vitest run src/components/games/ear-training-games/PianoKeyboardReveal.test.js` exits 0
  </acceptance_criteria>
  <done>PianoKeyboardReveal renders a 1-octave SVG keyboard with blue note1, orange note2, and optional dim in-between highlights. Slide-in animation controlled by visible prop. dir="ltr" enforced. aria-hidden="true" on SVG. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/games/ear-training-games/` — all test files in the directory pass
- `npm run lint` — no lint errors in new files
- earTrainingUtils.js exports all 8 functions
- PianoKeyboardReveal.jsx renders valid SVG with correct key dimensions
</verification>

<success_criteria>
- Shared utility functions cover note pair generation, tier progression, interval classification, ascending-first ordering
- PianoKeyboardReveal SVG component renders a 1-octave keyboard with correct highlight states for both game modes
- All unit tests pass
- Both NoteComparisonGame (Plan 02) and IntervalGame (Plan 03) can import and use these shared modules
</success_criteria>

<output>
After completion, create `.planning/phases/09-ear-training-games/09-01-SUMMARY.md`
</output>
