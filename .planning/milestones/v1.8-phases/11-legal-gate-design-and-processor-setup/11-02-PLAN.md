---
phase: 11-legal-gate-design-and-processor-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/config/subscriptionConfig.js
autonomous: true
requirements:
  - PAY-01

must_haves:
  truths:
    - "subscriptionConfig.js contains a PAYMENT_PROCESSOR object with name set to 'TBD' and a clear TODO comment for when the processor is chosen"
    - "The processor decision is documented as pending in the phase summary — no billing code, webhooks, or SDK integration until processor is selected"
  artifacts:
    - path: "src/config/subscriptionConfig.js"
      provides: "Placeholder processor config"
      contains: "PAYMENT_PROCESSOR"
  key_links: []
---

<objective>
Add a placeholder payment processor config to subscriptionConfig.js and document that the processor decision is pending.

Purpose: PAY-01 requires processor selection before billing code is written. Since the Israeli-market processor hasn't been decided yet, this plan records the decision as pending and adds a placeholder config so downstream phases know where to update when the choice is made.

Output: Updated config file with processor placeholder, phase summary documenting pending status.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-legal-gate-design-and-processor-setup/11-CONTEXT.md
@src/config/subscriptionConfig.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add placeholder payment processor config</name>
  <files>
    src/config/subscriptionConfig.js
  </files>
  <action>
Add to `src/config/subscriptionConfig.js` (created by Plan 11-01):

```javascript
/**
 * Payment processor configuration.
 * TODO: Replace placeholder once Israeli-market processor is selected.
 * Candidates under consideration: Paddle, Stripe, local Israeli processors.
 * Decision blocked on: Israeli bank payout support, VAT/MoR handling, sandbox availability.
 */
export const PAYMENT_PROCESSOR = {
  name: 'TBD',
  selected: false,
  sandboxUrl: null,
  liveUrl: null,
  payoutCurrency: null, // ILS or USD — depends on processor
};
```

This gives downstream phases (12-16) a single location to update when the processor is chosen.
  </action>
  <verify>
    <automated>node -e "const m = require('./src/config/subscriptionConfig.js'); console.log(m.PAYMENT_PROCESSOR?.name)" 2>/dev/null || echo "ESM module — verify manually"</automated>
    <manual>Confirm src/config/subscriptionConfig.js exports PAYMENT_PROCESSOR with name 'TBD'</manual>
  </verify>
  <done>subscriptionConfig.js exports PAYMENT_PROCESSOR placeholder. Processor decision is documented as pending with clear TODO and candidate list.</done>
</task>

</tasks>

<verification>
1. `src/config/subscriptionConfig.js` exports `PAYMENT_PROCESSOR` with `name: 'TBD'` and `selected: false`
2. TODO comment lists decision criteria (Israeli bank payout, VAT handling, sandbox)
3. No Paddle SDK, webhook code, or API keys are present anywhere in the codebase
</verification>

<success_criteria>
- Processor placeholder exists in the single config file
- Downstream phases have a clear location to update
- PAY-01 is partially satisfied (processor selection pending, documented as such)
- No premature billing code or processor-specific integration
</success_criteria>

<output>
After completion, create `.planning/phases/11-legal-gate-design-and-processor-setup/11-02-SUMMARY.md`
</output>
