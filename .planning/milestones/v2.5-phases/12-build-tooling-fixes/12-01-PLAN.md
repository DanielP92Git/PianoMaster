---
phase: 12-build-tooling-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/games/sight-reading-game/utils/keySignatureUtils.js
  - src/components/games/sight-reading-game/utils/keySignatureUtils.test.js
  - src/components/games/sight-reading-game/components/KeySignatureSelection.jsx
autonomous: true
requirements: [BUILD-01]

must_haves:
  truths:
    - "npm run verify:patterns exits with code 0"
    - "npm run build completes successfully"
    - "npm run test:run passes with no regressions"
  artifacts:
    - path: "src/components/games/sight-reading-game/utils/keySignatureUtils.js"
      provides: "Fixed ESM import with .js extension"
      contains: 'from "../constants/keySignatureConfig.js"'
    - path: "src/components/games/sight-reading-game/utils/keySignatureUtils.test.js"
      provides: "Consistent ESM import with .js extension"
      contains: 'from "../constants/keySignatureConfig.js"'
    - path: "src/components/games/sight-reading-game/components/KeySignatureSelection.jsx"
      provides: "Consistent ESM import with .js extension"
      contains: 'from "../constants/keySignatureConfig.js"'
  key_links:
    - from: "scripts/patternVerifier.mjs"
      to: "src/components/games/sight-reading-game/utils/keySignatureUtils.js"
      via: "Node ESM import chain: patternVerifier -> patternBuilder.js -> keySignatureUtils.js -> keySignatureConfig.js"
      pattern: 'from "../constants/keySignatureConfig.js"'
---

<objective>
Fix the verify:patterns script by adding the missing `.js` extension to all `keySignatureConfig` imports.

Purpose: The `npm run verify:patterns` script runs under raw Node ESM (not Vite), which requires explicit file extensions on relative imports. The import `from "../constants/keySignatureConfig"` in `keySignatureUtils.js` causes `ERR_MODULE_NOT_FOUND`. Vite and Vitest tolerate extensionless imports (their own resolver), so `npm run build` and `npm run test:run` pass — but the pattern verifier fails, breaking the CI pipeline.

Output: Three files updated with `.js` extension on `keySignatureConfig` imports; verify:patterns passes; build and tests remain green.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.5-phases/12-build-tooling-fixes/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .js extension to all keySignatureConfig imports</name>
  <files>
    src/components/games/sight-reading-game/utils/keySignatureUtils.js,
    src/components/games/sight-reading-game/utils/keySignatureUtils.test.js,
    src/components/games/sight-reading-game/components/KeySignatureSelection.jsx
  </files>
  <read_first>
    src/components/games/sight-reading-game/utils/keySignatureUtils.js,
    src/components/games/sight-reading-game/utils/keySignatureUtils.test.js,
    src/components/games/sight-reading-game/components/KeySignatureSelection.jsx,
    src/components/games/sight-reading-game/constants/keySignatureConfig.js
  </read_first>
  <action>
    In each of the three files, change the `keySignatureConfig` import to include the `.js` extension:

    **File 1: `keySignatureUtils.js` (line 1) — CRITICAL, this is the one that breaks verify:patterns**
    Change:
    ```
    import { KEY_NOTE_LETTERS } from "../constants/keySignatureConfig";
    ```
    To:
    ```
    import { KEY_NOTE_LETTERS } from "../constants/keySignatureConfig.js";
    ```

    **File 2: `keySignatureUtils.test.js` (line 6) — consistency fix**
    Change:
    ```
    } from "../constants/keySignatureConfig";
    ```
    To:
    ```
    } from "../constants/keySignatureConfig.js";
    ```
    Note: This is a multi-line import. Only the closing line with the path needs changing. The import path string is what matters.

    **File 3: `KeySignatureSelection.jsx` (line 3) — consistency fix**
    Change:
    ```
    import { KEY_SIGNATURE_OPTIONS } from "../constants/keySignatureConfig";
    ```
    To:
    ```
    import { KEY_SIGNATURE_OPTIONS } from "../constants/keySignatureConfig.js";
    ```

    Why all three: Only `keySignatureUtils.js` breaks the verifier (it runs under Node ESM). The other two run under Vite/Vitest which tolerates extensionless imports. However, fixing all three prevents future confusion and ensures codebase consistency — any new script running under raw Node ESM that happens to traverse these imports would also break.
  </action>
  <verify>
    <automated>npm run verify:patterns</automated>
  </verify>
  <acceptance_criteria>
    - `keySignatureUtils.js` line 1 contains `from "../constants/keySignatureConfig.js"`
    - `keySignatureUtils.test.js` contains `from "../constants/keySignatureConfig.js"`
    - `KeySignatureSelection.jsx` contains `from "../constants/keySignatureConfig.js"`
    - `npm run verify:patterns` exits with code 0
    - `npm run test:run` exits with code 0 (no regressions)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    verify:patterns passes (exit 0), all 211+ tests pass, build succeeds. All three keySignatureConfig imports use .js extension.
  </done>
</task>

</tasks>

<verification>
1. `npm run verify:patterns` — must exit 0 (was failing with ERR_MODULE_NOT_FOUND)
2. `npm run test:run` — all existing tests pass (no regressions from import path change)
3. `npm run build` — production build succeeds
</verification>

<success_criteria>
- `npm run verify:patterns` exits 0 with no import errors
- `npm run build` completes successfully
- All three files contain `keySignatureConfig.js` (with `.js` extension) in their import paths
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.5-phases/12-build-tooling-fixes/12-01-SUMMARY.md`
</output>
