---
phase: 12-build-tooling-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BUILD-02]

user_setup:
  - service: supabase
    why: "Migration push to production database"
    dashboard_config:
      - task: "Verify student_daily_challenges table does not already exist"
        location: "Supabase Dashboard -> SQL Editor"
      - task: "Verify RLS is active on student_daily_challenges after push"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "student_daily_challenges table exists in production Supabase"
    - "RLS is enabled on student_daily_challenges"
    - "DailyChallengeCard on the dashboard returns real challenge data (not empty/error state)"
    - "Supabase migration history is in sync (no orphaned remote-only entries)"
  artifacts:
    - path: "supabase/migrations/20260317000001_daily_challenges.sql"
      provides: "Daily challenges table schema with RLS"
      contains: "CREATE TABLE student_daily_challenges"
  key_links:
    - from: "src/services/dailyChallengeService.js"
      to: "student_daily_challenges table"
      via: "Supabase client query"
      pattern: "student_daily_challenges"
---

<objective>
Apply the pending daily_challenges migration to production Supabase by repairing migration history and running db push.

Purpose: The `student_daily_challenges` table was created in v2.3 Phase 06 but the migration was never pushed to production. The Supabase CLI refuses to push because 6 remote-only migration entries (from prior dashboard SQL execution) diverge from local migration history. This must be repaired before the migration can be applied. Without this table, DailyChallengeCard shows an error/empty state on the production dashboard.

Output: Production Supabase has `student_daily_challenges` table with RLS enabled; migration history is in sync; DailyChallengeCard renders real data.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.5-phases/12-build-tooling-fixes/12-RESEARCH.md
@supabase/migrations/20260317000001_daily_challenges.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review all local-only migrations for safety before push</name>
  <files>
    supabase/migrations/20260304000001_add_push_subscriptions.sql,
    supabase/migrations/20260305000001_streak_protection.sql,
    supabase/migrations/20260307000001_extend_xp_levels.sql,
    supabase/migrations/20260307000002_add_weekly_report_opt_out.sql,
    supabase/migrations/20260308000001_drop_points_columns.sql,
    supabase/migrations/20260317000001_daily_challenges.sql
  </files>
  <read_first>
    supabase/migrations/20260304000001_add_push_subscriptions.sql,
    supabase/migrations/20260305000001_streak_protection.sql,
    supabase/migrations/20260307000001_extend_xp_levels.sql,
    supabase/migrations/20260307000002_add_weekly_report_opt_out.sql,
    supabase/migrations/20260308000001_drop_points_columns.sql,
    supabase/migrations/20260317000001_daily_challenges.sql
  </read_first>
  <action>
    Read all 6 local-only migration files to determine which are safe to re-apply vs which need to be marked as already-applied. The key concern is that `supabase db push` after repair will attempt to apply ALL 6 local-only migrations, not just daily_challenges.

    Analysis required for each migration:
    1. `20260304000001_add_push_subscriptions.sql` — Uses `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`. SAFE to re-apply (idempotent guards).
    2. `20260305000001_streak_protection.sql` — Uses `ADD COLUMN IF NOT EXISTS`, constraint guarded with `DO $$ IF NOT EXISTS`. SAFE to re-apply.
    3. `20260307000001_extend_xp_levels.sql` — Uses `DROP CONSTRAINT IF EXISTS`, `ADD CONSTRAINT`, `DROP FUNCTION IF EXISTS`, `CREATE OR REPLACE FUNCTION`. SAFE to re-apply (all operations are idempotent or use IF EXISTS guards).
    4. `20260307000002_add_weekly_report_opt_out.sql` — Uses `ADD COLUMN IF NOT EXISTS`. SAFE to re-apply.
    5. `20260308000001_drop_points_columns.sql` — Uses `DROP COLUMN IF EXISTS`, `DROP FUNCTION IF EXISTS`, wrapped in `BEGIN/COMMIT`. SAFE to re-apply.
    6. `20260317000001_daily_challenges.sql` — Uses bare `CREATE TABLE` (NO `IF NOT EXISTS` guard). UNSAFE if table already exists.

    After reading the files, report findings to the user. The conclusion should be:
    - Migrations 1-5 (20260304 through 20260308) are SAFE to re-apply because they use IF EXISTS/IF NOT EXISTS guards throughout.
    - Migration 6 (20260317 daily_challenges) is the ONLY one that lacks guards and needs the pre-check.

    Present this analysis clearly to the user before proceeding to the checkpoint task.
  </action>
  <verify>
    <automated>grep -c "IF NOT EXISTS\|IF EXISTS\|CREATE OR REPLACE\|DROP.*IF EXISTS" supabase/migrations/20260304000001_add_push_subscriptions.sql supabase/migrations/20260305000001_streak_protection.sql supabase/migrations/20260307000001_extend_xp_levels.sql supabase/migrations/20260307000002_add_weekly_report_opt_out.sql supabase/migrations/20260308000001_drop_points_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - All 6 migration files have been read and analyzed
    - Migrations 20260304000001 through 20260308000001 confirmed to have idempotent guards (IF NOT EXISTS / IF EXISTS / CREATE OR REPLACE)
    - Migration 20260317000001_daily_challenges.sql confirmed to lack IF NOT EXISTS guard on CREATE TABLE
    - Analysis summary presented to user
  </acceptance_criteria>
  <done>
    All 6 local-only migration files reviewed. Safety assessment complete: 5 of 6 are idempotent, 1 (daily_challenges) needs pre-check.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Repair migration history and push to production</name>
  <files>supabase/migrations/20260317000001_daily_challenges.sql</files>
  <action>
    CHECKPOINT: Human must execute production database commands. Claude cannot access the production Supabase instance directly.

    The user must perform these steps in order:

    **Step 1 — Pre-check (Supabase SQL Editor):**
    Run this query in Supabase Dashboard -> SQL Editor:
    ```sql
    SELECT to_regclass('public.student_daily_challenges');
    ```
    - If result is `NULL`: Table does not exist, safe to proceed to Step 2.
    - If result is NOT NULL: Table already exists. Skip to Step 4 and use `migration repair --status applied` instead of `db push` for the daily_challenges migration.

    **Step 2 — Repair migration history (terminal):**
    Run this command to mark the 6 remote-only orphaned entries as reverted:
    ```bash
    npx supabase migration repair --status reverted 20260127000004 20260226000001 20260226000002 20260226000003 20260304222919 20260308120037
    ```

    **Step 3 — Dry run to confirm (terminal):**
    ```bash
    npx supabase db push --dry-run
    ```
    Verify output lists the 6 local-only migrations (20260304000001 through 20260317000001) as pending. No errors.

    **Step 4 — Push migrations (terminal):**
    ```bash
    npx supabase db push
    ```
    All 6 migrations should apply successfully. If daily_challenges table already existed (Step 1 returned non-null), instead run:
    ```bash
    npx supabase migration repair --status applied 20260317000001
    ```

    **Step 5 — Verify table and RLS (Supabase SQL Editor):**
    ```sql
    SELECT tablename, rowsecurity FROM pg_tables WHERE tablename = 'student_daily_challenges';
    ```
    Expected: `student_daily_challenges | true`

    **Step 6 — Verify migration sync (terminal):**
    ```bash
    npx supabase migration list
    ```
    Expected: All migrations show timestamps in both LOCAL and REMOTE columns. No orphaned entries.

    **Step 7 — Verify DailyChallengeCard (browser):**
    Open https://testpianomaster.netlify.app in browser, log in as a student, and verify the DailyChallengeCard on the dashboard shows challenge content (not a skeleton/loading state or error).
  </action>
  <verify>
    <automated>npx supabase migration list</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT to_regclass('public.student_daily_challenges')` returns `student_daily_challenges` (not NULL)
    - `SELECT rowsecurity FROM pg_tables WHERE tablename = 'student_daily_challenges'` returns `true`
    - `npx supabase migration list` shows no orphaned remote-only or local-only entries
    - DailyChallengeCard on production dashboard renders challenge content
  </acceptance_criteria>
  <done>
    student_daily_challenges table exists in production with RLS active. Migration history fully synchronized. DailyChallengeCard renders real data on production dashboard.
  </done>
</task>

</tasks>

<verification>
1. `SELECT to_regclass('public.student_daily_challenges')` returns non-null in Supabase SQL Editor
2. `SELECT rowsecurity FROM pg_tables WHERE tablename = 'student_daily_challenges'` returns `true`
3. `npx supabase migration list` shows all migrations in sync (no orphans)
4. DailyChallengeCard renders challenge content on production dashboard
</verification>

<success_criteria>
- The `student_daily_challenges` table exists in production Supabase with RLS confirmed active
- `DailyChallengeCard` on the dashboard returns real challenge data (not empty/error state)
- Supabase migration history is fully synchronized between local and remote
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.5-phases/12-build-tooling-fixes/12-02-SUMMARY.md`
</output>
