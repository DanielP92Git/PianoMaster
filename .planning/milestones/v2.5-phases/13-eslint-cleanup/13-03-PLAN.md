---
phase: 13-eslint-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/components/games/sight-reading-game/SightReadingGame.jsx
  - src/components/games/notes-master-games/MemoryGame.jsx
  - src/features/games/hooks/useGameTimer.js
  - src/components/ui/AudioRecorder.jsx
  - src/components/games/notes-master-games/NotesRecognitionGame.jsx
  - src/components/games/rhythm-games/MetronomeTrainer.jsx
  - src/components/games/shared/UnifiedGameSettings.jsx
  - src/components/pwa/PWAInstallPrompt.jsx
  - src/components/games/rhythm-games/components/RhythmNotationRenderer.jsx
  - src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx
  - src/components/layout/MobileTabsNav.jsx
  - src/components/teacher/AccountDeletionModal.jsx
  - src/components/teacher/DataExportModal.jsx
  - src/components/trail/ZigzagTrailLayout.jsx
  - src/components/ui/PracticeSessionPlayer.jsx
  - src/features/games/hooks/useGameProgress.js
  - src/hooks/useAudioEngine.js
autonomous: true
requirements:
  - LINT-03

must_haves:
  truths:
    - "ESLint reports 0 react-hooks/exhaustive-deps warnings across the entire codebase"
    - "All game components still function correctly — no infinite render loops introduced"
    - "Audio-heavy components (SightReadingGame, NotesRecognitionGame, MetronomeTrainer) have not changed their mount/unmount behavior"
    - "Every suppressed warning has a written rationale explaining WHY the dependency is intentionally omitted"
  artifacts:
    - path: "src/components/games/sight-reading-game/SightReadingGame.jsx"
      provides: "Fixed or suppressed 9 exhaustive-deps warnings"
    - path: "src/components/games/notes-master-games/MemoryGame.jsx"
      provides: "Fixed or suppressed 5 exhaustive-deps warnings"
    - path: "src/features/games/hooks/useGameTimer.js"
      provides: "Fixed or suppressed 5 exhaustive-deps warnings"
  key_links:
    - from: "game component useEffect hooks"
      to: "game state and audio refs"
      via: "dependency arrays"
      pattern: "eslint-disable-next-line react-hooks/exhaustive-deps"
---

<objective>
Resolve all 43 `react-hooks/exhaustive-deps` warnings across 17 files by either adding the correct missing dependencies or suppressing with written justification where adding deps would cause infinite loops or break intentional one-time-mount behavior.

Purpose: These are the highest-risk warnings. Incorrect dependency arrays can cause subtle bugs (stale closures) or catastrophic ones (infinite render loops). Each warning must be individually analyzed against the component's behavior before deciding fix vs. suppress.

Output: 17 files with 0 remaining exhaustive-deps warnings; every suppression has a written rationale.
</objective>

<execution_context>
@C:/Users/pagis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pagis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v2.5-phases/13-eslint-cleanup/13-CONTEXT.md
@.planning/milestones/v2.5-phases/13-eslint-cleanup/13-01-SUMMARY.md
@.planning/milestones/v2.5-phases/13-eslint-cleanup/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix exhaustive-deps in simple hooks and non-game components (8 files, 12 warnings)</name>
  <files>
    src/features/games/hooks/useGameTimer.js
    src/components/pwa/PWAInstallPrompt.jsx
    src/components/games/rhythm-games/components/RhythmNotationRenderer.jsx
    src/components/layout/MobileTabsNav.jsx
    src/components/teacher/AccountDeletionModal.jsx
    src/components/teacher/DataExportModal.jsx
    src/components/trail/ZigzagTrailLayout.jsx
    src/features/games/hooks/useGameProgress.js
  </files>
  <read_first>
    - src/features/games/hooks/useGameTimer.js
    - src/components/pwa/PWAInstallPrompt.jsx
    - src/components/games/rhythm-games/components/RhythmNotationRenderer.jsx
    - src/components/layout/MobileTabsNav.jsx
    - src/components/teacher/AccountDeletionModal.jsx
    - src/components/teacher/DataExportModal.jsx
    - src/components/trail/ZigzagTrailLayout.jsx
    - src/features/games/hooks/useGameProgress.js
  </read_first>
  <action>
Process each file individually. For EACH warning, read the hook/effect in context and determine the correct fix.

**Decision framework for each warning:**

A. **Add the missing dependency** if:
   - The dependency is a stable reference (state setter, dispatch, ref, queryClient)
   - The dependency is a prop that SHOULD trigger re-execution when it changes
   - Adding it won't cause an infinite loop (the effect doesn't modify the dependency)

B. **Wrap in useCallback/useMemo** if:
   - The missing dependency is a function defined in the component body
   - Wrapping it makes the reference stable so adding it as a dep is safe

C. **Move the function inside the hook** if:
   - The missing dependency is a helper function only used within that one hook
   - Moving it inside removes the need to list it as a dependency

D. **Suppress with justification** if:
   - The effect is intentionally a one-time mount effect (empty deps `[]`) and adding deps would cause it to re-run incorrectly
   - The dependency is an unstable reference (e.g., a new object/array on every render) that would cause infinite loops
   - The function uses a ref that should NOT trigger re-render

**File-specific guidance:**

1. **useGameTimer.js** (5 warnings, all about `debugLog`):
   - All 5 useCallback hooks at lines 56, 106, 114, 155, 174 are missing `debugLog`
   - `debugLog` is likely defined in the component body. Check if it can be wrapped in `useCallback` or moved inside each callback, or if it's a stable function (e.g., from a ref). If `debugLog` is a simple console wrapper with no deps of its own, wrap it in `useCallback` with empty deps at the top of the hook, then add it to all 5 dependency arrays.
   - If `debugLog` depends on component state, suppress all 5 with: `-- debugLog is a stable logging utility; including it would cause unnecessary re-renders`

2. **PWAInstallPrompt.jsx** (2 warnings, both about `shouldSuppressPrompt` at lines 76, 123):
   - Check if `shouldSuppressPrompt` is a function or computed value. If it's a function, wrap in `useCallback`. If it's a simple boolean computed from state/props, add it to the dependency array.

3. **RhythmNotationRenderer.jsx** (1 warning, `drawNotation` at line 38):
   - `drawNotation` is likely a rendering function. Move it inside the `useEffect` if it only depends on the effect's existing deps, otherwise wrap in `useCallback` with the correct deps.

4. **MobileTabsNav.jsx** (1 warning, `tabIds` conditional at line 17):
   - The warning says "conditional could make dependencies change on every render." Move the `tabIds` calculation inside the `useMemo` callback.

5. **AccountDeletionModal.jsx** (1 warning, `loadDeletionStatus` at line 26):
   - Move `loadDeletionStatus` inside the `useEffect`, or wrap in `useCallback` and add to deps.

6. **DataExportModal.jsx** (1 warning, `loadDataSummary` at line 18):
   - Same pattern as AccountDeletionModal — move inside `useEffect` or wrap in `useCallback`.

7. **ZigzagTrailLayout.jsx** (1 warning, `isRTL` at line 138):
   - `isRTL` is likely a value from i18n context. Add it to the `useMemo` dependency array — RTL should trigger recalculation when language changes.

8. **useGameProgress.js** (1 warning, unnecessary dependency `queryClient` at line 126):
   - ESLint says `queryClient` is an unnecessary dependency. Remove it from the dependency array.

**After fixing all 8 files, run tests:**
```bash
npm run test:run
```
If any test fails, revert the last change and suppress instead.
  </action>
  <verify>
    <automated>cd C:/Development/PianoApp2 && npx eslint src/features/games/hooks/useGameTimer.js src/components/pwa/PWAInstallPrompt.jsx src/components/games/rhythm-games/components/RhythmNotationRenderer.jsx src/components/layout/MobileTabsNav.jsx src/components/teacher/AccountDeletionModal.jsx src/components/teacher/DataExportModal.jsx src/components/trail/ZigzagTrailLayout.jsx src/features/games/hooks/useGameProgress.js 2>&1 | grep -c "exhaustive-deps" | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS"; else echo "FAIL: {} remaining"; fi' && npm run test:run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint` on all 8 files returns 0 `exhaustive-deps` warnings
    - `npm run test:run` passes with exit code 0
    - Every suppression (if any) uses `eslint-disable-next-line react-hooks/exhaustive-deps -- <written reason>`
    - No block-level `/* eslint-disable */` used
  </acceptance_criteria>
  <done>All 12 exhaustive-deps warnings in non-game utility hooks and simple components resolved. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix exhaustive-deps in game components and audio hooks (9 files, 31 warnings) + final verification</name>
  <files>
    src/components/games/sight-reading-game/SightReadingGame.jsx
    src/components/games/notes-master-games/MemoryGame.jsx
    src/components/ui/AudioRecorder.jsx
    src/components/games/notes-master-games/NotesRecognitionGame.jsx
    src/components/games/rhythm-games/MetronomeTrainer.jsx
    src/components/games/shared/UnifiedGameSettings.jsx
    src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx
    src/components/ui/PracticeSessionPlayer.jsx
    src/hooks/useAudioEngine.js
  </files>
  <read_first>
    - src/components/games/sight-reading-game/SightReadingGame.jsx
    - src/components/games/notes-master-games/MemoryGame.jsx
    - src/components/games/notes-master-games/NotesRecognitionGame.jsx
    - src/components/games/rhythm-games/MetronomeTrainer.jsx
    - src/components/games/shared/UnifiedGameSettings.jsx
    - src/components/games/sight-reading-game/components/VexFlowStaffDisplay.jsx
    - src/components/ui/AudioRecorder.jsx
    - src/components/ui/PracticeSessionPlayer.jsx
    - src/hooks/useAudioEngine.js
  </read_first>
  <action>
**CRITICAL: These are audio-heavy game components. The STATE.md explicitly warns: "Process react-hooks/exhaustive-deps warnings last and one file at a time — audio-heavy game components (SightReadingGame, NotesRecognitionGame) have intentional dep omissions; bulk fix risks infinite render loops."**

Process ONE FILE AT A TIME. After each file, run `npm run test:run` to verify no regressions. If tests fail, revert and suppress instead.

**File-specific guidance:**

**SightReadingGame.jsx** (9 warnings — largest file, most complex):
- L319: useEffect missing `audioContextRef`, `startGame`, `trailEnableFlats`, `trailEnableSharps` — This is the auto-start effect. These deps are intentionally omitted because the effect should only run once on mount (controlled by `hasAutoStartedRef`). **SUPPRESS** with: `-- one-time auto-start effect; deps intentionally omitted to prevent re-triggering; guarded by hasAutoStartedRef`
- L627, L891, L2105: useCallback missing `audioEngine` — `audioEngine` is likely a ref or context value that's stable. Check if it's from `useAudioEngine()`. If it's a stable ref, **SUPPRESS** with: `-- audioEngine ref is stable across renders; including would cause unnecessary invalidation`
- L1419: useEffect missing `queryClient` — `queryClient` is stable (singleton from React Query). **ADD** it to the dependency array — it won't cause re-renders.
- L1867: useCallback missing `audioEngine`, `inputMode`, `shortestNoteDurationMsRef` — Check each. If `inputMode` changes should indeed re-create the callback, add it. If `audioEngine` and the ref are stable, suppress with specific reason.
- L2720: useCallback missing `pauseTimer` — `pauseTimer` from SessionTimeout context may be stable. If wrapped in useCallback by the provider, add it. Otherwise suppress.
- L3018: useCallback missing `inputMode`, `setTimingStateSync` — `inputMode` likely should be included (affects behavior). `setTimingStateSync` if it's a state setter, it's stable — add it.
- L3116: useCallback missing `trailKeySignature` — This is a prop. If the callback should update when the key signature changes, add it.

**MemoryGame.jsx** (5 warnings, all about `createCards` or `applySettingsAndRestart`):
- `createCards` is likely defined in the component body and depends on state. Check if it can be wrapped in `useCallback`. If wrapping it would require listing many state deps that change frequently, **SUPPRESS** with: `-- createCards depends on game state that would cause infinite re-creation; game restart is explicitly triggered by user action`
- L267 useMemo, L523 useCallback, L794/L804 useEffect: All need `createCards`. If `createCards` can be made stable via `useCallback`, do so and add it. If not, suppress each individually.
- L333: `applySettingsAndRestart` — same analysis as `createCards`.

**NotesRecognitionGame.jsx** (3 warnings):
- L562: useEffect missing `audioContextProviderRef`, `startGame`, `trailEnableFlats`, `trailEnableSharps` — Same auto-start pattern as SightReadingGame. **SUPPRESS** with: `-- one-time auto-start effect; deps intentionally omitted to prevent re-triggering; guarded by hasAutoStartedRef`
- L585: useEffect missing `startGame`, `updateProgress`, `updateSettings` — Check if this is a settings sync effect. If one-time, suppress. If it should re-run, add deps.
- L1071: "The 'startGame' function makes dependencies change on every render" — Wrap `startGame` in `useCallback` with the correct deps, OR suppress with rationale.

**MetronomeTrainer.jsx** (2 warnings):
- L225: useEffect missing `audioContextRef`, `getTimeSignatureObject`, `startGame` — Auto-start pattern. **SUPPRESS** with same rationale as SightReadingGame.
- L1002: useCallback has unnecessary dependency `audioEngine` — **REMOVE** `audioEngine` from the dependency array.

**UnifiedGameSettings.jsx** (2 warnings):
- L833: `getNoteId` makes dependencies change on every render — Move `getNoteId` inside the useCallback at line 853, or wrap it in its own useCallback.
- L1390: useEffect missing `getNoteId` — If `getNoteId` is made stable (above), add it. Otherwise suppress.

**VexFlowStaffDisplay.jsx** (1 warning):
- L1509: useCallback has unnecessary dependency `currentNoteIndex` — **REMOVE** `currentNoteIndex` from the dependency array.

**AudioRecorder.jsx** (4 warnings):
- L119: missing `drawVisualization` — Move `drawVisualization` inside the callback, or wrap in useCallback.
- L280: missing `cleanup`, `stopRecording` — These should be stable if wrapped in useCallback. Check and fix or suppress.
- L327: missing `stopRecording` — Same as above.
- L348: missing `cleanup` — Same as above.

**PracticeSessionPlayer.jsx** (1 warning):
- L128: missing `audioUrl`, `getAudioUrl` — If these are props/state that should trigger re-fetch, add them. If the effect should only run once, suppress.

**useAudioEngine.js** (1 warning):
- L108: missing `loadPianoSoundAsync`, `loadTapSoundAsync` — If these are defined in the hook body, move them inside the useCallback or wrap in their own useCallback. If they're expensive async operations that should only run once, suppress.

**After ALL files are processed, run the full verification:**
```bash
npm run lint
npm run test:run
npm run build
```

All three must pass with 0 warnings and 0 errors.
  </action>
  <verify>
    <automated>cd C:/Development/PianoApp2 && npx eslint . 2>&1 | tail -3 && npm run test:run 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` reports "0 problems" (0 errors, 0 warnings)
    - `npm run test:run` exits 0 with all tests passing
    - `npm run build` exits 0 with no errors
    - Every `eslint-disable-next-line react-hooks/exhaustive-deps` comment has a `--` followed by a specific written rationale (not generic "intentional")
    - No block-level `/* eslint-disable */` used anywhere
    - `grep -r "eslint-disable-next-line react-hooks/exhaustive-deps" src/ | wc -l` returns the count of suppressed warnings (document this count in the SUMMARY)
  </acceptance_criteria>
  <done>All 43 react-hooks/exhaustive-deps warnings resolved (mix of genuine fixes and justified suppressions). `npm run lint` reports 0 warnings, `npm run test:run` passes, `npm run build` passes. The entire ESLint cleanup phase is complete.</done>
</task>

</tasks>

<verification>
Final phase-level verification after all 3 plans are complete:
1. `npm run lint` reports exactly: "0 problems (0 errors, 0 warnings)"
2. `npm run test:run` passes with exit code 0
3. `npm run build` passes with exit code 0
4. `grep -r "eslint-disable" src/ public/` — every suppression has a written rationale after `--`
5. No block-level `/* eslint-disable */` exists anywhere
</verification>

<success_criteria>
- ESLint reports 0 warnings and 0 errors across the entire codebase
- All existing tests pass with no regressions
- Production build succeeds
- Every intentional suppression has a line-level `eslint-disable-next-line` with a written rationale
- Game components (SightReadingGame, NotesRecognitionGame, MetronomeTrainer, MemoryGame) have not changed their mount/unmount or audio behavior
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.5-phases/13-eslint-cleanup/13-03-SUMMARY.md`
</output>
